import {
  Fragment,
  computed,
  createBaseVNode,
  createBlock,
  createCommentVNode,
  createElementBlock,
  defineAsyncComponent,
  h,
  inject,
  onMounted,
  onUnmounted,
  openBlock,
  provide,
  reactive,
  ref,
  unref,
  vShow,
  withDirectives
} from "./chunk-YRNSJHMG.js";

// node_modules/openlayers2dsceneeditor/index-15ba76cb.js
var Su = Object.defineProperty;
var Eu = (i, e, t) => e in i ? Su(i, e, { enumerable: true, configurable: true, writable: true, value: t }) : i[e] = t;
var Dr = (i, e, t) => (Eu(i, typeof e != "symbol" ? e + "" : e, t), t);
var Du = ({ bridge: i = null, mapApp: e, options: t = {} } = {}) => {
  if (!e)
    throw new Error("createMapContext requires a mapApp instance.");
  return {
    bridge: i,
    mapApp: e,
    options: t,
    get map() {
      return e.map;
    },
    get layer() {
      return e.layer;
    },
    get popup() {
      return e.popup;
    },
    get iconManager() {
      return e.iconManager;
    },
    get effectManager() {
      return e.effectManager;
    },
    get events() {
      return e.events;
    }
  };
};
var Vs = (i) => {
  const e = Number(i) || 0;
  return Math.abs(e) > Math.PI * 2 ? e * Math.PI / 180 : e;
};
var Ka = (i) => {
  const e = Number(i) % 360;
  return e >= 0 ? e : e + 360;
};
var Pu = (i) => {
  let e = Number(i) || 0;
  for (; e > Math.PI; )
    e -= Math.PI * 2;
  for (; e < -Math.PI; )
    e += Math.PI * 2;
  return e;
};
var Au = (i) => {
  var n, s;
  const e = (i == null ? void 0 : i.status) ?? ((n = i == null ? void 0 : i.get) == null ? void 0 : n.call(i, "status")), t = (i == null ? void 0 : i.visible) ?? ((s = i == null ? void 0 : i.get) == null ? void 0 : s.call(i, "visible"));
  return e === "hidden" || t === false ? 0 : 0.5;
};
var ku = class {
  constructor({
    getIconManager: e,
    getDisplayConfig: t,
    updateHeadingIndicator: n,
    refreshTargetIndicatorsFromFeatures: s,
    refreshSituationEffectsFromFeatures: r,
    syncPopupForFeature: o
  }) {
    this.getIconManager = e, this.getDisplayConfig = t, this.updateHeadingIndicator = n, this.refreshTargetIndicatorsFromFeatures = s, this.refreshSituationEffectsFromFeatures = r, this.syncPopupForFeature = o;
  }
  getFeatureScale(e) {
    return Au(e);
  }
  update(e, { coord: t, lon: n, lat: s, heading: r }) {
    var a, l, c, h2;
    if (!e)
      return;
    (l = (a = this.getIconManager()) == null ? void 0 : a.updateIcon) == null || l.call(a, e, {
      lon: n,
      lat: s,
      rotation: Vs(r),
      scale: this.getFeatureScale(e)
    }), t && ((h2 = (c = e.getGeometry()) == null ? void 0 : c.setCoordinates) == null || h2.call(c, t)), e.set("lon", n), e.set("lat", s), e.set("heading", r);
    const o = String(e.get("UID"));
    this.updateHeadingIndicator({
      UID: e.get("UID"),
      nation: e.get("nation"),
      status: e.get("status"),
      lon: n,
      lat: s,
      heading: r
    }, this.getDisplayConfig()), this.refreshTargetIndicatorsFromFeatures(o), this.refreshSituationEffectsFromFeatures(o), this.syncPopupForFeature(e);
  }
};
var mi = {
  /**
   * Triggered when a property is changed.
   * @event module:ol/Object.ObjectEvent#propertychange
   * @api
   */
  PROPERTYCHANGE: "propertychange"
};
var Z = {
  /**
   * Generic change event. Triggered when the revision counter is increased.
   * @event module:ol/events/Event~BaseEvent#change
   * @api
   */
  CHANGE: "change",
  /**
   * Generic error event. Triggered when an error occurs.
   * @event module:ol/events/Event~BaseEvent#error
   * @api
   */
  ERROR: "error",
  BLUR: "blur",
  CLEAR: "clear",
  CONTEXTMENU: "contextmenu",
  CLICK: "click",
  DBLCLICK: "dblclick",
  DRAGENTER: "dragenter",
  DRAGOVER: "dragover",
  DROP: "drop",
  FOCUS: "focus",
  KEYDOWN: "keydown",
  KEYPRESS: "keypress",
  LOAD: "load",
  RESIZE: "resize",
  TOUCHMOVE: "touchmove",
  WHEEL: "wheel"
};
var Ou = class {
  constructor() {
    this.disposed = false;
  }
  /**
   * Clean up.
   */
  dispose() {
    this.disposed || (this.disposed = true, this.disposeInternal());
  }
  /**
   * Extension point for disposable objects.
   * @protected
   */
  disposeInternal() {
  }
};
var gr = Ou;
function Nu(i, e, t) {
  let n, s;
  t = t || Yt;
  let r = 0, o = i.length, a = false;
  for (; r < o; )
    n = r + (o - r >> 1), s = +t(i[n], e), s < 0 ? r = n + 1 : (o = n, a = !s);
  return a ? r : ~r;
}
function Yt(i, e) {
  return i > e ? 1 : i < e ? -1 : 0;
}
function Uu(i, e) {
  return i < e ? 1 : i > e ? -1 : 0;
}
function Go(i, e, t) {
  if (i[0] <= e)
    return 0;
  const n = i.length;
  if (e <= i[n - 1])
    return n - 1;
  if (typeof t == "function") {
    for (let s = 1; s < n; ++s) {
      const r = i[s];
      if (r === e)
        return s;
      if (r < e)
        return t(e, i[s - 1], r) > 0 ? s - 1 : s;
    }
    return n - 1;
  }
  if (t > 0) {
    for (let s = 1; s < n; ++s)
      if (i[s] < e)
        return s - 1;
    return n - 1;
  }
  if (t < 0) {
    for (let s = 1; s < n; ++s)
      if (i[s] <= e)
        return s;
    return n - 1;
  }
  for (let s = 1; s < n; ++s) {
    if (i[s] == e)
      return s;
    if (i[s] < e)
      return i[s - 1] - e < e - i[s] ? s - 1 : s;
  }
  return n - 1;
}
function Gu(i, e, t) {
  for (; e < t; ) {
    const n = i[e];
    i[e] = i[t], i[t] = n, ++e, --t;
  }
}
function At(i, e) {
  const t = Array.isArray(e) ? e : [e], n = t.length;
  for (let s = 0; s < n; s++)
    i[i.length] = t[s];
}
function Sn(i, e) {
  const t = i.length;
  if (t !== e.length)
    return false;
  for (let n = 0; n < t; n++)
    if (i[n] !== e[n])
      return false;
  return true;
}
function Bu(i, e, t) {
  const n = e || Yt;
  return i.every(function(s, r) {
    if (r === 0)
      return true;
    const o = n(i[r - 1], s);
    return !(o > 0 || t && o === 0);
  });
}
function _n() {
  return true;
}
function vi() {
  return false;
}
function Ki() {
}
function hc(i) {
  let e, t, n;
  return function() {
    const s = Array.prototype.slice.call(arguments);
    return (!t || this !== n || !Sn(s, t)) && (n = this, t = s, e = i.apply(this, arguments)), e;
  };
}
function zu(i) {
  function e() {
    let t;
    try {
      t = i();
    } catch (n) {
      return Promise.reject(n);
    }
    return t instanceof Promise ? t : Promise.resolve(t);
  }
  return e();
}
function os(i) {
  for (const e in i)
    delete i[e];
}
function yi(i) {
  let e;
  for (e in i)
    return false;
  return !e;
}
var Vu = class {
  /**
   * @param {string} type Type.
   */
  constructor(e) {
    this.propagationStopped, this.defaultPrevented, this.type = e, this.target = null;
  }
  /**
   * Prevent default. This means that no emulated `click`, `singleclick` or `doubleclick` events
   * will be fired.
   * @api
   */
  preventDefault() {
    this.defaultPrevented = true;
  }
  /**
   * Stop event propagation.
   * @api
   */
  stopPropagation() {
    this.propagationStopped = true;
  }
};
var It = Vu;
var Xu = class extends gr {
  /**
   * @param {*} [target] Default event target for dispatched events.
   */
  constructor(e) {
    super(), this.eventTarget_ = e, this.pendingRemovals_ = null, this.dispatching_ = null, this.listeners_ = null;
  }
  /**
   * @param {string} type Type.
   * @param {import("../events.js").Listener} listener Listener.
   */
  addEventListener(e, t) {
    if (!e || !t)
      return;
    const n = this.listeners_ || (this.listeners_ = {}), s = n[e] || (n[e] = []);
    s.includes(t) || s.push(t);
  }
  /**
   * Dispatches an event and calls all listeners listening for events
   * of this type. The event parameter can either be a string or an
   * Object with a `type` property.
   *
   * @param {import("./Event.js").default|string} event Event object.
   * @return {boolean|undefined} `false` if anyone called preventDefault on the
   *     event object or if any of the listeners returned false.
   * @api
   */
  dispatchEvent(e) {
    const t = typeof e == "string", n = t ? e : e.type, s = this.listeners_ && this.listeners_[n];
    if (!s)
      return;
    const r = t ? new It(e) : (
      /** @type {Event} */
      e
    );
    r.target || (r.target = this.eventTarget_ || this);
    const o = this.dispatching_ || (this.dispatching_ = {}), a = this.pendingRemovals_ || (this.pendingRemovals_ = {});
    n in o || (o[n] = 0, a[n] = 0), ++o[n];
    let l;
    for (let c = 0, h2 = s.length; c < h2; ++c)
      if ("handleEvent" in s[c] ? l = /** @type {import("../events.js").ListenerObject} */
      s[c].handleEvent(r) : l = /** @type {import("../events.js").ListenerFunction} */
      s[c].call(this, r), l === false || r.propagationStopped) {
        l = false;
        break;
      }
    if (--o[n] === 0) {
      let c = a[n];
      for (delete a[n]; c--; )
        this.removeEventListener(n, Ki);
      delete o[n];
    }
    return l;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.listeners_ && os(this.listeners_);
  }
  /**
   * Get the listeners for a specified event type. Listeners are returned in the
   * order that they will be called in.
   *
   * @param {string} type Type.
   * @return {Array<import("../events.js").Listener>|undefined} Listeners.
   */
  getListeners(e) {
    return this.listeners_ && this.listeners_[e] || void 0;
  }
  /**
   * @param {string} [type] Type. If not provided,
   *     `true` will be returned if this event target has any listeners.
   * @return {boolean} Has listeners.
   */
  hasListener(e) {
    return this.listeners_ ? e ? e in this.listeners_ : Object.keys(this.listeners_).length > 0 : false;
  }
  /**
   * @param {string} type Type.
   * @param {import("../events.js").Listener} listener Listener.
   */
  removeEventListener(e, t) {
    if (!this.listeners_)
      return;
    const n = this.listeners_[e];
    if (!n)
      return;
    const s = n.indexOf(t);
    s !== -1 && (this.pendingRemovals_ && e in this.pendingRemovals_ ? (n[s] = Ki, ++this.pendingRemovals_[e]) : (n.splice(s, 1), n.length === 0 && delete this.listeners_[e]));
  }
};
var mr = Xu;
function le(i, e, t, n, s) {
  if (s) {
    const o = t;
    t = function(a) {
      return i.removeEventListener(e, t), o.call(n ?? this, a);
    };
  } else
    n && n !== i && (t = t.bind(n));
  const r = {
    target: i,
    type: e,
    listener: t
  };
  return i.addEventListener(e, t), r;
}
function Xs(i, e, t, n) {
  return le(i, e, t, n, true);
}
function Ee(i) {
  i && i.target && (i.target.removeEventListener(i.type, i.listener), os(i));
}
var as = class extends mr {
  constructor() {
    super(), this.on = /** @type {ObservableOnSignature<import("./events").EventsKey>} */
    this.onInternal, this.once = /** @type {ObservableOnSignature<import("./events").EventsKey>} */
    this.onceInternal, this.un = /** @type {ObservableOnSignature<void>} */
    this.unInternal, this.revision_ = 0;
  }
  /**
   * Increases the revision counter and dispatches a 'change' event.
   * @api
   */
  changed() {
    ++this.revision_, this.dispatchEvent(Z.CHANGE);
  }
  /**
   * Get the version number for this object.  Each time the object is modified,
   * its version number will be incremented.
   * @return {number} Revision.
   * @api
   */
  getRevision() {
    return this.revision_;
  }
  /**
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @return {import("./events.js").EventsKey|Array<import("./events.js").EventsKey>} Event key.
   * @protected
   */
  onInternal(e, t) {
    if (Array.isArray(e)) {
      const n = e.length, s = new Array(n);
      for (let r = 0; r < n; ++r)
        s[r] = le(this, e[r], t);
      return s;
    }
    return le(
      this,
      /** @type {string} */
      e,
      t
    );
  }
  /**
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @return {import("./events.js").EventsKey|Array<import("./events.js").EventsKey>} Event key.
   * @protected
   */
  onceInternal(e, t) {
    let n;
    if (Array.isArray(e)) {
      const s = e.length;
      n = new Array(s);
      for (let r = 0; r < s; ++r)
        n[r] = Xs(this, e[r], t);
    } else
      n = Xs(
        this,
        /** @type {string} */
        e,
        t
      );
    return t.ol_key = n, n;
  }
  /**
   * Unlisten for a certain type of event.
   * @param {string|Array<string>} type Type.
   * @param {function((Event|import("./events/Event").default)): ?} listener Listener.
   * @protected
   */
  unInternal(e, t) {
    const n = (
      /** @type {Object} */
      t.ol_key
    );
    if (n)
      uc(n);
    else if (Array.isArray(e))
      for (let s = 0, r = e.length; s < r; ++s)
        this.removeEventListener(e[s], t);
    else
      this.removeEventListener(e, t);
  }
};
as.prototype.on;
as.prototype.once;
as.prototype.un;
function uc(i) {
  if (Array.isArray(i))
    for (let e = 0, t = i.length; e < t; ++e)
      Ee(i[e]);
  else
    Ee(
      /** @type {import("./events.js").EventsKey} */
      i
    );
}
function ue() {
  throw new Error("Unimplemented abstract method.");
}
var Wu = 0;
function de(i) {
  return i.ol_uid || (i.ol_uid = String(++Wu));
}
var Ha = class extends It {
  /**
   * @param {string} type The event type.
   * @param {string} key The property name.
   * @param {*} oldValue The old value for `key`.
   */
  constructor(e, t, n) {
    super(e), this.key = t, this.oldValue = n;
  }
};
var Yu = class extends as {
  /**
   * @param {Object<string, *>} [values] An object with key-value pairs.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, de(this), this.values_ = null, e !== void 0 && this.setProperties(e);
  }
  /**
   * Gets a value.
   * @param {string} key Key name.
   * @return {*} Value.
   * @api
   */
  get(e) {
    let t;
    return this.values_ && this.values_.hasOwnProperty(e) && (t = this.values_[e]), t;
  }
  /**
   * Get a list of object property names.
   * @return {Array<string>} List of property names.
   * @api
   */
  getKeys() {
    return this.values_ && Object.keys(this.values_) || [];
  }
  /**
   * Get an object of all property names and values.
   * @return {Object<string, *>} Object.
   * @api
   */
  getProperties() {
    return this.values_ && Object.assign({}, this.values_) || {};
  }
  /**
   * Get an object of all property names and values.
   * @return {Object<string, *>?} Object.
   */
  getPropertiesInternal() {
    return this.values_;
  }
  /**
   * @return {boolean} The object has properties.
   */
  hasProperties() {
    return !!this.values_;
  }
  /**
   * @param {string} key Key name.
   * @param {*} oldValue Old value.
   */
  notify(e, t) {
    let n;
    n = `change:${e}`, this.hasListener(n) && this.dispatchEvent(new Ha(n, e, t)), n = mi.PROPERTYCHANGE, this.hasListener(n) && this.dispatchEvent(new Ha(n, e, t));
  }
  /**
   * @param {string} key Key name.
   * @param {import("./events.js").Listener} listener Listener.
   */
  addChangeListener(e, t) {
    this.addEventListener(`change:${e}`, t);
  }
  /**
   * @param {string} key Key name.
   * @param {import("./events.js").Listener} listener Listener.
   */
  removeChangeListener(e, t) {
    this.removeEventListener(`change:${e}`, t);
  }
  /**
   * Sets a value.
   * @param {string} key Key name.
   * @param {*} value Value.
   * @param {boolean} [silent] Update without triggering an event.
   * @api
   */
  set(e, t, n) {
    const s = this.values_ || (this.values_ = {});
    if (n)
      s[e] = t;
    else {
      const r = s[e];
      s[e] = t, r !== t && this.notify(e, r);
    }
  }
  /**
   * Sets a collection of key-value pairs.  Note that this changes any existing
   * properties and adds new ones (it does not remove any existing properties).
   * @param {Object<string, *>} values Values.
   * @param {boolean} [silent] Update without triggering an event.
   * @api
   */
  setProperties(e, t) {
    for (const n in e)
      this.set(n, e[n], t);
  }
  /**
   * Apply any properties from another object without triggering events.
   * @param {BaseObject} source The source object.
   * @protected
   */
  applyProperties(e) {
    e.values_ && Object.assign(this.values_ || (this.values_ = {}), e.values_);
  }
  /**
   * Unsets a property.
   * @param {string} key Key name.
   * @param {boolean} [silent] Unset without triggering an event.
   * @api
   */
  unset(e, t) {
    if (this.values_ && e in this.values_) {
      const n = this.values_[e];
      delete this.values_[e], yi(this.values_) && (this.values_ = null), t || this.notify(e, n);
    }
  }
};
var vt = Yu;
function _e(i, e) {
  if (!i)
    throw new Error(e);
}
var Bo = class _Bo extends vt {
  /**
   * @param {Geometry|ObjectWithGeometry<Geometry>} [geometryOrProperties]
   *     You may pass a Geometry object directly, or an object literal containing
   *     properties. If you pass an object literal, you may include a Geometry
   *     associated with a `geometry` key.
   */
  constructor(e) {
    if (super(), this.on, this.once, this.un, this.id_ = void 0, this.geometryName_ = "geometry", this.style_ = null, this.styleFunction_ = void 0, this.geometryChangeKey_ = null, this.addChangeListener(this.geometryName_, this.handleGeometryChanged_), e)
      if (typeof /** @type {?} */
      e.getSimplifiedGeometry == "function") {
        const t = (
          /** @type {Geometry} */
          e
        );
        this.setGeometry(t);
      } else {
        const t = e;
        this.setProperties(t);
      }
  }
  /**
   * Clone this feature. If the original feature has a geometry it
   * is also cloned. The feature id is not set in the clone.
   * @return {Feature<Geometry>} The clone.
   * @api
   */
  clone() {
    const e = (
      /** @type {Feature<Geometry>} */
      new _Bo(this.hasProperties() ? this.getProperties() : null)
    );
    e.setGeometryName(this.getGeometryName());
    const t = this.getGeometry();
    t && e.setGeometry(
      /** @type {Geometry} */
      t.clone()
    );
    const n = this.getStyle();
    return n && e.setStyle(n), e;
  }
  /**
   * Get the feature's default geometry.  A feature may have any number of named
   * geometries.  The "default" geometry (the one that is rendered by default) is
   * set when calling {@link module:ol/Feature~Feature#setGeometry}.
   * @return {Geometry|undefined} The default geometry for the feature.
   * @api
   * @observable
   */
  getGeometry() {
    return (
      /** @type {Geometry|undefined} */
      this.get(this.geometryName_)
    );
  }
  /**
   * Get the feature identifier.  This is a stable identifier for the feature and
   * is either set when reading data from a remote source or set explicitly by
   * calling {@link module:ol/Feature~Feature#setId}.
   * @return {number|string|undefined} Id.
   * @api
   */
  getId() {
    return this.id_;
  }
  /**
   * Get the name of the feature's default geometry.  By default, the default
   * geometry is named `geometry`.
   * @return {string} Get the property name associated with the default geometry
   *     for this feature.
   * @api
   */
  getGeometryName() {
    return this.geometryName_;
  }
  /**
   * Get the feature's style. Will return what was provided to the
   * {@link module:ol/Feature~Feature#setStyle} method.
   * @return {import("./style/Style.js").StyleLike|undefined} The feature style.
   * @api
   */
  getStyle() {
    return this.style_;
  }
  /**
   * Get the feature's style function.
   * @return {import("./style/Style.js").StyleFunction|undefined} Return a function
   * representing the current style of this feature.
   * @api
   */
  getStyleFunction() {
    return this.styleFunction_;
  }
  /**
   * @private
   */
  handleGeometryChange_() {
    this.changed();
  }
  /**
   * @private
   */
  handleGeometryChanged_() {
    this.geometryChangeKey_ && (Ee(this.geometryChangeKey_), this.geometryChangeKey_ = null);
    const e = this.getGeometry();
    e && (this.geometryChangeKey_ = le(
      e,
      Z.CHANGE,
      this.handleGeometryChange_,
      this
    )), this.changed();
  }
  /**
   * Set the default geometry for the feature.  This will update the property
   * with the name returned by {@link module:ol/Feature~Feature#getGeometryName}.
   * @param {Geometry|undefined} geometry The new geometry.
   * @api
   * @observable
   */
  setGeometry(e) {
    this.set(this.geometryName_, e);
  }
  /**
   * Set the style for the feature to override the layer style.  This can be a
   * single style object, an array of styles, or a function that takes a
   * resolution and returns an array of styles. To unset the feature style, call
   * `setStyle()` without arguments or a falsey value.
   * @param {import("./style/Style.js").StyleLike} [style] Style for this feature.
   * @api
   * @fires module:ol/events/Event~BaseEvent#event:change
   */
  setStyle(e) {
    this.style_ = e, this.styleFunction_ = e ? ju(e) : void 0, this.changed();
  }
  /**
   * Set the feature id.  The feature id is considered stable and may be used when
   * requesting features or comparing identifiers returned from a remote source.
   * The feature id can be used with the
   * {@link module:ol/source/Vector~VectorSource#getFeatureById} method.
   * @param {number|string|undefined} id The feature id.
   * @api
   * @fires module:ol/events/Event~BaseEvent#event:change
   */
  setId(e) {
    this.id_ = e, this.changed();
  }
  /**
   * Set the property name to be used when getting the feature's default geometry.
   * When calling {@link module:ol/Feature~Feature#getGeometry}, the value of the property with
   * this name will be returned.
   * @param {string} name The property name of the default geometry.
   * @api
   */
  setGeometryName(e) {
    this.removeChangeListener(this.geometryName_, this.handleGeometryChanged_), this.geometryName_ = e, this.addChangeListener(this.geometryName_, this.handleGeometryChanged_), this.handleGeometryChanged_();
  }
};
function ju(i) {
  if (typeof i == "function")
    return i;
  let e;
  return Array.isArray(i) ? e = i : (_e(
    typeof /** @type {?} */
    i.getZIndex == "function",
    "Expected an `ol/style/Style` or an array of `ol/style/Style.js`"
  ), e = [
    /** @type {import("./style/Style.js").default} */
    i
  ]), function() {
    return e;
  };
}
var ae = Bo;
var De = {
  UNKNOWN: 0,
  INTERSECTING: 1,
  ABOVE: 2,
  RIGHT: 4,
  BELOW: 8,
  LEFT: 16
};
function Ws(i) {
  const e = _t();
  for (let t = 0, n = i.length; t < n; ++t)
    Yi(e, i[t]);
  return e;
}
function $u(i, e, t) {
  const n = Math.min.apply(null, i), s = Math.min.apply(null, e), r = Math.max.apply(null, i), o = Math.max.apply(null, e);
  return kt(n, s, r, o, t);
}
function zo(i, e, t) {
  return t ? (t[0] = i[0] - e, t[1] = i[1] - e, t[2] = i[2] + e, t[3] = i[3] + e, t) : [
    i[0] - e,
    i[1] - e,
    i[2] + e,
    i[3] + e
  ];
}
function dc(i, e) {
  return e ? (e[0] = i[0], e[1] = i[1], e[2] = i[2], e[3] = i[3], e) : i.slice();
}
function Bn(i, e, t) {
  let n, s;
  return e < i[0] ? n = i[0] - e : i[2] < e ? n = e - i[2] : n = 0, t < i[1] ? s = i[1] - t : i[3] < t ? s = t - i[3] : s = 0, n * n + s * s;
}
function _i(i, e) {
  return Vo(i, e[0], e[1]);
}
function ai(i, e) {
  return i[0] <= e[0] && e[2] <= i[2] && i[1] <= e[1] && e[3] <= i[3];
}
function Vo(i, e, t) {
  return i[0] <= e && e <= i[2] && i[1] <= t && t <= i[3];
}
function fo(i, e) {
  const t = i[0], n = i[1], s = i[2], r = i[3], o = e[0], a = e[1];
  let l = De.UNKNOWN;
  return o < t ? l = l | De.LEFT : o > s && (l = l | De.RIGHT), a < n ? l = l | De.BELOW : a > r && (l = l | De.ABOVE), l === De.UNKNOWN && (l = De.INTERSECTING), l;
}
function _t() {
  return [1 / 0, 1 / 0, -1 / 0, -1 / 0];
}
function kt(i, e, t, n, s) {
  return s ? (s[0] = i, s[1] = e, s[2] = t, s[3] = n, s) : [i, e, t, n];
}
function Mi(i) {
  return kt(1 / 0, 1 / 0, -1 / 0, -1 / 0, i);
}
function fc(i, e) {
  const t = i[0], n = i[1];
  return kt(t, n, t, n, e);
}
function Xo(i, e, t, n, s) {
  const r = Mi(s);
  return mc(r, i, e, t, n);
}
function Hi(i, e) {
  return i[0] == e[0] && i[2] == e[2] && i[1] == e[1] && i[3] == e[3];
}
function gc(i, e) {
  return e[0] < i[0] && (i[0] = e[0]), e[2] > i[2] && (i[2] = e[2]), e[1] < i[1] && (i[1] = e[1]), e[3] > i[3] && (i[3] = e[3]), i;
}
function Yi(i, e) {
  e[0] < i[0] && (i[0] = e[0]), e[0] > i[2] && (i[2] = e[0]), e[1] < i[1] && (i[1] = e[1]), e[1] > i[3] && (i[3] = e[1]);
}
function mc(i, e, t, n, s) {
  for (; t < n; t += s)
    Ku(i, e[t], e[t + 1]);
  return i;
}
function Ku(i, e, t) {
  i[0] = Math.min(i[0], e), i[1] = Math.min(i[1], t), i[2] = Math.max(i[2], e), i[3] = Math.max(i[3], t);
}
function Wo(i, e) {
  let t;
  return t = e(pi(i)), t || (t = e(ls(i)), t) || (t = e(cs(i)), t) || (t = e(En(i)), t) ? t : false;
}
function go(i) {
  let e = 0;
  return hs(i) || (e = ye(i) * Ve(i)), e;
}
function pi(i) {
  return [i[0], i[1]];
}
function ls(i) {
  return [i[2], i[1]];
}
function pn(i) {
  return [(i[0] + i[2]) / 2, (i[1] + i[3]) / 2];
}
function Hu(i, e) {
  let t;
  if (e === "bottom-left")
    t = pi(i);
  else if (e === "bottom-right")
    t = ls(i);
  else if (e === "top-left")
    t = En(i);
  else if (e === "top-right")
    t = cs(i);
  else
    throw new Error("Invalid corner");
  return t;
}
function mo(i, e, t, n, s) {
  const [r, o, a, l, c, h2, u, d] = yc(
    i,
    e,
    t,
    n
  );
  return kt(
    Math.min(r, a, c, u),
    Math.min(o, l, h2, d),
    Math.max(r, a, c, u),
    Math.max(o, l, h2, d),
    s
  );
}
function yc(i, e, t, n) {
  const s = e * n[0] / 2, r = e * n[1] / 2, o = Math.cos(t), a = Math.sin(t), l = s * o, c = s * a, h2 = r * o, u = r * a, d = i[0], f = i[1];
  return [
    d - l + u,
    f - c - h2,
    d - l - u,
    f - c + h2,
    d + l - u,
    f + c + h2,
    d + l + u,
    f + c - h2,
    d - l + u,
    f - c - h2
  ];
}
function Ve(i) {
  return i[3] - i[1];
}
function On(i, e, t) {
  const n = t || _t();
  return $e(i, e) ? (i[0] > e[0] ? n[0] = i[0] : n[0] = e[0], i[1] > e[1] ? n[1] = i[1] : n[1] = e[1], i[2] < e[2] ? n[2] = i[2] : n[2] = e[2], i[3] < e[3] ? n[3] = i[3] : n[3] = e[3]) : Mi(n), n;
}
function En(i) {
  return [i[0], i[3]];
}
function cs(i) {
  return [i[2], i[3]];
}
function ye(i) {
  return i[2] - i[0];
}
function $e(i, e) {
  return i[0] <= e[2] && i[2] >= e[0] && i[1] <= e[3] && i[3] >= e[1];
}
function hs(i) {
  return i[2] < i[0] || i[3] < i[1];
}
function Zu(i, e) {
  return e ? (e[0] = i[0], e[1] = i[1], e[2] = i[2], e[3] = i[3], e) : i;
}
function qu(i, e, t) {
  let n = false;
  const s = fo(i, e), r = fo(i, t);
  if (s === De.INTERSECTING || r === De.INTERSECTING)
    n = true;
  else {
    const o = i[0], a = i[1], l = i[2], c = i[3], h2 = e[0], u = e[1], d = t[0], f = t[1], g = (f - u) / (d - h2);
    let y, m;
    r & De.ABOVE && !(s & De.ABOVE) && (y = d - (f - c) / g, n = y >= o && y <= l), !n && r & De.RIGHT && !(s & De.RIGHT) && (m = f - (d - l) * g, n = m >= a && m <= c), !n && r & De.BELOW && !(s & De.BELOW) && (y = d - (f - a) / g, n = y >= o && y <= l), !n && r & De.LEFT && !(s & De.LEFT) && (m = f - (d - o) * g, n = m >= a && m <= c);
  }
  return n;
}
function Ju(i, e, t, n) {
  if (hs(i))
    return Mi(t);
  let s = [];
  if (n > 1) {
    const a = i[2] - i[0], l = i[3] - i[1];
    for (let c = 0; c < n; ++c)
      s.push(
        i[0] + a * c / n,
        i[1],
        i[2],
        i[1] + l * c / n,
        i[2] - a * c / n,
        i[3],
        i[0],
        i[3] - l * c / n
      );
  } else
    s = [
      i[0],
      i[1],
      i[2],
      i[1],
      i[2],
      i[3],
      i[0],
      i[3]
    ];
  e(s, s, 2);
  const r = [], o = [];
  for (let a = 0, l = s.length; a < l; a += 2)
    r.push(s[a]), o.push(s[a + 1]);
  return $u(r, o, t);
}
function _c(i, e) {
  const t = e.getExtent(), n = pn(i);
  if (e.canWrapX() && (n[0] < t[0] || n[0] >= t[2])) {
    const s = ye(t), o = Math.floor(
      (n[0] - t[0]) / s
    ) * s;
    i[0] -= o, i[2] -= o;
  }
  return i;
}
function pc(i, e, t) {
  if (e.canWrapX()) {
    const n = e.getExtent();
    if (!isFinite(i[0]) || !isFinite(i[2]))
      return [[n[0], i[1], n[2], i[3]]];
    _c(i, e);
    const s = ye(n);
    if (ye(i) > s && !t)
      return [[n[0], i[1], n[2], i[3]]];
    if (i[0] < n[0])
      return [
        [i[0] + s, i[1], n[2], i[3]],
        [n[0], i[1], i[2], i[3]]
      ];
    if (i[2] > n[2])
      return [
        [i[0], i[1], n[2], i[3]],
        [n[0], i[1], i[2] - s, i[3]]
      ];
  }
  return [i];
}
var Sc = {
  info: 1,
  warn: 2,
  error: 3,
  none: 4
};
var Qu = Sc.info;
function Ec(...i) {
  Qu > Sc.warn || console.warn(...i);
}
function xe(i, e, t) {
  return Math.min(Math.max(i, e), t);
}
function ed(i, e, t, n, s, r) {
  const o = s - t, a = r - n;
  if (o !== 0 || a !== 0) {
    const l = ((i - t) * o + (e - n) * a) / (o * o + a * a);
    l > 1 ? (t = s, n = r) : l > 0 && (t += o * l, n += a * l);
  }
  return jt(i, e, t, n);
}
function jt(i, e, t, n) {
  const s = t - i, r = n - e;
  return s * s + r * r;
}
function td(i) {
  const e = i.length;
  for (let n = 0; n < e; n++) {
    let s = n, r = Math.abs(i[n][n]);
    for (let a = n + 1; a < e; a++) {
      const l = Math.abs(i[a][n]);
      l > r && (r = l, s = a);
    }
    if (r === 0)
      return null;
    const o = i[s];
    i[s] = i[n], i[n] = o;
    for (let a = n + 1; a < e; a++) {
      const l = -i[a][n] / i[n][n];
      for (let c = n; c < e + 1; c++)
        n == c ? i[a][c] = 0 : i[a][c] += l * i[n][c];
    }
  }
  const t = new Array(e);
  for (let n = e - 1; n >= 0; n--) {
    t[n] = i[n][e] / i[n][n];
    for (let s = n - 1; s >= 0; s--)
      i[s][e] -= i[s][n] * t[n];
  }
  return t;
}
function Za(i) {
  return i * 180 / Math.PI;
}
function Et(i) {
  return i * Math.PI / 180;
}
function gn(i, e) {
  const t = i % e;
  return t * e < 0 ? t + e : t;
}
function ot(i, e, t) {
  return i + t * (e - i);
}
function us(i, e) {
  const t = Math.pow(10, e);
  return Math.round(i * t) / t;
}
function ps(i, e) {
  return Math.floor(us(i, e));
}
function Ss(i, e) {
  return Math.ceil(us(i, e));
}
function yo(i, e, t) {
  if (i >= e && i < t)
    return i;
  const n = t - e;
  return ((i - e) % n + n) % n + e;
}
function nd(i, e) {
  return i[0] += +e[0], i[1] += +e[1], i;
}
function Ys(i, e) {
  let t = true;
  for (let n = i.length - 1; n >= 0; --n)
    if (i[n] != e[n]) {
      t = false;
      break;
    }
  return t;
}
function Yo(i, e) {
  const t = Math.cos(e), n = Math.sin(e), s = i[0] * t - i[1] * n, r = i[1] * t + i[0] * n;
  return i[0] = s, i[1] = r, i;
}
function id(i, e) {
  return i[0] *= e, i[1] *= e, i;
}
function jo(i, e) {
  const t = i[0] - e[0], n = i[1] - e[1];
  return t * t + n * n;
}
function wc(i, e) {
  return Math.sqrt(jo(i, e));
}
function Cc(i, e) {
  if (e.canWrapX()) {
    const t = ye(e.getExtent()), n = sd(i, e, t);
    n && (i[0] -= n * t);
  }
  return i;
}
function sd(i, e, t) {
  const n = e.getExtent();
  let s = 0;
  return e.canWrapX() && (i[0] < n[0] || i[0] > n[2]) && (t = t || ye(n), s = Math.floor(
    (i[0] - n[0]) / t
  )), s;
}
var Zi = {
  // use the radius of the Normal sphere
  radians: 6370997 / (2 * Math.PI),
  degrees: 2 * Math.PI * 6370997 / 360,
  ft: 0.3048,
  m: 1,
  "us-ft": 1200 / 3937
};
var rd = class {
  /**
   * @param {Options} options Projection options.
   */
  constructor(e) {
    this.code_ = e.code, this.units_ = /** @type {import("./Units.js").Units} */
    e.units, this.extent_ = e.extent !== void 0 ? e.extent : null, this.worldExtent_ = e.worldExtent !== void 0 ? e.worldExtent : null, this.axisOrientation_ = e.axisOrientation !== void 0 ? e.axisOrientation : "enu", this.global_ = e.global !== void 0 ? e.global : false, this.canWrapX_ = !!(this.global_ && this.extent_), this.getPointResolutionFunc_ = e.getPointResolution, this.defaultTileGrid_ = null, this.metersPerUnit_ = e.metersPerUnit;
  }
  /**
   * @return {boolean} The projection is suitable for wrapping the x-axis
   */
  canWrapX() {
    return this.canWrapX_;
  }
  /**
   * Get the code for this projection, e.g. 'EPSG:4326'.
   * @return {string} Code.
   * @api
   */
  getCode() {
    return this.code_;
  }
  /**
   * Get the validity extent for this projection.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_;
  }
  /**
   * Get the units of this projection.
   * @return {import("./Units.js").Units} Units.
   * @api
   */
  getUnits() {
    return this.units_;
  }
  /**
   * Get the amount of meters per unit of this projection.  If the projection is
   * not configured with `metersPerUnit` or a units identifier, the return is
   * `undefined`.
   * @return {number|undefined} Meters.
   * @api
   */
  getMetersPerUnit() {
    return this.metersPerUnit_ || Zi[this.units_];
  }
  /**
   * Get the world extent for this projection.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getWorldExtent() {
    return this.worldExtent_;
  }
  /**
   * Get the axis orientation of this projection.
   * Example values are:
   * enu - the default easting, northing, elevation.
   * neu - northing, easting, up - useful for "lat/long" geographic coordinates,
   *     or south orientated transverse mercator.
   * wnu - westing, northing, up - some planetary coordinate systems have
   *     "west positive" coordinate systems
   * @return {string} Axis orientation.
   * @api
   */
  getAxisOrientation() {
    return this.axisOrientation_;
  }
  /**
   * Is this projection a global projection which spans the whole world?
   * @return {boolean} Whether the projection is global.
   * @api
   */
  isGlobal() {
    return this.global_;
  }
  /**
   * Set if the projection is a global projection which spans the whole world
   * @param {boolean} global Whether the projection is global.
   * @api
   */
  setGlobal(e) {
    this.global_ = e, this.canWrapX_ = !!(e && this.extent_);
  }
  /**
   * @return {import("../tilegrid/TileGrid.js").default} The default tile grid.
   */
  getDefaultTileGrid() {
    return this.defaultTileGrid_;
  }
  /**
   * @param {import("../tilegrid/TileGrid.js").default} tileGrid The default tile grid.
   */
  setDefaultTileGrid(e) {
    this.defaultTileGrid_ = e;
  }
  /**
   * Set the validity extent for this projection.
   * @param {import("../extent.js").Extent} extent Extent.
   * @api
   */
  setExtent(e) {
    this.extent_ = e, this.canWrapX_ = !!(this.global_ && e);
  }
  /**
   * Set the world extent for this projection.
   * @param {import("../extent.js").Extent} worldExtent World extent
   *     [minlon, minlat, maxlon, maxlat].
   * @api
   */
  setWorldExtent(e) {
    this.worldExtent_ = e;
  }
  /**
   * Set the getPointResolution function (see {@link module:ol/proj.getPointResolution}
   * for this projection.
   * @param {function(number, import("../coordinate.js").Coordinate):number} func Function
   * @api
   */
  setGetPointResolution(e) {
    this.getPointResolutionFunc_ = e;
  }
  /**
   * Get the custom point resolution function for this projection (if set).
   * @return {GetPointResolution|undefined} The custom point
   * resolution function (if set).
   */
  getPointResolutionFunc() {
    return this.getPointResolutionFunc_;
  }
};
var $o = rd;
var ds = 6378137;
var li = Math.PI * ds;
var od = [-li, -li, li, li];
var ad = [-180, -85, 180, 85];
var Es = ds * Math.log(Math.tan(Math.PI / 2));
var Yn = class extends $o {
  /**
   * @param {string} code Code.
   */
  constructor(e) {
    super({
      code: e,
      units: "m",
      extent: od,
      global: true,
      worldExtent: ad,
      getPointResolution: function(t, n) {
        return t / Math.cosh(n[1] / ds);
      }
    });
  }
};
var qa = [
  new Yn("EPSG:3857"),
  new Yn("EPSG:102100"),
  new Yn("EPSG:102113"),
  new Yn("EPSG:900913"),
  new Yn("http://www.opengis.net/def/crs/EPSG/0/3857"),
  new Yn("http://www.opengis.net/gml/srs/epsg.xml#3857")
];
function ld(i, e, t, n) {
  const s = i.length;
  t = t > 1 ? t : 2, n = n ?? t, e === void 0 && (t > 2 ? e = i.slice() : e = new Array(s));
  for (let r = 0; r < s; r += n) {
    e[r] = li * i[r] / 180;
    let o = ds * Math.log(Math.tan(Math.PI * (+i[r + 1] + 90) / 360));
    o > Es ? o = Es : o < -Es && (o = -Es), e[r + 1] = o;
  }
  return e;
}
function cd(i, e, t, n) {
  const s = i.length;
  t = t > 1 ? t : 2, n = n ?? t, e === void 0 && (t > 2 ? e = i.slice() : e = new Array(s));
  for (let r = 0; r < s; r += n)
    e[r] = 180 * i[r] / li, e[r + 1] = 360 * Math.atan(Math.exp(i[r + 1] / ds)) / Math.PI - 90;
  return e;
}
var hd = 6378137;
var Ja = [-180, -90, 180, 90];
var ud = Math.PI * hd / 180;
var Tn = class extends $o {
  /**
   * @param {string} code Code.
   * @param {string} [axisOrientation] Axis orientation.
   */
  constructor(e, t) {
    super({
      code: e,
      units: "degrees",
      extent: Ja,
      axisOrientation: t,
      global: true,
      metersPerUnit: ud,
      worldExtent: Ja
    });
  }
};
var Qa = [
  new Tn("CRS:84"),
  new Tn("EPSG:4326", "neu"),
  new Tn("urn:ogc:def:crs:OGC:1.3:CRS84"),
  new Tn("urn:ogc:def:crs:OGC:2:84"),
  new Tn("http://www.opengis.net/def/crs/OGC/1.3/CRS84"),
  new Tn("http://www.opengis.net/gml/srs/epsg.xml#4326", "neu"),
  new Tn("http://www.opengis.net/def/crs/EPSG/0/4326", "neu")
];
var _o = {};
function dd(i) {
  return _o[i] || _o[i.replace(/urn:(x-)?ogc:def:crs:EPSG:(.*:)?(\w+)$/, "EPSG:$3")] || null;
}
function fd(i, e) {
  _o[i] = e;
}
var di = {};
function qi(i, e, t) {
  const n = i.getCode(), s = e.getCode();
  n in di || (di[n] = {}), di[n][s] = t;
}
function Pr(i, e) {
  return i in di && e in di[i] ? di[i][e] : null;
}
var js = 0.9996;
var yt = 669438e-8;
var yr = yt * yt;
var _r = yr * yt;
var Dn = yt / (1 - yt);
var el = Math.sqrt(1 - yt);
var Si = (1 - el) / (1 + el);
var xc = Si * Si;
var Ko = xc * Si;
var Ho = Ko * Si;
var Ic = Ho * Si;
var vc = 1 - yt / 4 - 3 * yr / 64 - 5 * _r / 256;
var gd = 3 * yt / 8 + 3 * yr / 32 + 45 * _r / 1024;
var md = 15 * yr / 256 + 45 * _r / 1024;
var yd = 35 * _r / 3072;
var _d = 3 / 2 * Si - 27 / 32 * Ko + 269 / 512 * Ic;
var pd = 21 / 16 * xc - 55 / 32 * Ho;
var Sd = 151 / 96 * Ko - 417 / 128 * Ic;
var Ed = 1097 / 512 * Ho;
var $s = 6378137;
function wd(i, e, t) {
  const n = i - 5e5, o = (t.north ? e : e - 1e7) / js / ($s * vc), a = o + _d * Math.sin(2 * o) + pd * Math.sin(4 * o) + Sd * Math.sin(6 * o) + Ed * Math.sin(8 * o), l = Math.sin(a), c = l * l, h2 = Math.cos(a), u = l / h2, d = u * u, f = d * d, g = 1 - yt * c, y = Math.sqrt(1 - yt * c), m = $s / y, _ = (1 - yt) / g, p = Dn * h2 ** 2, E = p * p, S = n / (m * js), w = S * S, x = w * S, b = x * S, v = b * S, R = v * S, P = a - u / _ * (w / 2 - b / 24 * (5 + 3 * d + 10 * p - 4 * E - 9 * Dn)) + R / 720 * (61 + 90 * d + 298 * p + 45 * f - 252 * Dn - 3 * E);
  let U = (S - x / 6 * (1 + 2 * d + p) + v / 120 * (5 - 2 * p + 28 * d - 3 * E + 8 * Dn + 24 * f)) / h2;
  return U = yo(
    U + Et(Mc(t.number)),
    -Math.PI,
    Math.PI
  ), [Za(U), Za(P)];
}
var tl = -80;
var nl = 84;
var Cd = -180;
var xd = 180;
function Id(i, e, t) {
  i = yo(i, Cd, xd), e < tl ? e = tl : e > nl && (e = nl);
  const n = Et(e), s = Math.sin(n), r = Math.cos(n), o = s / r, a = o * o, l = a * a, c = Et(i), h2 = Mc(t.number), u = Et(h2), d = $s / Math.sqrt(1 - yt * s ** 2), f = Dn * r ** 2, g = r * yo(c - u, -Math.PI, Math.PI), y = g * g, m = y * g, _ = m * g, p = _ * g, E = p * g, S = $s * (vc * n - gd * Math.sin(2 * n) + md * Math.sin(4 * n) - yd * Math.sin(6 * n)), w = js * d * (g + m / 6 * (1 - a + f) + p / 120 * (5 - 18 * a + l + 72 * f - 58 * Dn)) + 5e5;
  let x = js * (S + d * o * (y / 2 + _ / 24 * (5 - a + 9 * f + 4 * f ** 2) + E / 720 * (61 - 58 * a + l + 600 * f - 330 * Dn)));
  return t.north || (x += 1e7), [w, x];
}
function Mc(i) {
  return (i - 1) * 6 - 180 + 3;
}
var vd = [
  /^EPSG:(\d+)$/,
  /^urn:ogc:def:crs:EPSG::(\d+)$/,
  /^http:\/\/www\.opengis\.net\/def\/crs\/EPSG\/0\/(\d+)$/
];
function Tc(i) {
  let e = 0;
  for (const s of vd) {
    const r = i.match(s);
    if (r) {
      e = parseInt(r[1]);
      break;
    }
  }
  if (!e)
    return null;
  let t = 0, n = false;
  return e > 32700 && e < 32761 ? t = e - 32700 : e > 32600 && e < 32661 && (n = true, t = e - 32600), t ? { number: t, north: n } : null;
}
function il(i, e) {
  return function(t, n, s, r) {
    const o = t.length;
    s = s > 1 ? s : 2, r = r ?? s, n || (s > 2 ? n = t.slice() : n = new Array(o));
    for (let a = 0; a < o; a += r) {
      const l = t[a], c = t[a + 1], h2 = i(l, c, e);
      n[a] = h2[0], n[a + 1] = h2[1];
    }
    return n;
  };
}
function Md(i) {
  return Tc(i) ? new $o({ code: i, units: "m" }) : null;
}
function Td(i) {
  const e = Tc(i.getCode());
  return e ? {
    forward: il(Id, e),
    inverse: il(wd, e)
  } : null;
}
var Zo = 63710088e-1;
function Ks(i, e, t) {
  t = t || Zo;
  const n = Et(i[1]), s = Et(e[1]), r = (s - n) / 2, o = Et(e[0] - i[0]) / 2, a = Math.sin(r) * Math.sin(r) + Math.sin(o) * Math.sin(o) * Math.cos(n) * Math.cos(s);
  return 2 * t * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
function Ar(i, e) {
  let t = 0;
  for (let n = 0, s = i.length; n < s - 1; ++n)
    t += Ks(i[n], i[n + 1], e);
  return t;
}
function po(i, e) {
  e = e || {};
  const t = e.radius || Zo, n = e.projection || "EPSG:3857", s = i.getType();
  s !== "GeometryCollection" && (i = i.clone().transform(n, "EPSG:4326"));
  let r = 0, o, a, l, c, h2, u;
  switch (s) {
    case "Point":
    case "MultiPoint":
      break;
    case "LineString":
    case "LinearRing": {
      o = /** @type {import("./geom/SimpleGeometry.js").default} */
      i.getCoordinates(), r = Ar(o, t);
      break;
    }
    case "MultiLineString":
    case "Polygon": {
      for (o = /** @type {import("./geom/SimpleGeometry.js").default} */
      i.getCoordinates(), l = 0, c = o.length; l < c; ++l)
        r += Ar(o[l], t);
      break;
    }
    case "MultiPolygon": {
      for (o = /** @type {import("./geom/SimpleGeometry.js").default} */
      i.getCoordinates(), l = 0, c = o.length; l < c; ++l)
        for (a = o[l], h2 = 0, u = a.length; h2 < u; ++h2)
          r += Ar(a[h2], t);
      break;
    }
    case "GeometryCollection": {
      const d = (
        /** @type {import("./geom/GeometryCollection.js").default} */
        i.getGeometries()
      );
      for (l = 0, c = d.length; l < c; ++l)
        r += po(d[l], e);
      break;
    }
    default:
      throw new Error("Unsupported geometry type: " + s);
  }
  return r;
}
function ws(i, e) {
  let t = 0;
  const n = i.length;
  let s = i[n - 1][0], r = i[n - 1][1];
  for (let o = 0; o < n; o++) {
    const a = i[o][0], l = i[o][1];
    t += Et(a - s) * (2 + Math.sin(Et(r)) + Math.sin(Et(l))), s = a, r = l;
  }
  return t * e * e / 2;
}
function bc(i, e) {
  e = e || {};
  const t = e.radius || Zo, n = e.projection || "EPSG:3857", s = i.getType();
  s !== "GeometryCollection" && (i = i.clone().transform(n, "EPSG:4326"));
  let r = 0, o, a, l, c, h2, u;
  switch (s) {
    case "Point":
    case "MultiPoint":
    case "LineString":
    case "MultiLineString":
    case "LinearRing":
      break;
    case "Polygon": {
      for (o = /** @type {import("./geom/Polygon.js").default} */
      i.getCoordinates(), r = Math.abs(ws(o[0], t)), l = 1, c = o.length; l < c; ++l)
        r -= Math.abs(ws(o[l], t));
      break;
    }
    case "MultiPolygon": {
      for (o = /** @type {import("./geom/SimpleGeometry.js").default} */
      i.getCoordinates(), l = 0, c = o.length; l < c; ++l)
        for (a = o[l], r += Math.abs(ws(a[0], t)), h2 = 1, u = a.length; h2 < u; ++h2)
          r -= Math.abs(ws(a[h2], t));
      break;
    }
    case "GeometryCollection": {
      const d = (
        /** @type {import("./geom/GeometryCollection.js").default} */
        i.getGeometries()
      );
      for (l = 0, c = d.length; l < c; ++l)
        r += bc(d[l], e);
      break;
    }
    default:
      throw new Error("Unsupported geometry type: " + s);
  }
  return r;
}
var bd = [Td];
var Rd = [Md];
var So = true;
function Rc(i) {
  So = !(i === void 0 ? true : i);
}
function qo(i, e) {
  if (e !== void 0) {
    for (let t = 0, n = i.length; t < n; ++t)
      e[t] = i[t];
    e = e;
  } else
    e = i.slice();
  return e;
}
function Eo(i) {
  fd(i.getCode(), i), qi(i, i, qo);
}
function Ld(i) {
  i.forEach(Eo);
}
function nt(i) {
  if (typeof i != "string")
    return i;
  const e = dd(i);
  if (e)
    return e;
  for (const t of Rd) {
    const n = t(i);
    if (n)
      return n;
  }
  return null;
}
function sl(i, e, t, n) {
  i = nt(i);
  let s;
  const r = i.getPointResolutionFunc();
  if (r) {
    if (s = r(e, t), n && n !== i.getUnits()) {
      const o = i.getMetersPerUnit();
      o && (s = s * o / Zi[n]);
    }
  } else {
    const o = i.getUnits();
    if (o == "degrees" && !n || n == "degrees")
      s = e;
    else {
      const a = pr(
        i,
        nt("EPSG:4326")
      );
      if (!a && o !== "degrees")
        s = e * i.getMetersPerUnit();
      else {
        let c = [
          t[0] - e / 2,
          t[1],
          t[0] + e / 2,
          t[1],
          t[0],
          t[1] - e / 2,
          t[0],
          t[1] + e / 2
        ];
        c = a(c, c, 2);
        const h2 = Ks(c.slice(0, 2), c.slice(2, 4)), u = Ks(c.slice(4, 6), c.slice(6, 8));
        s = (h2 + u) / 2;
      }
      const l = n ? Zi[n] : i.getMetersPerUnit();
      l !== void 0 && (s /= l);
    }
  }
  return s;
}
function rl(i) {
  Ld(i), i.forEach(function(e) {
    i.forEach(function(t) {
      e !== t && qi(e, t, qo);
    });
  });
}
function Fd(i, e, t, n) {
  i.forEach(function(s) {
    e.forEach(function(r) {
      qi(s, r, t), qi(r, s, n);
    });
  });
}
function Jo(i, e) {
  return i ? typeof i == "string" ? nt(i) : (
    /** @type {Projection} */
    i
  ) : nt(e);
}
function Dd(i) {
  return (
    /**
     * @param {Array<number>} input Input.
     * @param {Array<number>} [output] Output.
     * @param {number} [dimension] Dimensions that should be transformed.
     * @param {number} [stride] Stride.
     * @return {Array<number>} Output.
     */
    function(e, t, n, s) {
      const r = e.length;
      n = n !== void 0 ? n : 2, s = s ?? n, t = t !== void 0 ? t : new Array(r);
      for (let o = 0; o < r; o += s) {
        const a = i(e.slice(o, o + n)), l = a.length;
        for (let c = 0, h2 = s; c < h2; ++c)
          t[o + c] = c >= l ? e[o + c] : a[c];
      }
      return t;
    }
  );
}
function wt(i, e) {
  return Rc(), Sr(
    i,
    "EPSG:4326",
    e !== void 0 ? e : "EPSG:3857"
  );
}
function $t(i, e) {
  const t = Sr(
    i,
    e !== void 0 ? e : "EPSG:3857",
    "EPSG:4326"
  ), n = t[0];
  return (n < -180 || n > 180) && (t[0] = gn(n + 180, 360) - 180), t;
}
function Gs(i, e) {
  if (i === e)
    return true;
  const t = i.getUnits() === e.getUnits();
  return (i.getCode() === e.getCode() || pr(i, e) === qo) && t;
}
function pr(i, e) {
  const t = i.getCode(), n = e.getCode();
  let s = Pr(t, n);
  if (s)
    return s;
  let r = null, o = null;
  for (const l of bd)
    r || (r = l(i)), o || (o = l(e));
  if (!r && !o)
    return null;
  const a = "EPSG:4326";
  if (o)
    if (r)
      s = kr(
        r.inverse,
        o.forward
      );
    else {
      const l = Pr(t, a);
      l && (s = kr(
        l,
        o.forward
      ));
    }
  else {
    const l = Pr(a, n);
    l && (s = kr(
      r.inverse,
      l
    ));
  }
  return s && (Eo(i), Eo(e), qi(i, e, s)), s;
}
function kr(i, e) {
  return function(t, n, s, r) {
    return n = i(t, n, s, r), e(n, n, s, r);
  };
}
function Ji(i, e) {
  const t = nt(i), n = nt(e);
  return pr(t, n);
}
function Sr(i, e, t) {
  const n = Ji(e, t);
  if (!n) {
    const s = nt(e).getCode(), r = nt(t).getCode();
    throw new Error(
      `No transform available between ${s} and ${r}`
    );
  }
  return n(i, void 0, i.length);
}
function Pd(i, e, t, n) {
  const s = Ji(e, t);
  return Ju(i, s, void 0, n);
}
var Ad = null;
function kd() {
  return Ad;
}
function wo(i, e) {
  return i;
}
function Be(i, e) {
  return So && !Ys(i, [0, 0]) && i[0] >= -180 && i[0] <= 180 && i[1] >= -90 && i[1] <= 90 && (So = false, Ec(
    "Call useGeographic() from ol/proj once to work with [longitude, latitude] coordinates."
  )), i;
}
function Qo(i, e) {
  return i;
}
function cn(i, e) {
  return i;
}
function Od() {
  rl(qa), rl(Qa), Fd(
    Qa,
    qa,
    ld,
    cd
  );
}
Od();
new Array(6);
function Ct() {
  return [1, 0, 0, 1, 0, 0];
}
function Nd(i, e) {
  return i[0] = e[0], i[1] = e[1], i[2] = e[2], i[3] = e[3], i[4] = e[4], i[5] = e[5], i;
}
function Pe(i, e) {
  const t = e[0], n = e[1];
  return e[0] = i[0] * t + i[2] * n + i[4], e[1] = i[1] * t + i[3] * n + i[5], e;
}
function Zt(i, e, t, n, s, r, o, a) {
  const l = Math.sin(r), c = Math.cos(r);
  return i[0] = n * c, i[1] = s * l, i[2] = -n * l, i[3] = s * c, i[4] = o * n * c - a * n * l + e, i[5] = o * s * l + a * s * c + t, i;
}
function Lc(i, e) {
  const t = Ud(e);
  _e(t !== 0, "Transformation matrix cannot be inverted");
  const n = e[0], s = e[1], r = e[2], o = e[3], a = e[4], l = e[5];
  return i[0] = o / t, i[1] = -s / t, i[2] = -r / t, i[3] = n / t, i[4] = (r * l - o * a) / t, i[5] = -(n * l - s * a) / t, i;
}
function Ud(i) {
  return i[0] * i[3] - i[1] * i[2];
}
var Gd = [1e5, 1e5, 1e5, 1e5, 2, 2];
function Bd(i) {
  return "matrix(" + i.join(", ") + ")";
}
function Co(i) {
  return i.substring(7, i.length - 1).split(",").map(parseFloat);
}
function zd(i, e) {
  const t = Co(i), n = Co(e);
  for (let s = 0; s < 6; ++s)
    if (Math.round((t[s] - n[s]) * Gd[s]) !== 0)
      return false;
  return true;
}
function mn(i, e, t, n, s, r, o) {
  r = r || [], o = o || 2;
  let a = 0;
  for (let l = e; l < t; l += n) {
    const c = i[l], h2 = i[l + 1];
    r[a++] = s[0] * c + s[2] * h2 + s[4], r[a++] = s[1] * c + s[3] * h2 + s[5];
    for (let u = 2; u < o; u++)
      r[a++] = i[l + u];
  }
  return r && r.length != a && (r.length = a), r;
}
function ea(i, e, t, n, s, r, o) {
  o = o || [];
  const a = Math.cos(s), l = Math.sin(s), c = r[0], h2 = r[1];
  let u = 0;
  for (let d = e; d < t; d += n) {
    const f = i[d] - c, g = i[d + 1] - h2;
    o[u++] = c + f * a - g * l, o[u++] = h2 + f * l + g * a;
    for (let y = d + 2; y < d + n; ++y)
      o[u++] = i[y];
  }
  return o && o.length != u && (o.length = u), o;
}
function Vd(i, e, t, n, s, r, o, a) {
  a = a || [];
  const l = o[0], c = o[1];
  let h2 = 0;
  for (let u = e; u < t; u += n) {
    const d = i[u] - l, f = i[u + 1] - c;
    a[h2++] = l + s * d, a[h2++] = c + r * f;
    for (let g = u + 2; g < u + n; ++g)
      a[h2++] = i[g];
  }
  return a && a.length != h2 && (a.length = h2), a;
}
function Xd(i, e, t, n, s, r, o) {
  o = o || [];
  let a = 0;
  for (let l = e; l < t; l += n) {
    o[a++] = i[l] + s, o[a++] = i[l + 1] + r;
    for (let c = l + 2; c < l + n; ++c)
      o[a++] = i[c];
  }
  return o && o.length != a && (o.length = a), o;
}
var ol = Ct();
var Wd = [NaN, NaN];
var Yd = class extends vt {
  constructor() {
    super(), this.extent_ = _t(), this.extentRevision_ = -1, this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = 0, this.simplifyTransformedInternal = hc(
      (e, t, n) => {
        if (!n)
          return this.getSimplifiedGeometry(t);
        const s = this.clone();
        return s.applyTransform(n), s.getSimplifiedGeometry(t);
      }
    );
  }
  /**
   * Get a transformed and simplified version of the geometry.
   * @abstract
   * @param {number} squaredTolerance Squared tolerance.
   * @param {import("../proj.js").TransformFunction} [transform] Optional transform function.
   * @return {Geometry} Simplified geometry.
   */
  simplifyTransformed(e, t) {
    return this.simplifyTransformedInternal(
      this.getRevision(),
      e,
      t
    );
  }
  /**
   * Make a complete copy of the geometry.
   * @abstract
   * @return {!Geometry} Clone.
   */
  clone() {
    return ue();
  }
  /**
   * @abstract
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   */
  closestPointXY(e, t, n, s) {
    return ue();
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   */
  containsXY(e, t) {
    return this.closestPointXY(e, t, Wd, Number.MIN_VALUE) === 0;
  }
  /**
   * Return the closest point of the geometry to the passed point as
   * {@link module:ol/coordinate~Coordinate coordinate}.
   * @param {import("../coordinate.js").Coordinate} point Point.
   * @param {import("../coordinate.js").Coordinate} [closestPoint] Closest point.
   * @return {import("../coordinate.js").Coordinate} Closest point.
   * @api
   */
  getClosestPoint(e, t) {
    return t = t || [NaN, NaN], this.closestPointXY(e[0], e[1], t, 1 / 0), t;
  }
  /**
   * Returns true if this geometry includes the specified coordinate. If the
   * coordinate is on the boundary of the geometry, returns false.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @return {boolean} Contains coordinate.
   * @api
   */
  intersectsCoordinate(e) {
    return this.containsXY(e[0], e[1]);
  }
  /**
   * @abstract
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   */
  computeExtent(e) {
    return ue();
  }
  /**
   * Get the extent of the geometry.
   * @param {import("../extent.js").Extent} [extent] Extent.
   * @return {import("../extent.js").Extent} extent Extent.
   * @api
   */
  getExtent(e) {
    if (this.extentRevision_ != this.getRevision()) {
      const t = this.computeExtent(this.extent_);
      (isNaN(t[0]) || isNaN(t[1])) && Mi(t), this.extentRevision_ = this.getRevision();
    }
    return Zu(this.extent_, e);
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} angle Rotation angle in radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   */
  rotate(e, t) {
    ue();
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   */
  scale(e, t, n) {
    ue();
  }
  /**
   * Create a simplified version of this geometry.  For linestrings, this uses
   * the [Douglas Peucker](https://en.wikipedia.org/wiki/Ramer-Douglas-Peucker_algorithm)
   * algorithm.  For polygons, a quantization-based
   * simplification is used to preserve topology.
   * @param {number} tolerance The tolerance distance for simplification.
   * @return {Geometry} A new, simplified version of the original geometry.
   * @api
   */
  simplify(e) {
    return this.getSimplifiedGeometry(e * e);
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker
   * algorithm.
   * See https://en.wikipedia.org/wiki/Ramer-Douglas-Peucker_algorithm.
   * @abstract
   * @param {number} squaredTolerance Squared tolerance.
   * @return {Geometry} Simplified geometry.
   */
  getSimplifiedGeometry(e) {
    return ue();
  }
  /**
   * Get the type of this geometry.
   * @abstract
   * @return {Type} Geometry type.
   */
  getType() {
    return ue();
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @abstract
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   */
  applyTransform(e) {
    ue();
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @abstract
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   */
  intersectsExtent(e) {
    return ue();
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @abstract
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   */
  translate(e, t) {
    ue();
  }
  /**
   * Transform each coordinate of the geometry from one coordinate reference
   * system to another. The geometry is modified in place.
   * For example, a line will be transformed to a line and a circle to a circle.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   *
   * @param {import("../proj.js").ProjectionLike} source The current projection.  Can be a
   *     string identifier or a {@link module:ol/proj/Projection~Projection} object.
   * @param {import("../proj.js").ProjectionLike} destination The desired projection.  Can be a
   *     string identifier or a {@link module:ol/proj/Projection~Projection} object.
   * @return {this} This geometry.  Note that original geometry is
   *     modified in place.
   * @api
   */
  transform(e, t) {
    const n = nt(e), s = n.getUnits() == "tile-pixels" ? function(r, o, a) {
      const l = n.getExtent(), c = n.getWorldExtent(), h2 = Ve(c) / Ve(l);
      Zt(
        ol,
        c[0],
        c[3],
        h2,
        -h2,
        0,
        0,
        0
      );
      const u = mn(
        r,
        0,
        r.length,
        a,
        ol,
        o
      ), d = Ji(n, t);
      return d ? d(u, u, a) : u;
    } : Ji(n, t);
    return this.applyTransform(s), this;
  }
};
var Fc = Yd;
var jd = class extends Fc {
  constructor() {
    super(), this.layout = "XY", this.stride = 2, this.flatCoordinates;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(e) {
    return Xo(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e
    );
  }
  /**
   * @abstract
   * @return {Array<*> | null} Coordinates.
   */
  getCoordinates() {
    return ue();
  }
  /**
   * Return the first coordinate of the geometry.
   * @return {import("../coordinate.js").Coordinate} First coordinate.
   * @api
   */
  getFirstCoordinate() {
    return this.flatCoordinates.slice(0, this.stride);
  }
  /**
   * @return {Array<number>} Flat coordinates.
   */
  getFlatCoordinates() {
    return this.flatCoordinates;
  }
  /**
   * Return the last coordinate of the geometry.
   * @return {import("../coordinate.js").Coordinate} Last point.
   * @api
   */
  getLastCoordinate() {
    return this.flatCoordinates.slice(
      this.flatCoordinates.length - this.stride
    );
  }
  /**
   * Return the {@link import("./Geometry.js").GeometryLayout layout} of the geometry.
   * @return {import("./Geometry.js").GeometryLayout} Layout.
   * @api
   */
  getLayout() {
    return this.layout;
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker algorithm.
   * @param {number} squaredTolerance Squared tolerance.
   * @return {SimpleGeometry} Simplified geometry.
   * @override
   */
  getSimplifiedGeometry(e) {
    if (this.simplifiedGeometryRevision !== this.getRevision() && (this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = this.getRevision()), e < 0 || this.simplifiedGeometryMaxMinSquaredTolerance !== 0 && e <= this.simplifiedGeometryMaxMinSquaredTolerance)
      return this;
    const t = this.getSimplifiedGeometryInternal(e);
    return t.getFlatCoordinates().length < this.flatCoordinates.length ? t : (this.simplifiedGeometryMaxMinSquaredTolerance = e, this);
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {SimpleGeometry} Simplified geometry.
   * @protected
   */
  getSimplifiedGeometryInternal(e) {
    return this;
  }
  /**
   * @return {number} Stride.
   */
  getStride() {
    return this.stride;
  }
  /**
   * @param {import("./Geometry.js").GeometryLayout} layout Layout.
   * @param {Array<number>} flatCoordinates Flat coordinates.
   */
  setFlatCoordinates(e, t) {
    this.stride = Hs(e), this.layout = e, this.flatCoordinates = t;
  }
  /**
   * @abstract
   * @param {!Array<*>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  setCoordinates(e, t) {
    ue();
  }
  /**
   * @param {import("./Geometry.js").GeometryLayout|undefined} layout Layout.
   * @param {Array<*>} coordinates Coordinates.
   * @param {number} nesting Nesting.
   * @protected
   */
  setLayout(e, t, n) {
    let s;
    if (e)
      s = Hs(e);
    else {
      for (let r = 0; r < n; ++r) {
        if (t.length === 0) {
          this.layout = "XY", this.stride = 2;
          return;
        }
        t = /** @type {Array<unknown>} */
        t[0];
      }
      s = t.length, e = $d(s);
    }
    this.layout = e, this.stride = s;
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   * @api
   * @override
   */
  applyTransform(e) {
    this.flatCoordinates && (e(
      this.flatCoordinates,
      this.flatCoordinates,
      this.layout.startsWith("XYZ") ? 3 : 2,
      this.stride
    ), this.changed());
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in counter-clockwise radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(e, t) {
    const n = this.getFlatCoordinates();
    if (n) {
      const s = this.getStride();
      ea(
        n,
        0,
        n.length,
        s,
        e,
        t,
        n
      ), this.changed();
    }
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   * @override
   */
  scale(e, t, n) {
    t === void 0 && (t = e), n || (n = pn(this.getExtent()));
    const s = this.getFlatCoordinates();
    if (s) {
      const r = this.getStride();
      Vd(
        s,
        0,
        s.length,
        r,
        e,
        t,
        n,
        s
      ), this.changed();
    }
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   * @override
   */
  translate(e, t) {
    const n = this.getFlatCoordinates();
    if (n) {
      const s = this.getStride();
      Xd(
        n,
        0,
        n.length,
        s,
        e,
        t,
        n
      ), this.changed();
    }
  }
};
function $d(i) {
  let e;
  return i == 2 ? e = "XY" : i == 3 ? e = "XYZ" : i == 4 && (e = "XYZM"), /** @type {import("./Geometry.js").GeometryLayout} */
  e;
}
function Hs(i) {
  let e;
  return i == "XY" ? e = 2 : i == "XYZ" || i == "XYM" ? e = 3 : i == "XYZM" && (e = 4), /** @type {number} */
  e;
}
function Kd(i, e, t) {
  const n = i.getFlatCoordinates();
  if (!n)
    return null;
  const s = i.getStride();
  return mn(
    n,
    0,
    n.length,
    s,
    e,
    t
  );
}
var wn = jd;
function al(i, e, t, n, s, r, o) {
  const a = i[e], l = i[e + 1], c = i[t] - a, h2 = i[t + 1] - l;
  let u;
  if (c === 0 && h2 === 0)
    u = e;
  else {
    const d = ((s - a) * c + (r - l) * h2) / (c * c + h2 * h2);
    if (d > 1)
      u = t;
    else if (d > 0) {
      for (let f = 0; f < n; ++f)
        o[f] = ot(
          i[e + f],
          i[t + f],
          d
        );
      o.length = n;
      return;
    } else
      u = e;
  }
  for (let d = 0; d < n; ++d)
    o[d] = i[u + d];
  o.length = n;
}
function ta(i, e, t, n, s) {
  let r = i[e], o = i[e + 1];
  for (e += n; e < t; e += n) {
    const a = i[e], l = i[e + 1], c = jt(r, o, a, l);
    c > s && (s = c), r = a, o = l;
  }
  return s;
}
function na(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    s = ta(i, e, a, n, s), e = a;
  }
  return s;
}
function Hd(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    s = na(i, e, a, n, s), e = a[a.length - 1];
  }
  return s;
}
function ia(i, e, t, n, s, r, o, a, l, c, h2) {
  if (e == t)
    return c;
  let u, d;
  if (s === 0) {
    if (d = jt(
      o,
      a,
      i[e],
      i[e + 1]
    ), d < c) {
      for (u = 0; u < n; ++u)
        l[u] = i[e + u];
      return l.length = n, d;
    }
    return c;
  }
  h2 = h2 || [NaN, NaN];
  let f = e + n;
  for (; f < t; )
    if (al(
      i,
      f - n,
      f,
      n,
      o,
      a,
      h2
    ), d = jt(o, a, h2[0], h2[1]), d < c) {
      for (c = d, u = 0; u < n; ++u)
        l[u] = h2[u];
      l.length = n, f += n;
    } else
      f += n * Math.max(
        (Math.sqrt(d) - Math.sqrt(c)) / s | 0,
        1
      );
  if (r && (al(
    i,
    t - n,
    e,
    n,
    o,
    a,
    h2
  ), d = jt(o, a, h2[0], h2[1]), d < c)) {
    for (c = d, u = 0; u < n; ++u)
      l[u] = h2[u];
    l.length = n;
  }
  return c;
}
function sa(i, e, t, n, s, r, o, a, l, c, h2) {
  h2 = h2 || [NaN, NaN];
  for (let u = 0, d = t.length; u < d; ++u) {
    const f = t[u];
    c = ia(
      i,
      e,
      f,
      n,
      s,
      r,
      o,
      a,
      l,
      c,
      h2
    ), e = f;
  }
  return c;
}
function Zd(i, e, t, n, s, r, o, a, l, c, h2) {
  h2 = h2 || [NaN, NaN];
  for (let u = 0, d = t.length; u < d; ++u) {
    const f = t[u];
    c = sa(
      i,
      e,
      f,
      n,
      s,
      r,
      o,
      a,
      l,
      c,
      h2
    ), e = f[f.length - 1];
  }
  return c;
}
function Dc(i, e, t, n) {
  for (let s = 0, r = t.length; s < r; ++s)
    i[e++] = t[s];
  return e;
}
function Er(i, e, t, n) {
  for (let s = 0, r = t.length; s < r; ++s) {
    const o = t[s];
    for (let a = 0; a < n; ++a)
      i[e++] = o[a];
  }
  return e;
}
function ra(i, e, t, n, s) {
  s = s || [];
  let r = 0;
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = Er(
      i,
      e,
      t[o],
      n
    );
    s[r++] = l, e = l;
  }
  return s.length = r, s;
}
function qd(i, e, t, n, s) {
  s = s || [];
  let r = 0;
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = ra(
      i,
      e,
      t[o],
      n,
      s[r]
    );
    l.length === 0 && (l[0] = e), s[r++] = l, e = l[l.length - 1];
  }
  return s.length = r, s;
}
function hn(i, e, t, n, s) {
  s = s !== void 0 ? s : [];
  let r = 0;
  for (let o = e; o < t; o += n)
    s[r++] = i.slice(o, o + n);
  return s.length = r, s;
}
function Qi(i, e, t, n, s) {
  s = s !== void 0 ? s : [];
  let r = 0;
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = t[o];
    s[r++] = hn(
      i,
      e,
      l,
      n,
      s[r]
    ), e = l;
  }
  return s.length = r, s;
}
function xo(i, e, t, n, s) {
  s = s !== void 0 ? s : [];
  let r = 0;
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = t[o];
    s[r++] = l.length === 1 && l[0] === e ? [] : Qi(
      i,
      e,
      l,
      n,
      s[r]
    ), e = l[l.length - 1];
  }
  return s.length = r, s;
}
function Zs(i, e, t, n, s, r, o) {
  let a, l;
  const c = (t - e) / n;
  if (c === 1)
    a = e;
  else if (c === 2)
    a = e, l = s;
  else if (c !== 0) {
    let h2 = i[e], u = i[e + 1], d = 0;
    const f = [0];
    for (let m = e + n; m < t; m += n) {
      const _ = i[m], p = i[m + 1];
      d += Math.sqrt((_ - h2) * (_ - h2) + (p - u) * (p - u)), f.push(d), h2 = _, u = p;
    }
    const g = s * d, y = Nu(f, g);
    y < 0 ? (l = (g - f[-y - 2]) / (f[-y - 1] - f[-y - 2]), a = e + (-y - 2) * n) : a = e + y * n;
  }
  o = o > 1 ? o : 2, r = r || new Array(o);
  for (let h2 = 0; h2 < o; ++h2)
    r[h2] = a === void 0 ? NaN : l === void 0 ? i[a + h2] : ot(i[a + h2], i[a + n + h2], l);
  return r;
}
function Io(i, e, t, n, s, r) {
  if (t == e)
    return null;
  let o;
  if (s < i[e + n - 1])
    return r ? (o = i.slice(e, e + n), o[n - 1] = s, o) : null;
  if (i[t - 1] < s)
    return r ? (o = i.slice(t - n, t), o[n - 1] = s, o) : null;
  if (s == i[e + n - 1])
    return i.slice(e, e + n);
  let a = e / n, l = t / n;
  for (; a < l; ) {
    const d = a + l >> 1;
    s < i[(d + 1) * n - 1] ? l = d : a = d + 1;
  }
  const c = i[a * n - 1];
  if (s == c)
    return i.slice((a - 1) * n, (a - 1) * n + n);
  const h2 = i[(a + 1) * n - 1], u = (s - c) / (h2 - c);
  o = [];
  for (let d = 0; d < n - 1; ++d)
    o.push(
      ot(
        i[(a - 1) * n + d],
        i[a * n + d],
        u
      )
    );
  return o.push(s), o;
}
function Jd(i, e, t, n, s, r, o) {
  if (o)
    return Io(
      i,
      e,
      t[t.length - 1],
      n,
      s,
      r
    );
  let a;
  if (s < i[n - 1])
    return r ? (a = i.slice(0, n), a[n - 1] = s, a) : null;
  if (i[i.length - 1] < s)
    return r ? (a = i.slice(i.length - n), a[n - 1] = s, a) : null;
  for (let l = 0, c = t.length; l < c; ++l) {
    const h2 = t[l];
    if (e != h2) {
      if (s < i[e + n - 1])
        return null;
      if (s <= i[h2 - 1])
        return Io(
          i,
          e,
          h2,
          n,
          s,
          false
        );
      e = h2;
    }
  }
  return null;
}
function Qd(i, e, t, n, s) {
  return !Wo(
    s,
    /**
     * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
     * @return {boolean} Contains (x, y).
     */
    function(o) {
      return !Pn(
        i,
        e,
        t,
        n,
        o[0],
        o[1]
      );
    }
  );
}
function Pn(i, e, t, n, s, r) {
  let o = 0, a = i[t - n], l = i[t - n + 1];
  for (; e < t; e += n) {
    const c = i[e], h2 = i[e + 1];
    l <= r ? h2 > r && (c - a) * (r - l) - (s - a) * (h2 - l) > 0 && o++ : h2 <= r && (c - a) * (r - l) - (s - a) * (h2 - l) < 0 && o--, a = c, l = h2;
  }
  return o !== 0;
}
function oa(i, e, t, n, s, r) {
  if (t.length === 0 || !Pn(i, e, t[0], n, s, r))
    return false;
  for (let o = 1, a = t.length; o < a; ++o)
    if (Pn(i, t[o - 1], t[o], n, s, r))
      return false;
  return true;
}
function ef(i, e, t, n, s, r) {
  if (t.length === 0)
    return false;
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = t[o];
    if (oa(i, e, l, n, s, r))
      return true;
    e = l[l.length - 1];
  }
  return false;
}
function Pc(i, e, t, n, s) {
  let r;
  for (e += n; e < t; e += n)
    if (r = s(
      i.slice(e - n, e),
      i.slice(e, e + n)
    ), r)
      return r;
  return false;
}
function wr(i, e, t, n, s, r) {
  return r = r ?? mc(_t(), i, e, t, n), $e(s, r) ? r[0] >= s[0] && r[2] <= s[2] || r[1] >= s[1] && r[3] <= s[3] ? true : Pc(
    i,
    e,
    t,
    n,
    /**
     * @param {import("../../coordinate.js").Coordinate} point1 Start point.
     * @param {import("../../coordinate.js").Coordinate} point2 End point.
     * @return {boolean} `true` if the segment and the extent intersect,
     *     `false` otherwise.
     */
    function(o, a) {
      return qu(s, o, a);
    }
  ) : false;
}
function tf(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r) {
    if (wr(i, e, t[r], n, s))
      return true;
    e = t[r];
  }
  return false;
}
function Ac(i, e, t, n, s) {
  return !!(wr(i, e, t, n, s) || Pn(
    i,
    e,
    t,
    n,
    s[0],
    s[1]
  ) || Pn(
    i,
    e,
    t,
    n,
    s[0],
    s[3]
  ) || Pn(
    i,
    e,
    t,
    n,
    s[2],
    s[1]
  ) || Pn(
    i,
    e,
    t,
    n,
    s[2],
    s[3]
  ));
}
function kc(i, e, t, n, s) {
  if (!Ac(i, e, t[0], n, s))
    return false;
  if (t.length === 1)
    return true;
  for (let r = 1, o = t.length; r < o; ++r)
    if (Qd(
      i,
      t[r - 1],
      t[r],
      n,
      s
    ) && !wr(
      i,
      t[r - 1],
      t[r],
      n,
      s
    ))
      return false;
  return true;
}
function nf(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    if (kc(i, e, a, n, s))
      return true;
    e = a[a.length - 1];
  }
  return false;
}
function aa(i, e, t, n) {
  let s = i[e], r = i[e + 1], o = 0;
  for (let a = e + n; a < t; a += n) {
    const l = i[a], c = i[a + 1];
    o += Math.sqrt((l - s) * (l - s) + (c - r) * (c - r)), s = l, r = c;
  }
  return o;
}
function Cr(i, e, t, n, s, r, o) {
  const a = (t - e) / n;
  if (a < 3) {
    for (; e < t; e += n)
      r[o++] = i[e], r[o++] = i[e + 1];
    return o;
  }
  const l = new Array(a);
  l[0] = 1, l[a - 1] = 1;
  const c = [e, t - n];
  let h2 = 0;
  for (; c.length > 0; ) {
    const u = c.pop(), d = c.pop();
    let f = 0;
    const g = i[d], y = i[d + 1], m = i[u], _ = i[u + 1];
    for (let p = d + n; p < u; p += n) {
      const E = i[p], S = i[p + 1], w = ed(E, S, g, y, m, _);
      w > f && (h2 = p, f = w);
    }
    f > s && (l[(h2 - e) / n] = 1, d + n < h2 && c.push(d, h2), h2 + n < u && c.push(h2, u));
  }
  for (let u = 0; u < a; ++u)
    l[u] && (r[o++] = i[e + u * n], r[o++] = i[e + u * n + 1]);
  return o;
}
function Oc(i, e, t, n, s, r, o, a) {
  for (let l = 0, c = t.length; l < c; ++l) {
    const h2 = t[l];
    o = Cr(
      i,
      e,
      h2,
      n,
      s,
      r,
      o
    ), a.push(o), e = h2;
  }
  return o;
}
function Ln(i, e) {
  return e * Math.round(i / e);
}
function sf(i, e, t, n, s, r, o) {
  if (e == t)
    return o;
  let a = Ln(i[e], s), l = Ln(i[e + 1], s);
  e += n, r[o++] = a, r[o++] = l;
  let c, h2;
  do
    if (c = Ln(i[e], s), h2 = Ln(i[e + 1], s), e += n, e == t)
      return r[o++] = c, r[o++] = h2, o;
  while (c == a && h2 == l);
  for (; e < t; ) {
    const u = Ln(i[e], s), d = Ln(i[e + 1], s);
    if (e += n, u == c && d == h2)
      continue;
    const f = c - a, g = h2 - l, y = u - a, m = d - l;
    if (f * m == g * y && (f < 0 && y < f || f == y || f > 0 && y > f) && (g < 0 && m < g || g == m || g > 0 && m > g)) {
      c = u, h2 = d;
      continue;
    }
    r[o++] = c, r[o++] = h2, a = c, l = h2, c = u, h2 = d;
  }
  return r[o++] = c, r[o++] = h2, o;
}
function la(i, e, t, n, s, r, o, a) {
  for (let l = 0, c = t.length; l < c; ++l) {
    const h2 = t[l];
    o = sf(
      i,
      e,
      h2,
      n,
      s,
      r,
      o
    ), a.push(o), e = h2;
  }
  return o;
}
function rf(i, e, t, n, s, r, o, a) {
  for (let l = 0, c = t.length; l < c; ++l) {
    const h2 = t[l], u = [];
    o = la(
      i,
      e,
      h2,
      n,
      s,
      r,
      o,
      u
    ), a.push(u), e = h2[h2.length - 1];
  }
  return o;
}
var qs = class _qs extends wn {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(e, t) {
    super(), this.flatMidpoint_ = null, this.flatMidpointRevision_ = -1, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, t !== void 0 && !Array.isArray(e[0]) ? this.setFlatCoordinates(
      t,
      /** @type {Array<number>} */
      e
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      e,
      t
    );
  }
  /**
   * Append the passed coordinate to the coordinates of the linestring.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @api
   */
  appendCoordinate(e) {
    At(this.flatCoordinates, e), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!LineString} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _qs(
      this.flatCoordinates.slice(),
      this.layout
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    return s < Bn(this.getExtent(), e, t) ? s : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      ta(
        this.flatCoordinates,
        0,
        this.flatCoordinates.length,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), ia(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      this.maxDelta_,
      false,
      e,
      t,
      n,
      s
    ));
  }
  /**
   * Iterate over each segment, calling the provided callback.
   * If the callback returns a truthy value the function returns that
   * value immediately. Otherwise the function returns `false`.
   *
   * @param {function(this: S, import("../coordinate.js").Coordinate, import("../coordinate.js").Coordinate): T} callback Function
   *     called for each segment. The function will receive two arguments, the start and end coordinates of the segment.
   * @return {T|boolean} Value.
   * @template T,S
   * @api
   */
  forEachSegment(e) {
    return Pc(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e
    );
  }
  /**
   * Returns the coordinate at `m` using linear interpolation, or `null` if no
   * such coordinate exists.
   *
   * `extrapolate` controls extrapolation beyond the range of Ms in the
   * MultiLineString. If `extrapolate` is `true` then Ms less than the first
   * M will return the first coordinate and Ms greater than the last M will
   * return the last coordinate.
   *
   * @param {number} m M.
   * @param {boolean} [extrapolate] Extrapolate. Default is `false`.
   * @return {import("../coordinate.js").Coordinate|null} Coordinate.
   * @api
   */
  getCoordinateAtM(e, t) {
    return this.layout != "XYM" && this.layout != "XYZM" ? null : (t = t !== void 0 ? t : false, Io(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e,
      t
    ));
  }
  /**
   * Return the coordinates of the linestring.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return hn(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the coordinate at the provided fraction along the linestring.
   * The `fraction` is a number between 0 and 1, where 0 is the start of the
   * linestring and 1 is the end.
   * @param {number} fraction Fraction.
   * @param {import("../coordinate.js").Coordinate} [dest] Optional coordinate whose values will
   *     be modified. If not provided, a new coordinate will be returned.
   * @return {import("../coordinate.js").Coordinate} Coordinate of the interpolated point.
   * @api
   */
  getCoordinateAt(e, t) {
    return Zs(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e,
      t,
      this.stride
    );
  }
  /**
   * Return the length of the linestring on projected plane.
   * @return {number} Length (on projected plane).
   * @api
   */
  getLength() {
    return aa(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * @return {Array<number>} Flat midpoint.
   */
  getFlatMidpoint() {
    return this.flatMidpointRevision_ != this.getRevision() && (this.flatMidpoint_ = this.getCoordinateAt(
      0.5,
      this.flatMidpoint_ ?? void 0
    ), this.flatMidpointRevision_ = this.getRevision()), /** @type {Array<number>} */
    this.flatMidpoint_;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {LineString} Simplified LineString.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(e) {
    const t = [];
    return t.length = Cr(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e,
      t,
      0
    ), new _qs(t, "XY");
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "LineString";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return wr(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e,
      this.getExtent()
    );
  }
  /**
   * Set the coordinates of the linestring.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = Er(
      this.flatCoordinates,
      0,
      e,
      this.stride
    ), this.changed();
  }
};
var tt = qs;
var ee = {
  IDLE: 0,
  LOADING: 1,
  LOADED: 2,
  ERROR: 3,
  EMPTY: 4
};
var Nn = typeof navigator < "u" && typeof navigator.userAgent < "u" ? navigator.userAgent.toLowerCase() : "";
var of = Nn.includes("safari") && !Nn.includes("chrom");
of && (Nn.includes("version/15.4") || /cpu (os|iphone os) 15_4 like mac os x/.test(Nn));
var af = Nn.includes("webkit") && !Nn.includes("edge");
var Nc = Nn.includes("macintosh");
var lf = typeof devicePixelRatio < "u" ? devicePixelRatio : 1;
var ft = typeof WorkerGlobalScope < "u" && typeof OffscreenCanvas < "u" && self instanceof WorkerGlobalScope;
var Uc = typeof Image < "u" && Image.prototype.decode;
var Gc = function() {
  let i = false;
  try {
    const e = Object.defineProperty({}, "passive", {
      get: function() {
        i = true;
      }
    });
    window.addEventListener("_", null, e), window.removeEventListener("_", null, e);
  } catch {
  }
  return i;
}();
function Fe(i, e, t, n) {
  let s;
  return t && t.length ? s = /** @type {HTMLCanvasElement} */
  t.shift() : ft ? s = new class extends OffscreenCanvas {
    constructor() {
      super(...arguments);
      Dr(this, "style", {});
    }
  }(i ?? 300, e ?? 150) : s = document.createElement("canvas"), i && (s.width = i), e && (s.height = e), /** @type {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} */
  s.getContext("2d", n);
}
var Or;
function Js() {
  return Or || (Or = Fe(1, 1)), Or;
}
function xr(i) {
  const e = i.canvas;
  e.width = 1, e.height = 1, i.clearRect(0, 0, 1, 1);
}
function cf(i) {
  let e = i.offsetWidth;
  const t = getComputedStyle(i);
  return e += parseInt(t.marginLeft, 10) + parseInt(t.marginRight, 10), e;
}
function hf(i) {
  let e = i.offsetHeight;
  const t = getComputedStyle(i);
  return e += parseInt(t.marginTop, 10) + parseInt(t.marginBottom, 10), e;
}
function ll(i, e) {
  const t = e.parentNode;
  t && t.replaceChild(i, e);
}
function Bc(i) {
  for (; i.lastChild; )
    i.lastChild.remove();
}
function uf(i, e) {
  const t = i.childNodes;
  for (let n = 0; ; ++n) {
    const s = t[n], r = e[n];
    if (!s && !r)
      break;
    if (s !== r) {
      if (!s) {
        i.appendChild(r);
        continue;
      }
      if (!r) {
        i.removeChild(s), --n;
        continue;
      }
      i.insertBefore(r, s);
    }
  }
}
function zc() {
  return new Proxy(
    {
      /**
       * @type {Array<HTMLElement>}
       */
      childNodes: [],
      /**
       * @param {HTMLElement} node html node.
       * @return {HTMLElement} html node.
       */
      appendChild: function(e) {
        return this.childNodes.push(e), e;
      },
      /**
       * dummy function, as this structure is not supposed to have a parent.
       */
      remove: function() {
      },
      /**
       * @param {HTMLElement} node html node.
       * @return {HTMLElement} html node.
       */
      removeChild: function(e) {
        const t = this.childNodes.indexOf(e);
        if (t === -1)
          throw new Error("Node to remove was not found");
        return this.childNodes.splice(t, 1), e;
      },
      /**
       * @param {HTMLElement} newNode new html node.
       * @param {HTMLElement} referenceNode reference html node.
       * @return {HTMLElement} new html node.
       */
      insertBefore: function(e, t) {
        const n = this.childNodes.indexOf(t);
        if (n === -1)
          throw new Error("Reference node not found");
        return this.childNodes.splice(n, 0, e), e;
      },
      style: {}
    },
    {
      get(e, t, n) {
        return t === "firstElementChild" ? e.childNodes.length > 0 ? e.childNodes[0] : null : Reflect.get(e, t, n);
      }
    }
  );
}
function un(i) {
  return typeof HTMLCanvasElement < "u" && i instanceof HTMLCanvasElement || typeof OffscreenCanvas < "u" && i instanceof OffscreenCanvas;
}
var ca = [NaN, NaN, NaN, 0];
var Nr;
function df() {
  return Nr || (Nr = Fe(1, 1, void 0, {
    willReadFrequently: true,
    desynchronized: true
  })), Nr;
}
var ff = /^rgba?\(\s*(\d+%?)\s+(\d+%?)\s+(\d+%?)(?:\s*\/\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i;
var gf = /^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)(?:\s*,\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i;
var mf = /^rgba?\(\s*(\d+%)\s*,\s*(\d+%)\s*,\s*(\d+%)(?:\s*,\s*(\d+%|\d*\.\d+|[01]))?\s*\)$/i;
var yf = /^#([\da-f]{3,4}|[\da-f]{6}|[\da-f]{8})$/i;
function Cs(i, e) {
  return i.endsWith("%") ? Number(i.substring(0, i.length - 1)) / e : Number(i);
}
function ji(i) {
  throw new Error('failed to parse "' + i + '" as color');
}
function Vc(i) {
  if (i.toLowerCase().startsWith("rgb")) {
    const r = i.match(gf) || i.match(ff) || i.match(mf);
    if (r) {
      const o = r[4], a = 100 / 255;
      return [
        xe(Cs(r[1], a) + 0.5 | 0, 0, 255),
        xe(Cs(r[2], a) + 0.5 | 0, 0, 255),
        xe(Cs(r[3], a) + 0.5 | 0, 0, 255),
        o !== void 0 ? xe(Cs(o, 100), 0, 1) : 1
      ];
    }
    ji(i);
  }
  if (i.startsWith("#")) {
    if (yf.test(i)) {
      const r = i.substring(1), o = r.length <= 4 ? 1 : 2, a = [0, 0, 0, 255];
      for (let l = 0, c = r.length; l < c; l += o) {
        let h2 = parseInt(r.substring(l, l + o), 16);
        o === 1 && (h2 += h2 << 4), a[l / o] = h2;
      }
      return a[3] = a[3] / 255, a;
    }
    ji(i);
  }
  const e = df();
  e.fillStyle = "#abcdef";
  let t = e.fillStyle;
  e.fillStyle = i, e.fillStyle === t && (e.fillStyle = "#fedcba", t = e.fillStyle, e.fillStyle = i, e.fillStyle === t && ji(i));
  const n = e.fillStyle;
  if (n.startsWith("#") || n.startsWith("rgba"))
    return Vc(n);
  e.clearRect(0, 0, 1, 1), e.fillRect(0, 0, 1, 1);
  const s = Array.from(e.getImageData(0, 0, 1, 1).data);
  return s[3] = us(s[3] / 255, 3), s;
}
function _f(i) {
  return typeof i == "string" ? i : ua(i);
}
var pf = 1024;
var Di = {};
var Ur = 0;
function Sf(i) {
  if (i.length === 4)
    return i;
  const e = i.slice();
  return e[3] = 1, e;
}
function Gr(i) {
  return i > 31308e-7 ? Math.pow(i, 1 / 2.4) * 269.025 - 14.025 : i * 3294.6;
}
function Br(i) {
  return i > 0.2068965 ? Math.pow(i, 3) : (i - 4 / 29) * (108 / 841);
}
function zr(i) {
  return i > 10.314724 ? Math.pow((i + 14.025) / 269.025, 2.4) : i / 3294.6;
}
function Vr(i) {
  return i > 88564e-7 ? Math.pow(i, 1 / 3) : i / (108 / 841) + 4 / 29;
}
function cl(i) {
  const e = zr(i[0]), t = zr(i[1]), n = zr(i[2]), s = Vr(e * 0.222488403 + t * 0.716873169 + n * 0.06060791), r = 500 * (Vr(e * 0.452247074 + t * 0.399439023 + n * 0.148375274) - s), o = 200 * (s - Vr(e * 0.016863605 + t * 0.117638439 + n * 0.865350722)), a = Math.atan2(o, r) * (180 / Math.PI);
  return [
    116 * s - 16,
    Math.sqrt(r * r + o * o),
    a < 0 ? a + 360 : a,
    i[3]
  ];
}
function Ef(i) {
  const e = (i[0] + 16) / 116, t = i[1], n = i[2] * Math.PI / 180, s = Br(e), r = Br(e + t / 500 * Math.cos(n)), o = Br(e - t / 200 * Math.sin(n)), a = Gr(r * 3.021973625 - s * 1.617392459 - o * 0.404875592), l = Gr(r * -0.943766287 + s * 1.916279586 + o * 0.027607165), c = Gr(r * 0.069407491 - s * 0.22898585 + o * 1.159737864);
  return [
    xe(a + 0.5 | 0, 0, 255),
    xe(l + 0.5 | 0, 0, 255),
    xe(c + 0.5 | 0, 0, 255),
    i[3]
  ];
}
function ha(i) {
  if (i === "none")
    return ca;
  if (Di.hasOwnProperty(i))
    return Di[i];
  if (Ur >= pf) {
    let t = 0;
    for (const n in Di)
      t++ & 3 || (delete Di[n], --Ur);
  }
  const e = Vc(i);
  e.length !== 4 && ji(i);
  for (const t of e)
    isNaN(t) && ji(i);
  return Di[i] = e, ++Ur, e;
}
function Ei(i) {
  return Array.isArray(i) ? i : ha(i);
}
function ua(i) {
  let e = i[0];
  e != (e | 0) && (e = e + 0.5 | 0);
  let t = i[1];
  t != (t | 0) && (t = t + 0.5 | 0);
  let n = i[2];
  n != (n | 0) && (n = n + 0.5 | 0);
  const s = i[3] === void 0 ? 1 : Math.round(i[3] * 1e3) / 1e3;
  return "rgba(" + e + "," + t + "," + n + "," + s + ")";
}
function wf(i, e, t) {
  const n = (
    /** @type {HTMLImageElement} */
    i
  );
  let s = true, r = false, o = false;
  const a = [
    Xs(n, Z.LOAD, function() {
      o = true, r || e();
    })
  ];
  return n.src && Uc ? (r = true, n.decode().then(function() {
    s && e();
  }).catch(function(l) {
    s && (o ? e() : t());
  })) : a.push(Xs(n, Z.ERROR, t)), function() {
    s = false, a.forEach(Ee);
  };
}
function Cf(i, e) {
  return new Promise((t, n) => {
    function s() {
      o(), t(i);
    }
    function r() {
      o(), n(new Error("Image load error"));
    }
    function o() {
      i.removeEventListener("load", s), i.removeEventListener("error", r);
    }
    i.addEventListener("load", s), i.addEventListener("error", r), e && (i.src = e);
  });
}
function xf(i, e) {
  return e && (i.src = e), i.src && Uc ? new Promise(
    (t, n) => i.decode().then(() => t(i)).catch(
      (s) => i.complete && i.width ? t(i) : n(s)
    )
  ) : Cf(i);
}
var If = class {
  constructor() {
    this.cache_ = {}, this.patternCache_ = {}, this.cacheSize_ = 0, this.maxCacheSize_ = 1024;
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    this.cache_ = {}, this.patternCache_ = {}, this.cacheSize_ = 0;
  }
  /**
   * @return {boolean} Can expire cache.
   */
  canExpireCache() {
    return this.cacheSize_ > this.maxCacheSize_;
  }
  /**
   * FIXME empty description for jsdoc
   */
  expire() {
    if (this.canExpireCache()) {
      let e = 0;
      for (const t in this.cache_) {
        const n = this.cache_[t];
        !(e++ & 3) && !n.hasListener() && (delete this.cache_[t], delete this.patternCache_[t], --this.cacheSize_);
      }
    }
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @return {import("./IconImage.js").default} Icon image.
   */
  get(e, t, n) {
    const s = Xr(e, t, n);
    return s in this.cache_ ? this.cache_[s] : null;
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @return {CanvasPattern} Icon image.
   */
  getPattern(e, t, n) {
    const s = Xr(e, t, n);
    return s in this.patternCache_ ? this.patternCache_[s] : null;
  }
  /**
   * @param {string} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../color.js").Color|string|null} color Color.
   * @param {import("./IconImage.js").default|null} iconImage Icon image.
   * @param {boolean} [pattern] Also cache a `'repeat'` pattern with this `iconImage`.
   */
  set(e, t, n, s, r) {
    const o = Xr(e, t, n), a = o in this.cache_;
    this.cache_[o] = s, r && (s.getImageState() === ee.IDLE && s.load(), s.getImageState() === ee.LOADING ? s.ready().then(() => {
      this.patternCache_[o] = Js().createPattern(
        s.getImage(1),
        "repeat"
      );
    }) : this.patternCache_[o] = Js().createPattern(
      s.getImage(1),
      "repeat"
    )), a || ++this.cacheSize_;
  }
  /**
   * Set the cache size of the icon cache. Default is `1024`. Change this value when
   * your map uses more than 1024 different icon images and you are not caching icon
   * styles on the application level.
   * @param {number} maxCacheSize Cache max size.
   * @api
   */
  setSize(e) {
    this.maxCacheSize_ = e, this.expire();
  }
};
function Xr(i, e, t) {
  const n = t ? Ei(t) : "null";
  return e + ":" + i + ":" + n;
}
var gt = new If();
var Pi = null;
var Xc = class extends mr {
  /**
   * @param {HTMLImageElement|HTMLCanvasElement|OffscreenCanvas|ImageBitmap|null} image Image.
   * @param {string|undefined} src Src.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("../ImageState.js").default|undefined} imageState Image state.
   * @param {import("../color.js").Color|string|null} color Color.
   */
  constructor(e, t, n, s, r) {
    super(), this.hitDetectionImage_ = null, this.image_ = e, this.crossOrigin_ = n, this.canvas_ = {}, this.color_ = r, this.imageState_ = s === void 0 ? ee.IDLE : s, this.size_ = e && e.width && e.height ? [e.width, e.height] : null, this.src_ = t, this.tainted_, this.ready_ = null;
  }
  /**
   * @private
   */
  initializeImage_() {
    this.image_ = new Image(), this.crossOrigin_ !== null && (this.image_.crossOrigin = this.crossOrigin_);
  }
  /**
   * @private
   * @return {boolean} The image canvas is tainted.
   */
  isTainted_() {
    if (this.tainted_ === void 0 && this.imageState_ === ee.LOADED) {
      Pi || (Pi = Fe(1, 1, void 0, {
        willReadFrequently: true
      })), Pi.drawImage(this.image_, 0, 0);
      try {
        Pi.getImageData(0, 0, 1, 1), this.tainted_ = false;
      } catch {
        Pi = null, this.tainted_ = true;
      }
    }
    return this.tainted_ === true;
  }
  /**
   * @private
   */
  dispatchChangeEvent_() {
    this.dispatchEvent(Z.CHANGE);
  }
  /**
   * @private
   */
  handleImageError_() {
    this.imageState_ = ee.ERROR, this.dispatchChangeEvent_();
  }
  /**
   * @private
   */
  handleImageLoad_() {
    this.imageState_ = ee.LOADED, this.size_ = [this.image_.width, this.image_.height], this.dispatchChangeEvent_();
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLImageElement|HTMLCanvasElement|OffscreenCanvas|ImageBitmap} Image or Canvas element or image bitmap.
   */
  getImage(e) {
    return this.image_ || this.initializeImage_(), this.replaceColor_(e), this.canvas_[e] ? this.canvas_[e] : this.image_;
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Image or Canvas element.
   */
  getPixelRatio(e) {
    return this.replaceColor_(e), this.canvas_[e] ? e : 1;
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   */
  getImageState() {
    return this.imageState_;
  }
  /**
   * @return {HTMLImageElement|HTMLCanvasElement|OffscreenCanvas|ImageBitmap} Image element.
   */
  getHitDetectionImage() {
    if (this.image_ || this.initializeImage_(), !this.hitDetectionImage_)
      if (this.isTainted_()) {
        const e = this.size_[0], t = this.size_[1], n = Fe(e, t);
        n.fillRect(0, 0, e, t), this.hitDetectionImage_ = n.canvas;
      } else
        this.hitDetectionImage_ = this.image_;
    return this.hitDetectionImage_;
  }
  /**
   * Get the size of the icon (in pixels).
   * @return {import("../size.js").Size} Image size.
   */
  getSize() {
    return this.size_;
  }
  /**
   * @return {string|undefined} Image src.
   */
  getSrc() {
    return this.src_;
  }
  /**
   * Load not yet loaded URI.
   */
  load() {
    if (this.imageState_ === ee.IDLE) {
      this.image_ || this.initializeImage_(), this.imageState_ = ee.LOADING;
      try {
        this.src_ !== void 0 && (this.image_.src = this.src_);
      } catch {
        this.handleImageError_();
      }
      this.image_ instanceof HTMLImageElement && xf(this.image_, this.src_).then((e) => {
        this.image_ = e, this.handleImageLoad_();
      }).catch(this.handleImageError_.bind(this));
    }
  }
  /**
   * @param {number} pixelRatio Pixel ratio.
   * @private
   */
  replaceColor_(e) {
    if (!this.color_ || this.canvas_[e] || this.imageState_ !== ee.LOADED)
      return;
    const t = this.image_, n = Fe(
      Math.ceil(t.width * e),
      Math.ceil(t.height * e)
    ), s = n.canvas;
    n.scale(e, e), n.drawImage(t, 0, 0), n.globalCompositeOperation = "multiply", n.fillStyle = _f(this.color_), n.fillRect(0, 0, s.width / e, s.height / e), n.globalCompositeOperation = "destination-in", n.drawImage(t, 0, 0), this.canvas_[e] = s;
  }
  /**
   * @return {Promise<void>} Promise that resolves when the image is loaded.
   */
  ready() {
    return this.ready_ || (this.ready_ = new Promise((e) => {
      if (this.imageState_ === ee.LOADED || this.imageState_ === ee.ERROR)
        e();
      else {
        const t = () => {
          (this.imageState_ === ee.LOADED || this.imageState_ === ee.ERROR) && (this.removeEventListener(Z.CHANGE, t), e());
        };
        this.addEventListener(Z.CHANGE, t);
      }
    })), this.ready_;
  }
};
function Qs(i, e, t, n, s, r) {
  let o = e === void 0 ? void 0 : gt.get(e, t, s);
  return o || (o = new Xc(
    i,
    i && "src" in i ? i.src || void 0 : e,
    t,
    n,
    s
  ), gt.set(e, t, s, o, r)), r && o && !gt.getPattern(e, t, s) && gt.set(e, t, s, o, r), o;
}
function Pt(i) {
  return i ? Array.isArray(i) ? ua(i) : typeof i == "object" && "src" in i ? vf(i) : i : null;
}
function vf(i) {
  if (!i.offset || !i.size)
    return gt.getPattern(i.src, "anonymous", i.color);
  const e = i.src + ":" + i.offset, t = gt.getPattern(
    e,
    void 0,
    i.color
  );
  if (t)
    return t;
  const n = gt.get(i.src, "anonymous", null);
  if (n.getImageState() !== ee.LOADED)
    return null;
  const s = Fe(
    i.size[0],
    i.size[1]
  );
  return s.drawImage(
    n.getImage(1),
    i.offset[0],
    i.offset[1],
    i.size[0],
    i.size[1],
    0,
    0,
    i.size[0],
    i.size[1]
  ), Qs(
    s.canvas,
    e,
    void 0,
    ee.LOADED,
    i.color,
    true
  ), gt.getPattern(e, void 0, i.color);
}
var xs = "ol-hidden";
var Mf = "ol-selectable";
var Ir = "ol-unselectable";
var da = "ol-control";
var hl = "ol-collapsed";
var Tf = new RegExp(
  [
    "^\\s*(?=(?:(?:[-a-z]+\\s*){0,2}(italic|oblique))?)",
    "(?=(?:(?:[-a-z]+\\s*){0,2}(small-caps))?)",
    "(?=(?:(?:[-a-z]+\\s*){0,2}(bold(?:er)?|lighter|[1-9]00 ))?)",
    "(?:(?:normal|\\1|\\2|\\3)\\s*){0,3}((?:xx?-)?",
    "(?:small|large)|medium|smaller|larger|[\\.\\d]+(?:\\%|in|[cem]m|ex|p[ctx]))",
    "(?:\\s*\\/\\s*(normal|[\\.\\d]+(?:\\%|in|[cem]m|ex|p[ctx])?))",
    `?\\s*([-,\\"\\'\\sa-z0-9]+?)\\s*$`
  ].join(""),
  "i"
);
var ul = [
  "style",
  "variant",
  "weight",
  "size",
  "lineHeight",
  "family"
];
var vo = {
  normal: 400,
  bold: 700
};
var Mo = function(i) {
  const e = i.match(Tf);
  if (!e)
    return null;
  const t = (
    /** @type {FontParameters} */
    {
      lineHeight: "normal",
      size: "1.2em",
      style: "normal",
      weight: "400",
      variant: "normal"
    }
  );
  for (let n = 0, s = ul.length; n < s; ++n) {
    const r = e[n + 1];
    r !== void 0 && (t[ul[n]] = typeof r == "string" ? r.trim() : r);
  }
  return isNaN(Number(t.weight)) && t.weight in vo && (t.weight = vo[t.weight]), t.families = t.family.split(/,\s?/).map((n) => n.trim().replace(/^['"]|['"]$/g, "")), t;
};
var Wc = "10px sans-serif";
var Ze = "#000";
var wi = "round";
var Kt = [];
var Ht = 0;
var Ci = "round";
var es = 10;
var ts = "#000";
var ns = "center";
var er = "middle";
var An = [0, 0, 0, 0];
var is = 1;
var ti = new vt();
var Ai = null;
var dl;
var To = {};
var bf = /* @__PURE__ */ new Set([
  "serif",
  "sans-serif",
  "monospace",
  "cursive",
  "fantasy",
  "system-ui",
  "ui-serif",
  "ui-sans-serif",
  "ui-monospace",
  "ui-rounded",
  "emoji",
  "math",
  "fangsong"
]);
function Rf(i, e, t) {
  return `${i} ${e} 16px "${t}"`;
}
var Lf = /* @__PURE__ */ function() {
  let e, t;
  async function n(r) {
    await t.ready;
    const o = await t.load(r);
    if (o.length === 0)
      return false;
    const a = Mo(r), l = a.families[0].toLowerCase(), c = a.weight;
    return o.some(
      /**
       * @param {import('../css.js').FontParameters} f Font.
       * @return {boolean} Font matches.
       */
      (h2) => {
        const u = h2.family.replace(/^['"]|['"]$/g, "").toLowerCase(), d = vo[h2.weight] || h2.weight;
        return u === l && h2.style === a.style && d == c;
      }
    );
  }
  async function s() {
    await t.ready;
    let r = true;
    const o = ti.getProperties(), a = Object.keys(o).filter(
      (l) => o[l] < 100
    );
    for (let l = a.length - 1; l >= 0; --l) {
      const c = a[l];
      let h2 = o[c];
      h2 < 100 && (await n(c) ? (os(To), ti.set(c, 100)) : (h2 += 10, ti.set(c, h2, true), h2 < 100 && (r = false)));
    }
    e = void 0, r || (e = setTimeout(s, 100));
  }
  return async function(r) {
    t || (t = ft ? self.fonts : document.fonts);
    const o = Mo(r);
    if (!o)
      return;
    const a = o.families;
    let l = false;
    for (const c of a) {
      if (bf.has(c))
        continue;
      const h2 = Rf(o.style, o.weight, c);
      ti.get(h2) === void 0 && (ti.set(h2, 0, true), l = true);
    }
    l && (clearTimeout(e), e = setTimeout(s, 100));
  };
}();
var Ff = /* @__PURE__ */ function() {
  let i;
  return function(e) {
    let t = To[e];
    if (t == null) {
      if (ft) {
        const n = Mo(e), s = Yc(e, "Žg");
        t = (isNaN(Number(n.lineHeight)) ? 1.2 : Number(n.lineHeight)) * (s.actualBoundingBoxAscent + s.actualBoundingBoxDescent);
      } else
        i || (i = document.createElement("div"), i.innerHTML = "M", i.style.minHeight = "0", i.style.maxHeight = "none", i.style.height = "auto", i.style.padding = "0", i.style.border = "none", i.style.position = "absolute", i.style.display = "block", i.style.left = "-99999px"), i.style.font = e, document.body.appendChild(i), t = i.offsetHeight, document.body.removeChild(i);
      To[e] = t;
    }
    return t;
  };
}();
function Yc(i, e) {
  return Ai || (Ai = Fe(1, 1)), i != dl && (Ai.font = i, dl = Ai.font), Ai.measureText(e);
}
function jc(i, e) {
  return Yc(i, e).width;
}
function fl(i, e, t) {
  if (e in t)
    return t[e];
  const n = e.split(`
`).reduce((s, r) => Math.max(s, jc(i, r)), 0);
  return t[e] = n, n;
}
function Df(i, e) {
  const t = [], n = [], s = [];
  let r = 0, o = 0, a = 0, l = 0;
  for (let c = 0, h2 = e.length; c <= h2; c += 2) {
    const u = e[c];
    if (u === `
` || c === h2) {
      r = Math.max(r, o), s.push(o), o = 0, a += l, l = 0;
      continue;
    }
    const d = e[c + 1] || i.font, f = jc(d, u);
    t.push(f), o += f;
    const g = Ff(d);
    n.push(g), l = Math.max(l, g);
  }
  return { width: r, height: a, widths: t, heights: n, lineWidths: s };
}
function Pf(i, e, t, n, s, r, o, a, l, c, h2) {
  i.save(), t !== 1 && (i.globalAlpha === void 0 ? i.globalAlpha = (u) => u.globalAlpha *= t : i.globalAlpha *= t), e && i.transform.apply(i, e), /** @type {*} */
  n.contextInstructions ? (i.translate(l, c), i.scale(h2[0], h2[1]), Af(
    /** @type {Label} */
    n,
    i
  )) : h2[0] < 0 || h2[1] < 0 ? (i.translate(l, c), i.scale(h2[0], h2[1]), i.drawImage(
    /** @type {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} */
    n,
    s,
    r,
    o,
    a,
    0,
    0,
    o,
    a
  )) : i.drawImage(
    /** @type {HTMLCanvasElement|HTMLImageElement|HTMLVideoElement} */
    n,
    s,
    r,
    o,
    a,
    l,
    c,
    o * h2[0],
    a * h2[1]
  ), i.restore();
}
function Af(i, e) {
  const t = i.contextInstructions;
  for (let n = 0, s = t.length; n < s; n += 2)
    Array.isArray(t[n + 1]) ? e[t[n]].apply(
      e,
      t[n + 1]
    ) : e[t[n]] = t[n + 1];
}
function gl(i) {
  return i[0] > 0 && i[1] > 0;
}
function kf(i, e, t) {
  return t === void 0 && (t = [0, 0]), t[0] = i[0] * e + 0.5 | 0, t[1] = i[1] * e + 0.5 | 0, t;
}
function et(i, e) {
  return Array.isArray(i) ? i : (e === void 0 ? e = [i, i] : (e[0] = i, e[1] = i), e);
}
var fa = class _fa {
  /**
   * @param {Options} options Options.
   */
  constructor(e) {
    this.opacity_ = e.opacity, this.rotateWithView_ = e.rotateWithView, this.rotation_ = e.rotation, this.scale_ = e.scale, this.scaleArray_ = et(e.scale), this.displacement_ = e.displacement, this.declutterMode_ = e.declutterMode;
  }
  /**
   * Clones the style.
   * @return {ImageStyle} The cloned style.
   * @api
   */
  clone() {
    const e = this.getScale();
    return new _fa({
      opacity: this.getOpacity(),
      scale: Array.isArray(e) ? e.slice() : e,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the symbolizer opacity.
   * @return {number} Opacity.
   * @api
   */
  getOpacity() {
    return this.opacity_;
  }
  /**
   * Determine whether the symbolizer rotates with the map.
   * @return {boolean} Rotate with map.
   * @api
   */
  getRotateWithView() {
    return this.rotateWithView_;
  }
  /**
   * Get the symoblizer rotation.
   * @return {number} Rotation.
   * @api
   */
  getRotation() {
    return this.rotation_;
  }
  /**
   * Get the symbolizer scale.
   * @return {number|import("../size.js").Size} Scale.
   * @api
   */
  getScale() {
    return this.scale_;
  }
  /**
   * Get the symbolizer scale array.
   * @return {import("../size.js").Size} Scale array.
   */
  getScaleArray() {
    return this.scaleArray_;
  }
  /**
   * Get the displacement of the shape
   * @return {Array<number>} Shape's center displacement
   * @api
   */
  getDisplacement() {
    return this.displacement_;
  }
  /**
   * Get the declutter mode of the shape
   * @return {import("./Style.js").DeclutterMode} Shape's declutter mode
   * @api
   */
  getDeclutterMode() {
    return this.declutterMode_;
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @abstract
   * @return {Array<number>} Anchor.
   */
  getAnchor() {
    return ue();
  }
  /**
   * Get the image element for the symbolizer.
   * @abstract
   * @param {number} pixelRatio Pixel ratio.
   * @return {import('../DataTile.js').ImageLike} Image element.
   */
  getImage(e) {
    return ue();
  }
  /**
   * @abstract
   * @return {import('../DataTile.js').ImageLike} Image element.
   */
  getHitDetectionImage() {
    return ue();
  }
  /**
   * Get the image pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Pixel ratio.
   */
  getPixelRatio(e) {
    return 1;
  }
  /**
   * @abstract
   * @return {import("../ImageState.js").default} Image state.
   */
  getImageState() {
    return ue();
  }
  /**
   * @abstract
   * @return {import("../size.js").Size} Image size.
   */
  getImageSize() {
    return ue();
  }
  /**
   * Get the origin of the symbolizer.
   * @abstract
   * @return {Array<number>} Origin.
   */
  getOrigin() {
    return ue();
  }
  /**
   * Get the size of the symbolizer (in pixels).
   * @abstract
   * @return {import("../size.js").Size} Size.
   */
  getSize() {
    return ue();
  }
  /**
   * Set the displacement.
   *
   * @param {Array<number>} displacement Displacement.
   * @api
   */
  setDisplacement(e) {
    this.displacement_ = e;
  }
  /**
   * Set the opacity.
   *
   * @param {number} opacity Opacity.
   * @api
   */
  setOpacity(e) {
    this.opacity_ = e;
  }
  /**
   * Set whether to rotate the style with the view.
   *
   * @param {boolean} rotateWithView Rotate with map.
   * @api
   */
  setRotateWithView(e) {
    this.rotateWithView_ = e;
  }
  /**
   * Set the rotation.
   *
   * @param {number} rotation Rotation.
   * @api
   */
  setRotation(e) {
    this.rotation_ = e;
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size} scale Scale.
   * @api
   */
  setScale(e) {
    this.scale_ = e, this.scaleArray_ = et(e);
  }
  /**
   * @abstract
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   */
  listenImageChange(e) {
    ue();
  }
  /**
   * Load not yet loaded URI.
   * @abstract
   */
  load() {
    ue();
  }
  /**
   * @abstract
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   */
  unlistenImageChange(e) {
    ue();
  }
  /**
   * @return {Promise<void>} `false` or Promise that resolves when the style is ready to use.
   */
  ready() {
    return Promise.resolve();
  }
};
var $c = fa;
var ga = class _ga extends $c {
  /**
   * @param {Options} options Options.
   */
  constructor(e) {
    super({
      opacity: 1,
      rotateWithView: e.rotateWithView !== void 0 ? e.rotateWithView : false,
      rotation: e.rotation !== void 0 ? e.rotation : 0,
      scale: e.scale !== void 0 ? e.scale : 1,
      displacement: e.displacement !== void 0 ? e.displacement : [0, 0],
      declutterMode: e.declutterMode
    }), this.hitDetectionCanvas_ = null, this.fill_ = e.fill !== void 0 ? e.fill : null, this.origin_ = [0, 0], this.points_ = e.points, this.radius = e.radius, this.radius2_ = e.radius2, this.angle_ = e.angle !== void 0 ? e.angle : 0, this.stroke_ = e.stroke !== void 0 ? e.stroke : null, this.size_, this.renderOptions_, this.imageState_ = this.fill_ && this.fill_.loading() ? ee.LOADING : ee.LOADED, this.imageState_ === ee.LOADING && this.ready().then(() => this.imageState_ = ee.LOADED), this.render();
  }
  /**
   * Clones the style.
   * @return {RegularShape} The cloned style.
   * @api
   * @override
   */
  clone() {
    const e = this.getScale(), t = new _ga({
      fill: this.getFill() ? this.getFill().clone() : void 0,
      points: this.getPoints(),
      radius: this.getRadius(),
      radius2: this.getRadius2(),
      angle: this.getAngle(),
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      scale: Array.isArray(e) ? e.slice() : e,
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
    return t.setOpacity(this.getOpacity()), t;
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @return {Array<number>} Anchor.
   * @api
   * @override
   */
  getAnchor() {
    const e = this.size_, t = this.getDisplacement(), n = this.getScaleArray();
    return [
      e[0] / 2 - t[0] / n[0],
      e[1] / 2 + t[1] / n[1]
    ];
  }
  /**
   * Get the angle used in generating the shape.
   * @return {number} Shape's rotation in radians.
   * @api
   */
  getAngle() {
    return this.angle_;
  }
  /**
   * Get the fill style for the shape.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Set the fill style.
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(e) {
    this.fill_ = e, this.render();
  }
  /**
   * @return {HTMLCanvasElement|OffscreenCanvas} Image element.
   * @override
   */
  getHitDetectionImage() {
    return this.hitDetectionCanvas_ || (this.hitDetectionCanvas_ = this.createHitDetectionCanvas_(
      this.renderOptions_
    )), this.hitDetectionCanvas_;
  }
  /**
   * Get the image icon.
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLCanvasElement|OffscreenCanvas} Image or Canvas element.
   * @api
   * @override
   */
  getImage(e) {
    var r, o;
    const t = (r = this.fill_) == null ? void 0 : r.getKey(), n = `${e},${this.angle_},${this.radius},${this.radius2_},${this.points_},${t}` + Object.values(this.renderOptions_).join(",");
    let s = (
      /** @type {HTMLCanvasElement|OffscreenCanvas} */
      (o = gt.get(n, null, null)) == null ? void 0 : o.getImage(1)
    );
    if (!s) {
      const a = this.renderOptions_, l = Math.ceil(a.size * e), c = Fe(l, l);
      this.draw_(a, c, e), s = c.canvas, gt.set(
        n,
        null,
        null,
        new Xc(s, void 0, null, ee.LOADED, null)
      );
    }
    return s;
  }
  /**
   * Get the image pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Pixel ratio.
   * @override
   */
  getPixelRatio(e) {
    return e;
  }
  /**
   * @return {import("../size.js").Size} Image size.
   * @override
   */
  getImageSize() {
    return this.size_;
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   * @override
   */
  getImageState() {
    return this.imageState_;
  }
  /**
   * Get the origin of the symbolizer.
   * @return {Array<number>} Origin.
   * @api
   * @override
   */
  getOrigin() {
    return this.origin_;
  }
  /**
   * Get the number of points for generating the shape.
   * @return {number} Number of points for stars and regular polygons.
   * @api
   */
  getPoints() {
    return this.points_;
  }
  /**
   * Get the (primary) radius for the shape.
   * @return {number} Radius.
   * @api
   */
  getRadius() {
    return this.radius;
  }
  /**
   * Get the secondary radius for the shape.
   * @return {number|undefined} Radius2.
   * @api
   */
  getRadius2() {
    return this.radius2_;
  }
  /**
   * Get the size of the symbolizer (in pixels).
   * @return {import("../size.js").Size} Size.
   * @api
   * @override
   */
  getSize() {
    return this.size_;
  }
  /**
   * Get the stroke style for the shape.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Set the stroke style.
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(e) {
    this.stroke_ = e, this.render();
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  listenImageChange(e) {
  }
  /**
   * Load not yet loaded URI.
   * @override
   */
  load() {
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  unlistenImageChange(e) {
  }
  /**
   * Calculate additional canvas size needed for the miter.
   * @param {string} lineJoin Line join
   * @param {number} strokeWidth Stroke width
   * @param {number} miterLimit Miter limit
   * @return {number} Additional canvas size needed
   * @private
   */
  calculateLineJoinSize_(e, t, n) {
    if (t === 0 || this.points_ === 1 / 0 || e !== "bevel" && e !== "miter")
      return t;
    let s = this.radius, r = this.radius2_ === void 0 ? s : this.radius2_;
    if (s < r) {
      const x = s;
      s = r, r = x;
    }
    const o = this.radius2_ === void 0 ? this.points_ : this.points_ * 2, a = 2 * Math.PI / o, l = r * Math.sin(a), c = Math.sqrt(r * r - l * l), h2 = s - c, u = Math.sqrt(l * l + h2 * h2), d = u / l;
    if (e === "miter" && d <= n)
      return d * t;
    const f = t / 2 / d, g = t / 2 * (h2 / u), m = Math.sqrt((s + f) * (s + f) + g * g) - s;
    if (this.radius2_ === void 0 || e === "bevel")
      return m * 2;
    const _ = s * Math.sin(a), p = Math.sqrt(s * s - _ * _), E = r - p, w = Math.sqrt(_ * _ + E * E) / _;
    if (w <= n) {
      const x = w * t / 2 - r - s;
      return 2 * Math.max(m, x);
    }
    return m * 2;
  }
  /**
   * @return {RenderOptions}  The render options
   * @protected
   */
  createRenderOptions() {
    let e = wi, t = Ci, n = 0, s = null, r = 0, o, a = 0;
    this.stroke_ && (o = Pt(this.stroke_.getColor() ?? ts), a = this.stroke_.getWidth() ?? is, s = this.stroke_.getLineDash(), r = this.stroke_.getLineDashOffset() ?? 0, t = this.stroke_.getLineJoin() ?? Ci, e = this.stroke_.getLineCap() ?? wi, n = this.stroke_.getMiterLimit() ?? es);
    const l = this.calculateLineJoinSize_(t, a, n), c = Math.max(this.radius, this.radius2_ || 0), h2 = Math.ceil(2 * c + l);
    return {
      strokeStyle: o,
      strokeWidth: a,
      size: h2,
      lineCap: e,
      lineDash: s,
      lineDashOffset: r,
      lineJoin: t,
      miterLimit: n
    };
  }
  /**
   * @protected
   */
  render() {
    this.renderOptions_ = this.createRenderOptions();
    const e = this.renderOptions_.size;
    this.hitDetectionCanvas_ = null, this.size_ = [e, e];
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context The rendering context.
   * @param {number} pixelRatio The pixel ratio.
   */
  draw_(e, t, n) {
    if (t.scale(n, n), t.translate(e.size / 2, e.size / 2), this.createPath_(t), this.fill_) {
      let s = this.fill_.getColor();
      s === null && (s = Ze), t.fillStyle = Pt(s), t.fill();
    }
    e.strokeStyle && (t.strokeStyle = e.strokeStyle, t.lineWidth = e.strokeWidth, e.lineDash && (t.setLineDash(e.lineDash), t.lineDashOffset = e.lineDashOffset), t.lineCap = e.lineCap, t.lineJoin = e.lineJoin, t.miterLimit = e.miterLimit, t.stroke());
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @return {HTMLCanvasElement|OffscreenCanvas} Canvas containing the icon
   */
  createHitDetectionCanvas_(e) {
    let t;
    if (this.fill_) {
      let n = this.fill_.getColor(), s = 0;
      typeof n == "string" && (n = Ei(n)), n === null ? s = 1 : Array.isArray(n) && (s = n.length === 4 ? n[3] : 1), s === 0 && (t = Fe(e.size, e.size), this.drawHitDetectionCanvas_(e, t));
    }
    return t ? t.canvas : this.getImage(1);
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context The context to draw in.
   */
  createPath_(e) {
    let t = this.points_;
    const n = this.radius;
    if (t === 1 / 0)
      e.arc(0, 0, n, 0, 2 * Math.PI);
    else {
      const s = this.radius2_ === void 0 ? n : this.radius2_;
      this.radius2_ !== void 0 && (t *= 2);
      const r = this.angle_ - Math.PI / 2, o = 2 * Math.PI / t;
      for (let a = 0; a < t; a++) {
        const l = r + a * o, c = a % 2 === 0 ? n : s;
        e.lineTo(c * Math.cos(l), c * Math.sin(l));
      }
      e.closePath();
    }
  }
  /**
   * @private
   * @param {RenderOptions} renderOptions Render options.
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context The context.
   */
  drawHitDetectionCanvas_(e, t) {
    t.translate(e.size / 2, e.size / 2), this.createPath_(t), t.fillStyle = Ze, t.fill(), e.strokeStyle && (t.strokeStyle = e.strokeStyle, t.lineWidth = e.strokeWidth, e.lineDash && (t.setLineDash(e.lineDash), t.lineDashOffset = e.lineDashOffset), t.lineJoin = e.lineJoin, t.miterLimit = e.miterLimit, t.stroke());
  }
  /**
   * @override
   */
  ready() {
    return this.fill_ ? this.fill_.ready() : Promise.resolve();
  }
};
var fs = ga;
var ma = class _ma extends fs {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || { radius: 5 }, super({
      points: 1 / 0,
      fill: e.fill,
      radius: e.radius,
      stroke: e.stroke,
      scale: e.scale !== void 0 ? e.scale : 1,
      rotation: e.rotation !== void 0 ? e.rotation : 0,
      rotateWithView: e.rotateWithView !== void 0 ? e.rotateWithView : false,
      displacement: e.displacement !== void 0 ? e.displacement : [0, 0],
      declutterMode: e.declutterMode
    });
  }
  /**
   * Clones the style.
   * @return {CircleStyle} The cloned style.
   * @api
   * @override
   */
  clone() {
    const e = this.getScale(), t = new _ma({
      fill: this.getFill() ? this.getFill().clone() : void 0,
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      radius: this.getRadius(),
      scale: Array.isArray(e) ? e.slice() : e,
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
    return t.setOpacity(this.getOpacity()), t;
  }
  /**
   * Set the circle radius.
   *
   * @param {number} radius Circle radius.
   * @api
   */
  setRadius(e) {
    this.radius = e, this.render();
  }
};
var we = ma;
var ya = class _ya {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, this.patternImage_ = null, this.color_ = null, e.color !== void 0 && this.setColor(e.color);
  }
  /**
   * Clones the style. The color is not cloned if it is a {@link module:ol/colorlike~ColorLike}.
   * @return {Fill} The cloned style.
   * @api
   */
  clone() {
    const e = this.getColor();
    return new _ya({
      color: Array.isArray(e) ? e.slice() : e || void 0
    });
  }
  /**
   * Get the fill color.
   * @return {import("../color.js").Color|import("../colorlike.js").ColorLike|import('../colorlike.js').PatternDescriptor|null} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Set the color.
   *
   * @param {import("../color.js").Color|import("../colorlike.js").ColorLike|import('../colorlike.js').PatternDescriptor|null} color Color.
   * @api
   */
  setColor(e) {
    if (e !== null && typeof e == "object" && "src" in e) {
      const t = Qs(
        null,
        e.src,
        "anonymous",
        void 0,
        e.offset ? null : e.color ? e.color : null,
        !(e.offset && e.size)
      );
      t.ready().then(() => {
        this.patternImage_ = null;
      }), t.getImageState() === ee.IDLE && t.load(), t.getImageState() === ee.LOADING && (this.patternImage_ = t);
    }
    this.color_ = e;
  }
  /**
   * @return {string} Key of the fill for cache lookup.
   */
  getKey() {
    const e = this.getColor();
    return e ? e instanceof CanvasPattern || e instanceof CanvasGradient ? de(e) : typeof e == "object" && "src" in e ? e.src + ":" + e.offset : Ei(e).toString() : "";
  }
  /**
   * @return {boolean} The fill style is loading an image pattern.
   */
  loading() {
    return !!this.patternImage_;
  }
  /**
   * @return {Promise<void>} `false` or a promise that resolves when the style is ready to use.
   */
  ready() {
    return this.patternImage_ ? this.patternImage_.ready() : Promise.resolve();
  }
};
var J = ya;
function ml(i, e, t, n) {
  return t !== void 0 && n !== void 0 ? [t / i, n / e] : t !== void 0 ? t / i : n !== void 0 ? n / e : 1;
}
var _a = class __a extends $c {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {};
    const t = e.opacity !== void 0 ? e.opacity : 1, n = e.rotation !== void 0 ? e.rotation : 0, s = e.scale !== void 0 ? e.scale : 1, r = e.rotateWithView !== void 0 ? e.rotateWithView : false;
    super({
      opacity: t,
      rotation: n,
      scale: s,
      displacement: e.displacement !== void 0 ? e.displacement : [0, 0],
      rotateWithView: r,
      declutterMode: e.declutterMode
    }), this.anchor_ = e.anchor !== void 0 ? e.anchor : [0.5, 0.5], this.normalizedAnchor_ = null, this.anchorOrigin_ = e.anchorOrigin !== void 0 ? e.anchorOrigin : "top-left", this.anchorXUnits_ = e.anchorXUnits !== void 0 ? e.anchorXUnits : "fraction", this.anchorYUnits_ = e.anchorYUnits !== void 0 ? e.anchorYUnits : "fraction", this.crossOrigin_ = e.crossOrigin !== void 0 ? e.crossOrigin : null;
    const o = e.img !== void 0 ? e.img : null;
    let a = e.src;
    _e(
      !(a !== void 0 && o),
      "`image` and `src` cannot be provided at the same time"
    ), (a === void 0 || a.length === 0) && o && (a = /** @type {HTMLImageElement} */
    o.src || de(o)), _e(
      a !== void 0 && a.length > 0,
      "A defined and non-empty `src` or `image` must be provided"
    ), _e(
      !((e.width !== void 0 || e.height !== void 0) && e.scale !== void 0),
      "`width` or `height` cannot be provided together with `scale`"
    );
    let l;
    if (e.src !== void 0 ? l = ee.IDLE : o !== void 0 && ("complete" in o ? o.complete ? l = o.src ? ee.LOADED : ee.IDLE : l = ee.LOADING : l = ee.LOADED), this.color_ = e.color !== void 0 ? Ei(e.color) : null, this.iconImage_ = Qs(
      o,
      /** @type {string} */
      a,
      this.crossOrigin_,
      l,
      this.color_
    ), this.offset_ = e.offset !== void 0 ? e.offset : [0, 0], this.offsetOrigin_ = e.offsetOrigin !== void 0 ? e.offsetOrigin : "top-left", this.origin_ = null, this.size_ = e.size !== void 0 ? e.size : null, this.initialOptions_, e.width !== void 0 || e.height !== void 0) {
      let c, h2;
      if (e.size)
        [c, h2] = e.size;
      else {
        const u = this.getImage(1);
        if (u.width && u.height)
          c = u.width, h2 = u.height;
        else if (u instanceof HTMLImageElement) {
          this.initialOptions_ = e;
          const d = () => {
            if (this.unlistenImageChange(d), !this.initialOptions_)
              return;
            const f = this.iconImage_.getSize();
            this.setScale(
              ml(
                f[0],
                f[1],
                e.width,
                e.height
              )
            );
          };
          this.listenImageChange(d);
          return;
        }
      }
      c !== void 0 && this.setScale(
        ml(c, h2, e.width, e.height)
      );
    }
  }
  /**
   * Clones the style. The underlying Image/HTMLCanvasElement is not cloned.
   * @return {Icon} The cloned style.
   * @api
   * @override
   */
  clone() {
    let e, t, n;
    return this.initialOptions_ ? (t = this.initialOptions_.width, n = this.initialOptions_.height) : (e = this.getScale(), e = Array.isArray(e) ? e.slice() : e), new __a({
      anchor: this.anchor_.slice(),
      anchorOrigin: this.anchorOrigin_,
      anchorXUnits: this.anchorXUnits_,
      anchorYUnits: this.anchorYUnits_,
      color: this.color_ && this.color_.slice ? this.color_.slice() : this.color_ || void 0,
      crossOrigin: this.crossOrigin_,
      offset: this.offset_.slice(),
      offsetOrigin: this.offsetOrigin_,
      opacity: this.getOpacity(),
      rotateWithView: this.getRotateWithView(),
      rotation: this.getRotation(),
      scale: e,
      width: t,
      height: n,
      size: this.size_ !== null ? this.size_.slice() : void 0,
      src: this.getSrc(),
      displacement: this.getDisplacement().slice(),
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the anchor point in pixels. The anchor determines the center point for the
   * symbolizer.
   * @return {Array<number>} Anchor.
   * @api
   * @override
   */
  getAnchor() {
    let e = this.normalizedAnchor_;
    if (!e) {
      e = this.anchor_;
      const s = this.getSize();
      if (this.anchorXUnits_ == "fraction" || this.anchorYUnits_ == "fraction") {
        if (!s)
          return null;
        e = this.anchor_.slice(), this.anchorXUnits_ == "fraction" && (e[0] *= s[0]), this.anchorYUnits_ == "fraction" && (e[1] *= s[1]);
      }
      if (this.anchorOrigin_ != "top-left") {
        if (!s)
          return null;
        e === this.anchor_ && (e = this.anchor_.slice()), (this.anchorOrigin_ == "top-right" || this.anchorOrigin_ == "bottom-right") && (e[0] = -e[0] + s[0]), (this.anchorOrigin_ == "bottom-left" || this.anchorOrigin_ == "bottom-right") && (e[1] = -e[1] + s[1]);
      }
      this.normalizedAnchor_ = e;
    }
    const t = this.getDisplacement(), n = this.getScaleArray();
    return [
      e[0] - t[0] / n[0],
      e[1] + t[1] / n[1]
    ];
  }
  /**
   * Set the anchor point. The anchor determines the center point for the
   * symbolizer.
   *
   * @param {Array<number>} anchor Anchor.
   * @api
   */
  setAnchor(e) {
    this.anchor_ = e, this.normalizedAnchor_ = null;
  }
  /**
   * Get the icon color.
   * @return {import("../color.js").Color} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Get the image icon.
   * @param {number} pixelRatio Pixel ratio.
   * @return {HTMLImageElement|HTMLCanvasElement|OffscreenCanvas|ImageBitmap} Image or Canvas element. If the Icon
   * style was configured with `src` or with a not let loaded `img`, an `ImageBitmap` will be returned.
   * @api
   * @override
   */
  getImage(e) {
    return this.iconImage_.getImage(e);
  }
  /**
   * Get the pixel ratio.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} The pixel ratio of the image.
   * @api
   * @override
   */
  getPixelRatio(e) {
    return this.iconImage_.getPixelRatio(e);
  }
  /**
   * @return {import("../size.js").Size} Image size.
   * @override
   */
  getImageSize() {
    return this.iconImage_.getSize();
  }
  /**
   * @return {import("../ImageState.js").default} Image state.
   * @override
   */
  getImageState() {
    return this.iconImage_.getImageState();
  }
  /**
   * @return {HTMLImageElement|HTMLCanvasElement|OffscreenCanvas|ImageBitmap} Image element.
   * @override
   */
  getHitDetectionImage() {
    return this.iconImage_.getHitDetectionImage();
  }
  /**
   * Get the origin of the symbolizer.
   * @return {Array<number>} Origin.
   * @api
   * @override
   */
  getOrigin() {
    if (this.origin_)
      return this.origin_;
    let e = this.offset_;
    if (this.offsetOrigin_ != "top-left") {
      const t = this.getSize(), n = this.iconImage_.getSize();
      if (!t || !n)
        return null;
      e = e.slice(), (this.offsetOrigin_ == "top-right" || this.offsetOrigin_ == "bottom-right") && (e[0] = n[0] - t[0] - e[0]), (this.offsetOrigin_ == "bottom-left" || this.offsetOrigin_ == "bottom-right") && (e[1] = n[1] - t[1] - e[1]);
    }
    return this.origin_ = e, this.origin_;
  }
  /**
   * Get the image URL.
   * @return {string|undefined} Image src.
   * @api
   */
  getSrc() {
    return this.iconImage_.getSrc();
  }
  /**
   * Set the image URI
   * @param {string} src Image source URI
   * @api
   */
  setSrc(e) {
    this.iconImage_ = Qs(
      null,
      e,
      this.crossOrigin_,
      ee.IDLE,
      this.color_
    );
  }
  /**
   * Get the size of the icon (in pixels).
   * @return {import("../size.js").Size} Image size.
   * @api
   * @override
   */
  getSize() {
    return this.size_ ? this.size_ : this.iconImage_.getSize();
  }
  /**
   * Get the width of the icon (in pixels). Will return undefined when the icon image is not yet loaded.
   * @return {number} Icon width (in pixels).
   * @api
   */
  getWidth() {
    const e = this.getScaleArray();
    if (this.size_)
      return this.size_[0] * e[0];
    if (this.iconImage_.getImageState() == ee.LOADED)
      return this.iconImage_.getSize()[0] * e[0];
  }
  /**
   * Get the height of the icon (in pixels). Will return undefined when the icon image is not yet loaded.
   * @return {number} Icon height (in pixels).
   * @api
   */
  getHeight() {
    const e = this.getScaleArray();
    if (this.size_)
      return this.size_[1] * e[1];
    if (this.iconImage_.getImageState() == ee.LOADED)
      return this.iconImage_.getSize()[1] * e[1];
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size} scale Scale.
   * @api
   * @override
   */
  setScale(e) {
    delete this.initialOptions_, super.setScale(e);
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  listenImageChange(e) {
    this.iconImage_.addEventListener(Z.CHANGE, e);
  }
  /**
   * Load not yet loaded URI.
   * When rendering a feature with an icon style, the vector renderer will
   * automatically call this method. However, you might want to call this
   * method yourself for preloading or other purposes.
   * @api
   * @override
   */
  load() {
    this.iconImage_.load();
  }
  /**
   * @param {function(import("../events/Event.js").default): void} listener Listener function.
   * @override
   */
  unlistenImageChange(e) {
    this.iconImage_.removeEventListener(Z.CHANGE, e);
  }
  /**
   * @override
   */
  ready() {
    return this.iconImage_.ready();
  }
};
var yn = _a;
var pa = class _pa {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, this.color_ = e.color !== void 0 ? e.color : null, this.lineCap_ = e.lineCap, this.lineDash_ = e.lineDash !== void 0 ? e.lineDash : null, this.lineDashOffset_ = e.lineDashOffset, this.lineJoin_ = e.lineJoin, this.miterLimit_ = e.miterLimit, this.width_ = e.width;
  }
  /**
   * Clones the style.
   * @return {Stroke} The cloned style.
   * @api
   */
  clone() {
    const e = this.getColor();
    return new _pa({
      color: Array.isArray(e) ? e.slice() : e || void 0,
      lineCap: this.getLineCap(),
      lineDash: this.getLineDash() ? this.getLineDash().slice() : void 0,
      lineDashOffset: this.getLineDashOffset(),
      lineJoin: this.getLineJoin(),
      miterLimit: this.getMiterLimit(),
      width: this.getWidth()
    });
  }
  /**
   * Get the stroke color.
   * @return {import("../color.js").Color|import("../colorlike.js").ColorLike} Color.
   * @api
   */
  getColor() {
    return this.color_;
  }
  /**
   * Get the line cap type for the stroke.
   * @return {CanvasLineCap|undefined} Line cap.
   * @api
   */
  getLineCap() {
    return this.lineCap_;
  }
  /**
   * Get the line dash style for the stroke.
   * @return {Array<number>|null} Line dash.
   * @api
   */
  getLineDash() {
    return this.lineDash_;
  }
  /**
   * Get the line dash offset for the stroke.
   * @return {number|undefined} Line dash offset.
   * @api
   */
  getLineDashOffset() {
    return this.lineDashOffset_;
  }
  /**
   * Get the line join type for the stroke.
   * @return {CanvasLineJoin|undefined} Line join.
   * @api
   */
  getLineJoin() {
    return this.lineJoin_;
  }
  /**
   * Get the miter limit for the stroke.
   * @return {number|undefined} Miter limit.
   * @api
   */
  getMiterLimit() {
    return this.miterLimit_;
  }
  /**
   * Get the stroke width.
   * @return {number|undefined} Width.
   * @api
   */
  getWidth() {
    return this.width_;
  }
  /**
   * Set the color.
   *
   * @param {import("../color.js").Color|import("../colorlike.js").ColorLike} color Color.
   * @api
   */
  setColor(e) {
    this.color_ = e;
  }
  /**
   * Set the line cap.
   *
   * @param {CanvasLineCap|undefined} lineCap Line cap.
   * @api
   */
  setLineCap(e) {
    this.lineCap_ = e;
  }
  /**
   * Set the line dash.
   *
   * @param {Array<number>|null} lineDash Line dash.
   * @api
   */
  setLineDash(e) {
    this.lineDash_ = e;
  }
  /**
   * Set the line dash offset.
   *
   * @param {number|undefined} lineDashOffset Line dash offset.
   * @api
   */
  setLineDashOffset(e) {
    this.lineDashOffset_ = e;
  }
  /**
   * Set the line join.
   *
   * @param {CanvasLineJoin|undefined} lineJoin Line join.
   * @api
   */
  setLineJoin(e) {
    this.lineJoin_ = e;
  }
  /**
   * Set the miter limit.
   *
   * @param {number|undefined} miterLimit Miter limit.
   * @api
   */
  setMiterLimit(e) {
    this.miterLimit_ = e;
  }
  /**
   * Set the width.
   *
   * @param {number|undefined} width Width.
   * @api
   */
  setWidth(e) {
    this.width_ = e;
  }
};
var se = pa;
var dn = class _dn {
  /**
   * @param {Options} [options] Style options.
   */
  constructor(e) {
    e = e || {}, this.geometry_ = null, this.geometryFunction_ = yl, e.geometry !== void 0 && this.setGeometry(e.geometry), this.fill_ = e.fill !== void 0 ? e.fill : null, this.image_ = e.image !== void 0 ? e.image : null, this.renderer_ = e.renderer !== void 0 ? e.renderer : null, this.hitDetectionRenderer_ = e.hitDetectionRenderer !== void 0 ? e.hitDetectionRenderer : null, this.stroke_ = e.stroke !== void 0 ? e.stroke : null, this.text_ = e.text !== void 0 ? e.text : null, this.zIndex_ = e.zIndex;
  }
  /**
   * Clones the style.
   * @return {Style} The cloned style.
   * @api
   */
  clone() {
    let e = this.getGeometry();
    return e && typeof e == "object" && (e = /** @type {import("../geom/Geometry.js").default} */
    e.clone()), new _dn({
      geometry: e ?? void 0,
      fill: this.getFill() ? this.getFill().clone() : void 0,
      image: this.getImage() ? this.getImage().clone() : void 0,
      renderer: this.getRenderer() ?? void 0,
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      text: this.getText() ? this.getText().clone() : void 0,
      zIndex: this.getZIndex()
    });
  }
  /**
   * Get the custom renderer function that was configured with
   * {@link #setRenderer} or the `renderer` constructor option.
   * @return {RenderFunction|null} Custom renderer function.
   * @api
   */
  getRenderer() {
    return this.renderer_;
  }
  /**
   * Sets a custom renderer function for this style. When set, `fill`, `stroke`
   * and `image` options of the style will be ignored.
   * @param {RenderFunction|null} renderer Custom renderer function.
   * @api
   */
  setRenderer(e) {
    this.renderer_ = e;
  }
  /**
   * Sets a custom renderer function for this style used
   * in hit detection.
   * @param {RenderFunction|null} renderer Custom renderer function.
   * @api
   */
  setHitDetectionRenderer(e) {
    this.hitDetectionRenderer_ = e;
  }
  /**
   * Get the custom renderer function that was configured with
   * {@link #setHitDetectionRenderer} or the `hitDetectionRenderer` constructor option.
   * @return {RenderFunction|null} Custom renderer function.
   * @api
   */
  getHitDetectionRenderer() {
    return this.hitDetectionRenderer_;
  }
  /**
   * Get the geometry to be rendered.
   * @return {string|import("../geom/Geometry.js").default|GeometryFunction|null}
   * Feature property or geometry or function that returns the geometry that will
   * be rendered with this style.
   * @api
   */
  getGeometry() {
    return this.geometry_;
  }
  /**
   * Get the function used to generate a geometry for rendering.
   * @return {!GeometryFunction} Function that is called with a feature
   * and returns the geometry to render instead of the feature's geometry.
   * @api
   */
  getGeometryFunction() {
    return this.geometryFunction_;
  }
  /**
   * Get the fill style.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Set the fill style.
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(e) {
    this.fill_ = e;
  }
  /**
   * Get the image style.
   * @return {import("./Image.js").default|null} Image style.
   * @api
   */
  getImage() {
    return this.image_;
  }
  /**
   * Set the image style.
   * @param {import("./Image.js").default} image Image style.
   * @api
   */
  setImage(e) {
    this.image_ = e;
  }
  /**
   * Get the stroke style.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Set the stroke style.
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(e) {
    this.stroke_ = e;
  }
  /**
   * Get the text style.
   * @return {import("./Text.js").default|null} Text style.
   * @api
   */
  getText() {
    return this.text_;
  }
  /**
   * Set the text style.
   * @param {import("./Text.js").default} text Text style.
   * @api
   */
  setText(e) {
    this.text_ = e;
  }
  /**
   * Get the z-index for the style.
   * @return {number|undefined} ZIndex.
   * @api
   */
  getZIndex() {
    return this.zIndex_;
  }
  /**
   * Set a geometry that is rendered instead of the feature's geometry.
   *
   * @param {string|import("../geom/Geometry.js").default|GeometryFunction|null} geometry
   *     Feature property or geometry or function returning a geometry to render
   *     for this style.
   * @api
   */
  setGeometry(e) {
    typeof e == "function" ? this.geometryFunction_ = e : typeof e == "string" ? this.geometryFunction_ = function(t) {
      return (
        /** @type {import("../geom/Geometry.js").default} */
        t.get(e)
      );
    } : e ? e !== void 0 && (this.geometryFunction_ = function() {
      return (
        /** @type {import("../geom/Geometry.js").default} */
        e
      );
    }) : this.geometryFunction_ = yl, this.geometry_ = e;
  }
  /**
   * Set the z-index.
   *
   * @param {number|undefined} zIndex ZIndex.
   * @api
   */
  setZIndex(e) {
    this.zIndex_ = e;
  }
};
function Of(i) {
  let e;
  if (typeof i == "function")
    e = i;
  else {
    let t;
    Array.isArray(i) ? t = i : (_e(
      typeof /** @type {?} */
      i.getZIndex == "function",
      "Expected an `Style` or an array of `Style`"
    ), t = [
      /** @type {Style} */
      i
    ]), e = function() {
      return t;
    };
  }
  return e;
}
var Wr = null;
function Kc(i, e) {
  if (!Wr) {
    const t = new J({
      color: "rgba(255,255,255,0.4)"
    }), n = new se({
      color: "#3399CC",
      width: 1.25
    });
    Wr = [
      new dn({
        image: new we({
          fill: t,
          stroke: n,
          radius: 5
        }),
        fill: t,
        stroke: n
      })
    ];
  }
  return Wr;
}
function Nf() {
  const i = {}, e = [255, 255, 255, 1], t = [0, 153, 255, 1], n = 3;
  return i.Polygon = [
    new dn({
      fill: new J({
        color: [255, 255, 255, 0.5]
      })
    })
  ], i.MultiPolygon = i.Polygon, i.LineString = [
    new dn({
      stroke: new se({
        color: e,
        width: n + 2
      })
    }),
    new dn({
      stroke: new se({
        color: t,
        width: n
      })
    })
  ], i.MultiLineString = i.LineString, i.Circle = i.Polygon.concat(i.LineString), i.Point = [
    new dn({
      image: new we({
        radius: n * 2,
        fill: new J({
          color: t
        }),
        stroke: new se({
          color: e,
          width: n / 2
        })
      }),
      zIndex: 1 / 0
    })
  ], i.MultiPoint = i.Point, i.GeometryCollection = i.Polygon.concat(
    i.LineString,
    i.Point
  ), i;
}
function yl(i) {
  return i.getGeometry();
}
var te = dn;
var Uf = "#333";
var Sa = class _Sa {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, this.font_ = e.font, this.rotation_ = e.rotation, this.rotateWithView_ = e.rotateWithView, this.keepUpright_ = e.keepUpright, this.scale_ = e.scale, this.scaleArray_ = et(e.scale !== void 0 ? e.scale : 1), this.text_ = e.text, this.textAlign_ = e.textAlign, this.justify_ = e.justify, this.repeat_ = e.repeat, this.textBaseline_ = e.textBaseline, this.fill_ = e.fill !== void 0 ? e.fill : new J({ color: Uf }), this.maxAngle_ = e.maxAngle !== void 0 ? e.maxAngle : Math.PI / 4, this.placement_ = e.placement !== void 0 ? e.placement : "point", this.overflow_ = !!e.overflow, this.stroke_ = e.stroke !== void 0 ? e.stroke : null, this.offsetX_ = e.offsetX !== void 0 ? e.offsetX : 0, this.offsetY_ = e.offsetY !== void 0 ? e.offsetY : 0, this.backgroundFill_ = e.backgroundFill ? e.backgroundFill : null, this.backgroundStroke_ = e.backgroundStroke ? e.backgroundStroke : null, this.padding_ = e.padding === void 0 ? null : e.padding, this.declutterMode_ = e.declutterMode;
  }
  /**
   * Clones the style.
   * @return {Text} The cloned style.
   * @api
   */
  clone() {
    const e = this.getScale();
    return new _Sa({
      font: this.getFont(),
      placement: this.getPlacement(),
      repeat: this.getRepeat(),
      maxAngle: this.getMaxAngle(),
      overflow: this.getOverflow(),
      rotation: this.getRotation(),
      rotateWithView: this.getRotateWithView(),
      keepUpright: this.getKeepUpright(),
      scale: Array.isArray(e) ? e.slice() : e,
      text: this.getText(),
      textAlign: this.getTextAlign(),
      justify: this.getJustify(),
      textBaseline: this.getTextBaseline(),
      fill: this.getFill() instanceof J ? this.getFill().clone() : this.getFill(),
      stroke: this.getStroke() ? this.getStroke().clone() : void 0,
      offsetX: this.getOffsetX(),
      offsetY: this.getOffsetY(),
      backgroundFill: this.getBackgroundFill() ? this.getBackgroundFill().clone() : void 0,
      backgroundStroke: this.getBackgroundStroke() ? this.getBackgroundStroke().clone() : void 0,
      padding: this.getPadding() || void 0,
      declutterMode: this.getDeclutterMode()
    });
  }
  /**
   * Get the `overflow` configuration.
   * @return {boolean} Let text overflow the length of the path they follow.
   * @api
   */
  getOverflow() {
    return this.overflow_;
  }
  /**
   * Get the font name.
   * @return {string|undefined} Font.
   * @api
   */
  getFont() {
    return this.font_;
  }
  /**
   * Get the maximum angle between adjacent characters.
   * @return {number} Angle in radians.
   * @api
   */
  getMaxAngle() {
    return this.maxAngle_;
  }
  /**
   * Get the label placement.
   * @return {TextPlacement} Text placement.
   * @api
   */
  getPlacement() {
    return this.placement_;
  }
  /**
   * Get the repeat interval of the text.
   * @return {number|undefined} Repeat interval in pixels.
   * @api
   */
  getRepeat() {
    return this.repeat_;
  }
  /**
   * Get the x-offset for the text.
   * @return {number} Horizontal text offset.
   * @api
   */
  getOffsetX() {
    return this.offsetX_;
  }
  /**
   * Get the y-offset for the text.
   * @return {number} Vertical text offset.
   * @api
   */
  getOffsetY() {
    return this.offsetY_;
  }
  /**
   * Get the fill style for the text.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getFill() {
    return this.fill_;
  }
  /**
   * Determine whether the text rotates with the map.
   * @return {boolean|undefined} Rotate with map.
   * @api
   */
  getRotateWithView() {
    return this.rotateWithView_;
  }
  /**
   * Determine whether the text can be rendered upside down.
   * @return {boolean|undefined} Keep text upright.
   * @api
   */
  getKeepUpright() {
    return this.keepUpright_;
  }
  /**
   * Get the text rotation.
   * @return {number|undefined} Rotation.
   * @api
   */
  getRotation() {
    return this.rotation_;
  }
  /**
   * Get the text scale.
   * @return {number|import("../size.js").Size|undefined} Scale.
   * @api
   */
  getScale() {
    return this.scale_;
  }
  /**
   * Get the symbolizer scale array.
   * @return {import("../size.js").Size} Scale array.
   */
  getScaleArray() {
    return this.scaleArray_;
  }
  /**
   * Get the stroke style for the text.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getStroke() {
    return this.stroke_;
  }
  /**
   * Get the text to be rendered.
   * @return {string|Array<string>|undefined} Text.
   * @api
   */
  getText() {
    return this.text_;
  }
  /**
   * Get the text alignment.
   * @return {CanvasTextAlign|undefined} Text align.
   * @api
   */
  getTextAlign() {
    return this.textAlign_;
  }
  /**
   * Get the justification.
   * @return {TextJustify|undefined} Justification.
   * @api
   */
  getJustify() {
    return this.justify_;
  }
  /**
   * Get the text baseline.
   * @return {CanvasTextBaseline|undefined} Text baseline.
   * @api
   */
  getTextBaseline() {
    return this.textBaseline_;
  }
  /**
   * Get the background fill style for the text.
   * @return {import("./Fill.js").default|null} Fill style.
   * @api
   */
  getBackgroundFill() {
    return this.backgroundFill_;
  }
  /**
   * Get the background stroke style for the text.
   * @return {import("./Stroke.js").default|null} Stroke style.
   * @api
   */
  getBackgroundStroke() {
    return this.backgroundStroke_;
  }
  /**
   * Get the padding for the text.
   * @return {Array<number>|null} Padding.
   * @api
   */
  getPadding() {
    return this.padding_;
  }
  /**
   * Get the declutter mode of the shape
   * @return {import("./Style.js").DeclutterMode} Shape's declutter mode
   * @api
   */
  getDeclutterMode() {
    return this.declutterMode_;
  }
  /**
   * Set the `overflow` property.
   *
   * @param {boolean} overflow Let text overflow the path that it follows.
   * @api
   */
  setOverflow(e) {
    this.overflow_ = e;
  }
  /**
   * Set the font.
   *
   * @param {string|undefined} font Font.
   * @api
   */
  setFont(e) {
    this.font_ = e;
  }
  /**
   * Set the maximum angle between adjacent characters.
   *
   * @param {number} maxAngle Angle in radians.
   * @api
   */
  setMaxAngle(e) {
    this.maxAngle_ = e;
  }
  /**
   * Set the x offset.
   *
   * @param {number} offsetX Horizontal text offset.
   * @api
   */
  setOffsetX(e) {
    this.offsetX_ = e;
  }
  /**
   * Set the y offset.
   *
   * @param {number} offsetY Vertical text offset.
   * @api
   */
  setOffsetY(e) {
    this.offsetY_ = e;
  }
  /**
   * Set the text placement.
   *
   * @param {TextPlacement} placement Placement.
   * @api
   */
  setPlacement(e) {
    this.placement_ = e;
  }
  /**
   * Set the repeat interval of the text.
   * @param {number|undefined} [repeat] Repeat interval in pixels.
   * @api
   */
  setRepeat(e) {
    this.repeat_ = e;
  }
  /**
   * Set whether to rotate the text with the view.
   *
   * @param {boolean} rotateWithView Rotate with map.
   * @api
   */
  setRotateWithView(e) {
    this.rotateWithView_ = e;
  }
  /**
   * Set whether the text can be rendered upside down.
   *
   * @param {boolean} keepUpright Keep text upright.
   * @api
   */
  setKeepUpright(e) {
    this.keepUpright_ = e;
  }
  /**
   * Set the fill.
   *
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setFill(e) {
    this.fill_ = e;
  }
  /**
   * Set the rotation.
   *
   * @param {number|undefined} rotation Rotation.
   * @api
   */
  setRotation(e) {
    this.rotation_ = e;
  }
  /**
   * Set the scale.
   *
   * @param {number|import("../size.js").Size|undefined} scale Scale.
   * @api
   */
  setScale(e) {
    this.scale_ = e, this.scaleArray_ = et(e !== void 0 ? e : 1);
  }
  /**
   * Set the stroke.
   *
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setStroke(e) {
    this.stroke_ = e;
  }
  /**
   * Set the text.
   *
   * @param {string|Array<string>|undefined} text Text.
   * @api
   */
  setText(e) {
    this.text_ = e;
  }
  /**
   * Set the text alignment.
   *
   * @param {CanvasTextAlign|undefined} textAlign Text align.
   * @api
   */
  setTextAlign(e) {
    this.textAlign_ = e;
  }
  /**
   * Set the justification.
   *
   * @param {TextJustify|undefined} justify Justification.
   * @api
   */
  setJustify(e) {
    this.justify_ = e;
  }
  /**
   * Set the text baseline.
   *
   * @param {CanvasTextBaseline|undefined} textBaseline Text baseline.
   * @api
   */
  setTextBaseline(e) {
    this.textBaseline_ = e;
  }
  /**
   * Set the background fill.
   *
   * @param {import("./Fill.js").default|null} fill Fill style.
   * @api
   */
  setBackgroundFill(e) {
    this.backgroundFill_ = e;
  }
  /**
   * Set the background stroke.
   *
   * @param {import("./Stroke.js").default|null} stroke Stroke style.
   * @api
   */
  setBackgroundStroke(e) {
    this.backgroundStroke_ = e;
  }
  /**
   * Set the padding (`[top, right, bottom, left]`).
   *
   * @param {Array<number>|null} padding Padding.
   * @api
   */
  setPadding(e) {
    this.padding_ = e;
  }
};
var zn = Sa;
var Gf = class {
  constructor({ getSource: e, getIconFeatures: t, coordinateFromLonLat: n, getResolution: s, getPixelLength: r } = {}) {
    this.getSource = e, this.getIconFeatures = t, this.coordinateFromLonLat = n, this.getResolution = s, this.getPixelLength = r, this.features = /* @__PURE__ */ new Map(), this.styleCache = /* @__PURE__ */ new Map();
  }
  getCoordinates(e) {
    var l, c, h2;
    const t = Number(e.lon), n = Number(e.lat), s = Vs(e.heading), r = (l = this.coordinateFromLonLat) == null ? void 0 : l.call(this, t, n);
    if (!r)
      return null;
    const o = Number((c = this.getResolution) == null ? void 0 : c.call(this)) || 1, a = Math.max(12, Number((h2 = this.getPixelLength) == null ? void 0 : h2.call(this)) || 42) * o;
    return [
      r,
      [
        r[0] + Math.sin(s) * a,
        r[1] + Math.cos(s) * a
      ]
    ];
  }
  getStyle(e) {
    const t = e === "blue" ? "rgba(72, 167, 255, 0.95)" : "rgba(255, 96, 96, 0.95)", n = `${e || "red"}|${t}`;
    if (this.styleCache.has(n))
      return this.styleCache.get(n);
    const s = new te({
      stroke: new se({
        color: t,
        width: 2.5,
        lineDash: [8, 4]
      })
    });
    return this.styleCache.set(n, s), s;
  }
  update(e, t = {}) {
    var u;
    const n = (u = this.getSource) == null ? void 0 : u.call(this);
    if (!n)
      return;
    const s = t.headingVisible || {}, r = String(e.UID), o = this.features.get(r);
    if (!(s[e.nation] === true && e.status !== "hidden")) {
      o && (n.removeFeature(o), this.features.delete(r));
      return;
    }
    const l = this.getCoordinates(e);
    if (!l)
      return;
    const c = new tt(l);
    if (o) {
      o.setGeometry(c), o.setStyle(this.getStyle(e.nation));
      return;
    }
    const h2 = new ae({
      geometry: c,
      headingUID: e.UID
    });
    h2.setStyle(this.getStyle(e.nation)), this.features.set(r, h2), n.addFeature(h2);
  }
  render(e = [], t = {}) {
    var o;
    const n = (o = this.getSource) == null ? void 0 : o.call(this);
    if (!n)
      return;
    const s = t.headingVisible || {}, r = /* @__PURE__ */ new Set();
    e.forEach((a) => {
      r.add(String(a.UID)), this.update(a, { headingVisible: s });
    }), Array.from(this.features.keys()).forEach((a) => {
      if (!r.has(a)) {
        const l = this.features.get(a);
        n.removeFeature(l), this.features.delete(a);
      }
    });
  }
  refresh(e = {}) {
    var s;
    const t = e.headingVisible || {};
    (((s = this.getIconFeatures) == null ? void 0 : s.call(this)) || []).forEach((r) => {
      this.update(
        {
          UID: r.get("UID"),
          nation: r.get("nation"),
          status: r.get("status"),
          lon: r.get("lon"),
          lat: r.get("lat"),
          heading: r.get("heading")
        },
        { headingVisible: t }
      );
    });
  }
  removeByUID(e) {
    var s;
    const t = (s = this.getSource) == null ? void 0 : s.call(this);
    if (!t)
      return;
    const n = this.features.get(String(e));
    n && (t.removeFeature(n), this.features.delete(String(e)));
  }
  reset() {
    this.features = /* @__PURE__ */ new Map(), this.styleCache = /* @__PURE__ */ new Map();
  }
};
var Bf = class extends It {
  /**
   * @param {string} type Event type.
   * @param {import("./Map.js").default} map Map.
   * @param {?import("./Map.js").FrameState} [frameState] Frame state.
   */
  constructor(e, t, n) {
    super(e), this.map = t, this.frameState = n !== void 0 ? n : null;
  }
};
var ni = Bf;
var zf = class extends ni {
  /**
   * @param {string} type Event type.
   * @param {import("./Map.js").default} map Map.
   * @param {EVENT} originalEvent Original event.
   * @param {boolean} [dragging] Is the map currently being dragged?
   * @param {import("./Map.js").FrameState} [frameState] Frame state.
   * @param {Array<PointerEvent>} [activePointers] Active pointers.
   */
  constructor(e, t, n, s, r, o) {
    super(e, t, r), this.originalEvent = n, this.pixel_ = null, this.coordinate_ = null, this.dragging = s !== void 0 ? s : false, this.activePointers = o;
  }
  /**
   * The map pixel relative to the viewport corresponding to the original event.
   * @type {import("./pixel.js").Pixel}
   * @api
   */
  get pixel() {
    return this.pixel_ || (this.pixel_ = this.map.getEventPixel(this.originalEvent)), this.pixel_;
  }
  set pixel(e) {
    this.pixel_ = e;
  }
  /**
   * The coordinate corresponding to the original browser event.  This will be in the user
   * projection if one is set.  Otherwise it will be in the view projection.
   * @type {import("./coordinate.js").Coordinate}
   * @api
   */
  get coordinate() {
    return this.coordinate_ || (this.coordinate_ = this.map.getCoordinateFromPixel(this.pixel)), this.coordinate_;
  }
  set coordinate(e) {
    this.coordinate_ = e;
  }
  /**
   * Prevents the default browser action.
   * See https://developer.mozilla.org/en-US/docs/Web/API/event.preventDefault.
   * @api
   * @override
   */
  preventDefault() {
    super.preventDefault(), "preventDefault" in this.originalEvent && this.originalEvent.preventDefault();
  }
  /**
   * Prevents further propagation of the current event.
   * See https://developer.mozilla.org/en-US/docs/Web/API/event.stopPropagation.
   * @api
   * @override
   */
  stopPropagation() {
    super.stopPropagation(), "stopPropagation" in this.originalEvent && this.originalEvent.stopPropagation();
  }
};
var Vt = zf;
var me = {
  /**
   * A true single click with no dragging and no double click. Note that this
   * event is delayed by 250 ms to ensure that it is not a double click.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#singleclick
   * @api
   */
  SINGLECLICK: "singleclick",
  /**
   * A click with no dragging. A double click will fire two of this.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#click
   * @api
   */
  CLICK: Z.CLICK,
  /**
   * A true double click, with no dragging.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#dblclick
   * @api
   */
  DBLCLICK: Z.DBLCLICK,
  /**
   * Triggered when a pointer is dragged.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#pointerdrag
   * @api
   */
  POINTERDRAG: "pointerdrag",
  /**
   * Triggered when a pointer is moved. Note that on touch devices this is
   * triggered when the map is panned, so is not the same as mousemove.
   * @event module:ol/MapBrowserEvent~MapBrowserEvent#pointermove
   * @api
   */
  POINTERMOVE: "pointermove",
  POINTERDOWN: "pointerdown",
  POINTERUP: "pointerup",
  POINTEROVER: "pointerover",
  POINTEROUT: "pointerout",
  POINTERENTER: "pointerenter",
  POINTERLEAVE: "pointerleave",
  POINTERCANCEL: "pointercancel"
};
function bo(i) {
  const e = arguments;
  return function(t) {
    let n = true;
    for (let s = 0, r = e.length; s < r && (n = n && e[s](t), !!n); ++s)
      ;
    return n;
  };
}
var Vf = function(i) {
  const e = i.originalEvent;
  return e.altKey && !(e.metaKey || e.ctrlKey) && e.shiftKey;
};
var Xf = function(i) {
  const e = i.map.getTargetElement(), t = e.getRootNode(), n = i.map.getOwnerDocument().activeElement;
  return t instanceof ShadowRoot ? t.host.contains(n) : e.contains(n);
};
var Hc = function(i) {
  const e = i.map.getTargetElement(), t = e.getRootNode();
  return (t instanceof ShadowRoot ? t.host : e).hasAttribute("tabindex") ? Xf(i) : true;
};
var $i = _n;
var Ea = function(i) {
  const e = i.originalEvent;
  return "pointerId" in e && e.button == 0 && !(af && Nc && e.ctrlKey);
};
var Wf = vi;
var wa = function(i) {
  const e = (
    /** @type {KeyboardEvent|MouseEvent|TouchEvent} */
    i.originalEvent
  );
  return !e.altKey && !(e.metaKey || e.ctrlKey) && !e.shiftKey;
};
var Yf = function(i) {
  const e = i.originalEvent;
  return Nc ? e.metaKey : e.ctrlKey;
};
var Ro = function(i) {
  const e = i.originalEvent;
  return !e.altKey && !(e.metaKey || e.ctrlKey) && e.shiftKey;
};
var Zc = function(i) {
  const e = i.originalEvent, t = (
    /** @type {Element} */
    e.target.tagName
  );
  return t !== "INPUT" && t !== "SELECT" && t !== "TEXTAREA" && // `isContentEditable` is only available on `HTMLElement`, but it may also be a
  // different type like `SVGElement`.
  // @ts-ignore
  !e.target.isContentEditable;
};
var Yr = function(i) {
  const e = i.originalEvent;
  return "pointerId" in e && e.pointerType == "mouse";
};
var jf = function(i) {
  const e = i.originalEvent;
  return "pointerId" in e && e.isPrimary && e.button === 0;
};
var vr = class _vr extends wn {
  /**
   * @param {!import("../coordinate.js").Coordinate} center Center.
   *     For internal use, flat coordinates in combination with `layout` and no
   *     `radius` are also accepted.
   * @param {number} [radius] Radius in units of the projection.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(e, t, n) {
    super(), n !== void 0 && t === void 0 ? this.setFlatCoordinates(n, e) : (t = t || 0, this.setCenterAndRadius(e, t, n));
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Circle} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _vr(
      this.flatCoordinates.slice(),
      void 0,
      this.layout
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    const r = this.flatCoordinates, o = e - r[0], a = t - r[1], l = o * o + a * a;
    if (l < s) {
      if (l === 0)
        for (let c = 0; c < this.stride; ++c)
          n[c] = r[c];
      else {
        const c = this.getRadius() / Math.sqrt(l);
        n[0] = r[0] + c * o, n[1] = r[1] + c * a;
        for (let h2 = 2; h2 < this.stride; ++h2)
          n[h2] = r[h2];
      }
      return n.length = this.stride, l;
    }
    return s;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(e, t) {
    const n = this.flatCoordinates, s = e - n[0], r = t - n[1];
    return s * s + r * r <= this.getRadiusSquared_();
  }
  /**
   * Return the center of the circle as {@link module:ol/coordinate~Coordinate coordinate}.
   * @return {import("../coordinate.js").Coordinate} Center.
   * @api
   */
  getCenter() {
    return this.flatCoordinates.slice(0, this.stride);
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(e) {
    const t = this.flatCoordinates, n = t[this.stride] - t[0];
    return kt(
      t[0] - n,
      t[1] - n,
      t[0] + n,
      t[1] + n,
      e
    );
  }
  /**
   * Return the radius of the circle.
   * @return {number} Radius.
   * @api
   */
  getRadius() {
    return Math.sqrt(this.getRadiusSquared_());
  }
  /**
   * @private
   * @return {number} Radius squared.
   */
  getRadiusSquared_() {
    const e = this.flatCoordinates[this.stride] - this.flatCoordinates[0], t = this.flatCoordinates[this.stride + 1] - this.flatCoordinates[1];
    return e * e + t * t;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Circle";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    const t = this.getExtent();
    if ($e(e, t)) {
      const n = this.getCenter();
      return e[0] <= n[0] && e[2] >= n[0] || e[1] <= n[1] && e[3] >= n[1] ? true : Wo(e, this.intersectsCoordinate.bind(this));
    }
    return false;
  }
  /**
   * Set the center of the circle as {@link module:ol/coordinate~Coordinate coordinate}.
   * @param {import("../coordinate.js").Coordinate} center Center.
   * @api
   */
  setCenter(e) {
    const t = this.stride, n = this.flatCoordinates[t] - this.flatCoordinates[0], s = e.slice();
    s[t] = s[0] + n;
    for (let r = 1; r < t; ++r)
      s[t + r] = e[r];
    this.setFlatCoordinates(this.layout, s), this.changed();
  }
  /**
   * Set the center (as {@link module:ol/coordinate~Coordinate coordinate}) and the radius (as
   * number) of the circle.
   * @param {!import("../coordinate.js").Coordinate} center Center.
   * @param {number} radius Radius.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   */
  setCenterAndRadius(e, t, n) {
    this.setLayout(n, e, 0), this.flatCoordinates || (this.flatCoordinates = []);
    const s = this.flatCoordinates;
    let r = Dc(s, 0, e, this.stride);
    s[r++] = s[0] + t;
    for (let o = 1, a = this.stride; o < a; ++o)
      s[r++] = s[o];
    s.length = r, this.changed();
  }
  /**
   * @override
   */
  getCoordinates() {
    return null;
  }
  /**
   * @override
   */
  setCoordinates(e, t) {
  }
  /**
   * Set the radius of the circle. The radius is in the units of the projection.
   * @param {number} radius Radius.
   * @api
   */
  setRadius(e) {
    this.flatCoordinates[this.stride] = this.flatCoordinates[0] + e, this.changed();
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in counter-clockwise radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(e, t) {
    const n = this.getCenter(), s = this.getStride();
    this.setCenter(
      ea(n, 0, n.length, s, e, t, n)
    ), this.changed();
  }
};
vr.prototype.transform;
var qc = vr;
var tr = class _tr extends wn {
  /**
   * @param {Array<Array<import("../coordinate.js").Coordinate>|LineString>|Array<number>} coordinates
   *     Coordinates or LineString geometries. (For internal use, flat coordinates in
   *     combination with `layout` and `ends` are also accepted.)
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<number>} [ends] Flat coordinate ends for internal use.
   */
  constructor(e, t, n) {
    if (super(), this.ends_ = [], this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, Array.isArray(e[0]))
      this.setCoordinates(
        /** @type {Array<Array<import("../coordinate.js").Coordinate>>} */
        e,
        t
      );
    else if (t !== void 0 && n)
      this.setFlatCoordinates(
        t,
        /** @type {Array<number>} */
        e
      ), this.ends_ = n;
    else {
      const s = (
        /** @type {Array<LineString>} */
        e
      ), r = [], o = [];
      for (let l = 0, c = s.length; l < c; ++l) {
        const h2 = s[l];
        At(r, h2.getFlatCoordinates()), o.push(r.length);
      }
      const a = s.length === 0 ? this.getLayout() : s[0].getLayout();
      this.setFlatCoordinates(a, r), this.ends_ = o;
    }
  }
  /**
   * Append the passed linestring to the multilinestring.
   * @param {LineString} lineString LineString.
   * @api
   */
  appendLineString(e) {
    At(this.flatCoordinates, e.getFlatCoordinates().slice()), this.ends_.push(this.flatCoordinates.length), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiLineString} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _tr(
      this.flatCoordinates.slice(),
      this.layout,
      this.ends_.slice()
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    return s < Bn(this.getExtent(), e, t) ? s : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      na(
        this.flatCoordinates,
        0,
        this.ends_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), sa(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      this.maxDelta_,
      false,
      e,
      t,
      n,
      s
    ));
  }
  /**
   * Returns the coordinate at `m` using linear interpolation, or `null` if no
   * such coordinate exists.
   *
   * `extrapolate` controls extrapolation beyond the range of Ms in the
   * MultiLineString. If `extrapolate` is `true` then Ms less than the first
   * M will return the first coordinate and Ms greater than the last M will
   * return the last coordinate.
   *
   * `interpolate` controls interpolation between consecutive LineStrings
   * within the MultiLineString. If `interpolate` is `true` the coordinates
   * will be linearly interpolated between the last coordinate of one LineString
   * and the first coordinate of the next LineString.  If `interpolate` is
   * `false` then the function will return `null` for Ms falling between
   * LineStrings.
   *
   * @param {number} m M.
   * @param {boolean} [extrapolate] Extrapolate. Default is `false`.
   * @param {boolean} [interpolate] Interpolate. Default is `false`.
   * @return {import("../coordinate.js").Coordinate|null} Coordinate.
   * @api
   */
  getCoordinateAtM(e, t, n) {
    return this.layout != "XYM" && this.layout != "XYZM" || this.flatCoordinates.length === 0 ? null : (t = t !== void 0 ? t : false, n = n !== void 0 ? n : false, Jd(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      e,
      t,
      n
    ));
  }
  /**
   * Return the coordinates of the multilinestring.
   * @return {Array<Array<import("../coordinate.js").Coordinate>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return Qi(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride
    );
  }
  /**
   * @return {Array<number>} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * Return the linestring at the specified index.
   * @param {number} index Index.
   * @return {LineString} LineString.
   * @api
   */
  getLineString(e) {
    return e < 0 || this.ends_.length <= e ? null : new tt(
      this.flatCoordinates.slice(
        e === 0 ? 0 : this.ends_[e - 1],
        this.ends_[e]
      ),
      this.layout
    );
  }
  /**
   * Return the linestrings of this multilinestring.
   * @return {Array<LineString>} LineStrings.
   * @api
   */
  getLineStrings() {
    const e = this.flatCoordinates, t = this.ends_, n = this.layout, s = [];
    let r = 0;
    for (let o = 0, a = t.length; o < a; ++o) {
      const l = t[o], c = new tt(
        e.slice(r, l),
        n
      );
      s.push(c), r = l;
    }
    return s;
  }
  /**
   * Return the sum of all line string lengths
   * @return {number} Length (on projected plane).
   * @api
   */
  getLength() {
    const e = this.ends_;
    let t = 0, n = 0;
    for (let s = 0, r = e.length; s < r; ++s)
      n += aa(
        this.flatCoordinates,
        t,
        e[s],
        this.stride
      ), t = e[s];
    return n;
  }
  /**
   * @return {Array<number>} Flat midpoints.
   */
  getFlatMidpoints() {
    const e = [], t = this.flatCoordinates;
    let n = 0;
    const s = this.ends_, r = this.stride;
    for (let o = 0, a = s.length; o < a; ++o) {
      const l = s[o], c = Zs(
        t,
        n,
        l,
        r,
        0.5
      );
      At(e, c), n = l;
    }
    return e;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {MultiLineString} Simplified MultiLineString.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(e) {
    const t = [], n = [];
    return t.length = Oc(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      e,
      t,
      0,
      n
    ), new _tr(t, "XY", n);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiLineString";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return tf(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      e
    );
  }
  /**
   * Set the coordinates of the multilinestring.
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 2), this.flatCoordinates || (this.flatCoordinates = []);
    const n = ra(
      this.flatCoordinates,
      0,
      e,
      this.stride,
      this.ends_
    );
    this.flatCoordinates.length = n.length === 0 ? 0 : n[n.length - 1], this.changed();
  }
};
var Ca = tr;
var xa = class _xa extends wn {
  /**
   * @param {import("../coordinate.js").Coordinate} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(e, t) {
    super(), this.setCoordinates(e, t);
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Point} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _xa(this.flatCoordinates.slice(), this.layout);
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    const r = this.flatCoordinates, o = jt(
      e,
      t,
      r[0],
      r[1]
    );
    if (o < s) {
      const a = this.stride;
      for (let l = 0; l < a; ++l)
        n[l] = r[l];
      return n.length = a, o;
    }
    return s;
  }
  /**
   * Return the coordinate of the point.
   * @return {import("../coordinate.js").Coordinate} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return this.flatCoordinates.slice();
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(e) {
    return fc(this.flatCoordinates, e);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Point";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return Vo(e, this.flatCoordinates[0], this.flatCoordinates[1]);
  }
  /**
   * @param {!Array<*>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 0), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = Dc(
      this.flatCoordinates,
      0,
      e,
      this.stride
    ), this.changed();
  }
};
var he = xa;
var Ia = class _Ia extends wn {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(e, t) {
    super(), t && !Array.isArray(e[0]) ? this.setFlatCoordinates(
      t,
      /** @type {Array<number>} */
      e
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      e,
      t
    );
  }
  /**
   * Append the passed point to this multipoint.
   * @param {Point} point Point.
   * @api
   */
  appendPoint(e) {
    At(this.flatCoordinates, e.getFlatCoordinates()), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiPoint} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _Ia(
      this.flatCoordinates.slice(),
      this.layout
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    if (s < Bn(this.getExtent(), e, t))
      return s;
    const r = this.flatCoordinates, o = this.stride;
    for (let a = 0, l = r.length; a < l; a += o) {
      const c = jt(
        e,
        t,
        r[a],
        r[a + 1]
      );
      if (c < s) {
        s = c;
        for (let h2 = 0; h2 < o; ++h2)
          n[h2] = r[a + h2];
        n.length = o;
      }
    }
    return s;
  }
  /**
   * Return the coordinates of the multipoint.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return hn(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the point at the specified index.
   * @param {number} index Index.
   * @return {Point} Point.
   * @api
   */
  getPoint(e) {
    const t = this.flatCoordinates.length / this.stride;
    return e < 0 || t <= e ? null : new he(
      this.flatCoordinates.slice(
        e * this.stride,
        (e + 1) * this.stride
      ),
      this.layout
    );
  }
  /**
   * Return the points of this multipoint.
   * @return {Array<Point>} Points.
   * @api
   */
  getPoints() {
    const e = this.flatCoordinates, t = this.layout, n = this.stride, s = [];
    for (let r = 0, o = e.length; r < o; r += n) {
      const a = new he(e.slice(r, r + n), t);
      s.push(a);
    }
    return s;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiPoint";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    const t = this.flatCoordinates, n = this.stride;
    for (let s = 0, r = t.length; s < r; s += n) {
      const o = t[s], a = t[s + 1];
      if (Vo(e, o, a))
        return true;
    }
    return false;
  }
  /**
   * Set the coordinates of the multipoint.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = Er(
      this.flatCoordinates,
      0,
      e,
      this.stride
    ), this.changed();
  }
};
var Jc = Ia;
function Qc(i, e, t, n) {
  let s = 0;
  const r = i[t - n], o = i[t - n + 1];
  let a = 0, l = 0;
  for (; e < t; e += n) {
    const c = i[e] - r, h2 = i[e + 1] - o;
    s += l * c - a * h2, a = c, l = h2;
  }
  return s / 2;
}
function eh(i, e, t, n) {
  let s = 0;
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    s += Qc(i, e, a, n), e = a;
  }
  return s;
}
function $f(i, e, t, n) {
  let s = 0;
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    s += eh(i, e, a, n), e = a[a.length - 1];
  }
  return s;
}
var nr = class _nr extends wn {
  /**
   * @param {Array<import("../coordinate.js").Coordinate>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   */
  constructor(e, t) {
    super(), this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, t !== void 0 && !Array.isArray(e[0]) ? this.setFlatCoordinates(
      t,
      /** @type {Array<number>} */
      e
    ) : this.setCoordinates(
      /** @type {Array<import("../coordinate.js").Coordinate>} */
      e,
      t
    );
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!LinearRing} Clone.
   * @api
   * @override
   */
  clone() {
    return new _nr(this.flatCoordinates.slice(), this.layout);
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    return s < Bn(this.getExtent(), e, t) ? s : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      ta(
        this.flatCoordinates,
        0,
        this.flatCoordinates.length,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), ia(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      this.maxDelta_,
      true,
      e,
      t,
      n,
      s
    ));
  }
  /**
   * Return the area of the linear ring on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return Qc(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * Return the coordinates of the linear ring.
   * @return {Array<import("../coordinate.js").Coordinate>} Coordinates.
   * @api
   * @override
   */
  getCoordinates() {
    return hn(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {LinearRing} Simplified LinearRing.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(e) {
    const t = [];
    return t.length = Cr(
      this.flatCoordinates,
      0,
      this.flatCoordinates.length,
      this.stride,
      e,
      t,
      0
    ), new _nr(t, "XY");
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "LinearRing";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return false;
  }
  /**
   * Set the coordinates of the linear ring.
   * @param {!Array<import("../coordinate.js").Coordinate>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 1), this.flatCoordinates || (this.flatCoordinates = []), this.flatCoordinates.length = Er(
      this.flatCoordinates,
      0,
      e,
      this.stride
    ), this.changed();
  }
};
var _l = nr;
function va(i, e, t, n, s, r, o) {
  let a, l, c, h2, u, d, f;
  const g = s[r + 1], y = [];
  for (let p = 0, E = t.length; p < E; ++p) {
    const S = t[p];
    for (h2 = i[S - n], d = i[S - n + 1], a = e; a < S; a += n)
      u = i[a], f = i[a + 1], (g <= d && f <= g || d <= g && g <= f) && (c = (g - d) / (f - d) * (u - h2) + h2, y.push(c)), h2 = u, d = f;
  }
  let m = NaN, _ = -1 / 0;
  for (y.sort(Yt), h2 = y[0], a = 1, l = y.length; a < l; ++a) {
    u = y[a];
    const p = Math.abs(u - h2);
    p > _ && (c = (h2 + u) / 2, oa(i, e, t, n, c, g) && (m = c, _ = p)), h2 = u;
  }
  return isNaN(m) && (m = s[r]), o ? (o.push(m, g, _), o) : [m, g, _];
}
function th(i, e, t, n, s) {
  let r = [];
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = t[o];
    r = va(
      i,
      e,
      l,
      n,
      s,
      2 * o,
      r
    ), e = l[l.length - 1];
  }
  return r;
}
function Kf(i, e, t, n) {
  for (; e < t - n; ) {
    for (let s = 0; s < n; ++s) {
      const r = i[e + s];
      i[e + s] = i[t - n + s], i[t - n + s] = r;
    }
    e += n, t -= n;
  }
}
function Ma(i, e, t, n) {
  let s = 0, r = i[t - n], o = i[t - n + 1];
  for (; e < t; e += n) {
    const a = i[e], l = i[e + 1];
    s += (a - r) * (l + o), r = a, o = l;
  }
  return s === 0 ? void 0 : s > 0;
}
function nh(i, e, t, n, s) {
  s = s !== void 0 ? s : false;
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r], l = Ma(
      i,
      e,
      a,
      n
    );
    if (r === 0) {
      if (s && l || !s && !l)
        return false;
    } else if (s && !l || !s && l)
      return false;
    e = a;
  }
  return true;
}
function Hf(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r];
    if (!nh(i, e, a, n, s))
      return false;
    a.length && (e = a[a.length - 1]);
  }
  return true;
}
function Lo(i, e, t, n, s) {
  s = s !== void 0 ? s : false;
  for (let r = 0, o = t.length; r < o; ++r) {
    const a = t[r], l = Ma(
      i,
      e,
      a,
      n
    );
    (r === 0 ? s && l || !s && !l : s && !l || !s && l) && Kf(i, e, a, n), e = a;
  }
  return e;
}
function pl(i, e, t, n, s) {
  for (let r = 0, o = t.length; r < o; ++r)
    e = Lo(
      i,
      e,
      t[r],
      n,
      s
    );
  return e;
}
function Zf(i, e) {
  const t = [];
  let n = 0, s = 0, r;
  for (let o = 0, a = e.length; o < a; ++o) {
    const l = e[o], c = Ma(i, n, l, 2);
    if (r === void 0 && (r = c), c === r)
      t.push(e.slice(s, o + 1));
    else {
      if (t.length === 0)
        continue;
      t[t.length - 1].push(e[s]);
    }
    s = o + 1, n = l;
  }
  return t;
}
var xi = class _xi extends wn {
  /**
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>|!Array<number>} coordinates
   *     Array of linear rings that define the polygon. The first linear ring of the
   *     array defines the outer-boundary or surface of the polygon. Each subsequent
   *     linear ring defines a hole in the surface of the polygon. A linear ring is
   *     an array of vertices' coordinates where the first coordinate and the last are
   *     equivalent. (For internal use, flat coordinates in combination with
   *     `layout` and `ends` are also accepted.)
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<number>} [ends] Ends (for internal use with flat coordinates).
   */
  constructor(e, t, n) {
    super(), this.ends_ = [], this.flatInteriorPointRevision_ = -1, this.flatInteriorPoint_ = null, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, this.orientedRevision_ = -1, this.orientedFlatCoordinates_ = null, t !== void 0 && n ? (this.setFlatCoordinates(
      t,
      /** @type {Array<number>} */
      e
    ), this.ends_ = n) : this.setCoordinates(
      /** @type {Array<Array<import("../coordinate.js").Coordinate>>} */
      e,
      t
    );
  }
  /**
   * Append the passed linear ring to this polygon.
   * @param {LinearRing} linearRing Linear ring.
   * @api
   */
  appendLinearRing(e) {
    this.flatCoordinates ? At(this.flatCoordinates, e.getFlatCoordinates()) : this.flatCoordinates = e.getFlatCoordinates().slice(), this.ends_.push(this.flatCoordinates.length), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!Polygon} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _xi(
      this.flatCoordinates.slice(),
      this.layout,
      this.ends_.slice()
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    return s < Bn(this.getExtent(), e, t) ? s : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      na(
        this.flatCoordinates,
        0,
        this.ends_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), sa(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      this.maxDelta_,
      true,
      e,
      t,
      n,
      s
    ));
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(e, t) {
    return oa(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride,
      e,
      t
    );
  }
  /**
   * Return the area of the polygon on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return eh(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride
    );
  }
  /**
   * Get the coordinate array for this geometry.  This array has the structure
   * of a GeoJSON coordinate array for polygons.
   *
   * @param {boolean} [right] Orient coordinates according to the right-hand
   *     rule (counter-clockwise for exterior and clockwise for interior rings).
   *     If `false`, coordinates will be oriented according to the left-hand rule
   *     (clockwise for exterior and counter-clockwise for interior rings).
   *     By default, coordinate orientation will depend on how the geometry was
   *     constructed.
   * @return {Array<Array<import("../coordinate.js").Coordinate>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates(e) {
    let t;
    return e !== void 0 ? (t = this.getOrientedFlatCoordinates().slice(), Lo(t, 0, this.ends_, this.stride, e)) : t = this.flatCoordinates, Qi(t, 0, this.ends_, this.stride);
  }
  /**
   * @return {Array<number>} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * @return {Array<number>} Interior point.
   */
  getFlatInteriorPoint() {
    if (this.flatInteriorPointRevision_ != this.getRevision()) {
      const e = pn(this.getExtent());
      this.flatInteriorPoint_ = va(
        this.getOrientedFlatCoordinates(),
        0,
        this.ends_,
        this.stride,
        e,
        0
      ), this.flatInteriorPointRevision_ = this.getRevision();
    }
    return (
      /** @type {import("../coordinate.js").Coordinate} */
      this.flatInteriorPoint_
    );
  }
  /**
   * Return an interior point of the polygon.
   * @return {Point} Interior point as XYM coordinate, where M is the
   * length of the horizontal intersection that the point belongs to.
   * @api
   */
  getInteriorPoint() {
    return new he(this.getFlatInteriorPoint(), "XYM");
  }
  /**
   * Return the number of rings of the polygon,  this includes the exterior
   * ring and any interior rings.
   *
   * @return {number} Number of rings.
   * @api
   */
  getLinearRingCount() {
    return this.ends_.length;
  }
  /**
   * Return the Nth linear ring of the polygon geometry. Return `null` if the
   * given index is out of range.
   * The exterior linear ring is available at index `0` and the interior rings
   * at index `1` and beyond.
   *
   * @param {number} index Index.
   * @return {LinearRing|null} Linear ring.
   * @api
   */
  getLinearRing(e) {
    return e < 0 || this.ends_.length <= e ? null : new _l(
      this.flatCoordinates.slice(
        e === 0 ? 0 : this.ends_[e - 1],
        this.ends_[e]
      ),
      this.layout
    );
  }
  /**
   * Return the linear rings of the polygon.
   * @return {Array<LinearRing>} Linear rings.
   * @api
   */
  getLinearRings() {
    const e = this.layout, t = this.flatCoordinates, n = this.ends_, s = [];
    let r = 0;
    for (let o = 0, a = n.length; o < a; ++o) {
      const l = n[o], c = new _l(
        t.slice(r, l),
        e
      );
      s.push(c), r = l;
    }
    return s;
  }
  /**
   * @return {Array<number>} Oriented flat coordinates.
   */
  getOrientedFlatCoordinates() {
    if (this.orientedRevision_ != this.getRevision()) {
      const e = this.flatCoordinates;
      nh(e, 0, this.ends_, this.stride) ? this.orientedFlatCoordinates_ = e : (this.orientedFlatCoordinates_ = e.slice(), this.orientedFlatCoordinates_.length = Lo(
        this.orientedFlatCoordinates_,
        0,
        this.ends_,
        this.stride
      )), this.orientedRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.orientedFlatCoordinates_
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {Polygon} Simplified Polygon.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(e) {
    const t = [], n = [];
    return t.length = la(
      this.flatCoordinates,
      0,
      this.ends_,
      this.stride,
      Math.sqrt(e),
      t,
      0,
      n
    ), new _xi(t, "XY", n);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "Polygon";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return kc(
      this.getOrientedFlatCoordinates(),
      0,
      this.ends_,
      this.stride,
      e
    );
  }
  /**
   * Set the coordinates of the polygon.
   * @param {!Array<Array<import("../coordinate.js").Coordinate>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 2), this.flatCoordinates || (this.flatCoordinates = []);
    const n = ra(
      this.flatCoordinates,
      0,
      e,
      this.stride,
      this.ends_
    );
    this.flatCoordinates.length = n.length === 0 ? 0 : n[n.length - 1], this.changed();
  }
};
var at = xi;
function Sl(i) {
  if (hs(i))
    throw new Error("Cannot create polygon from empty extent");
  const e = i[0], t = i[1], n = i[2], s = i[3], r = [
    e,
    t,
    e,
    s,
    n,
    s,
    n,
    t,
    e,
    t
  ];
  return new xi(r, "XY", [r.length]);
}
function qf(i, e, t) {
  e = e || 32;
  const n = i.getStride(), s = i.getLayout(), r = i.getCenter(), o = n * (e + 1), a = new Array(o);
  for (let h2 = 0; h2 < o; h2 += n) {
    a[h2] = 0, a[h2 + 1] = 0;
    for (let u = 2; u < n; u++)
      a[h2 + u] = r[u];
  }
  const l = [a.length], c = new xi(a, s, l);
  return ih(c, r, i.getRadius(), t), c;
}
function ih(i, e, t, n) {
  const s = i.getFlatCoordinates(), r = i.getStride(), o = s.length / r - 1, a = n || 0;
  for (let l = 0; l <= o; ++l) {
    const c = l * r, h2 = a + gn(l, o) * 2 * Math.PI / o;
    s[c] = e[0] + t * Math.cos(h2), s[c + 1] = e[1] + t * Math.sin(h2);
  }
  i.changed();
}
function sh(i, e, t, n) {
  const s = [];
  let r = _t();
  for (let o = 0, a = t.length; o < a; ++o) {
    const l = t[o];
    r = Xo(
      i,
      e,
      l[0],
      n
    ), s.push((r[0] + r[2]) / 2, (r[1] + r[3]) / 2), e = l[l.length - 1];
  }
  return s;
}
var ir = class _ir extends wn {
  /**
   * @param {Array<Array<Array<import("../coordinate.js").Coordinate>>|Polygon>|Array<number>} coordinates Coordinates.
   *     For internal use, flat coordinates in combination with `layout` and `endss` are also accepted.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @param {Array<Array<number>>} [endss] Array of ends for internal use with flat coordinates.
   */
  constructor(e, t, n) {
    if (super(), this.endss_ = [], this.flatInteriorPointsRevision_ = -1, this.flatInteriorPoints_ = null, this.maxDelta_ = -1, this.maxDeltaRevision_ = -1, this.orientedRevision_ = -1, this.orientedFlatCoordinates_ = null, !n && !Array.isArray(e[0])) {
      const s = (
        /** @type {Array<Polygon>} */
        e
      ), r = [], o = [];
      for (let a = 0, l = s.length; a < l; ++a) {
        const c = s[a], h2 = r.length, u = c.getEnds();
        for (let d = 0, f = u.length; d < f; ++d)
          u[d] += h2;
        At(r, c.getFlatCoordinates()), o.push(u);
      }
      t = s.length === 0 ? this.getLayout() : s[0].getLayout(), e = r, n = o;
    }
    t !== void 0 && n ? (this.setFlatCoordinates(
      t,
      /** @type {Array<number>} */
      e
    ), this.endss_ = n) : this.setCoordinates(
      /** @type {Array<Array<Array<import("../coordinate.js").Coordinate>>>} */
      e,
      t
    );
  }
  /**
   * Append the passed polygon to this multipolygon.
   * @param {Polygon} polygon Polygon.
   * @api
   */
  appendPolygon(e) {
    let t;
    if (!this.flatCoordinates)
      this.flatCoordinates = e.getFlatCoordinates().slice(), t = e.getEnds().slice(), this.endss_.push();
    else {
      const n = this.flatCoordinates.length;
      At(this.flatCoordinates, e.getFlatCoordinates()), t = e.getEnds().slice();
      for (let s = 0, r = t.length; s < r; ++s)
        t[s] += n;
    }
    this.endss_.push(t), this.changed();
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!MultiPolygon} Clone.
   * @api
   * @override
   */
  clone() {
    const e = this.endss_.length, t = new Array(e);
    for (let s = 0; s < e; ++s)
      t[s] = this.endss_[s].slice();
    const n = new _ir(
      this.flatCoordinates.slice(),
      this.layout,
      t
    );
    return n.applyProperties(this), n;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    return s < Bn(this.getExtent(), e, t) ? s : (this.maxDeltaRevision_ != this.getRevision() && (this.maxDelta_ = Math.sqrt(
      Hd(
        this.flatCoordinates,
        0,
        this.endss_,
        this.stride,
        0
      )
    ), this.maxDeltaRevision_ = this.getRevision()), Zd(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      this.maxDelta_,
      true,
      e,
      t,
      n,
      s
    ));
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(e, t) {
    return ef(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      e,
      t
    );
  }
  /**
   * Return the area of the multipolygon on projected plane.
   * @return {number} Area (on projected plane).
   * @api
   */
  getArea() {
    return $f(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride
    );
  }
  /**
   * Get the coordinate array for this geometry.  This array has the structure
   * of a GeoJSON coordinate array for multi-polygons.
   *
   * @param {boolean} [right] Orient coordinates according to the right-hand
   *     rule (counter-clockwise for exterior and clockwise for interior rings).
   *     If `false`, coordinates will be oriented according to the left-hand rule
   *     (clockwise for exterior and counter-clockwise for interior rings).
   *     By default, coordinate orientation will depend on how the geometry was
   *     constructed.
   * @return {Array<Array<Array<import("../coordinate.js").Coordinate>>>} Coordinates.
   * @api
   * @override
   */
  getCoordinates(e) {
    let t;
    return e !== void 0 ? (t = this.getOrientedFlatCoordinates().slice(), pl(
      t,
      0,
      this.endss_,
      this.stride,
      e
    )) : t = this.flatCoordinates, xo(
      t,
      0,
      this.endss_,
      this.stride
    );
  }
  /**
   * @return {Array<Array<number>>} Endss.
   */
  getEndss() {
    return this.endss_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoints() {
    if (this.flatInteriorPointsRevision_ != this.getRevision()) {
      const e = sh(
        this.flatCoordinates,
        0,
        this.endss_,
        this.stride
      );
      this.flatInteriorPoints_ = th(
        this.getOrientedFlatCoordinates(),
        0,
        this.endss_,
        this.stride,
        e
      ), this.flatInteriorPointsRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.flatInteriorPoints_
    );
  }
  /**
   * Return the interior points as {@link module:ol/geom/MultiPoint~MultiPoint multipoint}.
   * @return {MultiPoint} Interior points as XYM coordinates, where M is
   * the length of the horizontal intersection that the point belongs to.
   * @api
   */
  getInteriorPoints() {
    return new Jc(this.getFlatInteriorPoints().slice(), "XYM");
  }
  /**
   * @return {Array<number>} Oriented flat coordinates.
   */
  getOrientedFlatCoordinates() {
    if (this.orientedRevision_ != this.getRevision()) {
      const e = this.flatCoordinates;
      Hf(e, 0, this.endss_, this.stride) ? this.orientedFlatCoordinates_ = e : (this.orientedFlatCoordinates_ = e.slice(), this.orientedFlatCoordinates_.length = pl(
        this.orientedFlatCoordinates_,
        0,
        this.endss_,
        this.stride
      )), this.orientedRevision_ = this.getRevision();
    }
    return (
      /** @type {Array<number>} */
      this.orientedFlatCoordinates_
    );
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {MultiPolygon} Simplified MultiPolygon.
   * @protected
   * @override
   */
  getSimplifiedGeometryInternal(e) {
    const t = [], n = [];
    return t.length = rf(
      this.flatCoordinates,
      0,
      this.endss_,
      this.stride,
      Math.sqrt(e),
      t,
      0,
      n
    ), new _ir(t, "XY", n);
  }
  /**
   * Return the polygon at the specified index.
   * @param {number} index Index.
   * @return {Polygon} Polygon.
   * @api
   */
  getPolygon(e) {
    if (e < 0 || this.endss_.length <= e)
      return null;
    let t;
    if (e === 0)
      t = 0;
    else {
      const r = this.endss_[e - 1];
      t = r[r.length - 1];
    }
    const n = this.endss_[e].slice(), s = n[n.length - 1];
    if (t !== 0)
      for (let r = 0, o = n.length; r < o; ++r)
        n[r] -= t;
    return new at(
      this.flatCoordinates.slice(t, s),
      this.layout,
      n
    );
  }
  /**
   * Return the polygons of this multipolygon.
   * @return {Array<Polygon>} Polygons.
   * @api
   */
  getPolygons() {
    const e = this.layout, t = this.flatCoordinates, n = this.endss_, s = [];
    let r = 0;
    for (let o = 0, a = n.length; o < a; ++o) {
      const l = n[o].slice(), c = l[l.length - 1];
      if (r !== 0)
        for (let u = 0, d = l.length; u < d; ++u)
          l[u] -= r;
      const h2 = new at(
        t.slice(r, c),
        e,
        l
      );
      s.push(h2), r = c;
    }
    return s;
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "MultiPolygon";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    return nf(
      this.getOrientedFlatCoordinates(),
      0,
      this.endss_,
      this.stride,
      e
    );
  }
  /**
   * Set the coordinates of the multipolygon.
   * @param {!Array<Array<Array<import("../coordinate.js").Coordinate>>>} coordinates Coordinates.
   * @param {import("./Geometry.js").GeometryLayout} [layout] Layout.
   * @api
   * @override
   */
  setCoordinates(e, t) {
    this.setLayout(t, e, 3), this.flatCoordinates || (this.flatCoordinates = []);
    const n = qd(
      this.flatCoordinates,
      0,
      e,
      this.stride,
      this.endss_
    );
    if (n.length === 0)
      this.flatCoordinates.length = 0;
    else {
      const s = n[n.length - 1];
      this.flatCoordinates.length = s.length === 0 ? 0 : s[s.length - 1];
    }
    this.changed();
  }
};
var rh = ir;
var ze = {
  ANIMATING: 0,
  INTERACTING: 1
};
var mt = {
  /**
   * Triggered before a layer is rendered.
   * @event module:ol/render/Event~RenderEvent#prerender
   * @api
   */
  PRERENDER: "prerender",
  /**
   * Triggered after a layer is rendered.
   * @event module:ol/render/Event~RenderEvent#postrender
   * @api
   */
  POSTRENDER: "postrender",
  /**
   * Triggered before layers are composed.  When dispatched by the map, the event object will not have
   * a `context` set.  When dispatched by a layer, the event object will have a `context` set.  Only
   * WebGL layers currently dispatch this event.
   * @event module:ol/render/Event~RenderEvent#precompose
   * @api
   */
  PRECOMPOSE: "precompose",
  /**
   * Triggered after layers are composed.  When dispatched by the map, the event object will not have
   * a `context` set.  When dispatched by a layer, the event object will have a `context` set.  Only
   * WebGL layers currently dispatch this event.
   * @event module:ol/render/Event~RenderEvent#postcompose
   * @api
   */
  POSTCOMPOSE: "postcompose",
  /**
   * Triggered when rendering is complete, i.e. all sources and tiles have
   * finished loading for the current viewport, and all tiles are faded in.
   * The event object will not have a `context` set.
   * @event module:ol/render/Event~RenderEvent#rendercomplete
   * @api
   */
  RENDERCOMPLETE: "rendercomplete"
};
var Jf = class {
  /**
   * Render a geometry with a custom renderer.
   *
   * @param {import("../geom/SimpleGeometry.js").default} geometry Geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {Function} renderer Renderer.
   * @param {Function} hitDetectionRenderer Renderer.
   * @param {number} [index] Render order index.
   */
  drawCustom(e, t, n, s, r) {
  }
  /**
   * Render a geometry.
   *
   * @param {import("../geom/Geometry.js").default} geometry The geometry to render.
   */
  drawGeometry(e) {
  }
  /**
   * Set the rendering style.
   *
   * @param {import("../style/Style.js").default} style The rendering style.
   */
  setStyle(e) {
  }
  /**
   * @param {import("../geom/Circle.js").default} circleGeometry Circle geometry.
   * @param {import("../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawCircle(e, t, n) {
  }
  /**
   * @param {import("../Feature.js").default} feature Feature.
   * @param {import("../style/Style.js").default} style Style.
   * @param {number} [index] Render order index.
   */
  drawFeature(e, t, n) {
  }
  /**
   * @param {import("../geom/GeometryCollection.js").default} geometryCollectionGeometry Geometry collection.
   * @param {import("../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawGeometryCollection(e, t, n) {
  }
  /**
   * @param {import("../geom/LineString.js").default|import("./Feature.js").default} lineStringGeometry Line string geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawLineString(e, t, n) {
  }
  /**
   * @param {import("../geom/MultiLineString.js").default|import("./Feature.js").default} multiLineStringGeometry MultiLineString geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiLineString(e, t, n) {
  }
  /**
   * @param {import("../geom/MultiPoint.js").default|import("./Feature.js").default} multiPointGeometry MultiPoint geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiPoint(e, t, n) {
  }
  /**
   * @param {import("../geom/MultiPolygon.js").default} multiPolygonGeometry MultiPolygon geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawMultiPolygon(e, t, n) {
  }
  /**
   * @param {import("../geom/Point.js").default|import("./Feature.js").default} pointGeometry Point geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawPoint(e, t, n) {
  }
  /**
   * @param {import("../geom/Polygon.js").default|import("./Feature.js").default} polygonGeometry Polygon geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawPolygon(e, t, n) {
  }
  /**
   * @param {import("../geom/SimpleGeometry.js").default|import("./Feature.js").default} geometry Geometry.
   * @param {import("../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   */
  drawText(e, t, n) {
  }
  /**
   * @param {import("../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../style/Stroke.js").default} strokeStyle Stroke style.
   */
  setFillStrokeStyle(e, t) {
  }
  /**
   * @param {import("../style/Image.js").default} imageStyle Image style.
   * @param {import("../render/canvas.js").DeclutterImageWithText} [declutterImageWithText] Shared data for combined decluttering with a text style.
   */
  setImageStyle(e, t) {
  }
  /**
   * @param {import("../style/Text.js").default} textStyle Text style.
   * @param {import("../render/canvas.js").DeclutterImageWithText} [declutterImageWithText] Shared data for combined decluttering with an image style.
   */
  setTextStyle(e, t) {
  }
};
var oh = Jf;
var gs = {
  BEGIN_GEOMETRY: 0,
  BEGIN_PATH: 1,
  CIRCLE: 2,
  CLOSE_PATH: 3,
  CUSTOM: 4,
  DRAW_CHARS: 5,
  DRAW_IMAGE: 6,
  END_GEOMETRY: 7,
  FILL: 8,
  MOVE_TO_LINE_TO: 9,
  SET_FILL_STYLE: 10,
  SET_STROKE_STYLE: 11,
  STROKE: 12
};
var Is = [gs.FILL];
var fn = [gs.STROKE];
var kn = [gs.BEGIN_PATH];
var El = [gs.CLOSE_PATH];
var j = gs;
var Qf = class extends oh {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    super(), this.tolerance = e, this.maxExtent = t, this.pixelRatio = s, this.maxLineWidth = 0, this.resolution = n, this.beginGeometryInstruction1_ = null, this.beginGeometryInstruction2_ = null, this.bufferedMaxExtent_ = null, this.instructions = [], this.coordinates = [], this.tmpCoordinate_ = [], this.hitDetectionInstructions = [], this.state = /** @type {import("../canvas.js").FillStrokeState} */
    {};
  }
  /**
   * @protected
   * @param {Array<number>} dashArray Dash array.
   * @return {Array<number>} Dash array with pixel ratio applied
   */
  applyPixelRatio(e) {
    const t = this.pixelRatio;
    return t == 1 ? e : e.map(function(n) {
      return n * t;
    });
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} stride Stride.
   * @protected
   * @return {number} My end
   */
  appendFlatPointCoordinates(e, t) {
    const n = this.getBufferedMaxExtent(), s = this.tmpCoordinate_, r = this.coordinates;
    let o = r.length;
    for (let a = 0, l = e.length; a < l; a += t)
      s[0] = e[a], s[1] = e[a + 1], _i(n, s) && (r[o++] = s[0], r[o++] = s[1]);
    return o;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @param {boolean} closed Last input coordinate equals first.
   * @param {boolean} skipFirst Skip first coordinate.
   * @protected
   * @return {number} My end.
   */
  appendFlatLineCoordinates(e, t, n, s, r, o) {
    const a = this.coordinates;
    let l = a.length;
    const c = this.getBufferedMaxExtent();
    o && (t += s);
    let h2 = e[t], u = e[t + 1];
    const d = this.tmpCoordinate_;
    let f = true, g, y, m;
    for (g = t + s; g < n; g += s)
      d[0] = e[g], d[1] = e[g + 1], m = fo(c, d), m !== y ? (f && (a[l++] = h2, a[l++] = u, f = false), a[l++] = d[0], a[l++] = d[1]) : m === De.INTERSECTING ? (a[l++] = d[0], a[l++] = d[1], f = false) : f = true, h2 = d[0], u = d[1], y = m;
    return (r && f || g === t + s) && (a[l++] = h2, a[l++] = u), l;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @param {Array<number>} builderEnds Builder ends.
   * @return {number} Offset.
   */
  drawCustomCoordinates_(e, t, n, s, r) {
    for (let o = 0, a = n.length; o < a; ++o) {
      const l = n[o], c = this.appendFlatLineCoordinates(
        e,
        t,
        l,
        s,
        false,
        false
      );
      r.push(c), t = l;
    }
    return t;
  }
  /**
   * @param {import("../../geom/SimpleGeometry.js").default} geometry Geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {Function} renderer Renderer.
   * @param {Function} hitDetectionRenderer Renderer.
   * @param {number} [index] Render order index.
   * @override
   */
  drawCustom(e, t, n, s, r) {
    this.beginGeometry(e, t, r);
    const o = e.getType(), a = e.getStride(), l = this.coordinates.length;
    let c, h2, u, d, f;
    switch (o) {
      case "MultiPolygon":
        c = /** @type {import("../../geom/MultiPolygon.js").default} */
        e.getOrientedFlatCoordinates(), d = [];
        const g = (
          /** @type {import("../../geom/MultiPolygon.js").default} */
          e.getEndss()
        );
        f = 0;
        for (let y = 0, m = g.length; y < m; ++y) {
          const _ = [];
          f = this.drawCustomCoordinates_(
            c,
            f,
            g[y],
            a,
            _
          ), d.push(_);
        }
        this.instructions.push([
          j.CUSTOM,
          l,
          d,
          e,
          n,
          xo,
          r
        ]), this.hitDetectionInstructions.push([
          j.CUSTOM,
          l,
          d,
          e,
          s || n,
          xo,
          r
        ]);
        break;
      case "Polygon":
      case "MultiLineString":
        u = [], c = o == "Polygon" ? (
          /** @type {import("../../geom/Polygon.js").default} */
          e.getOrientedFlatCoordinates()
        ) : e.getFlatCoordinates(), f = this.drawCustomCoordinates_(
          c,
          0,
          /** @type {import("../../geom/Polygon.js").default|import("../../geom/MultiLineString.js").default} */
          e.getEnds(),
          a,
          u
        ), this.instructions.push([
          j.CUSTOM,
          l,
          u,
          e,
          n,
          Qi,
          r
        ]), this.hitDetectionInstructions.push([
          j.CUSTOM,
          l,
          u,
          e,
          s || n,
          Qi,
          r
        ]);
        break;
      case "LineString":
      case "Circle":
        c = e.getFlatCoordinates(), h2 = this.appendFlatLineCoordinates(
          c,
          0,
          c.length,
          a,
          false,
          false
        ), this.instructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          n,
          hn,
          r
        ]), this.hitDetectionInstructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          s || n,
          hn,
          r
        ]);
        break;
      case "MultiPoint":
        c = e.getFlatCoordinates(), h2 = this.appendFlatPointCoordinates(c, a), h2 > l && (this.instructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          n,
          hn,
          r
        ]), this.hitDetectionInstructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          s || n,
          hn,
          r
        ]));
        break;
      case "Point":
        c = e.getFlatCoordinates(), this.coordinates.push(c[0], c[1]), h2 = this.coordinates.length, this.instructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          n,
          void 0,
          r
        ]), this.hitDetectionInstructions.push([
          j.CUSTOM,
          l,
          h2,
          e,
          s || n,
          void 0,
          r
        ]);
        break;
    }
    this.endGeometry(t);
  }
  /**
   * @protected
   * @param {import("../../geom/Geometry").default|import("../Feature.js").default} geometry The geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} index Render order index
   */
  beginGeometry(e, t, n) {
    this.beginGeometryInstruction1_ = [
      j.BEGIN_GEOMETRY,
      t,
      0,
      e,
      n
    ], this.instructions.push(this.beginGeometryInstruction1_), this.beginGeometryInstruction2_ = [
      j.BEGIN_GEOMETRY,
      t,
      0,
      e,
      n
    ], this.hitDetectionInstructions.push(this.beginGeometryInstruction2_);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   */
  finish() {
    return {
      instructions: this.instructions,
      hitDetectionInstructions: this.hitDetectionInstructions,
      coordinates: this.coordinates
    };
  }
  /**
   * Reverse the hit detection instructions.
   */
  reverseHitDetectionInstructions() {
    const e = this.hitDetectionInstructions;
    e.reverse();
    let t;
    const n = e.length;
    let s, r, o = -1;
    for (t = 0; t < n; ++t)
      s = e[t], r = /** @type {import("./Instruction.js").default} */
      s[0], r == j.END_GEOMETRY ? o = t : r == j.BEGIN_GEOMETRY && (s[2] = t, Gu(this.hitDetectionInstructions, o, t), o = -1);
  }
  /**
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import('../canvas.js').FillStrokeState} [state] State.
   * @return {import('../canvas.js').FillStrokeState} State.
   */
  fillStyleToState(e, t = (
    /** @type {import('../canvas.js').FillStrokeState} */
    {}
  )) {
    if (e) {
      const n = e.getColor();
      t.fillPatternScale = n && typeof n == "object" && "src" in n ? this.pixelRatio : 1, t.fillStyle = Pt(
        n || Ze
      );
    } else
      t.fillStyle = void 0;
    return t;
  }
  /**
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {import("../canvas.js").FillStrokeState} State.
   */
  strokeStyleToState(e, t = (
    /** @type {import('../canvas.js').FillStrokeState} */
    {}
  )) {
    if (e) {
      const n = e.getColor();
      t.strokeStyle = Pt(
        n || ts
      );
      const s = e.getLineCap();
      t.lineCap = s !== void 0 ? s : wi;
      const r = e.getLineDash();
      t.lineDash = r ? r.slice() : Kt;
      const o = e.getLineDashOffset();
      t.lineDashOffset = o || Ht;
      const a = e.getLineJoin();
      t.lineJoin = a !== void 0 ? a : Ci;
      const l = e.getWidth();
      t.lineWidth = l !== void 0 ? l : is;
      const c = e.getMiterLimit();
      t.miterLimit = c !== void 0 ? c : es, t.lineWidth > this.maxLineWidth && (this.maxLineWidth = t.lineWidth, this.bufferedMaxExtent_ = null);
    } else
      t.strokeStyle = void 0, t.lineCap = void 0, t.lineDash = null, t.lineDashOffset = void 0, t.lineJoin = void 0, t.lineWidth = void 0, t.miterLimit = void 0;
    return t;
  }
  /**
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @override
   */
  setFillStrokeStyle(e, t) {
    const n = this.state;
    this.fillStyleToState(e, n), this.strokeStyleToState(t, n);
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {Array<*>} Fill instruction.
   */
  createFill(e) {
    const t = e.fillStyle, n = [j.SET_FILL_STYLE, t];
    return typeof t != "string" && n.push(e.fillPatternScale), n;
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   */
  applyStroke(e) {
    this.instructions.push(this.createStroke(e));
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @return {Array<*>} Stroke instruction.
   */
  createStroke(e) {
    return [
      j.SET_STROKE_STYLE,
      e.strokeStyle,
      e.lineWidth * this.pixelRatio,
      e.lineCap,
      e.lineJoin,
      e.miterLimit,
      e.lineDash ? this.applyPixelRatio(e.lineDash) : null,
      e.lineDashOffset * this.pixelRatio
    ];
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @param {function(this:CanvasBuilder, import("../canvas.js").FillStrokeState):Array<*>} createFill Create fill.
   */
  updateFillStyle(e, t) {
    const n = e.fillStyle;
    (typeof n != "string" || e.currentFillStyle != n) && (this.instructions.push(t.call(this, e)), e.currentFillStyle = n);
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @param {function(this:CanvasBuilder, import("../canvas.js").FillStrokeState): void} applyStroke Apply stroke.
   */
  updateStrokeStyle(e, t) {
    const n = e.strokeStyle, s = e.lineCap, r = e.lineDash, o = e.lineDashOffset, a = e.lineJoin, l = e.lineWidth, c = e.miterLimit;
    (e.currentStrokeStyle != n || e.currentLineCap != s || r != e.currentLineDash && !Sn(e.currentLineDash, r) || e.currentLineDashOffset != o || e.currentLineJoin != a || e.currentLineWidth != l || e.currentMiterLimit != c) && (t.call(this, e), e.currentStrokeStyle = n, e.currentLineCap = s, e.currentLineDash = r, e.currentLineDashOffset = o, e.currentLineJoin = a, e.currentLineWidth = l, e.currentMiterLimit = c);
  }
  /**
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   */
  endGeometry(e) {
    this.beginGeometryInstruction1_[2] = this.instructions.length, this.beginGeometryInstruction1_ = null, this.beginGeometryInstruction2_[2] = this.hitDetectionInstructions.length, this.beginGeometryInstruction2_ = null;
    const t = [j.END_GEOMETRY, e];
    this.instructions.push(t), this.hitDetectionInstructions.push(t);
  }
  /**
   * Get the buffered rendering extent.  Rendering will be clipped to the extent
   * provided to the constructor.  To account for symbolizers that may intersect
   * this extent, we calculate a buffered extent (e.g. based on stroke width).
   * @return {import("../../extent.js").Extent} The buffered rendering extent.
   * @protected
   */
  getBufferedMaxExtent() {
    if (!this.bufferedMaxExtent_ && (this.bufferedMaxExtent_ = dc(this.maxExtent), this.maxLineWidth > 0)) {
      const e = this.resolution * (this.maxLineWidth + 1) / 2;
      zo(this.bufferedMaxExtent_, e, this.bufferedMaxExtent_);
    }
    return this.bufferedMaxExtent_;
  }
};
var ms = Qf;
var eg = class extends ms {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    super(e, t, n, s), this.hitDetectionImage_ = null, this.image_ = null, this.imagePixelRatio_ = void 0, this.anchorX_ = void 0, this.anchorY_ = void 0, this.height_ = void 0, this.opacity_ = void 0, this.originX_ = void 0, this.originY_ = void 0, this.rotateWithView_ = void 0, this.rotation_ = void 0, this.scale_ = void 0, this.width_ = void 0, this.declutterMode_ = void 0, this.declutterImageWithText_ = void 0;
  }
  /**
   * @param {import("../../geom/Point.js").default|import("../Feature.js").default} pointGeometry Point geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawPoint(e, t, n) {
    if (!this.image_ || this.maxExtent && !_i(this.maxExtent, e.getFlatCoordinates()))
      return;
    this.beginGeometry(e, t, n);
    const s = e.getFlatCoordinates(), r = e.getStride(), o = this.coordinates.length, a = this.appendFlatPointCoordinates(s, r);
    this.instructions.push([
      j.DRAW_IMAGE,
      o,
      a,
      this.image_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_ * this.imagePixelRatio_,
      this.anchorY_ * this.imagePixelRatio_,
      Math.ceil(this.height_ * this.imagePixelRatio_),
      this.opacity_,
      this.originX_ * this.imagePixelRatio_,
      this.originY_ * this.imagePixelRatio_,
      this.rotateWithView_,
      this.rotation_,
      [
        this.scale_[0] * this.pixelRatio / this.imagePixelRatio_,
        this.scale_[1] * this.pixelRatio / this.imagePixelRatio_
      ],
      Math.ceil(this.width_ * this.imagePixelRatio_),
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.hitDetectionInstructions.push([
      j.DRAW_IMAGE,
      o,
      a,
      this.hitDetectionImage_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_,
      this.anchorY_,
      this.height_,
      1,
      this.originX_,
      this.originY_,
      this.rotateWithView_,
      this.rotation_,
      this.scale_,
      this.width_,
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.endGeometry(t);
  }
  /**
   * @param {import("../../geom/MultiPoint.js").default|import("../Feature.js").default} multiPointGeometry MultiPoint geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiPoint(e, t, n) {
    if (!this.image_)
      return;
    this.beginGeometry(e, t, n);
    const s = e.getFlatCoordinates(), r = [];
    for (let l = 0, c = s.length; l < c; l += e.getStride())
      (!this.maxExtent || _i(this.maxExtent, s.slice(l, l + 2))) && r.push(
        s[l],
        s[l + 1]
      );
    const o = this.coordinates.length, a = this.appendFlatPointCoordinates(r, 2);
    this.instructions.push([
      j.DRAW_IMAGE,
      o,
      a,
      this.image_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_ * this.imagePixelRatio_,
      this.anchorY_ * this.imagePixelRatio_,
      Math.ceil(this.height_ * this.imagePixelRatio_),
      this.opacity_,
      this.originX_ * this.imagePixelRatio_,
      this.originY_ * this.imagePixelRatio_,
      this.rotateWithView_,
      this.rotation_,
      [
        this.scale_[0] * this.pixelRatio / this.imagePixelRatio_,
        this.scale_[1] * this.pixelRatio / this.imagePixelRatio_
      ],
      Math.ceil(this.width_ * this.imagePixelRatio_),
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.hitDetectionInstructions.push([
      j.DRAW_IMAGE,
      o,
      a,
      this.hitDetectionImage_,
      // Remaining arguments to DRAW_IMAGE are in alphabetical order
      this.anchorX_,
      this.anchorY_,
      this.height_,
      1,
      this.originX_,
      this.originY_,
      this.rotateWithView_,
      this.rotation_,
      this.scale_,
      this.width_,
      this.declutterMode_,
      this.declutterImageWithText_
    ]), this.endGeometry(t);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    return this.reverseHitDetectionInstructions(), this.anchorX_ = void 0, this.anchorY_ = void 0, this.hitDetectionImage_ = null, this.image_ = null, this.imagePixelRatio_ = void 0, this.height_ = void 0, this.scale_ = void 0, this.opacity_ = void 0, this.originX_ = void 0, this.originY_ = void 0, this.rotateWithView_ = void 0, this.rotation_ = void 0, this.width_ = void 0, super.finish();
  }
  /**
   * @param {import("../../style/Image.js").default} imageStyle Image style.
   * @param {Object} [sharedData] Shared data.
   * @override
   */
  setImageStyle(e, t) {
    const n = e.getAnchor(), s = e.getSize(), r = e.getOrigin();
    this.imagePixelRatio_ = e.getPixelRatio(this.pixelRatio), this.anchorX_ = n[0], this.anchorY_ = n[1], this.hitDetectionImage_ = e.getHitDetectionImage(), this.image_ = e.getImage(this.pixelRatio), this.height_ = s[1], this.opacity_ = e.getOpacity(), this.originX_ = r[0], this.originY_ = r[1], this.rotateWithView_ = e.getRotateWithView(), this.rotation_ = e.getRotation(), this.scale_ = e.getScaleArray(), this.width_ = s[0], this.declutterMode_ = e.getDeclutterMode(), this.declutterImageWithText_ = t;
  }
};
var tg = eg;
var ng = class extends ms {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    super(e, t, n, s);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   * @return {number} end.
   */
  drawFlatCoordinates_(e, t, n, s) {
    const r = this.coordinates.length, o = this.appendFlatLineCoordinates(
      e,
      t,
      n,
      s,
      false,
      false
    ), a = [
      j.MOVE_TO_LINE_TO,
      r,
      o
    ];
    return this.instructions.push(a), this.hitDetectionInstructions.push(a), n;
  }
  /**
   * @param {import("../../geom/LineString.js").default|import("../Feature.js").default} lineStringGeometry Line string geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawLineString(e, t, n) {
    const s = this.state, r = s.strokeStyle, o = s.lineWidth;
    if (r === void 0 || o === void 0)
      return;
    this.updateStrokeStyle(s, this.applyStroke), this.beginGeometry(e, t, n), this.hitDetectionInstructions.push(
      [
        j.SET_STROKE_STYLE,
        s.strokeStyle,
        s.lineWidth,
        s.lineCap,
        s.lineJoin,
        s.miterLimit,
        Kt,
        Ht
      ],
      kn
    );
    const a = e.getFlatCoordinates(), l = e.getStride();
    this.drawFlatCoordinates_(
      a,
      0,
      a.length,
      l
    ), this.hitDetectionInstructions.push(fn), this.endGeometry(t);
  }
  /**
   * @param {import("../../geom/MultiLineString.js").default|import("../Feature.js").default} multiLineStringGeometry MultiLineString geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiLineString(e, t, n) {
    const s = this.state, r = s.strokeStyle, o = s.lineWidth;
    if (r === void 0 || o === void 0)
      return;
    this.updateStrokeStyle(s, this.applyStroke), this.beginGeometry(e, t, n), this.hitDetectionInstructions.push(
      [
        j.SET_STROKE_STYLE,
        s.strokeStyle,
        s.lineWidth,
        s.lineCap,
        s.lineJoin,
        s.miterLimit,
        Kt,
        Ht
      ],
      kn
    );
    const a = e.getEnds(), l = e.getFlatCoordinates(), c = e.getStride();
    let h2 = 0;
    for (let u = 0, d = a.length; u < d; ++u)
      h2 = this.drawFlatCoordinates_(
        l,
        h2,
        /** @type {number} */
        a[u],
        c
      );
    this.hitDetectionInstructions.push(fn), this.endGeometry(t);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    const e = this.state;
    return e.lastStroke != null && e.lastStroke != this.coordinates.length && this.instructions.push(fn), this.reverseHitDetectionInstructions(), this.state = null, super.finish();
  }
  /**
   * @param {import("../canvas.js").FillStrokeState} state State.
   * @override
   */
  applyStroke(e) {
    e.lastStroke != null && e.lastStroke != this.coordinates.length && (this.instructions.push(fn), e.lastStroke = this.coordinates.length), e.lastStroke = 0, super.applyStroke(e), this.instructions.push(kn);
  }
};
var ig = ng;
var sg = class extends ms {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    super(e, t, n, s);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @private
   * @return {number} End.
   */
  drawFlatCoordinatess_(e, t, n, s) {
    const r = this.state, o = r.fillStyle !== void 0, a = r.strokeStyle !== void 0, l = n.length;
    this.instructions.push(kn), this.hitDetectionInstructions.push(kn);
    for (let c = 0; c < l; ++c) {
      const h2 = n[c], u = this.coordinates.length, d = this.appendFlatLineCoordinates(
        e,
        t,
        h2,
        s,
        true,
        !a
      ), f = [
        j.MOVE_TO_LINE_TO,
        u,
        d
      ];
      this.instructions.push(f), this.hitDetectionInstructions.push(f), a && (this.instructions.push(El), this.hitDetectionInstructions.push(El)), t = h2;
    }
    return o && (this.instructions.push(Is), this.hitDetectionInstructions.push(Is)), a && (this.instructions.push(fn), this.hitDetectionInstructions.push(fn)), t;
  }
  /**
   * @param {import("../../geom/Circle.js").default} circleGeometry Circle geometry.
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawCircle(e, t, n) {
    const s = this.state, r = s.fillStyle, o = s.strokeStyle;
    if (r === void 0 && o === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(e, t, n), s.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_FILL_STYLE,
      Ze
    ]), s.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_STROKE_STYLE,
      s.strokeStyle,
      s.lineWidth,
      s.lineCap,
      s.lineJoin,
      s.miterLimit,
      Kt,
      Ht
    ]);
    const a = e.getFlatCoordinates(), l = e.getStride(), c = this.coordinates.length;
    this.appendFlatLineCoordinates(
      a,
      0,
      a.length,
      l,
      false,
      false
    );
    const h2 = [j.CIRCLE, c];
    this.instructions.push(kn, h2), this.hitDetectionInstructions.push(kn, h2), s.fillStyle !== void 0 && (this.instructions.push(Is), this.hitDetectionInstructions.push(Is)), s.strokeStyle !== void 0 && (this.instructions.push(fn), this.hitDetectionInstructions.push(fn)), this.endGeometry(t);
  }
  /**
   * @param {import("../../geom/Polygon.js").default|import("../Feature.js").default} polygonGeometry Polygon geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawPolygon(e, t, n) {
    const s = this.state, r = s.fillStyle, o = s.strokeStyle;
    if (r === void 0 && o === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(e, t, n), s.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_FILL_STYLE,
      Ze
    ]), s.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_STROKE_STYLE,
      s.strokeStyle,
      s.lineWidth,
      s.lineCap,
      s.lineJoin,
      s.miterLimit,
      Kt,
      Ht
    ]);
    const a = e.getEnds(), l = e.getOrientedFlatCoordinates(), c = e.getStride();
    this.drawFlatCoordinatess_(
      l,
      0,
      /** @type {Array<number>} */
      a,
      c
    ), this.endGeometry(t);
  }
  /**
   * @param {import("../../geom/MultiPolygon.js").default} multiPolygonGeometry MultiPolygon geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawMultiPolygon(e, t, n) {
    const s = this.state, r = s.fillStyle, o = s.strokeStyle;
    if (r === void 0 && o === void 0)
      return;
    this.setFillStrokeStyles_(), this.beginGeometry(e, t, n), s.fillStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_FILL_STYLE,
      Ze
    ]), s.strokeStyle !== void 0 && this.hitDetectionInstructions.push([
      j.SET_STROKE_STYLE,
      s.strokeStyle,
      s.lineWidth,
      s.lineCap,
      s.lineJoin,
      s.miterLimit,
      Kt,
      Ht
    ]);
    const a = e.getEndss(), l = e.getOrientedFlatCoordinates(), c = e.getStride();
    let h2 = 0;
    for (let u = 0, d = a.length; u < d; ++u)
      h2 = this.drawFlatCoordinatess_(
        l,
        h2,
        a[u],
        c
      );
    this.endGeometry(t);
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    this.reverseHitDetectionInstructions(), this.state = null;
    const e = this.tolerance;
    if (e !== 0) {
      const t = this.coordinates;
      for (let n = 0, s = t.length; n < s; ++n)
        t[n] = Ln(t[n], e);
    }
    return super.finish();
  }
  /**
   * @private
   */
  setFillStrokeStyles_() {
    const e = this.state;
    this.updateFillStyle(e, this.createFill), this.updateStrokeStyle(e, this.applyStroke);
  }
};
var wl = sg;
function rg(i, e, t, n, s) {
  const r = [];
  let o = t, a = 0, l = e.slice(t, 2);
  for (; a < i && o + s < n; ) {
    const [c, h2] = l.slice(-2), u = e[o + s], d = e[o + s + 1], f = Math.sqrt(
      (u - c) * (u - c) + (d - h2) * (d - h2)
    );
    if (a += f, a >= i) {
      const g = (i - a + f) / f, y = ot(c, u, g), m = ot(h2, d, g);
      l.push(y, m), r.push(l), l = [y, m], a == i && (o += s), a = 0;
    } else if (a < i)
      l.push(
        e[o + s],
        e[o + s + 1]
      ), o += s;
    else {
      const g = f - a, y = ot(c, u, g / f), m = ot(h2, d, g / f);
      l.push(y, m), r.push(l), l = [y, m], a = 0, o += s;
    }
  }
  return a > 0 && r.push(l), r;
}
function og(i, e, t, n, s) {
  let r = t, o = t, a = 0, l = 0, c = t, h2, u, d, f, g, y, m, _, p, E;
  for (u = t; u < n; u += s) {
    const S = e[u], w = e[u + 1];
    g !== void 0 && (p = S - g, E = w - y, f = Math.sqrt(p * p + E * E), m !== void 0 && (l += d, h2 = Math.acos((m * p + _ * E) / (d * f)), h2 > i && (l > a && (a = l, r = c, o = u), l = 0, c = u - s)), d = f, m = p, _ = E), g = S, y = w;
  }
  return l += f, l > a ? [c, u] : [r, o];
}
var sr = {
  left: 0,
  center: 0.5,
  right: 1,
  top: 0,
  middle: 0.5,
  hanging: 0.2,
  alphabetic: 0.8,
  ideographic: 0.8,
  bottom: 1
};
var ag = class extends ms {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Maximum extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    super(e, t, n, s), this.labels_ = null, this.text_ = "", this.textOffsetX_ = 0, this.textOffsetY_ = 0, this.textRotateWithView_ = void 0, this.textKeepUpright_ = void 0, this.textRotation_ = 0, this.textFillState_ = null, this.fillStates = {}, this.fillStates[Ze] = { fillStyle: Ze }, this.textStrokeState_ = null, this.strokeStates = {}, this.textState_ = /** @type {import("../canvas.js").TextState} */
    {}, this.textStates = {}, this.textKey_ = "", this.fillKey_ = "", this.strokeKey_ = "", this.declutterMode_ = void 0, this.declutterImageWithText_ = void 0;
  }
  /**
   * @return {import("../canvas.js").SerializableInstructions} the serializable instructions.
   * @override
   */
  finish() {
    const e = super.finish();
    return e.textStates = this.textStates, e.fillStates = this.fillStates, e.strokeStates = this.strokeStates, e;
  }
  /**
   * @param {import("../../geom/SimpleGeometry.js").default|import("../Feature.js").default} geometry Geometry.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @param {number} [index] Render order index.
   * @override
   */
  drawText(e, t, n) {
    const s = this.textFillState_, r = this.textStrokeState_, o = this.textState_;
    if (this.text_ === "" || !o || !s && !r)
      return;
    const a = this.coordinates;
    let l = a.length;
    const c = e.getType();
    let h2 = null, u = e.getStride();
    if (o.placement === "line" && (c == "LineString" || c == "MultiLineString" || c == "Polygon" || c == "MultiPolygon")) {
      if (!$e(this.maxExtent, e.getExtent()))
        return;
      let d;
      if (h2 = e.getFlatCoordinates(), c == "LineString")
        d = [h2.length];
      else if (c == "MultiLineString")
        d = /** @type {import("../../geom/MultiLineString.js").default} */
        e.getEnds();
      else if (c == "Polygon")
        d = /** @type {import("../../geom/Polygon.js").default} */
        e.getEnds().slice(0, 1);
      else if (c == "MultiPolygon") {
        const m = (
          /** @type {import("../../geom/MultiPolygon.js").default} */
          e.getEndss()
        );
        d = [];
        for (let _ = 0, p = m.length; _ < p; ++_)
          d.push(m[_][0]);
      }
      this.beginGeometry(e, t, n);
      const f = o.repeat, g = f ? void 0 : o.textAlign;
      let y = 0;
      for (let m = 0, _ = d.length; m < _; ++m) {
        let p;
        f ? p = rg(
          f * this.resolution,
          h2,
          y,
          d[m],
          u
        ) : p = [h2.slice(y, d[m])];
        for (let E = 0, S = p.length; E < S; ++E) {
          const w = p[E];
          let x = 0, b = w.length;
          if (g == null) {
            const R = og(
              o.maxAngle,
              w,
              0,
              w.length,
              2
            );
            x = R[0], b = R[1];
          }
          for (let R = x; R < b; R += u)
            a.push(w[R], w[R + 1]);
          const v = a.length;
          y = d[m], this.drawChars_(l, v), l = v;
        }
      }
      this.endGeometry(t);
    } else {
      let d = o.overflow ? null : [];
      switch (c) {
        case "Point":
        case "MultiPoint":
          h2 = /** @type {import("../../geom/MultiPoint.js").default} */
          e.getFlatCoordinates();
          break;
        case "LineString":
          h2 = /** @type {import("../../geom/LineString.js").default} */
          e.getFlatMidpoint();
          break;
        case "Circle":
          h2 = /** @type {import("../../geom/Circle.js").default} */
          e.getCenter();
          break;
        case "MultiLineString":
          h2 = /** @type {import("../../geom/MultiLineString.js").default} */
          e.getFlatMidpoints(), u = 2;
          break;
        case "Polygon":
          h2 = /** @type {import("../../geom/Polygon.js").default} */
          e.getFlatInteriorPoint(), o.overflow || d.push(h2[2] / this.resolution), u = 3;
          break;
        case "MultiPolygon":
          const S = (
            /** @type {import("../../geom/MultiPolygon.js").default} */
            e.getFlatInteriorPoints()
          );
          h2 = [];
          for (let w = 0, x = S.length; w < x; w += 3)
            o.overflow || d.push(S[w + 2] / this.resolution), h2.push(S[w], S[w + 1]);
          if (h2.length === 0)
            return;
          u = 2;
          break;
      }
      const f = this.appendFlatPointCoordinates(h2, u);
      if (f === l)
        return;
      if (d && (f - l) / 2 !== h2.length / u) {
        let S = l / 2;
        d = d.filter((w, x) => {
          const b = a[(S + x) * 2] === h2[x * u] && a[(S + x) * 2 + 1] === h2[x * u + 1];
          return b || --S, b;
        });
      }
      this.saveTextStates_();
      const g = o.backgroundFill ? this.createFill(this.fillStyleToState(o.backgroundFill)) : null, y = o.backgroundStroke ? this.createStroke(this.strokeStyleToState(o.backgroundStroke)) : null;
      this.beginGeometry(e, t, n);
      let m = o.padding;
      if (m != An && (o.scale[0] < 0 || o.scale[1] < 0)) {
        let S = o.padding[0], w = o.padding[1], x = o.padding[2], b = o.padding[3];
        o.scale[0] < 0 && (w = -w, b = -b), o.scale[1] < 0 && (S = -S, x = -x), m = [S, w, x, b];
      }
      const _ = this.pixelRatio;
      this.instructions.push([
        j.DRAW_IMAGE,
        l,
        f,
        null,
        NaN,
        NaN,
        NaN,
        1,
        0,
        0,
        this.textRotateWithView_,
        this.textRotation_,
        [1, 1],
        NaN,
        this.declutterMode_,
        this.declutterImageWithText_,
        m == An ? An : m.map(function(S) {
          return S * _;
        }),
        g,
        y,
        this.text_,
        this.textKey_,
        this.strokeKey_,
        this.fillKey_,
        this.textOffsetX_,
        this.textOffsetY_,
        d
      ]);
      const p = 1 / _, E = g ? g.slice(0) : null;
      E && (E[1] = Ze), this.hitDetectionInstructions.push([
        j.DRAW_IMAGE,
        l,
        f,
        null,
        NaN,
        NaN,
        NaN,
        1,
        0,
        0,
        this.textRotateWithView_,
        this.textRotation_,
        [p, p],
        NaN,
        this.declutterMode_,
        this.declutterImageWithText_,
        m,
        E,
        y,
        this.text_,
        this.textKey_,
        this.strokeKey_,
        this.fillKey_ ? Ze : this.fillKey_,
        this.textOffsetX_,
        this.textOffsetY_,
        d
      ]), this.endGeometry(t);
    }
  }
  /**
   * @private
   */
  saveTextStates_() {
    const e = this.textStrokeState_, t = this.textState_, n = this.textFillState_, s = this.strokeKey_;
    e && (s in this.strokeStates || (this.strokeStates[s] = {
      strokeStyle: e.strokeStyle,
      lineCap: e.lineCap,
      lineDashOffset: e.lineDashOffset,
      lineWidth: e.lineWidth,
      lineJoin: e.lineJoin,
      miterLimit: e.miterLimit,
      lineDash: e.lineDash
    }));
    const r = this.textKey_;
    r in this.textStates || (this.textStates[r] = {
      font: t.font,
      textAlign: t.textAlign || ns,
      justify: t.justify,
      textBaseline: t.textBaseline || er,
      scale: t.scale
    });
    const o = this.fillKey_;
    n && (o in this.fillStates || (this.fillStates[o] = {
      fillStyle: n.fillStyle
    }));
  }
  /**
   * @private
   * @param {number} begin Begin.
   * @param {number} end End.
   */
  drawChars_(e, t) {
    const n = this.textStrokeState_, s = this.textState_, r = this.strokeKey_, o = this.textKey_, a = this.fillKey_;
    this.saveTextStates_();
    const l = this.pixelRatio, c = sr[s.textBaseline], h2 = this.textOffsetY_ * l, u = this.text_, d = n ? n.lineWidth * Math.abs(s.scale[0]) / 2 : 0;
    this.instructions.push([
      j.DRAW_CHARS,
      e,
      t,
      c,
      s.overflow,
      a,
      s.maxAngle,
      l,
      h2,
      r,
      d * l,
      u,
      o,
      1,
      this.declutterMode_,
      this.textKeepUpright_
    ]), this.hitDetectionInstructions.push([
      j.DRAW_CHARS,
      e,
      t,
      c,
      s.overflow,
      a && Ze,
      s.maxAngle,
      l,
      h2,
      r,
      d * l,
      u,
      o,
      1 / l,
      this.declutterMode_,
      this.textKeepUpright_
    ]);
  }
  /**
   * @param {import("../../style/Text.js").default} textStyle Text style.
   * @param {Object} [sharedData] Shared data.
   * @override
   */
  setTextStyle(e, t) {
    let n, s, r;
    if (!e)
      this.text_ = "";
    else {
      const o = e.getFill();
      o ? (s = this.textFillState_, s || (s = /** @type {import("../canvas.js").FillState} */
      {}, this.textFillState_ = s), s.fillStyle = Pt(
        o.getColor() || Ze
      )) : (s = null, this.textFillState_ = s);
      const a = e.getStroke();
      if (!a)
        r = null, this.textStrokeState_ = r;
      else {
        r = this.textStrokeState_, r || (r = /** @type {import("../canvas.js").StrokeState} */
        {}, this.textStrokeState_ = r);
        const y = a.getLineDash(), m = a.getLineDashOffset(), _ = a.getWidth(), p = a.getMiterLimit();
        r.lineCap = a.getLineCap() || wi, r.lineDash = y ? y.slice() : Kt, r.lineDashOffset = m === void 0 ? Ht : m, r.lineJoin = a.getLineJoin() || Ci, r.lineWidth = _ === void 0 ? is : _, r.miterLimit = p === void 0 ? es : p, r.strokeStyle = Pt(
          a.getColor() || ts
        );
      }
      n = this.textState_;
      const l = e.getFont() || Wc;
      Lf(l);
      const c = e.getScaleArray();
      n.overflow = e.getOverflow(), n.font = l, n.maxAngle = e.getMaxAngle(), n.placement = e.getPlacement(), n.textAlign = e.getTextAlign(), n.repeat = e.getRepeat(), n.justify = e.getJustify(), n.textBaseline = e.getTextBaseline() || er, n.backgroundFill = e.getBackgroundFill(), n.backgroundStroke = e.getBackgroundStroke(), n.padding = e.getPadding() || An, n.scale = c === void 0 ? [1, 1] : c;
      const h2 = e.getOffsetX(), u = e.getOffsetY(), d = e.getRotateWithView(), f = e.getKeepUpright(), g = e.getRotation();
      this.text_ = e.getText() || "", this.textOffsetX_ = h2 === void 0 ? 0 : h2, this.textOffsetY_ = u === void 0 ? 0 : u, this.textRotateWithView_ = d === void 0 ? false : d, this.textKeepUpright_ = f === void 0 ? true : f, this.textRotation_ = g === void 0 ? 0 : g, this.strokeKey_ = r ? (typeof r.strokeStyle == "string" ? r.strokeStyle : de(r.strokeStyle)) + r.lineCap + r.lineDashOffset + "|" + r.lineWidth + r.lineJoin + r.miterLimit + "[" + r.lineDash.join() + "]" : "", this.textKey_ = n.font + n.scale + (n.textAlign || "?") + (n.repeat || "?") + (n.justify || "?") + (n.textBaseline || "?"), this.fillKey_ = s && s.fillStyle ? typeof s.fillStyle == "string" ? s.fillStyle : "|" + de(s.fillStyle) : "";
    }
    this.declutterMode_ = e.getDeclutterMode(), this.declutterImageWithText_ = t;
  }
};
var lg = {
  Circle: wl,
  Default: ms,
  Image: tg,
  LineString: ig,
  Polygon: wl,
  Text: ag
};
var cg = class {
  /**
   * @param {number} tolerance Tolerance.
   * @param {import("../../extent.js").Extent} maxExtent Max extent.
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   */
  constructor(e, t, n, s) {
    this.tolerance_ = e, this.maxExtent_ = t, this.pixelRatio_ = s, this.resolution_ = n, this.buildersByZIndex_ = {};
  }
  /**
   * @return {!Object<string, !Object<import("../canvas.js").BuilderType, import("./Builder.js").SerializableInstructions>>} The serializable instructions
   */
  finish() {
    const e = {};
    for (const t in this.buildersByZIndex_) {
      e[t] = e[t] || {};
      const n = this.buildersByZIndex_[t];
      for (const s in n) {
        const r = n[s].finish();
        e[t][s] = r;
      }
    }
    return e;
  }
  /**
   * @param {number|undefined} zIndex Z index.
   * @param {import("../canvas.js").BuilderType} builderType Replay type.
   * @return {import("../VectorContext.js").default} Replay.
   */
  getBuilder(e, t) {
    const n = e !== void 0 ? e.toString() : "0";
    let s = this.buildersByZIndex_[n];
    s === void 0 && (s = {}, this.buildersByZIndex_[n] = s);
    let r = s[t];
    if (r === void 0) {
      const o = lg[t];
      r = new o(
        this.tolerance_,
        this.maxExtent_,
        this.resolution_,
        this.pixelRatio_
      ), s[t] = r;
    }
    return r;
  }
};
var hg = cg;
function ug(i, e, t, n, s, r, o, a, l, c, h2, u, d = true) {
  let f = i[e], g = i[e + 1], y = 0, m = 0, _ = 0, p = 0;
  function E() {
    y = f, m = g, e += n, f = i[e], g = i[e + 1], p += _, _ = Math.sqrt((f - y) * (f - y) + (g - m) * (g - m));
  }
  do
    E();
  while (e < t - n && p + _ < r);
  let S = _ === 0 ? 0 : (r - p) / _;
  const w = ot(y, f, S), x = ot(m, g, S), b = e - n, v = p, R = r + a * l(c, s, h2);
  for (; e < t - n && p + _ < R; )
    E();
  S = _ === 0 ? 0 : (R - p) / _;
  const P = ot(y, f, S), U = ot(m, g, S);
  let N = false;
  if (d)
    if (u) {
      const H = [w, x, P, U];
      ea(H, 0, 4, 2, u, H, H), N = H[0] > H[2];
    } else
      N = w > P;
  const O = Math.PI, G = [], $ = b + n === e;
  e = b, _ = 0, p = v, f = i[e], g = i[e + 1];
  let z;
  if ($) {
    E(), z = Math.atan2(g - m, f - y), N && (z += z > 0 ? -O : O);
    const H = (P + w) / 2, V = (U + x) / 2;
    return G[0] = [H, V, (R - r) / 2, z, s], G;
  }
  s = s.replace(/\n/g, " ");
  for (let H = 0, V = s.length; H < V; ) {
    E();
    let q = Math.atan2(g - m, f - y);
    if (N && (q += q > 0 ? -O : O), z !== void 0) {
      let Ce = q - z;
      if (Ce += Ce > O ? -2 * O : Ce < -O ? 2 * O : 0, Math.abs(Ce) > o)
        return null;
    }
    z = q;
    const ne = H;
    let fe = 0;
    for (; H < V; ++H) {
      const Ce = N ? V - H - 1 : H, Oe = a * l(c, s[Ce], h2);
      if (e + n < t && p + _ < r + fe + Oe / 2)
        break;
      fe += Oe;
    }
    if (H === ne)
      continue;
    const F = N ? s.substring(V - ne, V - H) : s.substring(ne, H);
    S = _ === 0 ? 0 : (r + fe / 2 - p) / _;
    const Te = ot(y, f, S), Me = ot(m, g, S);
    G.push([Te, Me, fe / 2, q, F]), r += fe;
  }
  return G;
}
var dg = class {
  constructor() {
    Dr(this, "pushMethodArgs_", (...e) => (this.push_(e), this));
    this.instructions_ = [], this.zIndex = 0, this.offset_ = 0, this.context_ = /** @type {ZIndexContextProxy} */
    new Proxy(Js(), {
      get: (e, t) => {
        if (typeof /** @type {*} */
        Js()[t] == "function")
          return this.push_(t), this.pushMethodArgs_;
      },
      set: (e, t, n) => (this.push_(t, n), true)
    });
  }
  /**
   * @param {...*} args Arguments to push to the instructions array.
   * @private
   */
  push_(...e) {
    const t = this.instructions_, n = this.zIndex + this.offset_;
    t[n] || (t[n] = []), t[n].push(...e);
  }
  /**
   * Push a function that renders to the context directly.
   * @param {function(CanvasRenderingContext2D): void} render Function.
   */
  pushFunction(e) {
    this.push_(e);
  }
  /**
   * Get a proxy for CanvasRenderingContext2D which does not support getting state
   * (e.g. `context.globalAlpha`, which will return `undefined`). To set state, if it relies on a
   * previous state (e.g. `context.globalAlpha = context.globalAlpha / 2`), set a function,
   * e.g. `context.globalAlpha = (context) => context.globalAlpha / 2`.
   * @return {ZIndexContextProxy} Context.
   */
  getContext() {
    return this.context_;
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   */
  draw(e) {
    this.instructions_.forEach((t) => {
      for (let n = 0, s = t.length; n < s; ++n) {
        const r = t[n];
        if (typeof r == "function") {
          r(e);
          continue;
        }
        const o = t[++n];
        if (typeof /** @type {*} */
        e[r] == "function")
          e[r](...o);
        else {
          if (typeof o == "function") {
            e[r] = o(e);
            continue;
          }
          e[r] = o;
        }
      }
    });
  }
  clear() {
    this.instructions_.length = 0, this.zIndex = 0, this.offset_ = 0;
  }
  /**
   * Offsets the zIndex by the highest current zIndex. Useful for rendering multiple worlds or tiles, to
   * avoid conflicting context.clip() or context.save()/restore() calls.
   */
  offset() {
    this.offset_ = this.instructions_.length, this.zIndex = 0;
  }
};
var ah = dg;
var jn = _t();
var tn = [];
var Bt = [];
var zt = [];
var nn = [];
function Cl(i) {
  return i[3].declutterBox;
}
var xl = new RegExp(
  /* eslint-disable prettier/prettier */
  "[" + String.fromCharCode(1425) + "-" + String.fromCharCode(2303) + String.fromCharCode(64285) + "-" + String.fromCharCode(65023) + String.fromCharCode(65136) + "-" + String.fromCharCode(65276) + String.fromCharCode(67584) + "-" + String.fromCharCode(69631) + String.fromCharCode(124928) + "-" + String.fromCharCode(126975) + "]"
  /* eslint-enable prettier/prettier */
);
function jr(i, e) {
  return e === "start" ? e = xl.test(i) ? "right" : "left" : e === "end" && (e = xl.test(i) ? "left" : "right"), sr[e];
}
function fg(i, e, t) {
  return t > 0 && i.push(`
`, ""), i.push(e, ""), i;
}
function gg(i, e, t) {
  return t % 2 === 0 && (i += e), i;
}
var mg = class {
  /**
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   * @param {boolean} overlaps The replay can have overlapping geometries.
   * @param {import("../canvas.js").SerializableInstructions} instructions The serializable instructions.
   * @param {boolean} [deferredRendering] Enable deferred rendering.
   */
  constructor(e, t, n, s, r) {
    this.overlaps = n, this.pixelRatio = t, this.resolution = e, this.alignAndScaleFill_, this.instructions = s.instructions, this.coordinates = s.coordinates, this.coordinateCache_ = {}, this.renderedTransform_ = Ct(), this.hitDetectionInstructions = s.hitDetectionInstructions, this.pixelCoordinates_ = null, this.viewRotation_ = 0, this.fillStates = s.fillStates || {}, this.strokeStates = s.strokeStates || {}, this.textStates = s.textStates || {}, this.widths_ = {}, this.labels_ = {}, this.zIndexContext_ = r ? new ah() : null;
  }
  /**
   * @return {ZIndexContext} ZIndex context.
   */
  getZIndexContext() {
    return this.zIndexContext_;
  }
  /**
   * @param {string|Array<string>} text Text.
   * @param {string} textKey Text style key.
   * @param {string} fillKey Fill style key.
   * @param {string} strokeKey Stroke style key.
   * @return {import("../canvas.js").Label} Label.
   */
  createLabel(e, t, n, s) {
    const r = e + t + n + s;
    if (this.labels_[r])
      return this.labels_[r];
    const o = s ? this.strokeStates[s] : null, a = n ? this.fillStates[n] : null, l = this.textStates[t], c = this.pixelRatio, h2 = [
      l.scale[0] * c,
      l.scale[1] * c
    ], u = l.justify ? sr[l.justify] : jr(
      Array.isArray(e) ? e[0] : e,
      l.textAlign || ns
    ), d = s && o.lineWidth ? o.lineWidth : 0, f = Array.isArray(e) ? e : String(e).split(`
`).reduce(fg, []), { width: g, height: y, widths: m, heights: _, lineWidths: p } = Df(
      l,
      f
    ), E = g + d, S = [], w = (E + 2) * h2[0], x = (y + d) * h2[1], b = {
      width: w < 0 ? Math.floor(w) : Math.ceil(w),
      height: x < 0 ? Math.floor(x) : Math.ceil(x),
      contextInstructions: S
    };
    (h2[0] != 1 || h2[1] != 1) && S.push("scale", h2), s && (S.push("strokeStyle", o.strokeStyle), S.push("lineWidth", d), S.push("lineCap", o.lineCap), S.push("lineJoin", o.lineJoin), S.push("miterLimit", o.miterLimit), S.push("setLineDash", [o.lineDash]), S.push("lineDashOffset", o.lineDashOffset)), n && S.push("fillStyle", a.fillStyle), S.push("textBaseline", "middle"), S.push("textAlign", "center");
    const v = 0.5 - u;
    let R = u * E + v * d;
    const P = [], U = [];
    let N = 0, O = 0, G = 0, $ = 0, z;
    for (let H = 0, V = f.length; H < V; H += 2) {
      const q = f[H];
      if (q === `
`) {
        O += N, N = 0, R = u * E + v * d, ++$;
        continue;
      }
      const ne = f[H + 1] || l.font;
      ne !== z && (s && P.push("font", ne), n && U.push("font", ne), z = ne), N = Math.max(N, _[G]);
      const fe = [
        q,
        R + v * m[G] + u * (m[G] - p[$]),
        0.5 * (d + N) + O
      ];
      R += m[G], s && P.push("strokeText", fe), n && U.push("fillText", fe), ++G;
    }
    return Array.prototype.push.apply(S, P), Array.prototype.push.apply(S, U), this.labels_[r] = b, b;
  }
  /**
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import("../../coordinate.js").Coordinate} p1 1st point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p2 2nd point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p3 3rd point of the background box.
   * @param {import("../../coordinate.js").Coordinate} p4 4th point of the background box.
   * @param {Array<*>} fillInstruction Fill instruction.
   * @param {Array<*>} strokeInstruction Stroke instruction.
   */
  replayTextBackground_(e, t, n, s, r, o, a) {
    e.beginPath(), e.moveTo.apply(e, t), e.lineTo.apply(e, n), e.lineTo.apply(e, s), e.lineTo.apply(e, r), e.lineTo.apply(e, t), o && (this.alignAndScaleFill_ = /** @type {number} */
    o[2], e.fillStyle = /** @type {string} */
    o[1], this.fill_(e)), a && (this.setStrokeStyle_(
      e,
      /** @type {Array<*>} */
      a
    ), e.stroke());
  }
  /**
   * @private
   * @param {number} sheetWidth Width of the sprite sheet.
   * @param {number} sheetHeight Height of the sprite sheet.
   * @param {number} centerX X.
   * @param {number} centerY Y.
   * @param {number} width Width.
   * @param {number} height Height.
   * @param {number} anchorX Anchor X.
   * @param {number} anchorY Anchor Y.
   * @param {number} originX Origin X.
   * @param {number} originY Origin Y.
   * @param {number} rotation Rotation.
   * @param {import("../../size.js").Size} scale Scale.
   * @param {boolean} snapToPixel Snap to pixel.
   * @param {Array<number>} padding Padding.
   * @param {boolean} fillStroke Background fill or stroke.
   * @param {import("../../Feature.js").FeatureLike} feature Feature.
   * @return {ImageOrLabelDimensions} Dimensions for positioning and decluttering the image or label.
   */
  calculateImageOrLabelDimensions_(e, t, n, s, r, o, a, l, c, h2, u, d, f, g, y, m) {
    a *= d[0], l *= d[1];
    let _ = n - a, p = s - l;
    const E = r + c > e ? e - c : r, S = o + h2 > t ? t - h2 : o, w = g[3] + E * d[0] + g[1], x = g[0] + S * d[1] + g[2], b = _ - g[3], v = p - g[0];
    (y || u !== 0) && (tn[0] = b, nn[0] = b, tn[1] = v, Bt[1] = v, Bt[0] = b + w, zt[0] = Bt[0], zt[1] = v + x, nn[1] = zt[1]);
    let R;
    return u !== 0 ? (R = Zt(
      Ct(),
      n,
      s,
      1,
      1,
      u,
      -n,
      -s
    ), Pe(R, tn), Pe(R, Bt), Pe(R, zt), Pe(R, nn), kt(
      Math.min(tn[0], Bt[0], zt[0], nn[0]),
      Math.min(tn[1], Bt[1], zt[1], nn[1]),
      Math.max(tn[0], Bt[0], zt[0], nn[0]),
      Math.max(tn[1], Bt[1], zt[1], nn[1]),
      jn
    )) : kt(
      Math.min(b, b + w),
      Math.min(v, v + x),
      Math.max(b, b + w),
      Math.max(v, v + x),
      jn
    ), f && (_ = Math.round(_), p = Math.round(p)), {
      drawImageX: _,
      drawImageY: p,
      drawImageW: E,
      drawImageH: S,
      originX: c,
      originY: h2,
      declutterBox: {
        minX: jn[0],
        minY: jn[1],
        maxX: jn[2],
        maxY: jn[3],
        value: m
      },
      canvasTransform: R,
      scale: d
    };
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size.
   * @param {import("../canvas.js").Label|HTMLImageElement|HTMLCanvasElement|HTMLVideoElement} imageOrLabel Image.
   * @param {ImageOrLabelDimensions} dimensions Dimensions.
   * @param {number} opacity Opacity.
   * @param {Array<*>} fillInstruction Fill instruction.
   * @param {Array<*>} strokeInstruction Stroke instruction.
   * @return {boolean} The image or label was rendered.
   */
  replayImageOrLabel_(e, t, n, s, r, o, a) {
    const l = !!(o || a), c = s.declutterBox, h2 = a ? a[2] * s.scale[0] / 2 : 0;
    return c.minX - h2 <= t[0] && c.maxX + h2 >= 0 && c.minY - h2 <= t[1] && c.maxY + h2 >= 0 && (l && this.replayTextBackground_(
      e,
      tn,
      Bt,
      zt,
      nn,
      /** @type {Array<*>} */
      o,
      /** @type {Array<*>} */
      a
    ), Pf(
      e,
      s.canvasTransform,
      r,
      n,
      s.originX,
      s.originY,
      s.drawImageW,
      s.drawImageH,
      s.drawImageX,
      s.drawImageY,
      s.scale
    )), true;
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   */
  fill_(e) {
    const t = this.alignAndScaleFill_;
    if (t) {
      const n = Pe(this.renderedTransform_, [0, 0]), s = 512 * this.pixelRatio;
      e.save(), e.translate(n[0] % s, n[1] % s), t !== 1 && e.scale(t, t), e.rotate(this.viewRotation_);
    }
    e.fill(), t && e.restore();
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {Array<*>} instruction Instruction.
   */
  setStrokeStyle_(e, t) {
    e.strokeStyle = /** @type {import("../../colorlike.js").ColorLike} */
    t[1], t[1] && (e.lineWidth = /** @type {number} */
    t[2], e.lineCap = /** @type {CanvasLineCap} */
    t[3], e.lineJoin = /** @type {CanvasLineJoin} */
    t[4], e.miterLimit = /** @type {number} */
    t[5], e.lineDashOffset = /** @type {number} */
    t[7], e.setLineDash(
      /** @type {Array<number>} */
      t[6]
    ));
  }
  /**
   * @private
   * @param {string|Array<string>} text The text to draw.
   * @param {string} textKey The key of the text state.
   * @param {string} strokeKey The key for the stroke state.
   * @param {string} fillKey The key for the fill state.
   * @return {{label: import("../canvas.js").Label, anchorX: number, anchorY: number}} The text image and its anchor.
   */
  drawLabelWithPointPlacement_(e, t, n, s) {
    const r = this.textStates[t], o = this.createLabel(e, t, s, n), a = this.strokeStates[n], l = this.pixelRatio, c = jr(
      Array.isArray(e) ? e[0] : e,
      r.textAlign || ns
    ), h2 = sr[r.textBaseline || er], u = a && a.lineWidth ? a.lineWidth : 0, d = o.width / l - 2 * r.scale[0], f = c * d + 2 * (0.5 - c) * u, g = h2 * o.height / l + 2 * (0.5 - h2) * u;
    return {
      label: o,
      anchorX: f,
      anchorY: g
    };
  }
  /**
   * @private
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {Array<*>} instructions Instructions array.
   * @param {boolean} snapToPixel Snap point symbols and text to integer pixels.
   * @param {FeatureCallback<T>} [featureCallback] Feature callback.
   * @param {import("../../extent.js").Extent} [hitExtent] Only check
   *     features that intersect this extent.
   * @param {import("rbush").default<DeclutterEntry>} [declutterTree] Declutter tree.
   * @return {T|undefined} Callback result.
   * @template T
   */
  execute_(e, t, n, s, r, o, a, l) {
    const c = this.zIndexContext_;
    let h2;
    this.pixelCoordinates_ && Sn(n, this.renderedTransform_) ? h2 = this.pixelCoordinates_ : (this.pixelCoordinates_ || (this.pixelCoordinates_ = []), h2 = mn(
      this.coordinates,
      0,
      this.coordinates.length,
      2,
      n,
      this.pixelCoordinates_
    ), Nd(this.renderedTransform_, n));
    let u = 0;
    const d = s.length;
    let f = 0, g, y, m, _, p, E, S, w, x, b, v, R, P, U = 0, N = 0;
    const O = this.coordinateCache_, G = this.viewRotation_, $ = Math.round(Math.atan2(-n[1], n[0]) * 1e12) / 1e12, z = (
      /** @type {import("../../render.js").State} */
      {
        context: e,
        pixelRatio: this.pixelRatio,
        resolution: this.resolution,
        rotation: G
      }
    ), H = this.instructions != s || this.overlaps ? 0 : 200;
    let V, q, ne, fe;
    for (; u < d; ) {
      const F = s[u];
      switch (
        /** @type {import("./Instruction.js").default} */
        F[0]
      ) {
        case j.BEGIN_GEOMETRY:
          V = /** @type {import("../../Feature.js").FeatureLike} */
          F[1], fe = F[3], V.getGeometry() ? a !== void 0 && !$e(a, fe.getExtent()) ? u = /** @type {number} */
          F[2] + 1 : ++u : u = /** @type {number} */
          F[2], c && (c.zIndex = F[4]);
          break;
        case j.BEGIN_PATH:
          U > H && (this.fill_(e), U = 0), N > H && (e.stroke(), N = 0), !U && !N && (e.beginPath(), p = NaN, E = NaN), ++u;
          break;
        case j.CIRCLE:
          f = /** @type {number} */
          F[1];
          const Me = h2[f], Ce = h2[f + 1], Oe = h2[f + 2], be = h2[f + 3], it = Oe - Me, He = be - Ce, Ne = Math.sqrt(it * it + He * He);
          e.moveTo(Me + Ne, Ce), e.arc(Me, Ce, Ne, 0, 2 * Math.PI, true), ++u;
          break;
        case j.CLOSE_PATH:
          e.closePath(), ++u;
          break;
        case j.CUSTOM:
          f = /** @type {number} */
          F[1], g = F[2];
          const Cn = (
            /** @type {import("../../geom/SimpleGeometry.js").default} */
            F[3]
          ), Wn = F[4], Jt = F[5];
          z.geometry = Cn, z.feature = V, u in O || (O[u] = []);
          const Tt = O[u];
          Jt ? Jt(h2, f, g, 2, Tt) : (Tt[0] = h2[f], Tt[1] = h2[f + 1], Tt.length = 2), c && (c.zIndex = F[6]), Wn(Tt, z), ++u;
          break;
        case j.DRAW_IMAGE:
          f = /** @type {number} */
          F[1], g = /** @type {number} */
          F[2], x = /** @type {HTMLCanvasElement|HTMLVideoElement|HTMLImageElement} */
          F[3], y = /** @type {number} */
          F[4], m = /** @type {number} */
          F[5];
          let We = (
            /** @type {number} */
            F[6]
          );
          const st = (
            /** @type {number} */
            F[7]
          ), C = (
            /** @type {number} */
            F[8]
          ), T = (
            /** @type {number} */
            F[9]
          ), D = (
            /** @type {boolean} */
            F[10]
          );
          let A = (
            /** @type {number} */
            F[11]
          );
          const k = (
            /** @type {import("../../size.js").Size} */
            F[12]
          );
          let W = (
            /** @type {number} */
            F[13]
          );
          _ = F[14] || "declutter";
          const I = (
            /** @type {{args: import("../canvas.js").DeclutterImageWithText, declutterMode: import('../../style/Style.js').DeclutterMode}} */
            F[15]
          );
          if (!x && F.length >= 20) {
            b = /** @type {string} */
            F[19], v = /** @type {string} */
            F[20], R = /** @type {string} */
            F[21], P = /** @type {string} */
            F[22];
            const ct = this.drawLabelWithPointPlacement_(
              b,
              v,
              R,
              P
            );
            x = ct.label, F[3] = x;
            const Mn = (
              /** @type {number} */
              F[23]
            );
            y = (ct.anchorX - Mn) * this.pixelRatio, F[4] = y;
            const ht = (
              /** @type {number} */
              F[24]
            );
            m = (ct.anchorY - ht) * this.pixelRatio, F[5] = m, We = x.height, F[6] = We, W = x.width, F[13] = W;
          }
          let L;
          F.length > 25 && (L = /** @type {number} */
          F[25]);
          let B, re, ce;
          F.length > 17 ? (B = /** @type {Array<number>} */
          F[16], re = /** @type {Array<*>} */
          F[17], ce = /** @type {Array<*>} */
          F[18]) : (B = An, re = null, ce = null), D && $ ? A += G : !D && !$ && (A -= G);
          let Ie = 0;
          for (; f < g; f += 2) {
            if (L && L[Ie++] < W / this.pixelRatio)
              continue;
            const ct = this.calculateImageOrLabelDimensions_(
              x.width,
              x.height,
              h2[f],
              h2[f + 1],
              W,
              We,
              y,
              m,
              C,
              T,
              A,
              k,
              r,
              B,
              !!re || !!ce,
              V
            ), Mn = [
              e,
              t,
              x,
              ct,
              st,
              re,
              ce
            ];
            if (l) {
              let ht, bt, ut;
              if (I) {
                const ve = g - f;
                if (!I[ve]) {
                  I[ve] = { args: Mn, declutterMode: _ };
                  continue;
                }
                const Ye = I[ve];
                ht = Ye.args, bt = Ye.declutterMode, delete I[ve], ut = Cl(ht);
              }
              let Nt, Ut;
              if (ht && (bt !== "declutter" || !l.collides(ut)) && (Nt = true), (_ !== "declutter" || !l.collides(ct.declutterBox)) && (Ut = true), bt === "declutter" && _ === "declutter") {
                const ve = Nt && Ut;
                Nt = ve, Ut = ve;
              }
              Nt && (bt !== "none" && l.insert(ut), this.replayImageOrLabel_.apply(this, ht)), Ut && (_ !== "none" && l.insert(ct.declutterBox), this.replayImageOrLabel_.apply(this, Mn));
            } else
              this.replayImageOrLabel_.apply(this, Mn);
          }
          ++u;
          break;
        case j.DRAW_CHARS:
          const Ae = (
            /** @type {number} */
            F[1]
          ), rt = (
            /** @type {number} */
            F[2]
          ), lt = (
            /** @type {number} */
            F[3]
          ), Qt = (
            /** @type {number} */
            F[4]
          );
          P = /** @type {string} */
          F[5];
          const xn = (
            /** @type {number} */
            F[6]
          ), In = (
            /** @type {number} */
            F[7]
          ), Ot = (
            /** @type {number} */
            F[8]
          );
          R = /** @type {string} */
          F[9];
          const en = (
            /** @type {number} */
            F[10]
          );
          b = /** @type {string|Array<string>} */
          F[11], Array.isArray(b) && (b = b.reduce(gg, "")), v = /** @type {string} */
          F[12];
          const bi = [
            /** @type {number} */
            F[13],
            /** @type {number} */
            F[13]
          ];
          _ = F[14] || "declutter";
          const Fr = (
            /** @type {boolean} */
            F[15]
          ), Ri = this.textStates[v], vn = Ri.font, Li = [
            Ri.scale[0] * In,
            Ri.scale[1] * In
          ];
          let Fi;
          vn in this.widths_ ? Fi = this.widths_[vn] : (Fi = {}, this.widths_[vn] = Fi);
          const Wa = aa(h2, Ae, rt, 2), Ya = Math.abs(Li[0]) * fl(vn, b, Fi);
          if (Qt || Ya <= Wa) {
            const ct = this.textStates[v].textAlign, Mn = (Wa - Ya) * jr(b, ct), ht = ug(
              h2,
              Ae,
              rt,
              2,
              b,
              Mn,
              xn,
              Math.abs(Li[0]),
              fl,
              vn,
              Fi,
              $ ? 0 : this.viewRotation_,
              Fr
            );
            e:
              if (ht) {
                const bt = [];
                let ut, Nt, Ut, ve, Ye;
                if (R)
                  for (ut = 0, Nt = ht.length; ut < Nt; ++ut) {
                    Ye = ht[ut], Ut = /** @type {string} */
                    Ye[4], ve = this.createLabel(Ut, v, "", R), y = /** @type {number} */
                    Ye[2] + (Li[0] < 0 ? -en : en), m = lt * ve.height + (0.5 - lt) * 2 * en * Li[1] / Li[0] - Ot;
                    const Gt = this.calculateImageOrLabelDimensions_(
                      ve.width,
                      ve.height,
                      Ye[0],
                      Ye[1],
                      ve.width,
                      ve.height,
                      y,
                      m,
                      0,
                      0,
                      Ye[3],
                      bi,
                      false,
                      An,
                      false,
                      V
                    );
                    if (l && _ === "declutter" && l.collides(Gt.declutterBox))
                      break e;
                    bt.push([
                      e,
                      t,
                      ve,
                      Gt,
                      1,
                      null,
                      null
                    ]);
                  }
                if (P)
                  for (ut = 0, Nt = ht.length; ut < Nt; ++ut) {
                    Ye = ht[ut], Ut = /** @type {string} */
                    Ye[4], ve = this.createLabel(Ut, v, P, ""), y = /** @type {number} */
                    Ye[2], m = lt * ve.height - Ot;
                    const Gt = this.calculateImageOrLabelDimensions_(
                      ve.width,
                      ve.height,
                      Ye[0],
                      Ye[1],
                      ve.width,
                      ve.height,
                      y,
                      m,
                      0,
                      0,
                      Ye[3],
                      bi,
                      false,
                      An,
                      false,
                      V
                    );
                    if (l && _ === "declutter" && l.collides(Gt.declutterBox))
                      break e;
                    bt.push([
                      e,
                      t,
                      ve,
                      Gt,
                      1,
                      null,
                      null
                    ]);
                  }
                l && _ !== "none" && l.load(bt.map(Cl));
                for (let Gt = 0, pu = bt.length; Gt < pu; ++Gt)
                  this.replayImageOrLabel_.apply(this, bt[Gt]);
              }
          }
          ++u;
          break;
        case j.END_GEOMETRY:
          if (o !== void 0) {
            V = /** @type {import("../../Feature.js").FeatureLike} */
            F[1];
            const ct = o(
              V,
              fe,
              _
            );
            if (ct)
              return ct;
          }
          ++u;
          break;
        case j.FILL:
          H ? U++ : this.fill_(e), ++u;
          break;
        case j.MOVE_TO_LINE_TO:
          for (f = /** @type {number} */
          F[1], g = /** @type {number} */
          F[2], q = h2[f], ne = h2[f + 1], e.moveTo(q, ne), p = q + 0.5 | 0, E = ne + 0.5 | 0, f += 2; f < g; f += 2)
            q = h2[f], ne = h2[f + 1], S = q + 0.5 | 0, w = ne + 0.5 | 0, (f == g - 2 || S !== p || w !== E) && (e.lineTo(q, ne), p = S, E = w);
          ++u;
          break;
        case j.SET_FILL_STYLE:
          this.alignAndScaleFill_ = F[2], U && (this.fill_(e), U = 0, N && (e.stroke(), N = 0)), e.fillStyle = F[1], ++u;
          break;
        case j.SET_STROKE_STYLE:
          N && (e.stroke(), N = 0), this.setStrokeStyle_(
            e,
            /** @type {Array<*>} */
            F
          ), ++u;
          break;
        case j.STROKE:
          H ? N++ : e.stroke(), ++u;
          break;
        default:
          ++u;
          break;
      }
    }
    U && this.fill_(e), N && e.stroke();
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scaled canvas size.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {boolean} snapToPixel Snap point symbols and text to integer pixels.
   * @param {import("rbush").default<DeclutterEntry>} [declutterTree] Declutter tree.
   */
  execute(e, t, n, s, r, o) {
    this.viewRotation_ = s, this.execute_(
      e,
      t,
      n,
      this.instructions,
      r,
      void 0,
      void 0,
      o
    );
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {FeatureCallback<T>} [featureCallback] Feature callback.
   * @param {import("../../extent.js").Extent} [hitExtent] Only check
   *     features that intersect this extent.
   * @return {T|undefined} Callback result.
   * @template T
   */
  executeHitDetection(e, t, n, s, r) {
    return this.viewRotation_ = n, this.execute_(
      e,
      [e.canvas.width, e.canvas.height],
      t,
      this.hitDetectionInstructions,
      true,
      s,
      r
    );
  }
};
var yg = mg;
var Fn = [
  "Polygon",
  "Circle",
  "LineString",
  "Image",
  "Text",
  "Default"
];
var lh = ["Image", "Text"];
var _g = Fn.filter(
  (i) => !lh.includes(i)
);
var pg = class {
  /**
   * @param {import("../../extent.js").Extent} maxExtent Max extent for clipping. When a
   * `maxExtent` was set on the Builder for this executor group, the same `maxExtent`
   * should be set here, unless the target context does not exceed that extent (which
   * can be the case when rendering to tiles).
   * @param {number} resolution Resolution.
   * @param {number} pixelRatio Pixel ratio.
   * @param {boolean} overlaps The executor group can have overlapping geometries.
   * @param {!Object<string, !Object<import("../canvas.js").BuilderType, import("../canvas.js").SerializableInstructions>>} allInstructions
   * The serializable instructions.
   * @param {number} [renderBuffer] Optional rendering buffer.
   * @param {boolean} [deferredRendering] Enable deferred rendering with renderDeferred().
   */
  constructor(e, t, n, s, r, o, a) {
    this.maxExtent_ = e, this.overlaps_ = s, this.pixelRatio_ = n, this.resolution_ = t, this.renderBuffer_ = o, this.executorsByZIndex_ = {}, this.hitDetectionContext_ = null, this.hitDetectionTransform_ = Ct(), this.renderedContext_ = null, this.deferredZIndexContexts_ = {}, this.createExecutors_(r, a);
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../transform.js").Transform} transform Transform.
   */
  clip(e, t) {
    const n = this.getClipCoords(t);
    e.beginPath(), e.moveTo(n[0], n[1]), e.lineTo(n[2], n[3]), e.lineTo(n[4], n[5]), e.lineTo(n[6], n[7]), e.clip();
  }
  /**
   * Create executors and populate them using the provided instructions.
   * @private
   * @param {!Object<string, !Object<string, import("../canvas.js").SerializableInstructions>>} allInstructions The serializable instructions
   * @param {boolean} deferredRendering Enable deferred rendering.
   */
  createExecutors_(e, t) {
    for (const n in e) {
      let s = this.executorsByZIndex_[n];
      s === void 0 && (s = {}, this.executorsByZIndex_[n] = s);
      const r = e[n];
      for (const o in r) {
        const a = r[o];
        s[o] = new yg(
          this.resolution_,
          this.pixelRatio_,
          this.overlaps_,
          a,
          t
        );
      }
    }
  }
  /**
   * @param {Array<import("../canvas.js").BuilderType>} executors Executors.
   * @return {boolean} Has executors of the provided types.
   */
  hasExecutors(e) {
    for (const t in this.executorsByZIndex_) {
      const n = this.executorsByZIndex_[t];
      for (let s = 0, r = e.length; s < r; ++s)
        if (e[s] in n)
          return true;
    }
    return false;
  }
  /**
   * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {function(import("../../Feature.js").FeatureLike, import("../../geom/SimpleGeometry.js").default, number): T} callback Feature callback.
   * @param {Array<import("../../Feature.js").FeatureLike>} declutteredFeatures Decluttered features.
   * @return {T|undefined} Callback result.
   * @template T
   */
  forEachFeatureAtCoordinate(e, t, n, s, r, o) {
    s = Math.round(s);
    const a = s * 2 + 1, l = Zt(
      this.hitDetectionTransform_,
      s + 0.5,
      s + 0.5,
      1 / t,
      -1 / t,
      -n,
      -e[0],
      -e[1]
    ), c = !this.hitDetectionContext_;
    c && (this.hitDetectionContext_ = Fe(
      a,
      a
    ));
    const h2 = this.hitDetectionContext_;
    h2.canvas.width !== a || h2.canvas.height !== a ? (h2.canvas.width = a, h2.canvas.height = a) : c || h2.clearRect(0, 0, a, a);
    let u;
    this.renderBuffer_ !== void 0 && (u = _t(), Yi(u, e), zo(
      u,
      t * (this.renderBuffer_ + s),
      u
    ));
    const d = Sg(s);
    let f;
    function g(w, x, b) {
      const v = h2.getImageData(
        0,
        0,
        a,
        a
      ).data;
      for (let R = 0, P = d.length; R < P; R++)
        if (v[d[R]] > 0) {
          if (!o || b === "none" || f !== "Image" && f !== "Text" || o.includes(w)) {
            const U = (d[R] - 3) / 4, N = s - U % a, O = s - (U / a | 0), G = r(w, x, N * N + O * O);
            if (G)
              return G;
          }
          h2.clearRect(0, 0, a, a);
          break;
        }
    }
    const y = Object.keys(this.executorsByZIndex_).map(Number);
    y.sort(Yt);
    let m, _, p, E, S;
    for (m = y.length - 1; m >= 0; --m) {
      const w = y[m].toString();
      for (p = this.executorsByZIndex_[w], _ = Fn.length - 1; _ >= 0; --_)
        if (f = Fn[_], E = p[f], E !== void 0 && (S = E.executeHitDetection(
          h2,
          l,
          n,
          g,
          u
        ), S))
          return S;
    }
  }
  /**
   * @param {import("../../transform.js").Transform} transform Transform.
   * @return {Array<number>|null} Clip coordinates.
   */
  getClipCoords(e) {
    const t = this.maxExtent_;
    if (!t)
      return null;
    const n = t[0], s = t[1], r = t[2], o = t[3], a = [n, s, n, o, r, o, r, s];
    return mn(a, 0, 8, 2, e, a), a;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return yi(this.executorsByZIndex_);
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} targetContext Context.
   * @param {import('../../size.js').Size} scaledCanvasSize Scale of the context.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {boolean} snapToPixel Snap point symbols and test to integer pixel.
   * @param {Array<import("../canvas.js").BuilderType>} [builderTypes] Ordered replay types to replay.
   *     Default is {@link module:ol/render/replay~ALL}
   * @param {import("rbush").default<import('./Executor.js').DeclutterEntry>|null} [declutterTree] Declutter tree.
   *     When set to null, no decluttering is done, even when the executor group has a `ZIndexContext`.
   */
  execute(e, t, n, s, r, o, a) {
    const l = Object.keys(this.executorsByZIndex_).map(Number);
    l.sort(a ? Uu : Yt), o = o || Fn;
    const c = Fn.length;
    for (let h2 = 0, u = l.length; h2 < u; ++h2) {
      const d = l[h2].toString(), f = this.executorsByZIndex_[d];
      for (let g = 0, y = o.length; g < y; ++g) {
        const m = o[g], _ = f[m];
        if (_ !== void 0) {
          const p = a === null ? void 0 : _.getZIndexContext(), E = p ? p.getContext() : e, S = this.maxExtent_ && m !== "Image" && m !== "Text";
          if (S && (E.save(), this.clip(E, n)), !p || m === "Text" || m === "Image" ? _.execute(
            E,
            t,
            n,
            s,
            r,
            a
          ) : p.pushFunction(
            (w) => _.execute(
              w,
              t,
              n,
              s,
              r,
              a
            )
          ), S && E.restore(), p) {
            p.offset();
            const w = l[h2] * c + Fn.indexOf(m);
            this.deferredZIndexContexts_[w] || (this.deferredZIndexContexts_[w] = []), this.deferredZIndexContexts_[w].push(p);
          }
        }
      }
    }
    this.renderedContext_ = e;
  }
  getDeferredZIndexContexts() {
    return this.deferredZIndexContexts_;
  }
  getRenderedContext() {
    return this.renderedContext_;
  }
  renderDeferred() {
    const e = this.deferredZIndexContexts_, t = Object.keys(e).map(Number).sort(Yt);
    for (let n = 0, s = t.length; n < s; ++n)
      e[t[n]].forEach((r) => {
        r.draw(this.renderedContext_), r.clear();
      }), e[t[n]].length = 0;
  }
};
var $r = {};
function Sg(i) {
  if ($r[i] !== void 0)
    return $r[i];
  const e = i * 2 + 1, t = i * i, n = new Array(t + 1);
  for (let r = 0; r <= i; ++r)
    for (let o = 0; o <= i; ++o) {
      const a = r * r + o * o;
      if (a > t)
        break;
      let l = n[a];
      l || (l = [], n[a] = l), l.push(((i + r) * e + (i + o)) * 4 + 3), r > 0 && l.push(((i - r) * e + (i + o)) * 4 + 3), o > 0 && (l.push(((i + r) * e + (i - o)) * 4 + 3), r > 0 && l.push(((i - r) * e + (i - o)) * 4 + 3));
    }
  const s = [];
  for (let r = 0, o = n.length; r < o; ++r)
    n[r] && s.push(...n[r]);
  return $r[i] = s, s;
}
var Eg = pg;
var wg = class extends oh {
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../../extent.js").Extent} extent Extent.
   * @param {import("../../transform.js").Transform} transform Transform.
   * @param {number} viewRotation View rotation.
   * @param {number} [squaredTolerance] Optional squared tolerance for simplification.
   * @param {import("../../proj.js").TransformFunction} [userTransform] Transform from user to view projection.
   */
  constructor(e, t, n, s, r, o, a) {
    super(), this.context_ = e, this.pixelRatio_ = t, this.extent_ = n, this.transform_ = s, this.transformRotation_ = s ? us(Math.atan2(s[1], s[0]), 10) : 0, this.viewRotation_ = r, this.squaredTolerance_ = o, this.userTransform_ = a, this.contextFillState_ = null, this.contextStrokeState_ = null, this.contextTextState_ = null, this.fillState_ = null, this.strokeState_ = null, this.image_ = null, this.imageAnchorX_ = 0, this.imageAnchorY_ = 0, this.imageHeight_ = 0, this.imageOpacity_ = 0, this.imageOriginX_ = 0, this.imageOriginY_ = 0, this.imageRotateWithView_ = false, this.imageRotation_ = 0, this.imageScale_ = [0, 0], this.imageWidth_ = 0, this.text_ = "", this.textOffsetX_ = 0, this.textOffsetY_ = 0, this.textRotateWithView_ = false, this.textRotation_ = 0, this.textScale_ = [0, 0], this.textFillState_ = null, this.textStrokeState_ = null, this.textState_ = null, this.pixelCoordinates_ = [], this.tmpLocalTransform_ = Ct();
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   */
  drawImages_(e, t, n, s) {
    if (!this.image_)
      return;
    const r = mn(
      e,
      t,
      n,
      s,
      this.transform_,
      this.pixelCoordinates_
    ), o = this.context_, a = this.tmpLocalTransform_, l = o.globalAlpha;
    this.imageOpacity_ != 1 && (o.globalAlpha = l * this.imageOpacity_);
    let c = this.imageRotation_;
    this.transformRotation_ === 0 && (c -= this.viewRotation_), this.imageRotateWithView_ && (c += this.viewRotation_);
    for (let h2 = 0, u = r.length; h2 < u; h2 += 2) {
      const d = r[h2] - this.imageAnchorX_, f = r[h2 + 1] - this.imageAnchorY_;
      if (c !== 0 || this.imageScale_[0] != 1 || this.imageScale_[1] != 1) {
        const g = d + this.imageAnchorX_, y = f + this.imageAnchorY_;
        Zt(
          a,
          g,
          y,
          1,
          1,
          c,
          -g,
          -y
        ), o.save(), o.transform.apply(o, a), o.translate(g, y), o.scale(this.imageScale_[0], this.imageScale_[1]), o.drawImage(
          this.image_,
          this.imageOriginX_,
          this.imageOriginY_,
          this.imageWidth_,
          this.imageHeight_,
          -this.imageAnchorX_,
          -this.imageAnchorY_,
          this.imageWidth_,
          this.imageHeight_
        ), o.restore();
      } else
        o.drawImage(
          this.image_,
          this.imageOriginX_,
          this.imageOriginY_,
          this.imageWidth_,
          this.imageHeight_,
          d,
          f,
          this.imageWidth_,
          this.imageHeight_
        );
    }
    this.imageOpacity_ != 1 && (o.globalAlpha = l);
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @private
   */
  drawText_(e, t, n, s) {
    if (!this.textState_ || this.text_ === "")
      return;
    this.textFillState_ && this.setContextFillState_(this.textFillState_), this.textStrokeState_ && this.setContextStrokeState_(this.textStrokeState_), this.setContextTextState_(this.textState_);
    const r = mn(
      e,
      t,
      n,
      s,
      this.transform_,
      this.pixelCoordinates_
    ), o = this.context_;
    let a = this.textRotation_;
    for (this.transformRotation_ === 0 && (a -= this.viewRotation_), this.textRotateWithView_ && (a += this.viewRotation_); t < n; t += s) {
      const l = r[t] + this.textOffsetX_, c = r[t + 1] + this.textOffsetY_;
      a !== 0 || this.textScale_[0] != 1 || this.textScale_[1] != 1 ? (o.save(), o.translate(l - this.textOffsetX_, c - this.textOffsetY_), o.rotate(a), o.translate(this.textOffsetX_, this.textOffsetY_), o.scale(this.textScale_[0], this.textScale_[1]), this.textStrokeState_ && o.strokeText(this.text_, 0, 0), this.textFillState_ && o.fillText(this.text_, 0, 0), o.restore()) : (this.textStrokeState_ && o.strokeText(this.text_, l, c), this.textFillState_ && o.fillText(this.text_, l, c));
    }
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {number} end End.
   * @param {number} stride Stride.
   * @param {boolean} close Close.
   * @private
   * @return {number} end End.
   */
  moveToLineTo_(e, t, n, s, r) {
    const o = this.context_, a = mn(
      e,
      t,
      n,
      s,
      this.transform_,
      this.pixelCoordinates_
    );
    o.moveTo(a[0], a[1]);
    let l = a.length;
    r && (l -= 2);
    for (let c = 2; c < l; c += 2)
      o.lineTo(a[c], a[c + 1]);
    return r && o.closePath(), n;
  }
  /**
   * @param {Array<number>} flatCoordinates Flat coordinates.
   * @param {number} offset Offset.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @private
   * @return {number} End.
   */
  drawRings_(e, t, n, s) {
    for (let r = 0, o = n.length; r < o; ++r)
      t = this.moveToLineTo_(
        e,
        t,
        n[r],
        s,
        true
      );
    return t;
  }
  /**
   * Render a circle geometry into the canvas.  Rendering is immediate and uses
   * the current fill and stroke styles.
   *
   * @param {import("../../geom/Circle.js").default} geometry Circle geometry.
   * @api
   * @override
   */
  drawCircle(e) {
    if (this.squaredTolerance_ && (e = /** @type {import("../../geom/Circle.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!$e(this.extent_, e.getExtent())) {
      if (this.fillState_ || this.strokeState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const t = Kd(
          e,
          this.transform_,
          this.pixelCoordinates_
        ), n = t[2] - t[0], s = t[3] - t[1], r = Math.sqrt(n * n + s * s), o = this.context_;
        o.beginPath(), o.arc(
          t[0],
          t[1],
          r,
          0,
          2 * Math.PI
        ), this.fillState_ && o.fill(), this.strokeState_ && o.stroke();
      }
      this.text_ !== "" && this.drawText_(e.getCenter(), 0, 2, 2);
    }
  }
  /**
   * Set the rendering style.  Note that since this is an immediate rendering API,
   * any `zIndex` on the provided style will be ignored.
   *
   * @param {import("../../style/Style.js").default} style The rendering style.
   * @api
   * @override
   */
  setStyle(e) {
    this.setFillStrokeStyle(e.getFill(), e.getStroke()), this.setImageStyle(e.getImage()), this.setTextStyle(e.getText());
  }
  /**
   * @param {import("../../transform.js").Transform} transform Transform.
   */
  setTransform(e) {
    this.transform_ = e;
  }
  /**
   * Render a geometry into the canvas.  Call
   * {@link module:ol/render/canvas/Immediate~CanvasImmediateRenderer#setStyle renderer.setStyle()} first to set the rendering style.
   *
   * @param {import("../../geom/Geometry.js").default|import("../Feature.js").default} geometry The geometry to render.
   * @api
   * @override
   */
  drawGeometry(e) {
    switch (e.getType()) {
      case "Point":
        this.drawPoint(
          /** @type {import("../../geom/Point.js").default} */
          e
        );
        break;
      case "LineString":
        this.drawLineString(
          /** @type {import("../../geom/LineString.js").default} */
          e
        );
        break;
      case "Polygon":
        this.drawPolygon(
          /** @type {import("../../geom/Polygon.js").default} */
          e
        );
        break;
      case "MultiPoint":
        this.drawMultiPoint(
          /** @type {import("../../geom/MultiPoint.js").default} */
          e
        );
        break;
      case "MultiLineString":
        this.drawMultiLineString(
          /** @type {import("../../geom/MultiLineString.js").default} */
          e
        );
        break;
      case "MultiPolygon":
        this.drawMultiPolygon(
          /** @type {import("../../geom/MultiPolygon.js").default} */
          e
        );
        break;
      case "GeometryCollection":
        this.drawGeometryCollection(
          /** @type {import("../../geom/GeometryCollection.js").default} */
          e
        );
        break;
      case "Circle":
        this.drawCircle(
          /** @type {import("../../geom/Circle.js").default} */
          e
        );
        break;
    }
  }
  /**
   * Render a feature into the canvas.  Note that any `zIndex` on the provided
   * style will be ignored - features are rendered immediately in the order that
   * this method is called.  If you need `zIndex` support, you should be using an
   * {@link module:ol/layer/Vector~VectorLayer} instead.
   *
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {import("../../style/Style.js").default} style Style.
   * @api
   * @override
   */
  drawFeature(e, t) {
    const n = t.getGeometryFunction()(e);
    n && (this.setStyle(t), this.drawGeometry(n));
  }
  /**
   * Render a GeometryCollection to the canvas.  Rendering is immediate and
   * uses the current styles appropriate for each geometry in the collection.
   *
   * @param {import("../../geom/GeometryCollection.js").default} geometry Geometry collection.
   * @override
   */
  drawGeometryCollection(e) {
    const t = e.getGeometriesArray();
    for (let n = 0, s = t.length; n < s; ++n)
      this.drawGeometry(t[n]);
  }
  /**
   * Render a Point geometry into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/Point.js").default|import("../Feature.js").default} geometry Point geometry.
   * @override
   */
  drawPoint(e) {
    this.squaredTolerance_ && (e = /** @type {import("../../geom/Point.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const t = e.getFlatCoordinates(), n = e.getStride();
    this.image_ && this.drawImages_(t, 0, t.length, n), this.text_ !== "" && this.drawText_(t, 0, t.length, n);
  }
  /**
   * Render a MultiPoint geometry  into the canvas.  Rendering is immediate and
   * uses the current style.
   *
   * @param {import("../../geom/MultiPoint.js").default|import("../Feature.js").default} geometry MultiPoint geometry.
   * @override
   */
  drawMultiPoint(e) {
    this.squaredTolerance_ && (e = /** @type {import("../../geom/MultiPoint.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const t = e.getFlatCoordinates(), n = e.getStride();
    this.image_ && this.drawImages_(t, 0, t.length, n), this.text_ !== "" && this.drawText_(t, 0, t.length, n);
  }
  /**
   * Render a LineString into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/LineString.js").default|import("../Feature.js").default} geometry LineString geometry.
   * @override
   */
  drawLineString(e) {
    if (this.squaredTolerance_ && (e = /** @type {import("../../geom/LineString.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!$e(this.extent_, e.getExtent())) {
      if (this.strokeState_) {
        this.setContextStrokeState_(this.strokeState_);
        const t = this.context_, n = e.getFlatCoordinates();
        t.beginPath(), this.moveToLineTo_(
          n,
          0,
          n.length,
          e.getStride(),
          false
        ), t.stroke();
      }
      if (this.text_ !== "") {
        const t = e.getFlatMidpoint();
        this.drawText_(t, 0, 2, 2);
      }
    }
  }
  /**
   * Render a MultiLineString geometry into the canvas.  Rendering is immediate
   * and uses the current style.
   *
   * @param {import("../../geom/MultiLineString.js").default|import("../Feature.js").default} geometry MultiLineString geometry.
   * @override
   */
  drawMultiLineString(e) {
    this.squaredTolerance_ && (e = /** @type {import("../../geom/MultiLineString.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    ));
    const t = e.getExtent();
    if ($e(this.extent_, t)) {
      if (this.strokeState_) {
        this.setContextStrokeState_(this.strokeState_);
        const n = this.context_, s = e.getFlatCoordinates();
        let r = 0;
        const o = (
          /** @type {Array<number>} */
          e.getEnds()
        ), a = e.getStride();
        n.beginPath();
        for (let l = 0, c = o.length; l < c; ++l)
          r = this.moveToLineTo_(
            s,
            r,
            o[l],
            a,
            false
          );
        n.stroke();
      }
      if (this.text_ !== "") {
        const n = e.getFlatMidpoints();
        this.drawText_(n, 0, n.length, 2);
      }
    }
  }
  /**
   * Render a Polygon geometry into the canvas.  Rendering is immediate and uses
   * the current style.
   *
   * @param {import("../../geom/Polygon.js").default|import("../Feature.js").default} geometry Polygon geometry.
   * @override
   */
  drawPolygon(e) {
    if (this.squaredTolerance_ && (e = /** @type {import("../../geom/Polygon.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!$e(this.extent_, e.getExtent())) {
      if (this.strokeState_ || this.fillState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const t = this.context_;
        t.beginPath(), this.drawRings_(
          e.getOrientedFlatCoordinates(),
          0,
          /** @type {Array<number>} */
          e.getEnds(),
          e.getStride()
        ), this.fillState_ && t.fill(), this.strokeState_ && t.stroke();
      }
      if (this.text_ !== "") {
        const t = e.getFlatInteriorPoint();
        this.drawText_(t, 0, 2, 2);
      }
    }
  }
  /**
   * Render MultiPolygon geometry into the canvas.  Rendering is immediate and
   * uses the current style.
   * @param {import("../../geom/MultiPolygon.js").default} geometry MultiPolygon geometry.
   * @override
   */
  drawMultiPolygon(e) {
    if (this.squaredTolerance_ && (e = /** @type {import("../../geom/MultiPolygon.js").default} */
    e.simplifyTransformed(
      this.squaredTolerance_,
      this.userTransform_
    )), !!$e(this.extent_, e.getExtent())) {
      if (this.strokeState_ || this.fillState_) {
        this.fillState_ && this.setContextFillState_(this.fillState_), this.strokeState_ && this.setContextStrokeState_(this.strokeState_);
        const t = this.context_, n = e.getOrientedFlatCoordinates();
        let s = 0;
        const r = e.getEndss(), o = e.getStride();
        t.beginPath();
        for (let a = 0, l = r.length; a < l; ++a) {
          const c = r[a];
          s = this.drawRings_(n, s, c, o);
        }
        this.fillState_ && t.fill(), this.strokeState_ && t.stroke();
      }
      if (this.text_ !== "") {
        const t = e.getFlatInteriorPoints();
        this.drawText_(t, 0, t.length, 2);
      }
    }
  }
  /**
   * @param {import("../canvas.js").FillState} fillState Fill state.
   * @private
   */
  setContextFillState_(e) {
    const t = this.context_, n = this.contextFillState_;
    n ? n.fillStyle != e.fillStyle && (n.fillStyle = e.fillStyle, t.fillStyle = e.fillStyle) : (t.fillStyle = e.fillStyle, this.contextFillState_ = {
      fillStyle: e.fillStyle
    });
  }
  /**
   * @param {import("../canvas.js").StrokeState} strokeState Stroke state.
   * @private
   */
  setContextStrokeState_(e) {
    const t = this.context_, n = this.contextStrokeState_;
    n ? (n.lineCap != e.lineCap && (n.lineCap = e.lineCap, t.lineCap = e.lineCap), Sn(n.lineDash, e.lineDash) || t.setLineDash(
      n.lineDash = e.lineDash
    ), n.lineDashOffset != e.lineDashOffset && (n.lineDashOffset = e.lineDashOffset, t.lineDashOffset = e.lineDashOffset), n.lineJoin != e.lineJoin && (n.lineJoin = e.lineJoin, t.lineJoin = e.lineJoin), n.lineWidth != e.lineWidth && (n.lineWidth = e.lineWidth, t.lineWidth = e.lineWidth), n.miterLimit != e.miterLimit && (n.miterLimit = e.miterLimit, t.miterLimit = e.miterLimit), n.strokeStyle != e.strokeStyle && (n.strokeStyle = e.strokeStyle, t.strokeStyle = e.strokeStyle)) : (t.lineCap = e.lineCap, t.setLineDash(e.lineDash), t.lineDashOffset = e.lineDashOffset, t.lineJoin = e.lineJoin, t.lineWidth = e.lineWidth, t.miterLimit = e.miterLimit, t.strokeStyle = e.strokeStyle, this.contextStrokeState_ = {
      lineCap: e.lineCap,
      lineDash: e.lineDash,
      lineDashOffset: e.lineDashOffset,
      lineJoin: e.lineJoin,
      lineWidth: e.lineWidth,
      miterLimit: e.miterLimit,
      strokeStyle: e.strokeStyle
    });
  }
  /**
   * @param {import("../canvas.js").TextState} textState Text state.
   * @private
   */
  setContextTextState_(e) {
    const t = this.context_, n = this.contextTextState_, s = e.textAlign ? e.textAlign : ns;
    n ? (n.font != e.font && (n.font = e.font, t.font = e.font), n.textAlign != s && (n.textAlign = s, t.textAlign = s), n.textBaseline != e.textBaseline && (n.textBaseline = e.textBaseline, t.textBaseline = e.textBaseline)) : (t.font = e.font, t.textAlign = s, t.textBaseline = e.textBaseline, this.contextTextState_ = {
      font: e.font,
      textAlign: s,
      textBaseline: e.textBaseline
    });
  }
  /**
   * Set the fill and stroke style for subsequent draw operations.  To clear
   * either fill or stroke styles, pass null for the appropriate parameter.
   *
   * @param {import("../../style/Fill.js").default} fillStyle Fill style.
   * @param {import("../../style/Stroke.js").default} strokeStyle Stroke style.
   * @override
   */
  setFillStrokeStyle(e, t) {
    if (!e)
      this.fillState_ = null;
    else {
      const n = e.getColor();
      this.fillState_ = {
        fillStyle: Pt(
          n || Ze
        )
      };
    }
    if (!t)
      this.strokeState_ = null;
    else {
      const n = t.getColor(), s = t.getLineCap(), r = t.getLineDash(), o = t.getLineDashOffset(), a = t.getLineJoin(), l = t.getWidth(), c = t.getMiterLimit(), h2 = r || Kt;
      this.strokeState_ = {
        lineCap: s !== void 0 ? s : wi,
        lineDash: this.pixelRatio_ === 1 ? h2 : h2.map((u) => u * this.pixelRatio_),
        lineDashOffset: (o || Ht) * this.pixelRatio_,
        lineJoin: a !== void 0 ? a : Ci,
        lineWidth: (l !== void 0 ? l : is) * this.pixelRatio_,
        miterLimit: c !== void 0 ? c : es,
        strokeStyle: Pt(
          n || ts
        )
      };
    }
  }
  /**
   * Set the image style for subsequent draw operations.  Pass null to remove
   * the image style.
   *
   * @param {import("../../style/Image.js").default} imageStyle Image style.
   * @override
   */
  setImageStyle(e) {
    let t;
    if (!e || !(t = e.getSize())) {
      this.image_ = null;
      return;
    }
    const n = e.getPixelRatio(this.pixelRatio_), s = e.getAnchor(), r = e.getOrigin();
    this.image_ = e.getImage(this.pixelRatio_), this.imageAnchorX_ = s[0] * n, this.imageAnchorY_ = s[1] * n, this.imageHeight_ = t[1] * n, this.imageOpacity_ = e.getOpacity(), this.imageOriginX_ = r[0], this.imageOriginY_ = r[1], this.imageRotateWithView_ = e.getRotateWithView(), this.imageRotation_ = e.getRotation();
    const o = e.getScaleArray();
    this.imageScale_ = [
      o[0] * this.pixelRatio_ / n,
      o[1] * this.pixelRatio_ / n
    ], this.imageWidth_ = t[0] * n;
  }
  /**
   * Set the text style for subsequent draw operations.  Pass null to
   * remove the text style.
   *
   * @param {import("../../style/Text.js").default} textStyle Text style.
   * @override
   */
  setTextStyle(e) {
    if (!e)
      this.text_ = "";
    else {
      const t = e.getFill();
      if (!t)
        this.textFillState_ = null;
      else {
        const f = t.getColor();
        this.textFillState_ = {
          fillStyle: Pt(
            f || Ze
          )
        };
      }
      const n = e.getStroke();
      if (!n)
        this.textStrokeState_ = null;
      else {
        const f = n.getColor(), g = n.getLineCap(), y = n.getLineDash(), m = n.getLineDashOffset(), _ = n.getLineJoin(), p = n.getWidth(), E = n.getMiterLimit();
        this.textStrokeState_ = {
          lineCap: g !== void 0 ? g : wi,
          lineDash: y || Kt,
          lineDashOffset: m || Ht,
          lineJoin: _ !== void 0 ? _ : Ci,
          lineWidth: p !== void 0 ? p : is,
          miterLimit: E !== void 0 ? E : es,
          strokeStyle: Pt(
            f || ts
          )
        };
      }
      const s = e.getFont(), r = e.getOffsetX(), o = e.getOffsetY(), a = e.getRotateWithView(), l = e.getRotation(), c = e.getScaleArray(), h2 = e.getText(), u = e.getTextAlign(), d = e.getTextBaseline();
      this.textState_ = {
        font: s !== void 0 ? s : Wc,
        textAlign: u !== void 0 ? u : ns,
        textBaseline: d !== void 0 ? d : er
      }, this.text_ = h2 !== void 0 ? Array.isArray(h2) ? h2.reduce((f, g, y) => f += y % 2 ? " " : g, "") : h2 : "", this.textOffsetX_ = r !== void 0 ? this.pixelRatio_ * r : 0, this.textOffsetY_ = o !== void 0 ? this.pixelRatio_ * o : 0, this.textRotateWithView_ = a !== void 0 ? a : false, this.textRotation_ = l !== void 0 ? l : 0, this.textScale_ = [
        this.pixelRatio_ * c[0],
        this.pixelRatio_ * c[1]
      ];
    }
  }
};
var Cg = wg;
var Ft = 0.5;
function xg(i, e, t, n, s, r, o, a, l) {
  const c = l ? Qo(s) : s, h2 = i[0] * Ft, u = i[1] * Ft, d = Fe(h2, u);
  d.imageSmoothingEnabled = false;
  const f = d.canvas, g = new Cg(
    d,
    Ft,
    s,
    null,
    o,
    a,
    l ? pr(kd(), l) : null
  ), y = t.length, m = Math.floor((256 * 256 * 256 - 1) / y), _ = {};
  for (let E = 1; E <= y; ++E) {
    const S = t[E - 1], w = S.getStyleFunction() || n;
    if (!w)
      continue;
    let x = w(S, r);
    if (!x)
      continue;
    Array.isArray(x) || (x = [x]);
    const v = (E * m).toString(16).padStart(7, "#00000");
    for (let R = 0, P = x.length; R < P; ++R) {
      const U = x[R], N = U.getGeometryFunction()(S);
      if (!N || !$e(c, N.getExtent()))
        continue;
      const O = U.clone(), G = O.getFill();
      G && G.setColor(v);
      const $ = O.getStroke();
      $ && ($.setColor(v), $.setLineDash(null)), O.setText(void 0);
      const z = U.getImage();
      if (z) {
        const ne = z.getImageSize();
        if (!ne)
          continue;
        const fe = Fe(
          ne[0],
          ne[1],
          void 0,
          { alpha: false }
        ), F = fe.canvas;
        fe.fillStyle = v, fe.fillRect(0, 0, F.width, F.height), O.setImage(
          new yn({
            img: F,
            anchor: z.getAnchor(),
            anchorXUnits: "pixels",
            anchorYUnits: "pixels",
            offset: z.getOrigin(),
            opacity: 1,
            size: z.getSize(),
            scale: z.getScale(),
            rotation: z.getRotation(),
            rotateWithView: z.getRotateWithView()
          })
        );
      }
      const H = O.getZIndex() || 0;
      let V = _[H];
      V || (V = {}, _[H] = V, V.Polygon = [], V.Circle = [], V.LineString = [], V.Point = []);
      const q = N.getType();
      if (q === "GeometryCollection") {
        const ne = (
          /** @type {import("../../geom/GeometryCollection.js").default} */
          N.getGeometriesArrayRecursive()
        );
        for (let fe = 0, F = ne.length; fe < F; ++fe) {
          const Te = ne[fe];
          V[Te.getType().replace("Multi", "")].push(
            Te,
            O
          );
        }
      } else
        V[q.replace("Multi", "")].push(N, O);
    }
  }
  const p = Object.keys(_).map(Number).sort(Yt);
  for (let E = 0, S = p.length; E < S; ++E) {
    const w = _[p[E]];
    for (const x in w) {
      const b = w[x];
      for (let v = 0, R = b.length; v < R; v += 2) {
        g.setStyle(b[v + 1]);
        for (let P = 0, U = e.length; P < U; ++P)
          g.setTransform(e[P]), g.drawGeometry(b[v]);
      }
    }
  }
  return d.getImageData(0, 0, f.width, f.height);
}
function Ig(i, e, t) {
  const n = [];
  if (t) {
    const s = Math.floor(Math.round(i[0]) * Ft), r = Math.floor(Math.round(i[1]) * Ft), o = (xe(s, 0, t.width - 1) + xe(r, 0, t.height - 1) * t.width) * 4, a = t.data[o], l = t.data[o + 1], h2 = t.data[o + 2] + 256 * (l + 256 * a), u = Math.floor((256 * 256 * 256 - 1) / e.length);
    h2 && h2 % u === 0 && n.push(e[h2 / u - 1]);
  }
  return n;
}
var vg = 0.5;
var ch = {
  Point: Pg,
  LineString: Lg,
  Polygon: kg,
  MultiPoint: Ag,
  MultiLineString: Fg,
  MultiPolygon: Dg,
  GeometryCollection: Rg,
  Circle: Tg
};
function Mg(i, e) {
  return parseInt(de(i), 10) - parseInt(de(e), 10);
}
function Il(i, e) {
  const t = hh(i, e);
  return t * t;
}
function hh(i, e) {
  return vg * i / e;
}
function Tg(i, e, t, n, s) {
  const r = t.getFill(), o = t.getStroke();
  if (r || o) {
    const l = i.getBuilder(t.getZIndex(), "Circle");
    l.setFillStrokeStyle(r, o), l.drawCircle(e, n, s);
  }
  const a = t.getText();
  if (a && a.getText()) {
    const l = i.getBuilder(t.getZIndex(), "Text");
    l.setTextStyle(a), l.drawText(e, n);
  }
}
function vl(i, e, t, n, s, r, o, a) {
  const l = [], c = t.getImage();
  if (c) {
    let d = true;
    const f = c.getImageState();
    f == ee.LOADED || f == ee.ERROR ? d = false : f == ee.IDLE && c.load(), d && l.push(c.ready());
  }
  const h2 = t.getFill();
  h2 && h2.loading() && l.push(h2.ready());
  const u = l.length > 0;
  return u && Promise.all(l).then(() => s(null)), bg(
    i,
    e,
    t,
    n,
    r,
    o,
    a
  ), u;
}
function bg(i, e, t, n, s, r, o) {
  const a = t.getGeometryFunction()(e);
  if (!a)
    return;
  const l = a.simplifyTransformed(
    n,
    s
  );
  if (t.getRenderer())
    uh(i, l, t, e, o);
  else {
    const h2 = ch[l.getType()];
    h2(
      i,
      l,
      t,
      e,
      o,
      r
    );
  }
}
function uh(i, e, t, n, s) {
  if (e.getType() == "GeometryCollection") {
    const o = (
      /** @type {import("../geom/GeometryCollection.js").default} */
      e.getGeometries()
    );
    for (let a = 0, l = o.length; a < l; ++a)
      uh(i, o[a], t, n, s);
    return;
  }
  i.getBuilder(t.getZIndex(), "Default").drawCustom(
    /** @type {import("../geom/SimpleGeometry.js").default} */
    e,
    n,
    t.getRenderer(),
    t.getHitDetectionRenderer(),
    s
  );
}
function Rg(i, e, t, n, s, r) {
  const o = e.getGeometriesArray();
  let a, l;
  for (a = 0, l = o.length; a < l; ++a) {
    const c = ch[o[a].getType()];
    c(
      i,
      o[a],
      t,
      n,
      s,
      r
    );
  }
}
function Lg(i, e, t, n, s) {
  const r = t.getStroke();
  if (r) {
    const a = i.getBuilder(
      t.getZIndex(),
      "LineString"
    );
    a.setFillStrokeStyle(null, r), a.drawLineString(e, n, s);
  }
  const o = t.getText();
  if (o && o.getText()) {
    const a = i.getBuilder(t.getZIndex(), "Text");
    a.setTextStyle(o), a.drawText(e, n, s);
  }
}
function Fg(i, e, t, n, s) {
  const r = t.getStroke();
  if (r) {
    const a = i.getBuilder(
      t.getZIndex(),
      "LineString"
    );
    a.setFillStrokeStyle(null, r), a.drawMultiLineString(e, n, s);
  }
  const o = t.getText();
  if (o && o.getText()) {
    const a = i.getBuilder(t.getZIndex(), "Text");
    a.setTextStyle(o), a.drawText(e, n, s);
  }
}
function Dg(i, e, t, n, s) {
  const r = t.getFill(), o = t.getStroke();
  if (o || r) {
    const l = i.getBuilder(t.getZIndex(), "Polygon");
    l.setFillStrokeStyle(r, o), l.drawMultiPolygon(e, n, s);
  }
  const a = t.getText();
  if (a && a.getText()) {
    const l = i.getBuilder(t.getZIndex(), "Text");
    l.setTextStyle(a), l.drawText(e, n, s);
  }
}
function Pg(i, e, t, n, s, r) {
  const o = t.getImage(), a = t.getText(), l = a && a.getText(), c = r && o && l ? {} : void 0;
  if (o) {
    if (o.getImageState() != ee.LOADED)
      return;
    const h2 = i.getBuilder(t.getZIndex(), "Image");
    h2.setImageStyle(o, c), h2.drawPoint(e, n, s);
  }
  if (l) {
    const h2 = i.getBuilder(t.getZIndex(), "Text");
    h2.setTextStyle(a, c), h2.drawText(e, n, s);
  }
}
function Ag(i, e, t, n, s, r) {
  const o = t.getImage(), a = o && o.getOpacity() !== 0, l = t.getText(), c = l && l.getText(), h2 = r && a && c ? {} : void 0;
  if (a) {
    if (o.getImageState() != ee.LOADED)
      return;
    const u = i.getBuilder(t.getZIndex(), "Image");
    u.setImageStyle(o, h2), u.drawMultiPoint(e, n, s);
  }
  if (c) {
    const u = i.getBuilder(t.getZIndex(), "Text");
    u.setTextStyle(l, h2), u.drawText(e, n, s);
  }
}
function kg(i, e, t, n, s) {
  const r = t.getFill(), o = t.getStroke();
  if (r || o) {
    const l = i.getBuilder(t.getZIndex(), "Polygon");
    l.setFillStrokeStyle(r, o), l.drawPolygon(e, n, s);
  }
  const a = t.getText();
  if (a && a.getText()) {
    const l = i.getBuilder(t.getZIndex(), "Text");
    l.setTextStyle(a), l.drawText(e, n, s);
  }
}
var Og = class extends It {
  /**
   * @param {import("./EventType.js").default} type Type.
   * @param {import("../transform.js").Transform} [inversePixelTransform] Transform for
   *     CSS pixels to rendered pixels.
   * @param {import("../Map.js").FrameState} [frameState] Frame state.
   * @param {?(CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D|WebGLRenderingContext)} [context] Context.
   */
  constructor(e, t, n, s) {
    super(e), this.inversePixelTransform = t, this.frameState = n, this.context = s;
  }
};
var dh = Og;
var Ng = 5;
var Ug = class extends as {
  /**
   * @param {LayerType} layer Layer.
   */
  constructor(e) {
    super(), this.ready = true, this.boundHandleImageChange_ = this.handleImageChange_.bind(this), this.layer_ = e, this.staleKeys_ = new Array(), this.maxStaleKeys = Ng;
  }
  /**
   * @return {Array<string>} Get the list of stale keys.
   */
  getStaleKeys() {
    return this.staleKeys_;
  }
  /**
   * @param {string} key The new stale key.
   */
  prependStaleKey(e) {
    this.staleKeys_.unshift(e), this.staleKeys_.length > this.maxStaleKeys && (this.staleKeys_.length = this.maxStaleKeys);
  }
  /**
   * Asynchronous layer level hit detection.
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with
   * an array of features.
   */
  getFeatures(e) {
    return ue();
  }
  /**
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   */
  getData(e) {
    return null;
  }
  /**
   * Determine whether render should be called.
   * @abstract
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   */
  prepareFrame(e) {
    return ue();
  }
  /**
   * Render the layer.
   * @abstract
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement|null} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   */
  renderFrame(e, t) {
    return ue();
  }
  /**
   * @abstract
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {import("./vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {Array<import("./Map.js").HitMatch<T>>} matches The hit detected matches with tolerance.
   * @return {T|undefined} Callback result.
   * @template T
   */
  forEachFeatureAtCoordinate(e, t, n, s, r) {
  }
  /**
   * @return {LayerType} Layer.
   */
  getLayer() {
    return this.layer_;
  }
  /**
   * Perform action necessary to get the layer rendered after new fonts have loaded
   * @abstract
   */
  handleFontsChanged() {
  }
  /**
   * Handle changes in image state.
   * @param {import("../events/Event.js").default} event Image change event.
   * @private
   */
  handleImageChange_(e) {
    const t = (
      /** @type {import("../Image.js").default} */
      e.target
    );
    (t.getState() === ee.LOADED || t.getState() === ee.ERROR) && this.renderIfReadyAndVisible();
  }
  /**
   * Load the image if not already loaded, and register the image change
   * listener if needed.
   * @param {import("../Image.js").default} image Image.
   * @return {boolean} `true` if the image is already loaded, `false` otherwise.
   * @protected
   */
  loadImage(e) {
    let t = e.getState();
    return t != ee.LOADED && t != ee.ERROR && e.addEventListener(Z.CHANGE, this.boundHandleImageChange_), t == ee.IDLE && (e.load(), t = e.getState()), t == ee.LOADED;
  }
  /**
   * @protected
   */
  renderIfReadyAndVisible() {
    const e = this.getLayer();
    e && e.getVisible() && e.getSourceState() === "ready" && e.changed();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   */
  renderDeferred(e) {
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    delete this.layer_, super.disposeInternal();
  }
};
var Gg = Ug;
var Ml = [];
var ii = null;
function Bg() {
  ii = Fe(1, 1, void 0, {
    willReadFrequently: true
  });
}
var zg = class extends Gg {
  /**
   * @param {LayerType} layer Layer.
   */
  constructor(e) {
    super(e), this.container = null, this.renderedResolution, this.tempTransform = Ct(), this.pixelTransform = Ct(), this.inversePixelTransform = Ct(), this.context = null, this.deferredContext_ = null, this.containerReused = false, this.frameState = null;
  }
  /**
   * @param {import('../../DataTile.js').ImageLike} image Image.
   * @param {number} col The column index.
   * @param {number} row The row index.
   * @return {Uint8ClampedArray|null} The image data.
   */
  getImageData(e, t, n) {
    ii || Bg(), ii.clearRect(0, 0, 1, 1);
    let s;
    try {
      ii.drawImage(e, t, n, 1, 1, 0, 0, 1, 1), s = ii.getImageData(0, 0, 1, 1).data;
    } catch {
      return ii = null, null;
    }
    return s;
  }
  /**
   * @param {import('../../Map.js').FrameState} frameState Frame state.
   * @return {string} Background color.
   */
  getBackground(e) {
    let n = this.getLayer().getBackground();
    return typeof n == "function" && (n = n(e.viewState.resolution)), n || void 0;
  }
  /**
   * Get a rendering container from an existing target, if compatible.
   * @param {HTMLElement} target Potential render target.
   * @param {string} transform CSS transform matrix.
   * @param {string} [backgroundColor] Background color.
   */
  useContainer(e, t, n) {
    const s = this.getLayer().getClassName();
    let r, o;
    if (e && e.className === s && (!n || e && e.style.backgroundColor && Sn(
      Ei(e.style.backgroundColor),
      Ei(n)
    ))) {
      const a = e.firstElementChild;
      un(a) && (o = a.getContext("2d"));
    }
    if (o && zd(o.canvas.style.transform, t) ? (this.container = e, this.context = o, this.containerReused = true) : this.containerReused ? (this.container = null, this.context = null, this.containerReused = false) : this.container && (this.container.style.backgroundColor = null), !this.container) {
      r = ft ? zc() : document.createElement("div"), r.className = s;
      let a = r.style;
      a.position = "absolute", a.width = "100%", a.height = "100%", o = Fe();
      const l = (
        /** @type {HTMLCanvasElement} */
        o.canvas
      );
      r.appendChild(l), a = l.style, a.position = "absolute", a.left = "0", a.transformOrigin = "top left", this.container = r, this.context = o;
    }
    !this.containerReused && n && !this.container.style.backgroundColor && (this.container.style.backgroundColor = n);
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {import("../../extent.js").Extent} extent Clip extent.
   * @protected
   */
  clipUnrotated(e, t, n) {
    const s = En(n), r = cs(n), o = ls(n), a = pi(n);
    Pe(t.coordinateToPixelTransform, s), Pe(t.coordinateToPixelTransform, r), Pe(t.coordinateToPixelTransform, o), Pe(t.coordinateToPixelTransform, a);
    const l = this.inversePixelTransform;
    Pe(l, s), Pe(l, r), Pe(l, o), Pe(l, a), e.save(), e.beginPath(), e.moveTo(Math.round(s[0]), Math.round(s[1])), e.lineTo(Math.round(r[0]), Math.round(r[1])), e.lineTo(Math.round(o[0]), Math.round(o[1])), e.lineTo(Math.round(a[0]), Math.round(a[1])), e.clip();
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target that may be used to render content to.
   * @protected
   */
  prepareContainer(e, t) {
    const n = e.extent, s = e.viewState.resolution, r = e.viewState.rotation, o = e.pixelRatio, a = Math.round(ye(n) / s * o), l = Math.round(Ve(n) / s * o);
    Zt(
      this.pixelTransform,
      e.size[0] / 2,
      e.size[1] / 2,
      1 / o,
      1 / o,
      r,
      -a / 2,
      -l / 2
    ), Lc(this.inversePixelTransform, this.pixelTransform);
    const c = Bd(this.pixelTransform);
    if (this.useContainer(t, c, this.getBackground(e)), !this.containerReused) {
      const h2 = this.context.canvas;
      h2.width != a || h2.height != l ? (h2.width = a, h2.height = l) : this.context.clearRect(0, 0, a, l), c !== /** @type {HTMLCanvasElement} */
      h2.style.transform && (h2.style.transform = c);
    }
  }
  /**
   * @param {import("../../render/EventType.js").default} type Event type.
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @private
   */
  dispatchRenderEvent_(e, t, n) {
    const s = this.getLayer();
    if (s.hasListener(e)) {
      const r = new dh(
        e,
        this.inversePixelTransform,
        n,
        t
      );
      s.dispatchEvent(r);
    }
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  preRender(e, t) {
    this.frameState = t, !t.declutter && this.dispatchRenderEvent_(mt.PRERENDER, e, t);
  }
  /**
   * @param {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} context Context.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  postRender(e, t) {
    t.declutter || this.dispatchRenderEvent_(mt.POSTRENDER, e, t);
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   */
  renderDeferredInternal(e) {
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import('../../render/canvas/ZIndexContext.js').ZIndexContextProxy} Context.
   */
  getRenderContext(e) {
    return e.declutter && !this.deferredContext_ && (this.deferredContext_ = new ah()), e.declutter ? this.deferredContext_.getContext() : this.context;
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderDeferred(e) {
    e.declutter && (this.dispatchRenderEvent_(
      mt.PRERENDER,
      this.context,
      e
    ), e.declutter && this.deferredContext_ && (this.deferredContext_.draw(this.context), this.deferredContext_.clear()), this.renderDeferredInternal(e), this.dispatchRenderEvent_(
      mt.POSTRENDER,
      this.context,
      e
    ));
  }
  /**
   * Creates a transform for rendering to an element that will be rotated after rendering.
   * @param {import("../../coordinate.js").Coordinate} center Center.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {number} pixelRatio Pixel ratio.
   * @param {number} width Width of the rendered element (in pixels).
   * @param {number} height Height of the rendered element (in pixels).
   * @param {number} offsetX Offset on the x-axis in view coordinates.
   * @protected
   * @return {!import("../../transform.js").Transform} Transform.
   */
  getRenderTransform(e, t, n, s, r, o, a) {
    const l = r / 2, c = o / 2, h2 = s / t, u = -h2, d = -e[0] + a, f = -e[1];
    return Zt(
      this.tempTransform,
      l,
      c,
      h2,
      u,
      -n,
      d,
      f
    );
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    delete this.frameState, super.disposeInternal();
  }
};
var fh = zg;
var Vg = class extends fh {
  /**
   * @param {import("../../layer/BaseVector.js").default} vectorLayer Vector layer.
   */
  constructor(e) {
    super(e), this.boundHandleStyleImageChange_ = this.handleStyleImageChange_.bind(this), this.animatingOrInteracting_, this.hitDetectionImageData_ = null, this.clipped_ = false, this.renderedFeatures_ = null, this.renderedRevision_ = -1, this.renderedResolution_ = NaN, this.renderedExtent_ = _t(), this.wrappedRenderedExtent_ = _t(), this.renderedRotation_, this.renderedCenter_ = null, this.renderedProjection_ = null, this.renderedPixelRatio_ = 1, this.renderedRenderOrder_ = null, this.renderedFrameDeclutter_, this.replayGroup_ = null, this.replayGroupChanged = true, this.clipping = true, this.targetContext_ = null, this.opacity_ = 1;
  }
  /**
   * @param {ExecutorGroup} executorGroup Executor group.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {boolean} [declutterable] `true` to only render declutterable items,
   *     `false` to only render non-declutterable items, `undefined` to render all.
   */
  renderWorlds(e, t, n) {
    const s = t.extent, r = t.viewState, o = r.center, a = r.resolution, l = r.projection, c = r.rotation, h2 = l.getExtent(), u = this.getLayer().getSource(), d = this.getLayer().getDeclutter(), f = t.pixelRatio, g = t.viewHints, y = !(g[ze.ANIMATING] || g[ze.INTERACTING]), m = this.context, _ = Math.round(ye(s) / a * f), p = Math.round(Ve(s) / a * f), E = u.getWrapX() && l.canWrapX(), S = E ? ye(h2) : null, w = E ? Math.ceil((s[2] - h2[2]) / S) + 1 : 1;
    let x = E ? Math.floor((s[0] - h2[0]) / S) : 0;
    do {
      let b = this.getRenderTransform(
        o,
        a,
        0,
        f,
        _,
        p,
        x * S
      );
      t.declutter && (b = b.slice(0)), e.execute(
        m,
        [m.canvas.width, m.canvas.height],
        b,
        c,
        y,
        n === void 0 ? Fn : n ? lh : _g,
        n ? d && t.declutter[d] : void 0
      );
    } while (++x < w);
  }
  /**
   * @private
   */
  setDrawContext_() {
    this.opacity_ !== 1 && (this.targetContext_ = this.context, this.context = Fe(
      this.context.canvas.width,
      this.context.canvas.height,
      Ml
    ));
  }
  /**
   * @private
   */
  resetDrawContext_() {
    if (this.opacity_ !== 1 && this.targetContext_) {
      const e = this.targetContext_.globalAlpha;
      this.targetContext_.globalAlpha = this.opacity_, this.targetContext_.drawImage(this.context.canvas, 0, 0), this.targetContext_.globalAlpha = e, xr(this.context), Ml.push(this.context.canvas), this.context = this.targetContext_, this.targetContext_ = null;
    }
  }
  /**
   * Render declutter items for this layer
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   */
  renderDeclutter(e) {
    !this.replayGroup_ || !this.getLayer().getDeclutter() || this.renderWorlds(this.replayGroup_, e, true);
  }
  /**
   * Render deferred instructions.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderDeferredInternal(e) {
    this.replayGroup_ && (this.replayGroup_.renderDeferred(), this.clipped_ && this.context.restore(), this.resetDrawContext_());
  }
  /**
   * Render the layer.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement|null} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   * @override
   */
  renderFrame(e, t) {
    const n = e.layerStatesArray[e.layerIndex];
    this.opacity_ = n.opacity;
    const s = e.viewState;
    this.prepareContainer(e, t);
    const r = this.context, o = this.replayGroup_;
    let a = o && !o.isEmpty();
    if (!a && !(this.getLayer().hasListener(mt.PRERENDER) || this.getLayer().hasListener(mt.POSTRENDER)))
      return this.container;
    if (this.setDrawContext_(), this.preRender(r, e), s.projection, this.clipped_ = false, a && n.extent && this.clipping) {
      const l = cn(n.extent);
      a = $e(l, e.extent), this.clipped_ = a && !ai(l, e.extent), this.clipped_ && this.clipUnrotated(r, e, l);
    }
    return a && this.renderWorlds(
      o,
      e,
      this.getLayer().getDeclutter() ? false : void 0
    ), !e.declutter && this.clipped_ && r.restore(), this.postRender(r, e), this.renderedRotation_ !== s.rotation && (this.renderedRotation_ = s.rotation, this.hitDetectionImageData_ = null), e.declutter || this.resetDrawContext_(), this.container;
  }
  /**
   * Asynchronous layer level hit detection.
   * @param {import("../../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../../Feature").default>>} Promise
   * that resolves with an array of features.
   * @override
   */
  getFeatures(e) {
    return new Promise((t) => {
      if (this.frameState && !this.hitDetectionImageData_ && !this.animatingOrInteracting_) {
        const n = this.frameState.size.slice(), s = this.renderedCenter_, r = this.renderedResolution_, o = this.renderedRotation_, a = this.renderedProjection_, l = this.wrappedRenderedExtent_, c = this.getLayer(), h2 = [], u = n[0] * Ft, d = n[1] * Ft;
        h2.push(
          this.getRenderTransform(
            s,
            r,
            o,
            Ft,
            u,
            d,
            0
          ).slice()
        );
        const f = c.getSource(), g = a.getExtent();
        if (f.getWrapX() && a.canWrapX() && !ai(g, l)) {
          let y = l[0];
          const m = ye(g);
          let _ = 0, p;
          for (; y < g[0]; )
            --_, p = m * _, h2.push(
              this.getRenderTransform(
                s,
                r,
                o,
                Ft,
                u,
                d,
                p
              ).slice()
            ), y += m;
          for (_ = 0, y = l[2]; y > g[2]; )
            ++_, p = m * _, h2.push(
              this.getRenderTransform(
                s,
                r,
                o,
                Ft,
                u,
                d,
                p
              ).slice()
            ), y -= m;
        }
        this.hitDetectionImageData_ = xg(
          n,
          h2,
          this.renderedFeatures_,
          c.getStyleFunction(),
          l,
          r,
          o,
          Il(r, this.renderedPixelRatio_),
          null
        );
      }
      t(
        Ig(e, this.renderedFeatures_, this.hitDetectionImageData_)
      );
    });
  }
  /**
   * @param {import("../../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {import("../vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {Array<import("../Map.js").HitMatch<T>>} matches The hit detected matches with tolerance.
   * @return {T|undefined} Callback result.
   * @template T
   * @override
   */
  forEachFeatureAtCoordinate(e, t, n, s, r) {
    var d, f;
    if (!this.replayGroup_)
      return;
    const o = t.viewState.resolution, a = t.viewState.rotation, l = this.getLayer(), c = {}, h2 = function(g, y, m) {
      const _ = de(g), p = c[_];
      if (p) {
        if (p !== true && m < p.distanceSq) {
          if (m === 0)
            return c[_] = true, r.splice(r.lastIndexOf(p), 1), s(g, l, y);
          p.geometry = y, p.distanceSq = m;
        }
      } else {
        if (m === 0)
          return c[_] = true, s(g, l, y);
        r.push(
          c[_] = {
            feature: g,
            layer: l,
            geometry: y,
            distanceSq: m,
            callback: s
          }
        );
      }
    }, u = this.getLayer().getDeclutter();
    return this.replayGroup_.forEachFeatureAtCoordinate(
      e,
      o,
      a,
      n,
      h2,
      u ? (f = (d = t.declutter) == null ? void 0 : d[u]) == null ? void 0 : f.all().map((g) => g.value) : null
    );
  }
  /**
   * Perform action necessary to get the layer rendered after new fonts have loaded
   * @override
   */
  handleFontsChanged() {
    const e = this.getLayer();
    e.getVisible() && this.replayGroup_ && e.changed();
  }
  /**
   * Handle changes in image style state.
   * @param {import("../../events/Event.js").default} event Image style change event.
   * @private
   */
  handleStyleImageChange_(e) {
    this.renderIfReadyAndVisible();
  }
  /**
   * Determine whether render should be called.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   * @override
   */
  prepareFrame(e) {
    const t = this.getLayer(), n = t.getSource();
    if (!n)
      return false;
    const s = e.viewHints[ze.ANIMATING], r = e.viewHints[ze.INTERACTING], o = t.getUpdateWhileAnimating(), a = t.getUpdateWhileInteracting();
    if (this.ready && !o && s || !a && r)
      return this.animatingOrInteracting_ = true, true;
    this.animatingOrInteracting_ = false;
    const l = e.extent, c = e.viewState, h2 = c.projection, u = c.resolution, d = e.pixelRatio, f = t.getRevision(), g = t.getRenderBuffer();
    let y = t.getRenderOrder();
    y === void 0 && (y = Mg);
    const m = c.center.slice(), _ = zo(
      l,
      g * u
    ), p = _.slice(), E = [_.slice()], S = h2.getExtent();
    if (n.getWrapX() && h2.canWrapX() && !ai(S, e.extent)) {
      const G = ye(S), $ = Math.max(ye(_) / 2, G);
      _[0] = S[0] - $, _[2] = S[2] + $, Cc(m, h2);
      const z = _c(E[0], h2);
      z[0] < S[0] && z[2] < S[2] ? E.push([
        z[0] + G,
        z[1],
        z[2] + G,
        z[3]
      ]) : z[0] > S[0] && z[2] > S[2] && E.push([
        z[0] - G,
        z[1],
        z[2] - G,
        z[3]
      ]);
    }
    if (this.ready && this.renderedResolution_ == u && this.renderedRevision_ == f && this.renderedRenderOrder_ == y && this.renderedFrameDeclutter_ === !!e.declutter && ai(this.wrappedRenderedExtent_, _))
      return Sn(this.renderedExtent_, p) || (this.hitDetectionImageData_ = null, this.renderedExtent_ = p), this.renderedCenter_ = m, this.replayGroupChanged = false, true;
    this.replayGroup_ = null;
    const w = new hg(
      hh(u, d),
      _,
      u,
      d
    );
    let x;
    for (let G = 0, $ = E.length; G < $; ++G)
      n.loadFeatures(E[G], u, h2);
    const b = Il(u, d);
    let v = true;
    const R = (
      /**
       * @param {import("../../Feature.js").default} feature Feature.
       * @param {number} index Index.
       */
      (G, $) => {
        let z;
        const H = G.getStyleFunction() || t.getStyleFunction();
        if (H && (z = H(G, u)), z) {
          const V = this.renderFeature(
            G,
            b,
            z,
            w,
            x,
            this.getLayer().getDeclutter(),
            $
          );
          v = v && !V;
        }
      }
    ), P = Qo(_), U = n.getFeaturesInExtent(P);
    y && U.sort(y);
    for (let G = 0, $ = U.length; G < $; ++G)
      R(U[G], G);
    this.renderedFeatures_ = U, this.ready = v;
    const N = w.finish(), O = new Eg(
      _,
      u,
      d,
      n.getOverlaps(),
      N,
      t.getRenderBuffer(),
      !!e.declutter
    );
    return this.renderedResolution_ = u, this.renderedRevision_ = f, this.renderedRenderOrder_ = y, this.renderedFrameDeclutter_ = !!e.declutter, this.renderedExtent_ = p, this.wrappedRenderedExtent_ = _, this.renderedCenter_ = m, this.renderedProjection_ = h2, this.renderedPixelRatio_ = d, this.replayGroup_ = O, this.hitDetectionImageData_ = null, this.replayGroupChanged = true, true;
  }
  /**
   * @param {import("../../Feature.js").default} feature Feature.
   * @param {number} squaredTolerance Squared render tolerance.
   * @param {import("../../style/Style.js").default|Array<import("../../style/Style.js").default>} styles The style or array of styles.
   * @param {import("../../render/canvas/BuilderGroup.js").default} builderGroup Builder group.
   * @param {import("../../proj.js").TransformFunction} [transform] Transform from user to view projection.
   * @param {boolean} [declutter] Enable decluttering.
   * @param {number} [index] Render order index.
   * @return {boolean} `true` if an image is loading.
   */
  renderFeature(e, t, n, s, r, o, a) {
    if (!n)
      return false;
    let l = false;
    if (Array.isArray(n))
      for (let c = 0, h2 = n.length; c < h2; ++c)
        l = vl(
          s,
          e,
          n[c],
          t,
          this.boundHandleStyleImageChange_,
          r,
          o,
          a
        ) || l;
    else
      l = vl(
        s,
        e,
        n,
        t,
        this.boundHandleStyleImageChange_,
        r,
        o,
        a
      );
    return l;
  }
};
var Xg = Vg;
function gh(i, e, t = 0, n = i.length - 1, s = Wg) {
  for (; n > t; ) {
    if (n - t > 600) {
      const l = n - t + 1, c = e - t + 1, h2 = Math.log(l), u = 0.5 * Math.exp(2 * h2 / 3), d = 0.5 * Math.sqrt(h2 * u * (l - u) / l) * (c - l / 2 < 0 ? -1 : 1), f = Math.max(t, Math.floor(e - c * u / l + d)), g = Math.min(n, Math.floor(e + (l - c) * u / l + d));
      gh(i, e, f, g, s);
    }
    const r = i[e];
    let o = t, a = n;
    for (ki(i, t, e), s(i[n], r) > 0 && ki(i, t, n); o < a; ) {
      for (ki(i, o, a), o++, a--; s(i[o], r) < 0; )
        o++;
      for (; s(i[a], r) > 0; )
        a--;
    }
    s(i[t], r) === 0 ? ki(i, t, a) : (a++, ki(i, a, n)), a <= e && (t = a + 1), e <= a && (n = a - 1);
  }
}
function ki(i, e, t) {
  const n = i[e];
  i[e] = i[t], i[t] = n;
}
function Wg(i, e) {
  return i < e ? -1 : i > e ? 1 : 0;
}
var mh = class {
  constructor(e = 9) {
    this._maxEntries = Math.max(4, e), this._minEntries = Math.max(2, Math.ceil(this._maxEntries * 0.4)), this.clear();
  }
  all() {
    return this._all(this.data, []);
  }
  search(e) {
    let t = this.data;
    const n = [];
    if (!Ms(e, t))
      return n;
    const s = this.toBBox, r = [];
    for (; t; ) {
      for (let o = 0; o < t.children.length; o++) {
        const a = t.children[o], l = t.leaf ? s(a) : a;
        Ms(e, l) && (t.leaf ? n.push(a) : Hr(e, l) ? this._all(a, n) : r.push(a));
      }
      t = r.pop();
    }
    return n;
  }
  collides(e) {
    let t = this.data;
    if (!Ms(e, t))
      return false;
    const n = [];
    for (; t; ) {
      for (let s = 0; s < t.children.length; s++) {
        const r = t.children[s], o = t.leaf ? this.toBBox(r) : r;
        if (Ms(e, o)) {
          if (t.leaf || Hr(e, o))
            return true;
          n.push(r);
        }
      }
      t = n.pop();
    }
    return false;
  }
  load(e) {
    if (!(e && e.length))
      return this;
    if (e.length < this._minEntries) {
      for (let n = 0; n < e.length; n++)
        this.insert(e[n]);
      return this;
    }
    let t = this._build(e.slice(), 0, e.length - 1, 0);
    if (!this.data.children.length)
      this.data = t;
    else if (this.data.height === t.height)
      this._splitRoot(this.data, t);
    else {
      if (this.data.height < t.height) {
        const n = this.data;
        this.data = t, t = n;
      }
      this._insert(t, this.data.height - t.height - 1, true);
    }
    return this;
  }
  insert(e) {
    return e && this._insert(e, this.data.height - 1), this;
  }
  clear() {
    return this.data = si([]), this;
  }
  remove(e, t) {
    if (!e)
      return this;
    let n = this.data;
    const s = this.toBBox(e), r = [], o = [];
    let a, l, c;
    for (; n || r.length; ) {
      if (n || (n = r.pop(), l = r[r.length - 1], a = o.pop(), c = true), n.leaf) {
        const h2 = Yg(e, n.children, t);
        if (h2 !== -1)
          return n.children.splice(h2, 1), r.push(n), this._condense(r), this;
      }
      !c && !n.leaf && Hr(n, s) ? (r.push(n), o.push(a), a = 0, l = n, n = n.children[0]) : l ? (a++, n = l.children[a], c = false) : n = null;
    }
    return this;
  }
  toBBox(e) {
    return e;
  }
  compareMinX(e, t) {
    return e.minX - t.minX;
  }
  compareMinY(e, t) {
    return e.minY - t.minY;
  }
  toJSON() {
    return this.data;
  }
  fromJSON(e) {
    return this.data = e, this;
  }
  _all(e, t) {
    const n = [];
    for (; e; )
      e.leaf ? t.push(...e.children) : n.push(...e.children), e = n.pop();
    return t;
  }
  _build(e, t, n, s) {
    const r = n - t + 1;
    let o = this._maxEntries, a;
    if (r <= o)
      return a = si(e.slice(t, n + 1)), $n(a, this.toBBox), a;
    s || (s = Math.ceil(Math.log(r) / Math.log(o)), o = Math.ceil(r / Math.pow(o, s - 1))), a = si([]), a.leaf = false, a.height = s;
    const l = Math.ceil(r / o), c = l * Math.ceil(Math.sqrt(o));
    Tl(e, t, n, c, this.compareMinX);
    for (let h2 = t; h2 <= n; h2 += c) {
      const u = Math.min(h2 + c - 1, n);
      Tl(e, h2, u, l, this.compareMinY);
      for (let d = h2; d <= u; d += l) {
        const f = Math.min(d + l - 1, u);
        a.children.push(this._build(e, d, f, s - 1));
      }
    }
    return $n(a, this.toBBox), a;
  }
  _chooseSubtree(e, t, n, s) {
    for (; s.push(t), !(t.leaf || s.length - 1 === n); ) {
      let r = 1 / 0, o = 1 / 0, a;
      for (let l = 0; l < t.children.length; l++) {
        const c = t.children[l], h2 = Kr(c), u = Kg(e, c) - h2;
        u < o ? (o = u, r = h2 < r ? h2 : r, a = c) : u === o && h2 < r && (r = h2, a = c);
      }
      t = a || t.children[0];
    }
    return t;
  }
  _insert(e, t, n) {
    const s = n ? e : this.toBBox(e), r = [], o = this._chooseSubtree(s, this.data, t, r);
    for (o.children.push(e), Bi(o, s); t >= 0 && r[t].children.length > this._maxEntries; )
      this._split(r, t), t--;
    this._adjustParentBBoxes(s, r, t);
  }
  // split overflowed node into two
  _split(e, t) {
    const n = e[t], s = n.children.length, r = this._minEntries;
    this._chooseSplitAxis(n, r, s);
    const o = this._chooseSplitIndex(n, r, s), a = si(n.children.splice(o, n.children.length - o));
    a.height = n.height, a.leaf = n.leaf, $n(n, this.toBBox), $n(a, this.toBBox), t ? e[t - 1].children.push(a) : this._splitRoot(n, a);
  }
  _splitRoot(e, t) {
    this.data = si([e, t]), this.data.height = e.height + 1, this.data.leaf = false, $n(this.data, this.toBBox);
  }
  _chooseSplitIndex(e, t, n) {
    let s, r = 1 / 0, o = 1 / 0;
    for (let a = t; a <= n - t; a++) {
      const l = Gi(e, 0, a, this.toBBox), c = Gi(e, a, n, this.toBBox), h2 = Hg(l, c), u = Kr(l) + Kr(c);
      h2 < r ? (r = h2, s = a, o = u < o ? u : o) : h2 === r && u < o && (o = u, s = a);
    }
    return s || n - t;
  }
  // sorts node children by the best axis for split
  _chooseSplitAxis(e, t, n) {
    const s = e.leaf ? this.compareMinX : jg, r = e.leaf ? this.compareMinY : $g, o = this._allDistMargin(e, t, n, s), a = this._allDistMargin(e, t, n, r);
    o < a && e.children.sort(s);
  }
  // total margin of all possible split distributions where each node is at least m full
  _allDistMargin(e, t, n, s) {
    e.children.sort(s);
    const r = this.toBBox, o = Gi(e, 0, t, r), a = Gi(e, n - t, n, r);
    let l = vs(o) + vs(a);
    for (let c = t; c < n - t; c++) {
      const h2 = e.children[c];
      Bi(o, e.leaf ? r(h2) : h2), l += vs(o);
    }
    for (let c = n - t - 1; c >= t; c--) {
      const h2 = e.children[c];
      Bi(a, e.leaf ? r(h2) : h2), l += vs(a);
    }
    return l;
  }
  _adjustParentBBoxes(e, t, n) {
    for (let s = n; s >= 0; s--)
      Bi(t[s], e);
  }
  _condense(e) {
    for (let t = e.length - 1, n; t >= 0; t--)
      e[t].children.length === 0 ? t > 0 ? (n = e[t - 1].children, n.splice(n.indexOf(e[t]), 1)) : this.clear() : $n(e[t], this.toBBox);
  }
};
function Yg(i, e, t) {
  if (!t)
    return e.indexOf(i);
  for (let n = 0; n < e.length; n++)
    if (t(i, e[n]))
      return n;
  return -1;
}
function $n(i, e) {
  Gi(i, 0, i.children.length, e, i);
}
function Gi(i, e, t, n, s) {
  s || (s = si(null)), s.minX = 1 / 0, s.minY = 1 / 0, s.maxX = -1 / 0, s.maxY = -1 / 0;
  for (let r = e; r < t; r++) {
    const o = i.children[r];
    Bi(s, i.leaf ? n(o) : o);
  }
  return s;
}
function Bi(i, e) {
  return i.minX = Math.min(i.minX, e.minX), i.minY = Math.min(i.minY, e.minY), i.maxX = Math.max(i.maxX, e.maxX), i.maxY = Math.max(i.maxY, e.maxY), i;
}
function jg(i, e) {
  return i.minX - e.minX;
}
function $g(i, e) {
  return i.minY - e.minY;
}
function Kr(i) {
  return (i.maxX - i.minX) * (i.maxY - i.minY);
}
function vs(i) {
  return i.maxX - i.minX + (i.maxY - i.minY);
}
function Kg(i, e) {
  return (Math.max(e.maxX, i.maxX) - Math.min(e.minX, i.minX)) * (Math.max(e.maxY, i.maxY) - Math.min(e.minY, i.minY));
}
function Hg(i, e) {
  const t = Math.max(i.minX, e.minX), n = Math.max(i.minY, e.minY), s = Math.min(i.maxX, e.maxX), r = Math.min(i.maxY, e.maxY);
  return Math.max(0, s - t) * Math.max(0, r - n);
}
function Hr(i, e) {
  return i.minX <= e.minX && i.minY <= e.minY && e.maxX <= i.maxX && e.maxY <= i.maxY;
}
function Ms(i, e) {
  return e.minX <= i.maxX && e.minY <= i.maxY && e.maxX >= i.minX && e.maxY >= i.minY;
}
function si(i) {
  return {
    children: i,
    height: 1,
    leaf: true,
    minX: 1 / 0,
    minY: 1 / 0,
    maxX: -1 / 0,
    maxY: -1 / 0
  };
}
function Tl(i, e, t, n, s) {
  const r = [e, t];
  for (; r.length; ) {
    if (t = r.pop(), e = r.pop(), t - e <= n)
      continue;
    const o = e + Math.ceil((t - e) / n / 2) * n;
    gh(i, o, e, t, s), r.push(e, o, o, t);
  }
}
var Vn = 0;
var je = 1 << Vn++;
var oe = 1 << Vn++;
var qe = 1 << Vn++;
var St = 1 << Vn++;
var Un = 1 << Vn++;
var zi = 1 << Vn++;
var Ts = Math.pow(2, Vn) - 1;
var Ta = {
  [je]: "boolean",
  [oe]: "number",
  [qe]: "string",
  [St]: "color",
  [Un]: "number[]",
  [zi]: "size"
};
var Zg = Object.keys(Ta).map(Number).sort(Yt);
function qg(i) {
  return i in Ta;
}
function Vi(i) {
  const e = [];
  for (const t of Zg)
    Xi(i, t) && e.push(Ta[t]);
  return e.length === 0 ? "untyped" : e.length < 3 ? e.join(" or ") : e.slice(0, -1).join(", ") + ", or " + e[e.length - 1];
}
function Xi(i, e) {
  return (i & e) === e;
}
function sn(i, e) {
  return i === e;
}
var Re = class {
  /**
   * @param {number} type The value type.
   * @param {LiteralValue} value The literal value.
   */
  constructor(e, t) {
    if (!qg(e))
      throw new Error(
        `literal expressions must have a specific type, got ${Vi(e)}`
      );
    this.type = e, this.value = t;
  }
};
var Jg = class {
  /**
   * @param {number} type The return type.
   * @param {string} operator The operator.
   * @param {...Expression} args The arguments.
   */
  constructor(e, t, ...n) {
    this.type = e, this.operator = t, this.args = n;
  }
};
function yh() {
  return {
    variables: /* @__PURE__ */ new Set(),
    properties: /* @__PURE__ */ new Set(),
    featureId: false,
    geometryType: false,
    mapState: false
  };
}
function ke(i, e, t) {
  switch (typeof i) {
    case "boolean": {
      if (sn(e, qe))
        return new Re(qe, i ? "true" : "false");
      if (!Xi(e, je))
        throw new Error(
          `got a boolean, but expected ${Vi(e)}`
        );
      return new Re(je, i);
    }
    case "number": {
      if (sn(e, zi))
        return new Re(zi, et(i));
      if (sn(e, je))
        return new Re(je, !!i);
      if (sn(e, qe))
        return new Re(qe, i.toString());
      if (!Xi(e, oe))
        throw new Error(`got a number, but expected ${Vi(e)}`);
      return new Re(oe, i);
    }
    case "string": {
      if (sn(e, St))
        return new Re(St, ha(i));
      if (sn(e, je))
        return new Re(je, !!i);
      if (!Xi(e, qe))
        throw new Error(`got a string, but expected ${Vi(e)}`);
      return new Re(qe, i);
    }
  }
  if (!Array.isArray(i))
    throw new Error("expression must be an array or a primitive value");
  if (i.length === 0)
    throw new Error("empty expression");
  if (typeof i[0] == "string")
    return cm(i, e, t);
  for (const n of i)
    if (typeof n != "number")
      throw new Error("expected an array of numbers");
  if (sn(e, zi)) {
    if (i.length !== 2)
      throw new Error(
        `expected an array of two values for a size, got ${i.length}`
      );
    return new Re(zi, i);
  }
  if (sn(e, St)) {
    if (i.length === 3)
      return new Re(St, [...i, 1]);
    if (i.length === 4)
      return new Re(St, i);
    throw new Error(
      `expected an array of 3 or 4 values for a color, got ${i.length}`
    );
  }
  if (!Xi(e, Un))
    throw new Error(
      `got an array of numbers, but expected ${Vi(e)}`
    );
  return new Re(Un, i);
}
var M = {
  Get: "get",
  Var: "var",
  Concat: "concat",
  GeometryType: "geometry-type",
  LineMetric: "line-metric",
  Any: "any",
  All: "all",
  Not: "!",
  Resolution: "resolution",
  Zoom: "zoom",
  Time: "time",
  Equal: "==",
  NotEqual: "!=",
  GreaterThan: ">",
  GreaterThanOrEqualTo: ">=",
  LessThan: "<",
  LessThanOrEqualTo: "<=",
  Multiply: "*",
  Divide: "/",
  Add: "+",
  Subtract: "-",
  Clamp: "clamp",
  Mod: "%",
  Pow: "^",
  Abs: "abs",
  Floor: "floor",
  Ceil: "ceil",
  Round: "round",
  Sin: "sin",
  Cos: "cos",
  Atan: "atan",
  Sqrt: "sqrt",
  Match: "match",
  Between: "between",
  Interpolate: "interpolate",
  Coalesce: "coalesce",
  Case: "case",
  In: "in",
  Number: "number",
  String: "string",
  Array: "array",
  Color: "color",
  Id: "id",
  Band: "band",
  Palette: "palette",
  ToString: "to-string",
  Has: "has"
};
var Qg = {
  [M.Get]: K(Q(1, 1 / 0), bl),
  [M.Var]: K(Q(1, 1), em),
  [M.Has]: K(Q(1, 1 / 0), bl),
  [M.Id]: K(tm, Kn),
  [M.Concat]: K(
    Q(2, 1 / 0),
    ge(qe)
  ),
  [M.GeometryType]: K(nm, Kn),
  [M.LineMetric]: K(Kn),
  [M.Resolution]: K(Zr, Kn),
  [M.Zoom]: K(Zr, Kn),
  [M.Time]: K(Zr, Kn),
  [M.Any]: K(
    Q(2, 1 / 0),
    ge(je)
  ),
  [M.All]: K(
    Q(2, 1 / 0),
    ge(je)
  ),
  [M.Not]: K(
    Q(1, 1),
    ge(je)
  ),
  [M.Equal]: K(
    Q(2, 2),
    ge(Ts)
  ),
  [M.NotEqual]: K(
    Q(2, 2),
    ge(Ts)
  ),
  [M.GreaterThan]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.GreaterThanOrEqualTo]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.LessThan]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.LessThanOrEqualTo]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.Multiply]: K(
    Q(2, 1 / 0),
    Rl
  ),
  [M.Coalesce]: K(
    Q(2, 1 / 0),
    Rl
  ),
  [M.Divide]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.Add]: K(
    Q(2, 1 / 0),
    ge(oe)
  ),
  [M.Subtract]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.Clamp]: K(
    Q(3, 3),
    ge(oe)
  ),
  [M.Mod]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.Pow]: K(
    Q(2, 2),
    ge(oe)
  ),
  [M.Abs]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Floor]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Ceil]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Round]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Sin]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Cos]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Atan]: K(
    Q(1, 2),
    ge(oe)
  ),
  [M.Sqrt]: K(
    Q(1, 1),
    ge(oe)
  ),
  [M.Match]: K(
    Q(4, 1 / 0),
    Ll,
    sm
  ),
  [M.Between]: K(
    Q(3, 3),
    ge(oe)
  ),
  [M.Interpolate]: K(
    Q(6, 1 / 0),
    Ll,
    rm
  ),
  [M.Case]: K(
    Q(3, 1 / 0),
    im,
    om
  ),
  [M.In]: K(Q(2, 2), am),
  [M.Number]: K(
    Q(1, 1 / 0),
    ge(Ts)
  ),
  [M.String]: K(
    Q(1, 1 / 0),
    ge(Ts)
  ),
  [M.Array]: K(
    Q(1, 1 / 0),
    ge(oe)
  ),
  [M.Color]: K(
    Q(1, 4),
    ge(oe)
  ),
  [M.Band]: K(
    Q(1, 3),
    ge(oe)
  ),
  [M.Palette]: K(
    Q(2, 2),
    lm
  ),
  [M.ToString]: K(
    Q(1, 1),
    ge(je | oe | qe | St)
  )
};
function bl(i, e, t) {
  const n = i.length - 1, s = new Array(n);
  for (let r = 0; r < n; ++r) {
    const o = i[r + 1];
    switch (typeof o) {
      case "number": {
        s[r] = new Re(oe, o);
        break;
      }
      case "string": {
        s[r] = new Re(qe, o);
        break;
      }
      default:
        throw new Error(
          `expected a string key or numeric array index for a get operation, got ${o}`
        );
    }
    r === 0 && t.properties.add(String(o));
  }
  return s;
}
function em(i, e, t) {
  const n = i[1];
  if (typeof n != "string")
    throw new Error("expected a string argument for var operation");
  return t.variables.add(n), [new Re(qe, n)];
}
function tm(i, e, t) {
  t.featureId = true;
}
function nm(i, e, t) {
  t.geometryType = true;
}
function Zr(i, e, t) {
  t.mapState = true;
}
function Kn(i, e, t) {
  const n = i[0];
  if (i.length !== 1)
    throw new Error(`expected no arguments for ${n} operation`);
  return [];
}
function Q(i, e) {
  return function(t, n, s) {
    const r = t[0], o = t.length - 1;
    if (i === e) {
      if (o !== i) {
        const a = i === 1 ? "" : "s";
        throw new Error(
          `expected ${i} argument${a} for ${r}, got ${o}`
        );
      }
    } else if (o < i || o > e) {
      const a = e === 1 / 0 ? `${i} or more` : `${i} to ${e}`;
      throw new Error(
        `expected ${a} arguments for ${r}, got ${o}`
      );
    }
  };
}
function Rl(i, e, t) {
  const n = i.length - 1, s = new Array(n);
  for (let r = 0; r < n; ++r) {
    const o = ke(i[r + 1], e, t);
    s[r] = o;
  }
  return s;
}
function ge(i) {
  return function(e, t, n) {
    const s = e.length - 1, r = new Array(s);
    for (let o = 0; o < s; ++o) {
      const a = ke(e[o + 1], i, n);
      r[o] = a;
    }
    return r;
  };
}
function im(i, e, t) {
  const n = i[0], s = i.length - 1;
  if (s % 2 === 0)
    throw new Error(
      `expected an odd number of arguments for ${n}, got ${s} instead`
    );
}
function Ll(i, e, t) {
  const n = i[0], s = i.length - 1;
  if (s % 2 === 1)
    throw new Error(
      `expected an even number of arguments for operation ${n}, got ${s} instead`
    );
}
function sm(i, e, t) {
  const n = i.length - 1, s = qe | oe | je, r = ke(i[1], s, t), o = ke(i[i.length - 1], e, t), a = new Array(n - 2);
  for (let l = 0; l < n - 2; l += 2) {
    try {
      const c = ke(i[l + 2], r.type, t);
      a[l] = c;
    } catch (c) {
      throw new Error(
        `failed to parse argument ${l + 1} of match expression: ${c.message}`
      );
    }
    try {
      const c = ke(i[l + 3], o.type, t);
      a[l + 1] = c;
    } catch (c) {
      throw new Error(
        `failed to parse argument ${l + 2} of match expression: ${c.message}`
      );
    }
  }
  return [r, ...a, o];
}
function rm(i, e, t) {
  const n = i[1];
  let s;
  switch (n[0]) {
    case "linear":
      s = 1;
      break;
    case "exponential":
      const l = n[1];
      if (typeof l != "number" || l <= 0)
        throw new Error(
          `expected a number base for exponential interpolation, got ${JSON.stringify(l)} instead`
        );
      s = l;
      break;
    default:
      throw new Error(
        `invalid interpolation type: ${JSON.stringify(n)}`
      );
  }
  const r = new Re(oe, s);
  let o;
  try {
    o = ke(i[2], oe, t);
  } catch (l) {
    throw new Error(
      `failed to parse argument 1 in interpolate expression: ${l.message}`
    );
  }
  const a = new Array(i.length - 3);
  for (let l = 0; l < a.length; l += 2) {
    try {
      const c = ke(i[l + 3], oe, t);
      a[l] = c;
    } catch (c) {
      throw new Error(
        `failed to parse argument ${l + 2} for interpolate expression: ${c.message}`
      );
    }
    try {
      const c = ke(i[l + 4], e, t);
      a[l + 1] = c;
    } catch (c) {
      throw new Error(
        `failed to parse argument ${l + 3} for interpolate expression: ${c.message}`
      );
    }
  }
  return [r, o, ...a];
}
function om(i, e, t) {
  const n = ke(i[i.length - 1], e, t), s = new Array(i.length - 1);
  for (let r = 0; r < s.length - 1; r += 2) {
    try {
      const o = ke(i[r + 1], je, t);
      s[r] = o;
    } catch (o) {
      throw new Error(
        `failed to parse argument ${r} of case expression: ${o.message}`
      );
    }
    try {
      const o = ke(i[r + 2], n.type, t);
      s[r + 1] = o;
    } catch (o) {
      throw new Error(
        `failed to parse argument ${r + 1} of case expression: ${o.message}`
      );
    }
  }
  return s[s.length - 1] = n, s;
}
function am(i, e, t) {
  let n = i[2];
  if (!Array.isArray(n))
    throw new Error(
      'the second argument for the "in" operator must be an array'
    );
  let s;
  if (typeof n[0] == "string") {
    if (n[0] !== "literal")
      throw new Error(
        'for the "in" operator, a string array should be wrapped in a "literal" operator to disambiguate from expressions'
      );
    if (!Array.isArray(n[1]))
      throw new Error(
        'failed to parse "in" expression: the literal operator must be followed by an array'
      );
    n = n[1], s = qe;
  } else
    s = oe;
  const r = new Array(n.length);
  for (let a = 0; a < r.length; a++)
    try {
      const l = ke(n[a], s, t);
      r[a] = l;
    } catch (l) {
      throw new Error(
        `failed to parse haystack item ${a} for "in" expression: ${l.message}`
      );
    }
  return [ke(i[1], s, t), ...r];
}
function lm(i, e, t) {
  let n;
  try {
    n = ke(i[1], oe, t);
  } catch (o) {
    throw new Error(
      `failed to parse first argument in palette expression: ${o.message}`
    );
  }
  const s = i[2];
  if (!Array.isArray(s))
    throw new Error("the second argument of palette must be an array");
  const r = new Array(s.length);
  for (let o = 0; o < r.length; o++) {
    let a;
    try {
      a = ke(s[o], St, t);
    } catch (l) {
      throw new Error(
        `failed to parse color at index ${o} in palette expression: ${l.message}`
      );
    }
    if (!(a instanceof Re))
      throw new Error(
        `the palette color at index ${o} must be a literal value`
      );
    r[o] = a;
  }
  return [n, ...r];
}
function K(...i) {
  return function(e, t, n) {
    const s = e[0];
    let r;
    for (let o = 0; o < i.length; o++) {
      const a = i[o](e, t, n);
      if (o == i.length - 1) {
        if (!a)
          throw new Error(
            "expected last argument validator to return the parsed args"
          );
        r = a;
      }
    }
    return new Jg(t, s, ...r);
  };
}
function cm(i, e, t) {
  const n = i[0], s = Qg[n];
  if (!s)
    throw new Error(`unknown operator: ${n}`);
  return s(i, e, t);
}
function _h(i) {
  if (!i)
    return "";
  const e = i.getType();
  switch (e) {
    case "Point":
    case "LineString":
    case "Polygon":
      return e;
    case "MultiPoint":
    case "MultiLineString":
    case "MultiPolygon":
      return (
        /** @type {'Point'|'LineString'|'Polygon'} */
        e.substring(5)
      );
    case "Circle":
      return "Polygon";
    case "GeometryCollection":
      return _h(
        /** @type {import("../geom/GeometryCollection.js").default} */
        i.getGeometries()[0]
      );
    default:
      return "";
  }
}
function ph() {
  return {
    variables: {},
    properties: {},
    resolution: NaN,
    featureId: null,
    geometryType: ""
  };
}
function qt(i, e, t) {
  const n = ke(i, e, t);
  return xt(n);
}
function xt(i, e) {
  if (i instanceof Re) {
    if (i.type === St && typeof i.value == "string") {
      const n = ha(i.value);
      return function() {
        return n;
      };
    }
    return function() {
      return i.value;
    };
  }
  const t = i.operator;
  switch (t) {
    case M.Number:
    case M.String:
    case M.Coalesce:
      return hm(i);
    case M.Get:
    case M.Var:
    case M.Has:
      return um(i);
    case M.Id:
      return (n) => n.featureId;
    case M.GeometryType:
      return (n) => n.geometryType;
    case M.Concat: {
      const n = i.args.map((s) => xt(s));
      return (s) => "".concat(...n.map((r) => r(s).toString()));
    }
    case M.Resolution:
      return (n) => n.resolution;
    case M.Any:
    case M.All:
    case M.Between:
    case M.In:
    case M.Not:
      return fm(i);
    case M.Equal:
    case M.NotEqual:
    case M.LessThan:
    case M.LessThanOrEqualTo:
    case M.GreaterThan:
    case M.GreaterThanOrEqualTo:
      return dm(i);
    case M.Multiply:
    case M.Divide:
    case M.Add:
    case M.Subtract:
    case M.Clamp:
    case M.Mod:
    case M.Pow:
    case M.Abs:
    case M.Floor:
    case M.Ceil:
    case M.Round:
    case M.Sin:
    case M.Cos:
    case M.Atan:
    case M.Sqrt:
      return gm(i);
    case M.Case:
      return mm(i);
    case M.Match:
      return ym(i);
    case M.Interpolate:
      return _m(i);
    case M.ToString:
      return pm(i);
    default:
      throw new Error(`Unsupported operator ${t}`);
  }
}
function hm(i, e) {
  const t = i.operator, n = i.args.length, s = new Array(n);
  for (let r = 0; r < n; ++r)
    s[r] = xt(i.args[r]);
  switch (t) {
    case M.Coalesce:
      return (r) => {
        for (let o = 0; o < n; ++o) {
          const a = s[o](r);
          if (typeof a < "u" && a !== null)
            return a;
        }
        throw new Error("Expected one of the values to be non-null");
      };
    case M.Number:
    case M.String:
      return (r) => {
        for (let o = 0; o < n; ++o) {
          const a = s[o](r);
          if (typeof a === t)
            return a;
        }
        throw new Error(`Expected one of the values to be a ${t}`);
      };
    default:
      throw new Error(`Unsupported assertion operator ${t}`);
  }
}
function um(i, e) {
  const n = (
    /** @type {string} */
    /** @type {LiteralExpression} */
    i.args[0].value
  );
  switch (i.operator) {
    case M.Get:
      return (s) => {
        const r = i.args;
        let o = s.properties[n];
        for (let a = 1, l = r.length; a < l; ++a) {
          const h2 = (
            /** @type {string|number} */
            /** @type {LiteralExpression} */
            r[a].value
          );
          o = o[h2];
        }
        return o;
      };
    case M.Var:
      return (s) => s.variables[n];
    case M.Has:
      return (s) => {
        const r = i.args;
        if (!(n in s.properties))
          return false;
        let o = s.properties[n];
        for (let a = 1, l = r.length; a < l; ++a) {
          const h2 = (
            /** @type {string|number} */
            /** @type {LiteralExpression} */
            r[a].value
          );
          if (!o || !Object.hasOwn(o, h2))
            return false;
          o = o[h2];
        }
        return true;
      };
    default:
      throw new Error(`Unsupported accessor operator ${i.operator}`);
  }
}
function dm(i, e) {
  const t = i.operator, n = xt(i.args[0]), s = xt(i.args[1]);
  switch (t) {
    case M.Equal:
      return (r) => n(r) === s(r);
    case M.NotEqual:
      return (r) => n(r) !== s(r);
    case M.LessThan:
      return (r) => n(r) < s(r);
    case M.LessThanOrEqualTo:
      return (r) => n(r) <= s(r);
    case M.GreaterThan:
      return (r) => n(r) > s(r);
    case M.GreaterThanOrEqualTo:
      return (r) => n(r) >= s(r);
    default:
      throw new Error(`Unsupported comparison operator ${t}`);
  }
}
function fm(i, e) {
  const t = i.operator, n = i.args.length, s = new Array(n);
  for (let r = 0; r < n; ++r)
    s[r] = xt(i.args[r]);
  switch (t) {
    case M.Any:
      return (r) => {
        for (let o = 0; o < n; ++o)
          if (s[o](r))
            return true;
        return false;
      };
    case M.All:
      return (r) => {
        for (let o = 0; o < n; ++o)
          if (!s[o](r))
            return false;
        return true;
      };
    case M.Between:
      return (r) => {
        const o = s[0](r), a = s[1](r), l = s[2](r);
        return o >= a && o <= l;
      };
    case M.In:
      return (r) => {
        const o = s[0](r);
        for (let a = 1; a < n; ++a)
          if (o === s[a](r))
            return true;
        return false;
      };
    case M.Not:
      return (r) => !s[0](r);
    default:
      throw new Error(`Unsupported logical operator ${t}`);
  }
}
function gm(i, e) {
  const t = i.operator, n = i.args.length, s = new Array(n);
  for (let r = 0; r < n; ++r)
    s[r] = xt(i.args[r]);
  switch (t) {
    case M.Multiply:
      return (r) => {
        let o = 1;
        for (let a = 0; a < n; ++a)
          o *= s[a](r);
        return o;
      };
    case M.Divide:
      return (r) => s[0](r) / s[1](r);
    case M.Add:
      return (r) => {
        let o = 0;
        for (let a = 0; a < n; ++a)
          o += s[a](r);
        return o;
      };
    case M.Subtract:
      return (r) => s[0](r) - s[1](r);
    case M.Clamp:
      return (r) => {
        const o = s[0](r), a = s[1](r);
        if (o < a)
          return a;
        const l = s[2](r);
        return o > l ? l : o;
      };
    case M.Mod:
      return (r) => s[0](r) % s[1](r);
    case M.Pow:
      return (r) => Math.pow(s[0](r), s[1](r));
    case M.Abs:
      return (r) => Math.abs(s[0](r));
    case M.Floor:
      return (r) => Math.floor(s[0](r));
    case M.Ceil:
      return (r) => Math.ceil(s[0](r));
    case M.Round:
      return (r) => Math.round(s[0](r));
    case M.Sin:
      return (r) => Math.sin(s[0](r));
    case M.Cos:
      return (r) => Math.cos(s[0](r));
    case M.Atan:
      return n === 2 ? (r) => Math.atan2(s[0](r), s[1](r)) : (r) => Math.atan(s[0](r));
    case M.Sqrt:
      return (r) => Math.sqrt(s[0](r));
    default:
      throw new Error(`Unsupported numeric operator ${t}`);
  }
}
function mm(i, e) {
  const t = i.args.length, n = new Array(t);
  for (let s = 0; s < t; ++s)
    n[s] = xt(i.args[s]);
  return (s) => {
    for (let r = 0; r < t - 1; r += 2)
      if (n[r](s))
        return n[r + 1](s);
    return n[t - 1](s);
  };
}
function ym(i, e) {
  const t = i.args.length, n = new Array(t);
  for (let s = 0; s < t; ++s)
    n[s] = xt(i.args[s]);
  return (s) => {
    const r = n[0](s);
    for (let o = 1; o < t - 1; o += 2)
      if (r === n[o](s))
        return n[o + 1](s);
    return n[t - 1](s);
  };
}
function _m(i, e) {
  const t = i.args.length, n = new Array(t);
  for (let s = 0; s < t; ++s)
    n[s] = xt(i.args[s]);
  return (s) => {
    const r = n[0](s), o = n[1](s);
    let a, l;
    for (let c = 2; c < t; c += 2) {
      const h2 = n[c](s);
      let u = n[c + 1](s);
      const d = Array.isArray(u);
      if (d && (u = Sf(u)), h2 >= o)
        return c === 2 ? u : d ? Sm(
          r,
          o,
          a,
          l,
          h2,
          u
        ) : Wi(
          r,
          o,
          a,
          l,
          h2,
          u
        );
      a = h2, l = u;
    }
    return l;
  };
}
function pm(i, e) {
  const t = i.operator, n = i.args.length, s = new Array(n);
  for (let r = 0; r < n; ++r)
    s[r] = xt(i.args[r]);
  switch (t) {
    case M.ToString:
      return (r) => {
        const o = s[0](r);
        return i.args[0].type === St ? ua(o) : o.toString();
      };
    default:
      throw new Error(`Unsupported convert operator ${t}`);
  }
}
function Wi(i, e, t, n, s, r) {
  const o = s - t;
  if (o === 0)
    return n;
  const a = e - t, l = i === 1 ? a / o : (Math.pow(i, a) - 1) / (Math.pow(i, o) - 1);
  return n + l * (r - n);
}
function Sm(i, e, t, n, s, r) {
  if (s - t === 0)
    return n;
  const a = cl(n), l = cl(r);
  let c = l[2] - a[2];
  c > 180 ? c -= 360 : c < -180 && (c += 360);
  const h2 = [
    Wi(i, e, t, a[0], s, l[0]),
    Wi(i, e, t, a[1], s, l[1]),
    a[2] + Wi(i, e, t, 0, s, c),
    Wi(i, e, t, n[3], s, r[3])
  ];
  return Ef(h2);
}
function Em(i) {
  return true;
}
function wm(i) {
  const e = yh(), t = Cm(i, e), n = ph();
  return function(s, r) {
    if (n.properties = s.getPropertiesInternal(), n.resolution = r, e.featureId) {
      const o = s.getId();
      o !== void 0 ? n.featureId = o : n.featureId = null;
    }
    return e.geometryType && (n.geometryType = _h(
      s.getGeometry()
    )), t(n);
  };
}
function Fl(i) {
  const e = yh(), t = i.length, n = new Array(t);
  for (let o = 0; o < t; ++o)
    n[o] = Fo(i[o], e);
  const s = ph(), r = new Array(t);
  return function(o, a) {
    if (s.properties = o.getPropertiesInternal(), s.resolution = a, e.featureId) {
      const c = o.getId();
      c !== void 0 ? s.featureId = c : s.featureId = null;
    }
    let l = 0;
    for (let c = 0; c < t; ++c) {
      const h2 = n[c](s);
      h2 && (r[l] = h2, l += 1);
    }
    return r.length = l, r;
  };
}
function Cm(i, e) {
  const t = i.length, n = new Array(t);
  for (let s = 0; s < t; ++s) {
    const r = i[s], o = "filter" in r ? qt(r.filter, je, e) : Em;
    let a;
    if (Array.isArray(r.style)) {
      const l = r.style.length;
      a = new Array(l);
      for (let c = 0; c < l; ++c)
        a[c] = Fo(r.style[c], e);
    } else
      a = [Fo(r.style, e)];
    n[s] = { filter: o, styles: a };
  }
  return function(s) {
    const r = [];
    let o = false;
    for (let a = 0; a < t; ++a) {
      const l = n[a].filter;
      if (l(s) && !(i[a].else && o)) {
        o = true;
        for (const c of n[a].styles) {
          const h2 = c(s);
          h2 && r.push(h2);
        }
      }
    }
    return r;
  };
}
function Fo(i, e) {
  const t = ss(i, "", e), n = rs(i, "", e), s = xm(i, e), r = Im(i, e), o = Qe(i, "z-index", e);
  if (!t && !n && !s && !r && !yi(i))
    throw new Error(
      "No fill, stroke, point, or text symbolizer properties in style: " + JSON.stringify(i)
    );
  const a = new te();
  return function(l) {
    let c = true;
    if (t) {
      const h2 = t(l);
      h2 && (c = false), a.setFill(h2);
    }
    if (n) {
      const h2 = n(l);
      h2 && (c = false), a.setStroke(h2);
    }
    if (s) {
      const h2 = s(l);
      h2 && (c = false), a.setText(h2);
    }
    if (r) {
      const h2 = r(l);
      h2 && (c = false), a.setImage(h2);
    }
    return o && a.setZIndex(o(l)), c ? null : a;
  };
}
function ss(i, e, t) {
  let n;
  if (e + "fill-pattern-src" in i)
    n = bm(i, e + "fill-", t);
  else {
    if (i[e + "fill-color"] === "none")
      return (r) => null;
    n = ba(
      i,
      e + "fill-color",
      t
    );
  }
  if (!n)
    return null;
  const s = new J();
  return function(r) {
    const o = n(r);
    return o === ca ? null : (s.setColor(o), s);
  };
}
function rs(i, e, t) {
  const n = Qe(
    i,
    e + "stroke-width",
    t
  ), s = ba(
    i,
    e + "stroke-color",
    t
  );
  if (!n && !s)
    return null;
  const r = Wt(
    i,
    e + "stroke-line-cap",
    t
  ), o = Wt(
    i,
    e + "stroke-line-join",
    t
  ), a = Sh(
    i,
    e + "stroke-line-dash",
    t
  ), l = Qe(
    i,
    e + "stroke-line-dash-offset",
    t
  ), c = Qe(
    i,
    e + "stroke-miter-limit",
    t
  ), h2 = new se();
  return function(u) {
    if (s) {
      const d = s(u);
      if (d === ca)
        return null;
      h2.setColor(d);
    }
    if (n && h2.setWidth(n(u)), r) {
      const d = r(u);
      if (d !== "butt" && d !== "round" && d !== "square")
        throw new Error("Expected butt, round, or square line cap");
      h2.setLineCap(d);
    }
    if (o) {
      const d = o(u);
      if (d !== "bevel" && d !== "round" && d !== "miter")
        throw new Error("Expected bevel, round, or miter line join");
      h2.setLineJoin(d);
    }
    return a && h2.setLineDash(a(u)), l && h2.setLineDashOffset(l(u)), c && h2.setMiterLimit(c(u)), h2;
  };
}
function xm(i, e) {
  const t = "text-", n = Wt(i, t + "value", e);
  if (!n)
    return null;
  const s = ss(i, t, e), r = ss(
    i,
    t + "background-",
    e
  ), o = rs(i, t, e), a = rs(
    i,
    t + "background-",
    e
  ), l = Wt(i, t + "font", e), c = Qe(
    i,
    t + "max-angle",
    e
  ), h2 = Qe(
    i,
    t + "offset-x",
    e
  ), u = Qe(
    i,
    t + "offset-y",
    e
  ), d = fi(
    i,
    t + "overflow",
    e
  ), f = Wt(
    i,
    t + "placement",
    e
  ), g = Qe(i, t + "repeat", e), y = Mr(i, t + "scale", e), m = fi(
    i,
    t + "rotate-with-view",
    e
  ), _ = Qe(
    i,
    t + "rotation",
    e
  ), p = Wt(i, t + "align", e), E = Wt(
    i,
    t + "justify",
    e
  ), S = Wt(
    i,
    t + "baseline",
    e
  ), w = fi(
    i,
    t + "keep-upright",
    e
  ), x = Sh(
    i,
    t + "padding",
    e
  ), b = Tr(
    i,
    t + "declutter-mode"
  ), v = new zn({ declutterMode: b });
  return function(R) {
    if (v.setText(n(R)), s && v.setFill(s(R)), r && v.setBackgroundFill(r(R)), o && v.setStroke(o(R)), a && v.setBackgroundStroke(a(R)), l && v.setFont(l(R)), c && v.setMaxAngle(c(R)), h2 && v.setOffsetX(h2(R)), u && v.setOffsetY(u(R)), d && v.setOverflow(d(R)), f) {
      const P = f(R);
      if (P !== "point" && P !== "line")
        throw new Error("Expected point or line for text-placement");
      v.setPlacement(P);
    }
    if (g && v.setRepeat(g(R)), y && v.setScale(y(R)), m && v.setRotateWithView(m(R)), _ && v.setRotation(_(R)), p) {
      const P = p(R);
      if (P !== "left" && P !== "center" && P !== "right" && P !== "end" && P !== "start")
        throw new Error(
          "Expected left, right, center, start, or end for text-align"
        );
      v.setTextAlign(P);
    }
    if (E) {
      const P = E(R);
      if (P !== "left" && P !== "right" && P !== "center")
        throw new Error("Expected left, right, or center for text-justify");
      v.setJustify(P);
    }
    if (S) {
      const P = S(R);
      if (P !== "bottom" && P !== "top" && P !== "middle" && P !== "alphabetic" && P !== "hanging")
        throw new Error(
          "Expected bottom, top, middle, alphabetic, or hanging for text-baseline"
        );
      v.setTextBaseline(P);
    }
    return x && v.setPadding(x(R)), w && v.setKeepUpright(w(R)), v;
  };
}
function Im(i, e) {
  return "icon-src" in i ? vm(i, e) : "shape-points" in i ? Mm(i, e) : "circle-radius" in i ? Tm(i, e) : null;
}
function vm(i, e) {
  const t = "icon-", n = t + "src", s = Eh(i[n], n), r = rr(
    i,
    t + "anchor",
    e
  ), o = Mr(i, t + "scale", e), a = Qe(
    i,
    t + "opacity",
    e
  ), l = rr(
    i,
    t + "displacement",
    e
  ), c = Qe(
    i,
    t + "rotation",
    e
  ), h2 = fi(
    i,
    t + "rotate-with-view",
    e
  ), u = Pl(i, t + "anchor-origin"), d = Al(
    i,
    t + "anchor-x-units"
  ), f = Al(
    i,
    t + "anchor-y-units"
  ), g = Dm(i, t + "color"), y = Lm(i, t + "cross-origin"), m = Fm(i, t + "offset"), _ = Pl(i, t + "offset-origin"), p = or(i, t + "width"), E = or(i, t + "height"), S = Rm(i, t + "size"), w = Tr(
    i,
    t + "declutter-mode"
  ), x = new yn({
    src: s,
    anchorOrigin: u,
    anchorXUnits: d,
    anchorYUnits: f,
    color: g,
    crossOrigin: y,
    offset: m,
    offsetOrigin: _,
    height: E,
    width: p,
    size: S,
    declutterMode: w
  });
  return function(b) {
    return a && x.setOpacity(a(b)), l && x.setDisplacement(l(b)), c && x.setRotation(c(b)), h2 && x.setRotateWithView(h2(b)), o && x.setScale(o(b)), r && x.setAnchor(r(b)), x;
  };
}
function Mm(i, e) {
  const t = "shape-", n = t + "points", s = t + "radius", r = Do(i[n], n), o = Do(i[s], s), a = ss(i, t, e), l = rs(i, t, e), c = Mr(i, t + "scale", e), h2 = rr(
    i,
    t + "displacement",
    e
  ), u = Qe(
    i,
    t + "rotation",
    e
  ), d = fi(
    i,
    t + "rotate-with-view",
    e
  ), f = or(i, t + "radius2"), g = or(i, t + "angle"), y = Tr(
    i,
    t + "declutter-mode"
  ), m = new fs({
    points: r,
    radius: o,
    radius2: f,
    angle: g,
    declutterMode: y
  });
  return function(_) {
    return a && m.setFill(a(_)), l && m.setStroke(l(_)), h2 && m.setDisplacement(h2(_)), u && m.setRotation(u(_)), d && m.setRotateWithView(d(_)), c && m.setScale(c(_)), m;
  };
}
function Tm(i, e) {
  const t = "circle-", n = ss(i, t, e), s = rs(i, t, e), r = Qe(i, t + "radius", e), o = Mr(i, t + "scale", e), a = rr(
    i,
    t + "displacement",
    e
  ), l = Qe(
    i,
    t + "rotation",
    e
  ), c = fi(
    i,
    t + "rotate-with-view",
    e
  ), h2 = Tr(
    i,
    t + "declutter-mode"
  ), u = new we({
    radius: 5,
    // this is arbitrary, but required - the evaluated radius is used below
    declutterMode: h2
  });
  return function(d) {
    return r && u.setRadius(r(d)), n && u.setFill(n(d)), s && u.setStroke(s(d)), a && u.setDisplacement(a(d)), l && u.setRotation(l(d)), c && u.setRotateWithView(c(d)), o && u.setScale(o(d)), u;
  };
}
function Qe(i, e, t) {
  if (!(e in i))
    return;
  const n = qt(i[e], oe, t);
  return function(s) {
    return Do(n(s), e);
  };
}
function Wt(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], qe, t);
  return function(s) {
    return Eh(n(s), e);
  };
}
function bm(i, e, t) {
  const n = Wt(
    i,
    e + "pattern-src",
    t
  ), s = Dl(
    i,
    e + "pattern-offset",
    t
  ), r = Dl(
    i,
    e + "pattern-size",
    t
  ), o = ba(
    i,
    e + "color",
    t
  );
  return function(a) {
    return {
      src: n(a),
      offset: s && s(a),
      size: r && r(a),
      color: o && o(a)
    };
  };
}
function fi(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], je, t);
  return function(s) {
    const r = n(s);
    if (typeof r != "boolean")
      throw new Error(`Expected a boolean for ${e}`);
    return r;
  };
}
function ba(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], St, t);
  return function(s) {
    return wh(n(s), e);
  };
}
function Sh(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], Un, t);
  return function(s) {
    return ys(n(s), e);
  };
}
function rr(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], Un, t);
  return function(s) {
    const r = ys(n(s), e);
    if (r.length !== 2)
      throw new Error(`Expected two numbers for ${e}`);
    return r;
  };
}
function Dl(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(i[e], Un, t);
  return function(s) {
    return Ch(n(s), e);
  };
}
function Mr(i, e, t) {
  if (!(e in i))
    return null;
  const n = qt(
    i[e],
    Un | oe,
    t
  );
  return function(s) {
    return Pm(n(s), e);
  };
}
function or(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (typeof t != "number")
      throw new Error(`Expected a number for ${e}`);
    return t;
  }
}
function Rm(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (typeof t == "number")
      return et(t);
    if (!Array.isArray(t))
      throw new Error(`Expected a number or size array for ${e}`);
    if (t.length !== 2 || typeof t[0] != "number" || typeof t[1] != "number")
      throw new Error(`Expected a number or size array for ${e}`);
    return t;
  }
}
function Lm(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (typeof t != "string")
      throw new Error(`Expected a string for ${e}`);
    return t;
  }
}
function Pl(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (t !== "bottom-left" && t !== "bottom-right" && t !== "top-left" && t !== "top-right")
      throw new Error(
        `Expected bottom-left, bottom-right, top-left, or top-right for ${e}`
      );
    return t;
  }
}
function Al(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (t !== "pixels" && t !== "fraction")
      throw new Error(`Expected pixels or fraction for ${e}`);
    return t;
  }
}
function Fm(i, e) {
  const t = i[e];
  if (t !== void 0)
    return ys(t, e);
}
function Tr(i, e) {
  const t = i[e];
  if (t !== void 0) {
    if (typeof t != "string")
      throw new Error(`Expected a string for ${e}`);
    if (t !== "declutter" && t !== "obstacle" && t !== "none")
      throw new Error(`Expected declutter, obstacle, or none for ${e}`);
    return t;
  }
}
function Dm(i, e) {
  const t = i[e];
  if (t !== void 0)
    return wh(t, e);
}
function ys(i, e) {
  if (!Array.isArray(i))
    throw new Error(`Expected an array for ${e}`);
  const t = i.length;
  for (let n = 0; n < t; ++n)
    if (typeof i[n] != "number")
      throw new Error(`Expected an array of numbers for ${e}`);
  return i;
}
function Eh(i, e) {
  if (typeof i != "string")
    throw new Error(`Expected a string for ${e}`);
  return i;
}
function Do(i, e) {
  if (typeof i != "number")
    throw new Error(`Expected a number for ${e}`);
  return i;
}
function wh(i, e) {
  if (typeof i == "string")
    return i;
  const t = ys(i, e), n = t.length;
  if (n < 3 || n > 4)
    throw new Error(`Expected a color with 3 or 4 values for ${e}`);
  return t;
}
function Ch(i, e) {
  const t = ys(i, e);
  if (t.length !== 2)
    throw new Error(`Expected an array of two numbers for ${e}`);
  return t;
}
function Pm(i, e) {
  return typeof i == "number" ? i : Ch(i, e);
}
var pt = {
  CENTER: "center",
  RESOLUTION: "resolution",
  ROTATION: "rotation"
};
function kl(i, e, t) {
  return (
    /**
     * @param {import("./coordinate.js").Coordinate|undefined} center Center.
     * @param {number|undefined} resolution Resolution.
     * @param {import("./size.js").Size} size Viewport size; unused if `onlyCenter` was specified.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @param {Array<number>} [centerShift] Shift between map center and viewport center.
     * @return {import("./coordinate.js").Coordinate|undefined} Center.
     */
    function(n, s, r, o, a) {
      if (!n)
        return;
      if (!s && !e)
        return n;
      const l = e ? 0 : r[0] * s, c = e ? 0 : r[1] * s, h2 = a ? a[0] : 0, u = a ? a[1] : 0;
      let d = i[0] + l / 2 + h2, f = i[2] - l / 2 + h2, g = i[1] + c / 2 + u, y = i[3] - c / 2 + u;
      d > f && (d = (f + d) / 2, f = d), g > y && (g = (y + g) / 2, y = g);
      let m = xe(n[0], d, f), _ = xe(n[1], g, y);
      if (o && t && s) {
        const p = 30 * s;
        m += -p * Math.log(1 + Math.max(0, d - n[0]) / p) + p * Math.log(1 + Math.max(0, n[0] - f) / p), _ += -p * Math.log(1 + Math.max(0, g - n[1]) / p) + p * Math.log(1 + Math.max(0, n[1] - y) / p);
      }
      return [m, _];
    }
  );
}
function Am(i) {
  return i;
}
function xh(i) {
  return Math.pow(i, 3);
}
function Ti(i) {
  return 1 - xh(1 - i);
}
function km(i) {
  return 3 * i * i - 2 * i * i * i;
}
function Om(i) {
  return i;
}
function Ra(i, e, t, n) {
  const s = ye(e) / t[0], r = Ve(e) / t[1];
  return n ? Math.min(i, Math.max(s, r)) : Math.min(i, Math.min(s, r));
}
function La(i, e, t) {
  let n = Math.min(i, e);
  const s = 50;
  return n *= Math.log(1 + s * Math.max(0, i / e - 1)) / s + 1, t && (n = Math.max(n, t), n /= Math.log(1 + s * Math.max(0, t / i - 1)) / s + 1), xe(n, t / 2, e * 2);
}
function Nm(i, e, t, n) {
  return e = e !== void 0 ? e : true, /**
  * @param {number|undefined} resolution Resolution.
  * @param {number} direction Direction.
  * @param {import("./size.js").Size} size Viewport size.
  * @param {boolean} [isMoving] True if an interaction or animation is in progress.
  * @return {number|undefined} Resolution.
  */
  function(s, r, o, a) {
    if (s !== void 0) {
      const l = i[0], c = i[i.length - 1], h2 = t ? Ra(
        l,
        t,
        o,
        n
      ) : l;
      if (a)
        return e ? La(
          s,
          h2,
          c
        ) : xe(s, c, h2);
      const u = Math.min(h2, s), d = Math.floor(Go(i, u, r));
      return i[d] > h2 && d < i.length - 1 ? i[d + 1] : i[d];
    }
  };
}
function Um(i, e, t, n, s, r) {
  return n = n !== void 0 ? n : true, t = t !== void 0 ? t : 0, /**
  * @param {number|undefined} resolution Resolution.
  * @param {number} direction Direction.
  * @param {import("./size.js").Size} size Viewport size.
  * @param {boolean} [isMoving] True if an interaction or animation is in progress.
  * @return {number|undefined} Resolution.
  */
  function(o, a, l, c) {
    if (o !== void 0) {
      const h2 = s ? Ra(
        e,
        s,
        l,
        r
      ) : e;
      if (c)
        return n ? La(
          o,
          h2,
          t
        ) : xe(o, t, h2);
      const u = 1e-9, d = Math.ceil(
        Math.log(e / h2) / Math.log(i) - u
      ), f = -a * (0.5 - u) + 0.5, g = Math.min(h2, o), y = Math.floor(
        Math.log(e / g) / Math.log(i) + f
      ), m = Math.max(d, y), _ = e / Math.pow(i, m);
      return xe(_, t, h2);
    }
  };
}
function Ol(i, e, t, n, s) {
  return t = t !== void 0 ? t : true, /**
  * @param {number|undefined} resolution Resolution.
  * @param {number} direction Direction.
  * @param {import("./size.js").Size} size Viewport size.
  * @param {boolean} [isMoving] True if an interaction or animation is in progress.
  * @return {number|undefined} Resolution.
  */
  function(r, o, a, l) {
    if (r !== void 0) {
      const c = n ? Ra(
        i,
        n,
        a,
        s
      ) : i;
      return !t || !l ? xe(r, e, c) : La(
        r,
        c,
        e
      );
    }
  };
}
function Fa(i) {
  if (i !== void 0)
    return 0;
}
function Nl(i) {
  if (i !== void 0)
    return i;
}
function Gm(i) {
  const e = 2 * Math.PI / i;
  return (
    /**
     * @param {number|undefined} rotation Rotation.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @return {number|undefined} Rotation.
     */
    function(t, n) {
      if (n)
        return t;
      if (t !== void 0)
        return t = Math.floor(t / e + 0.5) * e, t;
    }
  );
}
function Bm(i) {
  const e = i === void 0 ? Et(5) : i;
  return (
    /**
     * @param {number|undefined} rotation Rotation.
     * @param {boolean} [isMoving] True if an interaction or animation is in progress.
     * @return {number|undefined} Rotation.
     */
    function(t, n) {
      return n || t === void 0 ? t : Math.abs(t) <= e ? 0 : t;
    }
  );
}
var zm = 42;
var Da = 256;
var qr = 0;
var Vm = class extends vt {
  /**
   * @param {ViewOptions} [options] View options.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, e = Object.assign({}, e), this.hints_ = [0, 0], this.animations_ = [], this.updateAnimationKey_, this.projection_ = Jo(e.projection, "EPSG:3857"), this.viewportSize_ = [100, 100], this.targetCenter_ = null, this.targetResolution_, this.targetRotation_, this.nextCenter_ = null, this.nextResolution_, this.nextRotation_, this.cancelAnchor_ = void 0, e.projection && Rc(), e.center && (e.center = Be(e.center, this.projection_)), e.extent && (e.extent = cn(e.extent, this.projection_)), this.applyOptions_(e);
  }
  /**
   * Set up the view with the given options.
   * @param {ViewOptions} options View options.
   */
  applyOptions_(e) {
    const t = Object.assign({}, e);
    for (const a in pt)
      delete t[a];
    this.setProperties(t, true);
    const n = Wm(e);
    this.maxResolution_ = n.maxResolution, this.minResolution_ = n.minResolution, this.zoomFactor_ = n.zoomFactor, this.resolutions_ = e.resolutions, this.padding_ = e.padding, this.minZoom_ = n.minZoom;
    const s = Xm(e), r = n.constraint, o = Ym(e);
    this.constraints_ = {
      center: s,
      resolution: r,
      rotation: o
    }, this.setRotation(e.rotation !== void 0 ? e.rotation : 0), this.setCenterInternal(
      e.center !== void 0 ? e.center : null
    ), e.resolution !== void 0 ? this.setResolution(e.resolution) : e.zoom !== void 0 && this.setZoom(e.zoom);
  }
  /**
   * Padding (in css pixels).
   * If the map viewport is partially covered with other content (overlays) along
   * its edges, this setting allows to shift the center of the viewport away from that
   * content. The order of the values in the array is top, right, bottom, left.
   * The default is no padding, which is equivalent to `[0, 0, 0, 0]`.
   * @type {Array<number>|undefined}
   * @api
   */
  get padding() {
    return this.padding_;
  }
  set padding(e) {
    let t = this.padding_;
    this.padding_ = e;
    const n = this.getCenterInternal();
    if (n) {
      const s = e || [0, 0, 0, 0];
      t = t || [0, 0, 0, 0];
      const r = this.getResolution(), o = r / 2 * (s[3] - t[3] + t[1] - s[1]), a = r / 2 * (s[0] - t[0] + t[2] - s[2]);
      this.setCenterInternal([n[0] + o, n[1] - a]);
    }
  }
  /**
   * Get an updated version of the view options used to construct the view.  The
   * current resolution (or zoom), center, and rotation are applied to any stored
   * options.  The provided options can be used to apply new min/max zoom or
   * resolution limits.
   * @param {ViewOptions} newOptions New options to be applied.
   * @return {ViewOptions} New options updated with the current view state.
   */
  getUpdatedOptions_(e) {
    const t = this.getProperties();
    return t.resolution !== void 0 ? t.resolution = this.getResolution() : t.zoom = this.getZoom(), t.center = this.getCenterInternal(), t.rotation = this.getRotation(), Object.assign({}, t, e);
  }
  /**
   * Animate the view.  The view's center, zoom (or resolution), and rotation
   * can be animated for smooth transitions between view states.  For example,
   * to animate the view to a new zoom level:
   *
   *     view.animate({zoom: view.getZoom() + 1});
   *
   * By default, the animation lasts one second and uses in-and-out easing.  You
   * can customize this behavior by including `duration` (in milliseconds) and
   * `easing` options (see {@link module:ol/easing}).
   *
   * To chain together multiple animations, call the method with multiple
   * animation objects.  For example, to first zoom and then pan:
   *
   *     view.animate({zoom: 10}, {center: [0, 0]});
   *
   * If you provide a function as the last argument to the animate method, it
   * will get called at the end of an animation series.  The callback will be
   * called with `true` if the animation series completed on its own or `false`
   * if it was cancelled.
   *
   * Animations are cancelled by user interactions (e.g. dragging the map) or by
   * calling `view.setCenter()`, `view.setResolution()`, or `view.setRotation()`
   * (or another method that calls one of these).
   *
   * @param {...(AnimationOptions|function(boolean): void)} var_args Animation
   *     options.  Multiple animations can be run in series by passing multiple
   *     options objects.  To run multiple animations in parallel, call the method
   *     multiple times.  An optional callback can be provided as a final
   *     argument.  The callback will be called with a boolean indicating whether
   *     the animation completed without being cancelled.
   * @api
   */
  animate(e) {
    this.isDef() && !this.getAnimating() && this.resolveConstraints(0);
    const t = new Array(arguments.length);
    for (let n = 0; n < t.length; ++n) {
      let s = arguments[n];
      s.center && (s = Object.assign({}, s), s.center = Be(
        s.center,
        this.getProjection()
      )), s.anchor && (s = Object.assign({}, s), s.anchor = Be(
        s.anchor,
        this.getProjection()
      )), t[n] = s;
    }
    this.animateInternal.apply(this, t);
  }
  /**
   * @param {...(AnimationOptions|function(boolean): void)} var_args Animation options.
   */
  animateInternal(e) {
    let t = arguments.length, n;
    t > 1 && typeof arguments[t - 1] == "function" && (n = arguments[t - 1], --t);
    let s = 0;
    for (; s < t && !this.isDef(); ++s) {
      const h2 = arguments[s];
      h2.center && this.setCenterInternal(h2.center), h2.zoom !== void 0 ? this.setZoom(h2.zoom) : h2.resolution && this.setResolution(h2.resolution), h2.rotation !== void 0 && this.setRotation(h2.rotation);
    }
    if (s === t) {
      n && bs(n, true);
      return;
    }
    let r = Date.now(), o = this.targetCenter_.slice(), a = this.targetResolution_, l = this.targetRotation_;
    const c = [];
    for (; s < t; ++s) {
      const h2 = (
        /** @type {AnimationOptions} */
        arguments[s]
      ), u = {
        start: r,
        complete: false,
        anchor: h2.anchor,
        duration: h2.duration !== void 0 ? h2.duration : 1e3,
        easing: h2.easing || km,
        callback: n
      };
      if (h2.center && (u.sourceCenter = o, u.targetCenter = h2.center.slice(), o = u.targetCenter), h2.zoom !== void 0 ? (u.sourceResolution = a, u.targetResolution = this.getResolutionForZoom(h2.zoom), a = u.targetResolution) : h2.resolution && (u.sourceResolution = a, u.targetResolution = h2.resolution, a = u.targetResolution), h2.rotation !== void 0) {
        u.sourceRotation = l;
        const d = gn(h2.rotation - l + Math.PI, 2 * Math.PI) - Math.PI;
        u.targetRotation = l + d, l = u.targetRotation;
      }
      jm(u) ? u.complete = true : r += u.duration, c.push(u);
    }
    this.animations_.push(c), this.setHint(ze.ANIMATING, 1), this.updateAnimations_();
  }
  /**
   * Determine if the view is being animated.
   * @return {boolean} The view is being animated.
   * @api
   */
  getAnimating() {
    return this.hints_[ze.ANIMATING] > 0;
  }
  /**
   * Determine if the user is interacting with the view, such as panning or zooming.
   * @return {boolean} The view is being interacted with.
   * @api
   */
  getInteracting() {
    return this.hints_[ze.INTERACTING] > 0;
  }
  /**
   * Cancel any ongoing animations.
   * @api
   */
  cancelAnimations() {
    this.setHint(ze.ANIMATING, -this.hints_[ze.ANIMATING]);
    let e;
    for (let t = 0, n = this.animations_.length; t < n; ++t) {
      const s = this.animations_[t];
      if (s[0].callback && bs(s[0].callback, false), !e)
        for (let r = 0, o = s.length; r < o; ++r) {
          const a = s[r];
          if (!a.complete) {
            e = a.anchor;
            break;
          }
        }
    }
    this.animations_.length = 0, this.cancelAnchor_ = e, this.nextCenter_ = null, this.nextResolution_ = NaN, this.nextRotation_ = NaN;
  }
  /**
   * Update all animations.
   */
  updateAnimations_() {
    if (this.updateAnimationKey_ !== void 0 && (cancelAnimationFrame(this.updateAnimationKey_), this.updateAnimationKey_ = void 0), !this.getAnimating())
      return;
    const e = Date.now();
    let t = false;
    for (let n = this.animations_.length - 1; n >= 0; --n) {
      const s = this.animations_[n];
      let r = true;
      for (let o = 0, a = s.length; o < a; ++o) {
        const l = s[o];
        if (l.complete)
          continue;
        const c = e - l.start;
        let h2 = l.duration > 0 ? c / l.duration : 1;
        h2 >= 1 ? (l.complete = true, h2 = 1) : r = false;
        const u = l.easing(h2);
        if (l.sourceCenter) {
          const d = l.sourceCenter[0], f = l.sourceCenter[1], g = l.targetCenter[0], y = l.targetCenter[1];
          this.nextCenter_ = l.targetCenter;
          const m = d + u * (g - d), _ = f + u * (y - f);
          this.targetCenter_ = [m, _];
        }
        if (l.sourceResolution && l.targetResolution) {
          const d = u === 1 ? l.targetResolution : l.sourceResolution + u * (l.targetResolution - l.sourceResolution);
          if (l.anchor) {
            const f = this.getViewportSize_(this.getRotation()), g = this.constraints_.resolution(
              d,
              0,
              f,
              true
            );
            this.targetCenter_ = this.calculateCenterZoom(
              g,
              l.anchor
            );
          }
          this.nextResolution_ = l.targetResolution, this.targetResolution_ = d, this.applyTargetState_(true);
        }
        if (l.sourceRotation !== void 0 && l.targetRotation !== void 0) {
          const d = u === 1 ? gn(l.targetRotation + Math.PI, 2 * Math.PI) - Math.PI : l.sourceRotation + u * (l.targetRotation - l.sourceRotation);
          if (l.anchor) {
            const f = this.constraints_.rotation(
              d,
              true
            );
            this.targetCenter_ = this.calculateCenterRotate(
              f,
              l.anchor
            );
          }
          this.nextRotation_ = l.targetRotation, this.targetRotation_ = d;
        }
        if (this.applyTargetState_(true), t = true, !l.complete)
          break;
      }
      if (r) {
        this.animations_[n] = null, this.setHint(ze.ANIMATING, -1), this.nextCenter_ = null, this.nextResolution_ = NaN, this.nextRotation_ = NaN;
        const o = s[0].callback;
        o && bs(o, true);
      }
    }
    this.animations_ = this.animations_.filter(Boolean), t && this.updateAnimationKey_ === void 0 && (this.updateAnimationKey_ = requestAnimationFrame(
      this.updateAnimations_.bind(this)
    ));
  }
  /**
   * @param {number} rotation Target rotation.
   * @param {import("./coordinate.js").Coordinate} anchor Rotation anchor.
   * @return {import("./coordinate.js").Coordinate|undefined} Center for rotation and anchor.
   */
  calculateCenterRotate(e, t) {
    let n;
    const s = this.getCenterInternal();
    return s !== void 0 && (n = [s[0] - t[0], s[1] - t[1]], Yo(n, e - this.getRotation()), nd(n, t)), n;
  }
  /**
   * @param {number} resolution Target resolution.
   * @param {import("./coordinate.js").Coordinate} anchor Zoom anchor.
   * @return {import("./coordinate.js").Coordinate|undefined} Center for resolution and anchor.
   */
  calculateCenterZoom(e, t) {
    let n;
    const s = this.getCenterInternal(), r = this.getResolution();
    if (s !== void 0 && r !== void 0) {
      const o = t[0] - e * (t[0] - s[0]) / r, a = t[1] - e * (t[1] - s[1]) / r;
      n = [o, a];
    }
    return n;
  }
  /**
   * Returns the current viewport size.
   * @private
   * @param {number} [rotation] Take into account the rotation of the viewport when giving the size
   * @return {import("./size.js").Size} Viewport size or `[100, 100]` when no viewport is found.
   */
  getViewportSize_(e) {
    const t = this.viewportSize_;
    if (e) {
      const n = t[0], s = t[1];
      return [
        Math.abs(n * Math.cos(e)) + Math.abs(s * Math.sin(e)),
        Math.abs(n * Math.sin(e)) + Math.abs(s * Math.cos(e))
      ];
    }
    return t;
  }
  /**
   * Stores the viewport size on the view. The viewport size is not read every time from the DOM
   * to avoid performance hit and layout reflow.
   * This should be done on map size change.
   * Note: the constraints are not resolved during an animation to avoid stopping it
   * @param {import("./size.js").Size} [size] Viewport size; if undefined, [100, 100] is assumed
   */
  setViewportSize(e) {
    this.viewportSize_ = Array.isArray(e) ? e.slice() : [100, 100], this.getAnimating() || this.resolveConstraints(0);
  }
  /**
   * Get the view center.
   * @return {import("./coordinate.js").Coordinate|undefined} The center of the view.
   * @observable
   * @api
   */
  getCenter() {
    const e = this.getCenterInternal();
    return e && wo(e, this.getProjection());
  }
  /**
   * Get the view center without transforming to user projection.
   * @return {import("./coordinate.js").Coordinate|undefined} The center of the view.
   */
  getCenterInternal() {
    return (
      /** @type {import("./coordinate.js").Coordinate|undefined} */
      this.get(pt.CENTER)
    );
  }
  /**
   * @return {Constraints} Constraints.
   */
  getConstraints() {
    return this.constraints_;
  }
  /**
   * @return {boolean} Resolution constraint is set
   */
  getConstrainResolution() {
    return this.get("constrainResolution");
  }
  /**
   * @param {Array<number>} [hints] Destination array.
   * @return {Array<number>} Hint.
   */
  getHints(e) {
    return e !== void 0 ? (e[0] = this.hints_[0], e[1] = this.hints_[1], e) : this.hints_.slice();
  }
  /**
   * Calculate the extent for the current view state and the passed box size.
   * @param {import("./size.js").Size} [size] The pixel dimensions of the box
   * into which the calculated extent should fit. Defaults to the size of the
   * map the view is associated with.
   * If no map or multiple maps are connected to the view, provide the desired
   * box size (e.g. `map.getSize()`).
   * @return {import("./extent.js").Extent} Extent.
   * @api
   */
  calculateExtent(e) {
    const t = this.calculateExtentInternal(e);
    return Qo(t, this.getProjection());
  }
  /**
   * @param {import("./size.js").Size} [size] Box pixel size. If not provided,
   * the map's last known viewport size will be used.
   * @return {import("./extent.js").Extent} Extent.
   */
  calculateExtentInternal(e) {
    e = e || this.getViewportSizeMinusPadding_();
    const t = (
      /** @type {!import("./coordinate.js").Coordinate} */
      this.getCenterInternal()
    );
    _e(t, "The view center is not defined");
    const n = (
      /** @type {!number} */
      this.getResolution()
    );
    _e(n !== void 0, "The view resolution is not defined");
    const s = (
      /** @type {!number} */
      this.getRotation()
    );
    return _e(s !== void 0, "The view rotation is not defined"), mo(t, n, s, e);
  }
  /**
   * Get the maximum resolution of the view.
   * @return {number} The maximum resolution of the view.
   * @api
   */
  getMaxResolution() {
    return this.maxResolution_;
  }
  /**
   * Get the minimum resolution of the view.
   * @return {number} The minimum resolution of the view.
   * @api
   */
  getMinResolution() {
    return this.minResolution_;
  }
  /**
   * Get the maximum zoom level for the view.
   * @return {number} The maximum zoom level.
   * @api
   */
  getMaxZoom() {
    return (
      /** @type {number} */
      this.getZoomForResolution(this.minResolution_)
    );
  }
  /**
   * Set a new maximum zoom level for the view.
   * @param {number} zoom The maximum zoom level.
   * @api
   */
  setMaxZoom(e) {
    this.applyOptions_(this.getUpdatedOptions_({ maxZoom: e }));
  }
  /**
   * Get the minimum zoom level for the view.
   * @return {number} The minimum zoom level.
   * @api
   */
  getMinZoom() {
    return (
      /** @type {number} */
      this.getZoomForResolution(this.maxResolution_)
    );
  }
  /**
   * Set a new minimum zoom level for the view.
   * @param {number} zoom The minimum zoom level.
   * @api
   */
  setMinZoom(e) {
    this.applyOptions_(this.getUpdatedOptions_({ minZoom: e }));
  }
  /**
   * Set whether the view should allow intermediary zoom levels.
   * @param {boolean} enabled Whether the resolution is constrained.
   * @api
   */
  setConstrainResolution(e) {
    this.applyOptions_(this.getUpdatedOptions_({ constrainResolution: e }));
  }
  /**
   * Get the view projection.
   * @return {import("./proj/Projection.js").default} The projection of the view.
   * @api
   */
  getProjection() {
    return this.projection_;
  }
  /**
   * Get the view resolution.
   * @return {number|undefined} The resolution of the view.
   * @observable
   * @api
   */
  getResolution() {
    return (
      /** @type {number|undefined} */
      this.get(pt.RESOLUTION)
    );
  }
  /**
   * Get the resolutions for the view. This returns the array of resolutions
   * passed to the constructor of the View, or undefined if none were given.
   * @return {Array<number>|undefined} The resolutions of the view.
   * @api
   */
  getResolutions() {
    return this.resolutions_;
  }
  /**
   * Get the resolution for a provided extent (in map units) and size (in pixels).
   * @param {import("./extent.js").Extent} extent Extent.
   * @param {import("./size.js").Size} [size] Box pixel size.
   * @return {number} The resolution at which the provided extent will render at
   *     the given size.
   * @api
   */
  getResolutionForExtent(e, t) {
    return this.getResolutionForExtentInternal(
      cn(e, this.getProjection()),
      t
    );
  }
  /**
   * Get the resolution for a provided extent (in map units) and size (in pixels).
   * @param {import("./extent.js").Extent} extent Extent.
   * @param {import("./size.js").Size} [size] Box pixel size.
   * @return {number} The resolution at which the provided extent will render at
   *     the given size.
   */
  getResolutionForExtentInternal(e, t) {
    t = t || this.getViewportSizeMinusPadding_();
    const n = ye(e) / t[0], s = Ve(e) / t[1];
    return Math.max(n, s);
  }
  /**
   * Return a function that returns a value between 0 and 1 for a
   * resolution. Exponential scaling is assumed.
   * @param {number} [power] Power.
   * @return {function(number): number} Resolution for value function.
   */
  getResolutionForValueFunction(e) {
    e = e || 2;
    const t = this.getConstrainedResolution(this.maxResolution_), n = this.minResolution_, s = Math.log(t / n) / Math.log(e);
    return (
      /**
       * @param {number} value Value.
       * @return {number} Resolution.
       */
      function(r) {
        return t / Math.pow(e, r * s);
      }
    );
  }
  /**
   * Get the view rotation.
   * @return {number} The rotation of the view in radians.
   * @observable
   * @api
   */
  getRotation() {
    return (
      /** @type {number} */
      this.get(pt.ROTATION)
    );
  }
  /**
   * Return a function that returns a resolution for a value between
   * 0 and 1. Exponential scaling is assumed.
   * @param {number} [power] Power.
   * @return {function(number): number} Value for resolution function.
   */
  getValueForResolutionFunction(e) {
    const t = Math.log(e || 2), n = this.getConstrainedResolution(this.maxResolution_), s = this.minResolution_, r = Math.log(n / s) / t;
    return (
      /**
       * @param {number} resolution Resolution.
       * @return {number} Value.
       */
      function(o) {
        return Math.log(n / o) / t / r;
      }
    );
  }
  /**
   * Returns the size of the viewport minus padding.
   * @private
   * @param {number} [rotation] Take into account the rotation of the viewport when giving the size
   * @return {import("./size.js").Size} Viewport size reduced by the padding.
   */
  getViewportSizeMinusPadding_(e) {
    let t = this.getViewportSize_(e);
    const n = this.padding_;
    return n && (t = [
      t[0] - n[1] - n[3],
      t[1] - n[0] - n[2]
    ]), t;
  }
  /**
   * @return {State} View state.
   */
  getState() {
    const e = this.getProjection(), t = this.getResolution(), n = this.getRotation();
    let s = (
      /** @type {import("./coordinate.js").Coordinate} */
      this.getCenterInternal()
    );
    const r = this.padding_;
    if (r) {
      const o = this.getViewportSizeMinusPadding_();
      s = Jr(
        s,
        this.getViewportSize_(),
        [o[0] / 2 + r[3], o[1] / 2 + r[0]],
        t,
        n
      );
    }
    return {
      center: s.slice(0),
      projection: e !== void 0 ? e : null,
      resolution: t,
      nextCenter: this.nextCenter_,
      nextResolution: this.nextResolution_,
      nextRotation: this.nextRotation_,
      rotation: n,
      zoom: this.getZoom()
    };
  }
  /**
   * @return {ViewStateLayerStateExtent} Like `FrameState`, but just `viewState` and `extent`.
   */
  getViewStateAndExtent() {
    return {
      viewState: this.getState(),
      extent: this.calculateExtent()
    };
  }
  /**
   * Get the current zoom level. This method may return non-integer zoom levels
   * if the view does not constrain the resolution, or if an interaction or
   * animation is underway.
   * @return {number|undefined} Zoom.
   * @api
   */
  getZoom() {
    let e;
    const t = this.getResolution();
    return t !== void 0 && (e = this.getZoomForResolution(t)), e;
  }
  /**
   * Get the zoom level for a resolution.
   * @param {number} resolution The resolution.
   * @return {number|undefined} The zoom level for the provided resolution.
   * @api
   */
  getZoomForResolution(e) {
    let t = this.minZoom_ || 0, n, s;
    if (this.resolutions_) {
      const r = Go(this.resolutions_, e, 1);
      t = r, n = this.resolutions_[r], r == this.resolutions_.length - 1 ? s = 2 : s = n / this.resolutions_[r + 1];
    } else
      n = this.maxResolution_, s = this.zoomFactor_;
    return t + Math.log(n / e) / Math.log(s);
  }
  /**
   * Get the resolution for a zoom level.
   * @param {number} zoom Zoom level.
   * @return {number} The view resolution for the provided zoom level.
   * @api
   */
  getResolutionForZoom(e) {
    var t;
    if ((t = this.resolutions_) != null && t.length) {
      if (this.resolutions_.length === 1)
        return this.resolutions_[0];
      const n = xe(
        Math.floor(e),
        0,
        this.resolutions_.length - 2
      ), s = this.resolutions_[n] / this.resolutions_[n + 1];
      return this.resolutions_[n] / Math.pow(s, xe(e - n, 0, 1));
    }
    return this.maxResolution_ / Math.pow(this.zoomFactor_, e - this.minZoom_);
  }
  /**
   * Fit the given geometry or extent based on the given map size and border.
   * The size is pixel dimensions of the box to fit the extent into.
   * In most cases you will want to use the map size, that is `map.getSize()`.
   * Takes care of the map angle.
   * @param {import("./geom/SimpleGeometry.js").default|import("./extent.js").Extent} geometryOrExtent The geometry or
   *     extent to fit the view to.
   * @param {FitOptions} [options] Options.
   * @api
   */
  fit(e, t) {
    let n;
    if (_e(
      Array.isArray(e) || typeof /** @type {?} */
      e.getSimplifiedGeometry == "function",
      "Invalid extent or geometry provided as `geometry`"
    ), Array.isArray(e)) {
      _e(
        !hs(e),
        "Cannot fit empty extent provided as `geometry`"
      );
      const s = cn(e, this.getProjection());
      n = Sl(s);
    } else if (e.getType() === "Circle") {
      const s = cn(
        e.getExtent(),
        this.getProjection()
      );
      n = Sl(s), n.rotate(this.getRotation(), pn(s));
    } else
      n = e;
    this.fitInternal(n, t);
  }
  /**
   * Calculate rotated extent
   * @param {import("./geom/SimpleGeometry.js").default} geometry The geometry.
   * @return {import("./extent").Extent} The rotated extent for the geometry.
   */
  rotatedExtentForGeometry(e) {
    const t = this.getRotation(), n = Math.cos(t), s = Math.sin(-t), r = e.getFlatCoordinates(), o = e.getStride();
    let a = 1 / 0, l = 1 / 0, c = -1 / 0, h2 = -1 / 0;
    for (let u = 0, d = r.length; u < d; u += o) {
      const f = r[u] * n - r[u + 1] * s, g = r[u] * s + r[u + 1] * n;
      a = Math.min(a, f), l = Math.min(l, g), c = Math.max(c, f), h2 = Math.max(h2, g);
    }
    return [a, l, c, h2];
  }
  /**
   * @param {import("./geom/SimpleGeometry.js").default} geometry The geometry.
   * @param {FitOptions} [options] Options.
   */
  fitInternal(e, t) {
    t = t || {};
    let n = t.size;
    n || (n = this.getViewportSizeMinusPadding_());
    const s = t.padding !== void 0 ? t.padding : [0, 0, 0, 0], r = t.nearest !== void 0 ? t.nearest : false;
    let o;
    t.minResolution !== void 0 ? o = t.minResolution : t.maxZoom !== void 0 ? o = this.getResolutionForZoom(t.maxZoom) : o = 0;
    const a = this.rotatedExtentForGeometry(e);
    let l = this.getResolutionForExtentInternal(a, [
      n[0] - s[1] - s[3],
      n[1] - s[0] - s[2]
    ]);
    l = isNaN(l) ? o : Math.max(l, o), l = this.getConstrainedResolution(l, r ? 0 : 1);
    const c = this.getRotation(), h2 = Math.sin(c), u = Math.cos(c), d = pn(a);
    d[0] += (s[1] - s[3]) / 2 * l, d[1] += (s[0] - s[2]) / 2 * l;
    const f = d[0] * u - d[1] * h2, g = d[1] * u + d[0] * h2, y = this.getConstrainedCenter([f, g], l), m = t.callback ? t.callback : Ki;
    t.duration !== void 0 ? this.animateInternal(
      {
        resolution: l,
        center: y,
        duration: t.duration,
        easing: t.easing
      },
      m
    ) : (this.targetResolution_ = l, this.targetCenter_ = y, this.applyTargetState_(false, true), bs(m, true));
  }
  /**
   * Center on coordinate and view position.
   * @param {import("./coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("./size.js").Size} size Box pixel size.
   * @param {import("./pixel.js").Pixel} position Position on the view to center on.
   * @api
   */
  centerOn(e, t, n) {
    this.centerOnInternal(
      Be(e, this.getProjection()),
      t,
      n
    );
  }
  /**
   * @param {import("./coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("./size.js").Size} size Box pixel size.
   * @param {import("./pixel.js").Pixel} position Position on the view to center on.
   */
  centerOnInternal(e, t, n) {
    this.setCenterInternal(
      Jr(
        e,
        t,
        n,
        this.getResolution(),
        this.getRotation()
      )
    );
  }
  /**
   * Calculates the shift between map and viewport center.
   * @param {import("./coordinate.js").Coordinate} center Center.
   * @param {number} resolution Resolution.
   * @param {number} rotation Rotation.
   * @param {import("./size.js").Size} size Size.
   * @return {Array<number>|undefined} Center shift.
   */
  calculateCenterShift(e, t, n, s) {
    let r;
    const o = this.padding_;
    if (o && e) {
      const a = this.getViewportSizeMinusPadding_(-n), l = Jr(
        e,
        s,
        [a[0] / 2 + o[3], a[1] / 2 + o[0]],
        t,
        n
      );
      r = [
        e[0] - l[0],
        e[1] - l[1]
      ];
    }
    return r;
  }
  /**
   * @return {boolean} Is defined.
   */
  isDef() {
    return !!this.getCenterInternal() && this.getResolution() !== void 0;
  }
  /**
   * Adds relative coordinates to the center of the view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate} deltaCoordinates Relative value to add.
   * @api
   */
  adjustCenter(e) {
    const t = wo(this.targetCenter_, this.getProjection());
    this.setCenter([
      t[0] + e[0],
      t[1] + e[1]
    ]);
  }
  /**
   * Adds relative coordinates to the center of the view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate} deltaCoordinates Relative value to add.
   */
  adjustCenterInternal(e) {
    const t = this.targetCenter_;
    this.setCenterInternal([
      t[0] + e[0],
      t[1] + e[1]
    ]);
  }
  /**
   * Multiply the view resolution by a ratio, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} ratio The ratio to apply on the view resolution.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  adjustResolution(e, t) {
    t = t && Be(t, this.getProjection()), this.adjustResolutionInternal(e, t);
  }
  /**
   * Multiply the view resolution by a ratio, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} ratio The ratio to apply on the view resolution.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  adjustResolutionInternal(e, t) {
    const n = this.getAnimating() || this.getInteracting(), s = this.getViewportSize_(this.getRotation()), r = this.constraints_.resolution(
      this.targetResolution_ * e,
      0,
      s,
      n
    );
    t && (this.targetCenter_ = this.calculateCenterZoom(r, t)), this.targetResolution_ *= e, this.applyTargetState_();
  }
  /**
   * Adds a value to the view zoom level, optionally using an anchor. Any resolution
   * constraint will apply.
   * @param {number} delta Relative value to add to the zoom level.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  adjustZoom(e, t) {
    this.adjustResolution(Math.pow(this.zoomFactor_, -e), t);
  }
  /**
   * Adds a value to the view rotation, optionally using an anchor. Any rotation
   * constraint will apply.
   * @param {number} delta Relative value to add to the zoom rotation, in radians.
   * @param {import("./coordinate.js").Coordinate} [anchor] The rotation center.
   * @api
   */
  adjustRotation(e, t) {
    t && (t = Be(t, this.getProjection())), this.adjustRotationInternal(e, t);
  }
  /**
   * @param {number} delta Relative value to add to the zoom rotation, in radians.
   * @param {import("./coordinate.js").Coordinate} [anchor] The rotation center.
   */
  adjustRotationInternal(e, t) {
    const n = this.getAnimating() || this.getInteracting(), s = this.constraints_.rotation(
      this.targetRotation_ + e,
      n
    );
    t && (this.targetCenter_ = this.calculateCenterRotate(s, t)), this.targetRotation_ += e, this.applyTargetState_();
  }
  /**
   * Set the center of the current view. Any extent constraint will apply.
   * @param {import("./coordinate.js").Coordinate|undefined} center The center of the view.
   * @observable
   * @api
   */
  setCenter(e) {
    this.setCenterInternal(
      e && Be(e, this.getProjection())
    );
  }
  /**
   * Set the center using the view projection (not the user projection).
   * @param {import("./coordinate.js").Coordinate|undefined} center The center of the view.
   */
  setCenterInternal(e) {
    this.targetCenter_ = e, this.applyTargetState_();
  }
  /**
   * @param {import("./ViewHint.js").default} hint Hint.
   * @param {number} delta Delta.
   * @return {number} New value.
   */
  setHint(e, t) {
    return this.hints_[e] += t, this.changed(), this.hints_[e];
  }
  /**
   * Set the resolution for this view. Any resolution constraint will apply.
   * @param {number|undefined} resolution The resolution of the view.
   * @observable
   * @api
   */
  setResolution(e) {
    this.targetResolution_ = e, this.applyTargetState_();
  }
  /**
   * Set the rotation for this view. Any rotation constraint will apply.
   * @param {number} rotation The rotation of the view in radians.
   * @observable
   * @api
   */
  setRotation(e) {
    this.targetRotation_ = e, this.applyTargetState_();
  }
  /**
   * Zoom to a specific zoom level. Any resolution constrain will apply.
   * @param {number} zoom Zoom level.
   * @api
   */
  setZoom(e) {
    this.setResolution(this.getResolutionForZoom(e));
  }
  /**
   * Recompute rotation/resolution/center based on target values.
   * Note: we have to compute rotation first, then resolution and center considering that
   * parameters can influence one another in case a view extent constraint is present.
   * @param {boolean} [doNotCancelAnims] Do not cancel animations.
   * @param {boolean} [forceMoving] Apply constraints as if the view is moving.
   * @private
   */
  applyTargetState_(e, t) {
    const n = this.getAnimating() || this.getInteracting() || t, s = this.constraints_.rotation(
      this.targetRotation_,
      n
    ), r = this.getViewportSize_(s), o = this.constraints_.resolution(
      this.targetResolution_,
      0,
      r,
      n
    ), a = this.constraints_.center(
      this.targetCenter_,
      o,
      r,
      n,
      this.calculateCenterShift(
        this.targetCenter_,
        o,
        s,
        r
      )
    );
    this.get(pt.ROTATION) !== s && this.set(pt.ROTATION, s), this.get(pt.RESOLUTION) !== o && (this.set(pt.RESOLUTION, o), this.set("zoom", this.getZoom(), true)), (!a || !this.get(pt.CENTER) || !Ys(this.get(pt.CENTER), a)) && this.set(pt.CENTER, a), this.getAnimating() && !e && this.cancelAnimations(), this.cancelAnchor_ = void 0;
  }
  /**
   * If any constraints need to be applied, an animation will be triggered.
   * This is typically done on interaction end.
   * Note: calling this with a duration of 0 will apply the constrained values straight away,
   * without animation.
   * @param {number} [duration] The animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  resolveConstraints(e, t, n) {
    e = e !== void 0 ? e : 200;
    const s = t || 0, r = this.constraints_.rotation(this.targetRotation_), o = this.getViewportSize_(r), a = this.constraints_.resolution(
      this.targetResolution_,
      s,
      o
    ), l = this.constraints_.center(
      this.targetCenter_,
      a,
      o,
      false,
      this.calculateCenterShift(
        this.targetCenter_,
        a,
        r,
        o
      )
    );
    if (e === 0 && !this.cancelAnchor_) {
      this.targetResolution_ = a, this.targetRotation_ = r, this.targetCenter_ = l, this.applyTargetState_();
      return;
    }
    n = n || (e === 0 ? this.cancelAnchor_ : void 0), this.cancelAnchor_ = void 0, (this.getResolution() !== a || this.getRotation() !== r || !this.getCenterInternal() || !Ys(this.getCenterInternal(), l)) && (this.getAnimating() && this.cancelAnimations(), this.animateInternal({
      rotation: r,
      center: l,
      resolution: a,
      duration: e,
      easing: Ti,
      anchor: n
    }));
  }
  /**
   * Notify the View that an interaction has started.
   * The view state will be resolved to a stable one if needed
   * (depending on its constraints).
   * @api
   */
  beginInteraction() {
    this.resolveConstraints(0), this.setHint(ze.INTERACTING, 1);
  }
  /**
   * Notify the View that an interaction has ended. The view state will be resolved
   * to a stable one if needed (depending on its constraints).
   * @param {number} [duration] Animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   * @api
   */
  endInteraction(e, t, n) {
    n = n && Be(n, this.getProjection()), this.endInteractionInternal(e, t, n);
  }
  /**
   * Notify the View that an interaction has ended. The view state will be resolved
   * to a stable one if needed (depending on its constraints).
   * @param {number} [duration] Animation duration in ms.
   * @param {number} [resolutionDirection] Which direction to zoom.
   * @param {import("./coordinate.js").Coordinate} [anchor] The origin of the transformation.
   */
  endInteractionInternal(e, t, n) {
    this.getInteracting() && (this.setHint(ze.INTERACTING, -1), this.resolveConstraints(e, t, n));
  }
  /**
   * Get a valid position for the view center according to the current constraints.
   * @param {import("./coordinate.js").Coordinate|undefined} targetCenter Target center position.
   * @param {number} [targetResolution] Target resolution. If not supplied, the current one will be used.
   * This is useful to guess a valid center position at a different zoom level.
   * @return {import("./coordinate.js").Coordinate|undefined} Valid center position.
   */
  getConstrainedCenter(e, t) {
    const n = this.getViewportSize_(this.getRotation());
    return this.constraints_.center(
      e,
      t || this.getResolution(),
      n
    );
  }
  /**
   * Get a valid zoom level according to the current view constraints.
   * @param {number|undefined} targetZoom Target zoom.
   * @param {number} [direction] Indicate which resolution should be used
   * by a renderer if the view resolution does not match any resolution of the tile source.
   * If 0, the nearest resolution will be used. If 1, the nearest lower resolution
   * will be used. If -1, the nearest higher resolution will be used.
   * @return {number|undefined} Valid zoom level.
   */
  getConstrainedZoom(e, t) {
    const n = this.getResolutionForZoom(e);
    return this.getZoomForResolution(
      this.getConstrainedResolution(n, t)
    );
  }
  /**
   * Get a valid resolution according to the current view constraints.
   * @param {number|undefined} targetResolution Target resolution.
   * @param {number} [direction] Indicate which resolution should be used
   * by a renderer if the view resolution does not match any resolution of the tile source.
   * If 0, the nearest resolution will be used. If 1, the nearest lower resolution
   * will be used. If -1, the nearest higher resolution will be used.
   * @return {number|undefined} Valid resolution.
   */
  getConstrainedResolution(e, t) {
    t = t || 0;
    const n = this.getViewportSize_(this.getRotation());
    return this.constraints_.resolution(e, t, n);
  }
};
function bs(i, e) {
  setTimeout(function() {
    i(e);
  }, 0);
}
function Xm(i) {
  if (i.extent !== void 0) {
    const t = i.smoothExtentConstraint !== void 0 ? i.smoothExtentConstraint : true;
    return kl(i.extent, i.constrainOnlyCenter, t);
  }
  const e = Jo(i.projection, "EPSG:3857");
  if (i.multiWorld !== true && e.isGlobal()) {
    const t = e.getExtent().slice();
    return t[0] = -1 / 0, t[2] = 1 / 0, kl(t, false, false);
  }
  return Am;
}
function Wm(i) {
  let e, t, n, o = i.minZoom !== void 0 ? i.minZoom : qr, a = i.maxZoom !== void 0 ? i.maxZoom : 28;
  const l = i.zoomFactor !== void 0 ? i.zoomFactor : 2, c = i.multiWorld !== void 0 ? i.multiWorld : false, h2 = i.smoothResolutionConstraint !== void 0 ? i.smoothResolutionConstraint : true, u = i.showFullExtent !== void 0 ? i.showFullExtent : false, d = Jo(i.projection, "EPSG:3857"), f = d.getExtent();
  let g = i.constrainOnlyCenter, y = i.extent;
  if (!c && !y && d.isGlobal() && (g = false, y = f), i.resolutions !== void 0) {
    const m = i.resolutions;
    t = m[o], n = m[a] !== void 0 ? m[a] : m[m.length - 1], i.constrainResolution ? e = Nm(
      m,
      h2,
      !g && y,
      u
    ) : e = Ol(
      t,
      n,
      h2,
      !g && y,
      u
    );
  } else {
    const _ = (f ? Math.max(ye(f), Ve(f)) : (
      // use an extent that can fit the whole world if need be
      360 * Zi.degrees / d.getMetersPerUnit()
    )) / Da / Math.pow(2, qr), p = _ / Math.pow(2, 28 - qr);
    t = i.maxResolution, t !== void 0 ? o = 0 : t = _ / Math.pow(l, o), n = i.minResolution, n === void 0 && (i.maxZoom !== void 0 ? i.maxResolution !== void 0 ? n = t / Math.pow(l, a) : n = _ / Math.pow(l, a) : n = p), a = o + Math.floor(
      Math.log(t / n) / Math.log(l)
    ), n = t / Math.pow(l, a - o), i.constrainResolution ? e = Um(
      l,
      t,
      n,
      h2,
      !g && y,
      u
    ) : e = Ol(
      t,
      n,
      h2,
      !g && y,
      u
    );
  }
  return {
    constraint: e,
    maxResolution: t,
    minResolution: n,
    minZoom: o,
    zoomFactor: l
  };
}
function Ym(i) {
  if (i.enableRotation !== void 0 ? i.enableRotation : true) {
    const t = i.constrainRotation;
    return t === void 0 || t === true ? Bm() : t === false ? Nl : typeof t == "number" ? Gm(t) : Nl;
  }
  return Fa;
}
function jm(i) {
  return !(i.sourceCenter && i.targetCenter && !Ys(i.sourceCenter, i.targetCenter) || i.sourceResolution !== i.targetResolution || i.sourceRotation !== i.targetRotation);
}
function Jr(i, e, t, n, s) {
  const r = Math.cos(-s);
  let o = Math.sin(-s), a = i[0] * r - i[1] * o, l = i[1] * r + i[0] * o;
  a += (e[0] / 2 - t[0]) * n, l += (t[1] - e[1] / 2) * n, o = -o;
  const c = a * r - l * o, h2 = l * r + a * o;
  return [c, h2];
}
var Dt = Vm;
var pe = {
  OPACITY: "opacity",
  VISIBLE: "visible",
  EXTENT: "extent",
  Z_INDEX: "zIndex",
  MAX_RESOLUTION: "maxResolution",
  MIN_RESOLUTION: "minResolution",
  MAX_ZOOM: "maxZoom",
  MIN_ZOOM: "minZoom",
  SOURCE: "source",
  MAP: "map"
};
var $m = class extends vt {
  /**
   * @param {Options} options Layer options.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, this.background_ = e.background;
    const t = Object.assign({}, e);
    typeof e.properties == "object" && (delete t.properties, Object.assign(t, e.properties)), t[pe.OPACITY] = e.opacity !== void 0 ? e.opacity : 1, _e(
      typeof t[pe.OPACITY] == "number",
      "Layer opacity must be a number"
    ), t[pe.VISIBLE] = e.visible !== void 0 ? e.visible : true, t[pe.Z_INDEX] = e.zIndex, t[pe.MAX_RESOLUTION] = e.maxResolution !== void 0 ? e.maxResolution : 1 / 0, t[pe.MIN_RESOLUTION] = e.minResolution !== void 0 ? e.minResolution : 0, t[pe.MIN_ZOOM] = e.minZoom !== void 0 ? e.minZoom : -1 / 0, t[pe.MAX_ZOOM] = e.maxZoom !== void 0 ? e.maxZoom : 1 / 0, this.className_ = t.className !== void 0 ? t.className : "ol-layer", delete t.className, this.setProperties(t), this.state_ = null;
  }
  /**
   * Get the background for this layer.
   * @return {BackgroundColor|false} Layer background.
   */
  getBackground() {
    return this.background_;
  }
  /**
   * @return {string} CSS class name.
   */
  getClassName() {
    return this.className_;
  }
  /**
   * This method is not meant to be called by layers or layer renderers because the state
   * is incorrect if the layer is included in a layer group.
   *
   * @param {boolean} [managed] Layer is managed.
   * @return {import("./Layer.js").State} Layer state.
   */
  getLayerState(e) {
    const t = this.state_ || /** @type {?} */
    {
      layer: this,
      managed: e === void 0 ? true : e
    }, n = this.getZIndex();
    return t.opacity = xe(Math.round(this.getOpacity() * 100) / 100, 0, 1), t.visible = this.getVisible(), t.extent = this.getExtent(), t.zIndex = n === void 0 && !t.managed ? 1 / 0 : n, t.maxResolution = this.getMaxResolution(), t.minResolution = Math.max(this.getMinResolution(), 0), t.minZoom = this.getMinZoom(), t.maxZoom = this.getMaxZoom(), this.state_ = t, t;
  }
  /**
   * @abstract
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be
   *     modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   */
  getLayersArray(e) {
    return ue();
  }
  /**
   * @abstract
   * @param {Array<import("./Layer.js").State>} [states] Optional list of layer
   *     states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   */
  getLayerStatesArray(e) {
    return ue();
  }
  /**
   * Return the {@link module:ol/extent~Extent extent} of the layer or `undefined` if it
   * will be visible regardless of extent.
   * @return {import("../extent.js").Extent|undefined} The layer extent.
   * @observable
   * @api
   */
  getExtent() {
    return (
      /** @type {import("../extent.js").Extent|undefined} */
      this.get(pe.EXTENT)
    );
  }
  /**
   * Return the maximum resolution of the layer. Returns Infinity if
   * the layer has no maximum resolution set.
   * @return {number} The maximum resolution of the layer.
   * @observable
   * @api
   */
  getMaxResolution() {
    return (
      /** @type {number} */
      this.get(pe.MAX_RESOLUTION)
    );
  }
  /**
   * Return the minimum resolution of the layer. Returns 0 if
   * the layer has no minimum resolution set.
   * @return {number} The minimum resolution of the layer.
   * @observable
   * @api
   */
  getMinResolution() {
    return (
      /** @type {number} */
      this.get(pe.MIN_RESOLUTION)
    );
  }
  /**
   * Return the minimum zoom level of the layer. Returns -Infinity if
   * the layer has no minimum zoom set.
   * @return {number} The minimum zoom level of the layer.
   * @observable
   * @api
   */
  getMinZoom() {
    return (
      /** @type {number} */
      this.get(pe.MIN_ZOOM)
    );
  }
  /**
   * Return the maximum zoom level of the layer. Returns Infinity if
   * the layer has no maximum zoom set.
   * @return {number} The maximum zoom level of the layer.
   * @observable
   * @api
   */
  getMaxZoom() {
    return (
      /** @type {number} */
      this.get(pe.MAX_ZOOM)
    );
  }
  /**
   * Return the opacity of the layer (between 0 and 1).
   * @return {number} The opacity of the layer.
   * @observable
   * @api
   */
  getOpacity() {
    return (
      /** @type {number} */
      this.get(pe.OPACITY)
    );
  }
  /**
   * @abstract
   * @return {import("../source/Source.js").State} Source state.
   */
  getSourceState() {
    return ue();
  }
  /**
   * Return the value of this layer's `visible` property. To find out whether the layer
   * is visible on a map, use `isVisible()` instead.
   * @return {boolean} The value of the `visible` property of the layer.
   * @observable
   * @api
   */
  getVisible() {
    return (
      /** @type {boolean} */
      this.get(pe.VISIBLE)
    );
  }
  /**
   * Return the Z-index of the layer, which is used to order layers before
   * rendering. Returns undefined if the layer is unmanaged.
   * @return {number|undefined} The Z-index of the layer.
   * @observable
   * @api
   */
  getZIndex() {
    return (
      /** @type {number|undefined} */
      this.get(pe.Z_INDEX)
    );
  }
  /**
   * Sets the background color.
   * @param {BackgroundColor} [background] Background color.
   */
  setBackground(e) {
    this.background_ = e, this.changed();
  }
  /**
   * Set the extent at which the layer is visible.  If `undefined`, the layer
   * will be visible at all extents.
   * @param {import("../extent.js").Extent|undefined} extent The extent of the layer.
   * @observable
   * @api
   */
  setExtent(e) {
    this.set(pe.EXTENT, e);
  }
  /**
   * Set the maximum resolution at which the layer is visible.
   * @param {number} maxResolution The maximum resolution of the layer.
   * @observable
   * @api
   */
  setMaxResolution(e) {
    this.set(pe.MAX_RESOLUTION, e);
  }
  /**
   * Set the minimum resolution at which the layer is visible.
   * @param {number} minResolution The minimum resolution of the layer.
   * @observable
   * @api
   */
  setMinResolution(e) {
    this.set(pe.MIN_RESOLUTION, e);
  }
  /**
   * Set the maximum zoom (exclusive) at which the layer is visible.
   * Note that the zoom levels for layer visibility are based on the
   * view zoom level, which may be different from a tile source zoom level.
   * @param {number} maxZoom The maximum zoom of the layer.
   * @observable
   * @api
   */
  setMaxZoom(e) {
    this.set(pe.MAX_ZOOM, e);
  }
  /**
   * Set the minimum zoom (inclusive) at which the layer is visible.
   * Note that the zoom levels for layer visibility are based on the
   * view zoom level, which may be different from a tile source zoom level.
   * @param {number} minZoom The minimum zoom of the layer.
   * @observable
   * @api
   */
  setMinZoom(e) {
    this.set(pe.MIN_ZOOM, e);
  }
  /**
   * Set the opacity of the layer, allowed values range from 0 to 1.
   * @param {number} opacity The opacity of the layer.
   * @observable
   * @api
   */
  setOpacity(e) {
    _e(typeof e == "number", "Layer opacity must be a number"), this.set(pe.OPACITY, e);
  }
  /**
   * Set the visibility of the layer (`true` or `false`).
   * @param {boolean} visible The visibility of the layer.
   * @observable
   * @api
   */
  setVisible(e) {
    this.set(pe.VISIBLE, e);
  }
  /**
   * Set Z-index of the layer, which is used to order layers before rendering.
   * The default Z-index is 0.
   * @param {number} zindex The z-index of the layer.
   * @observable
   * @api
   */
  setZIndex(e) {
    this.set(pe.Z_INDEX, e);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.state_ && (this.state_.layer = null, this.state_ = null), super.disposeInternal();
  }
};
var Ih = $m;
var Km = class extends Ih {
  /**
   * @param {Options<SourceType>} options Layer options.
   */
  constructor(e) {
    const t = Object.assign({}, e);
    delete t.source, super(t), this.on, this.once, this.un, this.mapPrecomposeKey_ = null, this.mapRenderKey_ = null, this.sourceChangeKey_ = null, this.renderer_ = null, this.sourceReady_ = false, this.rendered = false, e.render && (this.render = e.render), e.map && this.setMap(e.map), this.addChangeListener(
      pe.SOURCE,
      this.handleSourcePropertyChange_
    );
    const n = e.source ? (
      /** @type {SourceType} */
      e.source
    ) : null;
    this.setSource(n);
  }
  /**
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   * @override
   */
  getLayersArray(e) {
    return e = e || [], e.push(this), e;
  }
  /**
   * @param {Array<import("./Layer.js").State>} [states] Optional list of layer states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   * @override
   */
  getLayerStatesArray(e) {
    return e = e || [], e.push(this.getLayerState()), e;
  }
  /**
   * Get the layer source.
   * @return {SourceType|null} The layer source (or `null` if not yet set).
   * @observable
   * @api
   */
  getSource() {
    return (
      /** @type {SourceType} */
      this.get(pe.SOURCE) || null
    );
  }
  /**
   * @return {SourceType|null} The source being rendered.
   */
  getRenderSource() {
    return this.getSource();
  }
  /**
   * @return {import("../source/Source.js").State} Source state.
   * @override
   */
  getSourceState() {
    const e = this.getSource();
    return e ? e.getState() : "undefined";
  }
  /**
   * @private
   */
  handleSourceChange_() {
    this.changed(), !(this.sourceReady_ || this.getSource().getState() !== "ready") && (this.sourceReady_ = true, this.dispatchEvent("sourceready"));
  }
  /**
   * @private
   */
  handleSourcePropertyChange_() {
    this.sourceChangeKey_ && (Ee(this.sourceChangeKey_), this.sourceChangeKey_ = null), this.sourceReady_ = false;
    const e = this.getSource();
    e && (this.sourceChangeKey_ = le(
      e,
      Z.CHANGE,
      this.handleSourceChange_,
      this
    ), e.getState() === "ready" && (this.sourceReady_ = true, setTimeout(() => {
      this.dispatchEvent("sourceready");
    }, 0))), this.changed();
  }
  /**
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with
   * an array of features.
   */
  getFeatures(e) {
    return this.renderer_ ? this.renderer_.getFeatures(e) : Promise.resolve([]);
  }
  /**
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   */
  getData(e) {
    return !this.renderer_ || !this.rendered ? null : this.renderer_.getData(e);
  }
  /**
   * The layer is visible on the map view, i.e. within its min/max resolution or zoom and
   * extent, not set to `visible: false`, and not inside a layer group that is set
   * to `visible: false`.
   * @param {View|import("../View.js").ViewStateLayerStateExtent} [view] View or {@link import("../Map.js").FrameState}.
   * Only required when the layer is not added to a map.
   * @return {boolean} The layer is visible in the map view.
   * @api
   */
  isVisible(e) {
    let t;
    const n = this.getMapInternal();
    !e && n && (e = n.getView()), e instanceof Dt ? t = {
      viewState: e.getState(),
      extent: e.calculateExtent()
    } : t = e, !t.layerStatesArray && n && (t.layerStatesArray = n.getLayerGroup().getLayerStatesArray());
    let s;
    if (t.layerStatesArray) {
      if (s = t.layerStatesArray.find(
        (o) => o.layer === this
      ), !s)
        return false;
    } else
      s = this.getLayerState();
    const r = this.getExtent();
    return Pa(s, t.viewState) && (!r || $e(r, t.extent));
  }
  /**
   * Get the attributions of the source of this layer for the given view.
   * @param {View|import("../View.js").ViewStateLayerStateExtent} [view] View or {@link import("../Map.js").FrameState}.
   * Only required when the layer is not added to a map.
   * @return {Array<string>} Attributions for this layer at the given view.
   * @api
   */
  getAttributions(e) {
    var r;
    if (!this.isVisible(e))
      return [];
    const t = (r = this.getSource()) == null ? void 0 : r.getAttributions();
    if (!t)
      return [];
    const n = e instanceof Dt ? e.getViewStateAndExtent() : e;
    let s = t(n);
    return Array.isArray(s) || (s = [s]), s;
  }
  /**
   * In charge to manage the rendering of the layer. One layer type is
   * bounded with one layer renderer.
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target which the renderer may (but need not) use
   * for rendering its content.
   * @return {HTMLElement|null} The rendered element.
   */
  render(e, t) {
    const n = this.getRenderer();
    return n.prepareFrame(e) ? (this.rendered = true, n.renderFrame(e, t)) : null;
  }
  /**
   * Called when a layer is not visible during a map render.
   */
  unrender() {
    this.rendered = false;
  }
  /** @return {string} Declutter */
  getDeclutter() {
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {import("../layer/Layer.js").State} layerState Layer state.
   */
  renderDeclutter(e, t) {
  }
  /**
   * When the renderer follows a layout -> render approach, do the final rendering here.
   * @param {import('../Map.js').FrameState} frameState Frame state
   */
  renderDeferred(e) {
    const t = this.getRenderer();
    t && t.renderDeferred(e);
  }
  /**
   * For use inside the library only.
   * @param {import("../Map.js").default|null} map Map.
   */
  setMapInternal(e) {
    e || this.unrender(), this.set(pe.MAP, e);
  }
  /**
   * For use inside the library only.
   * @return {import("../Map.js").default|null} Map.
   */
  getMapInternal() {
    return this.get(pe.MAP);
  }
  /**
   * Sets the layer to be rendered on top of other layers on a map. The map will
   * not manage this layer in its layers collection. This
   * is useful for temporary layers. To remove an unmanaged layer from the map,
   * use `#setMap(null)`.
   *
   * To add the layer to a map and have it managed by the map, use
   * {@link module:ol/Map~Map#addLayer} instead.
   * @param {import("../Map.js").default|null} map Map.
   * @api
   */
  setMap(e) {
    this.mapPrecomposeKey_ && (Ee(this.mapPrecomposeKey_), this.mapPrecomposeKey_ = null), e || this.changed(), this.mapRenderKey_ && (Ee(this.mapRenderKey_), this.mapRenderKey_ = null), e && (this.mapPrecomposeKey_ = le(
      e,
      mt.PRECOMPOSE,
      this.handlePrecompose_,
      this
    ), this.mapRenderKey_ = le(this, Z.CHANGE, e.render, e), this.changed());
  }
  /**
   * @param {import("../events/Event.js").default} renderEvent Render event
   * @private
   */
  handlePrecompose_(e) {
    const t = (
      /** @type {import("../render/Event.js").default} */
      e.frameState.layerStatesArray
    ), n = this.getLayerState(false);
    _e(
      !t.some(
        (s) => s.layer === n.layer
      ),
      "A layer can only be added to the map once. Use either `layer.setMap()` or `map.addLayer()`, not both."
    ), t.push(n);
  }
  /**
   * Set the layer source.
   * @param {SourceType|null} source The layer source.
   * @observable
   * @api
   */
  setSource(e) {
    this.set(pe.SOURCE, e);
  }
  /**
   * Get the renderer for this layer.
   * @return {RendererType|null} The layer renderer.
   */
  getRenderer() {
    return this.renderer_ || (this.renderer_ = this.createRenderer()), this.renderer_;
  }
  /**
   * @return {boolean} The layer has a renderer.
   */
  hasRenderer() {
    return !!this.renderer_;
  }
  /**
   * Create a renderer for this layer.
   * @return {RendererType} A layer renderer.
   * @protected
   */
  createRenderer() {
    return null;
  }
  /**
   * This will clear the renderer so that a new one can be created next time it is needed
   */
  clearRenderer() {
    this.renderer_ && (this.renderer_.dispose(), delete this.renderer_);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.clearRenderer(), this.setSource(null), super.disposeInternal();
  }
};
function Pa(i, e) {
  if (!i.visible)
    return false;
  const t = e.resolution;
  if (t < i.minResolution || t >= i.maxResolution)
    return false;
  const n = e.zoom;
  return n > i.minZoom && n <= i.maxZoom;
}
var br = Km;
var Ul = {
  RENDER_ORDER: "renderOrder"
};
var Hm = class extends br {
  /**
   * @param {Options<FeatureType, VectorSourceType>} [options] Options.
   */
  constructor(e) {
    e = e || {};
    const t = Object.assign({}, e);
    delete t.style, delete t.renderBuffer, delete t.updateWhileAnimating, delete t.updateWhileInteracting, super(t), this.declutter_ = e.declutter ? String(e.declutter) : void 0, this.renderBuffer_ = e.renderBuffer !== void 0 ? e.renderBuffer : 100, this.style_ = null, this.styleFunction_ = void 0, this.setStyle(e.style), this.updateWhileAnimating_ = e.updateWhileAnimating !== void 0 ? e.updateWhileAnimating : false, this.updateWhileInteracting_ = e.updateWhileInteracting !== void 0 ? e.updateWhileInteracting : false;
  }
  /**
   * @return {string} Declutter group.
   * @override
   */
  getDeclutter() {
    return this.declutter_;
  }
  /**
   * Get the topmost feature that intersects the given pixel on the viewport. Returns a promise
   * that resolves with an array of features. The array will either contain the topmost feature
   * when a hit was detected, or it will be empty.
   *
   * The hit detection algorithm used for this method is optimized for performance, but is less
   * accurate than the one used in [map.getFeaturesAtPixel()]{@link import("../Map.js").default#getFeaturesAtPixel}.
   * Text is not considered, and icons are only represented by their bounding box instead of the exact
   * image.
   *
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @return {Promise<Array<import("../Feature").FeatureLike>>} Promise that resolves with an array of features.
   * @api
   * @override
   */
  getFeatures(e) {
    return super.getFeatures(e);
  }
  /**
   * @return {number|undefined} Render buffer.
   */
  getRenderBuffer() {
    return this.renderBuffer_;
  }
  /**
   * @return {import("../render.js").OrderFunction|null|undefined} Render order.
   */
  getRenderOrder() {
    return (
      /** @type {import("../render.js").OrderFunction|null|undefined} */
      this.get(Ul.RENDER_ORDER)
    );
  }
  /**
   * Get the style for features.  This returns whatever was passed to the `style`
   * option at construction or to the `setStyle` method.
   * @return {import("../style/Style.js").StyleLike|import("../style/flat.js").FlatStyleLike|null|undefined} Layer style.
   * @api
   */
  getStyle() {
    return this.style_;
  }
  /**
   * Get the style function.
   * @return {import("../style/Style.js").StyleFunction|undefined} Layer style function.
   * @api
   */
  getStyleFunction() {
    return this.styleFunction_;
  }
  /**
   * @return {boolean} Whether the rendered layer should be updated while
   *     animating.
   */
  getUpdateWhileAnimating() {
    return this.updateWhileAnimating_;
  }
  /**
   * @return {boolean} Whether the rendered layer should be updated while
   *     interacting.
   */
  getUpdateWhileInteracting() {
    return this.updateWhileInteracting_;
  }
  /**
   * Render declutter items for this layer
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {import("../layer/Layer.js").State} layerState Layer state.
   * @override
   */
  renderDeclutter(e, t) {
    const n = this.getDeclutter();
    n in e.declutter || (e.declutter[n] = new mh(9)), this.getRenderer().renderDeclutter(e, t);
  }
  /**
   * @param {import("../render.js").OrderFunction|null|undefined} renderOrder
   *     Render order.
   */
  setRenderOrder(e) {
    this.set(Ul.RENDER_ORDER, e);
  }
  /**
   * Set the style for features.  This can be a single style object, an array
   * of styles, or a function that takes a feature and resolution and returns
   * an array of styles. If set to `null`, the layer has no style (a `null` style),
   * so only features that have their own styles will be rendered in the layer. Call
   * `setStyle()` without arguments to reset to the default style. See
   * [the ol/style/Style module]{@link module:ol/style/Style~Style} for information on the default style.
   *
   * If your layer has a static style, you can use [flat style]{@link module:ol/style/flat~FlatStyle} object
   * literals instead of using the `Style` and symbolizer constructors (`Fill`, `Stroke`, etc.):
   * ```js
   * vectorLayer.setStyle({
   *   "fill-color": "yellow",
   *   "stroke-color": "black",
   *   "stroke-width": 4
   * })
   * ```
   *
   * @param {import("../style/Style.js").StyleLike|import("../style/flat.js").FlatStyleLike|null} [style] Layer style.
   * @api
   */
  setStyle(e) {
    this.style_ = e === void 0 ? Kc : e;
    const t = Zm(e);
    this.styleFunction_ = e === null ? void 0 : Of(t), this.changed();
  }
  /**
   * @param {boolean|string|number} declutter Declutter images and text.
   * @api
   */
  setDeclutter(e) {
    this.declutter_ = e ? String(e) : void 0, this.changed();
  }
};
function Zm(i) {
  if (i === void 0)
    return Kc;
  if (!i)
    return null;
  if (typeof i == "function" || i instanceof te)
    return i;
  if (!Array.isArray(i))
    return Fl([i]);
  if (i.length === 0)
    return [];
  const e = i.length, t = i[0];
  if (t instanceof te) {
    const s = new Array(e);
    for (let r = 0; r < e; ++r) {
      const o = i[r];
      if (!(o instanceof te))
        throw new Error("Expected a list of style instances");
      s[r] = o;
    }
    return s;
  }
  if ("style" in t) {
    const s = new Array(e);
    for (let r = 0; r < e; ++r) {
      const o = i[r];
      if (!("style" in o))
        throw new Error("Expected a list of rules with a style property");
      s[r] = o;
    }
    return wm(s);
  }
  return Fl(
    /** @type {Array<import("../style/flat.js").FlatStyle>} */
    i
  );
}
var vh = Hm;
var qm = class extends vh {
  /**
   * @param {Options<VectorSourceType, FeatureType>} [options] Options.
   */
  constructor(e) {
    super(e);
  }
  /**
   * @override
   */
  createRenderer() {
    return new Xg(this);
  }
};
var Rt = qm;
var Je = {
  /**
   * Triggered when an item is added to the collection.
   * @event module:ol/Collection.CollectionEvent#add
   * @api
   */
  ADD: "add",
  /**
   * Triggered when an item is removed from the collection.
   * @event module:ol/Collection.CollectionEvent#remove
   * @api
   */
  REMOVE: "remove"
};
var Gl = {
  LENGTH: "length"
};
var Rs = class extends It {
  /**
   * @param {import("./CollectionEventType.js").default} type Type.
   * @param {T} element Element.
   * @param {number} index The index of the added or removed element.
   */
  constructor(e, t, n) {
    super(e), this.element = t, this.index = n;
  }
};
var Jm = class extends vt {
  /**
   * @param {Array<T>} [array] Array.
   * @param {Options} [options] Collection options.
   */
  constructor(e, t) {
    if (super(), this.on, this.once, this.un, t = t || {}, this.unique_ = !!t.unique, this.array_ = e ?? [], this.unique_)
      for (let n = 1, s = this.array_.length; n < s; ++n)
        this.assertUnique_(this.array_[n], n);
    this.updateLength_();
  }
  /**
   * Remove all elements from the collection.
   * @api
   */
  clear() {
    for (; this.getLength() > 0; )
      this.pop();
  }
  /**
   * Add elements to the collection.  This pushes each item in the provided array
   * to the end of the collection.
   * @param {!Array<T>} arr Array.
   * @return {Collection<T>} This collection.
   * @api
   */
  extend(e) {
    for (let t = 0, n = e.length; t < n; ++t)
      this.push(e[t]);
    return this;
  }
  /**
   * Iterate over each element, calling the provided callback.
   * @param {function(T, number, Array<T>): *} f The function to call
   *     for every element. This function takes 3 arguments (the element, the
   *     index and the array). The return value is ignored.
   * @api
   */
  forEach(e) {
    const t = this.array_;
    for (let n = 0, s = t.length; n < s; ++n)
      e(t[n], n, t);
  }
  /**
   * Get a reference to the underlying Array object. Warning: if the array
   * is mutated, no events will be dispatched by the collection, and the
   * collection's "length" property won't be in sync with the actual length
   * of the array.
   * @return {!Array<T>} Array.
   * @api
   */
  getArray() {
    return this.array_;
  }
  /**
   * Get the element at the provided index.
   * @param {number} index Index.
   * @return {T} Element.
   * @api
   */
  item(e) {
    return this.array_[e];
  }
  /**
   * Get the length of this collection.
   * @return {number} The length of the array.
   * @observable
   * @api
   */
  getLength() {
    return this.get(Gl.LENGTH);
  }
  /**
   * Insert an element at the provided index.
   * @param {number} index Index.
   * @param {T} elem Element.
   * @api
   */
  insertAt(e, t) {
    if (e < 0 || e > this.getLength())
      throw new Error("Index out of bounds: " + e);
    this.unique_ && this.assertUnique_(t), this.array_.splice(e, 0, t), this.updateLength_(), this.dispatchEvent(
      new Rs(Je.ADD, t, e)
    );
  }
  /**
   * Remove the last element of the collection and return it.
   * Return `undefined` if the collection is empty.
   * @return {T|undefined} Element.
   * @api
   */
  pop() {
    return this.removeAt(this.getLength() - 1);
  }
  /**
   * Insert the provided element at the end of the collection.
   * @param {T} elem Element.
   * @return {number} New length of the collection.
   * @api
   */
  push(e) {
    const t = this.getLength();
    return this.insertAt(t, e), this.getLength();
  }
  /**
   * Remove the first occurrence of an element from the collection.
   * @param {T} elem Element.
   * @return {T|undefined} The removed element or undefined if none found.
   * @api
   */
  remove(e) {
    const t = this.array_;
    for (let n = 0, s = t.length; n < s; ++n)
      if (t[n] === e)
        return this.removeAt(n);
  }
  /**
   * Remove the element at the provided index and return it.
   * Return `undefined` if the collection does not contain this index.
   * @param {number} index Index.
   * @return {T|undefined} Value.
   * @api
   */
  removeAt(e) {
    if (e < 0 || e >= this.getLength())
      return;
    const t = this.array_[e];
    return this.array_.splice(e, 1), this.updateLength_(), this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new Rs(Je.REMOVE, t, e)
    ), t;
  }
  /**
   * Set the element at the provided index.
   * @param {number} index Index.
   * @param {T} elem Element.
   * @api
   */
  setAt(e, t) {
    const n = this.getLength();
    if (e >= n) {
      this.insertAt(e, t);
      return;
    }
    if (e < 0)
      throw new Error("Index out of bounds: " + e);
    this.unique_ && this.assertUnique_(t, e);
    const s = this.array_[e];
    this.array_[e] = t, this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new Rs(Je.REMOVE, s, e)
    ), this.dispatchEvent(
      /** @type {CollectionEvent<T>} */
      new Rs(Je.ADD, t, e)
    );
  }
  /**
   * @private
   */
  updateLength_() {
    this.set(Gl.LENGTH, this.array_.length);
  }
  /**
   * @private
   * @param {T} elem Element.
   * @param {number} [except] Optional index to ignore.
   */
  assertUnique_(e, t) {
    const n = this.array_;
    for (let s = 0, r = n.length; s < r; ++s)
      if (n[s] === e && s !== t)
        throw new Error("Duplicate item added to a unique collection");
  }
};
var Ke = Jm;
var Qm = false;
function ey(i, e, t, n, s, r, o) {
  const a = new XMLHttpRequest();
  a.open(
    "GET",
    typeof i == "function" ? i(t, n, s) : i,
    true
  ), e.getType() == "arraybuffer" && (a.responseType = "arraybuffer"), a.withCredentials = Qm, a.onload = function(l) {
    if (!a.status || a.status >= 200 && a.status < 300) {
      const c = e.getType();
      try {
        let h2;
        c == "text" || c == "json" ? h2 = a.responseText : c == "xml" ? h2 = a.responseXML || a.responseText : c == "arraybuffer" && (h2 = /** @type {ArrayBuffer} */
        a.response), h2 ? r(
          /** @type {Array<FeatureType>} */
          e.readFeatures(h2, {
            extent: t,
            featureProjection: s
          }),
          e.readProjection(h2)
        ) : o();
      } catch {
        o();
      }
    } else
      o();
  }, a.onerror = o, a.send();
}
function Bl(i, e) {
  return function(t, n, s, r, o) {
    ey(
      i,
      e,
      t,
      n,
      s,
      /**
       * @param {Array<FeatureType>} features The loaded features.
       * @param {import("./proj/Projection.js").default} dataProjection Data
       * projection.
       */
      (a, l) => {
        this.addFeatures(a), r !== void 0 && r(a);
      },
      () => {
        this.changed(), o !== void 0 && o();
      }
    );
  };
}
function ty(i, e) {
  return [[-1 / 0, -1 / 0, 1 / 0, 1 / 0]];
}
var ar = class _ar extends Fc {
  /**
   * @param {Array<Geometry>} geometries Geometries.
   */
  constructor(e) {
    super(), this.geometries_ = e, this.changeEventsKeys_ = [], this.listenGeometriesChange_();
  }
  /**
   * @private
   */
  unlistenGeometriesChange_() {
    this.changeEventsKeys_.forEach(Ee), this.changeEventsKeys_.length = 0;
  }
  /**
   * @private
   */
  listenGeometriesChange_() {
    const e = this.geometries_;
    for (let t = 0, n = e.length; t < n; ++t)
      this.changeEventsKeys_.push(
        le(e[t], Z.CHANGE, this.changed, this)
      );
  }
  /**
   * Make a complete copy of the geometry.
   * @return {!GeometryCollection} Clone.
   * @api
   * @override
   */
  clone() {
    const e = new _ar(
      Qr(this.geometries_)
    );
    return e.applyProperties(this), e;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @param {import("../coordinate.js").Coordinate} closestPoint Closest point.
   * @param {number} minSquaredDistance Minimum squared distance.
   * @return {number} Minimum squared distance.
   * @override
   */
  closestPointXY(e, t, n, s) {
    if (s < Bn(this.getExtent(), e, t))
      return s;
    const r = this.geometries_;
    for (let o = 0, a = r.length; o < a; ++o)
      s = r[o].closestPointXY(
        e,
        t,
        n,
        s
      );
    return s;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   * @return {boolean} Contains (x, y).
   * @override
   */
  containsXY(e, t) {
    const n = this.geometries_;
    for (let s = 0, r = n.length; s < r; ++s)
      if (n[s].containsXY(e, t))
        return true;
    return false;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @protected
   * @return {import("../extent.js").Extent} extent Extent.
   * @override
   */
  computeExtent(e) {
    Mi(e);
    const t = this.geometries_;
    for (let n = 0, s = t.length; n < s; ++n)
      gc(e, t[n].getExtent());
    return e;
  }
  /**
   * Return the geometries that make up this geometry collection.
   * @return {Array<Geometry>} Geometries.
   * @api
   */
  getGeometries() {
    return Qr(this.geometries_);
  }
  /**
   * @return {Array<Geometry>} Geometries.
   */
  getGeometriesArray() {
    return this.geometries_;
  }
  /**
   * @return {Array<Geometry>} Geometries.
   */
  getGeometriesArrayRecursive() {
    let e = [];
    const t = this.geometries_;
    for (let n = 0, s = t.length; n < s; ++n)
      t[n].getType() === this.getType() ? e = e.concat(
        /** @type {GeometryCollection} */
        t[n].getGeometriesArrayRecursive()
      ) : e.push(t[n]);
    return e;
  }
  /**
   * Create a simplified version of this geometry using the Douglas Peucker algorithm.
   * @param {number} squaredTolerance Squared tolerance.
   * @return {GeometryCollection} Simplified GeometryCollection.
   * @override
   */
  getSimplifiedGeometry(e) {
    if (this.simplifiedGeometryRevision !== this.getRevision() && (this.simplifiedGeometryMaxMinSquaredTolerance = 0, this.simplifiedGeometryRevision = this.getRevision()), e < 0 || this.simplifiedGeometryMaxMinSquaredTolerance !== 0 && e < this.simplifiedGeometryMaxMinSquaredTolerance)
      return this;
    const t = [], n = this.geometries_;
    let s = false;
    for (let r = 0, o = n.length; r < o; ++r) {
      const a = n[r], l = a.getSimplifiedGeometry(e);
      t.push(l), l !== a && (s = true);
    }
    return s ? new _ar(
      t
    ) : (this.simplifiedGeometryMaxMinSquaredTolerance = e, this);
  }
  /**
   * Get the type of this geometry.
   * @return {import("./Geometry.js").Type} Geometry type.
   * @api
   * @override
   */
  getType() {
    return "GeometryCollection";
  }
  /**
   * Test if the geometry and the passed extent intersect.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {boolean} `true` if the geometry and the extent intersect.
   * @api
   * @override
   */
  intersectsExtent(e) {
    const t = this.geometries_;
    for (let n = 0, s = t.length; n < s; ++n)
      if (t[n].intersectsExtent(e))
        return true;
    return false;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.geometries_.length === 0;
  }
  /**
   * Rotate the geometry around a given coordinate. This modifies the geometry
   * coordinates in place.
   * @param {number} angle Rotation angle in radians.
   * @param {import("../coordinate.js").Coordinate} anchor The rotation center.
   * @api
   * @override
   */
  rotate(e, t) {
    const n = this.geometries_;
    for (let s = 0, r = n.length; s < r; ++s)
      n[s].rotate(e, t);
    this.changed();
  }
  /**
   * Scale the geometry (with an optional origin).  This modifies the geometry
   * coordinates in place.
   * @abstract
   * @param {number} sx The scaling factor in the x-direction.
   * @param {number} [sy] The scaling factor in the y-direction (defaults to sx).
   * @param {import("../coordinate.js").Coordinate} [anchor] The scale origin (defaults to the center
   *     of the geometry extent).
   * @api
   * @override
   */
  scale(e, t, n) {
    n || (n = pn(this.getExtent()));
    const s = this.geometries_;
    for (let r = 0, o = s.length; r < o; ++r)
      s[r].scale(e, t, n);
    this.changed();
  }
  /**
   * Set the geometries that make up this geometry collection.
   * @param {Array<Geometry>} geometries Geometries.
   * @api
   */
  setGeometries(e) {
    this.setGeometriesArray(Qr(e));
  }
  /**
   * @param {Array<Geometry>} geometries Geometries.
   */
  setGeometriesArray(e) {
    this.unlistenGeometriesChange_(), this.geometries_ = e, this.listenGeometriesChange_(), this.changed();
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   * Called with a flat array of geometry coordinates.
   * @api
   * @override
   */
  applyTransform(e) {
    const t = this.geometries_;
    for (let n = 0, s = t.length; n < s; ++n)
      t[n].applyTransform(e);
    this.changed();
  }
  /**
   * Translate the geometry.  This modifies the geometry coordinates in place.  If
   * instead you want a new geometry, first `clone()` this geometry.
   * @param {number} deltaX Delta X.
   * @param {number} deltaY Delta Y.
   * @api
   * @override
   */
  translate(e, t) {
    const n = this.geometries_;
    for (let s = 0, r = n.length; s < r; ++s)
      n[s].translate(e, t);
    this.changed();
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.unlistenGeometriesChange_(), super.disposeInternal();
  }
};
function Qr(i) {
  return i.map((e) => e.clone());
}
var ny = ar;
var zl = Ct();
var Ii = class _Ii {
  /**
   * @param {Type} type Geometry type.
   * @param {Array<number>} flatCoordinates Flat coordinates. These always need
   *     to be right-handed for polygons.
   * @param {Array<number>} ends Ends.
   * @param {number} stride Stride.
   * @param {Object<string, *>} properties Properties.
   * @param {number|string|undefined} id Feature id.
   */
  constructor(e, t, n, s, r, o) {
    this.styleFunction, this.extent_, this.id_ = o, this.type_ = e, this.flatCoordinates_ = t, this.flatInteriorPoints_ = null, this.flatMidpoints_ = null, this.ends_ = n || null, this.properties_ = r, this.squaredTolerance_, this.stride_ = s, this.simplifiedGeometry_;
  }
  /**
   * Get a feature property by its key.
   * @param {string} key Key
   * @return {*} Value for the requested key.
   * @api
   */
  get(e) {
    return this.properties_[e];
  }
  /**
   * Get the extent of this feature's geometry.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_ || (this.extent_ = this.type_ === "Point" ? fc(this.flatCoordinates_) : Xo(
      this.flatCoordinates_,
      0,
      this.flatCoordinates_.length,
      2
    )), this.extent_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoint() {
    if (!this.flatInteriorPoints_) {
      const e = pn(this.getExtent());
      this.flatInteriorPoints_ = va(
        this.flatCoordinates_,
        0,
        this.ends_,
        2,
        e,
        0
      );
    }
    return this.flatInteriorPoints_;
  }
  /**
   * @return {Array<number>} Flat interior points.
   */
  getFlatInteriorPoints() {
    if (!this.flatInteriorPoints_) {
      const e = Zf(this.flatCoordinates_, this.ends_), t = sh(this.flatCoordinates_, 0, e, 2);
      this.flatInteriorPoints_ = th(
        this.flatCoordinates_,
        0,
        e,
        2,
        t
      );
    }
    return this.flatInteriorPoints_;
  }
  /**
   * @return {Array<number>} Flat midpoint.
   */
  getFlatMidpoint() {
    return this.flatMidpoints_ || (this.flatMidpoints_ = Zs(
      this.flatCoordinates_,
      0,
      this.flatCoordinates_.length,
      2,
      0.5
    )), this.flatMidpoints_;
  }
  /**
   * @return {Array<number>} Flat midpoints.
   */
  getFlatMidpoints() {
    if (!this.flatMidpoints_) {
      this.flatMidpoints_ = [];
      const e = this.flatCoordinates_;
      let t = 0;
      const n = (
        /** @type {Array<number>} */
        this.ends_
      );
      for (let s = 0, r = n.length; s < r; ++s) {
        const o = n[s], a = Zs(e, t, o, 2, 0.5);
        At(this.flatMidpoints_, a), t = o;
      }
    }
    return this.flatMidpoints_;
  }
  /**
   * Get the feature identifier.  This is a stable identifier for the feature and
   * is set when reading data from a remote source.
   * @return {number|string|undefined} Id.
   * @api
   */
  getId() {
    return this.id_;
  }
  /**
   * @return {Array<number>} Flat coordinates.
   */
  getOrientedFlatCoordinates() {
    return this.flatCoordinates_;
  }
  /**
   * For API compatibility with {@link module:ol/Feature~Feature}, this method is useful when
   * determining the geometry type in style function (see {@link #getType}).
   * @return {RenderFeature} Feature.
   * @api
   */
  getGeometry() {
    return this;
  }
  /**
   * @param {number} squaredTolerance Squared tolerance.
   * @return {RenderFeature} Simplified geometry.
   */
  getSimplifiedGeometry(e) {
    return this;
  }
  /**
   * Get a transformed and simplified version of the geometry.
   * @param {number} squaredTolerance Squared tolerance.
   * @param {import("../proj.js").TransformFunction} [transform] Optional transform function.
   * @return {RenderFeature} Simplified geometry.
   */
  simplifyTransformed(e, t) {
    return this;
  }
  /**
   * Get the feature properties.
   * @return {Object<string, *>} Feature properties.
   * @api
   */
  getProperties() {
    return this.properties_;
  }
  /**
   * Get an object of all property names and values.  This has the same behavior as getProperties,
   * but is here to conform with the {@link module:ol/Feature~Feature} interface.
   * @return {Object<string, *>?} Object.
   */
  getPropertiesInternal() {
    return this.properties_;
  }
  /**
   * @return {number} Stride.
   */
  getStride() {
    return this.stride_;
  }
  /**
   * @return {import('../style/Style.js').StyleFunction|undefined} Style
   */
  getStyleFunction() {
    return this.styleFunction;
  }
  /**
   * Get the type of this feature's geometry.
   * @return {Type} Geometry type.
   * @api
   */
  getType() {
    return this.type_;
  }
  /**
   * Transform geometry coordinates from tile pixel space to projected.
   *
   * @param {import("../proj.js").ProjectionLike} projection The data projection
   */
  transform(e) {
    e = nt(e);
    const t = e.getExtent(), n = e.getWorldExtent();
    if (t && n) {
      const s = Ve(n) / Ve(t);
      Zt(
        zl,
        n[0],
        n[3],
        s,
        -s,
        0,
        0,
        0
      ), mn(
        this.flatCoordinates_,
        0,
        this.flatCoordinates_.length,
        2,
        zl,
        this.flatCoordinates_
      );
    }
  }
  /**
   * Apply a transform function to the coordinates of the geometry.
   * The geometry is modified in place.
   * If you do not want the geometry modified in place, first `clone()` it and
   * then use this function on the clone.
   * @param {import("../proj.js").TransformFunction} transformFn Transform function.
   */
  applyTransform(e) {
    e(this.flatCoordinates_, this.flatCoordinates_, this.stride_);
  }
  /**
   * @return {RenderFeature} A cloned render feature.
   */
  clone() {
    var e;
    return new _Ii(
      this.type_,
      this.flatCoordinates_.slice(),
      (e = this.ends_) == null ? void 0 : e.slice(),
      this.stride_,
      Object.assign({}, this.properties_),
      this.id_
    );
  }
  /**
   * @return {Array<number>|null} Ends.
   */
  getEnds() {
    return this.ends_;
  }
  /**
   * Add transform and resolution based geometry simplification to this instance.
   * @return {RenderFeature} This render feature.
   */
  enableSimplifyTransformed() {
    return this.simplifyTransformed = hc((e, t) => {
      if (e === this.squaredTolerance_)
        return this.simplifiedGeometry_;
      this.simplifiedGeometry_ = this.clone(), t && this.simplifiedGeometry_.applyTransform(t);
      const n = this.simplifiedGeometry_.getFlatCoordinates();
      let s;
      switch (this.type_) {
        case "LineString":
          n.length = Cr(
            n,
            0,
            this.simplifiedGeometry_.flatCoordinates_.length,
            this.simplifiedGeometry_.stride_,
            e,
            n,
            0
          ), s = [n.length];
          break;
        case "MultiLineString":
          s = [], n.length = Oc(
            n,
            0,
            this.simplifiedGeometry_.ends_,
            this.simplifiedGeometry_.stride_,
            e,
            n,
            0,
            s
          );
          break;
        case "Polygon":
          s = [], n.length = la(
            n,
            0,
            this.simplifiedGeometry_.ends_,
            this.simplifiedGeometry_.stride_,
            Math.sqrt(e),
            n,
            0,
            s
          );
          break;
      }
      return s && (this.simplifiedGeometry_ = new _Ii(
        this.type_,
        n,
        s,
        2,
        this.properties_,
        this.id_
      )), this.squaredTolerance_ = e, this.simplifiedGeometry_;
    }), this;
  }
};
Ii.prototype.getFlatCoordinates = Ii.prototype.getOrientedFlatCoordinates;
var Hn = Ii;
var iy = class {
  /**
   * @param {number} [maxEntries] Max entries.
   */
  constructor(e) {
    this.rbush_ = new mh(e), this.items_ = {};
  }
  /**
   * Insert a value into the RBush.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {T} value Value.
   */
  insert(e, t) {
    const n = {
      minX: e[0],
      minY: e[1],
      maxX: e[2],
      maxY: e[3],
      value: t
    };
    this.rbush_.insert(n), this.items_[de(t)] = n;
  }
  /**
   * Bulk-insert values into the RBush.
   * @param {Array<import("../extent.js").Extent>} extents Extents.
   * @param {Array<T>} values Values.
   */
  load(e, t) {
    const n = new Array(t.length);
    for (let s = 0, r = t.length; s < r; s++) {
      const o = e[s], a = t[s], l = {
        minX: o[0],
        minY: o[1],
        maxX: o[2],
        maxY: o[3],
        value: a
      };
      n[s] = l, this.items_[de(a)] = l;
    }
    this.rbush_.load(n);
  }
  /**
   * Remove a value from the RBush.
   * @param {T} value Value.
   * @return {boolean} Removed.
   */
  remove(e) {
    const t = de(e), n = this.items_[t];
    return delete this.items_[t], this.rbush_.remove(n) !== null;
  }
  /**
   * Update the extent of a value in the RBush.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {T} value Value.
   */
  update(e, t) {
    const n = this.items_[de(t)], s = [n.minX, n.minY, n.maxX, n.maxY];
    Hi(s, e) || (this.remove(t), this.insert(e, t));
  }
  /**
   * Return all values in the RBush.
   * @return {Array<T>} All.
   */
  getAll() {
    return this.rbush_.all().map(function(t) {
      return t.value;
    });
  }
  /**
   * Return all values in the given extent.
   * @param {import("../extent.js").Extent} extent Extent.
   * @return {Array<T>} All in extent.
   */
  getInExtent(e) {
    const t = {
      minX: e[0],
      minY: e[1],
      maxX: e[2],
      maxY: e[3]
    };
    return this.rbush_.search(t).map(function(s) {
      return s.value;
    });
  }
  /**
   * Calls a callback function with each value in the tree.
   * If the callback returns a truthy value, this value is returned without
   * checking the rest of the tree.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   */
  forEach(e) {
    return this.forEach_(this.getAll(), e);
  }
  /**
   * Calls a callback function with each value in the provided extent.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   */
  forEachInExtent(e, t) {
    return this.forEach_(this.getInExtent(e), t);
  }
  /**
   * @param {Array<T>} values Values.
   * @param {function(T): R} callback Callback.
   * @return {R|undefined} Callback return value.
   * @template R
   * @private
   */
  forEach_(e, t) {
    let n;
    for (let s = 0, r = e.length; s < r; s++)
      if (n = t(e[s]), n)
        return n;
    return n;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return yi(this.items_);
  }
  /**
   * Remove all values from the RBush.
   */
  clear() {
    this.rbush_.clear(), this.items_ = {};
  }
  /**
   * @param {import("../extent.js").Extent} [extent] Extent.
   * @return {import("../extent.js").Extent} Extent.
   */
  getExtent(e) {
    const t = this.rbush_.toJSON();
    return kt(t.minX, t.minY, t.maxX, t.maxY, e);
  }
  /**
   * @param {RBush<T>} rbush R-Tree.
   */
  concat(e) {
    this.rbush_.load(e.rbush_.all());
    for (const t in e.items_)
      this.items_[t] = e.items_[t];
  }
};
var Vl = iy;
var sy = class extends vt {
  /**
   * @param {Options} options Source options.
   */
  constructor(e) {
    super(), this.projection = nt(e.projection), this.attributions_ = Xl(e.attributions), this.attributionsCollapsible_ = e.attributionsCollapsible ?? true, this.loading = false, this.state_ = e.state !== void 0 ? e.state : "ready", this.wrapX_ = e.wrapX !== void 0 ? e.wrapX : false, this.interpolate_ = !!e.interpolate, this.viewResolver = null, this.viewRejector = null;
    const t = this;
    this.viewPromise_ = new Promise(function(n, s) {
      t.viewResolver = n, t.viewRejector = s;
    });
  }
  /**
   * Get the attribution function for the source.
   * @return {?Attribution} Attribution function.
   * @api
   */
  getAttributions() {
    return this.attributions_;
  }
  /**
   * @return {boolean} Attributions are collapsible.
   * @api
   */
  getAttributionsCollapsible() {
    return this.attributionsCollapsible_;
  }
  /**
   * Get the projection of the source.
   * @return {import("../proj/Projection.js").default|null} Projection.
   * @api
   */
  getProjection() {
    return this.projection;
  }
  /**
   * @param {import("../proj/Projection").default} [projection] Projection.
   * @return {Array<number>|null} Resolutions.
   */
  getResolutions(e) {
    return null;
  }
  /**
   * @return {Promise<import("../View.js").ViewOptions>} A promise for view-related properties.
   */
  getView() {
    return this.viewPromise_;
  }
  /**
   * Get the state of the source, see {@link import("./Source.js").State} for possible states.
   * @return {import("./Source.js").State} State.
   * @api
   */
  getState() {
    return this.state_;
  }
  /**
   * @return {boolean|undefined} Wrap X.
   */
  getWrapX() {
    return this.wrapX_;
  }
  /**
   * @return {boolean} Use linear interpolation when resampling.
   */
  getInterpolate() {
    return this.interpolate_;
  }
  /**
   * Refreshes the source. The source will be cleared, and data from the server will be reloaded.
   * @api
   */
  refresh() {
    this.changed();
  }
  /**
   * Set the attributions of the source.
   * @param {AttributionLike|undefined} attributions Attributions.
   *     Can be passed as `string`, `Array<string>`, {@link module:ol/source/Source~Attribution},
   *     or `undefined`.
   * @api
   */
  setAttributions(e) {
    this.attributions_ = Xl(e), this.changed();
  }
  /**
   * Set the state of the source.
   * @param {import("./Source.js").State} state State.
   */
  setState(e) {
    this.state_ = e, this.changed();
  }
};
function Xl(i) {
  return i ? typeof i == "function" ? i : (Array.isArray(i) || (i = [i]), (e) => i) : null;
}
var Mh = sy;
var dt = {
  /**
   * Triggered when a feature is added to the source.
   * @event module:ol/source/Vector.VectorSourceEvent#addfeature
   * @api
   */
  ADDFEATURE: "addfeature",
  /**
   * Triggered when a feature is updated.
   * @event module:ol/source/Vector.VectorSourceEvent#changefeature
   * @api
   */
  CHANGEFEATURE: "changefeature",
  /**
   * Triggered when the clear method is called on the source.
   * @event module:ol/source/Vector.VectorSourceEvent#clear
   * @api
   */
  CLEAR: "clear",
  /**
   * Triggered when a feature is removed from the source.
   * See {@link module:ol/source/Vector~VectorSource#clear source.clear()} for exceptions.
   * @event module:ol/source/Vector.VectorSourceEvent#removefeature
   * @api
   */
  REMOVEFEATURE: "removefeature",
  /**
   * Triggered when features starts loading.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloadstart
   * @api
   */
  FEATURESLOADSTART: "featuresloadstart",
  /**
   * Triggered when features finishes loading.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloadend
   * @api
   */
  FEATURESLOADEND: "featuresloadend",
  /**
   * Triggered if feature loading results in an error.
   * @event module:ol/source/Vector.VectorSourceEvent#featuresloaderror
   * @api
   */
  FEATURESLOADERROR: "featuresloaderror"
};
var rn = class extends It {
  /**
   * @param {string} type Type.
   * @param {FeatureType} [feature] Feature.
   * @param {Array<FeatureType>} [features] Features.
   */
  constructor(e, t, n) {
    super(e), this.feature = t, this.features = n;
  }
};
var ry = class extends Mh {
  /**
   * @param {Options<FeatureType>} [options] Vector source options.
   */
  constructor(e) {
    e = e || {}, super({
      attributions: e.attributions,
      interpolate: true,
      projection: void 0,
      state: "ready",
      wrapX: e.wrapX !== void 0 ? e.wrapX : true
    }), this.on, this.once, this.un, this.loader_ = Ki, this.format_ = e.format || null, this.overlaps_ = e.overlaps === void 0 ? true : e.overlaps, this.url_ = e.url, e.loader !== void 0 ? this.loader_ = e.loader : this.url_ !== void 0 && (_e(this.format_, "`format` must be set when `url` is set"), this.loader_ = Bl(this.url_, this.format_)), this.strategy_ = e.strategy !== void 0 ? e.strategy : ty;
    const t = e.useSpatialIndex !== void 0 ? e.useSpatialIndex : true;
    this.featuresRtree_ = t ? new Vl() : null, this.loadedExtentsRtree_ = new Vl(), this.loadingExtentsCount_ = 0, this.nullGeometryFeatures_ = {}, this.idIndex_ = {}, this.uidIndex_ = {}, this.featureChangeKeys_ = {}, this.featuresCollection_ = null;
    let n, s;
    Array.isArray(e.features) ? s = e.features : e.features && (n = e.features, s = n.getArray()), !t && n === void 0 && (n = new Ke(s)), s !== void 0 && this.addFeaturesInternal(s), n !== void 0 && this.bindFeaturesCollection_(n);
  }
  /**
   * Add a single feature to the source.  If you want to add a batch of features
   * at once, call {@link module:ol/source/Vector~VectorSource#addFeatures #addFeatures()}
   * instead. A feature will not be added to the source if feature with
   * the same id is already there. The reason for this behavior is to avoid
   * feature duplication when using bbox or tile loading strategies.
   * Note: this also applies if a {@link module:ol/Collection~Collection} is used for features,
   * meaning that if a feature with a duplicate id is added in the collection, it will
   * be removed from it right away.
   * @param {FeatureType} feature Feature to add.
   * @api
   */
  addFeature(e) {
    this.addFeatureInternal(e), this.changed();
  }
  /**
   * Add a feature without firing a `change` event.
   * @param {FeatureType} feature Feature.
   * @protected
   */
  addFeatureInternal(e) {
    const t = de(e);
    if (!this.addToIndex_(t, e)) {
      this.featuresCollection_ && this.featuresCollection_.remove(e);
      return;
    }
    this.setupChangeEvents_(t, e);
    const n = e.getGeometry();
    if (n) {
      const s = n.getExtent();
      this.featuresRtree_ && this.featuresRtree_.insert(s, e);
    } else
      this.nullGeometryFeatures_[t] = e;
    this.dispatchEvent(
      new rn(dt.ADDFEATURE, e)
    );
  }
  /**
   * @param {string} featureKey Unique identifier for the feature.
   * @param {FeatureType} feature The feature.
   * @private
   */
  setupChangeEvents_(e, t) {
    t instanceof Hn || (this.featureChangeKeys_[e] = [
      le(t, Z.CHANGE, this.handleFeatureChange_, this),
      le(
        t,
        mi.PROPERTYCHANGE,
        this.handleFeatureChange_,
        this
      )
    ]);
  }
  /**
   * @param {string} featureKey Unique identifier for the feature.
   * @param {FeatureType} feature The feature.
   * @return {boolean} The feature is "valid", in the sense that it is also a
   *     candidate for insertion into the Rtree.
   * @private
   */
  addToIndex_(e, t) {
    let n = true;
    if (t.getId() !== void 0) {
      const s = String(t.getId());
      if (!(s in this.idIndex_))
        this.idIndex_[s] = t;
      else if (t instanceof Hn) {
        const r = this.idIndex_[s];
        r instanceof Hn ? Array.isArray(r) ? r.push(t) : this.idIndex_[s] = [r, t] : n = false;
      } else
        n = false;
    }
    return n && (_e(
      !(e in this.uidIndex_),
      "The passed `feature` was already added to the source"
    ), this.uidIndex_[e] = t), n;
  }
  /**
   * Add a batch of features to the source.
   * @param {Array<FeatureType>} features Features to add.
   * @api
   */
  addFeatures(e) {
    this.addFeaturesInternal(e), this.changed();
  }
  /**
   * Add features without firing a `change` event.
   * @param {Array<FeatureType>} features Features.
   * @protected
   */
  addFeaturesInternal(e) {
    const t = [], n = [], s = [];
    for (let r = 0, o = e.length; r < o; r++) {
      const a = e[r], l = de(a);
      this.addToIndex_(l, a) && n.push(a);
    }
    for (let r = 0, o = n.length; r < o; r++) {
      const a = n[r], l = de(a);
      this.setupChangeEvents_(l, a);
      const c = a.getGeometry();
      if (c) {
        const h2 = c.getExtent();
        t.push(h2), s.push(a);
      } else
        this.nullGeometryFeatures_[l] = a;
    }
    if (this.featuresRtree_ && this.featuresRtree_.load(t, s), this.hasListener(dt.ADDFEATURE))
      for (let r = 0, o = n.length; r < o; r++)
        this.dispatchEvent(
          new rn(dt.ADDFEATURE, n[r])
        );
  }
  /**
   * @param {!Collection<FeatureType>} collection Collection.
   * @private
   */
  bindFeaturesCollection_(e) {
    let t = false;
    this.addEventListener(
      dt.ADDFEATURE,
      /**
       * @param {VectorSourceEvent<FeatureType>} evt The vector source event
       */
      function(n) {
        t || (t = true, e.push(n.feature), t = false);
      }
    ), this.addEventListener(
      dt.REMOVEFEATURE,
      /**
       * @param {VectorSourceEvent<FeatureType>} evt The vector source event
       */
      function(n) {
        t || (t = true, e.remove(n.feature), t = false);
      }
    ), e.addEventListener(
      Je.ADD,
      /**
       * @param {import("../Collection.js").CollectionEvent<FeatureType>} evt The collection event
       */
      (n) => {
        t || (t = true, this.addFeature(n.element), t = false);
      }
    ), e.addEventListener(
      Je.REMOVE,
      /**
       * @param {import("../Collection.js").CollectionEvent<FeatureType>} evt The collection event
       */
      (n) => {
        t || (t = true, this.removeFeature(n.element), t = false);
      }
    ), this.featuresCollection_ = e;
  }
  /**
   * Remove all features from the source.
   * @param {boolean} [fast] Skip dispatching of {@link module:ol/source/Vector.VectorSourceEvent#event:removefeature} events.
   * @api
   */
  clear(e) {
    if (e) {
      for (const n in this.featureChangeKeys_)
        this.featureChangeKeys_[n].forEach(Ee);
      this.featuresCollection_ || (this.featureChangeKeys_ = {}, this.idIndex_ = {}, this.uidIndex_ = {});
    } else if (this.featuresRtree_) {
      this.featuresRtree_.forEach((n) => {
        this.removeFeatureInternal(n);
      });
      for (const n in this.nullGeometryFeatures_)
        this.removeFeatureInternal(this.nullGeometryFeatures_[n]);
    }
    this.featuresCollection_ && this.featuresCollection_.clear(), this.featuresRtree_ && this.featuresRtree_.clear(), this.nullGeometryFeatures_ = {};
    const t = new rn(dt.CLEAR);
    this.dispatchEvent(t), this.changed();
  }
  /**
   * Iterate through all features on the source, calling the provided callback
   * with each one.  If the callback returns any "truthy" value, iteration will
   * stop and the function will return the same value.
   * Note: this function only iterate through the feature that have a defined geometry.
   *
   * @param {function(FeatureType): T} callback Called with each feature
   *     on the source.  Return a truthy value to stop iteration.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeature(e) {
    if (this.featuresRtree_)
      return this.featuresRtree_.forEach(e);
    this.featuresCollection_ && this.featuresCollection_.forEach(e);
  }
  /**
   * Iterate through all features whose geometries contain the provided
   * coordinate, calling the callback with each feature.  If the callback returns
   * a "truthy" value, iteration will stop and the function will return the same
   * value.
   *
   * For {@link module:ol/render/Feature~RenderFeature} features, the callback will be
   * called for all features.
   *
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose goemetry contains the provided coordinate.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   */
  forEachFeatureAtCoordinateDirect(e, t) {
    const n = [e[0], e[1], e[0], e[1]];
    return this.forEachFeatureInExtent(n, function(s) {
      const r = s.getGeometry();
      if (r instanceof Hn || r.intersectsCoordinate(e))
        return t(s);
    });
  }
  /**
   * Iterate through all features whose bounding box intersects the provided
   * extent (note that the feature's geometry may not intersect the extent),
   * calling the callback with each feature.  If the callback returns a "truthy"
   * value, iteration will stop and the function will return the same value.
   *
   * If you are interested in features whose geometry intersects an extent, call
   * the {@link module:ol/source/Vector~VectorSource#forEachFeatureIntersectingExtent #forEachFeatureIntersectingExtent()} method instead.
   *
   * When `useSpatialIndex` is set to false, this method will loop through all
   * features, equivalent to {@link module:ol/source/Vector~VectorSource#forEachFeature #forEachFeature()}.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose bounding box intersects the provided extent.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeatureInExtent(e, t) {
    if (this.featuresRtree_)
      return this.featuresRtree_.forEachInExtent(e, t);
    this.featuresCollection_ && this.featuresCollection_.forEach(t);
  }
  /**
   * Iterate through all features whose geometry intersects the provided extent,
   * calling the callback with each feature.  If the callback returns a "truthy"
   * value, iteration will stop and the function will return the same value.
   *
   * If you only want to test for bounding box intersection, call the
   * {@link module:ol/source/Vector~VectorSource#forEachFeatureInExtent #forEachFeatureInExtent()} method instead.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {function(FeatureType): T} callback Called with each feature
   *     whose geometry intersects the provided extent.
   * @return {T|undefined} The return value from the last call to the callback.
   * @template T
   * @api
   */
  forEachFeatureIntersectingExtent(e, t) {
    return this.forEachFeatureInExtent(
      e,
      /**
       * @param {FeatureType} feature Feature.
       * @return {T|undefined} The return value from the last call to the callback.
       */
      function(n) {
        const s = n.getGeometry();
        if (s instanceof Hn || s.intersectsExtent(e)) {
          const r = t(n);
          if (r)
            return r;
        }
      }
    );
  }
  /**
   * Get the features collection associated with this source. Will be `null`
   * unless the source was configured with `useSpatialIndex` set to `false`, or
   * with a {@link module:ol/Collection~Collection} as `features`.
   * @return {Collection<FeatureType>|null} The collection of features.
   * @api
   */
  getFeaturesCollection() {
    return this.featuresCollection_;
  }
  /**
   * Get a snapshot of the features currently on the source in random order. The returned array
   * is a copy, the features are references to the features in the source.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeatures() {
    let e;
    return this.featuresCollection_ ? e = this.featuresCollection_.getArray().slice(0) : this.featuresRtree_ && (e = this.featuresRtree_.getAll(), yi(this.nullGeometryFeatures_) || At(e, Object.values(this.nullGeometryFeatures_))), e;
  }
  /**
   * Get all features whose geometry intersects the provided coordinate.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeaturesAtCoordinate(e) {
    const t = [];
    return this.forEachFeatureAtCoordinateDirect(e, function(n) {
      t.push(n);
    }), t;
  }
  /**
   * Get all features whose bounding box intersects the provided extent.  Note that this returns an array of
   * all features intersecting the given extent in random order (so it may include
   * features whose geometries do not intersect the extent).
   *
   * When `useSpatialIndex` is set to false, this method will return all
   * features.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {import("../proj/Projection.js").default} [projection] Include features
   * where `extent` exceeds the x-axis bounds of `projection` and wraps around the world.
   * @return {Array<FeatureType>} Features.
   * @api
   */
  getFeaturesInExtent(e, t) {
    if (this.featuresRtree_) {
      if (!(t && t.canWrapX() && this.getWrapX()))
        return this.featuresRtree_.getInExtent(e);
      const s = pc(e, t);
      return [].concat(
        ...s.map((r) => this.featuresRtree_.getInExtent(r))
      );
    }
    return this.featuresCollection_ ? this.featuresCollection_.getArray().slice(0) : [];
  }
  /**
   * Get the closest feature to the provided coordinate.
   *
   * This method is not available when the source is configured with
   * `useSpatialIndex` set to `false` and the features in this source are of type
   * {@link module:ol/Feature~Feature}.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {function(FeatureType):boolean} [filter] Feature filter function.
   *     The filter function will receive one argument, the {@link module:ol/Feature~Feature feature}
   *     and it should return a boolean value. By default, no filtering is made.
   * @return {FeatureType|null} Closest feature (or `null` if none found).
   * @api
   */
  getClosestFeatureToCoordinate(e, t) {
    const n = e[0], s = e[1];
    let r = null;
    const o = [NaN, NaN];
    let a = 1 / 0;
    const l = [-1 / 0, -1 / 0, 1 / 0, 1 / 0];
    return t = t || _n, this.featuresRtree_.forEachInExtent(
      l,
      /**
       * @param {FeatureType} feature Feature.
       */
      function(c) {
        if (t(c)) {
          const h2 = c.getGeometry(), u = a;
          if (a = h2 instanceof Hn ? 0 : h2.closestPointXY(n, s, o, a), a < u) {
            r = c;
            const d = Math.sqrt(a);
            l[0] = n - d, l[1] = s - d, l[2] = n + d, l[3] = s + d;
          }
        }
      }
    ), r;
  }
  /**
   * Get the extent of the features currently in the source.
   *
   * This method is not available when the source is configured with
   * `useSpatialIndex` set to `false`.
   * @param {import("../extent.js").Extent} [extent] Destination extent. If provided, no new extent
   *     will be created. Instead, that extent's coordinates will be overwritten.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent(e) {
    return this.featuresRtree_.getExtent(e);
  }
  /**
   * Get a feature by its identifier (the value returned by feature.getId()). When `RenderFeature`s
   * are used, `getFeatureById()` can return an array of `RenderFeature`s. This allows for handling
   * of `GeometryCollection` geometries, where format readers create one `RenderFeature` per
   * `GeometryCollection` member.
   * Note that the index treats string and numeric identifiers as the same.  So
   * `source.getFeatureById(2)` will return a feature with id `'2'` or `2`.
   *
   * @param {string|number} id Feature identifier.
   * @return {FeatureClassOrArrayOfRenderFeatures<FeatureType>|null} The feature (or `null` if not found).
   * @api
   */
  getFeatureById(e) {
    const t = this.idIndex_[e.toString()];
    return t !== void 0 ? (
      /** @type {FeatureClassOrArrayOfRenderFeatures<FeatureType>} */
      t
    ) : null;
  }
  /**
   * Get a feature by its internal unique identifier (using `getUid`).
   *
   * @param {string} uid Feature identifier.
   * @return {FeatureType|null} The feature (or `null` if not found).
   */
  getFeatureByUid(e) {
    const t = this.uidIndex_[e];
    return t !== void 0 ? t : null;
  }
  /**
   * Get the format associated with this source.
   *
   * @return {import("../format/Feature.js").default<FeatureType>|null}} The feature format.
   * @api
   */
  getFormat() {
    return this.format_;
  }
  /**
   * @return {boolean} The source can have overlapping geometries.
   */
  getOverlaps() {
    return this.overlaps_;
  }
  /**
   * Get the url associated with this source.
   *
   * @return {string|import("../featureloader.js").FeatureUrlFunction|undefined} The url.
   * @api
   */
  getUrl() {
    return this.url_;
  }
  /**
   * @param {Event} event Event.
   * @private
   */
  handleFeatureChange_(e) {
    const t = (
      /** @type {FeatureType} */
      e.target
    ), n = de(t), s = t.getGeometry();
    if (!s)
      n in this.nullGeometryFeatures_ || (this.featuresRtree_ && this.featuresRtree_.remove(t), this.nullGeometryFeatures_[n] = t);
    else {
      const o = s.getExtent();
      n in this.nullGeometryFeatures_ ? (delete this.nullGeometryFeatures_[n], this.featuresRtree_ && this.featuresRtree_.insert(o, t)) : this.featuresRtree_ && this.featuresRtree_.update(o, t);
    }
    const r = t.getId();
    if (r !== void 0) {
      const o = r.toString();
      this.idIndex_[o] !== t && (this.removeFromIdIndex_(t), this.idIndex_[o] = t);
    } else
      this.removeFromIdIndex_(t), this.uidIndex_[n] = t;
    this.changed(), this.dispatchEvent(
      new rn(dt.CHANGEFEATURE, t)
    );
  }
  /**
   * Returns true if the feature is contained within the source.
   * @param {FeatureType} feature Feature.
   * @return {boolean} Has feature.
   * @api
   */
  hasFeature(e) {
    const t = e.getId();
    return t !== void 0 ? t in this.idIndex_ : de(e) in this.uidIndex_;
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.featuresRtree_ ? this.featuresRtree_.isEmpty() && yi(this.nullGeometryFeatures_) : this.featuresCollection_ ? this.featuresCollection_.getLength() === 0 : true;
  }
  /**
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} resolution Resolution.
   * @param {import("../proj/Projection.js").default} projection Projection.
   */
  loadFeatures(e, t, n) {
    const s = this.loadedExtentsRtree_, r = this.strategy_(e, t, n);
    for (let o = 0, a = r.length; o < a; ++o) {
      const l = r[o];
      s.forEachInExtent(
        l,
        /**
         * @param {{extent: import("../extent.js").Extent}} object Object.
         * @return {boolean} Contains.
         */
        function(h2) {
          return ai(h2.extent, l);
        }
      ) || (++this.loadingExtentsCount_, this.dispatchEvent(
        new rn(dt.FEATURESLOADSTART)
      ), this.loader_.call(
        this,
        l,
        t,
        n,
        /**
         * @param {Array<FeatureType>} features Loaded features
         */
        (h2) => {
          --this.loadingExtentsCount_, this.dispatchEvent(
            new rn(
              dt.FEATURESLOADEND,
              void 0,
              h2
            )
          );
        },
        () => {
          --this.loadingExtentsCount_, this.dispatchEvent(
            new rn(dt.FEATURESLOADERROR)
          );
        }
      ), s.insert(l, { extent: l.slice() }));
    }
    this.loading = this.loader_.length < 4 ? false : this.loadingExtentsCount_ > 0;
  }
  /**
   * @override
   */
  refresh() {
    this.clear(true), this.loadedExtentsRtree_.clear(), super.refresh();
  }
  /**
   * Remove an extent from the list of loaded extents.
   * @param {import("../extent.js").Extent} extent Extent.
   * @api
   */
  removeLoadedExtent(e) {
    const t = this.loadedExtentsRtree_, n = t.forEachInExtent(e, function(s) {
      if (Hi(s.extent, e))
        return s;
    });
    n && t.remove(n);
  }
  /**
   * Batch remove features from the source.  If you want to remove all features
   * at once, use the {@link module:ol/source/Vector~VectorSource#clear #clear()} method
   * instead.
   * @param {Array<FeatureType>} features Features to remove.
   * @api
   */
  removeFeatures(e) {
    let t = false;
    for (let n = 0, s = e.length; n < s; ++n)
      t = this.removeFeatureInternal(e[n]) || t;
    t && this.changed();
  }
  /**
   * Remove a single feature from the source. If you want to batch remove
   * features, use the {@link module:ol/source/Vector~VectorSource#removeFeatures #removeFeatures()} method
   * instead.
   * @param {FeatureType} feature Feature to remove.
   * @api
   */
  removeFeature(e) {
    if (!e)
      return;
    this.removeFeatureInternal(e) && this.changed();
  }
  /**
   * Remove feature without firing a `change` event.
   * @param {FeatureType} feature Feature.
   * @return {boolean} True if the feature was removed, false if it was not found.
   * @protected
   */
  removeFeatureInternal(e) {
    const t = de(e);
    if (!(t in this.uidIndex_))
      return false;
    t in this.nullGeometryFeatures_ ? delete this.nullGeometryFeatures_[t] : this.featuresRtree_ && this.featuresRtree_.remove(e);
    const n = this.featureChangeKeys_[t];
    n == null || n.forEach(Ee), delete this.featureChangeKeys_[t];
    const s = e.getId();
    if (s !== void 0) {
      const r = s.toString(), o = this.idIndex_[r];
      o === e ? delete this.idIndex_[r] : Array.isArray(o) && (o.splice(o.indexOf(e), 1), o.length === 1 && (this.idIndex_[r] = o[0]));
    }
    return delete this.uidIndex_[t], this.hasListener(dt.REMOVEFEATURE) && this.dispatchEvent(
      new rn(dt.REMOVEFEATURE, e)
    ), true;
  }
  /**
   * Remove a feature from the id index.  Called internally when the feature id
   * may have changed.
   * @param {FeatureType} feature The feature.
   * @private
   */
  removeFromIdIndex_(e) {
    for (const t in this.idIndex_)
      if (this.idIndex_[t] === e) {
        delete this.idIndex_[t];
        break;
      }
  }
  /**
   * Set the new loader of the source. The next render cycle will use the
   * new loader.
   * @param {import("../featureloader.js").FeatureLoader} loader The loader to set.
   * @api
   */
  setLoader(e) {
    this.loader_ = e;
  }
  /**
   * Points the source to a new url. The next render cycle will use the new url.
   * @param {string|import("../featureloader.js").FeatureUrlFunction} url Url.
   * @api
   */
  setUrl(e) {
    _e(this.format_, "`format` must be set when `url` is set"), this.url_ = e, this.setLoader(Bl(e, this.format_));
  }
  /**
   * @param {boolean} overlaps The source can have overlapping geometries.
   */
  setOverlaps(e) {
    this.overlaps_ = e, this.changed();
  }
};
var Lt = ry;
var lr = {
  ACTIVE: "active"
};
var oy = class extends vt {
  /**
   * @param {InteractionOptions} [options] Options.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, e && e.handleEvent && (this.handleEvent = e.handleEvent), this.map_ = null, this.setActive(true);
  }
  /**
   * Return whether the interaction is currently active.
   * @return {boolean} `true` if the interaction is active, `false` otherwise.
   * @observable
   * @api
   */
  getActive() {
    return (
      /** @type {boolean} */
      this.get(lr.ACTIVE)
    );
  }
  /**
   * Get the map associated with this interaction.
   * @return {import("../Map.js").default|null} Map.
   * @api
   */
  getMap() {
    return this.map_;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event}.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   */
  handleEvent(e) {
    return true;
  }
  /**
   * Activate or deactivate the interaction.
   * @param {boolean} active Active.
   * @observable
   * @api
   */
  setActive(e) {
    this.set(lr.ACTIVE, e);
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default|null} map Map.
   */
  setMap(e) {
    this.map_ = e;
  }
};
function ay(i, e, t) {
  const n = i.getCenterInternal();
  if (n) {
    const s = [n[0] + e[0], n[1] + e[1]];
    i.animateInternal({
      duration: t !== void 0 ? t : 250,
      easing: Om,
      center: i.getConstrainedCenter(s)
    });
  }
}
function Aa(i, e, t, n) {
  const s = i.getZoom();
  if (s === void 0)
    return;
  const r = i.getConstrainedZoom(s + e), o = i.getResolutionForZoom(r);
  i.getAnimating() && i.cancelAnimations(), i.animate({
    resolution: o,
    anchor: t,
    duration: n !== void 0 ? n : 250,
    easing: Ti
  });
}
var _s = oy;
var ly = class extends _s {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, super(
      /** @type {import("./Interaction.js").InteractionOptions} */
      e
    ), e.handleDownEvent && (this.handleDownEvent = e.handleDownEvent), e.handleDragEvent && (this.handleDragEvent = e.handleDragEvent), e.handleMoveEvent && (this.handleMoveEvent = e.handleMoveEvent), e.handleUpEvent && (this.handleUpEvent = e.handleUpEvent), e.stopDown && (this.stopDown = e.stopDown), this.handlingDownUpSequence = false, this.targetPointers = [];
  }
  /**
   * Returns the current number of pointers involved in the interaction,
   * e.g. `2` when two fingers are used.
   * @return {number} The number of pointers.
   * @api
   */
  getPointerCount() {
    return this.targetPointers.length;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @protected
   */
  handleDownEvent(e) {
    return false;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @protected
   */
  handleDragEvent(e) {
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} and may call into
   * other functions, if event sequences like e.g. 'drag' or 'down-up' etc. are
   * detected.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   * @override
   */
  handleEvent(e) {
    if (!e.originalEvent)
      return true;
    let t = false;
    if (this.updateTrackedPointers_(e), this.handlingDownUpSequence) {
      if (e.type == me.POINTERDRAG)
        this.handleDragEvent(e), e.originalEvent.preventDefault();
      else if (e.type == me.POINTERUP) {
        const n = this.handleUpEvent(e);
        this.handlingDownUpSequence = n && this.targetPointers.length > 0;
      }
    } else if (e.type == me.POINTERDOWN) {
      const n = this.handleDownEvent(e);
      this.handlingDownUpSequence = n, t = this.stopDown(n);
    } else
      e.type == me.POINTERMOVE && this.handleMoveEvent(e);
    return !t;
  }
  /**
   * Handle pointer move events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @protected
   */
  handleMoveEvent(e) {
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @protected
   */
  handleUpEvent(e) {
    return false;
  }
  /**
   * This function is used to determine if "down" events should be propagated
   * to other interactions or should be stopped.
   * @param {boolean} handled Was the event handled by the interaction?
   * @return {boolean} Should the `down` event be stopped?
   */
  stopDown(e) {
    return e;
  }
  /**
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @private
   */
  updateTrackedPointers_(e) {
    e.activePointers && (this.targetPointers = e.activePointers);
  }
};
function ka(i) {
  const e = i.length;
  let t = 0, n = 0;
  for (let s = 0; s < e; s++)
    t += i[s].clientX, n += i[s].clientY;
  return { clientX: t / e, clientY: n / e };
}
var Xn = ly;
function ci(i, e) {
  const t = i.length;
  return e < 0 ? i[e + t] : e >= t ? i[e - t] : i[e];
}
function hi(i, e) {
  const t = i.length;
  let n = Math.floor(e);
  const s = e - n;
  n >= t ? n -= t : n < 0 && (n += t);
  let r = n + 1;
  r >= t && (r -= t);
  const o = i[n], a = o[0], l = o[1], c = i[r], h2 = c[0] - a, u = c[1] - l;
  return [a + h2 * s, l + u * s];
}
var Ls = {
  index: -1,
  endIndex: NaN,
  closestTargetDistance: 1 / 0
};
function cy(i, e, t, n) {
  const s = i[0], r = i[1];
  let o = 1 / 0, a = -1, l = NaN;
  for (let u = 0; u < e.targets.length; ++u) {
    const d = e.targets[u], f = d.coordinates;
    let g = 1 / 0, y;
    for (let m = 0; m < f.length - 1; ++m) {
      const _ = f[m], p = f[m + 1], E = bh(s, r, _, p);
      E.squaredDistance < g && (g = E.squaredDistance, y = m + E.along);
    }
    g < o && (o = g, d.ring && e.targetIndex === u && (d.endIndex > d.startIndex ? y < d.startIndex && (y += f.length) : d.endIndex < d.startIndex && y > d.startIndex && (y -= f.length)), l = y, a = u);
  }
  const c = e.targets[a];
  let h2 = c.ring;
  if (e.targetIndex === a && h2) {
    const u = hi(
      c.coordinates,
      l
    ), d = t.getPixelFromCoordinate(u), f = t.getPixelFromCoordinate(e.startCoord);
    wc(d, f) > n && (h2 = false);
  }
  if (h2) {
    const u = c.coordinates, d = u.length, f = c.startIndex, g = l;
    if (f < g) {
      const y = Ps(
        u,
        f,
        g
      );
      Ps(
        u,
        f,
        g - d
      ) < y && (l -= d);
    } else {
      const y = Ps(
        u,
        f,
        g
      );
      Ps(
        u,
        f,
        g + d
      ) < y && (l += d);
    }
  }
  return Ls.index = a, Ls.endIndex = l, Ls.closestTargetDistance = o, Ls;
}
function hy(i, e) {
  const t = [];
  for (let n = 0; n < e.length; ++n) {
    const r = e[n].getGeometry();
    Th(i, r, t);
  }
  return t;
}
function Th(i, e, t) {
  if (e instanceof tt) {
    Fs(i, e.getCoordinates(), false, t);
    return;
  }
  if (e instanceof Ca) {
    const n = e.getCoordinates();
    for (let s = 0, r = n.length; s < r; ++s)
      Fs(i, n[s], false, t);
    return;
  }
  if (e instanceof at) {
    const n = e.getCoordinates();
    for (let s = 0, r = n.length; s < r; ++s)
      Fs(i, n[s], true, t);
    return;
  }
  if (e instanceof rh) {
    const n = e.getCoordinates();
    for (let s = 0, r = n.length; s < r; ++s) {
      const o = n[s];
      for (let a = 0, l = o.length; a < l; ++a)
        Fs(i, o[a], true, t);
    }
    return;
  }
  if (e instanceof ny) {
    const n = e.getGeometries();
    for (let s = 0; s < n.length; ++s)
      Th(i, n[s], t);
    return;
  }
}
function Fs(i, e, t, n) {
  const s = i[0], r = i[1];
  for (let o = 0, a = e.length - 1; o < a; ++o) {
    const l = e[o], c = e[o + 1], h2 = bh(s, r, l, c);
    if (h2.squaredDistance === 0) {
      const u = o + h2.along;
      n.push({
        coordinates: e,
        ring: t,
        startIndex: u,
        endIndex: u
      });
      return;
    }
  }
}
function Ds(i, e) {
  return jt(i[0], i[1], e[0], e[1]);
}
function Ps(i, e, t) {
  let n, s;
  e < t ? (n = e, s = t) : (n = t, s = e);
  const r = Math.ceil(n), o = Math.floor(s);
  if (r > o) {
    const l = hi(i, n), c = hi(i, s);
    return Ds(l, c);
  }
  let a = 0;
  if (n < r) {
    const l = hi(i, n), c = ci(i, r);
    a += Ds(l, c);
  }
  if (o < s) {
    const l = ci(i, o), c = hi(i, s);
    a += Ds(l, c);
  }
  for (let l = r; l < o - 1; ++l) {
    const c = ci(i, l), h2 = ci(i, l + 1);
    a += Ds(c, h2);
  }
  return a;
}
var eo = { along: 0, squaredDistance: 0 };
function bh(i, e, t, n) {
  const s = t[0], r = t[1], o = n[0], a = n[1], l = o - s, c = a - r;
  let h2 = 0, u = s, d = r;
  return (l !== 0 || c !== 0) && (h2 = xe(((i - s) * l + (e - r) * c) / (l * l + c * c), 0, 1), u += l * h2, d += c * h2), eo.along = h2, eo.squaredDistance = us(jt(i, e, u, d), 10), eo;
}
var As = {
  /**
   * Triggered upon feature draw start
   * @event DrawEvent#drawstart
   * @api
   */
  DRAWSTART: "drawstart",
  /**
   * Triggered upon feature draw end
   * @event DrawEvent#drawend
   * @api
   */
  DRAWEND: "drawend",
  /**
   * Triggered upon feature draw abortion
   * @event DrawEvent#drawabort
   * @api
   */
  DRAWABORT: "drawabort"
};
var ks = class extends It {
  /**
   * @param {DrawEventType} type Type.
   * @param {Feature} feature The feature drawn.
   */
  constructor(e, t) {
    super(e), this.feature = t;
  }
};
var uy = class extends Xn {
  /**
   * @param {Options} options Options.
   */
  constructor(e) {
    const t = (
      /** @type {import("./Pointer.js").Options} */
      e
    );
    t.stopDown || (t.stopDown = vi), super(t), this.on, this.once, this.un, this.options_ = e, this.shouldHandle_ = false, this.downPx_ = null, this.downTimeout_, this.lastDragTime_, this.pointerType_, this.freehand_ = false, this.source_ = e.source ? e.source : null, this.features_ = e.features ? e.features : null, this.snapTolerance_ = e.snapTolerance ? e.snapTolerance : 12, this.type_ = /** @type {import("../geom/Geometry.js").Type} */
    e.type, this.mode_ = my(this.type_), this.stopClick_ = !!e.stopClick, this.ignoreNextUpEvent_ = false, this.minPoints_ = e.minPoints ? e.minPoints : this.mode_ === "Polygon" ? 3 : 2, this.maxPoints_ = this.mode_ === "Circle" ? 2 : e.maxPoints ? e.maxPoints : 1 / 0, this.finishCondition_ = e.finishCondition ? e.finishCondition : _n, this.geometryLayout_ = e.geometryLayout ? e.geometryLayout : "XY";
    let n = e.geometryFunction;
    if (!n) {
      const s = this.mode_;
      if (s === "Circle")
        n = (r, o, a) => {
          const l = o || new qc([NaN, NaN]), c = Be(r[0]), h2 = jo(
            c,
            Be(r[r.length - 1])
          );
          return l.setCenterAndRadius(
            c,
            Math.sqrt(h2),
            this.geometryLayout_
          ), l;
        };
      else {
        let r;
        s === "Point" ? r = he : s === "LineString" ? r = tt : s === "Polygon" && (r = at), n = (o, a, l) => (a ? s === "Polygon" ? o[0].length ? a.setCoordinates(
          [o[0].concat([o[0][0]])],
          this.geometryLayout_
        ) : a.setCoordinates([], this.geometryLayout_) : a.setCoordinates(o, this.geometryLayout_) : a = new r(o, this.geometryLayout_), a);
      }
    }
    this.geometryFunction_ = n, this.dragVertexDelay_ = e.dragVertexDelay !== void 0 ? e.dragVertexDelay : 500, this.finishCoordinate_ = null, this.sketchFeature_ = null, this.sketchPoint_ = null, this.sketchCoords_ = null, this.sketchLine_ = null, this.sketchLineCoords_ = null, this.squaredClickTolerance_ = e.clickTolerance ? e.clickTolerance * e.clickTolerance : 36, this.overlay_ = new Rt({
      source: new Lt({
        useSpatialIndex: false,
        wrapX: e.wrapX ? e.wrapX : false
      }),
      style: e.style ? e.style : dy(),
      updateWhileInteracting: true
    }), this.geometryName_ = e.geometryName, this.condition_ = e.condition ? e.condition : wa, this.freehandCondition_, e.freehand ? this.freehandCondition_ = $i : this.freehandCondition_ = e.freehandCondition ? e.freehandCondition : Ro, this.traceCondition_, this.setTrace(e.trace || false), this.traceState_ = { active: false }, this.traceSource_ = e.traceSource || e.source || null, this.addChangeListener(lr.ACTIVE, this.updateState_);
  }
  /**
   * Toggle tracing mode or set a tracing condition.
   *
   * @param {boolean|import("../events/condition.js").Condition} trace A boolean to toggle tracing mode or an event
   *     condition that will be checked when a feature is clicked to determine if tracing should be active.
   */
  setTrace(e) {
    let t;
    e ? e === true ? t = $i : t = e : t = Wf, this.traceCondition_ = t;
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default} map Map.
   * @override
   */
  setMap(e) {
    super.setMap(e), this.updateState_();
  }
  /**
   * Set whether the drawing is done in freehand mode.
   *
   * @param {boolean} freehand Freehand drawing.
   * @api
   */
  setFreehand(e) {
    this.freehand_ = e, this.freehand_ ? this.freehandCondition_ = $i : this.freehandCondition_ = this.options_ && this.options_.freehandCondition ? this.options_.freehandCondition : Ro;
  }
  /**
   * Get the overlay layer that this interaction renders sketch features to.
   * @return {VectorLayer} Overlay layer.
   * @api
   */
  getOverlay() {
    return this.overlay_;
  }
  /**
   * Get if this interaction is in freehand mode.
   * @return {boolean} Freehand drawing.
   * @api
   */
  getFreehand() {
    return this.freehand_;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} and may actually draw or finish the drawing.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @api
   * @override
   */
  handleEvent(e) {
    e.originalEvent.type === Z.CONTEXTMENU && e.originalEvent.preventDefault(), this.freehand_ = this.mode_ !== "Point" && this.freehandCondition_(e);
    let t = e.type === me.POINTERMOVE, n = true;
    return !this.freehand_ && this.lastDragTime_ && e.type === me.POINTERDRAG && (Date.now() - this.lastDragTime_ >= this.dragVertexDelay_ ? (this.downPx_ = e.pixel, this.shouldHandle_ = !this.freehand_, t = true) : this.lastDragTime_ = void 0, this.shouldHandle_ && this.downTimeout_ !== void 0 && (clearTimeout(this.downTimeout_), this.downTimeout_ = void 0)), this.freehand_ && e.type === me.POINTERDRAG && this.sketchFeature_ !== null ? (this.addToDrawing_(e.coordinate), n = false) : this.freehand_ && e.type === me.POINTERDOWN ? n = false : t && this.getPointerCount() < 2 ? (n = e.type === me.POINTERMOVE, n && this.freehand_ ? (this.handlePointerMove_(e), this.shouldHandle_ && e.originalEvent.preventDefault()) : (e.originalEvent.pointerType === "mouse" || e.type === me.POINTERDRAG && this.downTimeout_ === void 0) && this.handlePointerMove_(e)) : e.type === me.DBLCLICK && (n = false), super.handleEvent(e) && n;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    return this.shouldHandle_ = !this.freehand_, this.freehand_ ? (this.downPx_ = e.pixel, this.finishCoordinate_ || this.startDrawing_(e.coordinate), true) : this.condition_(e) ? (this.lastDragTime_ = Date.now(), this.downTimeout_ = setTimeout(() => {
      this.handlePointerMove_(
        new Vt(
          me.POINTERMOVE,
          e.map,
          e.originalEvent,
          false,
          e.frameState
        )
      );
    }, this.dragVertexDelay_), this.downPx_ = e.pixel, true) : (this.lastDragTime_ = void 0, false);
  }
  /**
   * @private
   */
  deactivateTrace_() {
    this.traceState_ = { active: false };
  }
  /**
   * Activate or deactivate trace state based on a browser event.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @private
   */
  toggleTraceState_(e) {
    if (!this.traceSource_ || !this.traceCondition_(e))
      return;
    if (this.traceState_.active) {
      this.deactivateTrace_();
      return;
    }
    const t = this.getMap(), n = t.getCoordinateFromPixel([
      e.pixel[0] - this.snapTolerance_,
      e.pixel[1] + this.snapTolerance_
    ]), s = t.getCoordinateFromPixel([
      e.pixel[0] + this.snapTolerance_,
      e.pixel[1] - this.snapTolerance_
    ]), r = Ws([n, s]), o = this.traceSource_.getFeaturesInExtent(r);
    if (o.length === 0)
      return;
    const a = hy(e.coordinate, o);
    a.length && (this.traceState_ = {
      active: true,
      startCoord: e.coordinate.slice(),
      targets: a,
      targetIndex: -1
    });
  }
  /**
   * @param {TraceTarget} target The trace target.
   * @param {number} endIndex The new end index of the trace.
   * @private
   */
  addOrRemoveTracedCoordinates_(e, t) {
    const n = e.startIndex <= e.endIndex, s = e.startIndex <= t;
    n === s ? n && t > e.endIndex || !n && t < e.endIndex ? this.addTracedCoordinates_(e, e.endIndex, t) : (n && t < e.endIndex || !n && t > e.endIndex) && this.removeTracedCoordinates_(t, e.endIndex) : (this.removeTracedCoordinates_(e.startIndex, e.endIndex), this.addTracedCoordinates_(e, e.startIndex, t));
  }
  /**
   * @param {number} fromIndex The start index.
   * @param {number} toIndex The end index.
   * @private
   */
  removeTracedCoordinates_(e, t) {
    if (e === t)
      return;
    let n = 0;
    if (e < t) {
      const s = Math.ceil(e);
      let r = Math.floor(t);
      r === t && (r -= 1), n = r - s + 1;
    } else {
      const s = Math.floor(e);
      let r = Math.ceil(t);
      r === t && (r += 1), n = s - r + 1;
    }
    n > 0 && this.removeLastPoints_(n);
  }
  /**
   * @param {TraceTarget} target The trace target.
   * @param {number} fromIndex The start index.
   * @param {number} toIndex The end index.
   * @private
   */
  addTracedCoordinates_(e, t, n) {
    if (t === n)
      return;
    const s = [];
    if (t < n) {
      const r = Math.ceil(t);
      let o = Math.floor(n);
      o === n && (o -= 1);
      for (let a = r; a <= o; ++a)
        s.push(ci(e.coordinates, a));
    } else {
      const r = Math.floor(t);
      let o = Math.ceil(n);
      o === n && (o += 1);
      for (let a = r; a >= o; --a)
        s.push(ci(e.coordinates, a));
    }
    s.length && this.appendCoordinates(s);
  }
  /**
   * Update the trace.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @private
   */
  updateTrace_(e) {
    const t = this.traceState_;
    if (!t.active)
      return;
    if (t.targetIndex === -1) {
      const a = e.map.getPixelFromCoordinate(t.startCoord);
      if (wc(a, e.pixel) < this.snapTolerance_)
        return;
    }
    const n = cy(
      e.coordinate,
      t,
      this.getMap(),
      this.snapTolerance_
    );
    if (t.targetIndex !== n.index) {
      if (t.targetIndex !== -1) {
        const l = t.targets[t.targetIndex];
        this.removeTracedCoordinates_(l.startIndex, l.endIndex);
      }
      const a = t.targets[n.index];
      this.addTracedCoordinates_(
        a,
        a.startIndex,
        n.endIndex
      );
    } else {
      const a = t.targets[t.targetIndex];
      this.addOrRemoveTracedCoordinates_(a, n.endIndex);
    }
    t.targetIndex = n.index;
    const s = t.targets[t.targetIndex];
    s.endIndex = n.endIndex;
    const r = hi(
      s.coordinates,
      s.endIndex
    ), o = this.getMap().getPixelFromCoordinate(r);
    e.coordinate = r, e.pixel = [Math.round(o[0]), Math.round(o[1])];
  }
  /**
   * Handle drag events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Event.
   * @override
   */
  handleDragEvent(e) {
    this.ignoreNextUpEvent_ = true, super.handleDragEvent(e);
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    let t = true;
    if (this.getPointerCount() === 0) {
      this.downTimeout_ && (clearTimeout(this.downTimeout_), this.downTimeout_ = void 0), this.handlePointerMove_(e);
      const n = this.traceState_.active;
      if (this.ignoreNextUpEvent_ || this.toggleTraceState_(e), this.shouldHandle_) {
        const s = !this.finishCoordinate_;
        s && this.startDrawing_(e.coordinate), !s && this.freehand_ ? this.finishDrawing() : !this.freehand_ && (!s || this.mode_ === "Point") && (this.atFinish_(e.pixel, n) ? this.finishCondition_(e) && this.finishDrawing() : this.addToDrawing_(e.coordinate)), t = false;
      } else
        this.freehand_ && this.abortDrawing();
    }
    return this.ignoreNextUpEvent_ = false, !t && this.stopClick_ && e.preventDefault(), t;
  }
  /**
   * Handle move events.
   * @param {import("../MapBrowserEvent.js").default<PointerEvent>} event A move event.
   * @private
   */
  handlePointerMove_(e) {
    if (this.pointerType_ = e.originalEvent.pointerType, this.downPx_ && (!this.freehand_ && this.shouldHandle_ || this.freehand_ && !this.shouldHandle_)) {
      const t = this.downPx_, n = e.pixel, s = t[0] - n[0], r = t[1] - n[1], o = s * s + r * r;
      if (this.shouldHandle_ = this.freehand_ ? o > this.squaredClickTolerance_ : o <= this.squaredClickTolerance_, !this.shouldHandle_)
        return;
    }
    if (!this.finishCoordinate_) {
      this.createOrUpdateSketchPoint_(e.coordinate.slice());
      return;
    }
    this.updateTrace_(e), this.modifyDrawing_(e.coordinate);
  }
  /**
   * Determine if an event is within the snapping tolerance of the start coord.
   * @param {import("../pixel.js").Pixel} pixel Pixel.
   * @param {boolean} [tracing] Drawing in trace mode (only stop if at the starting point).
   * @return {boolean} The event is within the snapping tolerance of the start.
   * @private
   */
  atFinish_(e, t) {
    let n = false;
    if (this.sketchFeature_) {
      let s = false, r = [this.finishCoordinate_];
      const o = this.mode_;
      if (o === "Point")
        n = true;
      else if (o === "Circle")
        n = this.sketchCoords_.length === 2;
      else if (o === "LineString")
        s = !t && this.sketchCoords_.length > this.minPoints_;
      else if (o === "Polygon") {
        const a = (
          /** @type {PolyCoordType} */
          this.sketchCoords_
        );
        s = a[0].length > this.minPoints_, r = [
          a[0][0],
          a[0][a[0].length - 2]
        ], t ? r = [a[0][0]] : r = [
          a[0][0],
          a[0][a[0].length - 2]
        ];
      }
      if (s) {
        const a = this.getMap();
        for (let l = 0, c = r.length; l < c; l++) {
          const h2 = r[l], u = a.getPixelFromCoordinate(h2), d = e[0] - u[0], f = e[1] - u[1], g = this.freehand_ ? 1 : this.snapTolerance_;
          if (n = Math.sqrt(d * d + f * f) <= g, n) {
            this.finishCoordinate_ = h2;
            break;
          }
        }
      }
    }
    return n;
  }
  /**
   * @param {import("../coordinate").Coordinate} coordinates Coordinate.
   * @private
   */
  createOrUpdateSketchPoint_(e) {
    this.sketchPoint_ ? this.sketchPoint_.getGeometry().setCoordinates(e) : (this.sketchPoint_ = new ae(new he(e)), this.updateSketchFeatures_());
  }
  /**
   * @param {import("../geom/Polygon.js").default} geometry Polygon geometry.
   * @private
   */
  createOrUpdateCustomSketchLine_(e) {
    this.sketchLine_ || (this.sketchLine_ = new ae());
    const t = e.getLinearRing(0);
    let n = this.sketchLine_.getGeometry();
    n ? (n.setFlatCoordinates(
      t.getLayout(),
      t.getFlatCoordinates()
    ), n.changed()) : (n = new tt(
      t.getFlatCoordinates(),
      t.getLayout()
    ), this.sketchLine_.setGeometry(n));
  }
  /**
   * Start the drawing.
   * @param {import("../coordinate.js").Coordinate} start Start coordinate.
   * @private
   */
  startDrawing_(e) {
    const t = this.getMap().getView().getProjection(), n = Hs(this.geometryLayout_);
    for (; e.length < n; )
      e.push(0);
    this.finishCoordinate_ = e, this.mode_ === "Point" ? this.sketchCoords_ = e.slice() : this.mode_ === "Polygon" ? (this.sketchCoords_ = [[e.slice(), e.slice()]], this.sketchLineCoords_ = this.sketchCoords_[0]) : this.sketchCoords_ = [e.slice(), e.slice()], this.sketchLineCoords_ && (this.sketchLine_ = new ae(new tt(this.sketchLineCoords_)));
    const s = this.geometryFunction_(
      this.sketchCoords_,
      void 0,
      t
    );
    this.sketchFeature_ = new ae(), this.geometryName_ && this.sketchFeature_.setGeometryName(this.geometryName_), this.sketchFeature_.setGeometry(s), this.updateSketchFeatures_(), this.dispatchEvent(
      new ks(As.DRAWSTART, this.sketchFeature_)
    );
  }
  /**
   * Modify the drawing.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @private
   */
  modifyDrawing_(e) {
    const t = this.getMap(), n = this.sketchFeature_.getGeometry(), s = t.getView().getProjection(), r = Hs(this.geometryLayout_);
    let o, a;
    for (; e.length < r; )
      e.push(0);
    this.mode_ === "Point" ? a = this.sketchCoords_ : this.mode_ === "Polygon" ? (o = /** @type {PolyCoordType} */
    this.sketchCoords_[0], a = o[o.length - 1], this.atFinish_(t.getPixelFromCoordinate(e)) && (e = this.finishCoordinate_.slice())) : (o = this.sketchCoords_, a = o[o.length - 1]), a[0] = e[0], a[1] = e[1], this.geometryFunction_(
      /** @type {!LineCoordType} */
      this.sketchCoords_,
      n,
      s
    ), this.sketchPoint_ && this.sketchPoint_.getGeometry().setCoordinates(e), n.getType() === "Polygon" && this.mode_ !== "Polygon" ? this.createOrUpdateCustomSketchLine_(
      /** @type {Polygon} */
      n
    ) : this.sketchLineCoords_ && this.sketchLine_.getGeometry().setCoordinates(this.sketchLineCoords_), this.updateSketchFeatures_();
  }
  /**
   * Add a new coordinate to the drawing.
   * @param {!PointCoordType} coordinate Coordinate
   * @return {Feature<import("../geom/SimpleGeometry.js").default>} The sketch feature.
   * @private
   */
  addToDrawing_(e) {
    const t = this.sketchFeature_.getGeometry(), n = this.getMap().getView().getProjection();
    let s, r;
    const o = this.mode_;
    return o === "LineString" || o === "Circle" ? (this.finishCoordinate_ = e.slice(), r = /** @type {LineCoordType} */
    this.sketchCoords_, r.length >= this.maxPoints_ && (this.freehand_ ? r.pop() : s = true), r.push(e.slice()), this.geometryFunction_(r, t, n)) : o === "Polygon" && (r = /** @type {PolyCoordType} */
    this.sketchCoords_[0], r.length >= this.maxPoints_ && (this.freehand_ ? r.pop() : s = true), r.push(e.slice()), s && (this.finishCoordinate_ = r[0]), this.geometryFunction_(this.sketchCoords_, t, n)), this.createOrUpdateSketchPoint_(e.slice()), this.updateSketchFeatures_(), s ? this.finishDrawing() : this.sketchFeature_;
  }
  /**
   * @param {number} n The number of points to remove.
   */
  removeLastPoints_(e) {
    if (!this.sketchFeature_)
      return;
    const t = this.sketchFeature_.getGeometry(), n = this.getMap().getView().getProjection(), s = this.mode_;
    for (let r = 0; r < e; ++r) {
      let o;
      if (s === "LineString" || s === "Circle") {
        if (o = /** @type {LineCoordType} */
        this.sketchCoords_, o.splice(-2, 1), o.length >= 2) {
          this.finishCoordinate_ = o[o.length - 2].slice();
          const a = this.finishCoordinate_.slice();
          o[o.length - 1] = a, this.createOrUpdateSketchPoint_(a);
        }
        this.geometryFunction_(o, t, n), t.getType() === "Polygon" && this.sketchLine_ && this.createOrUpdateCustomSketchLine_(
          /** @type {Polygon} */
          t
        );
      } else if (s === "Polygon") {
        o = /** @type {PolyCoordType} */
        this.sketchCoords_[0], o.splice(-2, 1);
        const a = this.sketchLine_.getGeometry();
        if (o.length >= 2) {
          const l = o[o.length - 2].slice();
          o[o.length - 1] = l, this.createOrUpdateSketchPoint_(l);
        }
        a.setCoordinates(o), this.geometryFunction_(this.sketchCoords_, t, n);
      }
      if (o.length === 1) {
        this.abortDrawing();
        break;
      }
    }
    this.updateSketchFeatures_();
  }
  /**
   * Remove last point of the feature currently being drawn. Does not do anything when
   * drawing POINT or MULTI_POINT geometries.
   * @api
   */
  removeLastPoint() {
    this.removeLastPoints_(1);
  }
  /**
   * Stop drawing and add the sketch feature to the target layer.
   * The {@link module:ol/interaction/Draw~DrawEventType.DRAWEND} event is
   * dispatched before inserting the feature.
   * @return {Feature<import("../geom/SimpleGeometry.js").default>|null} The drawn feature.
   * @api
   */
  finishDrawing() {
    const e = this.abortDrawing_();
    if (!e)
      return null;
    let t = this.sketchCoords_;
    const n = e.getGeometry(), s = this.getMap().getView().getProjection();
    return this.mode_ === "LineString" ? (t.pop(), this.geometryFunction_(t, n, s)) : this.mode_ === "Polygon" && (t[0].pop(), this.geometryFunction_(t, n, s), t = n.getCoordinates()), this.type_ === "MultiPoint" ? e.setGeometry(
      new Jc([
        /** @type {PointCoordType} */
        t
      ])
    ) : this.type_ === "MultiLineString" ? e.setGeometry(
      new Ca([
        /** @type {LineCoordType} */
        t
      ])
    ) : this.type_ === "MultiPolygon" && e.setGeometry(
      new rh([
        /** @type {PolyCoordType} */
        t
      ])
    ), this.dispatchEvent(new ks(As.DRAWEND, e)), this.features_ && this.features_.push(e), this.source_ && this.source_.addFeature(e), e;
  }
  /**
   * Stop drawing without adding the sketch feature to the target layer.
   * @return {Feature<import("../geom/SimpleGeometry.js").default>|null} The sketch feature (or null if none).
   * @private
   */
  abortDrawing_() {
    this.finishCoordinate_ = null;
    const e = this.sketchFeature_;
    return this.sketchFeature_ = null, this.sketchPoint_ = null, this.sketchLine_ = null, this.overlay_.getSource().clear(true), this.deactivateTrace_(), e;
  }
  /**
   * Stop drawing without adding the sketch feature to the target layer.
   * @api
   */
  abortDrawing() {
    const e = this.abortDrawing_();
    e && this.dispatchEvent(new ks(As.DRAWABORT, e));
  }
  /**
   * Append coordinates to the end of the geometry that is currently being drawn.
   * This can be used when drawing LineStrings or Polygons. Coordinates will
   * either be appended to the current LineString or the outer ring of the current
   * Polygon. If no geometry is being drawn, a new one will be created.
   * @param {!LineCoordType} coordinates Linear coordinates to be appended to
   * the coordinate array.
   * @api
   */
  appendCoordinates(e) {
    const t = this.mode_, n = !this.sketchFeature_;
    n && this.startDrawing_(e[0]);
    let s;
    if (t === "LineString" || t === "Circle")
      s = /** @type {LineCoordType} */
      this.sketchCoords_;
    else if (t === "Polygon")
      s = this.sketchCoords_ && this.sketchCoords_.length ? (
        /** @type {PolyCoordType} */
        this.sketchCoords_[0]
      ) : [];
    else
      return;
    n && s.shift(), s.pop();
    for (let o = 0; o < e.length; o++)
      this.addToDrawing_(e[o]);
    const r = e[e.length - 1];
    this.sketchFeature_ = this.addToDrawing_(r), this.modifyDrawing_(r);
  }
  /**
   * Initiate draw mode by starting from an existing geometry which will
   * receive new additional points. This only works on features with
   * `LineString` geometries, where the interaction will extend lines by adding
   * points to the end of the coordinates array.
   * This will change the original feature, instead of drawing a copy.
   *
   * The function will dispatch a `drawstart` event.
   *
   * @param {!Feature<LineString>} feature Feature to be extended.
   * @api
   */
  extend(e) {
    const n = e.getGeometry();
    this.sketchFeature_ = e, this.sketchCoords_ = n.getCoordinates();
    const s = this.sketchCoords_[this.sketchCoords_.length - 1];
    this.finishCoordinate_ = s.slice(), this.sketchCoords_.push(s.slice()), this.sketchPoint_ = new ae(new he(s)), this.updateSketchFeatures_(), this.dispatchEvent(
      new ks(As.DRAWSTART, this.sketchFeature_)
    );
  }
  /**
   * Redraw the sketch features.
   * @private
   */
  updateSketchFeatures_() {
    const e = [];
    this.sketchFeature_ && e.push(this.sketchFeature_), this.sketchLine_ && e.push(this.sketchLine_), this.sketchPoint_ && e.push(this.sketchPoint_);
    const t = this.overlay_.getSource();
    t.clear(true), t.addFeatures(e);
  }
  /**
   * @private
   */
  updateState_() {
    const e = this.getMap(), t = this.getActive();
    (!e || !t) && this.abortDrawing(), this.overlay_.setMap(t ? e : null);
  }
};
function dy() {
  const i = Nf();
  return function(e, t) {
    return i[e.getGeometry().getType()];
  };
}
function fy(i, e) {
  return function(t, n, s) {
    const r = Be(
      /** @type {LineCoordType} */
      t[0]
    ), o = Be(
      /** @type {LineCoordType} */
      t[t.length - 1]
    ), a = Math.sqrt(jo(r, o));
    n = n || qf(new qc(r), i);
    let l = e;
    if (!e && e !== 0) {
      const c = o[0] - r[0], h2 = o[1] - r[1];
      l = Math.atan2(h2, c);
    }
    return ih(
      /** @type {Polygon} */
      n,
      r,
      a,
      l
    ), n;
  };
}
function gy() {
  return function(i, e, t) {
    const n = Ws(
      /** @type {LineCoordType} */
      [
        i[0],
        i[i.length - 1]
      ].map(function(r) {
        return Be(r);
      })
    ), s = [
      [
        pi(n),
        ls(n),
        cs(n),
        En(n),
        pi(n)
      ]
    ];
    return e ? e.setCoordinates(s) : e = new at(s), e;
  };
}
function my(i) {
  switch (i) {
    case "Point":
    case "MultiPoint":
      return "Point";
    case "LineString":
    case "MultiLineString":
      return "LineString";
    case "Polygon":
    case "MultiPolygon":
      return "Polygon";
    case "Circle":
      return "Circle";
    default:
      throw new Error("Invalid type: " + i);
  }
}
var Rh = uy;
var yy = (i, e, t) => {
  const n = t === "military" || t === "noFlyZone" || i === "noFlyZone";
  return e === "rectangle" || e === "circle" ? "Circle" : n || i === "polygon" ? "Polygon" : i === "line" || i === "arrow" || i === "attackRoute" ? "LineString" : i === "circle" ? "Circle" : "Point";
};
var _y = (i) => i instanceof he ? {
  geometry: {
    type: "point",
    coordinates: $t(i.getCoordinates())
  }
} : i instanceof tt ? {
  geometry: {
    type: "line",
    coordinates: i.getCoordinates().map((e) => $t(e))
  }
} : i instanceof at ? {
  geometry: {
    type: "polygon",
    coordinates: i.getCoordinates()[0].map((e) => $t(e))
  }
} : null;
var py = class {
  constructor({ getMap: e, getAnnotationSource: t, stopInteractions: n, onDrawEnd: s }) {
    this.getMap = e, this.getAnnotationSource = t, this.stopInteractions = n, this.onDrawEnd = s, this.interaction = null;
  }
  start(e, t = {}) {
    var c, h2, u;
    const n = (c = this.getMap) == null ? void 0 : c.call(this), s = (h2 = this.getAnnotationSource) == null ? void 0 : h2.call(this);
    if (!n || !s)
      return false;
    (u = this.stopInteractions) == null || u.call(this);
    const r = t.drawShape || "custom", o = yy(e, r, t.category), a = {
      source: s,
      type: o
    };
    r === "rectangle" ? a.geometryFunction = gy() : (r === "circle" || e === "circle") && (a.geometryFunction = fy(64));
    const l = new Rh(a);
    return l.on("drawend", (d) => {
      var y;
      const f = d.feature, g = _y(f.getGeometry());
      s.removeFeature(f), (y = this.onDrawEnd) == null || y.call(this, {
        ...g,
        name: t.name || "Annotation",
        type: e,
        shape: r === "custom" && e === "circle" ? "circle" : r,
        category: t.category || (e === "noFlyZone" ? "noFlyZone" : "basic"),
        text: t.text || ""
      });
    }), this.interaction = l, n.addInteraction(l), n.getTargetElement().style.cursor = "crosshair", true;
  }
  stop() {
    var t;
    const e = (t = this.getMap) == null ? void 0 : t.call(this);
    e && this.interaction && e.removeInteraction(this.interaction), this.interaction = null;
  }
};
var Sy = (i) => i ? i.endsWith("/") ? i : `${i}/` : "";
var Le = {
  assetBaseUrl: "",
  tileUrlTemplate: "",
  modelIconResolver: null,
  templateIconResolver: null,
  annotationPointIconUrl: ""
};
var Lh = (i) => (i && (Object.prototype.hasOwnProperty.call(i, "assetBaseUrl") && (Le.assetBaseUrl = Sy(i.assetBaseUrl || "")), Object.prototype.hasOwnProperty.call(i, "tileUrlTemplate") && (Le.tileUrlTemplate = i.tileUrlTemplate || ""), Object.prototype.hasOwnProperty.call(i, "modelIconResolver") && (Le.modelIconResolver = typeof i.modelIconResolver == "function" ? i.modelIconResolver : null), Object.prototype.hasOwnProperty.call(i, "templateIconResolver") && (Le.templateIconResolver = typeof i.templateIconResolver == "function" ? i.templateIconResolver : null), Object.prototype.hasOwnProperty.call(i, "annotationPointIconUrl") && (Le.annotationPointIconUrl = i.annotationPointIconUrl || "")), Po());
var Po = () => ({
  ...Le
});
var Ey = () => Le.tileUrlTemplate || "";
var wy = () => Le.annotationPointIconUrl ? Le.annotationPointIconUrl : Le.assetBaseUrl ? `${Le.assetBaseUrl}vite.svg` : "";
var Cy = (i) => {
  if (typeof (i == null ? void 0 : i.iconUrl) == "string" && i.iconUrl)
    return i.iconUrl;
  if (Le.modelIconResolver) {
    const e = Le.modelIconResolver(i);
    if (e)
      return e;
  }
  if (Le.assetBaseUrl) {
    const e = (i == null ? void 0 : i.iconKey) || (i == null ? void 0 : i.NodeType) || "default", t = (i == null ? void 0 : i.nation) || "red";
    return `${Le.assetBaseUrl}two/images/${t}/${e}.png`;
  }
  return "";
};
var aE = (i) => {
  if (typeof (i == null ? void 0 : i.templateIconUrl) == "string" && i.templateIconUrl)
    return i.templateIconUrl;
  if (Le.templateIconResolver) {
    const e = Le.templateIconResolver(i);
    if (e)
      return e;
  }
  if (Le.assetBaseUrl) {
    const e = (i == null ? void 0 : i.templateIcon) || (i == null ? void 0 : i.NodeType) || "default";
    return `${Le.assetBaseUrl}two/images/white/${e}.png`;
  }
  return "";
};
var xy = (i) => {
  const e = i.type === "text" ? i.text || i.name || "" : i.category === "basic" ? "" : i.text || i.name || "", t = i.category === "noFlyZone" ? "rgba(255, 128, 64, 0.95)" : i.category === "military" ? "rgba(255, 90, 90, 0.95)" : "rgba(36, 184, 255, 0.95)", n = i.category === "noFlyZone" ? "rgba(255, 128, 64, 0.15)" : i.category === "military" ? "rgba(255, 90, 90, 0.12)" : "rgba(36, 184, 255, 0.12)", s = wy(), r = i.type === "point" || i.type === "text", o = s ? new yn({
    src: s,
    scale: 0.2,
    opacity: r ? 0.9 : 0
  }) : new we({
    radius: r ? 6 : 0.1,
    fill: new J({ color: t }),
    stroke: new se({ color: "#ffffff", width: 1.5 })
  });
  return new te({
    stroke: new se({
      color: t,
      width: 2,
      lineDash: i.type === "arrow" ? [10, 8] : void 0
    }),
    fill: new J({ color: n }),
    image: o,
    text: new zn({
      text: e,
      offsetY: -16,
      padding: [3, 6, 3, 6],
      backgroundFill: new J({ color: "rgba(9, 18, 30, 0.72)" }),
      fill: new J({ color: "#f8fbff" }),
      stroke: new se({ color: "rgba(0,0,0,0.65)", width: 3 })
    })
  });
};
var Iy = (i, e) => {
  var t, n, s;
  return ((t = i.geometry) == null ? void 0 : t.type) === "point" ? new he(e(
    i.geometry.coordinates[0],
    i.geometry.coordinates[1]
  )) : ((n = i.geometry) == null ? void 0 : n.type) === "line" ? new tt(
    i.geometry.coordinates.map((r) => e(r[0], r[1]))
  ) : ((s = i.geometry) == null ? void 0 : s.type) === "polygon" ? new at([
    i.geometry.coordinates.map((r) => e(r[0], r[1]))
  ]) : null;
};
var vy = class {
  constructor({ getAnnotationLayer: e, coordinateFromLonLat: t }) {
    this.getAnnotationLayer = e, this.coordinateFromLonLat = t;
  }
  render(e = []) {
    var n, s;
    const t = (s = (n = this.getAnnotationLayer()) == null ? void 0 : n.getSource) == null ? void 0 : s.call(n);
    t && (t.clear(), e.forEach((r) => {
      const o = this.createFeature(r);
      o && t.addFeature(o);
    }));
  }
  createFeature(e) {
    const t = Iy(e, this.coordinateFromLonLat);
    if (!t)
      return null;
    const n = new ae({
      geometry: t,
      annotationId: e.id
    });
    return n.setStyle(xy(e)), n;
  }
};
var My = (i) => JSON.parse(JSON.stringify(i));
var Ty = () => (/* @__PURE__ */ new Date()).toISOString();
var Fh = () => ({
  name: "Joint Scenario",
  description: "Scenario editor built with OpenLayers",
  author: "Codex",
  updatedAt: Ty()
});
var Dh = () => ({
  playing: false,
  speed: 1,
  progress: 0,
  duration: 120,
  elapsed: 0,
  currentTimeLabel: "T+00:00",
  maxTrailPoints: 4e3
});
var by = {
  target: {
    color: "rgba(255, 185, 80, 0.95)",
    width: 3,
    lineType: "flowDash"
  }
};
var Oi = () => ({
  mode: "edit",
  metadata: Fh(),
  models: [],
  relations: [],
  effects: [],
  annotations: [],
  selectionModelUID: null,
  selectionAnnotationId: null,
  display: {
    showPopup: true,
    showModelName: true,
    showTargetLine: false,
    targetLineStyle: {
      redColor: "#ffb950",
      blueColor: "#60d2ff",
      width: 3,
      lineType: "flowDash"
    },
    relationLineStyles: My(by),
    headingVisible: {
      red: false,
      blue: false
    },
    effectVisible: {},
    effectStyles: {},
    performance: {
      level: "quality"
    }
  },
  permissions: {
    editable: true,
    role: "planner"
  },
  playback: Dh(),
  validation: []
});
var Se = (i) => JSON.parse(JSON.stringify(i));
var bn = (i, e = 0) => {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
};
var Gn = () => +`${Date.now()}${Math.floor(Math.random() * 1e3)}`;
var cr = (i, e = "default") => String(i || e);
var Ph = (i, e = "default") => {
  const t = i.id ?? Gn(), n = i.UID ?? Gn(), s = i.name || i.showName || `payload_${t}`, r = i.showName || s;
  return {
    id: t,
    UID: n,
    name: s,
    showName: r,
    NodeType: i.NodeType || "default",
    iconKey: i.iconKey || i.NodeType || "default",
    templateIcon: i.templateIcon || i.NodeType || "default",
    modelType: i.modelType || i.NodeType || "PAYLOAD",
    description: i.description || "",
    nation: cr(i.nation, e),
    side: cr(i.side || i.nation, e),
    status: i.status || "mounted",
    mountable: i.mountable ?? true,
    meta: Se(i.meta || {})
  };
};
var Ni = (i) => {
  var d, f, g;
  const e = i.id ?? Gn(), t = i.UID ?? Gn(), n = i.name || i.showName || `unit_${e}`, s = i.showName || n, r = cr(i.nation || i.side), o = bn(i.height, 0), a = bn(i.speed, 0), l = bn(i.heading, 0), c = bn(i.lon, 116.394926), h2 = bn(i.lat, 39.9125), u = {
    name: ((d = i.routeMeta) == null ? void 0 : d.name) || `${s}_route`,
    speed: bn(((f = i.routeMeta) == null ? void 0 : f.speed) ?? a, a),
    altitude: bn(((g = i.routeMeta) == null ? void 0 : g.altitude) ?? o, o)
  };
  return {
    id: e,
    UID: t,
    name: n,
    showName: s,
    nation: r,
    NodeType: i.NodeType || "default",
    iconKey: i.iconKey || i.NodeType || "default",
    templateIcon: i.templateIcon || i.NodeType || "default",
    modelType: i.modelType || i.NodeType || "PLATFORM",
    side: cr(i.side || r),
    templateCategory: i.templateCategory || "platform",
    mountable: i.mountable ?? false,
    description: i.description || "",
    lon: c,
    lat: h2,
    height: o,
    speed: a,
    heading: l,
    targetUID: i.targetUID ?? null,
    status: i.status || "normal",
    visible: i.visible ?? true,
    line: Array.isArray(i.line) ? Se(i.line) : [],
    routeMeta: u,
    mounts: Array.isArray(i.mounts) ? i.mounts.map((y) => Ph(y, r)) : [],
    tags: Array.isArray(i.tags) ? Se(i.tags) : [],
    meta: Se(i.meta || {})
  };
};
var to = (i) => ({
  id: i.id || `${i.type || "target"}_${i.sourceUID}_${i.targetUID}`,
  type: i.type || "target",
  sourceUID: i.sourceUID ?? i.fromUID ?? i.from ?? null,
  targetUID: i.targetUID ?? i.toUID ?? i.to ?? null,
  visible: i.visible ?? true,
  label: i.label || "",
  style: Se(i.style || {})
});
var Os = (i) => ({
  id: i.id || `${i.type || "effect"}_${i.sourceUID || i.uid || Gn()}`,
  type: i.type || "radar",
  sourceUID: i.sourceUID ?? i.uid ?? null,
  visible: i.visible ?? true,
  geometry: Se(i.geometry || {}),
  style: Se(i.style || {}),
  label: i.label || "",
  meta: Se(i.meta || {})
});
var Oa = () => {
  const i = () => (/* @__PURE__ */ new Date()).toISOString(), e = {
    mountCompatibility: {},
    mountValidator: null,
    validators: [],
    defaultEffectPresets: {}
  }, t = ref([]), n = ref([]), s = ref({}), r = ref([]), o = reactive(Oi()), a = reactive({
    undoStack: [],
    redoStack: []
  });
  let l = false;
  const c = (C) => String(C).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), h2 = (C, T) => {
    if (!C || !T)
      return null;
    const D = C.match(
      new RegExp(`^${c(T)}_(\\d+)$`)
    );
    return D ? Number(D[1]) : null;
  }, u = () => {
    const C = [];
    return o.models.forEach((T) => {
      T != null && T.showName && C.push(T.showName), ((T == null ? void 0 : T.mounts) || []).forEach((D) => {
        D != null && D.showName && C.push(D.showName);
      });
    }), C;
  }, d = (C) => {
    const T = String(C || "").trim();
    if (!T)
      return `模型_${Date.now()}`;
    let D = 0;
    return u().forEach((A) => {
      const k = h2(A, T);
      k && (D = Math.max(D, k));
    }), `${T}_${D + 1}`;
  }, f = () => {
    t.value = o.models.filter((C) => C.nation === "red"), n.value = o.models.filter((C) => C.nation === "blue"), s.value = o.models.reduce((C, T) => {
      const D = String(T.nation || T.side || "default");
      return C[D] = C[D] || [], C[D].push(T), C;
    }, {});
  }, g = (C) => (r.value = Array.isArray(C) ? Se(C) : [], r.value), y = (C) => (e.mountCompatibility = Se(C || {}), Se(e.mountCompatibility)), m = (C) => (e.mountValidator = typeof C == "function" ? C : null, e.mountValidator), _ = (C) => (e.validators = Array.isArray(C) ? C.filter((T) => typeof T == "function") : [], v(), e.validators), p = (C) => (e.defaultEffectPresets = Se(C || {}), Se(e.defaultEffectPresets)), E = (C = {}) => (Object.prototype.hasOwnProperty.call(C, "templates") && g(C.templates), Object.prototype.hasOwnProperty.call(C, "mountCompatibility") && y(C.mountCompatibility), Object.prototype.hasOwnProperty.call(C, "mountValidator") && m(C.mountValidator), Object.prototype.hasOwnProperty.call(C, "validators") && _(C.validators), Object.prototype.hasOwnProperty.call(C, "defaultEffectPresets") && p(C.defaultEffectPresets), {
    templates: r.value,
    mountCompatibility: Se(e.mountCompatibility),
    validators: e.validators,
    defaultEffectPresets: Se(e.defaultEffectPresets)
  }), S = () => {
    o.metadata.updatedAt = i();
  }, w = () => Se({
    mode: o.mode,
    metadata: o.metadata,
    models: o.models,
    relations: o.relations,
    effects: o.effects,
    annotations: o.annotations,
    selectionModelUID: o.selectionModelUID,
    selectionAnnotationId: o.selectionAnnotationId,
    display: o.display,
    permissions: o.permissions,
    playback: {
      ...o.playback,
      playing: false,
      progress: o.playback.progress
    }
  }), x = (C, T = {}) => {
    const D = String((C == null ? void 0 : C.NodeType) || (C == null ? void 0 : C.modelType) || "").toLowerCase(), A = e.defaultEffectPresets || {}, k = T.type || D, W = A[k] || A[D];
    return !W || !(C != null && C.UID) ? null : Os({
      id: `${W.type}_${C.UID}`,
      sourceUID: C.UID,
      ...W,
      ...Se(T),
      geometry: {
        ...W.geometry || {},
        ...T.geometry || {}
      },
      style: {
        ...W.style || {},
        ...T.style || {}
      }
    });
  }, b = (C, T) => {
    if (!(C != null && C.mountable) || !T)
      return false;
    if (e.mountValidator)
      return e.mountValidator(C, T, o) !== false;
    const D = T.modelType || T.NodeType, A = C.modelType || C.NodeType;
    return (e.mountCompatibility[D] || []).includes(A);
  }, v = () => {
    const C = [], T = /* @__PURE__ */ new Set();
    return o.models.forEach((D) => {
      var k;
      const A = (k = D.showName) == null ? void 0 : k.trim();
      A && (T.has(A) ? C.push({
        level: "error",
        code: "DUPLICATE_NAME",
        message: `Duplicate model name: ${A}`
      }) : T.add(A)), (Math.abs(Number(D.lon)) > 180 || Math.abs(Number(D.lat)) > 90) && C.push({
        level: "error",
        code: "INVALID_COORDINATE",
        message: `${D.showName} has out-of-range coordinates`
      }), (D.mounts || []).forEach((W) => {
        var L;
        const I = (L = W.showName) == null ? void 0 : L.trim();
        if (I) {
          if (T.has(I)) {
            C.push({
              level: "error",
              code: "DUPLICATE_NAME",
              message: `Duplicate model name: ${I}`
            });
            return;
          }
          T.add(I);
        }
      });
    }), e.validators.forEach((D) => {
      const A = D(o);
      Array.isArray(A) ? C.push(...A.filter(Boolean)) : A && C.push(A);
    }), o.validation = C, C;
  }, R = (C) => {
    l || (a.undoStack.push({
      label: C,
      snapshot: w()
    }), a.undoStack.length > 80 && a.undoStack.shift(), a.redoStack = []);
  }, P = (C) => {
    var D, A, k, W, I, L, B, re, ce, Ie, Ae, rt, lt;
    l = true;
    const T = Se(C || Oi());
    o.mode = T.mode || "edit", o.metadata = {
      ...Fh(),
      ...T.metadata || {}
    }, o.models = Array.isArray(T.models) ? T.models.map(Ni) : [], o.relations = Array.isArray(T.relations) ? T.relations.map(to) : [], o.effects = Array.isArray(T.effects) ? T.effects.map(Os) : [], o.annotations = Array.isArray(T.annotations) ? T.annotations.map((Qt) => Se(Qt)) : [], o.selectionModelUID = T.selectionModelUID || null, o.selectionAnnotationId = T.selectionAnnotationId || null, o.display = {
      showPopup: ((D = T.display) == null ? void 0 : D.showPopup) ?? true,
      showModelName: ((A = T.display) == null ? void 0 : A.showModelName) ?? true,
      showTargetLine: ((k = T.display) == null ? void 0 : k.showTargetLine) ?? false,
      targetLineStyle: {
        redColor: ((I = (W = T.display) == null ? void 0 : W.targetLineStyle) == null ? void 0 : I.color) || "#ffb950",
        blueColor: ((B = (L = T.display) == null ? void 0 : L.targetLineStyle) == null ? void 0 : B.color) || "#60d2ff",
        width: 3,
        lineType: "flowDash",
        ...((re = T.display) == null ? void 0 : re.targetLineStyle) || {}
      },
      relationLineStyles: {
        ...Oi().display.relationLineStyles || {},
        ...((ce = T.display) == null ? void 0 : ce.relationLineStyles) || {}
      },
      headingVisible: {
        red: false,
        blue: false,
        ...((Ie = T.display) == null ? void 0 : Ie.headingVisible) || {}
      },
      effectVisible: {
        ...((Ae = T.display) == null ? void 0 : Ae.effectVisible) || {}
      },
      effectStyles: {
        ...((rt = T.display) == null ? void 0 : rt.effectStyles) || {}
      },
      performance: {
        level: "quality",
        ...((lt = T.display) == null ? void 0 : lt.performance) || {}
      }
    }, o.permissions = {
      editable: true,
      role: "planner",
      ...T.permissions || {}
    }, o.playback = {
      ...Dh(),
      ...T.playback || {},
      playing: false
    }, f(), S(), v(), l = false;
  }, U = (C, T = "Load Scene") => {
    R(T), P(C);
  }, N = () => {
    R("Reset Scene"), P(Oi());
  }, O = (C, T = {}) => Ni({
    ...Se(C),
    ...Se(T),
    id: T.id ?? Gn(),
    UID: T.UID ?? Gn()
  }), G = (C) => {
    o.selectionModelUID = C || null;
  }, $ = (C) => {
    o.selectionAnnotationId = C || null;
  }, z = (C, { recordHistory: T = true } = {}) => {
    const D = Ni(C);
    return T && R(`Add Model ${D.showName}`), o.models.push(D), f(), S(), v(), D;
  }, H = (C, T, { recordHistory: D = true, force: A = false } = {}) => {
    const k = o.models.find((B) => B.UID === C);
    if (!k || !A && !b(T, k))
      return null;
    const W = T.name || T.showName || "挂载", I = d(W);
    D && R(`Mount ${I} to ${k.showName}`);
    const L = Ph({
      ...Se(T),
      name: W,
      showName: I,
      nation: k.nation
    }, k.nation);
    return k.mounts = [...k.mounts || [], L], S(), v(), {
      host: k,
      mount: L
    };
  }, V = (C, T, { recordHistory: D = true } = {}) => {
    const A = o.models.find((W) => W.UID === C);
    if (!A)
      return null;
    const k = (A.mounts || []).find((W) => W.UID === T);
    return k ? (D && R(`Unmount ${k.showName} from ${A.showName}`), A.mounts = (A.mounts || []).filter((W) => W.UID !== T), S(), v(), {
      host: A,
      mount: k
    }) : null;
  }, q = (C, T, { recordHistory: D = true } = {}) => {
    const A = o.models.find((k) => k.UID === C);
    return A ? (D && R(`Update Model ${A.showName}`), Object.assign(A, Ni({
      ...A,
      ...Se(T),
      UID: A.UID,
      id: A.id
    })), f(), S(), v(), A) : null;
  }, ne = (C = [], { recordHistory: T = true, label: D = "Batch Update Models" } = {}) => {
    if (!Array.isArray(C) || !C.length)
      return [];
    const A = new Map(
      C.filter((W) => (W == null ? void 0 : W.uid) !== void 0 && (W == null ? void 0 : W.uid) !== null).map((W) => [String(W.uid), Se(W.patch || {})])
    );
    if (!A.size)
      return [];
    T && R(D);
    const k = [];
    return o.models.forEach((W) => {
      const I = A.get(String(W.UID));
      I && (Object.assign(W, Ni({
        ...W,
        ...I,
        UID: W.UID,
        id: W.id
      })), k.push(W));
    }), f(), S(), v(), k;
  }, fe = (C) => {
    const T = ["edit", "view", "playback"].includes(C) ? C : "edit";
    return o.mode = T, o.permissions = {
      ...o.permissions,
      editable: T === "edit"
    }, S(), o.mode;
  }, F = (C = []) => {
    const T = Array.isArray(C) ? C : [C], D = [];
    return T.filter(Boolean).forEach((A) => {
      const k = to(A);
      if (!k.sourceUID || !k.targetUID)
        return;
      const W = o.relations.findIndex((I) => String(I.id) === String(k.id));
      W >= 0 ? o.relations[W] = k : o.relations.push(k), D.push(k);
    }), D.length && S(), D;
  }, Te = (C) => {
    const T = Array.isArray(C) ? C : [C], D = new Set(T.map((k) => String(k)));
    if (!D.size)
      return [];
    const A = o.relations.filter((k) => D.has(String(k.id)));
    return o.relations = o.relations.filter((k) => !D.has(String(k.id))), A.length && S(), A;
  }, Me = (C = []) => {
    const T = Array.isArray(C) ? C : [C], D = [];
    return T.filter(Boolean).forEach((A) => {
      const k = Os(A);
      if (!k.sourceUID)
        return;
      const W = o.effects.findIndex((I) => String(I.id) === String(k.id));
      W >= 0 ? o.effects[W] = k : o.effects.push(k), D.push(k);
    }), D.length && S(), D;
  }, Ce = (C) => {
    const T = Array.isArray(C) ? C : [C], D = new Set(T.map((k) => String(k)));
    if (!D.size)
      return [];
    const A = o.effects.filter((k) => D.has(String(k.id)));
    return o.effects = o.effects.filter((k) => !D.has(String(k.id))), A.length && S(), A;
  }, Oe = (C, { recordHistory: T = true } = {}) => {
    const D = o.models.findIndex((k) => k.UID === C);
    if (D < 0)
      return null;
    const A = o.models[D];
    return T && R(`Delete Model ${A.showName}`), o.models.splice(D, 1), o.relations = o.relations.filter(
      (k) => String(k.sourceUID) !== String(C) && String(k.targetUID) !== String(C)
    ), o.effects = o.effects.filter((k) => String(k.sourceUID) !== String(C)), o.models.forEach((k) => {
      String(k.targetUID) === String(C) && (k.targetUID = null);
    }), o.selectionModelUID === C && (o.selectionModelUID = null), f(), S(), v(), A;
  }, be = (C = [], { recordHistory: T = true } = {}) => {
    const D = new Set(C.map((A) => String(A)));
    D.size && (T && R("Batch Delete Models"), o.models = o.models.filter((A) => !D.has(String(A.UID))), o.relations = o.relations.filter(
      (A) => !D.has(String(A.sourceUID)) && !D.has(String(A.targetUID))
    ), o.effects = o.effects.filter((A) => !D.has(String(A.sourceUID))), o.models.forEach((A) => {
      D.has(String(A.targetUID)) && (A.targetUID = null);
    }), o.selectionModelUID && D.has(String(o.selectionModelUID)) && (o.selectionModelUID = null), f(), S(), v());
  }, it = (C, T, D = {}, { recordHistory: A = true } = {}) => {
    const k = o.models.find((W) => W.UID === C);
    return k ? (A && R(`Update Route ${k.showName}`), k.line = Array.isArray(T) ? Se(T) : [], k.routeMeta = {
      ...k.routeMeta,
      ...Se(D)
    }, S(), v(), k) : null;
  }, He = (C, { recordHistory: T = true } = {}) => {
    var A;
    const D = {
      id: C.id || `anno-${Date.now()}-${Math.floor(Math.random() * 1e3)}`,
      name: C.name || "Unnamed Annotation",
      type: C.type || "point",
      shape: C.shape || (((A = C.geometry) == null ? void 0 : A.type) === "polygon" ? "custom" : C.type || "point"),
      category: C.category || "basic",
      text: C.text || "",
      style: Se(C.style || {}),
      geometry: Se(C.geometry || { type: "point", coordinates: [] })
    };
    return T && R(`Add Annotation ${D.name}`), o.annotations.push(D), S(), v(), D;
  }, Ne = (C, T, { recordHistory: D = true } = {}) => {
    const A = o.annotations.find((k) => k.id === C);
    return A ? (D && R(`Update Annotation ${A.name}`), Object.assign(A, Se(T)), S(), v(), A) : null;
  }, Cn = (C, { recordHistory: T = true } = {}) => {
    const D = o.annotations.findIndex((k) => k.id === C);
    if (D < 0)
      return null;
    const A = o.annotations[D];
    return T && R(`Delete Annotation ${A.name}`), o.annotations.splice(D, 1), o.selectionAnnotationId === C && (o.selectionAnnotationId = null), S(), v(), A;
  }, Wn = () => {
    const C = a.undoStack.pop();
    return C ? (a.redoStack.push({
      label: "redo",
      snapshot: w()
    }), P(C.snapshot), true) : false;
  }, Jt = () => {
    const C = a.redoStack.pop();
    return C ? (a.undoStack.push({
      label: "undo",
      snapshot: w()
    }), P(C.snapshot), true) : false;
  }, Tt = computed(
    () => o.models.find((C) => C.UID === o.selectionModelUID) || null
  ), We = computed(() => ({
    modelCount: o.models.length,
    redCount: t.value.length,
    blueCount: n.value.length,
    annotationCount: o.annotations.length,
    issueCount: o.validation.length
  })), st = () => w();
  return P(Oi()), {
    redModelList: t,
    blueModelList: n,
    modelGroups: s,
    modelList: r,
    selectedModel: Tt,
    sceneStatistics: We,
    sceneState: o,
    historyState: a,
    sceneRuntimeConfig: e,
    hydrateRelation: to,
    hydrateEffect: Os,
    createDefaultEffectForModel: x,
    canMountTemplateOnModel: b,
    validateScene: v,
    setSceneTemplates: g,
    setMountCompatibility: y,
    setMountValidator: m,
    setSceneValidators: _,
    setDefaultEffectPresets: p,
    configureSceneRuntime: E,
    restoreScene: P,
    loadSceneData: U,
    resetScene: N,
    createModelFromTemplate: O,
    setSelectedModelUID: G,
    setSelectedAnnotationId: $,
    addModelToScene: z,
    addMountToModel: H,
    removeMountFromModel: V,
    updateModelInScene: q,
    batchUpdateModelsInScene: ne,
    setSceneMode: fe,
    upsertRelationsInScene: F,
    removeRelationsFromScene: Te,
    upsertEffectsInScene: Me,
    removeEffectsFromScene: Ce,
    removeModelFromScene: Oe,
    removeModelsFromScene: be,
    setModelRoute: it,
    addAnnotationToScene: He,
    updateAnnotationInScene: Ne,
    removeAnnotationFromScene: Cn,
    undoSceneChange: Wn,
    redoSceneChange: Jt,
    exportScenePayload: st,
    generateSceneUniqueName: d
  };
};
var Y = Oa();
var Ry = Y.redModelList;
var Ly = Y.blueModelList;
var Fy = Y.modelGroups;
var Ah = Y.modelList;
var Dy = Y.selectedModel;
var Py = Y.sceneStatistics;
var kh = Y.sceneState;
var Ay = Y.historyState;
var ky = Y.sceneRuntimeConfig;
var Oy = (...i) => Y.hydrateRelation(...i);
var Ny = (...i) => Y.hydrateEffect(...i);
var Uy = (...i) => Y.createDefaultEffectForModel(...i);
var Oh = (...i) => Y.canMountTemplateOnModel(...i);
var Gy = (...i) => Y.validateScene(...i);
var Nh = (...i) => Y.setSceneTemplates(...i);
var Uh = (...i) => Y.setMountCompatibility(...i);
var Gh = (...i) => Y.setMountValidator(...i);
var Bh = (...i) => Y.setSceneValidators(...i);
var zh = (...i) => Y.setDefaultEffectPresets(...i);
var Vh = (...i) => Y.configureSceneRuntime(...i);
var By = (...i) => Y.restoreScene(...i);
var zy = (...i) => Y.loadSceneData(...i);
var Vy = (...i) => Y.resetScene(...i);
var Xy = (...i) => Y.createModelFromTemplate(...i);
var Wy = (...i) => Y.setSelectedModelUID(...i);
var Yy = (...i) => Y.setSelectedAnnotationId(...i);
var jy = (...i) => Y.addModelToScene(...i);
var $y = (...i) => Y.addMountToModel(...i);
var Ky = (...i) => Y.removeMountFromModel(...i);
var Hy = (...i) => Y.updateModelInScene(...i);
var Zy = (...i) => Y.batchUpdateModelsInScene(...i);
var qy = (...i) => Y.setSceneMode(...i);
var Jy = (...i) => Y.upsertRelationsInScene(...i);
var Qy = (...i) => Y.removeRelationsFromScene(...i);
var e_ = (...i) => Y.upsertEffectsInScene(...i);
var t_ = (...i) => Y.removeEffectsFromScene(...i);
var n_ = (...i) => Y.removeModelFromScene(...i);
var i_ = (...i) => Y.removeModelsFromScene(...i);
var s_ = (...i) => Y.setModelRoute(...i);
var r_ = (...i) => Y.addAnnotationToScene(...i);
var o_ = (...i) => Y.updateAnnotationInScene(...i);
var a_ = (...i) => Y.removeAnnotationFromScene(...i);
var l_ = (...i) => Y.undoSceneChange(...i);
var c_ = (...i) => Y.redoSceneChange(...i);
var h_ = (...i) => Y.exportScenePayload(...i);
var Xh = (...i) => Y.generateSceneUniqueName(...i);
var u_ = Object.freeze(Object.defineProperty({
  __proto__: null,
  addAnnotationToScene: r_,
  addModelToScene: jy,
  addMountToModel: $y,
  batchUpdateModelsInScene: Zy,
  blueModelList: Ly,
  canMountTemplateOnModel: Oh,
  configureSceneRuntime: Vh,
  createDefaultEffectForModel: Uy,
  createModelFromTemplate: Xy,
  createSceneStore: Oa,
  default: Y,
  defaultSceneStore: Y,
  exportScenePayload: h_,
  generateSceneUniqueName: Xh,
  historyState: Ay,
  hydrateEffect: Ny,
  hydrateRelation: Oy,
  loadSceneData: zy,
  modelGroups: Fy,
  modelList: Ah,
  redModelList: Ry,
  redoSceneChange: c_,
  removeAnnotationFromScene: a_,
  removeEffectsFromScene: t_,
  removeModelFromScene: n_,
  removeModelsFromScene: i_,
  removeMountFromModel: Ky,
  removeRelationsFromScene: Qy,
  resetScene: Vy,
  restoreScene: By,
  sceneRuntimeConfig: ky,
  sceneState: kh,
  sceneStatistics: Py,
  selectedModel: Dy,
  setDefaultEffectPresets: zh,
  setModelRoute: s_,
  setMountCompatibility: Uh,
  setMountValidator: Gh,
  setSceneMode: qy,
  setSceneTemplates: Nh,
  setSceneValidators: Bh,
  setSelectedAnnotationId: Yy,
  setSelectedModelUID: Wy,
  undoSceneChange: l_,
  updateAnnotationInScene: o_,
  updateModelInScene: Hy,
  upsertEffectsInScene: e_,
  upsertRelationsInScene: Jy,
  validateScene: Gy
}, Symbol.toStringTag, { value: "Module" }));
var d_ = class {
  constructor({ renderAnnotations: e, store: t = Y } = {}) {
    this.renderAnnotations = e, this.store = t;
  }
  sync(e = {}) {
    var t;
    e.sync !== false && ((t = this.renderAnnotations) == null || t.call(this, this.store.sceneState.annotations));
  }
  async add(e, t = {}) {
    const n = this.store.addAnnotationToScene(e, t);
    return this.sync(t), n;
  }
  async update(e, t, n = {}) {
    const s = this.store.updateAnnotationInScene(e, t, n);
    return s && this.sync(n), s;
  }
  async remove(e, t = {}) {
    const n = this.store.removeAnnotationFromScene(e, t);
    return n && this.sync(t), n;
  }
};
var f_ = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    super({
      stopDown: vi
    }), e = e || {}, this.kinetic_ = e.kinetic, this.lastCentroid = null, this.lastPointersCount_, this.panning_ = false;
    const t = e.condition ? e.condition : bo(wa, jf);
    this.condition_ = e.onFocusOnly ? bo(Hc, t) : t, this.noKinetic_ = false;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(e) {
    const t = e.map;
    this.panning_ || (this.panning_ = true, t.getView().beginInteraction());
    const n = this.targetPointers, s = t.getEventPixel(ka(n));
    if (n.length == this.lastPointersCount_) {
      if (this.kinetic_ && this.kinetic_.update(s[0], s[1]), this.lastCentroid) {
        const r = [
          this.lastCentroid[0] - s[0],
          s[1] - this.lastCentroid[1]
        ], a = e.map.getView();
        id(r, a.getResolution()), Yo(r, a.getRotation()), a.adjustCenterInternal(r);
      }
    } else
      this.kinetic_ && this.kinetic_.begin();
    this.lastCentroid = s, this.lastPointersCount_ = n.length, e.originalEvent.preventDefault();
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    const t = e.map, n = t.getView();
    if (this.targetPointers.length === 0) {
      if (!this.noKinetic_ && this.kinetic_ && this.kinetic_.end()) {
        const s = this.kinetic_.getDistance(), r = this.kinetic_.getAngle(), o = n.getCenterInternal(), a = t.getPixelFromCoordinateInternal(o), l = t.getCoordinateFromPixelInternal([
          a[0] - s * Math.cos(r),
          a[1] - s * Math.sin(r)
        ]);
        n.animateInternal({
          center: n.getConstrainedCenter(l),
          duration: 500,
          easing: Ti
        });
      }
      return this.panning_ && (this.panning_ = false, n.endInteraction()), false;
    }
    return this.kinetic_ && this.kinetic_.begin(), this.lastCentroid = null, true;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    if (this.targetPointers.length > 0 && this.condition_(e)) {
      const n = e.map.getView();
      return this.lastCentroid = null, n.getAnimating() && n.cancelAnimations(), this.kinetic_ && this.kinetic_.begin(), this.noKinetic_ = this.targetPointers.length > 1, true;
    }
    return false;
  }
};
var Na = f_;
var g_ = (i) => {
  var e;
  return i instanceof Na || ((e = i == null ? void 0 : i.constructor) == null ? void 0 : e.name) === "DragPan";
};
var m_ = class {
  constructor({
    getMap: e,
    getEvents: t,
    getRouteEditor: n,
    getFeatureById: s,
    lonLatFromCoordinate: r,
    updateFeatureTransformPreview: o,
    stopInteractions: a
  }) {
    this.getMap = e, this.getEvents = t, this.getRouteEditor = n, this.getFeatureById = s, this.lonLatFromCoordinate = r, this.updateFeatureTransformPreview = o, this.stopInteractions = a, this.state = null, this.listenerKeys = [], this.disabledInteractions = [];
  }
  isActive() {
    return !!this.state;
  }
  restoreSnapshot(e = []) {
    e.forEach((t) => {
      this.updateFeatureTransformPreview(t.feature, {
        coord: t.startCoord,
        lon: t.startLon,
        lat: t.startLat,
        heading: t.startHeading
      });
    });
  }
  disable({ revertPreview: e = true, emitCancel: t = true } = {}) {
    var r, o, a;
    const n = this.state;
    if (!n)
      return;
    e && n.hasPreview && this.restoreSnapshot(n.items), this.listenerKeys.length && (uc(this.listenerKeys), this.listenerKeys = []), this.disabledInteractions.forEach((l) => {
      var c;
      (c = l == null ? void 0 : l.setActive) == null || c.call(l, true);
    }), this.disabledInteractions = [], this.state = null;
    const s = (r = this.getMap) == null ? void 0 : r.call(this);
    (o = s == null ? void 0 : s.getTargetElement) != null && o.call(s) && (s.getTargetElement().style.cursor = ""), t && ((a = n.onCancel) == null || a.call(n));
  }
  createItems(e = []) {
    return (e || []).map((t) => {
      var n;
      return (n = this.getFeatureById) == null ? void 0 : n.call(this, t);
    }).filter(Boolean).map((t) => {
      var o, a;
      const n = (a = (o = t.getGeometry()) == null ? void 0 : o.getCoordinates) == null ? void 0 : a.call(o), [s, r] = this.lonLatFromCoordinate(n);
      return {
        uid: t.get("UID"),
        feature: t,
        startCoord: Array.isArray(n) ? [...n] : n,
        startLon: s,
        startLat: r,
        startHeading: Number(t.get("heading") || 0)
      };
    });
  }
  prepare(e = []) {
    var s, r, o, a, l, c, h2, u, d, f, g, y;
    const t = (s = this.getMap) == null ? void 0 : s.call(this);
    if (!t || !e.length)
      return false;
    this.disable({ revertPreview: true, emitCancel: false }), (r = this.stopInteractions) == null || r.call(this), (l = (a = (o = this.getEvents) == null ? void 0 : o.call(this)) == null ? void 0 : a.disableRouteEdit) == null || l.call(a), (u = (h2 = (c = this.getRouteEditor) == null ? void 0 : c.call(this)) == null ? void 0 : h2.clear) == null || u.call(h2, true);
    const n = e.reduce(
      (m, _) => [m[0] + _.startCoord[0], m[1] + _.startCoord[1]],
      [0, 0]
    ).map((m) => m / e.length);
    return t.getInteractions().forEach((m) => {
      var _;
      g_(m) && ((_ = m.getActive) != null && _.call(m)) && (m.setActive(false), this.disabledInteractions.push(m));
    }), [(f = (d = this.getEvents) == null ? void 0 : d.call(this)) == null ? void 0 : f.iconTranslate, (y = (g = this.getEvents) == null ? void 0 : g.call(this)) == null ? void 0 : y.pointTranslate].forEach((m) => {
      var _;
      (_ = m == null ? void 0 : m.getActive) != null && _.call(m) && (m.setActive(false), this.disabledInteractions.push(m));
    }), n;
  }
  startRotate(e = [], { onCommit: t = null, onCancel: n = null } = {}) {
    var f;
    const s = (f = this.getMap) == null ? void 0 : f.call(this);
    if (!s)
      return false;
    const r = this.createItems(e);
    if (!r.length)
      return false;
    const o = this.prepare(r);
    if (!o)
      return false;
    const a = {
      mode: "rotate",
      items: r,
      center: o,
      onCommit: t,
      onCancel: n,
      dragging: false,
      hasPreview: false,
      startPointerAngle: 0,
      currentAngle: 0
    }, l = (g) => Math.atan2(g[1] - o[1], g[0] - o[0]), c = (g) => {
      a.currentAngle = g, a.hasPreview = true, a.items.forEach((y) => {
        const m = y.startCoord[0] - o[0], _ = y.startCoord[1] - o[1], p = [
          o[0] + m * Math.cos(g) - _ * Math.sin(g),
          o[1] + m * Math.sin(g) + _ * Math.cos(g)
        ], [E, S] = this.lonLatFromCoordinate(p), w = Ka(y.startHeading + g * 180 / Math.PI);
        this.updateFeatureTransformPreview(y.feature, {
          coord: p,
          lon: E,
          lat: S,
          heading: w
        });
      });
    }, h2 = (g) => {
      var y, m, _;
      ((y = g.originalEvent) == null ? void 0 : y.button) === 0 && ((_ = (m = g.originalEvent) == null ? void 0 : m.preventDefault) == null || _.call(m), a.dragging = true, a.hasPreview = false, a.startPointerAngle = l(g.coordinate));
    }, u = (g) => {
      var _, p;
      if (!a.dragging)
        return;
      (p = (_ = g.originalEvent) == null ? void 0 : _.preventDefault) == null || p.call(_);
      const y = l(g.coordinate), m = Pu(a.startPointerAngle - y);
      c(m);
    }, d = async () => {
      if (!a.dragging || (a.dragging = false, !a.hasPreview))
        return;
      const g = a.items.map((y) => ({
        uid: y.uid,
        patch: {
          lon: Number(y.feature.get("lon")),
          lat: Number(y.feature.get("lat")),
          heading: Ka(y.feature.get("heading"))
        }
      }));
      this.disable({ revertPreview: false, emitCancel: false }), await (t == null ? void 0 : t(g, {
        angleDegrees: a.currentAngle * 180 / Math.PI
      }));
    };
    return this.state = a, this.listenerKeys = [
      s.on("pointerdown", h2),
      s.on("pointerdrag", u),
      s.on("pointerup", d)
    ], s.getTargetElement().style.cursor = "grab", true;
  }
  startMove(e = [], { onCommit: t = null, onCancel: n = null } = {}) {
    var d;
    const s = (d = this.getMap) == null ? void 0 : d.call(this);
    if (!s)
      return false;
    const r = this.createItems(e);
    if (!r.length)
      return false;
    const o = this.prepare(r);
    if (!o)
      return false;
    const a = {
      mode: "move",
      items: r,
      center: o,
      onCommit: t,
      onCancel: n,
      dragging: false,
      hasPreview: false,
      startPointerCoord: null,
      delta: [0, 0]
    }, l = (f) => {
      a.delta = f, a.hasPreview = true, a.items.forEach((g) => {
        const y = [
          g.startCoord[0] + f[0],
          g.startCoord[1] + f[1]
        ], [m, _] = this.lonLatFromCoordinate(y);
        this.updateFeatureTransformPreview(g.feature, {
          coord: y,
          lon: m,
          lat: _,
          heading: g.startHeading
        });
      });
    }, c = (f) => {
      var g, y, m;
      ((g = f.originalEvent) == null ? void 0 : g.button) === 0 && ((m = (y = f.originalEvent) == null ? void 0 : y.preventDefault) == null || m.call(y), a.dragging = true, a.hasPreview = false, a.startPointerCoord = [...f.coordinate]);
    }, h2 = (f) => {
      var y, m;
      if (!a.dragging || !a.startPointerCoord)
        return;
      (m = (y = f.originalEvent) == null ? void 0 : y.preventDefault) == null || m.call(y);
      const g = [
        f.coordinate[0] - a.startPointerCoord[0],
        f.coordinate[1] - a.startPointerCoord[1]
      ];
      l(g);
    }, u = async () => {
      if (!a.dragging || (a.dragging = false, !a.hasPreview))
        return;
      const f = a.items.map((g) => ({
        uid: g.uid,
        patch: {
          lon: Number(g.feature.get("lon")),
          lat: Number(g.feature.get("lat"))
        }
      }));
      this.disable({ revertPreview: false, emitCancel: false }), await (t == null ? void 0 : t(f, {
        delta: [...a.delta]
      }));
    };
    return this.state = a, this.listenerKeys = [
      s.on("pointerdown", c),
      s.on("pointerdrag", h2),
      s.on("pointerup", u)
    ], s.getTargetElement().style.cursor = "move", true;
  }
  reset() {
    this.state = null, this.listenerKeys = [], this.disabledInteractions = [];
  }
};
var y_ = class extends gr {
  /**
   * @param {string} className CSS class name.
   */
  constructor(e) {
    super(), this.geometry_ = null, this.element_ = document.createElement("div"), this.element_.style.position = "absolute", this.element_.style.pointerEvents = "auto", this.element_.className = "ol-box " + e, this.map_ = null, this.startPixel_ = null, this.endPixel_ = null;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.setMap(null);
  }
  /**
   * @private
   */
  render_() {
    const e = this.startPixel_, t = this.endPixel_, n = "px", s = this.element_.style;
    s.left = Math.min(e[0], t[0]) + n, s.top = Math.min(e[1], t[1]) + n, s.width = Math.abs(t[0] - e[0]) + n, s.height = Math.abs(t[1] - e[1]) + n;
  }
  /**
   * @param {import("../Map.js").default|null} map Map.
   */
  setMap(e) {
    if (this.map_) {
      this.map_.getOverlayContainer().removeChild(this.element_);
      const t = this.element_.style;
      t.left = "inherit", t.top = "inherit", t.width = "inherit", t.height = "inherit";
    }
    this.map_ = e, this.map_ && this.map_.getOverlayContainer().appendChild(this.element_);
  }
  /**
   * @param {import("../pixel.js").Pixel} startPixel Start pixel.
   * @param {import("../pixel.js").Pixel} endPixel End pixel.
   */
  setPixels(e, t) {
    this.startPixel_ = e, this.endPixel_ = t, this.createOrUpdateGeometry(), this.render_();
  }
  /**
   * Creates or updates the cached geometry.
   */
  createOrUpdateGeometry() {
    if (!this.map_)
      return;
    const e = this.startPixel_, t = this.endPixel_, s = [
      e,
      [e[0], t[1]],
      t,
      [t[0], e[1]]
    ].map(
      this.map_.getCoordinateFromPixelInternal,
      this.map_
    );
    s[4] = s[0].slice(), this.geometry_ ? this.geometry_.setCoordinates([s]) : this.geometry_ = new at([s]);
  }
  /**
   * @return {import("../geom/Polygon.js").default} Geometry.
   */
  getGeometry() {
    return this.geometry_;
  }
};
var __ = y_;
var Zn = {
  /**
   * Triggered upon drag box start.
   * @event DragBoxEvent#boxstart
   * @api
   */
  BOXSTART: "boxstart",
  /**
   * Triggered on drag when box is active.
   * @event DragBoxEvent#boxdrag
   * @api
   */
  BOXDRAG: "boxdrag",
  /**
   * Triggered upon drag box end.
   * @event DragBoxEvent#boxend
   * @api
   */
  BOXEND: "boxend",
  /**
   * Triggered upon drag box canceled.
   * @event DragBoxEvent#boxcancel
   * @api
   */
  BOXCANCEL: "boxcancel"
};
var Ui = class extends It {
  /**
   * @param {string} type The event type.
   * @param {import("../coordinate.js").Coordinate} coordinate The event coordinate.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Originating event.
   */
  constructor(e, t, n) {
    super(e), this.coordinate = t, this.mapBrowserEvent = n;
  }
};
var p_ = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, e = e ?? {}, this.box_ = new __(e.className || "ol-dragbox"), this.minArea_ = e.minArea ?? 64, e.onBoxEnd && (this.onBoxEnd = e.onBoxEnd), this.startPixel_ = null, this.condition_ = e.condition ?? Ea, this.boxEndCondition_ = e.boxEndCondition ?? this.defaultBoxEndCondition;
  }
  /**
   * The default condition for determining whether the boxend event
   * should fire.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent The originating MapBrowserEvent
   *     leading to the box end.
   * @param {import("../pixel.js").Pixel} startPixel The starting pixel of the box.
   * @param {import("../pixel.js").Pixel} endPixel The end pixel of the box.
   * @return {boolean} Whether or not the boxend condition should be fired.
   */
  defaultBoxEndCondition(e, t, n) {
    const s = n[0] - t[0], r = n[1] - t[1];
    return s * s + r * r >= this.minArea_;
  }
  /**
   * Returns geometry of last drawn box.
   * @return {import("../geom/Polygon.js").default} Geometry.
   * @api
   */
  getGeometry() {
    return this.box_.getGeometry();
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(e) {
    this.startPixel_ && (this.box_.setPixels(this.startPixel_, e.pixel), this.dispatchEvent(
      new Ui(
        Zn.BOXDRAG,
        e.coordinate,
        e
      )
    ));
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    if (!this.startPixel_)
      return false;
    const t = this.boxEndCondition_(
      e,
      this.startPixel_,
      e.pixel
    );
    return t && this.onBoxEnd(e), this.dispatchEvent(
      new Ui(
        t ? Zn.BOXEND : Zn.BOXCANCEL,
        e.coordinate,
        e
      )
    ), this.box_.setMap(null), this.startPixel_ = null, false;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    return this.condition_(e) ? (this.startPixel_ = e.pixel, this.box_.setMap(e.map), this.box_.setPixels(this.startPixel_, this.startPixel_), this.dispatchEvent(
      new Ui(
        Zn.BOXSTART,
        e.coordinate,
        e
      )
    ), true) : false;
  }
  /**
   * Function to execute just before `onboxend` is fired
   * @param {import("../MapBrowserEvent.js").default} event Event.
   */
  onBoxEnd(e) {
  }
  /**
   * Activate or deactivate the interaction.
   * @param {boolean} active Active.
   * @observable
   * @api
   * @override
   */
  setActive(e) {
    e || (this.box_.setMap(null), this.startPixel_ && (this.dispatchEvent(
      new Ui(Zn.BOXCANCEL, this.startPixel_, null)
    ), this.startPixel_ = null)), super.setActive(e);
  }
  /**
   * @param {import("../Map.js").default|null} map Map.
   * @override
   */
  setMap(e) {
    this.getMap() && (this.box_.setMap(null), this.startPixel_ && (this.dispatchEvent(
      new Ui(Zn.BOXCANCEL, this.startPixel_, null)
    ), this.startPixel_ = null)), super.setMap(e);
  }
};
var Wh = p_;
var S_ = (i) => {
  var e;
  return i instanceof Na || ((e = i == null ? void 0 : i.constructor) == null ? void 0 : e.name) === "DragPan";
};
var E_ = class {
  constructor({ getMap: e, getFeaturesInExtent: t, stopInteractions: n, setBatchSelection: s, selectFeature: r, onSelection: o }) {
    this.getMap = e, this.getFeaturesInExtent = t, this.stopInteractions = n, this.setBatchSelection = s, this.selectFeature = r, this.onSelection = o, this.interaction = null, this.visual = null, this.startPixel = null, this.disabledInteractions = [];
  }
  ensureVisual() {
    var r, o, a;
    const e = (r = this.getMap) == null ? void 0 : r.call(this);
    if (!e)
      return null;
    if ((o = this.visual) != null && o.isConnected)
      return this.visual;
    const t = (a = e.getTargetElement) == null ? void 0 : a.call(e);
    if (!t)
      return null;
    const n = window.getComputedStyle(t).position;
    (!n || n === "static") && (t.style.position = "relative");
    const s = document.createElement("div");
    return s.className = "scenario-box-selection", Object.assign(s.style, {
      position: "absolute",
      left: "0px",
      top: "0px",
      width: "0px",
      height: "0px",
      display: "none",
      pointerEvents: "none",
      zIndex: "9999",
      boxSizing: "border-box",
      border: "2px dashed rgba(76, 196, 255, 0.98)",
      background: "rgba(76, 196, 255, 0.2)",
      boxShadow: "inset 0 0 0 1px rgba(255,255,255,0.28), 0 0 0 1px rgba(76,196,255,0.18), 0 0 22px rgba(76,196,255,0.32)",
      borderRadius: "2px"
    }), t.appendChild(s), this.visual = s, s;
  }
  updateVisual(e, t) {
    const n = this.ensureVisual();
    if (!n || !e || !t)
      return;
    const s = Math.min(e[0], t[0]), r = Math.min(e[1], t[1]), o = Math.abs(t[0] - e[0]), a = Math.abs(t[1] - e[1]);
    n.style.display = "block", n.style.left = `${s}px`, n.style.top = `${r}px`, n.style.width = `${o}px`, n.style.height = `${a}px`;
  }
  hideVisual() {
    this.visual && (this.visual.style.display = "none", this.visual.style.width = "0px", this.visual.style.height = "0px");
  }
  destroyVisual() {
    this.visual && (this.visual.remove(), this.visual = null, this.startPixel = null);
  }
  enable() {
    var n, s;
    const e = (n = this.getMap) == null ? void 0 : n.call(this);
    if (!e || this.interaction)
      return false;
    (s = this.stopInteractions) == null || s.call(this), this.disabledInteractions = [], e.getInteractions().forEach((r) => {
      S_(r) && (r.setActive(false), this.disabledInteractions.push(r));
    });
    const t = new Wh({
      condition: Ea,
      className: "ol-dragbox scenario-box-selection"
    });
    return t.on("boxstart", (r) => {
      var o;
      this.startPixel = [...((o = r.mapBrowserEvent) == null ? void 0 : o.pixel) || [0, 0]], this.updateVisual(this.startPixel, this.startPixel);
    }), t.on("boxdrag", (r) => {
      var a;
      const o = (a = r.mapBrowserEvent) == null ? void 0 : a.pixel;
      !this.startPixel || !o || this.updateVisual(this.startPixel, o);
    }), t.on("boxend", () => {
      var h2, u, d, f;
      this.hideVisual(), this.startPixel = null;
      const r = t.getGeometry(), o = r.getExtent(), l = (((h2 = this.getFeaturesInExtent) == null ? void 0 : h2.call(this, o)) || []).filter((g) => {
        var m, _;
        const y = (_ = (m = g.getGeometry()) == null ? void 0 : m.getCoordinates) == null ? void 0 : _.call(m);
        return y && r.intersectsCoordinate(y);
      }), c = l.map((g) => String(g.get("UID")));
      (u = this.setBatchSelection) == null || u.call(this, c), (d = this.onSelection) == null || d.call(this, {
        uids: c,
        features: l
      }), (f = this.selectFeature) == null || f.call(this, l[0] || null, false);
    }), t.on("boxcancel", () => {
      this.hideVisual(), this.startPixel = null;
    }), e.addInteraction(t), this.interaction = t, e.getTargetElement().style.cursor = "crosshair", true;
  }
  disable() {
    var t;
    const e = (t = this.getMap) == null ? void 0 : t.call(this);
    if (!e) {
      this.interaction = null, this.disabledInteractions = [];
      return;
    }
    this.interaction && (e.removeInteraction(this.interaction), this.interaction = null), this.hideVisual(), this.startPixel = null, this.disabledInteractions.forEach((n) => {
      n.setActive(true);
    }), this.disabledInteractions = [], e.getTargetElement().style.cursor = "";
  }
  destroy() {
    this.disable(), this.destroyVisual();
  }
};
var w_ = class {
  constructor() {
    this.items = [], this.options = { enabled: true }, this.handler = null, this.element = null, this.outsideHandler = null;
  }
  setHandler(e) {
    this.handler = typeof e == "function" ? e : null;
  }
  setItems(e = [], t = {}) {
    this.items = Array.isArray(e) || typeof e == "function" ? e : [], this.options = {
      ...this.options,
      ...t || {}
    };
  }
  normalizeItem(e, t) {
    return typeof e == "function" ? this.normalizeItem(e(t), t) : !e || e.visible === false || typeof e.visible == "function" && e.visible(t) === false ? null : {
      id: e.id || e.key || e.label,
      label: typeof e.label == "function" ? e.label(t) : e.label,
      disabled: typeof e.disabled == "function" ? e.disabled(t) : e.disabled,
      danger: !!e.danger,
      action: e.action || e.onClick || e.handler
    };
  }
  getItems(e) {
    const t = typeof this.items == "function" ? this.items(e) : this.items;
    return (Array.isArray(t) ? t : []).map((n) => this.normalizeItem(n, e)).filter((n) => n == null ? void 0 : n.label);
  }
  ensureElement() {
    if (this.element)
      return this.element;
    const e = document.createElement("div");
    return e.className = "ol-scenario-context-menu", Object.assign(e.style, {
      position: "fixed",
      zIndex: 9999,
      minWidth: "140px",
      padding: "6px",
      borderRadius: "8px",
      background: "rgba(9, 18, 30, 0.96)",
      border: "1px solid rgba(120, 180, 255, 0.35)",
      boxShadow: "0 10px 30px rgba(0,0,0,0.35)",
      color: "#f8fbff",
      fontSize: "13px",
      lineHeight: "1.4",
      display: "none",
      userSelect: "none"
    }), document.body.appendChild(e), this.element = e, this.outsideHandler = (t) => {
      var n;
      (n = this.element) != null && n.contains(t.target) || this.close();
    }, document.addEventListener("mousedown", this.outsideHandler), window.addEventListener("blur", this.outsideHandler), e;
  }
  close() {
    this.element && (this.element.style.display = "none");
  }
  open(e = {}) {
    var o, a, l, c, h2;
    if (((o = this.options) == null ? void 0 : o.enabled) === false)
      return false;
    const t = {
      ...e,
      uid: ((a = e.model) == null ? void 0 : a.UID) || ((c = (l = e.feature) == null ? void 0 : l.get) == null ? void 0 : c.call(l, "UID")) || null,
      close: () => this.close()
    };
    (h2 = this.handler) == null || h2.call(this, t);
    const n = this.getItems(t);
    if (!n.length)
      return false;
    const s = this.ensureElement();
    s.innerHTML = "", n.forEach((u) => {
      const d = document.createElement("button");
      d.type = "button", d.textContent = u.label, d.disabled = !!u.disabled, Object.assign(d.style, {
        display: "block",
        width: "100%",
        border: "none",
        borderRadius: "6px",
        padding: "7px 10px",
        margin: "0",
        textAlign: "left",
        color: u.danger ? "#ff8f8f" : "#f8fbff",
        background: "transparent",
        cursor: u.disabled ? "not-allowed" : "pointer",
        opacity: u.disabled ? "0.48" : "1"
      }), d.onmouseenter = () => {
        d.disabled || (d.style.background = "rgba(80, 160, 255, 0.18)");
      }, d.onmouseleave = () => {
        d.style.background = "transparent";
      }, d.onclick = async () => {
        var f;
        u.disabled || (await ((f = u.action) == null ? void 0 : f.call(u, t, u)), this.close());
      }, s.appendChild(d);
    });
    const r = e.originalEvent;
    return s.style.left = `${Math.min((r == null ? void 0 : r.clientX) || 0, window.innerWidth - 160)}px`, s.style.top = `${Math.min((r == null ? void 0 : r.clientY) || 0, window.innerHeight - n.length * 36 - 16)}px`, s.style.display = "block", true;
  }
  destroy() {
    var e, t;
    this.outsideHandler && (document.removeEventListener("mousedown", this.outsideHandler), window.removeEventListener("blur", this.outsideHandler), this.outsideHandler = null), (t = (e = this.element) == null ? void 0 : e.remove) == null || t.call(e), this.element = null;
  }
};
var C_ = class {
  constructor(e) {
    this.layer = e, this.source = e.getSource();
  }
  /**
   * 播放击毁特效
   * @param {Array} coord - 坐标 [x, y]
   * @param {Object} options - 配置选项
   * @param {string} options.type - 特效类型: 'realistic'(真实爆炸) | 'explosion'(简单爆炸) | 'flash'(闪光) | 'particle'(粒子爆炸) | 'sprite'(序列帧)
   * @param {number} options.duration - 持续时间(ms)
   * @param {string} options.color - 特效颜色
   * @param {string} options.spriteUrl - 序列帧图片URL（仅sprite类型使用）
   * @param {number} options.spriteFrames - 序列帧数量（仅sprite类型使用）
   */
  playDestroyEffect(e, {
    type: t = "realistic",
    duration: n = 1500,
    color: s = "rgba(255, 100, 0, 1)",
    spriteUrl: r = null,
    spriteFrames: o = 16
  } = {}) {
    t === "realistic" ? this._playRealisticExplosion(e, n) : t === "sprite" && r ? this._playSpriteExplosion(e, r, o, n) : t === "explosion" ? this._playExplosionEffect(e, n, s) : t === "flash" ? this._playFlashEffect(e, n, s) : t === "particle" && this._playParticleExplosion(e, n);
  }
  /**
   * 真实爆炸特效 - 最逼真的效果
   */
  _playRealisticExplosion(e, t) {
    this._createInitialFlash(e, t * 0.1), this._createEnhancedFireCore(e, t), this._createEnhancedShockwaves(e, t), this._createEnhancedParticles(e, t), this._createSparks(e, t), this._createEnhancedSmoke(e, t), this._createDebris(e, t);
  }
  /**
   * 初始闪光 - 模拟爆炸瞬间的强光
   */
  _createInitialFlash(e, t) {
    const n = new ae({
      geometry: new he(e)
    }), s = Date.now(), r = 60, o = () => {
      const a = Date.now() - s, l = Math.min(a / t, 1);
      if (l >= 1) {
        this.source.removeFeature(n);
        return;
      }
      const c = r * (l < 0.3 ? l / 0.3 : 1 - (l - 0.3) / 0.7), h2 = 1 - l;
      n.setStyle(
        new te({
          image: new we({
            radius: c,
            fill: new J({
              color: `rgba(255, 255, 255, ${h2})`
            })
          })
        })
      ), requestAnimationFrame(o);
    };
    this.source.addFeature(n), o();
  }
  /**
   * 增强版火焰核心
   */
  _createEnhancedFireCore(e, t) {
    const n = new ae({
      geometry: new he(e)
    }), s = Date.now(), r = 45, o = () => {
      const a = Date.now() - s, l = Math.min(a / t, 1);
      if (l >= 1) {
        this.source.removeFeature(n);
        return;
      }
      let c;
      l < 0.15 ? c = r * (l / 0.15) : l < 0.4 ? c = r * (1 + Math.sin((l - 0.15) * 20) * 0.1) : c = r * (1 - (l - 0.4) / 0.6) * 0.8;
      let h2;
      l < 0.05 ? h2 = `rgba(255, 255, 255, ${1 - l * 10})` : l < 0.15 ? h2 = `rgba(255, 255, 200, ${1 - l * 3})` : l < 0.3 ? h2 = `rgba(255, 220, 100, ${1 - l * 2})` : l < 0.5 ? h2 = `rgba(255, 150, 50, ${1 - l * 1.5})` : l < 0.7 ? h2 = `rgba(255, 100, 0, ${1 - l})` : h2 = `rgba(200, 50, 0, ${1 - l})`, n.setStyle(
        new te({
          image: new we({
            radius: c,
            fill: new J({ color: h2 }),
            stroke: new se({
              color: h2.replace(/[\d.]+\)$/g, `${(1 - l) * 0.5})`),
              width: 3
            })
          })
        })
      ), requestAnimationFrame(o);
    };
    this.source.addFeature(n), o();
  }
  /**
   * 增强版冲击波
   */
  _createEnhancedShockwaves(e, t) {
    [0, 80, 160, 240, 320].forEach((n, s) => {
      setTimeout(() => {
        const r = new ae({
          geometry: new he(e)
        }), o = Date.now(), a = 60 + s * 15, l = () => {
          const c = Date.now() - o, h2 = Math.min(c / (t * 0.5), 1);
          if (h2 >= 1) {
            this.source.removeFeature(r);
            return;
          }
          const u = a * h2, d = (1 - h2) * (0.7 - s * 0.1), f = 255, g = Math.floor(220 - h2 * 120), y = Math.floor(100 - h2 * 100);
          r.setStyle(
            new te({
              image: new we({
                radius: u,
                stroke: new se({
                  color: `rgba(${f}, ${g}, ${y}, ${d})`,
                  width: 4 - s * 0.5
                })
              })
            })
          ), requestAnimationFrame(l);
        };
        this.source.addFeature(r), l();
      }, n);
    });
  }
  /**
   * 增强版粒子（带拖尾效果）
   */
  _createEnhancedParticles(e, t) {
    for (let s = 0; s < 40; s++) {
      const r = Math.PI * 2 * s / 40 + (Math.random() - 0.5) * 0.3, o = 0.4 + Math.random() * 0.6, a = 50 + Math.random() * 30, l = 3 + Math.random() * 4;
      this._createEnhancedParticle(e, r, o, a, l, t);
    }
  }
  /**
   * 创建单个增强粒子
   */
  _createEnhancedParticle(e, t, n, s, r, o) {
    const a = new ae({
      geometry: new he(e)
    }), l = Date.now(), c = o * (0.6 + Math.random() * 0.3), h2 = () => {
      const u = Date.now() - l, d = Math.min(u / c, 1);
      if (d >= 1) {
        this.source.removeFeature(a);
        return;
      }
      const f = s * d * (2 - d), g = Math.cos(t) * f, y = Math.sin(t) * f - d * d * 10;
      a.getGeometry().setCoordinates([
        e[0] + g,
        e[1] + y
      ]);
      const m = r * (1 - d * 0.7), _ = 1 - d;
      let p;
      d < 0.3 ? p = `rgba(255, 255, 150, ${_})` : d < 0.6 ? p = `rgba(255, 180, 50, ${_})` : p = `rgba(255, 80, 0, ${_})`, a.setStyle(
        new te({
          image: new we({
            radius: m,
            fill: new J({ color: p }),
            stroke: new se({
              color: p,
              width: 1
            })
          })
        })
      ), requestAnimationFrame(h2);
    };
    this.source.addFeature(a), h2();
  }
  /**
   * 火花效果
   */
  _createSparks(e, t) {
    for (let s = 0; s < 30; s++)
      setTimeout(() => {
        const r = Math.random() * Math.PI * 2, o = 0.8 + Math.random() * 0.4, a = 60 + Math.random() * 20;
        this._createSingleSpark(e, r, o, a, t * 0.4);
      }, Math.random() * t * 0.2);
  }
  /**
   * 创建单个火花
   */
  _createSingleSpark(e, t, n, s, r) {
    const o = new ae({
      geometry: new he(e)
    }), a = Date.now(), l = () => {
      const c = Date.now() - a, h2 = Math.min(c / r, 1);
      if (h2 >= 1) {
        this.source.removeFeature(o);
        return;
      }
      const u = s * h2, d = Math.cos(t) * u, f = Math.sin(t) * u - h2 * h2 * 15;
      o.getGeometry().setCoordinates([
        e[0] + d,
        e[1] + f
      ]);
      const g = 2 * (1 - h2), y = 1 - h2;
      o.setStyle(
        new te({
          image: new we({
            radius: g,
            fill: new J({
              color: `rgba(255, 255, 200, ${y})`
            })
          })
        })
      ), requestAnimationFrame(l);
    };
    this.source.addFeature(o), l();
  }
  /**
   * 增强版烟雾
   */
  _createEnhancedSmoke(e, t) {
    setTimeout(() => {
      for (let s = 0; s < 12; s++)
        setTimeout(() => {
          const r = new ae({
            geometry: new he(e)
          }), o = Date.now(), a = t * 0.9, l = Math.random() * Math.PI * 2, c = 25 + Math.random() * 20, h2 = 8 + Math.random() * 5, u = () => {
            const d = Date.now() - o, f = Math.min(d / a, 1);
            if (f >= 1) {
              this.source.removeFeature(r);
              return;
            }
            const g = c * f, y = Math.cos(l) * g * 0.4 + Math.sin(f * 10) * 5, m = Math.sin(l) * g * 0.4 + g * 1.5;
            r.getGeometry().setCoordinates([
              e[0] + y,
              e[1] + m
            ]);
            const _ = h2 + 25 * f, p = (1 - f) * (0.5 + Math.random() * 0.2), E = Math.floor(60 + f * 80);
            r.setStyle(
              new te({
                image: new we({
                  radius: _,
                  fill: new J({
                    color: `rgba(${E}, ${E}, ${E}, ${p})`
                  })
                })
              })
            ), requestAnimationFrame(u);
          };
          this.source.addFeature(r), u();
        }, s * 80);
    }, t * 0.25);
  }
  /**
   * 碎片效果
   */
  _createDebris(e, t) {
    for (let s = 0; s < 15; s++) {
      const r = Math.random() * Math.PI * 2, o = 0.3 + Math.random() * 0.5, a = 40 + Math.random() * 25, l = 2 + Math.random() * 3;
      this._createSingleDebris(e, r, o, a, l, t * 0.8);
    }
  }
  /**
   * 创建单个碎片
   */
  _createSingleDebris(e, t, n, s, r, o) {
    const a = new ae({
      geometry: new he(e)
    }), l = Date.now(), c = () => {
      const h2 = Date.now() - l, u = Math.min(h2 / o, 1);
      if (u >= 1) {
        this.source.removeFeature(a);
        return;
      }
      const d = s * u, f = Math.cos(t) * d, g = Math.sin(t) * d - u * u * 20;
      a.getGeometry().setCoordinates([
        e[0] + f,
        e[1] + g
      ]);
      const y = r * (1 - u * 0.5), m = 1 - u;
      a.setStyle(
        new te({
          image: new we({
            radius: y,
            fill: new J({
              color: `rgba(100, 100, 100, ${m})`
            })
          })
        })
      ), requestAnimationFrame(c);
    };
    this.source.addFeature(a), c();
  }
  /**
   * 序列帧爆炸动画
   * @param {Array} coord - 坐标
   * @param {string} spriteUrl - 序列帧图片URL（可以是雪碧图或GIF）
   * @param {number} frames - 帧数
   * @param {number} duration - 持续时间
   */
  _playSpriteExplosion(e, t, n, s) {
    const r = new ae({
      geometry: new he(e)
    }), o = Date.now(), a = () => {
      const l = Date.now() - o;
      if (Math.min(l / s, 1) >= 1) {
        this.source.removeFeature(r);
        return;
      }
      r.setStyle(
        new te({
          image: new yn({
            src: t,
            scale: 1,
            anchor: [0.5, 0.5]
            // 如果是雪碧图，需要设置offset和size
            // offset: [currentFrame * frameWidth, 0],
            // size: [frameWidth, frameHeight]
          })
        })
      ), requestAnimationFrame(a);
    };
    this.source.addFeature(r), a();
  }
  /**
   * 粒子爆炸特效 - 原版
   */
  _playParticleExplosion(e, t) {
    this._createFireCore(e, t), this._createShockwaves(e, t), this._createParticles(e, t), this._createSmoke(e, t);
  }
  _createFireCore(e, t) {
    const n = new ae({
      geometry: new he(e)
    }), s = Date.now(), r = 35, o = () => {
      const a = Date.now() - s, l = Math.min(a / t, 1);
      if (l >= 1) {
        this.source.removeFeature(n);
        return;
      }
      let c;
      l < 0.2 ? c = r * (l / 0.2) : l < 0.5 ? c = r : c = r * (1 - (l - 0.5) / 0.5);
      let h2;
      l < 0.1 ? h2 = `rgba(255, 255, 255, ${1 - l * 5})` : l < 0.3 ? h2 = `rgba(255, 255, 100, ${1 - l * 2})` : l < 0.6 ? h2 = `rgba(255, 150, 0, ${1 - l})` : h2 = `rgba(255, 50, 0, ${1 - l})`, n.setStyle(
        new te({
          image: new we({
            radius: c,
            fill: new J({ color: h2 })
          })
        })
      ), requestAnimationFrame(o);
    };
    this.source.addFeature(n), o();
  }
  _createShockwaves(e, t) {
    [0, 100, 200].forEach((n, s) => {
      setTimeout(() => {
        const r = new ae({
          geometry: new he(e)
        }), o = Date.now(), a = 50 + s * 10, l = () => {
          const c = Date.now() - o, h2 = Math.min(c / (t * 0.6), 1);
          if (h2 >= 1) {
            this.source.removeFeature(r);
            return;
          }
          const u = a * h2, d = (1 - h2) * 0.6;
          r.setStyle(
            new te({
              image: new we({
                radius: u,
                stroke: new se({
                  color: `rgba(255, 200, 100, ${d})`,
                  width: 3
                })
              })
            })
          ), requestAnimationFrame(l);
        };
        this.source.addFeature(r), l();
      }, n);
    });
  }
  _createParticles(e, t) {
    for (let s = 0; s < 20; s++) {
      const r = Math.PI * 2 * s / 20, o = 0.3 + Math.random() * 0.4, a = 40 + Math.random() * 20;
      this._createSingleParticle(e, r, o, a, t);
    }
  }
  _createSingleParticle(e, t, n, s, r) {
    const o = new ae({
      geometry: new he(e)
    }), a = Date.now(), l = r * 0.7, c = () => {
      const h2 = Date.now() - a, u = Math.min(h2 / l, 1);
      if (u >= 1) {
        this.source.removeFeature(o);
        return;
      }
      const d = s * u * (2 - u), f = Math.cos(t) * d, g = Math.sin(t) * d;
      o.getGeometry().setCoordinates([
        e[0] + f,
        e[1] + g
      ]);
      const y = 4 * (1 - u * 0.5), m = 1 - u, _ = 255, p = Math.floor(200 * (1 - u)), E = 0;
      o.setStyle(
        new te({
          image: new we({
            radius: y,
            fill: new J({
              color: `rgba(${_}, ${p}, ${E}, ${m})`
            })
          })
        })
      ), requestAnimationFrame(c);
    };
    this.source.addFeature(o), c();
  }
  _createSmoke(e, t) {
    setTimeout(() => {
      for (let s = 0; s < 5; s++)
        setTimeout(() => {
          const r = new ae({
            geometry: new he(e)
          }), o = Date.now(), a = t * 0.8, l = Math.random() * Math.PI * 2, c = 20 + Math.random() * 15, h2 = () => {
            const u = Date.now() - o, d = Math.min(u / a, 1);
            if (d >= 1) {
              this.source.removeFeature(r);
              return;
            }
            const f = c * d, g = Math.cos(l) * f * 0.3, y = Math.sin(l) * f * 0.3 + f;
            r.getGeometry().setCoordinates([
              e[0] + g,
              e[1] + y
            ]);
            const m = 10 + 20 * d, _ = (1 - d) * 0.4;
            r.setStyle(
              new te({
                image: new we({
                  radius: m,
                  fill: new J({
                    color: `rgba(80, 80, 80, ${_})`
                  })
                })
              })
            ), requestAnimationFrame(h2);
          };
          this.source.addFeature(r), h2();
        }, s * 100);
    }, t * 0.3);
  }
  /**
   * 简单爆炸特效
   */
  _playExplosionEffect(e, t, n) {
    const s = new ae({
      geometry: new he(e)
    }), r = Date.now(), o = 5, a = 40, l = () => {
      const c = Date.now() - r, h2 = Math.min(c / t, 1);
      if (h2 >= 1) {
        this.source.removeFeature(s);
        return;
      }
      const u = o + (a - o) * h2, d = 1 - h2, f = n.replace(/[\d.]+\)$/g, `${d})`);
      s.setStyle(
        new te({
          image: new we({
            radius: u,
            fill: new J({
              color: f
            }),
            stroke: new se({
              color: f,
              width: 2
            })
          })
        })
      ), requestAnimationFrame(l);
    };
    this.source.addFeature(s), l();
  }
  /**
   * 闪光特效
   */
  _playFlashEffect(e, t, n) {
    const s = new ae({
      geometry: new he(e)
    }), r = Date.now(), o = 30, a = () => {
      const l = Date.now() - r, c = Math.min(l / t, 1);
      if (c >= 1) {
        this.source.removeFeature(s);
        return;
      }
      const h2 = Math.abs(Math.sin(c * Math.PI * 6)), u = (1 - c) * h2, d = n.replace(/[\d.]+\)$/g, `${u})`);
      s.setStyle(
        new te({
          image: new we({
            radius: o,
            fill: new J({
              color: d
            })
          })
        })
      ), requestAnimationFrame(a);
    };
    this.source.addFeature(s), a();
  }
};
var no = {
  /**
   * Triggered upon feature translation start.
   * @event TranslateEvent#translatestart
   * @api
   */
  TRANSLATESTART: "translatestart",
  /**
   * Triggered upon feature translation.
   * @event TranslateEvent#translating
   * @api
   */
  TRANSLATING: "translating",
  /**
   * Triggered upon feature translation end.
   * @event TranslateEvent#translateend
   * @api
   */
  TRANSLATEEND: "translateend"
};
var io = class extends It {
  /**
   * @param {TranslateEventType} type Type.
   * @param {Collection<Feature>} features The features translated.
   * @param {import("../coordinate.js").Coordinate} coordinate The event coordinate.
   * @param {import("../coordinate.js").Coordinate} startCoordinate The original coordinates before.translation started
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   */
  constructor(e, t, n, s, r) {
    super(e), this.features = t, this.coordinate = n, this.startCoordinate = s, this.mapBrowserEvent = r;
  }
};
var x_ = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, super(
      /** @type {import("./Pointer.js").Options} */
      e
    ), this.on, this.once, this.un, this.lastCoordinate_ = null, this.startCoordinate_ = null, this.features_ = e.features !== void 0 ? e.features : null;
    let t;
    if (e.layers && !this.features_)
      if (typeof e.layers == "function")
        t = e.layers;
      else {
        const n = e.layers;
        t = function(s) {
          return n.includes(s);
        };
      }
    else
      t = _n;
    this.layerFilter_ = t, this.filter_ = e.filter && !this.features_ ? e.filter : _n, this.hitTolerance_ = e.hitTolerance ? e.hitTolerance : 0, this.condition_ = e.condition ? e.condition : $i, this.lastFeature_ = null, this.addChangeListener(
      lr.ACTIVE,
      this.handleActiveChanged_
    );
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    if (!e.originalEvent || !this.condition_(e))
      return false;
    if (this.lastFeature_ = this.featuresAtPixel_(e.pixel, e.map), !this.lastCoordinate_ && this.lastFeature_) {
      this.startCoordinate_ = e.coordinate, this.lastCoordinate_ = e.coordinate, this.handleMoveEvent(e);
      const t = this.features_ || new Ke([this.lastFeature_]);
      return this.dispatchEvent(
        new io(
          no.TRANSLATESTART,
          t,
          e.coordinate,
          this.startCoordinate_,
          e
        )
      ), true;
    }
    return false;
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    if (this.lastCoordinate_) {
      this.lastCoordinate_ = null, this.handleMoveEvent(e);
      const t = this.features_ || new Ke([this.lastFeature_]);
      return this.dispatchEvent(
        new io(
          no.TRANSLATEEND,
          t,
          e.coordinate,
          this.startCoordinate_,
          e
        )
      ), this.startCoordinate_ = null, true;
    }
    return false;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @override
   */
  handleDragEvent(e) {
    if (this.lastCoordinate_) {
      const t = e.coordinate;
      e.map.getView().getProjection();
      const n = Be(t), s = Be(
        this.lastCoordinate_
      ), r = n[0] - s[0], o = n[1] - s[1], a = this.features_ || new Ke([this.lastFeature_]);
      a.forEach(function(l) {
        const c = l.getGeometry();
        c.translate(r, o), l.setGeometry(c);
      }), this.lastCoordinate_ = t, this.dispatchEvent(
        new io(
          no.TRANSLATING,
          a,
          t,
          this.startCoordinate_,
          e
        )
      );
    }
  }
  /**
   * Handle pointer move events.
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @override
   */
  handleMoveEvent(e) {
    const t = e.map.getViewport();
    this.featuresAtPixel_(e.pixel, e.map) ? (t.classList.remove(this.lastCoordinate_ ? "ol-grab" : "ol-grabbing"), t.classList.add(this.lastCoordinate_ ? "ol-grabbing" : "ol-grab")) : t.classList.remove("ol-grab", "ol-grabbing");
  }
  /**
   * Tests to see if the given coordinates intersects any of our selected
   * features.
   * @param {import("../pixel.js").Pixel} pixel Pixel coordinate to test for intersection.
   * @param {import("../Map.js").default} map Map to test the intersection on.
   * @return {Feature} Returns the feature found at the specified pixel
   * coordinates.
   * @private
   */
  featuresAtPixel_(e, t) {
    return t.forEachFeatureAtPixel(
      e,
      (n, s) => {
        if (!(!(n instanceof ae) || !this.filter_(n, s)) && !(this.features_ && !this.features_.getArray().includes(n)))
          return n;
      },
      {
        layerFilter: this.layerFilter_,
        hitTolerance: this.hitTolerance_
      }
    );
  }
  /**
   * Returns the Hit-detection tolerance.
   * @return {number} Hit tolerance in pixels.
   * @api
   */
  getHitTolerance() {
    return this.hitTolerance_;
  }
  /**
   * Hit-detection tolerance. Pixels inside the radius around the given position
   * will be checked for features.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @api
   */
  setHitTolerance(e) {
    this.hitTolerance_ = e;
  }
  /**
   * Remove the interaction from its current map and attach it to the new map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default} map Map.
   * @override
   */
  setMap(e) {
    const t = this.getMap();
    super.setMap(e), this.updateState_(t);
  }
  /**
   * @private
   */
  handleActiveChanged_() {
    this.updateState_(null);
  }
  /**
   * @param {import("../Map.js").default} oldMap Old map.
   * @private
   */
  updateState_(e) {
    let t = this.getMap();
    const n = this.getActive();
    (!t || !n) && (t = t || e, t && t.getViewport().classList.remove("ol-grab", "ol-grabbing"));
  }
};
var Wl = x_;
var Yl = "ol-scene-native-toast-root";
var I_ = () => {
  let i = document.getElementById(Yl);
  return i || (i = document.createElement("div"), i.id = Yl, Object.assign(i.style, {
    position: "fixed",
    top: "16px",
    left: "50%",
    transform: "translateX(-50%)",
    zIndex: 1e4,
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    pointerEvents: "none"
  }), document.body.appendChild(i), i);
};
var qn = (i, e = "info", t = 2600) => {
  const n = I_(), s = document.createElement("div"), r = {
    success: ["rgba(25, 135, 84, 0.96)", "#ffffff"],
    warning: ["rgba(190, 125, 20, 0.96)", "#ffffff"],
    error: ["rgba(190, 55, 55, 0.96)", "#ffffff"],
    info: ["rgba(22, 34, 52, 0.96)", "#f8fbff"]
  }, [o, a] = r[e] || r.info;
  return s.textContent = String(i || ""), Object.assign(s.style, {
    maxWidth: "520px",
    padding: "9px 14px",
    borderRadius: "8px",
    background: o,
    color: a,
    boxShadow: "0 10px 28px rgba(0,0,0,0.28)",
    border: "1px solid rgba(255,255,255,0.18)",
    fontSize: "14px",
    lineHeight: "1.4",
    pointerEvents: "auto"
  }), n.appendChild(s), window.setTimeout(() => s.remove(), t), s;
};
var ui = Object.assign(
  (i) => typeof i == "string" ? qn(i) : qn(i == null ? void 0 : i.message, i == null ? void 0 : i.type, i == null ? void 0 : i.duration),
  {
    success: (i) => qn(i, "success"),
    warning: (i) => qn(i, "warning"),
    error: (i) => qn(i, "error"),
    info: (i) => qn(i, "info")
  }
);
var lE = {
  confirm(i, e = "Confirm") {
    const t = e ? `${e}

${i}` : i;
    return window.confirm(t) ? Promise.resolve("confirm") : Promise.reject(new Error("cancel"));
  }
};
var v_ = class {
  constructor(e, t, n, s, r, o) {
    this.map = e, this.layers = t, this.popup = n, this.features = s, this.mapApp = r, this.routeEditor = o, this.store = (r == null ? void 0 : r.store) || null, this.routeEditEnabled = false, this.currentEditingFeature = null, this.iconTranslate = null, this.pointTranslate = null, this.onContextMenu = this.onContextMenu.bind(this);
  }
  getProjectionCode() {
    var e, t, n, s, r, o;
    return ((o = (r = (s = (n = (t = (e = this.map) == null ? void 0 : e.getView) == null ? void 0 : t.call(e)) == null ? void 0 : n.getProjection) == null ? void 0 : s.call(n)) == null ? void 0 : r.getCode) == null ? void 0 : o.call(r)) || "EPSG:3857";
  }
  lonLatFromCoordinate(e) {
    const t = this.getProjectionCode();
    return t === "EPSG:4326" ? [Number(e == null ? void 0 : e[0]), Number(e == null ? void 0 : e[1])] : $t(e, t);
  }
  coordinateFromLonLat(e, t) {
    const n = this.getProjectionCode();
    return n === "EPSG:4326" ? [Number(e), Number(t)] : wt([Number(e), Number(t)], n);
  }
  init() {
    this.bindDrop(), this.bindClick(), this.bindRoutePointTranslate(), this.bindIconTranslate(), this.bindContextMenu();
  }
  /* ================= 右键 ================= */
  bindContextMenu() {
    this.map.getViewport().addEventListener("contextmenu", this.onContextMenu);
  }
  onContextMenu(e) {
    if (this.routeEditEnabled) {
      if (!this.mapApp.isEditable())
        return;
      e.preventDefault();
      const a = this.routeEditor.getPoints();
      this.currentEditingFeature && (this.currentEditingFeature.set("line", a), this.store.updateModelInScene(
        this.currentEditingFeature.get("UID"),
        { line: a },
        { recordHistory: false }
      ), this.mapApp.handleRouteSaved(this.currentEditingFeature, a)), this.routeEditor.clear(true), this.disableRouteEdit();
      return;
    }
    e.preventDefault();
    const t = this.map.getEventPixel(e), n = this.map.getCoordinateFromPixel(t), [s, r] = this.lonLatFromCoordinate(n), o = this.findIconFeatureAtPixel(t);
    this.mapApp.openContextMenu({
      originalEvent: e,
      pixel: t,
      coordinate: { lon: s, lat: r },
      mapCoordinate: n,
      feature: o,
      model: o && this.store.sceneState.models.find((a) => String(a.UID) === String(o.get("UID"))) || null
    });
  }
  /* ================= 拖拽投放模型 ================= */
  getDropTemplate(e) {
    const t = e.getData("application/json");
    if (!t)
      return null;
    const n = JSON.parse(t);
    return (n == null ? void 0 : n.dragType) === "template" ? n.template || null : n;
  }
  findIconFeatureAtPixel(e) {
    let t = null;
    return this.map.forEachFeatureAtPixel(
      e,
      (n) => {
        t = n;
      },
      {
        hitTolerance: 16,
        layerFilter: (n) => n === this.layers.iconLayer
      }
    ), t;
  }
  syncFeaturePositionVisuals(e) {
    if (!e)
      return;
    const t = {
      UID: e.get("UID"),
      nation: e.get("nation"),
      status: e.get("status"),
      heading: e.get("heading"),
      lon: e.get("lon"),
      lat: e.get("lat")
    };
    this.mapApp.updateHeadingIndicator(t, this.mapApp.displayConfig), this.mapApp.refreshTargetIndicatorsFromFeatures(String(e.get("UID"))), this.mapApp.syncPopupForFeature(e);
  }
  bindDrop() {
    const e = this.map.getViewport();
    e.addEventListener("dragover", (t) => {
      this.mapApp.isEditable() && (t.preventDefault(), t.dataTransfer && (t.dataTransfer.dropEffect = "copy"));
    }), e.addEventListener("drop", async (t) => {
      if (!this.mapApp.isEditable())
        return;
      t.preventDefault();
      const n = this.getDropTemplate(t.dataTransfer);
      if (!n)
        return;
      const s = this.map.getEventPixel(t), r = this.findIconFeatureAtPixel(s), o = this.map.getCoordinateFromPixel(s), [a, l] = this.lonLatFromCoordinate(o), c = r && this.store.sceneState.models.find(
        (g) => String(g.UID) === String(r.get("UID"))
      ) || null;
      let h2 = false;
      const u = {
        template: n,
        coordinate: { lon: a, lat: l },
        pixel: Array.isArray(s) ? [...s] : s,
        targetModel: c,
        targetFeature: r,
        canMount: this.store.canMountTemplateOnModel(n, c),
        originalEvent: t,
        get defaultPrevented() {
          return h2;
        },
        preventDefault: () => {
          h2 = true;
        }
      };
      if (await this.mapApp.handleTemplateDrop(u), u.defaultPrevented)
        return;
      if (r) {
        const g = this.store.addMountToModel(r.get("UID"), n, {
          recordHistory: true,
          force: true
        });
        if (g != null && g.host) {
          await this.mapApp.upsertModel(g.host), this.mapApp.selectFeature(r, false), ui.success(`${g.mount.showName} 已挂载到 ${g.host.showName}`);
          return;
        }
      }
      const d = this.mapApp.createModelFromTemplate(n, {
        lat: Number(l.toFixed(5)),
        lon: Number(a.toFixed(5)),
        num: 1,
        height: Number(n.height || 0),
        speed: Number(n.speed || 0),
        heading: Number(n.heading || 0),
        time: 0,
        nation: n.nation || "red"
      }), f = await this.mapApp.addModelToState(d, { recordHistory: true });
      ui.success(`已部署 ${f.showName}`);
    });
  }
  /* ================= 单击 ================= */
  bindClick() {
    this.map.on("singleclick", (e) => {
      if (this.mapApp.isBatchTransformActive())
        return;
      if (this.routeEditEnabled) {
        if (!this.mapApp.isEditable())
          return;
        this.routeEditor.onMapClick(e);
        return;
      }
      let t = null;
      this.map.forEachFeatureAtPixel(
        e.pixel,
        (n) => {
          t = n;
        },
        {
          hitTolerance: 5,
          layerFilter: (n) => n === this.layers.iconLayer
        }
      ), this.mapApp.selectFeature(t, false);
    });
  }
  /* ================= 图标拖拽 ================= */
  bindIconTranslate() {
    this.iconTranslate && this.map.removeInteraction(this.iconTranslate);
    const e = this, t = new Wl({
      hitTolerance: 5,
      filter: function(s, r) {
        var o;
        return !r || r.get("layerType") !== "icon" ? false : e.routeEditEnabled ? e.mapApp.isEditable() ? s.get("UID") === ((o = e.currentEditingFeature) == null ? void 0 : o.get("UID")) : false : e.mapApp.isBatchTransformActive() ? false : e.mapApp.isEditable();
      }
    });
    let n = null;
    t.on("translatestart", (s) => {
      var o;
      const r = (o = s.features) == null ? void 0 : o.item(0);
      r && (n = r.getGeometry().getCoordinates());
    }), t.on("translating", (s) => {
      var c;
      const r = (c = s.features) == null ? void 0 : c.item(0);
      if (!r)
        return;
      const o = r.getGeometry().getCoordinates(), [a, l] = e.lonLatFromCoordinate(o);
      r.set("lon", a), r.set("lat", l), e.syncFeaturePositionVisuals(r);
    }), t.on("translateend", (s) => {
      var c;
      const r = (c = s.features) == null ? void 0 : c.item(0);
      if (!r || !n)
        return;
      const o = r.getGeometry().getCoordinates();
      if (n[0] === o[0] && n[1] === o[1])
        return;
      const [a, l] = e.lonLatFromCoordinate(o);
      if (r.set("lon", a), r.set("lat", l), e.syncFeaturePositionVisuals(r), this.store.updateModelInScene(r.get("UID"), { lon: a, lat: l }, { recordHistory: false }), e.mapApp.selectFeature(r), e.routeEditor && e.routeEditor.points.length > 0) {
        e.routeEditor.points[0].lon = a, e.routeEditor.points[0].lat = l;
        const u = e.layers.pointLayer.getSource().getFeatures().find(
          (d) => d.get("index") === 0
        );
        u && u.getGeometry().setCoordinates(
          e.coordinateFromLonLat(a, l)
        ), e.routeEditor.lineDraw.drawLine(
          e.routeEditor.points,
          r.get("UID"),
          r.get("nation")
        );
      }
      n = null;
    }), this.map.addInteraction(t), this.iconTranslate = t;
  }
  /* ================= 航路点拖拽 ================= */
  bindRoutePointTranslate() {
    this.pointTranslate && this.map.removeInteraction(this.pointTranslate);
    const e = this, t = new Wl({
      hitTolerance: 5,
      filter: function(n, s) {
        return e.mapApp.isEditable() && e.routeEditEnabled && s && s.get("layerType") === "trackPoint";
      }
    });
    t.on("translateend", (n) => {
      var c;
      const s = (c = n.features) == null ? void 0 : c.item(0);
      if (!s)
        return;
      const r = s.get("index"), o = s.getGeometry().getCoordinates(), [a, l] = e.lonLatFromCoordinate(o);
      e.routeEditor.updatePoint(r, a, l);
    }), this.map.addInteraction(t), this.pointTranslate = t;
  }
  /* ================= 航路编辑控制 ================= */
  startRouteEdit(e) {
    if (!this.mapApp.isEditable() || !e)
      return;
    this.currentEditingFeature = e, this.mapApp.selectFeature(e);
    const t = e.get("nation"), n = e.get("UID"), [s, r] = this.lonLatFromCoordinate(
      e.getGeometry().getCoordinates()
    );
    this.routeEditor.clear(true), this.routeEditor.setNation(t, s, r, n);
    const o = e.get("line");
    if (o && o.length > 1)
      for (let a = 1; a < o.length; a++) {
        const l = o[a];
        this.routeEditor.addPoint(l.lon, l.lat);
      }
    this.enableRouteEdit();
  }
  enableRouteEdit() {
    this.routeEditEnabled = true, this.map.getTargetElement().style.cursor = "crosshair";
  }
  disableRouteEdit() {
    this.routeEditEnabled = false, this.currentEditingFeature = null, this.map.getTargetElement().style.cursor = "";
  }
  destroy() {
    var t, n;
    const e = (n = (t = this.map) == null ? void 0 : t.getViewport) == null ? void 0 : n.call(t);
    e && e.removeEventListener("contextmenu", this.onContextMenu), this.iconTranslate && (this.map.removeInteraction(this.iconTranslate), this.iconTranslate = null), this.pointTranslate && (this.map.removeInteraction(this.pointTranslate), this.pointTranslate = null), this.disableRouteEdit();
  }
};
var M_ = class {
  create(e, t) {
    const { UID: n, lon: s, lat: r, showName: o, nation: a, NodeType: l, height: c, scale: h2 = 0.5 } = e, u = new ae({
      geometry: new he(wt([s, r])),
      UID: n,
      showName: o,
      nation: a,
      NodeType: l,
      height: c
    });
    u.setId(n);
    const d = new yn({
      src: t,
      scale: h2,
      imgSize: [50, 50],
      anchor: [50, 50],
      anchorXUnits: "pixels",
      anchorYUnits: "pixels",
      crossOrigin: "anonymous"
    }), f = new te({ image: d });
    return u.set("defaultStyle", f), u.setStyle(f), u;
  }
  createSelectStyle(e) {
    const n = e.get("nation") === "red" ? "rgba(255,0,0,0.3)" : "rgba(0,128,255,0.3)";
    return [
      new te({
        image: new we({
          radius: 28,
          fill: new J({ color: n }),
          stroke: new se({
            color: n.replace("0.3", "1"),
            width: 2.5
          })
        })
      }),
      e.get("defaultStyle")
    ];
  }
  /**
   * @param {VectorSource} source - feature 所属的 OpenLayers VectorSource 实例。
   * @param {string | number} UID - 要删除的 feature 的唯一标识符。
   * @returns {Feature | null} 被移除的 feature，如果未找到则返回 null。
   */
  remove(e, t) {
    const n = e.getFeatureById(t);
    return n ? (e.removeFeature(n), n) : null;
  }
};
var T_ = class {
  constructor(e, t = null, n = null) {
    this.layer = e, this.source = e.getSource(), this.effectManager = t, this.getProjectionCode = typeof n == "function" ? n : () => "EPSG:3857", this.glowCache = /* @__PURE__ */ new Map();
  }
  getMapProjectionCode() {
    return this.getProjectionCode() || "EPSG:3857";
  }
  coordinateFromLonLat(e, t) {
    const n = this.getMapProjectionCode();
    return n === "EPSG:4326" ? [Number(e), Number(t)] : wt([Number(e), Number(t)], n);
  }
  lonLatFromCoordinate(e) {
    const t = this.getMapProjectionCode();
    return t === "EPSG:4326" ? [
      Number(e == null ? void 0 : e[0]),
      Number(e == null ? void 0 : e[1])
    ] : $t(e, t);
  }
  findFeatureByUID(e) {
    const t = [
      e,
      String(e),
      Number.isFinite(Number(e)) ? Number(e) : null
    ].filter(
      (n) => n != null && n !== ""
    );
    for (const n of t) {
      const s = this.source.getFeatureById(n);
      if (s)
        return s;
    }
    return this.source.getFeatures().find(
      (n) => String(n.get("UID")) === String(e)
    ) || null;
  }
  createVisualStyle({
    nation: e = "red",
    iconUrl: t = "",
    scale: n = 0.5,
    rotation: s = 0,
    text: r = "",
    showText: o = true
  } = {}) {
    const a = this.createLabelText({ text: r, showText: o });
    if (t)
      return new te({
        image: new yn({
          src: t,
          scale: n,
          rotation: s,
          imgSize: [50, 50],
          anchor: [0.5, 0.5],
          crossOrigin: "anonymous"
        }),
        text: a
      });
    const l = e === "blue" ? "rgba(72,167,255,0.88)" : "rgba(255,96,96,0.88)", c = n === 0 ? 0.1 : Math.max(8, 18 * n);
    return new te({
      image: new we({
        radius: c,
        fill: new J({
          color: l
        }),
        stroke: new se({
          color: "rgba(255,255,255,0.92)",
          width: 2
        })
      }),
      text: a
    });
  }
  getVisualStyleKey({
    nation: e = "red",
    iconUrl: t = "",
    scale: n = 0.5,
    rotation: s = 0,
    text: r = "",
    showText: o = true
  } = {}) {
    return [
      e,
      t || "",
      Number(n).toFixed(4),
      Number(s).toFixed(6),
      o === false ? "0" : "1",
      r || ""
    ].join("|");
  }
  getFeatureLabelOptions(e) {
    return {
      text: e.get("showName") || e.get("name") || "",
      showText: e.get("showModelName") !== false && e.get("status") !== "hidden"
    };
  }
  createLabelText({ text: e = "", showText: t = true } = {}) {
    if (!(!t || !e))
      return new zn({
        text: String(e),
        offsetY: -28,
        padding: [3, 6, 3, 6],
        backgroundFill: new J({ color: "rgba(9, 18, 30, 0.72)" }),
        fill: new J({ color: "#f8fbff" }),
        stroke: new se({ color: "rgba(0,0,0,0.65)", width: 3 })
      });
  }
  async createIcon(e, t = "") {
    const {
      UID: n,
      lon: s,
      lat: r,
      showName: o,
      nation: a,
      NodeType: l,
      height: c,
      scale: h2 = 0.5,
      rotation: u = 0
    } = e, d = new ae({
      geometry: new he(
        this.coordinateFromLonLat(s, r)
      ),
      UID: n,
      showName: o,
      nation: a,
      NodeType: l,
      height: c
    });
    d.setId(n);
    const f = this.createVisualStyle({
      nation: a,
      iconUrl: t,
      scale: h2,
      rotation: u,
      text: o,
      showText: e.showModelName !== false
    });
    return d.set("defaultStyle", f), d.set("defaultStyleKey", this.getVisualStyleKey({
      nation: a,
      iconUrl: t,
      scale: h2,
      rotation: u,
      text: o,
      showText: e.showModelName !== false
    })), d.setStyle(f), this.source.addFeature(d), d;
  }
  createIconSync(e, t = "") {
    return this.createIcon(e, t);
  }
  updateIcon(e, t = {}) {
    if (!e)
      return;
    const {
      lon: n,
      lat: s,
      iconUrl: r,
      scale: o,
      rotation: a
    } = t;
    if (n !== void 0 && s !== void 0) {
      const l = this.coordinateFromLonLat(n, s), c = e.getGeometry(), h2 = c.getCoordinates();
      ((h2 == null ? void 0 : h2[0]) !== l[0] || (h2 == null ? void 0 : h2[1]) !== l[1]) && c.setCoordinates(l);
    }
    if (r !== void 0 || o !== void 0 || a !== void 0) {
      const l = this.getBaseImage(e), c = r !== void 0 ? r : typeof (l == null ? void 0 : l.getSrc) == "function" ? l.getSrc() : "", h2 = o !== void 0 ? o : typeof (l == null ? void 0 : l.getScale) == "function" ? l.getScale() : 0.5, u = a !== void 0 ? a : typeof (l == null ? void 0 : l.getRotation) == "function" ? l.getRotation() : 0, d = {
        nation: e.get("nation"),
        iconUrl: c,
        scale: h2,
        rotation: u,
        ...this.getFeatureLabelOptions(e)
      }, f = this.getVisualStyleKey(d);
      if (e.get("defaultStyleKey") === f)
        return;
      e.set("defaultStyle", this.createVisualStyle(d)), e.set("defaultStyleKey", f), this.applyCurrentStyle(e);
    }
  }
  setRotation(e, t) {
    if (!e)
      return;
    const n = this.getBaseImage(e), s = typeof (n == null ? void 0 : n.getSrc) == "function" ? n.getSrc() : "", r = typeof (n == null ? void 0 : n.getScale) == "function" ? n.getScale() : 0.5;
    e.set(
      "defaultStyle",
      this.createVisualStyle({
        nation: e.get("nation"),
        iconUrl: s,
        scale: r,
        rotation: t,
        ...this.getFeatureLabelOptions(e)
      })
    ), this.applyCurrentStyle(e);
  }
  getBaseImage(e) {
    var r, o, a, l;
    const t = e.get("defaultStyle"), n = (r = t == null ? void 0 : t.getImage) == null ? void 0 : r.call(t);
    if (n)
      return n;
    const s = e.getStyle();
    return Array.isArray(s) ? (a = (o = s.find((c) => {
      var h2;
      return (h2 = c == null ? void 0 : c.getImage) == null ? void 0 : h2.call(c);
    })) == null ? void 0 : o.getImage) == null ? void 0 : a.call(o) : (l = s == null ? void 0 : s.getImage) == null ? void 0 : l.call(s);
  }
  getFeatureBaseScale(e) {
    var s;
    const t = this.getBaseImage(e), n = Number((s = t == null ? void 0 : t.getScale) == null ? void 0 : s.call(t));
    return Number.isFinite(n) ? n : 0.5;
  }
  createFallbackGlowStyle(e, {
    glowColor: t = "#00ffff"
  } = {}) {
    const n = this.getFeatureBaseScale(e), s = Math.max(10, 18 * n);
    return [
      new te({
        image: new we({
          radius: s + 8,
          fill: new J({
            color: "rgba(0,0,0,0)"
          }),
          stroke: new se({
            color: t,
            width: 8
          })
        })
      }),
      new te({
        image: new we({
          radius: s + 4,
          fill: new J({
            color: "rgba(0,0,0,0)"
          }),
          stroke: new se({
            color: "rgba(255,255,255,0.92)",
            width: 3
          })
        })
      }),
      e.get("defaultStyle")
    ];
  }
  async createRealOutlineGlowStyle(e, {
    glowColor: t = "#00ffff",
    blur: n = 24
  } = {}) {
    const s = this.getBaseImage(e);
    if (!s)
      return e.get("defaultStyle");
    const r = typeof (s == null ? void 0 : s.getSrc) == "function" ? s.getSrc() : "", o = typeof (s == null ? void 0 : s.getScale) == "function" ? s.getScale() : 0.5, a = typeof (s == null ? void 0 : s.getRotation) == "function" ? s.getRotation() : 0;
    if (!r)
      return this.createFallbackGlowStyle(
        e,
        { glowColor: t }
      );
    const l = `${r}_${t}_${n}`;
    if (this.glowCache.has(l)) {
      const f = this.glowCache.get(l);
      return new te({
        image: new yn({
          img: f,
          imgSize: [
            f.width,
            f.height
          ],
          scale: o,
          rotation: a,
          anchor: [0.5, 0.5],
          crossOrigin: "anonymous"
        }),
        text: this.createLabelText(this.getFeatureLabelOptions(e))
      });
    }
    const c = document.createElement("canvas"), h2 = c.getContext("2d"), u = new Image();
    return u.crossOrigin = "anonymous", new Promise((f) => {
      u.onload = () => {
        const g = u.width, y = u.height;
        c.width = g + n * 4, c.height = y + n * 4;
        const m = (c.width - g) / 2, _ = (c.height - y) / 2;
        h2.save(), h2.shadowColor = t, h2.shadowBlur = n, h2.drawImage(
          u,
          m,
          _,
          g,
          y
        ), h2.restore(), h2.drawImage(
          u,
          m,
          _,
          g,
          y
        ), this.glowCache.set(
          l,
          c
        ), f(
          new te({
            image: new yn({
              img: c,
              imgSize: [
                c.width,
                c.height
              ],
              scale: o,
              rotation: a,
              anchor: [0.5, 0.5],
              crossOrigin: "anonymous"
            }),
            text: this.createLabelText(this.getFeatureLabelOptions(e))
          })
        );
      }, u.src = r;
    });
  }
  async applyCurrentStyle(e) {
    if (e) {
      if (e.get("isSelected")) {
        const t = await this.createSelectStyle(e);
        e.setStyle(t);
        return;
      }
      if (e.get("isBatchSelected")) {
        const t = await this.createBatchSelectStyle(e);
        e.setStyle(t);
        return;
      }
      e.setStyle(
        e.get("defaultStyle")
      );
    }
  }
  async createSelectStyle(e) {
    const t = e.get("nation");
    return this.createRealOutlineGlowStyle(
      e,
      {
        glowColor: t === "blue" ? "#00d8ff" : "#ff4040",
        blur: 28
      }
    );
  }
  async createBatchSelectStyle(e) {
    return this.createSelectStyle(e);
  }
  setRotationToTarget(e, t, n) {
    if (!e)
      return;
    const s = e.getGeometry().getCoordinates(), [r, o] = this.lonLatFromCoordinate(s), a = this.calculateBearing(
      r,
      o,
      t,
      n
    );
    this.setRotation(e, a);
  }
  calculateBearing(e, t, n, s) {
    const r = t * Math.PI / 180, o = s * Math.PI / 180, a = (n - e) * Math.PI / 180, l = Math.sin(a) * Math.cos(o), c = Math.cos(r) * Math.sin(o) - Math.sin(r) * Math.cos(o) * Math.cos(a);
    return Math.atan2(l, c);
  }
  deleteIcon(e, {
    showEffect: t = true,
    effectType: n = "realistic",
    effectDuration: s = 1500,
    effectColor: r = "rgba(255,100,0,1)",
    spriteUrl: o = null,
    spriteFrames: a = 16
  } = {}) {
    if (!e)
      return;
    const l = e.getGeometry().getCoordinates();
    t && this.effectManager && this.effectManager.playDestroyEffect(
      l,
      {
        type: n,
        duration: s,
        color: r,
        spriteUrl: o,
        spriteFrames: a
      }
    ), this.source.removeFeature(e);
  }
  deleteByUID(e, t) {
    const n = this.findFeatureByUID(e);
    return n ? (this.deleteIcon(
      n,
      t
    ), true) : false;
  }
  getByUID(e) {
    return this.findFeatureByUID(e);
  }
  getAllIcons() {
    return this.source.getFeatures();
  }
  getByNation(e) {
    return this.source.getFeatures().filter(
      (t) => t.get("nation") === e
    );
  }
};
var b_ = class {
  constructor(e) {
    this.map = e, this.iconLayer = null, this.pointLayer = null, this.lineLayer = null, this.headingLayer = null, this.targetLayer = null, this.relationLayer = null, this.annotationLayer = null, this.measureLayer = null, this.situationEffectLayer = null, this.destroyLayer = null;
  }
  initIconLayer() {
    this.iconLayer || (this.iconLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 100,
      layerType: "icon"
    }), this.map.addLayer(this.iconLayer));
  }
  initPointLayer() {
    this.pointLayer || (this.pointLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 90,
      layerType: "trackPoint"
    }), this.map.addLayer(this.pointLayer));
  }
  initLineLayer() {
    this.lineLayer || (this.lineLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 80,
      layerType: "trackLine"
    }), this.map.addLayer(this.lineLayer));
  }
  initHeadingLayer() {
    this.headingLayer || (this.headingLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 95,
      layerType: "heading"
    }), this.map.addLayer(this.headingLayer));
  }
  initTargetLayer() {
    this.relationLayer || (this.relationLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 94,
      layerType: "relation"
    }), this.targetLayer = this.relationLayer, this.map.addLayer(this.relationLayer));
  }
  initAnnotationLayer() {
    this.annotationLayer || (this.annotationLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 70,
      layerType: "annotation"
    }), this.map.addLayer(this.annotationLayer));
  }
  initMeasureLayer() {
    this.measureLayer || (this.measureLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 120,
      layerType: "measure"
    }), this.map.addLayer(this.measureLayer));
  }
  initSituationEffectLayer() {
    this.situationEffectLayer || (this.situationEffectLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 75,
      layerType: "situationEffect"
    }), this.map.addLayer(this.situationEffectLayer));
  }
  initDestroyLayer() {
    this.destroyLayer || (this.destroyLayer = new Rt({
      source: new Lt({ wrapX: true }),
      zIndex: 110,
      layerType: "destroyEffect"
    }), this.map.addLayer(this.destroyLayer));
  }
  init() {
    this.initMeasureLayer(), this.initAnnotationLayer(), this.initSituationEffectLayer(), this.initLineLayer(), this.initTargetLayer(), this.initHeadingLayer(), this.initPointLayer(), this.initIconLayer(), this.initDestroyLayer();
  }
  addFeature(e) {
    this.iconLayer.getSource().addFeature(e);
  }
  getFeatureById(e) {
    const t = this.iconLayer.getSource(), n = [
      e,
      String(e),
      Number.isFinite(Number(e)) ? Number(e) : null
    ].filter((s) => s != null && s !== "");
    for (const s of n) {
      const r = t.getFeatureById(s);
      if (r)
        return r;
    }
    return t.getFeatures().find((s) => String(s.get("UID")) === String(e)) || null;
  }
  addDestroyFeature(e) {
    this.destroyLayer.getSource().addFeature(e);
  }
  removeDestroyFeature(e) {
    this.destroyLayer.getSource().removeFeature(e);
  }
  getIconByUID(e) {
    return this.getFeatureById(e);
  }
};
var R_ = class {
  constructor(e) {
    this.source = e.getSource(), this.color = "red", this.routeFeatures = /* @__PURE__ */ new Map();
  }
  setColor(e) {
    this.color = e === "blue" ? "blue" : "red";
  }
  splitByDateLine(e) {
    const t = [];
    let n = [];
    for (let s = 0; s < e.length; s += 1) {
      if (s > 0) {
        const r = e[s - 1], o = e[s];
        Math.abs(o.lon - r.lon) > 180 && (n.length > 1 && t.push(n), n = []);
      }
      n.push(e[s]);
    }
    return n.length > 1 && t.push(n), t;
  }
  buildWorldLines(e, t = 1) {
    const n = [];
    for (let s = -t; s <= t; s += 1)
      e.forEach((r) => {
        n.push(r.map((o) => wt([o.lon + s * 360, o.lat])));
      });
    return n;
  }
  getStyle(e = this.color) {
    const t = e === "blue" ? "rgba(0, 120, 255, 0.9)" : "rgba(255, 60, 60, 0.9)";
    return new te({
      stroke: new se({
        color: t,
        width: 2
      })
    });
  }
  drawLine(e, t, n = this.color) {
    if (!t || !Array.isArray(e) || e.length < 2)
      return null;
    const s = this.splitByDateLine(e), r = this.buildWorldLines(s, 1), o = new Ca(r);
    let a = this.routeFeatures.get(t);
    return a ? (a.setGeometry(o), a.setStyle(this.getStyle(n))) : (a = new ae({ geometry: o }), a.set("iconUID", t), a.setStyle(this.getStyle(n)), this.routeFeatures.set(t, a), this.source.addFeature(a)), a;
  }
  clear() {
    this.routeFeatures.clear(), this.source.clear();
  }
  clearByIconUID(e) {
    const t = this.routeFeatures.get(e);
    t && (this.source.removeFeature(t), this.routeFeatures.delete(e));
  }
};
var X = {
  IDLE: 0,
  LOADING: 1,
  LOADED: 2,
  /**
   * Indicates that tile loading failed
   * @type {number}
   */
  ERROR: 3,
  EMPTY: 4
};
var L_ = class extends mr {
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("./TileState.js").default} state State.
   * @param {Options} [options] Tile options.
   */
  constructor(e, t, n) {
    super(), n = n || {}, this.tileCoord = e, this.state = t, this.key = "", this.transition_ = n.transition === void 0 ? 250 : n.transition, this.transitionStarts_ = {}, this.interpolate = !!n.interpolate;
  }
  /**
   * @protected
   */
  changed() {
    this.dispatchEvent(Z.CHANGE);
  }
  /**
   * Called by the tile cache when the tile is removed from the cache due to expiry
   */
  release() {
    this.setState(X.EMPTY);
  }
  /**
   * @return {string} Key.
   */
  getKey() {
    return this.key + "/" + this.tileCoord;
  }
  /**
   * Get the tile coordinate for this tile.
   * @return {import("./tilecoord.js").TileCoord} The tile coordinate.
   * @api
   */
  getTileCoord() {
    return this.tileCoord;
  }
  /**
   * @return {import("./TileState.js").default} State.
   */
  getState() {
    return this.state;
  }
  /**
   * Sets the state of this tile. If you write your own {@link module:ol/Tile~LoadFunction tileLoadFunction} ,
   * it is important to set the state correctly to {@link module:ol/TileState~ERROR}
   * when the tile cannot be loaded. Otherwise the tile cannot be removed from
   * the tile queue and will block other requests.
   * @param {import("./TileState.js").default} state State.
   * @api
   */
  setState(e) {
    if (this.state !== X.EMPTY) {
      if (this.state !== X.ERROR && this.state > e)
        throw new Error("Tile load sequence violation");
      this.state = e, this.changed();
    }
  }
  /**
   * Load the image or retry if loading previously failed.
   * Loading is taken care of by the tile queue, and calling this method is
   * only needed for preloading or for reloading in case of an error.
   * @abstract
   * @api
   */
  load() {
    ue();
  }
  /**
   * Get the alpha value for rendering.
   * @param {string} id An id for the renderer.
   * @param {number} time The render frame time.
   * @return {number} A number between 0 and 1.
   */
  getAlpha(e, t) {
    if (!this.transition_)
      return 1;
    let n = this.transitionStarts_[e];
    if (!n)
      n = t, this.transitionStarts_[e] = n;
    else if (n === -1)
      return 1;
    const s = t - n + 1e3 / 60;
    return s >= this.transition_ ? 1 : xh(s / this.transition_);
  }
  /**
   * Determine if a tile is in an alpha transition.  A tile is considered in
   * transition if tile.getAlpha() has not yet been called or has been called
   * and returned 1.
   * @param {string} id An id for the renderer.
   * @return {boolean} The tile is in transition.
   */
  inTransition(e) {
    return this.transition_ ? this.transitionStarts_[e] !== -1 : false;
  }
  /**
   * Mark a transition as complete.
   * @param {string} id An id for the renderer.
   */
  endTransition(e) {
    this.transition_ && (this.transitionStarts_[e] = -1);
  }
  /**
   * @override
   */
  disposeInternal() {
    this.release(), super.disposeInternal();
  }
};
var Ua = L_;
var F_ = class extends Ua {
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("./TileState.js").default} state State.
   * @param {string} src Image source URI.
   * @param {?string} crossOrigin Cross origin.
   * @param {import("./Tile.js").LoadFunction} tileLoadFunction Tile load function.
   * @param {import("./Tile.js").Options} [options] Tile options.
   */
  constructor(e, t, n, s, r, o) {
    super(e, t, o), this.crossOrigin_ = s, this.src_ = n, this.key = n, this.image_, ft ? this.image_ = new OffscreenCanvas(1, 1) : (this.image_ = new Image(), s !== null && (this.image_.crossOrigin = s)), this.unlisten_ = null, this.tileLoadFunction_ = r;
  }
  /**
   * Get the HTML image element for this tile (may be a Canvas, OffscreenCanvas, Image, or Video).
   * @return {HTMLCanvasElement|OffscreenCanvas|HTMLImageElement|HTMLVideoElement} Image.
   * @api
   */
  getImage() {
    return this.image_;
  }
  /**
   * Sets an HTML image element for this tile (may be a Canvas or preloaded Image).
   * @param {HTMLCanvasElement|OffscreenCanvas|HTMLImageElement} element Element.
   */
  setImage(e) {
    this.image_ = e, this.state = X.LOADED, this.unlistenImage_(), this.changed();
  }
  /**
   * Get the cross origin of the ImageTile.
   * @return {string} Cross origin.
   */
  getCrossOrigin() {
    return this.crossOrigin_;
  }
  /**
   * Tracks loading or read errors.
   *
   * @private
   */
  handleImageError_() {
    this.state = X.ERROR, this.unlistenImage_(), this.image_ = D_(), this.changed();
  }
  /**
   * Tracks successful image load.
   *
   * @private
   */
  handleImageLoad_() {
    if (ft)
      this.state = X.LOADED;
    else {
      const e = (
        /** @type {HTMLImageElement} */
        this.image_
      );
      e.naturalWidth && e.naturalHeight ? this.state = X.LOADED : this.state = X.EMPTY;
    }
    this.unlistenImage_(), this.changed();
  }
  /**
   * Load the image or retry if loading previously failed.
   * Loading is taken care of by the tile queue, and calling this method is
   * only needed for preloading or for reloading in case of an error.
   *
   * To retry loading tiles on failed requests, use a custom `tileLoadFunction`
   * that checks for error status codes and reloads only when the status code is
   * 408, 429, 500, 502, 503 and 504, and only when not too many retries have been
   * made already:
   *
   * ```js
   * const retryCodes = [408, 429, 500, 502, 503, 504];
   * const retries = {};
   * source.setTileLoadFunction((tile, src) => {
   *   const image = tile.getImage();
   *   fetch(src)
   *     .then((response) => {
   *       if (retryCodes.includes(response.status)) {
   *         retries[src] = (retries[src] || 0) + 1;
   *         if (retries[src] <= 3) {
   *           setTimeout(() => tile.load(), retries[src] * 1000);
   *         }
   *         return Promise.reject();
   *       }
   *       return response.blob();
   *     })
   *     .then((blob) => {
   *       const imageUrl = URL.createObjectURL(blob);
   *       image.src = imageUrl;
   *       setTimeout(() => URL.revokeObjectURL(imageUrl), 5000);
   *     })
   *     .catch(() => tile.setState(3)); // error
   * });
   * ```
   * @api
   * @override
   */
  load() {
    this.state == X.ERROR && (this.state = X.IDLE, this.image_ = new Image(), this.crossOrigin_ !== null && (this.image_.crossOrigin = this.crossOrigin_)), this.state == X.IDLE && (this.state = X.LOADING, this.changed(), this.tileLoadFunction_(this, this.src_), this.unlisten_ = wf(
      this.image_,
      this.handleImageLoad_.bind(this),
      this.handleImageError_.bind(this)
    ));
  }
  /**
   * Discards event handlers which listen for load completion or errors.
   *
   * @private
   */
  unlistenImage_() {
    this.unlisten_ && (this.unlisten_(), this.unlisten_ = null);
  }
  /**
   * @override
   */
  disposeInternal() {
    this.unlistenImage_(), this.image_ = null, super.disposeInternal();
  }
};
function D_() {
  const i = Fe(1, 1);
  return i.fillStyle = "rgba(0,0,0,0)", i.fillRect(0, 0, 1, 1), i.canvas;
}
var Yh = F_;
var P_ = class {
  /**
   * @param {number} decay Rate of decay (must be negative).
   * @param {number} minVelocity Minimum velocity (pixels/millisecond).
   * @param {number} delay Delay to consider to calculate the kinetic
   *     initial values (milliseconds).
   */
  constructor(e, t, n) {
    this.decay_ = e, this.minVelocity_ = t, this.delay_ = n, this.points_ = [], this.angle_ = 0, this.initialVelocity_ = 0;
  }
  /**
   * FIXME empty description for jsdoc
   */
  begin() {
    this.points_.length = 0, this.angle_ = 0, this.initialVelocity_ = 0;
  }
  /**
   * @param {number} x X.
   * @param {number} y Y.
   */
  update(e, t) {
    this.points_.push(e, t, Date.now());
  }
  /**
   * @return {boolean} Whether we should do kinetic animation.
   */
  end() {
    if (this.points_.length < 6)
      return false;
    const e = Date.now() - this.delay_, t = this.points_.length - 3;
    if (this.points_[t + 2] < e)
      return false;
    let n = t - 3;
    for (; n > 0 && this.points_[n + 2] > e; )
      n -= 3;
    const s = this.points_[t + 2] - this.points_[n + 2];
    if (s < 1e3 / 60)
      return false;
    const r = this.points_[t] - this.points_[n], o = this.points_[t + 1] - this.points_[n + 1];
    return this.angle_ = Math.atan2(o, r), this.initialVelocity_ = Math.sqrt(r * r + o * o) / s, this.initialVelocity_ > this.minVelocity_;
  }
  /**
   * @return {number} Total distance travelled (pixels).
   */
  getDistance() {
    return (this.minVelocity_ - this.initialVelocity_) / this.decay_;
  }
  /**
   * @return {number} Angle of the kinetic panning animation (radians).
   */
  getAngle() {
    return this.angle_;
  }
};
var A_ = P_;
var Ao = {
  POINTERMOVE: "pointermove",
  POINTERDOWN: "pointerdown",
  POINTERUP: "pointerup",
  POINTEROVER: "pointerover",
  POINTEROUT: "pointerout",
  POINTERENTER: "pointerenter",
  POINTERLEAVE: "pointerleave",
  POINTERCANCEL: "pointercancel"
};
var k_ = class extends mr {
  /**
   * @param {import("./Map.js").default} map The map with the viewport to listen to events on.
   * @param {number} [moveTolerance] The minimal distance the pointer must travel to trigger a move.
   */
  constructor(e, t) {
    super(e), this.map_ = e, this.clickTimeoutId_, this.emulateClicks_ = false, this.dragging_ = false, this.dragListenerKeys_ = [], this.moveTolerance_ = t === void 0 ? 1 : t, this.down_ = null;
    const n = this.map_.getViewport();
    this.activePointers_ = [], this.trackedTouches_ = {}, this.element_ = n, this.pointerdownListenerKey_ = le(
      n,
      Ao.POINTERDOWN,
      this.handlePointerDown_,
      this
    ), this.originalPointerMoveEvent_, this.relayedListenerKey_ = le(
      n,
      Ao.POINTERMOVE,
      this.relayMoveEvent_,
      this
    ), this.boundHandleTouchMove_ = this.handleTouchMove_.bind(this), this.element_.addEventListener(
      Z.TOUCHMOVE,
      this.boundHandleTouchMove_,
      Gc ? { passive: false } : false
    );
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  emulateClick_(e) {
    let t = new Vt(
      me.CLICK,
      this.map_,
      e
    );
    this.dispatchEvent(t), this.clickTimeoutId_ !== void 0 ? (clearTimeout(this.clickTimeoutId_), this.clickTimeoutId_ = void 0, t = new Vt(
      me.DBLCLICK,
      this.map_,
      e
    ), this.dispatchEvent(t)) : this.clickTimeoutId_ = setTimeout(() => {
      this.clickTimeoutId_ = void 0;
      const n = new Vt(
        me.SINGLECLICK,
        this.map_,
        e
      );
      this.dispatchEvent(n);
    }, 250);
  }
  /**
   * Keeps track on how many pointers are currently active.
   *
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  updateActivePointers_(e) {
    const t = e, n = t.pointerId;
    if (t.type == me.POINTERUP || t.type == me.POINTERCANCEL) {
      delete this.trackedTouches_[n];
      for (const s in this.trackedTouches_)
        if (this.trackedTouches_[s].target !== t.target) {
          delete this.trackedTouches_[s];
          break;
        }
    } else
      (t.type == me.POINTERDOWN || t.type == me.POINTERMOVE) && (this.trackedTouches_[n] = t);
    this.activePointers_ = Object.values(this.trackedTouches_);
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerUp_(e) {
    this.updateActivePointers_(e);
    const t = new Vt(
      me.POINTERUP,
      this.map_,
      e,
      void 0,
      void 0,
      this.activePointers_
    );
    this.dispatchEvent(t), this.emulateClicks_ && !t.defaultPrevented && !this.dragging_ && this.isMouseActionButton_(e) && this.emulateClick_(this.down_), this.activePointers_.length === 0 && (this.dragListenerKeys_.forEach(Ee), this.dragListenerKeys_.length = 0, this.dragging_ = false, this.down_ = null);
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @return {boolean} If the left mouse button was pressed.
   * @private
   */
  isMouseActionButton_(e) {
    return e.button === 0;
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerDown_(e) {
    this.emulateClicks_ = this.activePointers_.length === 0, this.updateActivePointers_(e);
    const t = new Vt(
      me.POINTERDOWN,
      this.map_,
      e,
      void 0,
      void 0,
      this.activePointers_
    );
    if (this.dispatchEvent(t), this.down_ = new PointerEvent(e.type, e), Object.defineProperty(this.down_, "target", {
      writable: false,
      value: e.target
    }), this.dragListenerKeys_.length === 0) {
      const n = this.map_.getOwnerDocument();
      this.dragListenerKeys_.push(
        le(
          n,
          me.POINTERMOVE,
          this.handlePointerMove_,
          this
        ),
        le(n, me.POINTERUP, this.handlePointerUp_, this),
        /* Note that the listener for `pointercancel is set up on
         * `pointerEventHandler_` and not `documentPointerEventHandler_` like
         * the `pointerup` and `pointermove` listeners.
         *
         * The reason for this is the following: `TouchSource.vacuumTouches_()`
         * issues `pointercancel` events, when there was no `touchend` for a
         * `touchstart`. Now, let's say a first `touchstart` is registered on
         * `pointerEventHandler_`. The `documentPointerEventHandler_` is set up.
         * But `documentPointerEventHandler_` doesn't know about the first
         * `touchstart`. If there is no `touchend` for the `touchstart`, we can
         * only receive a `touchcancel` from `pointerEventHandler_`, because it is
         * only registered there.
         */
        le(
          this.element_,
          me.POINTERCANCEL,
          this.handlePointerUp_,
          this
        )
      ), this.element_.getRootNode && this.element_.getRootNode() !== n && this.dragListenerKeys_.push(
        le(
          this.element_.getRootNode(),
          me.POINTERUP,
          this.handlePointerUp_,
          this
        )
      );
    }
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  handlePointerMove_(e) {
    if (this.isMoving_(e)) {
      this.updateActivePointers_(e), this.dragging_ = true;
      const t = new Vt(
        me.POINTERDRAG,
        this.map_,
        e,
        this.dragging_,
        void 0,
        this.activePointers_
      );
      this.dispatchEvent(t);
    }
  }
  /**
   * Wrap and relay a pointermove event.
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @private
   */
  relayMoveEvent_(e) {
    this.originalPointerMoveEvent_ = e;
    const t = !!(this.down_ && this.isMoving_(e));
    this.dispatchEvent(
      new Vt(
        me.POINTERMOVE,
        this.map_,
        e,
        t
      )
    );
  }
  /**
   * Flexible handling of a `touch-action: none` css equivalent: because calling
   * `preventDefault()` on a `pointermove` event does not stop native page scrolling
   * and zooming, we also listen for `touchmove` and call `preventDefault()` on it
   * when an interaction (currently `DragPan` handles the event.
   * @param {TouchEvent} event Event.
   * @private
   */
  handleTouchMove_(e) {
    const t = this.originalPointerMoveEvent_;
    (!t || t.defaultPrevented) && (typeof e.cancelable != "boolean" || e.cancelable === true) && e.preventDefault();
  }
  /**
   * @param {PointerEvent} pointerEvent Pointer
   * event.
   * @return {boolean} Is moving.
   * @private
   */
  isMoving_(e) {
    return this.dragging_ || Math.abs(e.clientX - this.down_.clientX) > this.moveTolerance_ || Math.abs(e.clientY - this.down_.clientY) > this.moveTolerance_;
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.relayedListenerKey_ && (Ee(this.relayedListenerKey_), this.relayedListenerKey_ = null), this.element_.removeEventListener(
      Z.TOUCHMOVE,
      this.boundHandleTouchMove_
    ), this.pointerdownListenerKey_ && (Ee(this.pointerdownListenerKey_), this.pointerdownListenerKey_ = null), this.dragListenerKeys_.forEach(Ee), this.dragListenerKeys_.length = 0, this.element_ = null, super.disposeInternal();
  }
};
var O_ = k_;
var Xt = {
  /**
   * Triggered after a map frame is rendered.
   * @event module:ol/MapEvent~MapEvent#postrender
   * @api
   */
  POSTRENDER: "postrender",
  /**
   * Triggered when the map starts moving.
   * @event module:ol/MapEvent~MapEvent#movestart
   * @api
   */
  MOVESTART: "movestart",
  /**
   * Triggered after the map is moved.
   * @event module:ol/MapEvent~MapEvent#moveend
   * @api
   */
  MOVEEND: "moveend",
  /**
   * Triggered when loading of additional map data (tiles, images, features) starts.
   * @event module:ol/MapEvent~MapEvent#loadstart
   * @api
   */
  LOADSTART: "loadstart",
  /**
   * Triggered when loading of additional map data has completed.
   * @event module:ol/MapEvent~MapEvent#loadend
   * @api
   */
  LOADEND: "loadend"
};
var Ge = {
  LAYERGROUP: "layergroup",
  SIZE: "size",
  TARGET: "target",
  VIEW: "view"
};
var hr = 1 / 0;
var N_ = class {
  /**
   * @param {function(T): number} priorityFunction Priority function.
   * @param {function(T): string} keyFunction Key function.
   */
  constructor(e, t) {
    this.priorityFunction_ = e, this.keyFunction_ = t, this.elements_ = [], this.priorities_ = [], this.queuedElements_ = {};
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    this.elements_.length = 0, this.priorities_.length = 0, os(this.queuedElements_);
  }
  /**
   * Remove and return the highest-priority element. O(log N).
   * @return {T} Element.
   */
  dequeue() {
    const e = this.elements_, t = this.priorities_, n = e[0];
    e.length == 1 ? (e.length = 0, t.length = 0) : (e[0] = /** @type {T} */
    e.pop(), t[0] = /** @type {number} */
    t.pop(), this.siftUp_(0));
    const s = this.keyFunction_(n);
    return delete this.queuedElements_[s], n;
  }
  /**
   * Enqueue an element. O(log N).
   * @param {T} element Element.
   * @return {boolean} The element was added to the queue.
   */
  enqueue(e) {
    _e(
      !(this.keyFunction_(e) in this.queuedElements_),
      "Tried to enqueue an `element` that was already added to the queue"
    );
    const t = this.priorityFunction_(e);
    return t != hr ? (this.elements_.push(e), this.priorities_.push(t), this.queuedElements_[this.keyFunction_(e)] = true, this.siftDown_(0, this.elements_.length - 1), true) : false;
  }
  /**
   * @return {number} Count.
   */
  getCount() {
    return this.elements_.length;
  }
  /**
   * Gets the index of the left child of the node at the given index.
   * @param {number} index The index of the node to get the left child for.
   * @return {number} The index of the left child.
   * @private
   */
  getLeftChildIndex_(e) {
    return e * 2 + 1;
  }
  /**
   * Gets the index of the right child of the node at the given index.
   * @param {number} index The index of the node to get the right child for.
   * @return {number} The index of the right child.
   * @private
   */
  getRightChildIndex_(e) {
    return e * 2 + 2;
  }
  /**
   * Gets the index of the parent of the node at the given index.
   * @param {number} index The index of the node to get the parent for.
   * @return {number} The index of the parent.
   * @private
   */
  getParentIndex_(e) {
    return e - 1 >> 1;
  }
  /**
   * Make this a heap. O(N).
   * @private
   */
  heapify_() {
    let e;
    for (e = (this.elements_.length >> 1) - 1; e >= 0; e--)
      this.siftUp_(e);
  }
  /**
   * @return {boolean} Is empty.
   */
  isEmpty() {
    return this.elements_.length === 0;
  }
  /**
   * @param {string} key Key.
   * @return {boolean} Is key queued.
   */
  isKeyQueued(e) {
    return e in this.queuedElements_;
  }
  /**
   * @param {T} element Element.
   * @return {boolean} Is queued.
   */
  isQueued(e) {
    return this.isKeyQueued(this.keyFunction_(e));
  }
  /**
   * @param {number} index The index of the node to move down.
   * @private
   */
  siftUp_(e) {
    const t = this.elements_, n = this.priorities_, s = t.length, r = t[e], o = n[e], a = e;
    for (; e < s >> 1; ) {
      const l = this.getLeftChildIndex_(e), c = this.getRightChildIndex_(e), h2 = c < s && n[c] < n[l] ? c : l;
      t[e] = t[h2], n[e] = n[h2], e = h2;
    }
    t[e] = r, n[e] = o, this.siftDown_(a, e);
  }
  /**
   * @param {number} startIndex The index of the root.
   * @param {number} index The index of the node to move up.
   * @private
   */
  siftDown_(e, t) {
    const n = this.elements_, s = this.priorities_, r = n[t], o = s[t];
    for (; t > e; ) {
      const a = this.getParentIndex_(t);
      if (s[a] > o)
        n[t] = n[a], s[t] = s[a], t = a;
      else
        break;
    }
    n[t] = r, s[t] = o;
  }
  /**
   * FIXME empty description for jsdoc
   */
  reprioritize() {
    const e = this.priorityFunction_, t = this.elements_, n = this.priorities_;
    let s = 0;
    const r = t.length;
    let o, a, l;
    for (a = 0; a < r; ++a)
      o = t[a], l = e(o), l == hr ? delete this.queuedElements_[this.keyFunction_(o)] : (n[s] = l, t[s++] = o);
    t.length = s, n.length = s, this.heapify_();
  }
};
var U_ = N_;
var G_ = class extends U_ {
  /**
   * @param {PriorityFunction} tilePriorityFunction Tile priority function.
   * @param {function(): ?} tileChangeCallback Function called on each tile change event.
   */
  constructor(e, t) {
    super(
      (n) => e.apply(null, n),
      (n) => n[0].getKey()
    ), this.boundHandleTileChange_ = this.handleTileChange.bind(this), this.tileChangeCallback_ = t, this.tilesLoading_ = 0, this.tilesLoadingKeys_ = {};
  }
  /**
   * @param {TileQueueElement} element Element.
   * @return {boolean} The element was added to the queue.
   * @override
   */
  enqueue(e) {
    const t = super.enqueue(e);
    return t && e[0].addEventListener(Z.CHANGE, this.boundHandleTileChange_), t;
  }
  /**
   * @return {number} Number of tiles loading.
   */
  getTilesLoading() {
    return this.tilesLoading_;
  }
  /**
   * @param {import("./events/Event.js").default} event Event.
   * @protected
   */
  handleTileChange(e) {
    const t = (
      /** @type {import("./Tile.js").default} */
      e.target
    ), n = t.getState();
    if (n === X.LOADED || n === X.ERROR || n === X.EMPTY) {
      n !== X.ERROR && t.removeEventListener(Z.CHANGE, this.boundHandleTileChange_);
      const s = t.getKey();
      s in this.tilesLoadingKeys_ && (delete this.tilesLoadingKeys_[s], --this.tilesLoading_), this.tileChangeCallback_();
    }
  }
  /**
   * @param {number} maxTotalLoading Maximum number tiles to load simultaneously.
   * @param {number} maxNewLoads Maximum number of new tiles to load.
   */
  loadMoreTiles(e, t) {
    let n = 0;
    for (; this.tilesLoading_ < e && n < t && this.getCount() > 0; ) {
      const s = this.dequeue()[0], r = s.getKey();
      s.getState() === X.IDLE && !(r in this.tilesLoadingKeys_) && (this.tilesLoadingKeys_[r] = true, ++this.tilesLoading_, ++n, s.load());
    }
  }
};
var B_ = G_;
function z_(i, e, t, n, s) {
  if (!i || !(t in i.wantedTiles) || !i.wantedTiles[t][e.getKey()])
    return hr;
  const r = i.viewState.center, o = n[0] - r[0], a = n[1] - r[1];
  return 65536 * Math.log(s) + Math.sqrt(o * o + a * a) / s;
}
var V_ = class extends vt {
  /**
   * @param {Options} options Control options.
   */
  constructor(e) {
    super();
    const t = e.element;
    t && !e.target && !t.style.pointerEvents && (t.style.pointerEvents = "auto"), this.element = t || null, this.target_ = null, this.map_ = null, this.listenerKeys = [], e.render && (this.render = e.render), e.target && this.setTarget(e.target);
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    var e;
    (e = this.element) == null || e.remove(), super.disposeInternal();
  }
  /**
   * Get the map associated with this control.
   * @return {import("../Map.js").default|null} Map.
   * @api
   */
  getMap() {
    return this.map_;
  }
  /**
   * Remove the control from its current map and attach it to the new map.
   * Pass `null` to just remove the control from the current map.
   * Subclasses may set up event handlers to get notified about changes to
   * the map here.
   * @param {import("../Map.js").default|null} map Map.
   * @api
   */
  setMap(e) {
    var t;
    this.map_ && ((t = this.element) == null || t.remove());
    for (let n = 0, s = this.listenerKeys.length; n < s; ++n)
      Ee(this.listenerKeys[n]);
    if (this.listenerKeys.length = 0, this.map_ = e, e) {
      const n = this.target_ ?? e.getOverlayContainerStopEvent();
      this.element && n.appendChild(this.element), this.render !== Ki && this.listenerKeys.push(
        le(e, Xt.POSTRENDER, this.render, this)
      ), e.render();
    }
  }
  /**
   * Renders the control.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @api
   */
  render(e) {
  }
  /**
   * This function is used to set a target element for the control. It has no
   * effect if it is called after the control has been added to the map (i.e.
   * after `setMap` is called on the control). If no `target` is set in the
   * options passed to the control constructor and if `setTarget` is not called
   * then the control is added to the map's overlay container.
   * @param {HTMLElement|string} target Target.
   * @api
   */
  setTarget(e) {
    this.target_ = typeof e == "string" ? document.getElementById(e) : e;
  }
};
var Ga = V_;
var X_ = class extends Ga {
  /**
   * @param {Options} [options] Attribution options.
   */
  constructor(e) {
    e = e || {}, super({
      element: document.createElement("div"),
      render: e.render,
      target: e.target
    }), this.ulElement_ = document.createElement("ul"), this.collapsed_ = e.collapsed !== void 0 ? e.collapsed : true, this.userCollapsed_ = this.collapsed_, this.overrideCollapsible_ = e.collapsible !== void 0, this.collapsible_ = e.collapsible !== void 0 ? e.collapsible : true, this.collapsible_ || (this.collapsed_ = false), this.attributions_ = e.attributions;
    const t = e.className !== void 0 ? e.className : "ol-attribution", n = e.tipLabel !== void 0 ? e.tipLabel : "Attributions", s = e.expandClassName !== void 0 ? e.expandClassName : t + "-expand", r = e.collapseLabel !== void 0 ? e.collapseLabel : "›", o = e.collapseClassName !== void 0 ? e.collapseClassName : t + "-collapse";
    typeof r == "string" ? (this.collapseLabel_ = document.createElement("span"), this.collapseLabel_.textContent = r, this.collapseLabel_.className = o) : this.collapseLabel_ = r;
    const a = e.label !== void 0 ? e.label : "i";
    typeof a == "string" ? (this.label_ = document.createElement("span"), this.label_.textContent = a, this.label_.className = s) : this.label_ = a;
    const l = this.collapsible_ && !this.collapsed_ ? this.collapseLabel_ : this.label_;
    this.toggleButton_ = document.createElement("button"), this.toggleButton_.setAttribute("type", "button"), this.toggleButton_.setAttribute("aria-expanded", String(!this.collapsed_)), this.toggleButton_.title = n, this.toggleButton_.appendChild(l), this.toggleButton_.addEventListener(
      Z.CLICK,
      this.handleClick_.bind(this),
      false
    );
    const c = t + " " + Ir + " " + da + (this.collapsed_ && this.collapsible_ ? " " + hl : "") + (this.collapsible_ ? "" : " ol-uncollapsible"), h2 = this.element;
    h2.className = c, h2.appendChild(this.toggleButton_), h2.appendChild(this.ulElement_), this.renderedAttributions_ = [], this.renderedVisible_ = true;
  }
  /**
   * Collect a list of visible attributions and set the collapsible state.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @return {Array<string>} Attributions.
   * @private
   */
  collectSourceAttributions_(e) {
    const t = this.getMap().getAllLayers(), n = new Set(
      t.flatMap((s) => s.getAttributions(e))
    );
    if (this.attributions_ !== void 0 && (Array.isArray(this.attributions_) ? this.attributions_.forEach((s) => n.add(s)) : n.add(this.attributions_)), !this.overrideCollapsible_) {
      const s = !t.some(
        (r) => {
          var o;
          return ((o = r.getSource()) == null ? void 0 : o.getAttributionsCollapsible()) === false;
        }
      );
      this.setCollapsible(s);
    }
    return Array.from(n);
  }
  /**
   * @private
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   */
  async updateElement_(e) {
    if (!e) {
      this.renderedVisible_ && (this.element.style.display = "none", this.renderedVisible_ = false);
      return;
    }
    const t = await Promise.all(
      this.collectSourceAttributions_(e).map(
        (s) => zu(() => s)
      )
    ), n = t.length > 0;
    if (this.renderedVisible_ != n && (this.element.style.display = n ? "" : "none", this.renderedVisible_ = n), !Sn(t, this.renderedAttributions_)) {
      Bc(this.ulElement_);
      for (let s = 0, r = t.length; s < r; ++s) {
        const o = document.createElement("li");
        o.innerHTML = t[s], this.ulElement_.appendChild(o);
      }
      this.renderedAttributions_ = t;
    }
  }
  /**
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(e) {
    e.preventDefault(), this.handleToggle_(), this.userCollapsed_ = this.collapsed_;
  }
  /**
   * @private
   */
  handleToggle_() {
    this.element.classList.toggle(hl), this.collapsed_ ? ll(this.collapseLabel_, this.label_) : ll(this.label_, this.collapseLabel_), this.collapsed_ = !this.collapsed_, this.toggleButton_.setAttribute("aria-expanded", String(!this.collapsed_));
  }
  /**
   * Return `true` if the attribution is collapsible, `false` otherwise.
   * @return {boolean} True if the widget is collapsible.
   * @api
   */
  getCollapsible() {
    return this.collapsible_;
  }
  /**
   * Set whether the attribution should be collapsible.
   * @param {boolean} collapsible True if the widget is collapsible.
   * @api
   */
  setCollapsible(e) {
    this.collapsible_ !== e && (this.collapsible_ = e, this.element.classList.toggle("ol-uncollapsible"), this.userCollapsed_ && this.handleToggle_());
  }
  /**
   * Collapse or expand the attribution according to the passed parameter. Will
   * not do anything if the attribution isn't collapsible or if the current
   * collapsed state is already the one requested.
   * @param {boolean} collapsed True if the widget is collapsed.
   * @api
   */
  setCollapsed(e) {
    this.userCollapsed_ = e, !(!this.collapsible_ || this.collapsed_ === e) && this.handleToggle_();
  }
  /**
   * Return `true` when the attribution is currently collapsed or `false`
   * otherwise.
   * @return {boolean} True if the widget is collapsed.
   * @api
   */
  getCollapsed() {
    return this.collapsed_;
  }
  /**
   * Update the attribution element.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @override
   */
  render(e) {
    this.updateElement_(e.frameState);
  }
};
var W_ = X_;
var Y_ = class extends Ga {
  /**
   * @param {Options} [options] Rotate options.
   */
  constructor(e) {
    e = e || {}, super({
      element: document.createElement("div"),
      render: e.render,
      target: e.target
    });
    const t = e.className !== void 0 ? e.className : "ol-rotate", n = e.label !== void 0 ? e.label : "⇧", s = e.compassClassName !== void 0 ? e.compassClassName : "ol-compass";
    this.label_ = null, typeof n == "string" ? (this.label_ = document.createElement("span"), this.label_.className = s, this.label_.textContent = n) : (this.label_ = n, this.label_.classList.add(s));
    const r = e.tipLabel ? e.tipLabel : "Reset rotation", o = document.createElement("button");
    o.className = t + "-reset", o.setAttribute("type", "button"), o.title = r, o.appendChild(this.label_), o.addEventListener(
      Z.CLICK,
      this.handleClick_.bind(this),
      false
    );
    const a = t + " " + Ir + " " + da, l = this.element;
    l.className = a, l.appendChild(o), this.callResetNorth_ = e.resetNorth ? e.resetNorth : void 0, this.duration_ = e.duration !== void 0 ? e.duration : 250, this.autoHide_ = e.autoHide !== void 0 ? e.autoHide : true, this.rotation_ = void 0, this.autoHide_ && this.element.classList.add(xs);
  }
  /**
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(e) {
    e.preventDefault(), this.callResetNorth_ !== void 0 ? this.callResetNorth_() : this.resetNorth_();
  }
  /**
   * @private
   */
  resetNorth_() {
    const t = this.getMap().getView();
    if (!t)
      return;
    const n = t.getRotation();
    n !== void 0 && (this.duration_ > 0 && n % (2 * Math.PI) !== 0 ? t.animate({
      rotation: 0,
      duration: this.duration_,
      easing: Ti
    }) : t.setRotation(0));
  }
  /**
   * Update the rotate control element.
   * @param {import("../MapEvent.js").default} mapEvent Map event.
   * @override
   */
  render(e) {
    const t = e.frameState;
    if (!t)
      return;
    const n = t.viewState.rotation;
    if (n != this.rotation_) {
      const s = "rotate(" + n + "rad)";
      if (this.autoHide_) {
        const r = this.element.classList.contains(xs);
        !r && n === 0 ? this.element.classList.add(xs) : r && n !== 0 && this.element.classList.remove(xs);
      }
      this.label_.style.transform = s;
    }
    this.rotation_ = n;
  }
};
var j_ = Y_;
var $_ = class extends Ga {
  /**
   * @param {Options} [options] Zoom options.
   */
  constructor(e) {
    e = e || {}, super({
      element: document.createElement("div"),
      target: e.target
    });
    const t = e.className !== void 0 ? e.className : "ol-zoom", n = e.delta !== void 0 ? e.delta : 1, s = e.zoomInClassName !== void 0 ? e.zoomInClassName : t + "-in", r = e.zoomOutClassName !== void 0 ? e.zoomOutClassName : t + "-out", o = e.zoomInLabel !== void 0 ? e.zoomInLabel : "+", a = e.zoomOutLabel !== void 0 ? e.zoomOutLabel : "–", l = e.zoomInTipLabel !== void 0 ? e.zoomInTipLabel : "Zoom in", c = e.zoomOutTipLabel !== void 0 ? e.zoomOutTipLabel : "Zoom out", h2 = document.createElement("button");
    h2.className = s, h2.setAttribute("type", "button"), h2.title = l, h2.appendChild(
      typeof o == "string" ? document.createTextNode(o) : o
    ), h2.addEventListener(
      Z.CLICK,
      this.handleClick_.bind(this, n),
      false
    );
    const u = document.createElement("button");
    u.className = r, u.setAttribute("type", "button"), u.title = c, u.appendChild(
      typeof a == "string" ? document.createTextNode(a) : a
    ), u.addEventListener(
      Z.CLICK,
      this.handleClick_.bind(this, -n),
      false
    );
    const d = t + " " + Ir + " " + da, f = this.element;
    f.className = d, f.appendChild(h2), f.appendChild(u), this.duration_ = e.duration !== void 0 ? e.duration : 250;
  }
  /**
   * @param {number} delta Zoom delta.
   * @param {MouseEvent} event The event to handle
   * @private
   */
  handleClick_(e, t) {
    t.preventDefault(), this.zoomByDelta_(e);
  }
  /**
   * @param {number} delta Zoom delta.
   * @private
   */
  zoomByDelta_(e) {
    const n = this.getMap().getView();
    if (!n)
      return;
    const s = n.getZoom();
    if (s !== void 0) {
      const r = n.getConstrainedZoom(s + e);
      this.duration_ > 0 ? (n.getAnimating() && n.cancelAnimations(), n.animate({
        zoom: r,
        duration: this.duration_,
        easing: Ti
      })) : n.setZoom(r);
    }
  }
};
var K_ = $_;
function H_(i) {
  i = i || {};
  const e = new Ke();
  return (i.zoom !== void 0 ? i.zoom : true) && e.push(new K_(i.zoomOptions)), (i.rotate !== void 0 ? i.rotate : true) && e.push(new j_(i.rotateOptions)), (i.attribution !== void 0 ? i.attribution : true) && e.push(new W_(i.attributionOptions)), e;
}
var Z_ = class extends _s {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    super(), e = e || {}, this.delta_ = e.delta ? e.delta : 1, this.duration_ = e.duration !== void 0 ? e.duration : 250;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} (if it was a
   * doubleclick) and eventually zooms the map.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(e) {
    let t = false;
    if (e.type == me.DBLCLICK) {
      const n = (
        /** @type {MouseEvent} */
        e.originalEvent
      ), s = e.map, r = e.coordinate, o = n.shiftKey ? -this.delta_ : this.delta_, a = s.getView();
      Aa(a, o, r, this.duration_), n.preventDefault(), t = true;
    }
    return !t;
  }
};
var q_ = Z_;
var J_ = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, super({
      stopDown: vi
    }), this.condition_ = e.condition ? e.condition : Vf, this.lastAngle_ = void 0, this.duration_ = e.duration !== void 0 ? e.duration : 250;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(e) {
    if (!Yr(e))
      return;
    const t = e.map, n = t.getView();
    if (n.getConstraints().rotation === Fa)
      return;
    const s = t.getSize(), r = e.pixel, o = Math.atan2(s[1] / 2 - r[1], r[0] - s[0] / 2);
    if (this.lastAngle_ !== void 0) {
      const a = o - this.lastAngle_;
      n.adjustRotationInternal(-a);
    }
    this.lastAngle_ = o;
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    return Yr(e) ? (e.map.getView().endInteraction(this.duration_), false) : true;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    return Yr(e) && Ea(e) && this.condition_(e) ? (e.map.getView().beginInteraction(), this.lastAngle_ = void 0, true) : false;
  }
};
var Q_ = J_;
var ep = class extends Wh {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {};
    const t = e.condition ? e.condition : Ro;
    super({
      condition: t,
      className: e.className || "ol-dragzoom",
      minArea: e.minArea
    }), this.duration_ = e.duration !== void 0 ? e.duration : 200, this.out_ = e.out !== void 0 ? e.out : false;
  }
  /**
   * Function to execute just before `onboxend` is fired
   * @param {import("../MapBrowserEvent.js").default} event Event.
   * @override
   */
  onBoxEnd(e) {
    const n = (
      /** @type {!import("../View.js").default} */
      this.getMap().getView()
    );
    let s = this.getGeometry();
    if (this.out_) {
      const r = n.rotatedExtentForGeometry(s), o = n.getResolutionForExtentInternal(r), a = n.getResolution() / o;
      s = s.clone(), s.scale(a * a);
    }
    n.fitInternal(s, {
      duration: this.duration_,
      easing: Ti
    });
  }
};
var tp = ep;
var Rn = {
  LEFT: "ArrowLeft",
  UP: "ArrowUp",
  RIGHT: "ArrowRight",
  DOWN: "ArrowDown"
};
var np = class extends _s {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    super(), e = e || {}, this.defaultCondition_ = function(t) {
      return wa(t) && Zc(t);
    }, this.condition_ = e.condition !== void 0 ? e.condition : this.defaultCondition_, this.duration_ = e.duration !== void 0 ? e.duration : 100, this.pixelDelta_ = e.pixelDelta !== void 0 ? e.pixelDelta : 128;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} if it was a
   * `KeyEvent`, and decides the direction to pan to (if an arrow key was
   * pressed).
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(e) {
    let t = false;
    if (e.type == Z.KEYDOWN) {
      const n = (
        /** @type {KeyboardEvent} */
        e.originalEvent
      ), s = n.key;
      if (this.condition_(e) && (s == Rn.DOWN || s == Rn.LEFT || s == Rn.RIGHT || s == Rn.UP)) {
        const o = e.map.getView(), a = o.getResolution() * this.pixelDelta_;
        let l = 0, c = 0;
        s == Rn.DOWN ? c = -a : s == Rn.LEFT ? l = -a : s == Rn.RIGHT ? l = a : c = a;
        const h2 = [l, c];
        Yo(h2, o.getRotation()), ay(o, h2, this.duration_), n.preventDefault(), t = true;
      }
    }
    return !t;
  }
};
var ip = np;
var sp = class extends _s {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    super(), e = e || {}, this.condition_ = e.condition ? e.condition : function(t) {
      return !Yf(t) && Zc(t);
    }, this.delta_ = e.delta ? e.delta : 1, this.duration_ = e.duration !== void 0 ? e.duration : 100;
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} if it was a
   * `KeyEvent`, and decides whether to zoom in or out (depending on whether the
   * key pressed was '+' or '-').
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(e) {
    let t = false;
    if (e.type == Z.KEYDOWN || e.type == Z.KEYPRESS) {
      const n = (
        /** @type {KeyboardEvent} */
        e.originalEvent
      ), s = n.key;
      if (this.condition_(e) && (s === "+" || s === "-")) {
        const r = e.map, o = s === "+" ? this.delta_ : -this.delta_, a = r.getView();
        Aa(a, o, void 0, this.duration_), n.preventDefault(), t = true;
      }
    }
    return !t;
  }
};
var rp = sp;
var op = 40;
var ap = 300;
var lp = class extends _s {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {}, super(
      /** @type {import("./Interaction.js").InteractionOptions} */
      e
    ), this.totalDelta_ = 0, this.lastDelta_ = 0, this.maxDelta_ = e.maxDelta !== void 0 ? e.maxDelta : 1, this.duration_ = e.duration !== void 0 ? e.duration : 250, this.timeout_ = e.timeout !== void 0 ? e.timeout : 80, this.useAnchor_ = e.useAnchor !== void 0 ? e.useAnchor : true, this.constrainResolution_ = e.constrainResolution !== void 0 ? e.constrainResolution : false;
    const t = e.condition ? e.condition : $i;
    this.condition_ = e.onFocusOnly ? bo(Hc, t) : t, this.lastAnchor_ = null, this.startTime_ = void 0, this.timeoutId_, this.mode_ = void 0, this.trackpadEventGap_ = 400, this.trackpadTimeoutId_, this.deltaPerZoom_ = 300;
  }
  /**
   * @private
   */
  endInteraction_() {
    this.trackpadTimeoutId_ = void 0;
    const e = this.getMap();
    if (!e)
      return;
    e.getView().endInteraction(
      void 0,
      this.lastDelta_ ? this.lastDelta_ > 0 ? 1 : -1 : 0,
      this.lastAnchor_ ? e.getCoordinateFromPixel(this.lastAnchor_) : null
    );
  }
  /**
   * Handles the {@link module:ol/MapBrowserEvent~MapBrowserEvent map browser event} (if it was a mousewheel-event) and eventually
   * zooms the map.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Map browser event.
   * @return {boolean} `false` to stop event propagation.
   * @override
   */
  handleEvent(e) {
    if (!this.condition_(e) || e.type !== Z.WHEEL)
      return true;
    const n = e.map, s = (
      /** @type {WheelEvent} */
      e.originalEvent
    );
    s.preventDefault(), this.useAnchor_ && (this.lastAnchor_ = e.pixel);
    let r = s.deltaY;
    switch (s.deltaMode) {
      case WheelEvent.DOM_DELTA_LINE:
        r *= op;
        break;
      case WheelEvent.DOM_DELTA_PAGE:
        r *= ap;
        break;
    }
    if (r === 0)
      return false;
    this.lastDelta_ = r;
    const o = Date.now();
    this.startTime_ === void 0 && (this.startTime_ = o), (!this.mode_ || o - this.startTime_ > this.trackpadEventGap_) && (this.mode_ = Math.abs(r) < 4 ? "trackpad" : "wheel");
    const a = n.getView();
    if (this.mode_ === "trackpad" && !(a.getConstrainResolution() || this.constrainResolution_))
      return this.trackpadTimeoutId_ ? clearTimeout(this.trackpadTimeoutId_) : (a.getAnimating() && a.cancelAnimations(), a.beginInteraction()), this.trackpadTimeoutId_ = setTimeout(
        this.endInteraction_.bind(this),
        this.timeout_
      ), a.adjustZoom(
        -r / this.deltaPerZoom_,
        this.lastAnchor_ ? n.getCoordinateFromPixel(this.lastAnchor_) : null
      ), this.startTime_ = o, false;
    this.totalDelta_ += r;
    const l = Math.max(this.timeout_ - (o - this.startTime_), 0);
    return clearTimeout(this.timeoutId_), this.timeoutId_ = setTimeout(
      this.handleWheelZoom_.bind(this, n),
      l
    ), false;
  }
  /**
   * @private
   * @param {import("../Map.js").default} map Map.
   */
  handleWheelZoom_(e) {
    const t = e.getView();
    t.getAnimating() && t.cancelAnimations();
    let n = -xe(
      this.totalDelta_,
      -this.maxDelta_ * this.deltaPerZoom_,
      this.maxDelta_ * this.deltaPerZoom_
    ) / this.deltaPerZoom_;
    (t.getConstrainResolution() || this.constrainResolution_) && (n = n ? n > 0 ? 1 : -1 : 0), Aa(
      t,
      n,
      this.lastAnchor_ ? e.getCoordinateFromPixel(this.lastAnchor_) : null,
      this.duration_
    ), this.mode_ = void 0, this.totalDelta_ = 0, this.lastAnchor_ = null, this.startTime_ = void 0, this.timeoutId_ = void 0;
  }
  /**
   * Enable or disable using the mouse's location as an anchor when zooming
   * @param {boolean} useAnchor true to zoom to the mouse's location, false
   * to zoom to the center of the map
   * @api
   */
  setMouseAnchor(e) {
    this.useAnchor_ = e, e || (this.lastAnchor_ = null);
  }
};
var cp = lp;
var hp = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {};
    const t = (
      /** @type {import("./Pointer.js").Options} */
      e
    );
    t.stopDown || (t.stopDown = vi), super(t), this.anchor_ = null, this.lastAngle_ = void 0, this.rotating_ = false, this.rotationDelta_ = 0, this.threshold_ = e.threshold !== void 0 ? e.threshold : 0.3, this.duration_ = e.duration !== void 0 ? e.duration : 250;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(e) {
    let t = 0;
    const n = this.targetPointers[0], s = this.targetPointers[1], r = Math.atan2(
      s.clientY - n.clientY,
      s.clientX - n.clientX
    );
    if (this.lastAngle_ !== void 0) {
      const l = r - this.lastAngle_;
      this.rotationDelta_ += l, !this.rotating_ && Math.abs(this.rotationDelta_) > this.threshold_ && (this.rotating_ = true), t = l;
    }
    this.lastAngle_ = r;
    const o = e.map, a = o.getView();
    a.getConstraints().rotation !== Fa && (this.anchor_ = o.getCoordinateFromPixelInternal(
      o.getEventPixel(ka(this.targetPointers))
    ), this.rotating_ && (o.render(), a.adjustRotationInternal(t, this.anchor_)));
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    return this.targetPointers.length < 2 ? (e.map.getView().endInteraction(this.duration_), false) : true;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    if (this.targetPointers.length >= 2) {
      const t = e.map;
      return this.anchor_ = null, this.lastAngle_ = void 0, this.rotating_ = false, this.rotationDelta_ = 0, this.handlingDownUpSequence || t.getView().beginInteraction(), true;
    }
    return false;
  }
};
var up = hp;
var dp = class extends Xn {
  /**
   * @param {Options} [options] Options.
   */
  constructor(e) {
    e = e || {};
    const t = (
      /** @type {import("./Pointer.js").Options} */
      e
    );
    t.stopDown || (t.stopDown = vi), super(t), this.anchor_ = null, this.duration_ = e.duration !== void 0 ? e.duration : 400, this.lastDistance_ = void 0, this.lastScaleDelta_ = 1;
  }
  /**
   * Handle pointer drag events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @override
   */
  handleDragEvent(e) {
    let t = 1;
    const n = this.targetPointers[0], s = this.targetPointers[1], r = n.clientX - s.clientX, o = n.clientY - s.clientY, a = Math.sqrt(r * r + o * o);
    this.lastDistance_ !== void 0 && (t = this.lastDistance_ / a), this.lastDistance_ = a;
    const l = e.map, c = l.getView();
    t != 1 && (this.lastScaleDelta_ = t), this.anchor_ = l.getCoordinateFromPixelInternal(
      l.getEventPixel(ka(this.targetPointers))
    ), l.render(), c.adjustResolutionInternal(t, this.anchor_);
  }
  /**
   * Handle pointer up events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleUpEvent(e) {
    if (this.targetPointers.length < 2) {
      const n = e.map.getView(), s = this.lastScaleDelta_ > 1 ? 1 : -1;
      return n.endInteraction(this.duration_, s), false;
    }
    return true;
  }
  /**
   * Handle pointer down events.
   * @param {import("../MapBrowserEvent.js").default} mapBrowserEvent Event.
   * @return {boolean} If the event was consumed.
   * @override
   */
  handleDownEvent(e) {
    if (this.targetPointers.length >= 2) {
      const t = e.map;
      return this.anchor_ = null, this.lastDistance_ = void 0, this.lastScaleDelta_ = 1, this.handlingDownUpSequence || t.getView().beginInteraction(), true;
    }
    return false;
  }
};
var fp = dp;
function jh(i) {
  i = i || {};
  const e = new Ke(), t = new A_(-5e-3, 0.05, 100);
  return (i.altShiftDragRotate !== void 0 ? i.altShiftDragRotate : true) && e.push(new Q_()), (i.doubleClickZoom !== void 0 ? i.doubleClickZoom : true) && e.push(
    new q_({
      delta: i.zoomDelta,
      duration: i.zoomDuration
    })
  ), (i.dragPan !== void 0 ? i.dragPan : true) && e.push(
    new Na({
      onFocusOnly: i.onFocusOnly,
      kinetic: t
    })
  ), (i.pinchRotate !== void 0 ? i.pinchRotate : true) && e.push(new up()), (i.pinchZoom !== void 0 ? i.pinchZoom : true) && e.push(
    new fp({
      duration: i.zoomDuration
    })
  ), (i.keyboard !== void 0 ? i.keyboard : true) && (e.push(new ip()), e.push(
    new rp({
      delta: i.zoomDelta,
      duration: i.zoomDuration
    })
  )), (i.mouseWheelZoom !== void 0 ? i.mouseWheelZoom : true) && e.push(
    new cp({
      onFocusOnly: i.onFocusOnly,
      duration: i.zoomDuration
    })
  ), (i.shiftDragZoom !== void 0 ? i.shiftDragZoom : true) && e.push(
    new tp({
      duration: i.zoomDuration
    })
  ), e;
}
var on = {
  /**
   * Triggered when a layer is added
   * @event GroupEvent#addlayer
   * @api
   */
  ADDLAYER: "addlayer",
  /**
   * Triggered when a layer is removed
   * @event GroupEvent#removelayer
   * @api
   */
  REMOVELAYER: "removelayer"
};
var ln = class extends It {
  /**
   * @param {GroupEventType} type The event type.
   * @param {BaseLayer} layer The layer.
   */
  constructor(e, t) {
    super(e), this.layer = t;
  }
};
var so = {
  LAYERS: "layers"
};
var Ba = class _Ba extends Ih {
  /**
   * @param {Options} [options] Layer options.
   */
  constructor(e) {
    e = e || {};
    const t = (
      /** @type {Options} */
      Object.assign({}, e)
    );
    delete t.layers;
    let n = e.layers;
    super(t), this.on, this.once, this.un, this.layersListenerKeys_ = [], this.listenerKeys_ = {}, this.addChangeListener(so.LAYERS, this.handleLayersChanged_), n ? Array.isArray(n) ? n = new Ke(n.slice(), { unique: true }) : _e(
      typeof /** @type {?} */
      n.getArray == "function",
      "Expected `layers` to be an array or a `Collection`"
    ) : n = new Ke(void 0, { unique: true }), this.setLayers(n);
  }
  /**
   * @private
   */
  handleLayerChange_() {
    this.changed();
  }
  /**
   * @private
   */
  handleLayersChanged_() {
    this.layersListenerKeys_.forEach(Ee), this.layersListenerKeys_.length = 0;
    const e = this.getLayers();
    this.layersListenerKeys_.push(
      le(e, Je.ADD, this.handleLayersAdd_, this),
      le(
        e,
        Je.REMOVE,
        this.handleLayersRemove_,
        this
      )
    );
    for (const n in this.listenerKeys_)
      this.listenerKeys_[n].forEach(Ee);
    os(this.listenerKeys_);
    const t = e.getArray();
    for (let n = 0, s = t.length; n < s; n++) {
      const r = t[n];
      this.registerLayerListeners_(r), this.dispatchEvent(new ln(on.ADDLAYER, r));
    }
    this.changed();
  }
  /**
   * @param {BaseLayer} layer The layer.
   */
  registerLayerListeners_(e) {
    const t = [
      le(
        e,
        mi.PROPERTYCHANGE,
        this.handleLayerChange_,
        this
      ),
      le(e, Z.CHANGE, this.handleLayerChange_, this)
    ];
    e instanceof _Ba && t.push(
      le(e, on.ADDLAYER, this.handleLayerGroupAdd_, this),
      le(
        e,
        on.REMOVELAYER,
        this.handleLayerGroupRemove_,
        this
      )
    ), this.listenerKeys_[de(e)] = t;
  }
  /**
   * @param {GroupEvent} event The layer group event.
   */
  handleLayerGroupAdd_(e) {
    this.dispatchEvent(new ln(on.ADDLAYER, e.layer));
  }
  /**
   * @param {GroupEvent} event The layer group event.
   */
  handleLayerGroupRemove_(e) {
    this.dispatchEvent(new ln(on.REMOVELAYER, e.layer));
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<import("./Base.js").default>} collectionEvent CollectionEvent.
   * @private
   */
  handleLayersAdd_(e) {
    const t = e.element;
    this.registerLayerListeners_(t), this.dispatchEvent(new ln(on.ADDLAYER, t)), this.changed();
  }
  /**
   * @param {import("../Collection.js").CollectionEvent<import("./Base.js").default>} collectionEvent CollectionEvent.
   * @private
   */
  handleLayersRemove_(e) {
    const t = e.element, n = de(t);
    this.listenerKeys_[n].forEach(Ee), delete this.listenerKeys_[n], this.dispatchEvent(new ln(on.REMOVELAYER, t)), this.changed();
  }
  /**
   * Returns the {@link module:ol/Collection~Collection collection} of {@link module:ol/layer/Layer~Layer layers}
   * in this group.
   * @return {!Collection<import("./Base.js").default>} Collection of
   *   {@link module:ol/layer/Base~BaseLayer layers} that are part of this group.
   * @observable
   * @api
   */
  getLayers() {
    return (
      /** @type {!Collection<import("./Base.js").default>} */
      this.get(so.LAYERS)
    );
  }
  /**
   * Set the {@link module:ol/Collection~Collection collection} of {@link module:ol/layer/Layer~Layer layers}
   * in this group.
   * @param {!Collection<import("./Base.js").default>} layers Collection of
   *   {@link module:ol/layer/Base~BaseLayer layers} that are part of this group.
   * @observable
   * @api
   */
  setLayers(e) {
    const t = this.getLayers();
    if (t) {
      const n = t.getArray();
      for (let s = 0, r = n.length; s < r; ++s)
        this.dispatchEvent(
          new ln(on.REMOVELAYER, n[s])
        );
    }
    this.set(so.LAYERS, e);
  }
  /**
   * @param {Array<import("./Layer.js").default>} [array] Array of layers (to be modified in place).
   * @return {Array<import("./Layer.js").default>} Array of layers.
   * @override
   */
  getLayersArray(e) {
    return e = e !== void 0 ? e : [], this.getLayers().forEach(function(t) {
      t.getLayersArray(e);
    }), e;
  }
  /**
   * Get the layer states list and use this groups z-index as the default
   * for all layers in this and nested groups, if it is unset at this point.
   * If dest is not provided and this group's z-index is undefined
   * 0 is used a the default z-index.
   * @param {Array<import("./Layer.js").State>} [dest] Optional list
   * of layer states (to be modified in place).
   * @return {Array<import("./Layer.js").State>} List of layer states.
   * @override
   */
  getLayerStatesArray(e) {
    const t = e !== void 0 ? e : [], n = t.length;
    this.getLayers().forEach(function(o) {
      o.getLayerStatesArray(t);
    });
    const s = this.getLayerState();
    let r = s.zIndex;
    !e && s.zIndex === void 0 && (r = 0);
    for (let o = n, a = t.length; o < a; o++) {
      const l = t[o];
      l.opacity *= s.opacity, l.visible = l.visible && s.visible, l.maxResolution = Math.min(
        l.maxResolution,
        s.maxResolution
      ), l.minResolution = Math.max(
        l.minResolution,
        s.minResolution
      ), l.minZoom = Math.max(l.minZoom, s.minZoom), l.maxZoom = Math.min(l.maxZoom, s.maxZoom), s.extent !== void 0 && (l.extent !== void 0 ? l.extent = On(
        l.extent,
        s.extent
      ) : l.extent = s.extent), l.zIndex === void 0 && (l.zIndex = r);
    }
    return t;
  }
  /**
   * @return {import("../source/Source.js").State} Source state.
   * @override
   */
  getSourceState() {
    return "ready";
  }
};
var Rr = Ba;
var gp = class extends gr {
  /**
   * @param {import("../Map.js").default} map Map.
   */
  constructor(e) {
    super(), this.map_ = e;
  }
  /**
   * @abstract
   * @param {import("../render/EventType.js").default} type Event type.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   */
  dispatchRenderEvent(e, t) {
    ue();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @protected
   */
  calculateMatrices2D(e) {
    const t = e.viewState, n = e.coordinateToPixelTransform, s = e.pixelToCoordinateTransform;
    Zt(
      n,
      e.size[0] / 2,
      e.size[1] / 2,
      1 / t.resolution,
      -1 / t.resolution,
      -t.rotation,
      -t.center[0],
      -t.center[1]
    ), Lc(s, n);
  }
  /**
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {boolean} checkWrapped Check for wrapped geometries.
   * @param {import("./vector.js").FeatureCallback<T>} callback Feature callback.
   * @param {S} thisArg Value to use as `this` when executing `callback`.
   * @param {function(this: U, import("../layer/Layer.js").default): boolean} layerFilter Layer filter
   *     function, only layers which are visible and for which this function
   *     returns `true` will be tested for features.  By default, all visible
   *     layers will be tested.
   * @param {U} thisArg2 Value to use as `this` when executing `layerFilter`.
   * @return {T|undefined} Callback result.
   * @template S,T,U
   */
  forEachFeatureAtCoordinate(e, t, n, s, r, o, a, l) {
    let c;
    const h2 = t.viewState;
    function u(S, w, x, b) {
      return r.call(o, w, S ? x : null, b);
    }
    const d = h2.projection, f = Cc(e.slice(), d), g = [[0, 0]];
    if (d.canWrapX() && s) {
      const S = d.getExtent(), w = ye(S);
      g.push([-w, 0], [w, 0]);
    }
    const y = t.layerStatesArray, m = y.length, _ = (
      /** @type {Array<HitMatch<T>>} */
      []
    ), p = [];
    for (let S = 0; S < g.length; S++)
      for (let w = m - 1; w >= 0; --w) {
        const x = y[w], b = x.layer;
        if (b.hasRenderer() && Pa(x, h2) && a.call(l, b)) {
          const v = b.getRenderer(), R = b.getSource();
          if (v && R) {
            const P = R.getWrapX() ? f : e, U = u.bind(
              null,
              x.managed
            );
            p[0] = P[0] + g[S][0], p[1] = P[1] + g[S][1], c = v.forEachFeatureAtCoordinate(
              p,
              t,
              n,
              U,
              _
            );
          }
          if (c)
            return c;
        }
      }
    if (_.length === 0)
      return;
    const E = 1 / _.length;
    return _.forEach((S, w) => S.distanceSq += w * E), _.sort((S, w) => S.distanceSq - w.distanceSq), _.some((S) => c = S.callback(S.feature, S.layer, S.geometry)), c;
  }
  /**
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {import("../Map.js").FrameState} frameState FrameState.
   * @param {number} hitTolerance Hit tolerance in pixels.
   * @param {boolean} checkWrapped Check for wrapped geometries.
   * @param {function(this: U, import("../layer/Layer.js").default): boolean} layerFilter Layer filter
   *     function, only layers which are visible and for which this function
   *     returns `true` will be tested for features.  By default, all visible
   *     layers will be tested.
   * @param {U} thisArg Value to use as `this` when executing `layerFilter`.
   * @return {boolean} Is there a feature at the given coordinate?
   * @template U
   */
  hasFeatureAtCoordinate(e, t, n, s, r, o) {
    return this.forEachFeatureAtCoordinate(
      e,
      t,
      n,
      s,
      _n,
      this,
      r,
      o
    ) !== void 0;
  }
  /**
   * @return {import("../Map.js").default} Map.
   */
  getMap() {
    return this.map_;
  }
  /**
   * Render.
   * @abstract
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   */
  renderFrame(e) {
    ue();
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @protected
   */
  scheduleExpireIconCache(e) {
    gt.canExpireCache() && e.postRenderFunctions.push(mp);
  }
};
function mp(i, e) {
  gt.expire();
}
var yp = gp;
var _p = class extends yp {
  /**
   * @param {import("../Map.js").default} map Map.
   */
  constructor(e) {
    super(e), this.fontChangeListenerKey_ = le(
      ti,
      mi.PROPERTYCHANGE,
      e.redrawText,
      e
    ), this.element_ = ft ? zc() : document.createElement("div");
    const t = this.element_.style;
    t.position = "absolute", t.width = "100%", t.height = "100%", t.zIndex = "0", this.element_.className = Ir + " ol-layers";
    const n = e.getViewport();
    n && n.insertBefore(this.element_, n.firstChild || null), this.children_ = [], this.renderedVisible_ = true;
  }
  /**
   * @param {import("../render/EventType.js").default} type Event type.
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @override
   */
  dispatchRenderEvent(e, t) {
    const n = this.getMap();
    if (n.hasListener(e)) {
      const s = new dh(e, void 0, t);
      n.dispatchEvent(s);
    }
  }
  /**
   * @override
   */
  disposeInternal() {
    Ee(this.fontChangeListenerKey_), this.element_.remove(), super.disposeInternal();
  }
  /**
   * Render.
   * @param {?import("../Map.js").FrameState} frameState Frame state.
   * @override
   */
  renderFrame(e) {
    if (!e) {
      this.renderedVisible_ && (this.element_.style.display = "none", this.renderedVisible_ = false);
      return;
    }
    this.calculateMatrices2D(e), this.dispatchRenderEvent(mt.PRECOMPOSE, e);
    const t = e.layerStatesArray.sort(
      (c, h2) => c.zIndex - h2.zIndex
    );
    t.some(
      (c) => c.layer instanceof vh && c.layer.getDeclutter()
    ) && (e.declutter = {});
    const s = e.viewState;
    this.children_.length = 0;
    const r = [];
    let o = null;
    for (let c = 0, h2 = t.length; c < h2; ++c) {
      const u = t[c];
      e.layerIndex = c;
      const d = u.layer, f = d.getSourceState();
      if (!Pa(u, s) || f != "ready" && f != "undefined") {
        d.unrender();
        continue;
      }
      const g = d.render(e, o);
      g && (g !== o && (this.children_.push(g), o = g), r.push(u));
    }
    this.declutter(e, r), uf(this.element_, this.children_);
    const l = this.getMap().getTargetElement();
    if (un(l)) {
      const c = l.getContext("2d");
      for (const h2 of this.children_) {
        const u = h2.firstElementChild || h2, d = h2.style.backgroundColor;
        if (d && (!un(u) || u.width > 0) && (c.fillStyle = d, c.fillRect(0, 0, l.width, l.height)), un(u) && u.width > 0) {
          const f = h2.style.opacity || u.style.opacity;
          c.globalAlpha = f === "" ? 1 : Number(f);
          const g = u.style.transform;
          if (g)
            c.setTransform(
              .../** @type {[number, number, number, number, number, number]} */
              Co(g)
            );
          else {
            const y = parseFloat(u.style.width) / u.width, m = parseFloat(u.style.height) / u.height;
            c.setTransform(y, 0, 0, m, 0, 0);
          }
          c.drawImage(u, 0, 0);
        }
      }
      c.globalAlpha = 1, c.setTransform(1, 0, 0, 1, 0, 0);
    }
    this.dispatchRenderEvent(mt.POSTCOMPOSE, e), this.renderedVisible_ || (this.element_.style.display = "", this.renderedVisible_ = true), this.scheduleExpireIconCache(e);
  }
  /**
   * @param {import("../Map.js").FrameState} frameState Frame state.
   * @param {Array<import('../layer/Layer.js').State>} layerStates Layers.
   */
  declutter(e, t) {
    if (e.declutter) {
      for (let n = t.length - 1; n >= 0; --n) {
        const s = t[n], r = s.layer;
        r.getDeclutter() && r.renderDeclutter(e, s);
      }
      t.forEach(
        (n) => n.layer.renderDeferred(e)
      );
    }
  }
};
var pp = _p;
function $h(i) {
  if (i instanceof br) {
    i.setMapInternal(null);
    return;
  }
  i instanceof Rr && i.getLayers().forEach($h);
}
function Kh(i, e) {
  if (i instanceof br) {
    i.setMapInternal(e);
    return;
  }
  if (i instanceof Rr) {
    const t = i.getLayers().getArray();
    for (let n = 0, s = t.length; n < s; ++n)
      Kh(t[n], e);
  }
}
var Sp = class extends vt {
  /**
   * @param {MapOptions} [options] Map options.
   */
  constructor(e) {
    super(), e = e || {}, this.on, this.once, this.un;
    const t = Ep(e);
    this.renderComplete_ = false, this.loaded_ = true, this.boundHandleBrowserEvent_ = this.handleBrowserEvent.bind(this), this.maxTilesLoading_ = e.maxTilesLoading !== void 0 ? e.maxTilesLoading : 16, this.pixelRatio_ = e.pixelRatio !== void 0 ? e.pixelRatio : lf, this.postRenderTimeoutHandle_, this.animationDelayKey_, this.animationDelay_ = this.animationDelay_.bind(this), this.coordinateToPixelTransform_ = Ct(), this.pixelToCoordinateTransform_ = Ct(), this.frameIndex_ = 0, this.frameState_ = null, this.previousExtent_ = null, this.viewPropertyListenerKey_ = null, this.viewChangeListenerKey_ = null, this.layerGroupPropertyListenerKeys_ = null, ft || (this.viewport_ = document.createElement("div"), this.viewport_.className = "ol-viewport" + ("ontouchstart" in window ? " ol-touch" : ""), this.viewport_.style.position = "relative", this.viewport_.style.overflow = "hidden", this.viewport_.style.width = "100%", this.viewport_.style.height = "100%", this.overlayContainer_ = document.createElement("div"), this.overlayContainer_.style.position = "absolute", this.overlayContainer_.style.zIndex = "0", this.overlayContainer_.style.width = "100%", this.overlayContainer_.style.height = "100%", this.overlayContainer_.style.pointerEvents = "none", this.overlayContainer_.className = "ol-overlaycontainer", this.viewport_.appendChild(this.overlayContainer_), this.overlayContainerStopEvent_ = document.createElement("div"), this.overlayContainerStopEvent_.style.position = "absolute", this.overlayContainerStopEvent_.style.zIndex = "0", this.overlayContainerStopEvent_.style.width = "100%", this.overlayContainerStopEvent_.style.height = "100%", this.overlayContainerStopEvent_.style.pointerEvents = "none", this.overlayContainerStopEvent_.className = "ol-overlaycontainer-stopevent", this.viewport_.appendChild(this.overlayContainerStopEvent_)), this.mapBrowserEventHandler_ = null, this.moveTolerance_ = e.moveTolerance, this.keyboardEventTarget_ = t.keyboardEventTarget, this.targetChangeHandlerKeys_ = null, this.targetElement_ = null, ft || (this.resizeObserver_ = new ResizeObserver(() => this.updateSize())), this.controls = t.controls || (ft ? new Ke() : H_()), this.interactions = t.interactions || (ft ? new Ke() : jh({
      onFocusOnly: true
    })), this.overlays_ = t.overlays, this.overlayIdIndex_ = {}, this.renderer_ = null, this.postRenderFunctions_ = [], this.tileQueue_ = new B_(
      this.getTilePriority.bind(this),
      this.handleTileChange_.bind(this)
    ), this.addChangeListener(
      Ge.LAYERGROUP,
      this.handleLayerGroupChanged_
    ), this.addChangeListener(Ge.VIEW, this.handleViewChanged_), this.addChangeListener(Ge.SIZE, this.handleSizeChanged_), this.addChangeListener(Ge.TARGET, this.handleTargetChanged_), this.setProperties(t.values);
    const n = this;
    e.view && !(e.view instanceof Dt) && e.view.then(function(s) {
      n.setView(new Dt(s));
    }), this.controls.addEventListener(
      Je.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./control/Control.js").default>} event CollectionEvent
       */
      (s) => {
        s.element.setMap(this);
      }
    ), this.controls.addEventListener(
      Je.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./control/Control.js").default>} event CollectionEvent.
       */
      (s) => {
        s.element.setMap(null);
      }
    ), this.interactions.addEventListener(
      Je.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./interaction/Interaction.js").default>} event CollectionEvent.
       */
      (s) => {
        s.element.setMap(this);
      }
    ), this.interactions.addEventListener(
      Je.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./interaction/Interaction.js").default>} event CollectionEvent.
       */
      (s) => {
        s.element.setMap(null);
      }
    ), this.overlays_.addEventListener(
      Je.ADD,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./Overlay.js").default>} event CollectionEvent.
       */
      (s) => {
        this.addOverlayInternal_(s.element);
      }
    ), this.overlays_.addEventListener(
      Je.REMOVE,
      /**
       * @param {import("./Collection.js").CollectionEvent<import("./Overlay.js").default>} event CollectionEvent.
       */
      (s) => {
        const r = s.element.getId();
        r !== void 0 && delete this.overlayIdIndex_[r.toString()], s.element.setMap(null);
      }
    ), this.controls.forEach(
      /**
       * @param {import("./control/Control.js").default} control Control.
       */
      (s) => {
        s.setMap(this);
      }
    ), this.interactions.forEach(
      /**
       * @param {import("./interaction/Interaction.js").default} interaction Interaction.
       */
      (s) => {
        s.setMap(this);
      }
    ), this.overlays_.forEach(this.addOverlayInternal_.bind(this));
  }
  /**
   * Add the given control to the map.
   * @param {import("./control/Control.js").default} control Control.
   * @api
   */
  addControl(e) {
    this.getControls().push(e);
  }
  /**
   * Add the given interaction to the map. If you want to add an interaction
   * at another point of the collection use `getInteractions()` and the methods
   * available on {@link module:ol/Collection~Collection}. This can be used to
   * stop the event propagation from the handleEvent function. The interactions
   * get to handle the events in the reverse order of this collection.
   * @param {import("./interaction/Interaction.js").default} interaction Interaction to add.
   * @api
   */
  addInteraction(e) {
    this.getInteractions().push(e);
  }
  /**
   * Adds the given layer to the top of this map. If you want to add a layer
   * elsewhere in the stack, use `getLayers()` and the methods available on
   * {@link module:ol/Collection~Collection}.
   * @param {import("./layer/Base.js").default} layer Layer.
   * @api
   */
  addLayer(e) {
    this.getLayerGroup().getLayers().push(e);
  }
  /**
   * @param {import("./layer/Group.js").GroupEvent} event The layer add event.
   * @private
   */
  handleLayerAdd_(e) {
    Kh(e.layer, this);
  }
  /**
   * Add the given overlay to the map.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @api
   */
  addOverlay(e) {
    this.getOverlays().push(e);
  }
  /**
   * This deals with map's overlay collection changes.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @private
   */
  addOverlayInternal_(e) {
    const t = e.getId();
    t !== void 0 && (this.overlayIdIndex_[t.toString()] = e), e.setMap(this);
  }
  /**
   *
   * Clean up.
   * @override
   */
  disposeInternal() {
    var e;
    this.controls.clear(), this.interactions.clear(), this.overlays_.clear(), (e = this.resizeObserver_) == null || e.disconnect(), this.setTarget(null), super.disposeInternal();
  }
  /**
   * Detect features that intersect a pixel on the viewport, and execute a
   * callback with each intersecting feature. Layers included in the detection can
   * be configured through the `layerFilter` option in `options`.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {function(import("./Feature.js").FeatureLike, import("./layer/Layer.js").default<import("./source/Source").default>, import("./geom/SimpleGeometry.js").default): T} callback Feature callback. The callback will be
   *     called with two arguments. The first argument is one
   *     {@link module:ol/Feature~Feature feature} or
   *     {@link module:ol/render/Feature~RenderFeature render feature} at the pixel, the second is
   *     the {@link module:ol/layer/Layer~Layer layer} of the feature and will be null for
   *     unmanaged layers. To stop detection, callback functions can return a
   *     truthy value.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {T|undefined} Callback result, i.e. the return value of last
   * callback execution, or the first truthy callback return value.
   * @template T
   * @api
   */
  forEachFeatureAtPixel(e, t, n) {
    if (!this.frameState_ || !this.renderer_)
      return;
    const s = this.getCoordinateFromPixelInternal(e);
    n = n !== void 0 ? n : {};
    const r = n.hitTolerance !== void 0 ? n.hitTolerance : 0, o = n.layerFilter !== void 0 ? n.layerFilter : _n, a = n.checkWrapped !== false;
    return this.renderer_.forEachFeatureAtCoordinate(
      s,
      this.frameState_,
      r,
      a,
      t,
      null,
      o,
      null
    );
  }
  /**
   * Get all features that intersect a pixel on the viewport.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {Array<import("./Feature.js").FeatureLike>} The detected features or
   * an empty array if none were found.
   * @api
   */
  getFeaturesAtPixel(e, t) {
    const n = [];
    return this.forEachFeatureAtPixel(
      e,
      function(s) {
        n.push(s);
      },
      t
    ), n;
  }
  /**
   * Get all layers from all layer groups.
   * @return {Array<import("./layer/Layer.js").default>} Layers.
   * @api
   */
  getAllLayers() {
    const e = [];
    function t(n) {
      n.forEach(function(s) {
        s instanceof Rr ? t(s.getLayers()) : e.push(s);
      });
    }
    return t(this.getLayers()), e;
  }
  /**
   * Detect if features intersect a pixel on the viewport. Layers included in the
   * detection can be configured through the `layerFilter` option.
   * For polygons without a fill, only the stroke will be used for hit detection.
   * Polygons must have a fill style applied to ensure that pixels inside a polygon are detected.
   * The fill can be transparent.
   * @param {import("./pixel.js").Pixel} pixel Pixel.
   * @param {AtPixelOptions} [options] Optional options.
   * @return {boolean} Is there a feature at the given pixel?
   * @api
   */
  hasFeatureAtPixel(e, t) {
    if (!this.frameState_ || !this.renderer_)
      return false;
    const n = this.getCoordinateFromPixelInternal(e);
    t = t !== void 0 ? t : {};
    const s = t.layerFilter !== void 0 ? t.layerFilter : _n, r = t.hitTolerance !== void 0 ? t.hitTolerance : 0, o = t.checkWrapped !== false;
    return this.renderer_.hasFeatureAtCoordinate(
      n,
      this.frameState_,
      r,
      o,
      s,
      null
    );
  }
  /**
   * Returns the coordinate in user projection for a browser event.
   * @param {MouseEvent} event Event.
   * @return {import("./coordinate.js").Coordinate} Coordinate.
   * @api
   */
  getEventCoordinate(e) {
    return this.getCoordinateFromPixel(this.getEventPixel(e));
  }
  /**
   * Returns the coordinate in view projection for a browser event.
   * @param {MouseEvent} event Event.
   * @return {import("./coordinate.js").Coordinate} Coordinate.
   */
  getEventCoordinateInternal(e) {
    return this.getCoordinateFromPixelInternal(this.getEventPixel(e));
  }
  /**
   * Returns the map pixel position for a browser event relative to the viewport.
   * @param {UIEvent|{clientX: number, clientY: number}} event Event.
   * @return {import("./pixel.js").Pixel} Pixel.
   * @api
   */
  getEventPixel(e) {
    const n = this.viewport_.getBoundingClientRect(), s = this.getSize(), r = n.width / s[0], o = n.height / s[1], a = (
      //FIXME Are we really calling this with a TouchEvent anywhere?
      "changedTouches" in e ? (
        /** @type {TouchEvent} */
        e.changedTouches[0]
      ) : (
        /** @type {MouseEvent} */
        e
      )
    );
    return [
      (a.clientX - n.left) / r,
      (a.clientY - n.top) / o
    ];
  }
  /**
   * Get the target in which this map is rendered.
   * Note that this returns what is entered as an option or in setTarget:
   * if that was an element, it returns an element; if a string, it returns that.
   * @return {HTMLElement|string|undefined} The Element or id of the Element that the
   *     map is rendered in.
   * @observable
   * @api
   */
  getTarget() {
    return (
      /** @type {HTMLElement|string|undefined} */
      this.get(Ge.TARGET)
    );
  }
  /**
   * Get the DOM element into which this map is rendered. In contrast to
   * `getTarget` this method always return an `Element`, or `null` if the
   * map has no target.
   * @return {HTMLElement} The element that the map is rendered in.
   * @api
   */
  getTargetElement() {
    return this.targetElement_;
  }
  /**
   * Get the coordinate for a given pixel.  This returns a coordinate in the
   * user projection.
   * @param {import("./pixel.js").Pixel} pixel Pixel position in the map viewport.
   * @return {import("./coordinate.js").Coordinate} The coordinate for the pixel position.
   * @api
   */
  getCoordinateFromPixel(e) {
    return wo(
      this.getCoordinateFromPixelInternal(e),
      this.getView().getProjection()
    );
  }
  /**
   * Get the coordinate for a given pixel.  This returns a coordinate in the
   * map view projection.
   * @param {import("./pixel.js").Pixel} pixel Pixel position in the map viewport.
   * @return {import("./coordinate.js").Coordinate} The coordinate for the pixel position.
   */
  getCoordinateFromPixelInternal(e) {
    const t = this.frameState_;
    return t ? Pe(t.pixelToCoordinateTransform, e.slice()) : null;
  }
  /**
   * Get the map controls. Modifying this collection changes the controls
   * associated with the map.
   * @return {Collection<import("./control/Control.js").default>} Controls.
   * @api
   */
  getControls() {
    return this.controls;
  }
  /**
   * Get the map overlays. Modifying this collection changes the overlays
   * associated with the map.
   * @return {Collection<import("./Overlay.js").default>} Overlays.
   * @api
   */
  getOverlays() {
    return this.overlays_;
  }
  /**
   * Get an overlay by its identifier (the value returned by overlay.getId()).
   * Note that the index treats string and numeric identifiers as the same. So
   * `map.getOverlayById(2)` will return an overlay with id `'2'` or `2`.
   * @param {string|number} id Overlay identifier.
   * @return {import("./Overlay.js").default|null} Overlay.
   * @api
   */
  getOverlayById(e) {
    const t = this.overlayIdIndex_[e.toString()];
    return t !== void 0 ? t : null;
  }
  /**
   * Get the map interactions. Modifying this collection changes the interactions
   * associated with the map.
   *
   * Interactions are used for e.g. pan, zoom and rotate.
   * @return {Collection<import("./interaction/Interaction.js").default>} Interactions.
   * @api
   */
  getInteractions() {
    return this.interactions;
  }
  /**
   * Get the layergroup associated with this map.
   * @return {LayerGroup} A layer group containing the layers in this map.
   * @observable
   * @api
   */
  getLayerGroup() {
    return (
      /** @type {LayerGroup} */
      this.get(Ge.LAYERGROUP)
    );
  }
  /**
   * Clear any existing layers and add layers to the map.
   * @param {Array<import("./layer/Base.js").default>|Collection<import("./layer/Base.js").default>} layers The layers to be added to the map.
   * @api
   */
  setLayers(e) {
    const t = this.getLayerGroup();
    if (e instanceof Ke) {
      t.setLayers(e);
      return;
    }
    const n = t.getLayers();
    n.clear(), n.extend(e);
  }
  /**
   * Get the collection of layers associated with this map.
   * @return {!Collection<import("./layer/Base.js").default>} Layers.
   * @api
   */
  getLayers() {
    return this.getLayerGroup().getLayers();
  }
  /**
   * @return {boolean} Layers have sources that are still loading.
   */
  getLoadingOrNotReady() {
    const e = this.getLayerGroup().getLayerStatesArray();
    for (let t = 0, n = e.length; t < n; ++t) {
      const s = e[t];
      if (!s.visible)
        continue;
      const r = s.layer.getRenderer();
      if (r && !r.ready)
        return true;
      const o = s.layer.getSource();
      if (o && o.loading)
        return true;
    }
    return false;
  }
  /**
   * Get the pixel for a coordinate.  This takes a coordinate in the user
   * projection and returns the corresponding pixel.
   * @param {import("./coordinate.js").Coordinate} coordinate A map coordinate.
   * @return {import("./pixel.js").Pixel} A pixel position in the map viewport.
   * @api
   */
  getPixelFromCoordinate(e) {
    const t = Be(
      e,
      this.getView().getProjection()
    );
    return this.getPixelFromCoordinateInternal(t);
  }
  /**
   * Get the pixel for a coordinate.  This takes a coordinate in the map view
   * projection and returns the corresponding pixel.
   * @param {import("./coordinate.js").Coordinate} coordinate A map coordinate.
   * @return {import("./pixel.js").Pixel} A pixel position in the map viewport.
   */
  getPixelFromCoordinateInternal(e) {
    const t = this.frameState_;
    return t ? Pe(
      t.coordinateToPixelTransform,
      e.slice(0, 2)
    ) : null;
  }
  /**
   * Get the map renderer.
   * @return {import("./renderer/Map.js").default|null} Renderer
   */
  getRenderer() {
    return this.renderer_;
  }
  /**
   * Get the size of this map.
   * @return {import("./size.js").Size|undefined} The size in pixels of the map in the DOM.
   * @observable
   * @api
   */
  getSize() {
    return (
      /** @type {import("./size.js").Size|undefined} */
      this.get(Ge.SIZE)
    );
  }
  /**
   * Get the view associated with this map. A view manages properties such as
   * center and resolution.
   * @return {View} The view that controls this map.
   * @observable
   * @api
   */
  getView() {
    return (
      /** @type {View} */
      this.get(Ge.VIEW)
    );
  }
  /**
   * Get the element that serves as the map viewport.
   * @return {HTMLElement} Viewport.
   * @api
   */
  getViewport() {
    return this.viewport_;
  }
  /**
   * Get the element that serves as the container for overlays.  Elements added to
   * this container will let mousedown and touchstart events through to the map,
   * so clicks and gestures on an overlay will trigger {@link module:ol/MapBrowserEvent~MapBrowserEvent}
   * events.
   * @return {!HTMLElement} The map's overlay container.
   */
  getOverlayContainer() {
    return this.overlayContainer_;
  }
  /**
   * Get the element that serves as a container for overlays that don't allow
   * event propagation. Elements added to this container won't let mousedown and
   * touchstart events through to the map, so clicks and gestures on an overlay
   * don't trigger any {@link module:ol/MapBrowserEvent~MapBrowserEvent}.
   * @return {!HTMLElement} The map's overlay container that stops events.
   */
  getOverlayContainerStopEvent() {
    return this.overlayContainerStopEvent_;
  }
  /**
   * @return {!Document} The document where the map is displayed.
   */
  getOwnerDocument() {
    const e = this.getTargetElement();
    return e ? e.ownerDocument : document;
  }
  /**
   * @param {import("./Tile.js").default} tile Tile.
   * @param {string} tileSourceKey Tile source key.
   * @param {import("./coordinate.js").Coordinate} tileCenter Tile center.
   * @param {number} tileResolution Tile resolution.
   * @return {number} Tile priority.
   */
  getTilePriority(e, t, n, s) {
    return z_(
      this.frameState_,
      e,
      t,
      n,
      s
    );
  }
  /**
   * @param {PointerEvent|KeyboardEvent|WheelEvent} browserEvent Browser event.
   * @param {string} [type] Type.
   */
  handleBrowserEvent(e, t) {
    t = t || e.type;
    const n = new Vt(t, this, e);
    this.handleMapBrowserEvent(n);
  }
  /**
   * @param {MapBrowserEvent} mapBrowserEvent The event to handle.
   */
  handleMapBrowserEvent(e) {
    if (!this.frameState_)
      return;
    const t = e.originalEvent, n = t.type;
    if (n === Ao.POINTERDOWN || n === Z.WHEEL || n === Z.KEYDOWN) {
      const s = this.getOwnerDocument(), r = this.viewport_.getRootNode ? this.viewport_.getRootNode() : s, o = (
        /** @type {Node} */
        t.target
      ), a = r instanceof ShadowRoot ? r.host === o ? r.host.ownerDocument : r : r === s ? s.documentElement : r;
      if (
        // Abort if the target is a child of the container for elements whose events are not meant
        // to be handled by map interactions.
        this.overlayContainerStopEvent_.contains(o) || // Abort if the event target is a child of the container that is no longer in the page.
        // It's possible for the target to no longer be in the page if it has been removed in an
        // event listener, this might happen in a Control that recreates it's content based on
        // user interaction either manually or via a render in something like https://reactjs.org/
        !a.contains(o)
      )
        return;
    }
    if (e.frameState = this.frameState_, this.dispatchEvent(e) !== false) {
      const s = this.getInteractions().getArray().slice();
      for (let r = s.length - 1; r >= 0; r--) {
        const o = s[r];
        if (o.getMap() !== this || !o.getActive() || !this.getTargetElement())
          continue;
        if (!o.handleEvent(e) || e.propagationStopped)
          break;
      }
    }
  }
  /**
   * @protected
   */
  handlePostRender() {
    const e = this.frameState_, t = this.tileQueue_;
    if (!t.isEmpty()) {
      let s = this.maxTilesLoading_, r = s;
      if (e) {
        const o = e.viewHints;
        if (o[ze.ANIMATING] || o[ze.INTERACTING]) {
          const a = Date.now() - e.time > 8;
          s = a ? 0 : 8, r = a ? 0 : 2;
        }
      }
      t.getTilesLoading() < s && (t.reprioritize(), t.loadMoreTiles(s, r));
    }
    e && this.renderer_ && !e.animate && (this.renderComplete_ ? (this.hasListener(mt.RENDERCOMPLETE) && this.renderer_.dispatchRenderEvent(
      mt.RENDERCOMPLETE,
      e
    ), this.loaded_ === false && (this.loaded_ = true, this.dispatchEvent(
      new ni(Xt.LOADEND, this, e)
    ))) : this.loaded_ === true && (this.loaded_ = false, this.dispatchEvent(
      new ni(Xt.LOADSTART, this, e)
    )));
    const n = this.postRenderFunctions_;
    if (e)
      for (let s = 0, r = n.length; s < r; ++s)
        n[s](this, e);
    n.length = 0;
  }
  /**
   * @private
   */
  handleSizeChanged_() {
    this.getView() && !this.getView().getAnimating() && this.getView().resolveConstraints(0), this.render();
  }
  /**
   * @private
   */
  handleTargetChanged_() {
    var n, s;
    if (this.mapBrowserEventHandler_) {
      for (let r = 0, o = this.targetChangeHandlerKeys_.length; r < o; ++r)
        Ee(this.targetChangeHandlerKeys_[r]);
      this.targetChangeHandlerKeys_ = null, this.viewport_.removeEventListener(
        Z.CONTEXTMENU,
        this.boundHandleBrowserEvent_
      ), this.viewport_.removeEventListener(
        Z.WHEEL,
        this.boundHandleBrowserEvent_
      ), this.mapBrowserEventHandler_.dispose(), this.mapBrowserEventHandler_ = null, this.viewport_.remove();
    }
    if (this.targetElement_ && !un(this.targetElement_)) {
      (n = this.resizeObserver_) == null || n.unobserve(this.targetElement_);
      const r = this.targetElement_.getRootNode();
      r instanceof ShadowRoot && this.resizeObserver_.unobserve(r.host), this.setSize(void 0);
    }
    const e = this.getTarget(), t = typeof e == "string" ? document.getElementById(e) : e;
    if (this.targetElement_ = t, !t)
      this.renderer_ && (clearTimeout(this.postRenderTimeoutHandle_), this.postRenderTimeoutHandle_ = void 0, this.postRenderFunctions_.length = 0, this.renderer_.dispose(), this.renderer_ = null), this.animationDelayKey_ && (cancelAnimationFrame(this.animationDelayKey_), this.animationDelayKey_ = void 0);
    else {
      if (un(t) || t.appendChild(this.viewport_), this.renderer_ || (this.renderer_ = new pp(this)), !un(t)) {
        this.mapBrowserEventHandler_ = new O_(
          this,
          this.moveTolerance_
        );
        for (const o in me)
          this.mapBrowserEventHandler_.addEventListener(
            me[o],
            this.handleMapBrowserEvent.bind(this)
          );
        this.viewport_.addEventListener(
          Z.CONTEXTMENU,
          this.boundHandleBrowserEvent_,
          false
        ), this.viewport_.addEventListener(
          Z.WHEEL,
          this.boundHandleBrowserEvent_,
          Gc ? { passive: false } : false
        );
        let r;
        if (this.keyboardEventTarget_)
          r = this.keyboardEventTarget_;
        else {
          const o = t.getRootNode();
          r = o instanceof ShadowRoot ? o.host : t;
        }
        if (this.targetChangeHandlerKeys_ = [
          le(
            r,
            Z.KEYDOWN,
            this.handleBrowserEvent,
            this
          ),
          le(
            r,
            Z.KEYPRESS,
            this.handleBrowserEvent,
            this
          )
        ], t instanceof HTMLElement) {
          const o = t.getRootNode();
          o instanceof ShadowRoot && this.resizeObserver_.observe(o.host), (s = this.resizeObserver_) == null || s.observe(t);
        }
      }
      this.updateSize();
    }
  }
  /**
   * @private
   */
  handleTileChange_() {
    this.render();
  }
  /**
   * @private
   */
  handleViewPropertyChanged_() {
    this.render();
  }
  /**
   * @private
   */
  handleViewChanged_() {
    this.viewPropertyListenerKey_ && (Ee(this.viewPropertyListenerKey_), this.viewPropertyListenerKey_ = null), this.viewChangeListenerKey_ && (Ee(this.viewChangeListenerKey_), this.viewChangeListenerKey_ = null);
    const e = this.getView();
    e && (this.updateViewportSize_(this.getSize()), this.viewPropertyListenerKey_ = le(
      e,
      mi.PROPERTYCHANGE,
      this.handleViewPropertyChanged_,
      this
    ), this.viewChangeListenerKey_ = le(
      e,
      Z.CHANGE,
      this.handleViewPropertyChanged_,
      this
    ), e.resolveConstraints(0)), this.render();
  }
  /**
   * @private
   */
  handleLayerGroupChanged_() {
    this.layerGroupPropertyListenerKeys_ && (this.layerGroupPropertyListenerKeys_.forEach(Ee), this.layerGroupPropertyListenerKeys_ = null);
    const e = this.getLayerGroup();
    e && (this.handleLayerAdd_(new ln("addlayer", e)), this.layerGroupPropertyListenerKeys_ = [
      le(e, mi.PROPERTYCHANGE, this.render, this),
      le(e, Z.CHANGE, this.render, this),
      le(e, "addlayer", this.handleLayerAdd_, this),
      le(e, "removelayer", this.handleLayerRemove_, this)
    ]), this.render();
  }
  /**
   * @return {boolean} Is rendered.
   */
  isRendered() {
    return !!this.frameState_;
  }
  /**
   * @private
   */
  animationDelay_() {
    this.animationDelayKey_ = void 0, this.renderFrame_(Date.now());
  }
  /**
   * Requests an immediate render in a synchronous manner.
   * @api
   */
  renderSync() {
    this.animationDelayKey_ && cancelAnimationFrame(this.animationDelayKey_), this.animationDelay_();
  }
  /**
   * Redraws all text after new fonts have loaded
   */
  redrawText() {
    if (!this.frameState_)
      return;
    const e = this.frameState_.layerStatesArray;
    for (let t = 0, n = e.length; t < n; ++t) {
      const s = e[t].layer;
      s.hasRenderer() && s.getRenderer().handleFontsChanged();
    }
  }
  /**
   * Request a map rendering (at the next animation frame).
   * @api
   */
  render() {
    this.renderer_ && this.animationDelayKey_ === void 0 && (this.animationDelayKey_ = requestAnimationFrame(this.animationDelay_));
  }
  /**
   * Remove the given control from the map.
   * @param {import("./control/Control.js").default} control Control.
   * @return {import("./control/Control.js").default|undefined} The removed control (or undefined
   *     if the control was not found).
   * @api
   */
  removeControl(e) {
    return this.getControls().remove(e);
  }
  /**
   * Remove the given interaction from the map.
   * @param {import("./interaction/Interaction.js").default} interaction Interaction to remove.
   * @return {import("./interaction/Interaction.js").default|undefined} The removed interaction (or
   *     undefined if the interaction was not found).
   * @api
   */
  removeInteraction(e) {
    return this.getInteractions().remove(e);
  }
  /**
   * Removes the given layer from the map.
   * @param {import("./layer/Base.js").default} layer Layer.
   * @return {import("./layer/Base.js").default|undefined} The removed layer (or undefined if the
   *     layer was not found).
   * @api
   */
  removeLayer(e) {
    return this.getLayerGroup().getLayers().remove(e);
  }
  /**
   * @param {import("./layer/Group.js").GroupEvent} event The layer remove event.
   * @private
   */
  handleLayerRemove_(e) {
    $h(e.layer);
  }
  /**
   * Remove the given overlay from the map.
   * @param {import("./Overlay.js").default} overlay Overlay.
   * @return {import("./Overlay.js").default|undefined} The removed overlay (or undefined
   *     if the overlay was not found).
   * @api
   */
  removeOverlay(e) {
    return this.getOverlays().remove(e);
  }
  /**
   * @param {number} time Time.
   * @private
   */
  renderFrame_(e) {
    const t = this.getSize(), n = this.getView(), s = this.frameState_;
    let r = null;
    if (t !== void 0 && gl(t) && n && n.isDef()) {
      const o = n.getHints(
        this.frameState_ ? this.frameState_.viewHints : void 0
      ), a = n.getState();
      if (r = {
        animate: false,
        coordinateToPixelTransform: this.coordinateToPixelTransform_,
        declutter: null,
        extent: mo(
          a.center,
          a.resolution,
          a.rotation,
          t
        ),
        index: this.frameIndex_++,
        layerIndex: 0,
        layerStatesArray: this.getLayerGroup().getLayerStatesArray(),
        pixelRatio: this.pixelRatio_,
        pixelToCoordinateTransform: this.pixelToCoordinateTransform_,
        postRenderFunctions: [],
        size: t,
        tileQueue: this.tileQueue_,
        time: e,
        usedTiles: {},
        viewState: a,
        viewHints: o,
        wantedTiles: {},
        mapId: de(this),
        renderTargets: {}
      }, a.nextCenter && a.nextResolution) {
        const l = isNaN(a.nextRotation) ? a.rotation : a.nextRotation;
        r.nextExtent = mo(
          a.nextCenter,
          a.nextResolution,
          l,
          t
        );
      }
    }
    this.frameState_ = r, this.renderer_.renderFrame(r), r && (r.animate && this.render(), Array.prototype.push.apply(
      this.postRenderFunctions_,
      r.postRenderFunctions
    ), s && (!this.previousExtent_ || !hs(this.previousExtent_) && !Hi(r.extent, this.previousExtent_)) && (this.dispatchEvent(
      new ni(Xt.MOVESTART, this, s)
    ), this.previousExtent_ = Mi(this.previousExtent_)), this.previousExtent_ && !r.viewHints[ze.ANIMATING] && !r.viewHints[ze.INTERACTING] && !Hi(r.extent, this.previousExtent_) && (this.dispatchEvent(
      new ni(Xt.MOVEEND, this, r)
    ), dc(r.extent, this.previousExtent_))), this.dispatchEvent(new ni(Xt.POSTRENDER, this, r)), this.renderComplete_ = (this.hasListener(Xt.LOADSTART) || this.hasListener(Xt.LOADEND) || this.hasListener(mt.RENDERCOMPLETE)) && !this.tileQueue_.getTilesLoading() && !this.tileQueue_.getCount() && !this.getLoadingOrNotReady(), this.postRenderTimeoutHandle_ || (this.postRenderTimeoutHandle_ = setTimeout(() => {
      this.postRenderTimeoutHandle_ = void 0, this.handlePostRender();
    }, 0));
  }
  /**
   * Sets the layergroup of this map.
   * @param {LayerGroup} layerGroup A layer group containing the layers in this map.
   * @observable
   * @api
   */
  setLayerGroup(e) {
    const t = this.getLayerGroup();
    t && this.handleLayerRemove_(new ln("removelayer", t)), this.set(Ge.LAYERGROUP, e);
  }
  /**
   * Set the size of this map.
   * @param {import("./size.js").Size|undefined} size The size in pixels of the map in the DOM.
   * @observable
   * @api
   */
  setSize(e) {
    this.set(Ge.SIZE, e);
  }
  /**
   * Set the target element to render this map into.
   * For accessibility (focus and keyboard events for map navigation), the `target` element must have a
   *  properly configured `tabindex` attribute. If the `target` element is inside a Shadow DOM, the
   *  `tabindex` atribute must be set on the custom element's host element.
   * @param {HTMLElement|string} [target] The Element or id of the Element
   *     that the map is rendered in.
   * @observable
   * @api
   */
  setTarget(e) {
    this.set(Ge.TARGET, e);
  }
  /**
   * Set the view for this map.
   * @param {View|Promise<import("./View.js").ViewOptions>|null} view The view that controls this map.
   * It is also possible to pass a promise that resolves to options for constructing a view.  This
   * alternative allows view properties to be resolved by sources or other components that load
   * view-related metadata.
   * @observable
   * @api
   */
  setView(e) {
    if (!e || e instanceof Dt) {
      this.set(Ge.VIEW, e);
      return;
    }
    this.set(Ge.VIEW, new Dt());
    const t = this;
    e.then(function(n) {
      t.setView(new Dt(n));
    });
  }
  /**
   * Force a recalculation of the map viewport size.  This should be called when
   * third-party code changes the size of the map viewport.
   * @api
   */
  updateSize() {
    const e = this.getTargetElement();
    let t;
    if (e) {
      let s, r;
      if (un(e))
        s = e.width, r = e.height;
      else {
        const o = getComputedStyle(e);
        s = e.offsetWidth - parseFloat(o.borderLeftWidth) - parseFloat(o.paddingLeft) - parseFloat(o.paddingRight) - parseFloat(o.borderRightWidth), r = e.offsetHeight - parseFloat(o.borderTopWidth) - parseFloat(o.paddingTop) - parseFloat(o.paddingBottom) - parseFloat(o.borderBottomWidth);
      }
      !isNaN(s) && !isNaN(r) && (t = [Math.max(0, s), Math.max(0, r)], !gl(t) && (e.offsetWidth || e.offsetHeight || e.getClientRects().length) && Ec(
        "No map visible because the map container's width or height are 0."
      ));
    }
    const n = this.getSize();
    t && (!n || !Sn(t, n)) && (this.setSize(t), this.updateViewportSize_(t));
  }
  /**
   * Recomputes the viewport size and save it on the view object (if any)
   * @param {import("./size.js").Size|undefined} size The size.
   * @private
   */
  updateViewportSize_(e) {
    const t = this.getView();
    t && t.setViewportSize(e);
  }
};
function Ep(i) {
  let e = null;
  i.keyboardEventTarget !== void 0 && (e = typeof i.keyboardEventTarget == "string" ? document.getElementById(i.keyboardEventTarget) : i.keyboardEventTarget);
  const t = {}, n = i.layers && typeof /** @type {?} */
  i.layers.getLayers == "function" ? (
    /** @type {LayerGroup} */
    i.layers
  ) : new Rr({
    layers: (
      /** @type {Collection<import("./layer/Base.js").default>|Array<import("./layer/Base.js").default>} */
      i.layers
    )
  });
  t[Ge.LAYERGROUP] = n, t[Ge.TARGET] = i.target, t[Ge.VIEW] = i.view instanceof Dt ? i.view : new Dt();
  let s;
  i.controls !== void 0 && (Array.isArray(i.controls) ? s = new Ke(i.controls.slice()) : (_e(
    typeof /** @type {?} */
    i.controls.getArray == "function",
    "Expected `controls` to be an array or an `ol/Collection.js`"
  ), s = i.controls));
  let r;
  i.interactions !== void 0 && (Array.isArray(i.interactions) ? r = new Ke(i.interactions.slice()) : (_e(
    typeof /** @type {?} */
    i.interactions.getArray == "function",
    "Expected `interactions` to be an array or an `ol/Collection.js`"
  ), r = i.interactions));
  let o;
  return i.overlays !== void 0 ? Array.isArray(i.overlays) ? o = new Ke(i.overlays.slice()) : (_e(
    typeof /** @type {?} */
    i.overlays.getArray == "function",
    "Expected `overlays` to be an array or an `ol/Collection.js`"
  ), o = i.overlays) : o = new Ke(), {
    controls: s,
    interactions: r,
    keyboardEventTarget: e,
    overlays: o,
    values: t
  };
}
var wp = Sp;
var Ue = {
  ELEMENT: "element",
  MAP: "map",
  OFFSET: "offset",
  POSITION: "position",
  POSITIONING: "positioning"
};
var Cp = class extends vt {
  /**
   * @param {Options} options Overlay options.
   */
  constructor(e) {
    super(), this.on, this.once, this.un, this.options = e, this.id = e.id, this.insertFirst = e.insertFirst !== void 0 ? e.insertFirst : true, this.stopEvent = e.stopEvent !== void 0 ? e.stopEvent : true, this.element = document.createElement("div"), this.element.className = e.className !== void 0 ? e.className : "ol-overlay-container " + Mf, this.element.style.position = "absolute", this.element.style.pointerEvents = "auto", this.autoPan = e.autoPan === true ? {} : e.autoPan || void 0, this.rendered = {
      transform_: "",
      visible: true
    }, this.mapPostrenderListenerKey = null, this.addChangeListener(Ue.ELEMENT, this.handleElementChanged), this.addChangeListener(Ue.MAP, this.handleMapChanged), this.addChangeListener(Ue.OFFSET, this.handleOffsetChanged), this.addChangeListener(Ue.POSITION, this.handlePositionChanged), this.addChangeListener(Ue.POSITIONING, this.handlePositioningChanged), e.element !== void 0 && this.setElement(e.element), this.setOffset(e.offset !== void 0 ? e.offset : [0, 0]), this.setPositioning(e.positioning || "top-left"), e.position !== void 0 && this.setPosition(e.position);
  }
  /**
   * Get the DOM element of this overlay.
   * @return {HTMLElement|undefined} The Element containing the overlay.
   * @observable
   * @api
   */
  getElement() {
    return (
      /** @type {HTMLElement|undefined} */
      this.get(Ue.ELEMENT)
    );
  }
  /**
   * Get the overlay identifier which is set on constructor.
   * @return {number|string|undefined} Id.
   * @api
   */
  getId() {
    return this.id;
  }
  /**
   * Get the map associated with this overlay.
   * @return {import("./Map.js").default|null} The map that the
   * overlay is part of.
   * @observable
   * @api
   */
  getMap() {
    return (
      /** @type {import("./Map.js").default|null} */
      this.get(Ue.MAP) || null
    );
  }
  /**
   * Get the offset of this overlay.
   * @return {Array<number>} The offset.
   * @observable
   * @api
   */
  getOffset() {
    return (
      /** @type {Array<number>} */
      this.get(Ue.OFFSET)
    );
  }
  /**
   * Get the current position of this overlay.
   * @return {import("./coordinate.js").Coordinate|undefined} The spatial point that the overlay is
   *     anchored at.
   * @observable
   * @api
   */
  getPosition() {
    return (
      /** @type {import("./coordinate.js").Coordinate|undefined} */
      this.get(Ue.POSITION)
    );
  }
  /**
   * Get the current positioning of this overlay.
   * @return {Positioning} How the overlay is positioned
   *     relative to its point on the map.
   * @observable
   * @api
   */
  getPositioning() {
    return (
      /** @type {Positioning} */
      this.get(Ue.POSITIONING)
    );
  }
  /**
   * @protected
   */
  handleElementChanged() {
    Bc(this.element);
    const e = this.getElement();
    e && this.element.appendChild(e);
  }
  /**
   * @protected
   */
  handleMapChanged() {
    var t;
    this.mapPostrenderListenerKey && ((t = this.element) == null || t.remove(), Ee(this.mapPostrenderListenerKey), this.mapPostrenderListenerKey = null);
    const e = this.getMap();
    if (e) {
      this.mapPostrenderListenerKey = le(
        e,
        Xt.POSTRENDER,
        this.render,
        this
      ), this.updatePixelPosition();
      const n = this.stopEvent ? e.getOverlayContainerStopEvent() : e.getOverlayContainer();
      this.insertFirst ? n.insertBefore(this.element, n.childNodes[0] || null) : n.appendChild(this.element), this.performAutoPan();
    }
  }
  /**
   * @protected
   */
  render() {
    this.updatePixelPosition();
  }
  /**
   * @protected
   */
  handleOffsetChanged() {
    this.updatePixelPosition();
  }
  /**
   * @protected
   */
  handlePositionChanged() {
    this.updatePixelPosition(), this.performAutoPan();
  }
  /**
   * @protected
   */
  handlePositioningChanged() {
    this.updatePixelPosition();
  }
  /**
   * Set the DOM element to be associated with this overlay.
   * @param {HTMLElement|undefined} element The Element containing the overlay.
   * @observable
   * @api
   */
  setElement(e) {
    this.set(Ue.ELEMENT, e);
  }
  /**
   * Set the map to be associated with this overlay.
   * @param {import("./Map.js").default|null} map The map that the
   * overlay is part of. Pass `null` to just remove the overlay from the current map.
   * @observable
   * @api
   */
  setMap(e) {
    this.set(Ue.MAP, e);
  }
  /**
   * Set the offset for this overlay.
   * @param {Array<number>} offset Offset.
   * @observable
   * @api
   */
  setOffset(e) {
    this.set(Ue.OFFSET, e);
  }
  /**
   * Set the position for this overlay. If the position is `undefined` the
   * overlay is hidden.
   * @param {import("./coordinate.js").Coordinate|undefined} position The spatial point that the overlay
   *     is anchored at.
   * @observable
   * @api
   */
  setPosition(e) {
    this.set(Ue.POSITION, e);
  }
  /**
   * Pan the map so that the overlay is entirely visible in the current viewport
   * (if necessary) using the configured autoPan parameters
   * @protected
   */
  performAutoPan() {
    this.autoPan && this.panIntoView(this.autoPan);
  }
  /**
   * Pan the map so that the overlay is entirely visible in the current viewport
   * (if necessary).
   * @param {PanIntoViewOptions} [panIntoViewOptions] Options for the pan action
   * @api
   */
  panIntoView(e) {
    const t = this.getMap();
    if (!t || !t.getTargetElement() || !this.get(Ue.POSITION))
      return;
    const n = this.getRect(t.getTargetElement(), t.getSize()), s = this.getElement(), r = this.getRect(s, [
      cf(s),
      hf(s)
    ]);
    e = e || {};
    const o = e.margin === void 0 ? 20 : e.margin;
    if (!ai(n, r)) {
      const a = r[0] - n[0], l = n[2] - r[2], c = r[1] - n[1], h2 = n[3] - r[3], u = [0, 0];
      if (a < 0 ? u[0] = a - o : l < 0 && (u[0] = Math.abs(l) + o), c < 0 ? u[1] = c - o : h2 < 0 && (u[1] = Math.abs(h2) + o), u[0] !== 0 || u[1] !== 0) {
        const d = (
          /** @type {import("./coordinate.js").Coordinate} */
          t.getView().getCenterInternal()
        ), f = t.getPixelFromCoordinateInternal(d);
        if (!f)
          return;
        const g = [f[0] + u[0], f[1] + u[1]], y = e.animation || {};
        t.getView().animateInternal({
          center: t.getCoordinateFromPixelInternal(g),
          duration: y.duration,
          easing: y.easing
        });
      }
    }
  }
  /**
   * Get the extent of an element relative to the document
   * @param {HTMLElement} element The element.
   * @param {import("./size.js").Size} size The size of the element.
   * @return {import("./extent.js").Extent} The extent.
   * @protected
   */
  getRect(e, t) {
    const n = e.getBoundingClientRect(), s = n.left + window.pageXOffset, r = n.top + window.pageYOffset;
    return [s, r, s + t[0], r + t[1]];
  }
  /**
   * Set the positioning for this overlay.
   * @param {Positioning} positioning how the overlay is
   *     positioned relative to its point on the map.
   * @observable
   * @api
   */
  setPositioning(e) {
    this.set(Ue.POSITIONING, e);
  }
  /**
   * Modify the visibility of the element.
   * @param {boolean} visible Element visibility.
   * @protected
   */
  setVisible(e) {
    this.rendered.visible !== e && (this.element.style.display = e ? "" : "none", this.rendered.visible = e);
  }
  /**
   * Update pixel position.
   * @protected
   */
  updatePixelPosition() {
    const e = this.getMap(), t = this.getPosition();
    if (!e || !e.isRendered() || !t) {
      this.setVisible(false);
      return;
    }
    const n = e.getPixelFromCoordinate(t), s = e.getSize();
    this.updateRenderedPosition(n, s);
  }
  /**
   * @param {import("./pixel.js").Pixel} pixel The pixel location.
   * @param {import("./size.js").Size|undefined} mapSize The map size.
   * @protected
   */
  updateRenderedPosition(e, t) {
    const n = this.element.style, s = this.getOffset(), r = this.getPositioning();
    this.setVisible(true);
    const o = `${e[0] + s[0]}px`, a = `${e[1] + s[1]}px`;
    let l = "0%", c = "0%";
    r == "bottom-right" || r == "center-right" || r == "top-right" ? l = "-100%" : (r == "bottom-center" || r == "center-center" || r == "top-center") && (l = "-50%"), r == "bottom-left" || r == "bottom-center" || r == "bottom-right" ? c = "-100%" : (r == "center-left" || r == "center-center" || r == "center-right") && (c = "-50%");
    const h2 = `translate(${l}, ${c}) translate(${o}, ${a})`;
    this.rendered.transform_ != h2 && (this.rendered.transform_ = h2, n.transform = h2);
  }
  /**
   * returns the options this Overlay has been created with
   * @return {Options} overlay options
   */
  getOptions() {
    return this.options;
  }
};
var xp = Cp;
var Hh = class {
  /**
   * @param {number} minX Minimum X.
   * @param {number} maxX Maximum X.
   * @param {number} minY Minimum Y.
   * @param {number} maxY Maximum Y.
   */
  constructor(e, t, n, s) {
    this.minX = e, this.maxX = t, this.minY = n, this.maxY = s;
  }
  /**
   * @param {import("./tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {boolean} Contains tile coordinate.
   */
  contains(e) {
    return this.containsXY(e[1], e[2]);
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Contains.
   */
  containsTileRange(e) {
    return this.minX <= e.minX && e.maxX <= this.maxX && this.minY <= e.minY && e.maxY <= this.maxY;
  }
  /**
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @return {boolean} Contains coordinate.
   */
  containsXY(e, t) {
    return this.minX <= e && e <= this.maxX && this.minY <= t && t <= this.maxY;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Equals.
   */
  equals(e) {
    return this.minX == e.minX && this.minY == e.minY && this.maxX == e.maxX && this.maxY == e.maxY;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   */
  extend(e) {
    e.minX < this.minX && (this.minX = e.minX), e.maxX > this.maxX && (this.maxX = e.maxX), e.minY < this.minY && (this.minY = e.minY), e.maxY > this.maxY && (this.maxY = e.maxY);
  }
  /**
   * @return {number} Height.
   */
  getHeight() {
    return this.maxY - this.minY + 1;
  }
  /**
   * @return {import("./size.js").Size} Size.
   */
  getSize() {
    return [this.getWidth(), this.getHeight()];
  }
  /**
   * @return {number} Width.
   */
  getWidth() {
    return this.maxX - this.minX + 1;
  }
  /**
   * @param {TileRange} tileRange Tile range.
   * @return {boolean} Intersects.
   */
  intersects(e) {
    return this.minX <= e.maxX && this.maxX >= e.minX && this.minY <= e.maxY && this.maxY >= e.minY;
  }
};
function Jn(i, e, t, n, s) {
  return s !== void 0 ? (s.minX = i, s.maxX = e, s.minY = t, s.maxY = n, s) : new Hh(i, e, t, n);
}
var Zh = Hh;
function ko(i) {
  return i instanceof Image || i instanceof HTMLCanvasElement || i instanceof HTMLVideoElement || i instanceof ImageBitmap ? i : null;
}
var Ip = new Error("disposed");
var vp = [256, 256];
var Mp = class extends Ua {
  /**
   * @param {Options} options Tile options.
   */
  constructor(e) {
    const t = X.IDLE;
    super(e.tileCoord, t, {
      transition: e.transition,
      interpolate: e.interpolate
    }), this.loader_ = e.loader, this.data_ = null, this.error_ = null, this.size_ = e.size || null, this.controller_ = e.controller || null;
  }
  /**
   * Get the tile size.
   * @return {import('./size.js').Size} Tile size.
   */
  getSize() {
    if (this.size_)
      return this.size_;
    const e = ko(this.data_);
    return e ? [e.width, e.height] : vp;
  }
  /**
   * Get the data for the tile.
   * @return {Data} Tile data.
   * @api
   */
  getData() {
    return this.data_;
  }
  /**
   * Get any loading error.
   * @return {Error} Loading error.
   * @api
   */
  getError() {
    return this.error_;
  }
  /**
   * Load the tile data.
   * @api
   * @override
   */
  load() {
    if (this.state !== X.IDLE && this.state !== X.ERROR)
      return;
    this.state = X.LOADING, this.changed();
    const e = this;
    this.loader_().then(function(t) {
      e.data_ = t, e.state = X.LOADED, e.changed();
    }).catch(function(t) {
      e.error_ = t, e.state = X.ERROR, e.changed();
    });
  }
  /**
   * Clean up.
   * @override
   */
  disposeInternal() {
    this.controller_ && (this.controller_.abort(Ip), this.controller_ = null), super.disposeInternal();
  }
};
var jl = Mp;
var ro;
var gi = [];
function $l(i, e, t, n, s) {
  i.beginPath(), i.moveTo(0, 0), i.lineTo(e, t), i.lineTo(n, s), i.closePath(), i.save(), i.clip(), i.fillRect(0, 0, Math.max(e, n) + 1, Math.max(t, s)), i.restore();
}
function oo(i, e) {
  return Math.abs(i[e * 4] - 210) > 2 || Math.abs(i[e * 4 + 3] - 0.75 * 255) > 2;
}
function Tp() {
  if (ro === void 0) {
    const i = Fe(6, 6, gi);
    i.globalCompositeOperation = "lighter", i.fillStyle = "rgba(210, 0, 0, 0.75)", $l(i, 4, 5, 4, 0), $l(i, 4, 5, 0, 5);
    const e = i.getImageData(0, 0, 3, 3).data;
    ro = oo(e, 0) || oo(e, 4) || oo(e, 8), xr(i), gi.push(i.canvas);
  }
  return ro;
}
function Kl(i, e, t, n) {
  const s = Sr(t, e, i);
  let r = sl(
    e,
    n,
    t
  );
  const o = e.getMetersPerUnit();
  o !== void 0 && (r *= o);
  const a = i.getMetersPerUnit();
  a !== void 0 && (r /= a);
  const l = i.getExtent();
  if (!l || _i(l, s)) {
    const c = sl(i, r, s) / r;
    isFinite(c) && c > 0 && (r /= c);
  }
  return r;
}
function bp(i, e, t, n) {
  const s = pn(t);
  let r = Kl(
    i,
    e,
    s,
    n
  );
  return (!isFinite(r) || r <= 0) && Wo(t, function(o) {
    return r = Kl(
      i,
      e,
      o,
      n
    ), isFinite(r) && r > 0;
  }), r;
}
function Rp(i, e, t, n, s, r, o, a, l, c, h2, u, d, f) {
  const g = Fe(
    Math.round(t * i),
    Math.round(t * e),
    gi
  );
  if (u || (g.imageSmoothingEnabled = false), l.length === 0)
    return g.canvas;
  g.scale(t, t);
  function y(w) {
    return Math.round(w * t) / t;
  }
  g.globalCompositeOperation = "lighter";
  const m = _t();
  l.forEach(function(w, x, b) {
    gc(m, w.extent);
  });
  let _;
  const p = t / n, E = (u ? 1 : 1 + Math.pow(2, -24)) / p;
  if (!d || l.length !== 1 || c !== 0) {
    if (_ = Fe(
      Math.round(ye(m) * p),
      Math.round(Ve(m) * p),
      gi
    ), u || (_.imageSmoothingEnabled = false), s && f) {
      const w = (s[0] - m[0]) * p, x = -(s[3] - m[3]) * p, b = ye(s) * p, v = Ve(s) * p;
      _.rect(w, x, b, v), _.clip();
    }
    l.forEach(function(w, x, b) {
      if (w.image.width > 0 && w.image.height > 0) {
        if (w.clipExtent) {
          _.save();
          const N = (w.clipExtent[0] - m[0]) * p, O = -(w.clipExtent[3] - m[3]) * p, G = ye(w.clipExtent) * p, $ = Ve(w.clipExtent) * p;
          _.rect(
            u ? N : Math.round(N),
            u ? O : Math.round(O),
            u ? G : Math.round(N + G) - Math.round(N),
            u ? $ : Math.round(O + $) - Math.round(O)
          ), _.clip();
        }
        const v = (w.extent[0] - m[0]) * p, R = -(w.extent[3] - m[3]) * p, P = ye(w.extent) * p, U = Ve(w.extent) * p;
        _.drawImage(
          w.image,
          c,
          c,
          w.image.width - 2 * c,
          w.image.height - 2 * c,
          u ? v : Math.round(v),
          u ? R : Math.round(R),
          u ? P : Math.round(v + P) - Math.round(v),
          u ? U : Math.round(R + U) - Math.round(R)
        ), w.clipExtent && _.restore();
      }
    });
  }
  const S = En(o);
  return a.getTriangles().forEach(function(w, x, b) {
    const v = w.source, R = w.target;
    let P = v[0][0], U = v[0][1], N = v[1][0], O = v[1][1], G = v[2][0], $ = v[2][1];
    const z = y((R[0][0] - S[0]) / r), H = y(
      -(R[0][1] - S[1]) / r
    ), V = y((R[1][0] - S[0]) / r), q = y(
      -(R[1][1] - S[1]) / r
    ), ne = y((R[2][0] - S[0]) / r), fe = y(
      -(R[2][1] - S[1]) / r
    ), F = P, Te = U;
    P = 0, U = 0, N -= F, O -= Te, G -= F, $ -= Te;
    const Me = [
      [N, O, 0, 0, V - z],
      [G, $, 0, 0, ne - z],
      [0, 0, N, O, q - H],
      [0, 0, G, $, fe - H]
    ], Ce = td(Me);
    if (!Ce)
      return;
    if (g.save(), g.beginPath(), Tp() || !u) {
      g.moveTo(V, q);
      const be = 4, it = z - V, He = H - q;
      for (let Ne = 0; Ne < be; Ne++)
        g.lineTo(
          V + y((Ne + 1) * it / be),
          q + y(Ne * He / (be - 1))
        ), Ne != be - 1 && g.lineTo(
          V + y((Ne + 1) * it / be),
          q + y((Ne + 1) * He / (be - 1))
        );
      g.lineTo(ne, fe);
    } else
      g.moveTo(V, q), g.lineTo(z, H), g.lineTo(ne, fe);
    g.clip(), g.transform(
      Ce[0],
      Ce[2],
      Ce[1],
      Ce[3],
      z,
      H
    ), g.translate(
      m[0] - F,
      m[3] - Te
    );
    let Oe;
    if (_)
      Oe = _.canvas, g.scale(E, -E);
    else {
      const be = l[0], it = be.extent;
      Oe = be.image, g.scale(
        ye(it) / Oe.width,
        -Ve(it) / Oe.height
      );
    }
    g.drawImage(Oe, 0, 0), g.restore();
  }), _ && (xr(_), gi.push(_.canvas)), h2 && (g.save(), g.globalCompositeOperation = "source-over", g.strokeStyle = "black", g.lineWidth = 1, a.getTriangles().forEach(function(w, x, b) {
    const v = w.target, R = (v[0][0] - S[0]) / r, P = -(v[0][1] - S[1]) / r, U = (v[1][0] - S[0]) / r, N = -(v[1][1] - S[1]) / r, O = (v[2][0] - S[0]) / r, G = -(v[2][1] - S[1]) / r;
    g.beginPath(), g.moveTo(U, N), g.lineTo(R, P), g.lineTo(O, G), g.closePath(), g.stroke();
  }), g.restore()), g.canvas;
}
var Lp = 10;
var Hl = 0.25;
var Fp = class {
  /**
   * @param {import("../proj/Projection.js").default} sourceProj Source projection.
   * @param {import("../proj/Projection.js").default} targetProj Target projection.
   * @param {import("../extent.js").Extent} targetExtent Target extent to triangulate.
   * @param {import("../extent.js").Extent} maxSourceExtent Maximal source extent that can be used.
   * @param {number} errorThreshold Acceptable error (in source units).
   * @param {?number} destinationResolution The (optional) resolution of the destination.
   * @param {import("../transform.js").Transform} [sourceMatrix] Source transform matrix.
   */
  constructor(e, t, n, s, r, o, a) {
    this.sourceProj_ = e, this.targetProj_ = t;
    let l = {};
    const c = a ? Dd(
      (E) => Pe(
        a,
        Sr(E, this.targetProj_, this.sourceProj_)
      )
    ) : Ji(this.targetProj_, this.sourceProj_);
    this.transformInv_ = function(E) {
      const S = E[0] + "/" + E[1];
      return l[S] || (l[S] = c(E)), l[S];
    }, this.maxSourceExtent_ = s, this.errorThresholdSquared_ = r * r, this.triangles_ = [], this.wrapsXInSource_ = false, this.canWrapXInSource_ = this.sourceProj_.canWrapX() && !!s && !!this.sourceProj_.getExtent() && ye(s) >= ye(this.sourceProj_.getExtent()), this.sourceWorldWidth_ = this.sourceProj_.getExtent() ? ye(this.sourceProj_.getExtent()) : null, this.targetWorldWidth_ = this.targetProj_.getExtent() ? ye(this.targetProj_.getExtent()) : null;
    const h2 = En(n), u = cs(n), d = ls(n), f = pi(n), g = this.transformInv_(h2), y = this.transformInv_(u), m = this.transformInv_(d), _ = this.transformInv_(f), p = Lp + (o ? Math.max(
      0,
      Math.ceil(
        Math.log2(
          go(n) / (o * o * 256 * 256)
        )
      )
    ) : 0);
    if (this.addQuad_(
      h2,
      u,
      d,
      f,
      g,
      y,
      m,
      _,
      p
    ), this.wrapsXInSource_) {
      let E = 1 / 0;
      this.triangles_.forEach(function(S, w, x) {
        E = Math.min(
          E,
          S.source[0][0],
          S.source[1][0],
          S.source[2][0]
        );
      }), this.triangles_.forEach((S) => {
        if (Math.max(
          S.source[0][0],
          S.source[1][0],
          S.source[2][0]
        ) - E > this.sourceWorldWidth_ / 2) {
          const w = [
            [S.source[0][0], S.source[0][1]],
            [S.source[1][0], S.source[1][1]],
            [S.source[2][0], S.source[2][1]]
          ];
          w[0][0] - E > this.sourceWorldWidth_ / 2 && (w[0][0] -= this.sourceWorldWidth_), w[1][0] - E > this.sourceWorldWidth_ / 2 && (w[1][0] -= this.sourceWorldWidth_), w[2][0] - E > this.sourceWorldWidth_ / 2 && (w[2][0] -= this.sourceWorldWidth_);
          const x = Math.min(
            w[0][0],
            w[1][0],
            w[2][0]
          );
          Math.max(
            w[0][0],
            w[1][0],
            w[2][0]
          ) - x < this.sourceWorldWidth_ / 2 && (S.source = w);
        }
      });
    }
    l = {};
  }
  /**
   * Adds triangle to the triangulation.
   * @param {import("../coordinate.js").Coordinate} a The target a coordinate.
   * @param {import("../coordinate.js").Coordinate} b The target b coordinate.
   * @param {import("../coordinate.js").Coordinate} c The target c coordinate.
   * @param {import("../coordinate.js").Coordinate} aSrc The source a coordinate.
   * @param {import("../coordinate.js").Coordinate} bSrc The source b coordinate.
   * @param {import("../coordinate.js").Coordinate} cSrc The source c coordinate.
   * @private
   */
  addTriangle_(e, t, n, s, r, o) {
    this.triangles_.push({
      source: [s, r, o],
      target: [e, t, n]
    });
  }
  /**
   * Adds quad (points in clock-wise order) to the triangulation
   * (and reprojects the vertices) if valid.
   * Performs quad subdivision if needed to increase precision.
   *
   * @param {import("../coordinate.js").Coordinate} a The target a coordinate.
   * @param {import("../coordinate.js").Coordinate} b The target b coordinate.
   * @param {import("../coordinate.js").Coordinate} c The target c coordinate.
   * @param {import("../coordinate.js").Coordinate} d The target d coordinate.
   * @param {import("../coordinate.js").Coordinate} aSrc The source a coordinate.
   * @param {import("../coordinate.js").Coordinate} bSrc The source b coordinate.
   * @param {import("../coordinate.js").Coordinate} cSrc The source c coordinate.
   * @param {import("../coordinate.js").Coordinate} dSrc The source d coordinate.
   * @param {number} maxSubdivision Maximal allowed subdivision of the quad.
   * @private
   */
  addQuad_(e, t, n, s, r, o, a, l, c) {
    const h2 = Ws([r, o, a, l]), u = this.sourceWorldWidth_ ? ye(h2) / this.sourceWorldWidth_ : null, d = (
      /** @type {number} */
      this.sourceWorldWidth_
    ), f = this.sourceProj_.canWrapX() && u > 0.5 && u < 1;
    let g = false;
    if (c > 0) {
      if (this.targetProj_.isGlobal() && this.targetWorldWidth_) {
        const m = Ws([e, t, n, s]);
        g = ye(m) / this.targetWorldWidth_ > Hl || g;
      }
      !f && this.sourceProj_.isGlobal() && u && (g = u > Hl || g);
    }
    if (!g && this.maxSourceExtent_ && isFinite(h2[0]) && isFinite(h2[1]) && isFinite(h2[2]) && isFinite(h2[3]) && !$e(h2, this.maxSourceExtent_))
      return;
    let y = 0;
    if (!g && (!isFinite(r[0]) || !isFinite(r[1]) || !isFinite(o[0]) || !isFinite(o[1]) || !isFinite(a[0]) || !isFinite(a[1]) || !isFinite(l[0]) || !isFinite(l[1]))) {
      if (c > 0)
        g = true;
      else if (y = (!isFinite(r[0]) || !isFinite(r[1]) ? 8 : 0) + (!isFinite(o[0]) || !isFinite(o[1]) ? 4 : 0) + (!isFinite(a[0]) || !isFinite(a[1]) ? 2 : 0) + (!isFinite(l[0]) || !isFinite(l[1]) ? 1 : 0), y != 1 && y != 2 && y != 4 && y != 8)
        return;
    }
    if (c > 0) {
      if (!g) {
        const m = [(e[0] + n[0]) / 2, (e[1] + n[1]) / 2], _ = this.transformInv_(m);
        let p;
        f ? p = (gn(r[0], d) + gn(a[0], d)) / 2 - gn(_[0], d) : p = (r[0] + a[0]) / 2 - _[0];
        const E = (r[1] + a[1]) / 2 - _[1];
        g = p * p + E * E > this.errorThresholdSquared_;
      }
      if (g) {
        if (Math.abs(e[0] - n[0]) <= Math.abs(e[1] - n[1])) {
          const m = [(t[0] + n[0]) / 2, (t[1] + n[1]) / 2], _ = this.transformInv_(m), p = [(s[0] + e[0]) / 2, (s[1] + e[1]) / 2], E = this.transformInv_(p);
          this.addQuad_(
            e,
            t,
            m,
            p,
            r,
            o,
            _,
            E,
            c - 1
          ), this.addQuad_(
            p,
            m,
            n,
            s,
            E,
            _,
            a,
            l,
            c - 1
          );
        } else {
          const m = [(e[0] + t[0]) / 2, (e[1] + t[1]) / 2], _ = this.transformInv_(m), p = [(n[0] + s[0]) / 2, (n[1] + s[1]) / 2], E = this.transformInv_(p);
          this.addQuad_(
            e,
            m,
            p,
            s,
            r,
            _,
            E,
            l,
            c - 1
          ), this.addQuad_(
            m,
            t,
            n,
            p,
            _,
            o,
            a,
            E,
            c - 1
          );
        }
        return;
      }
    }
    if (f) {
      if (!this.canWrapXInSource_)
        return;
      this.wrapsXInSource_ = true;
    }
    y & 11 || this.addTriangle_(e, n, s, r, a, l), y & 14 || this.addTriangle_(e, n, t, r, a, o), y && (y & 13 || this.addTriangle_(t, s, e, o, l, r), y & 7 || this.addTriangle_(t, s, n, o, l, a));
  }
  /**
   * Calculates extent of the `source` coordinates from all the triangles.
   *
   * @return {import("../extent.js").Extent} Calculated extent.
   */
  calculateSourceExtent() {
    const e = _t();
    return this.triangles_.forEach(function(t, n, s) {
      const r = t.source;
      Yi(e, r[0]), Yi(e, r[1]), Yi(e, r[2]);
    }), e;
  }
  /**
   * @return {Array<Triangle>} Array of the calculated triangles.
   */
  getTriangles() {
    return this.triangles_;
  }
};
var Dp = Fp;
var Pp = 0.5;
var Ap = class extends Ua {
  /**
   * @param {import("../proj/Projection.js").default} sourceProj Source projection.
   * @param {import("../tilegrid/TileGrid.js").default} sourceTileGrid Source tile grid.
   * @param {import("../proj/Projection.js").default} targetProj Target projection.
   * @param {import("../tilegrid/TileGrid.js").default} targetTileGrid Target tile grid.
   * @param {import("../tilecoord.js").TileCoord} tileCoord Coordinate of the tile.
   * @param {import("../tilecoord.js").TileCoord} wrappedTileCoord Coordinate of the tile wrapped in X.
   * @param {number} pixelRatio Pixel ratio.
   * @param {number} gutter Gutter of the source tiles.
   * @param {FunctionType} getTileFunction
   *     Function returning source tiles (z, x, y, pixelRatio).
   * @param {number} [errorThreshold] Acceptable reprojection error (in px).
   * @param {boolean} [renderEdges] Render reprojection edges.
   * @param {import("../Tile.js").Options} [options] Tile options.
   */
  constructor(e, t, n, s, r, o, a, l, c, h2, u, d) {
    super(r, X.IDLE, d), this.renderEdges_ = u !== void 0 ? u : false, this.pixelRatio_ = a, this.gutter_ = l, this.canvas_ = null, this.sourceTileGrid_ = t, this.targetTileGrid_ = s, this.wrappedTileCoord_ = o || r, this.sourceTiles_ = [], this.sourcesListenerKeys_ = null, this.sourceZ_ = 0, this.clipExtent_ = e.canWrapX() ? e.getExtent() : void 0;
    const f = s.getTileCoordExtent(
      this.wrappedTileCoord_
    ), g = this.targetTileGrid_.getExtent();
    let y = this.sourceTileGrid_.getExtent();
    const m = g ? On(f, g) : f;
    if (go(m) === 0) {
      this.state = X.EMPTY;
      return;
    }
    const _ = e.getExtent();
    _ && (y ? y = On(y, _) : y = _);
    const p = s.getResolution(
      this.wrappedTileCoord_[0]
    ), E = bp(
      e,
      n,
      m,
      p
    );
    if (!isFinite(E) || E <= 0) {
      this.state = X.EMPTY;
      return;
    }
    const S = h2 !== void 0 ? h2 : Pp;
    if (this.triangulation_ = new Dp(
      e,
      n,
      m,
      y,
      E * S,
      p
    ), this.triangulation_.getTriangles().length === 0) {
      this.state = X.EMPTY;
      return;
    }
    this.sourceZ_ = t.getZForResolution(E);
    let w = this.triangulation_.calculateSourceExtent();
    if (y && (e.canWrapX() ? (w[1] = xe(
      w[1],
      y[1],
      y[3]
    ), w[3] = xe(
      w[3],
      y[1],
      y[3]
    )) : w = On(w, y)), !go(w))
      this.state = X.EMPTY;
    else {
      let x = 0, b = 0;
      e.canWrapX() && (x = ye(_), b = Math.floor(
        (w[0] - _[0]) / x
      )), pc(
        w.slice(),
        e,
        true
      ).forEach((R) => {
        const P = t.getTileRangeForExtentAndZ(
          R,
          this.sourceZ_
        );
        for (let U = P.minX; U <= P.maxX; U++)
          for (let N = P.minY; N <= P.maxY; N++) {
            const O = c(this.sourceZ_, U, N, a);
            if (O) {
              const G = b * x;
              this.sourceTiles_.push({ tile: O, offset: G });
            }
          }
        ++b;
      }), this.sourceTiles_.length === 0 && (this.state = X.EMPTY);
    }
  }
  /**
   * Get the HTML Canvas element for this tile.
   * @return {HTMLCanvasElement|OffscreenCanvas} Canvas.
   */
  getImage() {
    return this.canvas_;
  }
  /**
   * @private
   */
  reproject_() {
    const e = [];
    if (this.sourceTiles_.forEach((t) => {
      var s;
      const n = t.tile;
      if (n && n.getState() == X.LOADED) {
        const r = this.sourceTileGrid_.getTileCoordExtent(n.tileCoord);
        r[0] += t.offset, r[2] += t.offset;
        const o = (s = this.clipExtent_) == null ? void 0 : s.slice();
        o && (o[0] += t.offset, o[2] += t.offset), e.push({
          extent: r,
          clipExtent: o,
          image: n.getImage()
        });
      }
    }), this.sourceTiles_.length = 0, e.length === 0)
      this.state = X.ERROR;
    else {
      const t = this.wrappedTileCoord_[0], n = this.targetTileGrid_.getTileSize(t), s = typeof n == "number" ? n : n[0], r = typeof n == "number" ? n : n[1], o = this.targetTileGrid_.getResolution(t), a = this.sourceTileGrid_.getResolution(
        this.sourceZ_
      ), l = this.targetTileGrid_.getTileCoordExtent(
        this.wrappedTileCoord_
      );
      this.canvas_ = Rp(
        s,
        r,
        this.pixelRatio_,
        a,
        this.sourceTileGrid_.getExtent(),
        o,
        l,
        this.triangulation_,
        e,
        this.gutter_,
        this.renderEdges_,
        this.interpolate
      ), this.state = X.LOADED;
    }
    this.changed();
  }
  /**
   * Load not yet loaded URI.
   * @override
   */
  load() {
    if (this.state == X.IDLE) {
      this.state = X.LOADING, this.changed();
      let e = 0;
      this.sourcesListenerKeys_ = [], this.sourceTiles_.forEach(({ tile: t }) => {
        const n = t.getState();
        if (n == X.IDLE || n == X.LOADING) {
          e++;
          const s = le(t, Z.CHANGE, (r) => {
            const o = t.getState();
            (o == X.LOADED || o == X.ERROR || o == X.EMPTY) && (Ee(s), e--, e === 0 && (this.unlistenSources_(), this.reproject_()));
          });
          this.sourcesListenerKeys_.push(s);
        }
      }), e === 0 ? setTimeout(this.reproject_.bind(this), 0) : this.sourceTiles_.forEach(function({ tile: t }, n, s) {
        t.getState() == X.IDLE && t.load();
      });
    }
  }
  /**
   * @private
   */
  unlistenSources_() {
    this.sourcesListenerKeys_.forEach(Ee), this.sourcesListenerKeys_ = null;
  }
  /**
   * Remove from the cache due to expiry
   * @override
   */
  release() {
    this.canvas_ && (xr(
      /** @type {CanvasRenderingContext2D|OffscreenCanvasRenderingContext2D} */
      this.canvas_.getContext("2d")
    ), gi.push(this.canvas_), this.canvas_ = null), super.release();
  }
};
var qh = Ap;
var kp = class {
  /**
   * @param {number} [highWaterMark] High water mark.
   */
  constructor(e) {
    this.highWaterMark = e !== void 0 ? e : 2048, this.count_ = 0, this.entries_ = {}, this.oldest_ = null, this.newest_ = null;
  }
  deleteOldest() {
    const e = this.pop();
    e instanceof gr && e.dispose();
  }
  /**
   * @return {boolean} Can expire cache.
   */
  canExpireCache() {
    return this.highWaterMark > 0 && this.getCount() > this.highWaterMark;
  }
  /**
   * Expire the cache. When the cache entry is a {@link module:ol/Disposable~Disposable},
   * the entry will be disposed.
   * @param {!Object<string, boolean>} [keep] Keys to keep. To be implemented by subclasses.
   */
  expireCache(e) {
    for (; this.canExpireCache(); )
      this.deleteOldest();
  }
  /**
   * FIXME empty description for jsdoc
   */
  clear() {
    for (; this.oldest_; )
      this.deleteOldest();
  }
  /**
   * @param {string} key Key.
   * @return {boolean} Contains key.
   */
  containsKey(e) {
    return this.entries_.hasOwnProperty(e);
  }
  /**
   * @param {function(T, string, LRUCache<T>): ?} f The function
   *     to call for every entry from the oldest to the newer. This function takes
   *     3 arguments (the entry value, the entry key and the LRUCache object).
   *     The return value is ignored.
   */
  forEach(e) {
    let t = this.oldest_;
    for (; t; )
      e(t.value_, t.key_, this), t = t.newer;
  }
  /**
   * @param {string} key Key.
   * @param {*} [options] Options (reserved for subclasses).
   * @return {T} Value.
   */
  get(e, t) {
    const n = this.entries_[e];
    return _e(
      n !== void 0,
      "Tried to get a value for a key that does not exist in the cache"
    ), n === this.newest_ || (n === this.oldest_ ? (this.oldest_ = /** @type {Entry} */
    this.oldest_.newer, this.oldest_.older = null) : (n.newer.older = n.older, n.older.newer = n.newer), n.newer = null, n.older = this.newest_, this.newest_.newer = n, this.newest_ = n), n.value_;
  }
  /**
   * Remove an entry from the cache.
   * @param {string} key The entry key.
   * @return {T} The removed entry.
   */
  remove(e) {
    const t = this.entries_[e];
    return _e(
      t !== void 0,
      "Tried to get a value for a key that does not exist in the cache"
    ), t === this.newest_ ? (this.newest_ = /** @type {Entry} */
    t.older, this.newest_ && (this.newest_.newer = null)) : t === this.oldest_ ? (this.oldest_ = /** @type {Entry} */
    t.newer, this.oldest_ && (this.oldest_.older = null)) : (t.newer.older = t.older, t.older.newer = t.newer), delete this.entries_[e], --this.count_, t.value_;
  }
  /**
   * @return {number} Count.
   */
  getCount() {
    return this.count_;
  }
  /**
   * @return {Array<string>} Keys.
   */
  getKeys() {
    const e = new Array(this.count_);
    let t = 0, n;
    for (n = this.newest_; n; n = n.older)
      e[t++] = n.key_;
    return e;
  }
  /**
   * @return {Array<T>} Values.
   */
  getValues() {
    const e = new Array(this.count_);
    let t = 0, n;
    for (n = this.newest_; n; n = n.older)
      e[t++] = n.value_;
    return e;
  }
  /**
   * @return {T} Last value.
   */
  peekLast() {
    return this.oldest_.value_;
  }
  /**
   * @return {string} Last key.
   */
  peekLastKey() {
    return this.oldest_.key_;
  }
  /**
   * Get the key of the newest item in the cache.  Throws if the cache is empty.
   * @return {string} The newest key.
   */
  peekFirstKey() {
    return this.newest_.key_;
  }
  /**
   * Return an entry without updating least recently used time.
   * @param {string} key Key.
   * @return {T|undefined} Value.
   */
  peek(e) {
    var t;
    return (t = this.entries_[e]) == null ? void 0 : t.value_;
  }
  /**
   * @return {T} value Value.
   */
  pop() {
    const e = this.oldest_;
    return delete this.entries_[e.key_], e.newer && (e.newer.older = null), this.oldest_ = /** @type {Entry} */
    e.newer, this.oldest_ || (this.newest_ = null), --this.count_, e.value_;
  }
  /**
   * @param {string} key Key.
   * @param {T} value Value.
   */
  replace(e, t) {
    this.get(e), this.entries_[e].value_ = t;
  }
  /**
   * @param {string} key Key.
   * @param {T} value Value.
   */
  set(e, t) {
    _e(
      !(e in this.entries_),
      "Tried to set a value for a key that is used already"
    );
    const n = {
      key_: e,
      newer: null,
      older: this.newest_,
      value_: t
    };
    this.newest_ ? this.newest_.newer = n : this.oldest_ = n, this.newest_ = n, this.entries_[e] = n, ++this.count_;
  }
  /**
   * Set a maximum number of entries for the cache.
   * @param {number} size Cache size.
   * @api
   */
  setSize(e) {
    this.highWaterMark = e;
  }
};
var Zl = kp;
function ur(i, e, t, n) {
  return n !== void 0 ? (n[0] = i, n[1] = e, n[2] = t, n) : [i, e, t];
}
function Op(i, e, t) {
  return i + "/" + e + "/" + t;
}
function Bs(i, e, t, n, s) {
  return `${de(i)},${e},${Op(t, n, s)}`;
}
function Np(i) {
  return Up(i[0], i[1], i[2]);
}
function Up(i, e, t) {
  return (e << i) + t;
}
function Gp(i, e) {
  const t = i[0], n = i[1], s = i[2];
  if (e.getMinZoom() > t || t > e.getMaxZoom())
    return false;
  const r = e.getFullTileRange(t);
  return r ? r.containsXY(n, s) : true;
}
function ao(i, e, t) {
  if (!(t in i))
    return i[t] = /* @__PURE__ */ new Set([e]), true;
  const n = i[t], s = n.has(e);
  return s || n.add(e), !s;
}
function Bp(i, e, t) {
  const n = i[t];
  return n ? n.delete(e) : false;
}
function ql(i, e) {
  const t = i.layerStatesArray[i.layerIndex];
  t.extent && (e = On(
    e,
    cn(t.extent, i.viewState.projection)
  ));
  const n = (
    /** @type {import("../../source/Tile.js").default} */
    t.layer.getRenderSource()
  );
  if (!n.getWrapX()) {
    const s = n.getTileGridForProjection(i.viewState.projection).getExtent();
    s && (e = On(e, s));
  }
  return e;
}
var zp = class extends fh {
  /**
   * @param {LayerType} tileLayer Tile layer.
   * @param {Options} [options] Options.
   */
  constructor(e, t) {
    super(e), t = t || {}, this.extentChanged = true, this.renderComplete = false, this.renderedExtent_ = null, this.renderedPixelRatio, this.renderedProjection = null, this.renderedTiles = [], this.renderedSourceKey_, this.renderedSourceRevision_, this.tempExtent = _t(), this.tempTileRange_ = new Zh(0, 0, 0, 0), this.tempTileCoord_ = ur(0, 0, 0);
    const n = t.cacheSize !== void 0 ? t.cacheSize : 512;
    this.tileCache_ = new Zl(n), this.sourceTileCache_ = null, this.maxStaleKeys = n * 0.5;
  }
  /**
   * @return {LRUCache} Tile cache.
   */
  getTileCache() {
    return this.tileCache_;
  }
  /**
   * @return {LRUCache} Tile cache.
   */
  getSourceTileCache() {
    return this.sourceTileCache_ || (this.sourceTileCache_ = new Zl(512)), this.sourceTileCache_;
  }
  /**
   * Get a tile from the cache or create one if needed.
   *
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import("../../Tile.js").default|null} Tile (or null if outside source extent).
   * @protected
   */
  getOrCreateTile(e, t, n, s) {
    const r = this.tileCache_, a = this.getLayer().getSource(), l = Bs(a, a.getKey(), e, t, n);
    let c;
    if (r.containsKey(l))
      c = r.get(l);
    else {
      const h2 = s.viewState.projection, u = a.getProjection();
      if (c = a.getTile(
        e,
        t,
        n,
        s.pixelRatio,
        h2,
        !u || Gs(u, h2) ? void 0 : this.getSourceTileCache()
      ), !c)
        return null;
      r.set(l, c);
    }
    return c;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {import("../../Tile.js").default|null} Tile (or null if outside source extent).
   * @protected
   */
  getTile(e, t, n, s) {
    const r = this.getOrCreateTile(e, t, n, s);
    return r || null;
  }
  /**
   * @param {import("../../pixel.js").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray} Data at the pixel location.
   * @override
   */
  getData(e) {
    const t = this.frameState;
    if (!t)
      return null;
    const n = this.getLayer(), s = Pe(
      t.pixelToCoordinateTransform,
      e.slice()
    ), r = n.getExtent();
    if (r && !_i(r, s))
      return null;
    const o = t.viewState, a = n.getRenderSource(), l = a.getTileGridForProjection(o.projection), c = a.getTilePixelRatio(t.pixelRatio);
    for (let h2 = l.getZForResolution(o.resolution); h2 >= l.getMinZoom(); --h2) {
      const u = l.getTileCoordForCoordAndZ(s, h2), d = this.getTile(h2, u[1], u[2], t);
      if (!d || d.getState() !== X.LOADED)
        continue;
      const f = l.getOrigin(h2), g = et(l.getTileSize(h2)), y = l.getResolution(h2);
      let m;
      if (d instanceof Yh || d instanceof qh)
        m = d.getImage();
      else if (d instanceof jl) {
        if (m = ko(d.getData()), !m)
          continue;
      } else
        continue;
      const _ = Math.floor(
        c * ((s[0] - f[0]) / y - u[1] * g[0])
      ), p = Math.floor(
        c * ((f[1] - s[1]) / y - u[2] * g[1])
      ), E = Math.round(
        c * a.getGutterForProjection(o.projection)
      );
      return this.getImageData(m, _ + E, p + E);
    }
    return null;
  }
  /**
   * Determine whether render should be called.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @return {boolean} Layer is ready to be rendered.
   * @override
   */
  prepareFrame(e) {
    var s;
    this.renderedProjection ? e.viewState.projection !== this.renderedProjection && (this.tileCache_.clear(), this.renderedProjection = e.viewState.projection) : this.renderedProjection = e.viewState.projection;
    const t = this.getLayer().getSource();
    if (!t)
      return false;
    const n = t.getRevision();
    return this.renderedSourceRevision_ ? this.renderedSourceRevision_ !== n && (this.renderedSourceRevision_ = n, this.renderedSourceKey_ === t.getKey() && (this.tileCache_.clear(), (s = this.sourceTileCache_) == null || s.clear())) : this.renderedSourceRevision_ = n, true;
  }
  /**
   * Determine whether tiles for next extent should be enqueued for rendering.
   * @return {boolean} Rendering tiles for next extent is supported.
   * @protected
   */
  enqueueTilesForNextExtent() {
    return true;
  }
  /**
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {import("../../extent.js").Extent} extent The extent to be rendered.
   * @param {number} initialZ The zoom level.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @param {number} preload Number of additional levels to load.
   */
  enqueueTiles(e, t, n, s, r) {
    const o = e.viewState, a = this.getLayer(), l = a.getRenderSource(), c = l.getTileGridForProjection(o.projection), h2 = de(l);
    h2 in e.wantedTiles || (e.wantedTiles[h2] = {});
    const u = e.wantedTiles[h2], d = a.getMapInternal(), f = Math.max(
      n - r,
      c.getMinZoom(),
      c.getZForResolution(
        Math.min(
          a.getMaxResolution(),
          d ? d.getView().getResolutionForZoom(Math.max(a.getMinZoom(), 0)) : c.getResolution(0)
        ),
        l.zDirection
      )
    ), g = o.rotation, y = g ? yc(
      o.center,
      o.resolution,
      g,
      e.size
    ) : void 0;
    for (let m = n; m >= f; --m) {
      const _ = c.getTileRangeForExtentAndZ(
        t,
        m,
        this.tempTileRange_
      ), p = c.getResolution(m);
      for (let E = _.minX; E <= _.maxX; ++E)
        for (let S = _.minY; S <= _.maxY; ++S) {
          if (g && !c.tileCoordIntersectsViewport([m, E, S], y))
            continue;
          const w = this.getTile(m, E, S, e);
          if (!w || !ao(s, w, m))
            continue;
          const b = w.getKey();
          if (u[b] = true, w.getState() === X.IDLE && !e.tileQueue.isKeyQueued(b)) {
            const v = ur(m, E, S, this.tempTileCoord_);
            e.tileQueue.enqueue([
              w,
              h2,
              c.getTileCoordCenter(v),
              p
            ]);
          }
        }
    }
  }
  /**
   * Look for tiles covering the provided tile coordinate at an alternate
   * zoom level.  Loaded tiles will be added to the provided tile texture lookup.
   * @param {import("../../tilecoord.js").TileCoord} tileCoord The target tile coordinate.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @return {boolean} The tile coordinate is covered by loaded tiles at the alternate zoom level.
   * @private
   */
  findStaleTile_(e, t) {
    const n = this.tileCache_, s = e[0], r = e[1], o = e[2], a = this.getStaleKeys();
    for (let l = 0; l < a.length; ++l) {
      const c = Bs(
        this.getLayer().getSource(),
        a[l],
        s,
        r,
        o
      );
      if (n.containsKey(c)) {
        const h2 = n.peek(c);
        if (h2.getState() === X.LOADED)
          return h2.endTransition(de(this)), ao(t, h2, s), true;
      }
    }
    return false;
  }
  /**
   * Look for tiles covering the provided tile coordinate at an alternate
   * zoom level.  Loaded tiles will be added to the provided tile texture lookup.
   * @param {import("../../tilegrid/TileGrid.js").default} tileGrid The tile grid.
   * @param {import("../../tilecoord.js").TileCoord} tileCoord The target tile coordinate.
   * @param {number} altZ The alternate zoom level.
   * @param {TileLookup} tilesByZ Lookup of tiles by zoom level.
   * @return {boolean} The tile coordinate is covered by loaded tiles at the alternate zoom level.
   * @private
   */
  findAltTiles_(e, t, n, s) {
    const r = e.getTileRangeForTileCoordAndZ(
      t,
      n,
      this.tempTileRange_
    );
    if (!r)
      return false;
    let o = true;
    const a = this.tileCache_, l = this.getLayer().getRenderSource(), c = l.getKey();
    for (let h2 = r.minX; h2 <= r.maxX; ++h2)
      for (let u = r.minY; u <= r.maxY; ++u) {
        const d = Bs(l, c, n, h2, u);
        let f = false;
        if (a.containsKey(d)) {
          const g = a.peek(d);
          g.getState() === X.LOADED && (ao(s, g, n), f = true);
        }
        f || (o = false);
      }
    return o;
  }
  /**
   * Render the layer.
   *
   * The frame rendering logic has three parts:
   *
   *  1. Enqueue tiles
   *  2. Find alt tiles for those that are not yet loaded
   *  3. Render loaded tiles
   *
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {HTMLElement} target Target that may be used to render content to.
   * @return {HTMLElement} The rendered element.
   * @override
   */
  renderFrame(e, t) {
    this.renderComplete = true;
    const n = e.layerStatesArray[e.layerIndex], s = e.viewState, r = s.projection, o = s.resolution, a = s.center, l = e.pixelRatio, c = this.getLayer(), h2 = c.getSource(), u = h2.getTileGridForProjection(r), d = u.getZForResolution(o, h2.zDirection), f = u.getResolution(d), g = h2.getKey();
    this.renderedSourceKey_ ? this.renderedSourceKey_ !== g && (this.prependStaleKey(this.renderedSourceKey_), this.renderedSourceKey_ = g) : this.renderedSourceKey_ = g;
    let y = e.extent;
    const m = h2.getTilePixelRatio(l);
    this.prepareContainer(e, t);
    const _ = this.context.canvas.width, p = this.context.canvas.height, E = n.extent && cn(n.extent);
    E && (y = On(
      y,
      cn(n.extent)
    ));
    const S = f * _ / 2 / m, w = f * p / 2 / m, x = [
      a[0] - S,
      a[1] - w,
      a[0] + S,
      a[1] + w
    ], b = {};
    this.renderedTiles.length = 0;
    const v = c.getPreload();
    if (e.nextExtent && this.enqueueTilesForNextExtent()) {
      const V = u.getZForResolution(
        s.nextResolution,
        h2.zDirection
      ), q = ql(e, e.nextExtent);
      this.enqueueTiles(e, q, V, b, v);
    }
    const R = ql(e, y);
    if (this.enqueueTiles(e, R, d, b, 0), v > 0 && setTimeout(() => {
      this.enqueueTiles(
        e,
        R,
        d - 1,
        b,
        v - 1
      );
    }, 0), !(d in b))
      return this.container;
    const P = de(this), U = e.time;
    for (const V of b[d]) {
      const q = V.getState();
      if (q === X.EMPTY)
        continue;
      const ne = V.tileCoord;
      if (q === X.LOADED && V.getAlpha(P, U) === 1) {
        V.endTransition(P);
        continue;
      }
      if (q !== X.ERROR && (this.renderComplete = false), this.findStaleTile_(ne, b)) {
        Bp(b, V, d), e.animate = true;
        continue;
      }
      if (this.findAltTiles_(
        u,
        ne,
        d + 1,
        b
      ))
        continue;
      const Te = u.getMinZoom();
      for (let Me = d - 1; Me >= Te && !this.findAltTiles_(
        u,
        ne,
        Me,
        b
      ); --Me)
        ;
    }
    const N = f / o * l / m, O = this.getRenderContext(e);
    Zt(
      this.tempTransform,
      _ / 2,
      p / 2,
      N,
      N,
      0,
      -_ / 2,
      -p / 2
    ), n.extent && this.clipUnrotated(O, e, E), h2.getInterpolate() || (O.imageSmoothingEnabled = false), this.preRender(O, e);
    const G = Object.keys(b).map(Number);
    G.sort(Yt);
    let $;
    const z = [], H = [];
    for (let V = G.length - 1; V >= 0; --V) {
      const q = G[V], ne = h2.getTilePixelSize(
        q,
        l,
        r
      ), F = u.getResolution(q) / f, Te = ne[0] * F * N, Me = ne[1] * F * N, Ce = u.getTileCoordForCoordAndZ(
        En(x),
        q
      ), Oe = u.getTileCoordExtent(Ce), be = Pe(this.tempTransform, [
        m * (Oe[0] - x[0]) / f,
        m * (x[3] - Oe[3]) / f
      ]), it = m * h2.getGutterForProjection(r);
      for (const He of b[q]) {
        if (He.getState() !== X.LOADED)
          continue;
        const Ne = He.tileCoord, Cn = Ce[1] - Ne[1], Wn = Math.round(be[0] - (Cn - 1) * Te), Jt = Ce[2] - Ne[2], Tt = Math.round(be[1] - (Jt - 1) * Me), We = Math.round(be[0] - Cn * Te), st = Math.round(be[1] - Jt * Me), C = Wn - We, T = Tt - st, D = G.length === 1;
        let A = false;
        $ = [We, st, We + C, st, We + C, st + T, We, st + T];
        for (let k = 0, W = z.length; k < W; ++k)
          if (!D && q < H[k]) {
            const I = z[k];
            $e(
              [We, st, We + C, st + T],
              [I[0], I[3], I[4], I[7]]
            ) && (A || (O.save(), A = true), O.beginPath(), O.moveTo($[0], $[1]), O.lineTo($[2], $[3]), O.lineTo($[4], $[5]), O.lineTo($[6], $[7]), O.moveTo(I[6], I[7]), O.lineTo(I[4], I[5]), O.lineTo(I[2], I[3]), O.lineTo(I[0], I[1]), O.clip());
          }
        z.push($), H.push(q), this.drawTile(He, e, We, st, C, T, it, D), A && O.restore(), this.renderedTiles.unshift(He), this.updateUsedTiles(e.usedTiles, h2, He);
      }
    }
    if (this.renderedResolution = f, this.extentChanged = !this.renderedExtent_ || !Hi(this.renderedExtent_, x), this.renderedExtent_ = x, this.renderedPixelRatio = l, this.postRender(this.context, e), n.extent && O.restore(), O.imageSmoothingEnabled = true, this.renderComplete) {
      const V = (q, ne) => {
        var Me;
        const fe = de(h2), F = ne.wantedTiles[fe], Te = F ? Object.keys(F).length : 0;
        this.updateCacheSize(Te), this.tileCache_.expireCache(), (Me = this.sourceTileCache_) == null || Me.expireCache();
      };
      e.postRenderFunctions.push(V);
    }
    return this.container;
  }
  /**
   * Increases the cache size if needed
   * @param {number} tileCount Minimum number of tiles needed.
   */
  updateCacheSize(e) {
    this.tileCache_.highWaterMark = Math.max(
      this.tileCache_.highWaterMark,
      e * 2
    );
  }
  /**
   * @param {import("../../Tile.js").default} tile Tile.
   * @param {import("../../Map.js").FrameState} frameState Frame state.
   * @param {number} x Left of the tile.
   * @param {number} y Top of the tile.
   * @param {number} w Width of the tile.
   * @param {number} h Height of the tile.
   * @param {number} gutter Tile gutter.
   * @param {boolean} transition Apply an alpha transition.
   * @protected
   */
  drawTile(e, t, n, s, r, o, a, l) {
    let c;
    if (e instanceof jl) {
      if (c = ko(e.getData()), !c)
        throw new Error("Rendering array data is not yet supported");
    } else
      c = this.getTileImage(
        /** @type {import("../../ImageTile.js").default} */
        e
      );
    if (!c)
      return;
    const h2 = this.getRenderContext(t), u = de(this), d = t.layerStatesArray[t.layerIndex], f = d.opacity * (l ? e.getAlpha(u, t.time) : 1), g = f !== h2.globalAlpha;
    g && (h2.save(), h2.globalAlpha = f), h2.drawImage(
      c,
      a,
      a,
      c.width - 2 * a,
      c.height - 2 * a,
      n,
      s,
      r,
      o
    ), g && h2.restore(), f !== d.opacity ? t.animate = true : l && e.endTransition(u);
  }
  /**
   * @return {HTMLCanvasElement|OffscreenCanvas} Image
   */
  getImage() {
    const e = this.context;
    return e ? e.canvas : null;
  }
  /**
   * Get the image from a tile.
   * @param {import("../../ImageTile.js").default} tile Tile.
   * @return {HTMLCanvasElement|OffscreenCanvas|HTMLImageElement|HTMLVideoElement} Image.
   * @protected
   */
  getTileImage(e) {
    return e.getImage();
  }
  /**
   * @param {!Object<string, !Object<string, boolean>>} usedTiles Used tiles.
   * @param {import("../../source/Tile.js").default} tileSource Tile source.
   * @param {import('../../Tile.js').default} tile Tile.
   * @protected
   */
  updateUsedTiles(e, t, n) {
    const s = de(t);
    s in e || (e[s] = {}), e[s][n.getKey()] = true;
  }
};
var Vp = zp;
var Ns = {
  PRELOAD: "preload",
  USE_INTERIM_TILES_ON_ERROR: "useInterimTilesOnError"
};
var Xp = class extends br {
  /**
   * @param {Options<TileSourceType>} [options] Tile layer options.
   */
  constructor(e) {
    e = e || {};
    const t = Object.assign({}, e), n = e.cacheSize;
    delete e.cacheSize, delete t.preload, delete t.useInterimTilesOnError, super(t), this.on, this.once, this.un, this.cacheSize_ = n, this.setPreload(e.preload !== void 0 ? e.preload : 0), this.setUseInterimTilesOnError(
      e.useInterimTilesOnError !== void 0 ? e.useInterimTilesOnError : true
    );
  }
  /**
   * @return {number|undefined} The suggested cache size
   * @protected
   */
  getCacheSize() {
    return this.cacheSize_;
  }
  /**
   * Return the level as number to which we will preload tiles up to.
   * @return {number} The level to preload tiles up to.
   * @observable
   * @api
   */
  getPreload() {
    return (
      /** @type {number} */
      this.get(Ns.PRELOAD)
    );
  }
  /**
   * Set the level as number to which we will preload tiles up to.
   * @param {number} preload The level to preload tiles up to.
   * @observable
   * @api
   */
  setPreload(e) {
    this.set(Ns.PRELOAD, e);
  }
  /**
   * Deprecated.  Whether we use interim tiles on error.
   * @return {boolean} Use interim tiles on error.
   * @observable
   * @api
   */
  getUseInterimTilesOnError() {
    return (
      /** @type {boolean} */
      this.get(Ns.USE_INTERIM_TILES_ON_ERROR)
    );
  }
  /**
   * Deprecated.  Set whether we use interim tiles on error.
   * @param {boolean} useInterimTilesOnError Use interim tiles on error.
   * @observable
   * @api
   */
  setUseInterimTilesOnError(e) {
    this.set(Ns.USE_INTERIM_TILES_ON_ERROR, e);
  }
  /**
   * Get data for a pixel location.  The return type depends on the source data.  For image tiles,
   * a four element RGBA array will be returned.  For data tiles, the array length will match the
   * number of bands in the dataset.  For requests outside the layer extent, `null` will be returned.
   * Data for a image tiles can only be retrieved if the source's `crossOrigin` property is set.
   *
   * ```js
   * // display layer data on every pointer move
   * map.on('pointermove', (event) => {
   *   console.log(layer.getData(event.pixel));
   * });
   * ```
   * @param {import("../pixel").Pixel} pixel Pixel.
   * @return {Uint8ClampedArray|Uint8Array|Float32Array|DataView|null} Pixel data.
   * @api
   * @override
   */
  getData(e) {
    return super.getData(e);
  }
};
var Wp = Xp;
var Yp = class extends Wp {
  /**
   * @param {import("./BaseTile.js").Options<TileSourceType>} [options] Tile layer options.
   */
  constructor(e) {
    super(e);
  }
  /**
   * @override
   */
  createRenderer() {
    return new Vp(this, {
      cacheSize: this.getCacheSize()
    });
  }
};
var Jl = Yp;
var Qn = [0, 0, 0];
var an = 5;
var jp = class {
  /**
   * @param {Options} options Tile grid options.
   */
  constructor(e) {
    this.minZoom = e.minZoom !== void 0 ? e.minZoom : 0, this.resolutions_ = e.resolutions, _e(
      Bu(
        this.resolutions_,
        /**
         * @param {number} a First resolution
         * @param {number} b Second resolution
         * @return {number} Comparison result
         */
        (s, r) => r - s,
        true
      ),
      "`resolutions` must be sorted in descending order"
    );
    let t;
    if (!e.origins) {
      for (let s = 0, r = this.resolutions_.length - 1; s < r; ++s)
        if (!t)
          t = this.resolutions_[s] / this.resolutions_[s + 1];
        else if (this.resolutions_[s] / this.resolutions_[s + 1] !== t) {
          t = void 0;
          break;
        }
    }
    this.zoomFactor_ = t, this.maxZoom = this.resolutions_.length - 1, this.origin_ = e.origin !== void 0 ? e.origin : null, this.origins_ = null, e.origins !== void 0 && (this.origins_ = e.origins, _e(
      this.origins_.length == this.resolutions_.length,
      "Number of `origins` and `resolutions` must be equal"
    ));
    const n = e.extent;
    n !== void 0 && !this.origin_ && !this.origins_ && (this.origin_ = En(n)), _e(
      !this.origin_ && this.origins_ || this.origin_ && !this.origins_,
      "Either `origin` or `origins` must be configured, never both"
    ), this.tileSizes_ = null, e.tileSizes !== void 0 && (this.tileSizes_ = e.tileSizes, _e(
      this.tileSizes_.length == this.resolutions_.length,
      "Number of `tileSizes` and `resolutions` must be equal"
    )), this.tileSize_ = e.tileSize !== void 0 ? e.tileSize : this.tileSizes_ ? null : Da, _e(
      !this.tileSize_ && this.tileSizes_ || this.tileSize_ && !this.tileSizes_,
      "Either `tileSize` or `tileSizes` must be configured, never both"
    ), this.extent_ = n !== void 0 ? n : null, this.fullTileRanges_ = null, this.tmpSize_ = [0, 0], this.tmpExtent_ = [0, 0, 0, 0], e.sizes !== void 0 ? this.fullTileRanges_ = e.sizes.map((s, r) => {
      const o = new Zh(
        Math.min(0, s[0]),
        Math.max(s[0] - 1, -1),
        Math.min(0, s[1]),
        Math.max(s[1] - 1, -1)
      );
      if (n) {
        const a = this.getTileRangeForExtentAndZ(n, r);
        o.minX = Math.max(a.minX, o.minX), o.maxX = Math.min(a.maxX, o.maxX), o.minY = Math.max(a.minY, o.minY), o.maxY = Math.min(a.maxY, o.maxY);
      }
      return o;
    }) : n && this.calculateTileRanges_(n);
  }
  /**
   * Call a function with each tile coordinate for a given extent and zoom level.
   *
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} zoom Integer zoom level.
   * @param {function(import("../tilecoord.js").TileCoord): void} callback Function called with each tile coordinate.
   * @api
   */
  forEachTileCoord(e, t, n) {
    const s = this.getTileRangeForExtentAndZ(e, t);
    for (let r = s.minX, o = s.maxX; r <= o; ++r)
      for (let a = s.minY, l = s.maxY; a <= l; ++a)
        n([t, r, a]);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {function(number, import("../TileRange.js").default): boolean} callback Callback.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary import("../extent.js").Extent object.
   * @return {boolean} Callback succeeded.
   */
  forEachTileCoordParentTileRange(e, t, n, s) {
    let r, o, a, l = null, c = e[0] - 1;
    for (this.zoomFactor_ === 2 ? (o = e[1], a = e[2]) : l = this.getTileCoordExtent(e, s); c >= this.minZoom; ) {
      if (o !== void 0 && a !== void 0 ? (o = Math.floor(o / 2), a = Math.floor(a / 2), r = Jn(o, o, a, a, n)) : r = this.getTileRangeForExtentAndZ(
        l,
        c,
        n
      ), t(c, r))
        return true;
      --c;
    }
    return false;
  }
  /**
   * Get the extent for this tile grid, if it was configured.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getExtent() {
    return this.extent_;
  }
  /**
   * Get the maximum zoom level for the grid.
   * @return {number} Max zoom.
   * @api
   */
  getMaxZoom() {
    return this.maxZoom;
  }
  /**
   * Get the minimum zoom level for the grid.
   * @return {number} Min zoom.
   * @api
   */
  getMinZoom() {
    return this.minZoom;
  }
  /**
   * Get the origin for the grid at the given zoom level.
   * @param {number} z Integer zoom level.
   * @return {import("../coordinate.js").Coordinate} Origin.
   * @api
   */
  getOrigin(e) {
    return this.origin_ ? this.origin_ : this.origins_[e];
  }
  /**
   * Get the resolution for the given zoom level.
   * @param {number} z Integer zoom level.
   * @return {number} Resolution.
   * @api
   */
  getResolution(e) {
    return this.resolutions_[e];
  }
  /**
   * Get the list of resolutions for the tile grid.
   * @return {Array<number>} Resolutions.
   * @api
   */
  getResolutions() {
    return this.resolutions_;
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary import("../extent.js").Extent object.
   * @return {import("../TileRange.js").default|null} Tile range.
   */
  getTileCoordChildTileRange(e, t, n) {
    if (e[0] < this.maxZoom) {
      if (this.zoomFactor_ === 2) {
        const r = e[1] * 2, o = e[2] * 2;
        return Jn(
          r,
          r + 1,
          o,
          o + 1,
          t
        );
      }
      const s = this.getTileCoordExtent(
        e,
        n || this.tmpExtent_
      );
      return this.getTileRangeForExtentAndZ(
        s,
        e[0] + 1,
        t
      );
    }
    return null;
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {number} z Integer zoom level.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary import("../TileRange.js").default object.
   * @return {import("../TileRange.js").default|null} Tile range.
   */
  getTileRangeForTileCoordAndZ(e, t, n) {
    if (t > this.maxZoom || t < this.minZoom)
      return null;
    const s = e[0], r = e[1], o = e[2];
    if (t === s)
      return Jn(
        r,
        o,
        r,
        o,
        n
      );
    if (this.zoomFactor_) {
      const l = Math.pow(this.zoomFactor_, t - s), c = Math.floor(r * l), h2 = Math.floor(o * l);
      if (t < s)
        return Jn(c, c, h2, h2, n);
      const u = Math.floor(l * (r + 1)) - 1, d = Math.floor(l * (o + 1)) - 1;
      return Jn(c, u, h2, d, n);
    }
    const a = this.getTileCoordExtent(e, this.tmpExtent_);
    return this.getTileRangeForExtentAndZ(a, t, n);
  }
  /**
   * Get a tile range for the given extent and integer zoom level.
   * @param {import("../extent.js").Extent} extent Extent.
   * @param {number} z Integer zoom level.
   * @param {import("../TileRange.js").default} [tempTileRange] Temporary tile range object.
   * @return {import("../TileRange.js").default} Tile range.
   */
  getTileRangeForExtentAndZ(e, t, n) {
    this.getTileCoordForXYAndZ_(e[0], e[3], t, false, Qn);
    const s = Qn[1], r = Qn[2];
    this.getTileCoordForXYAndZ_(e[2], e[1], t, true, Qn);
    const o = Qn[1], a = Qn[2];
    return Jn(s, o, r, a, n);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {import("../coordinate.js").Coordinate} Tile center.
   */
  getTileCoordCenter(e) {
    const t = this.getOrigin(e[0]), n = this.getResolution(e[0]), s = et(this.getTileSize(e[0]), this.tmpSize_);
    return [
      t[0] + (e[1] + 0.5) * s[0] * n,
      t[1] - (e[2] + 0.5) * s[1] * n
    ];
  }
  /**
   * Get the extent of a tile coordinate.
   *
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../extent.js").Extent} [tempExtent] Temporary extent object.
   * @return {import("../extent.js").Extent} Extent.
   * @api
   */
  getTileCoordExtent(e, t) {
    const n = this.getOrigin(e[0]), s = this.getResolution(e[0]), r = et(this.getTileSize(e[0]), this.tmpSize_), o = n[0] + e[1] * r[0] * s, a = n[1] - (e[2] + 1) * r[1] * s, l = o + r[0] * s, c = a + r[1] * s;
    return kt(o, a, l, c, t);
  }
  /**
   * Get the tile coordinate for the given map coordinate and resolution.  This
   * method considers that coordinates that intersect tile boundaries should be
   * assigned the higher tile coordinate.
   *
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} resolution Resolution.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Destination import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @api
   */
  getTileCoordForCoordAndResolution(e, t, n) {
    return this.getTileCoordForXYAndResolution_(
      e[0],
      e[1],
      t,
      false,
      n
    );
  }
  /**
   * Note that this method should not be called for resolutions that correspond
   * to an integer zoom level.  Instead call the `getTileCoordForXYAndZ_` method.
   * @param {number} x X.
   * @param {number} y Y.
   * @param {number} resolution Resolution (for a non-integer zoom level).
   * @param {boolean} reverseIntersectionPolicy Instead of letting edge
   *     intersections go to the higher tile coordinate, let edge intersections
   *     go to the lower tile coordinate.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Temporary import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @private
   */
  getTileCoordForXYAndResolution_(e, t, n, s, r) {
    const o = this.getZForResolution(n), a = n / this.getResolution(o), l = this.getOrigin(o), c = et(this.getTileSize(o), this.tmpSize_);
    let h2 = a * (e - l[0]) / n / c[0], u = a * (l[1] - t) / n / c[1];
    return s ? (h2 = Ss(h2, an) - 1, u = Ss(u, an) - 1) : (h2 = ps(h2, an), u = ps(u, an)), ur(o, h2, u, r);
  }
  /**
   * Although there is repetition between this method and `getTileCoordForXYAndResolution_`,
   * they should have separate implementations.  This method is for integer zoom
   * levels.  The other method should only be called for resolutions corresponding
   * to non-integer zoom levels.
   * @param {number} x Map x coordinate.
   * @param {number} y Map y coordinate.
   * @param {number} z Integer zoom level.
   * @param {boolean} reverseIntersectionPolicy Instead of letting edge
   *     intersections go to the higher tile coordinate, let edge intersections
   *     go to the lower tile coordinate.
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Temporary import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @private
   */
  getTileCoordForXYAndZ_(e, t, n, s, r) {
    const o = this.getOrigin(n), a = this.getResolution(n), l = et(this.getTileSize(n), this.tmpSize_);
    let c = (e - o[0]) / a / l[0], h2 = (o[1] - t) / a / l[1];
    return s ? (c = Ss(c, an) - 1, h2 = Ss(h2, an) - 1) : (c = ps(c, an), h2 = ps(h2, an)), ur(n, c, h2, r);
  }
  /**
   * Get a tile coordinate given a map coordinate and zoom level.
   * @param {import("../coordinate.js").Coordinate} coordinate Coordinate.
   * @param {number} z Integer zoom level, e.g. the result of a `getZForResolution()` method call
   * @param {import("../tilecoord.js").TileCoord} [opt_tileCoord] Destination import("../tilecoord.js").TileCoord object.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate.
   * @api
   */
  getTileCoordForCoordAndZ(e, t, n) {
    return this.getTileCoordForXYAndZ_(
      e[0],
      e[1],
      t,
      false,
      n
    );
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @return {number} Tile resolution.
   */
  getTileCoordResolution(e) {
    return this.resolutions_[e[0]];
  }
  /**
   * Get the tile size for a zoom level. The type of the return value matches the
   * `tileSize` or `tileSizes` that the tile grid was configured with. To always
   * get an {@link import("../size.js").Size}, run the result through {@link module:ol/size.toSize}.
   * @param {number} z Z.
   * @return {number|import("../size.js").Size} Tile size.
   * @api
   */
  getTileSize(e) {
    return this.tileSize_ ? this.tileSize_ : this.tileSizes_[e];
  }
  /**
   * @param {number} z Zoom level.
   * @return {import("../TileRange.js").default|null} Extent tile range for the specified zoom level.
   */
  getFullTileRange(e) {
    return this.fullTileRanges_ ? this.fullTileRanges_[e] : this.extent_ ? this.getTileRangeForExtentAndZ(this.extent_, e) : null;
  }
  /**
   * @param {number} resolution Resolution.
   * @param {number|import("../array.js").NearestDirectionFunction} [opt_direction]
   *     If 0, the nearest resolution will be used.
   *     If 1, the nearest higher resolution (lower Z) will be used. If -1, the
   *     nearest lower resolution (higher Z) will be used. Default is 0.
   *     Use a {@link module:ol/array~NearestDirectionFunction} for more precise control.
   *
   * For example to change tile Z at the midpoint of zoom levels
   * ```js
   * function(value, high, low) {
   *   return value - low * Math.sqrt(high / low);
   * }
   * ```
   * @return {number} Z.
   * @api
   */
  getZForResolution(e, t) {
    const n = Go(
      this.resolutions_,
      e,
      t || 0
    );
    return xe(n, this.minZoom, this.maxZoom);
  }
  /**
   * The tile with the provided tile coordinate intersects the given viewport.
   * @param {import('../tilecoord.js').TileCoord} tileCoord Tile coordinate.
   * @param {Array<number>} viewport Viewport as returned from {@link module:ol/extent.getRotatedViewport}.
   * @return {boolean} The tile with the provided tile coordinate intersects the given viewport.
   */
  tileCoordIntersectsViewport(e, t) {
    return Ac(
      t,
      0,
      t.length,
      2,
      this.getTileCoordExtent(e)
    );
  }
  /**
   * @param {!import("../extent.js").Extent} extent Extent for this tile grid.
   * @private
   */
  calculateTileRanges_(e) {
    const t = this.resolutions_.length, n = new Array(t);
    for (let s = this.minZoom; s < t; ++s)
      n[s] = this.getTileRangeForExtentAndZ(e, s);
    this.fullTileRanges_ = n;
  }
};
var Jh = jp;
function Qh(i) {
  let e = i.getDefaultTileGrid();
  return e || (e = Hp(i), i.setDefaultTileGrid(e)), e;
}
function $p(i, e, t) {
  const n = e[0], s = i.getTileCoordCenter(e), r = za(t);
  if (!_i(r, s)) {
    const o = ye(r), a = Math.ceil(
      (r[0] - s[0]) / o
    );
    return s[0] += o * a, i.getTileCoordForCoordAndZ(s, n);
  }
  return e;
}
function Kp(i, e, t, n) {
  n = n !== void 0 ? n : "top-left";
  const s = eu(i, e, t);
  return new Jh({
    extent: i,
    origin: Hu(i, n),
    resolutions: s,
    tileSize: t
  });
}
function Oo(i) {
  const e = i || {}, t = e.extent || nt("EPSG:3857").getExtent(), n = {
    extent: t,
    minZoom: e.minZoom,
    tileSize: e.tileSize,
    resolutions: eu(
      t,
      e.maxZoom,
      e.tileSize,
      e.maxResolution
    )
  };
  return new Jh(n);
}
function eu(i, e, t, n) {
  e = e !== void 0 ? e : zm, t = et(t !== void 0 ? t : Da);
  const s = Ve(i), r = ye(i);
  n = n > 0 ? n : Math.max(r / t[0], s / t[1]);
  const o = e + 1, a = new Array(o);
  for (let l = 0; l < o; ++l)
    a[l] = n / Math.pow(2, l);
  return a;
}
function Hp(i, e, t, n) {
  const s = za(i);
  return Kp(s, e, t, n);
}
function za(i) {
  i = nt(i);
  let e = i.getExtent();
  if (!e) {
    const t = 180 * Zi.degrees / i.getMetersPerUnit();
    e = kt(-t, -t, t, t);
  }
  return e;
}
var Zp = /\{z\}/g;
var qp = /\{x\}/g;
var Jp = /\{y\}/g;
var Qp = /\{-y\}/g;
function e0(i, e, t, n, s) {
  return i.replace(Zp, e.toString()).replace(qp, t.toString()).replace(Jp, n.toString()).replace(Qp, function() {
    if (s === void 0)
      throw new Error(
        "If the URL template has a {-y} placeholder, the grid extent must be known"
      );
    return (s - n).toString();
  });
}
function t0(i) {
  const e = [];
  let t = /\{([a-z])-([a-z])\}/.exec(i);
  if (t) {
    const n = t[1].charCodeAt(0), s = t[2].charCodeAt(0);
    let r;
    for (r = n; r <= s; ++r)
      e.push(i.replace(t[0], String.fromCharCode(r)));
    return e;
  }
  if (t = /\{(\d+)-(\d+)\}/.exec(i), t) {
    const n = parseInt(t[2], 10);
    for (let s = parseInt(t[1], 10); s <= n; s++)
      e.push(i.replace(t[0], s.toString()));
    return e;
  }
  return e.push(i), e;
}
function n0(i, e) {
  return (
    /**
     * @param {import("./tilecoord.js").TileCoord} tileCoord Tile Coordinate.
     * @param {number} pixelRatio Pixel ratio.
     * @param {import("./proj/Projection.js").default} projection Projection.
     * @return {string|undefined} Tile URL.
     */
    function(t, n, s) {
      if (!t)
        return;
      let r;
      const o = t[0];
      if (e) {
        const a = e.getFullTileRange(o);
        a && (r = a.getHeight() - 1);
      }
      return e0(i, o, t[1], t[2], r);
    }
  );
}
function i0(i, e) {
  const t = i.length, n = new Array(t);
  for (let s = 0; s < t; ++s)
    n[s] = n0(i[s], e);
  return s0(n);
}
function s0(i) {
  return i.length === 1 ? i[0] : (
    /**
     * @param {import("./tilecoord.js").TileCoord} tileCoord Tile Coordinate.
     * @param {number} pixelRatio Pixel ratio.
     * @param {import("./proj/Projection.js").default} projection Projection.
     * @return {string|undefined} Tile URL.
     */
    function(e, t, n) {
      if (!e)
        return;
      const s = Np(e), r = gn(s, i.length);
      return i[r](e, t, n);
    }
  );
}
var r0 = class extends Mh {
  /**
   * @param {Options} options SourceTile source options.
   */
  constructor(e) {
    super({
      attributions: e.attributions,
      attributionsCollapsible: e.attributionsCollapsible,
      projection: e.projection,
      state: e.state,
      wrapX: e.wrapX,
      interpolate: e.interpolate
    }), this.on, this.once, this.un, this.tilePixelRatio_ = e.tilePixelRatio !== void 0 ? e.tilePixelRatio : 1, this.tileGrid = e.tileGrid !== void 0 ? e.tileGrid : null;
    const t = [256, 256];
    this.tileGrid && et(this.tileGrid.getTileSize(this.tileGrid.getMinZoom()), t), this.tmpSize = [0, 0], this.key_ = e.key || de(this), this.tileOptions = {
      transition: e.transition,
      interpolate: e.interpolate
    }, this.zDirection = e.zDirection ? e.zDirection : 0;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {number} Gutter.
   */
  getGutterForProjection(e) {
    return 0;
  }
  /**
   * Return the key to be used for all tiles in the source.
   * @return {string} The key for all tiles.
   */
  getKey() {
    return this.key_;
  }
  /**
   * Set the value to be used as the key for all tiles in the source.
   * @param {string} key The key for tiles.
   * @protected
   */
  setKey(e) {
    this.key_ !== e && (this.key_ = e, this.changed());
  }
  /**
   * @param {import("../proj/Projection").default} [projection] Projection.
   * @return {Array<number>|null} Resolutions.
   * @override
   */
  getResolutions(e) {
    const t = e ? this.getTileGridForProjection(e) : this.tileGrid;
    return t ? t.getResolutions() : null;
  }
  /**
   * @abstract
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @param {import("../structs/LRUCache.js").default<import("../Tile.js").default>} [tileCache] Tile cache.
   * @return {TileType|null} Tile.
   */
  getTile(e, t, n, s, r, o) {
    return ue();
  }
  /**
   * Return the tile grid of the tile source.
   * @return {import("../tilegrid/TileGrid.js").default|null} Tile grid.
   * @api
   */
  getTileGrid() {
    return this.tileGrid;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {!import("../tilegrid/TileGrid.js").default} Tile grid.
   */
  getTileGridForProjection(e) {
    return this.tileGrid ? this.tileGrid : Qh(e);
  }
  /**
   * Get the tile pixel ratio for this source. Subclasses may override this
   * method, which is meant to return a supported pixel ratio that matches the
   * provided `pixelRatio` as close as possible.
   * @param {number} pixelRatio Pixel ratio.
   * @return {number} Tile pixel ratio.
   */
  getTilePixelRatio(e) {
    return this.tilePixelRatio_;
  }
  /**
   * @param {number} z Z.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {import("../size.js").Size} Tile size.
   */
  getTilePixelSize(e, t, n) {
    const s = this.getTileGridForProjection(n), r = this.getTilePixelRatio(t), o = et(s.getTileSize(e), this.tmpSize);
    return r == 1 ? o : kf(o, r, this.tmpSize);
  }
  /**
   * Returns a tile coordinate wrapped around the x-axis. When the tile coordinate
   * is outside the resolution and extent range of the tile grid, `null` will be
   * returned.
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {import("../proj/Projection.js").default} [projection] Projection.
   * @return {import("../tilecoord.js").TileCoord} Tile coordinate to be passed to the tileUrlFunction or
   *     null if no tile URL should be created for the passed `tileCoord`.
   */
  getTileCoordForTileUrlFunction(e, t) {
    const n = t !== void 0 ? t : this.getProjection(), s = t !== void 0 ? this.getTileGridForProjection(n) : this.tileGrid || this.getTileGridForProjection(n);
    return this.getWrapX() && n.isGlobal() && (e = $p(s, e, n)), Gp(e, s) ? e : null;
  }
  /**
   * Remove all cached reprojected tiles from the source. The next render cycle will create new tiles.
   * @api
   */
  clear() {
  }
  /**
   * @override
   */
  refresh() {
    this.clear(), super.refresh();
  }
};
var o0 = class extends It {
  /**
   * @param {string} type Type.
   * @param {import("../Tile.js").default} tile The tile.
   */
  constructor(e, t) {
    super(e), this.tile = t;
  }
};
var a0 = r0;
var lo = {
  /**
   * Triggered when a tile starts loading.
   * @event module:ol/source/Tile.TileSourceEvent#tileloadstart
   * @api
   */
  TILELOADSTART: "tileloadstart",
  /**
   * Triggered when a tile finishes loading, either when its data is loaded,
   * or when loading was aborted because the tile is no longer needed.
   * @event module:ol/source/Tile.TileSourceEvent#tileloadend
   * @api
   */
  TILELOADEND: "tileloadend",
  /**
   * Triggered if tile loading results in an error. Note that this is not the
   * right place to re-fetch tiles. See {@link module:ol/ImageTile~ImageTile#load}
   * for details.
   * @event module:ol/source/Tile.TileSourceEvent#tileloaderror
   * @api
   */
  TILELOADERROR: "tileloaderror"
};
var Va = class _Va extends a0 {
  /**
   * @param {Options} options Image tile options.
   */
  constructor(e) {
    super({
      attributions: e.attributions,
      cacheSize: e.cacheSize,
      projection: e.projection,
      state: e.state,
      tileGrid: e.tileGrid,
      tilePixelRatio: e.tilePixelRatio,
      wrapX: e.wrapX,
      transition: e.transition,
      interpolate: e.interpolate,
      key: e.key,
      attributionsCollapsible: e.attributionsCollapsible,
      zDirection: e.zDirection
    }), this.generateTileUrlFunction_ = this.tileUrlFunction === _Va.prototype.tileUrlFunction, this.tileLoadFunction = e.tileLoadFunction, e.tileUrlFunction && (this.tileUrlFunction = e.tileUrlFunction), this.urls = null, e.urls ? this.setUrls(e.urls) : e.url && this.setUrl(e.url), this.tileLoadingKeys_ = {};
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the tile load function of the source.
   * @return {import("../Tile.js").LoadFunction} TileLoadFunction
   * @api
   */
  getTileLoadFunction() {
    return this.tileLoadFunction;
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the tile URL function of the source.
   * @return {import("../Tile.js").UrlFunction} TileUrlFunction
   * @api
   */
  getTileUrlFunction() {
    return Object.getPrototypeOf(this).tileUrlFunction === this.tileUrlFunction ? this.tileUrlFunction.bind(this) : this.tileUrlFunction;
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Return the URLs used for this source.
   * When a tileUrlFunction is used instead of url or urls,
   * null will be returned.
   * @return {!Array<string>|null} URLs.
   * @api
   */
  getUrls() {
    return this.urls;
  }
  /**
   * Handle tile change events.
   * @param {import("../events/Event.js").default} event Event.
   * @protected
   */
  handleTileChange(e) {
    const t = (
      /** @type {import("../Tile.js").default} */
      e.target
    ), n = de(t), s = t.getState();
    let r;
    s == X.LOADING ? (this.tileLoadingKeys_[n] = true, r = lo.TILELOADSTART) : n in this.tileLoadingKeys_ && (delete this.tileLoadingKeys_[n], r = s == X.ERROR ? lo.TILELOADERROR : s == X.LOADED ? lo.TILELOADEND : void 0), r != null && this.dispatchEvent(new o0(r, t));
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the tile load function of the source.
   * @param {import("../Tile.js").LoadFunction} tileLoadFunction Tile load function.
   * @api
   */
  setTileLoadFunction(e) {
    this.tileLoadFunction = e, this.changed();
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the tile URL function of the source.
   * @param {import("../Tile.js").UrlFunction} tileUrlFunction Tile URL function.
   * @param {string} [key] Optional new tile key for the source.
   * @api
   */
  setTileUrlFunction(e, t) {
    this.tileUrlFunction = e, typeof t < "u" ? this.setKey(t) : this.changed();
  }
  /**
   * Set the URL to use for requests.
   * @param {string} url URL.
   * @api
   */
  setUrl(e) {
    const t = t0(e);
    this.urls = t, this.setUrls(t);
  }
  /**
   * Deprecated.  Use an ImageTile source instead.
   * Set the URLs to use for requests.
   * @param {Array<string>} urls URLs.
   * @api
   */
  setUrls(e) {
    this.urls = e;
    const t = e.join(`
`);
    this.generateTileUrlFunction_ ? this.setTileUrlFunction(i0(e, this.tileGrid), t) : this.setKey(t);
  }
  /**
   * @param {import("../tilecoord.js").TileCoord} tileCoord Tile coordinate.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {string|undefined} Tile URL.
   */
  tileUrlFunction(e, t, n) {
  }
};
var l0 = Va;
var c0 = class extends l0 {
  /**
   * @param {!Options} options Image tile options.
   */
  constructor(e) {
    super({
      attributions: e.attributions,
      cacheSize: e.cacheSize,
      projection: e.projection,
      state: e.state,
      tileGrid: e.tileGrid,
      tileLoadFunction: e.tileLoadFunction ? e.tileLoadFunction : h0,
      tilePixelRatio: e.tilePixelRatio,
      tileUrlFunction: e.tileUrlFunction,
      url: e.url,
      urls: e.urls,
      wrapX: e.wrapX,
      transition: e.transition,
      interpolate: e.interpolate !== void 0 ? e.interpolate : true,
      key: e.key,
      attributionsCollapsible: e.attributionsCollapsible,
      zDirection: e.zDirection
    }), this.crossOrigin = e.crossOrigin !== void 0 ? e.crossOrigin : null, this.tileClass = e.tileClass !== void 0 ? e.tileClass : Yh, this.tileGridForProjection = {}, this.reprojectionErrorThreshold_ = e.reprojectionErrorThreshold, this.renderReprojectionEdges_ = false;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {number} Gutter.
   * @override
   */
  getGutterForProjection(e) {
    return this.getProjection() && e && !Gs(this.getProjection(), e) ? 0 : this.getGutter();
  }
  /**
   * @return {number} Gutter.
   */
  getGutter() {
    return 0;
  }
  /**
   * Return the key to be used for all tiles in the source.
   * @return {string} The key for all tiles.
   * @override
   */
  getKey() {
    let e = super.getKey();
    return this.getInterpolate() || (e += ":disable-interpolation"), e;
  }
  /**
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @return {!import("../tilegrid/TileGrid.js").default} Tile grid.
   * @override
   */
  getTileGridForProjection(e) {
    const t = this.getProjection();
    if (this.tileGrid && (!t || Gs(t, e)))
      return this.tileGrid;
    const n = de(e);
    return n in this.tileGridForProjection || (this.tileGridForProjection[n] = Qh(e)), this.tileGridForProjection[n];
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @param {string} key The key set on the tile.
   * @return {!ImageTile} Tile.
   * @private
   */
  createTile_(e, t, n, s, r, o) {
    const a = [e, t, n], l = this.getTileCoordForTileUrlFunction(
      a,
      r
    ), c = l ? this.tileUrlFunction(l, s, r) : void 0, h2 = new this.tileClass(
      a,
      c !== void 0 ? X.IDLE : X.EMPTY,
      c !== void 0 ? c : "",
      this.crossOrigin,
      this.tileLoadFunction,
      this.tileOptions
    );
    return h2.key = o, h2.addEventListener(Z.CHANGE, this.handleTileChange.bind(this)), h2;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {import("../proj/Projection.js").default} projection Projection.
   * @param {import("../structs/LRUCache.js").default<import("../Tile.js").default>} [tileCache] Tile cache.
   * @return {!(ImageTile|ReprojTile)} Tile.
   * @override
   */
  getTile(e, t, n, s, r, o) {
    const a = this.getProjection();
    if (!a || !r || Gs(a, r))
      return this.getTileInternal(
        e,
        t,
        n,
        s,
        a || r
      );
    const l = [e, t, n], c = this.getKey(), h2 = this.getTileGridForProjection(a), u = this.getTileGridForProjection(r), d = this.getTileCoordForTileUrlFunction(
      l,
      r
    ), f = new qh(
      a,
      h2,
      r,
      u,
      l,
      d,
      this.getTilePixelRatio(s),
      this.getGutter(),
      (g, y, m, _) => this.getTileInternal(g, y, m, _, a, o),
      this.reprojectionErrorThreshold_,
      this.renderReprojectionEdges_,
      this.tileOptions
    );
    return f.key = c, f;
  }
  /**
   * @param {number} z Tile coordinate z.
   * @param {number} x Tile coordinate x.
   * @param {number} y Tile coordinate y.
   * @param {number} pixelRatio Pixel ratio.
   * @param {!import("../proj/Projection.js").default} projection Projection.
   * @param {import("../structs/LRUCache.js").default<import("../Tile.js").default>} [tileCache] Tile cache.
   * @return {!ImageTile} Tile.
   * @protected
   */
  getTileInternal(e, t, n, s, r, o) {
    const a = this.getKey(), l = Bs(this, a, e, t, n);
    if (o && o.containsKey(l))
      return (
        /** @type {!ImageTile} */
        o.get(l)
      );
    const c = this.createTile_(e, t, n, s, r, a);
    return o == null || o.set(l, c), c;
  }
  /**
   * Sets whether to render reprojection edges or not (usually for debugging).
   * @param {boolean} render Render the edges.
   * @api
   */
  setRenderReprojectionEdges(e) {
    this.renderReprojectionEdges_ != e && (this.renderReprojectionEdges_ = e, this.changed());
  }
  /**
   * Sets the tile grid to use when reprojecting the tiles to the given
   * projection instead of the default tile grid for the projection.
   *
   * This can be useful when the default tile grid cannot be created
   * (e.g. projection has no extent defined) or
   * for optimization reasons (custom tile size, resolutions, ...).
   *
   * @param {import("../proj.js").ProjectionLike} projection Projection.
   * @param {import("../tilegrid/TileGrid.js").default} tilegrid Tile grid to use for the projection.
   * @api
   */
  setTileGridForProjection(e, t) {
    const n = nt(e);
    if (n) {
      const s = de(n);
      s in this.tileGridForProjection || (this.tileGridForProjection[s] = t);
    }
  }
};
function h0(i, e) {
  if (ft) {
    const t = i.getCrossOrigin();
    let n = "same-origin", s = "same-origin";
    t === "anonymous" || t === "" ? (n = "cors", s = "omit") : t === "use-credentials" && (n = "cors", s = "include"), fetch(e, {
      mode: n,
      credentials: s
    }).then((r) => {
      if (!r.ok)
        throw new Error(`HTTP ${r.status}`);
      return r.blob();
    }).then((r) => createImageBitmap(r)).then((r) => {
      var l;
      const o = i.getImage();
      o.width = r.width, o.height = r.height, /** @type {OffscreenCanvas} */
      o.getContext("2d").drawImage(r, 0, 0), (l = r.close) == null || l.call(r), o.dispatchEvent(new Event("load"));
    }).catch(() => {
      i.getImage().dispatchEvent(new Event("error"));
    });
    return;
  }
  i.getImage().src = e;
}
var u0 = c0;
var d0 = class extends u0 {
  /**
   * @param {Options} [options] XYZ options.
   */
  constructor(e) {
    e = e || {};
    const t = e.projection !== void 0 ? e.projection : "EPSG:3857", n = e.tileGrid !== void 0 ? e.tileGrid : Oo({
      extent: za(t),
      maxResolution: e.maxResolution,
      maxZoom: e.maxZoom,
      minZoom: e.minZoom,
      tileSize: e.tileSize
    });
    super({
      attributions: e.attributions,
      cacheSize: e.cacheSize,
      crossOrigin: e.crossOrigin,
      interpolate: e.interpolate,
      projection: t,
      reprojectionErrorThreshold: e.reprojectionErrorThreshold,
      tileGrid: n,
      tileLoadFunction: e.tileLoadFunction,
      tilePixelRatio: e.tilePixelRatio,
      tileUrlFunction: e.tileUrlFunction,
      url: e.url,
      urls: e.urls,
      wrapX: e.wrapX !== void 0 ? e.wrapX : true,
      transition: e.transition,
      attributionsCollapsible: e.attributionsCollapsible,
      zDirection: e.zDirection
    }), this.gutter_ = e.gutter !== void 0 ? e.gutter : 0;
  }
  /**
   * @return {number} Gutter.
   * @override
   */
  getGutter() {
    return this.gutter_;
  }
};
var Ql = d0;
var f0 = [
  -2003750834e-2,
  -2003750834e-2,
  2003750834e-2,
  2003750834e-2
];
var g0 = (i, e) => {
  if (!Array.isArray(i) || i.length < 2)
    return wt([116.394926, 39.9125], e);
  const [t, n] = i.map((s) => Number(s));
  return !Number.isFinite(t) || !Number.isFinite(n) ? wt([116.394926, 39.9125], e) : e === "EPSG:4326" ? [t, n] : Math.abs(t) <= 180 && Math.abs(n) <= 90 ? wt([t, n], e) : [t, n];
};
var ec = (i, e, t = "EPSG:3857") => t === "EPSG:4326" ? [Number(i), Number(e)] : wt([Number(i), Number(e)], t);
var m0 = (i, e = "EPSG:3857") => !Array.isArray(i) || i.length < 2 ? [0, 0] : e === "EPSG:4326" ? [Number(i[0]), Number(i[1])] : $t(i, e);
var y0 = (i, e) => {
  if (!Array.isArray(i) || i.length < 4)
    return null;
  const t = i.map((s) => Number(s));
  return t.some((s) => !Number.isFinite(s)) ? null : e === "EPSG:4326" ? t : Math.abs(t[0]) <= 180 && Math.abs(t[2]) <= 180 && Math.abs(t[1]) <= 90 && Math.abs(t[3]) <= 90 ? Pd(t, "EPSG:4326", e) : t;
};
var tc = (i = {}, e = "EPSG:3857") => {
  const t = i.id || `local-region-${Date.now()}-${Math.floor(Math.random() * 1e3)}`, n = y0(i.extent, e);
  return !n || !i.urlTemplate ? null : {
    id: String(t),
    name: i.name || String(t),
    urlTemplate: i.urlTemplate,
    extent: n,
    projection: i.projection || e,
    minZoom: Number.isFinite(Number(i.minZoom)) ? Number(i.minZoom) : 7,
    maxZoom: Number.isFinite(Number(i.maxZoom)) ? Number(i.maxZoom) : 20,
    tileSize: Number.isFinite(Number(i.tileSize)) ? Number(i.tileSize) : 256,
    opacity: Number.isFinite(Number(i.opacity)) ? Number(i.opacity) : 1,
    zIndex: Number.isFinite(Number(i.zIndex)) ? Number(i.zIndex) : 20,
    visible: i.visible !== false,
    crossOrigin: i.crossOrigin || "anonymous"
  };
};
var _0 = class {
  init(e = {}) {
    const {
      target: t = "map",
      tileUrlTemplate: n = "",
      projection: s = "EPSG:3857",
      center: r = [116.394926, 39.9125],
      zoom: o = 4,
      minZoom: a = 2,
      maxZoom: l = 20,
      multiWorld: c = true,
      extent: h2 = f0,
      smoothExtentConstraint: u = true,
      localTileRegions: d = []
    } = e, f = Oo({ tileSize: 256, maxZoom: 6 }), g = [];
    return n && g.push(
      new Jl({
        source: new Ql({
          url: n,
          tileGrid: f,
          crossOrigin: "anonymous",
          maxZoom: 6,
          zDirection: 1
        })
      })
    ), this.map = new wp({
      target: t,
      controls: [],
      interactions: jh({ doubleClickZoom: false }),
      layers: g,
      view: new Dt({
        center: g0(r, s),
        projection: s,
        zoom: o,
        minZoom: a,
        maxZoom: l,
        multiWorld: c,
        extent: h2,
        smoothExtentConstraint: u
      })
    }), this.localTileRegionLayers = new globalThis.Map(), this.setLocalTileRegions(d, s), this.map;
  }
  createLocalTileRegionLayer(e) {
    const t = Oo({
      tileSize: e.tileSize,
      maxZoom: e.maxZoom
    }), n = new Jl({
      extent: e.extent,
      visible: e.visible,
      opacity: e.opacity,
      zIndex: e.zIndex,
      source: new Ql({
        url: e.urlTemplate,
        tileGrid: t,
        crossOrigin: e.crossOrigin,
        minZoom: e.minZoom,
        maxZoom: e.maxZoom,
        zDirection: 1
      })
    });
    return n.set("localTileRegionId", e.id), n.set("localTileRegionConfig", { ...e }), n;
  }
  setLocalTileRegions(e = [], t = null) {
    var s, r, o, a, l, c;
    this.clearLocalTileRegions();
    const n = t || ((c = (l = (a = (o = (r = (s = this.map) == null ? void 0 : s.getView) == null ? void 0 : r.call(s)) == null ? void 0 : o.getProjection) == null ? void 0 : a.call(o)) == null ? void 0 : l.getCode) == null ? void 0 : c.call(l)) || "EPSG:3857";
    return (Array.isArray(e) ? e : []).forEach((h2) => {
      var f, g;
      const u = tc(h2, n);
      if (!u)
        return;
      const d = this.createLocalTileRegionLayer(u);
      this.localTileRegionLayers.set(u.id, d), (g = (f = this.map) == null ? void 0 : f.addLayer) == null || g.call(f, d);
    }), this.getLocalTileRegions();
  }
  addLocalTileRegion(e, t = null) {
    var o, a, l, c, h2, u, d, f;
    const n = t || ((u = (h2 = (c = (l = (a = (o = this.map) == null ? void 0 : o.getView) == null ? void 0 : a.call(o)) == null ? void 0 : l.getProjection) == null ? void 0 : c.call(l)) == null ? void 0 : h2.getCode) == null ? void 0 : u.call(h2)) || "EPSG:3857", s = tc(e, n);
    if (!s)
      return null;
    this.removeLocalTileRegion(s.id);
    const r = this.createLocalTileRegionLayer(s);
    return this.localTileRegionLayers.set(s.id, r), (f = (d = this.map) == null ? void 0 : d.addLayer) == null || f.call(d, r), { ...s };
  }
  removeLocalTileRegion(e) {
    var s, r, o, a;
    const t = String(e || ""), n = (r = (s = this.localTileRegionLayers) == null ? void 0 : s.get) == null ? void 0 : r.call(s, t);
    return n ? ((a = (o = this.map) == null ? void 0 : o.removeLayer) == null || a.call(o, n), this.localTileRegionLayers.delete(t), true) : false;
  }
  clearLocalTileRegions() {
    this.localTileRegionLayers && (this.localTileRegionLayers.forEach((e) => {
      var t, n;
      (n = (t = this.map) == null ? void 0 : t.removeLayer) == null || n.call(t, e);
    }), this.localTileRegionLayers.clear());
  }
  getLocalTileRegions() {
    var e, t;
    return Array.from(((t = (e = this.localTileRegionLayers) == null ? void 0 : e.values) == null ? void 0 : t.call(e)) || []).map((n) => ({
      ...n.get("localTileRegionConfig") || {}
    }));
  }
  destroy() {
    this.map && (this.clearLocalTileRegions(), this.map.setTarget(null), this.map = null, this.localTileRegionLayers = new globalThis.Map());
  }
  /**
   * 设置地图中心点
   * @param {number} lon - 经度
   * @param {number} lat - 纬度
   * @param {boolean} animate - 是否使用动画
   * @param {number} duration - 动画持续时间（毫秒）
   */
  setCenter(e, t, n = true, s = 500) {
    var l, c, h2;
    const r = this.map.getView(), o = ((h2 = (c = (l = r == null ? void 0 : r.getProjection) == null ? void 0 : l.call(r)) == null ? void 0 : c.getCode) == null ? void 0 : h2.call(c)) || "EPSG:3857", a = ec(e, t, o);
    n ? r.animate({
      center: a,
      duration: s
    }) : r.setCenter(a);
  }
  /**
   * 设置地图缩放级别
   * @param {number} zoom - 缩放级别
   * @param {boolean} animate - 是否使用动画
   * @param {number} duration - 动画持续时间（毫秒）
   */
  setZoom(e, t = true, n = 500) {
    const s = this.map.getView();
    t ? s.animate({
      zoom: e,
      duration: n
    }) : s.setZoom(e);
  }
  /**
   * 同时设置中心点和缩放级别
   * @param {number} lon - 经度
   * @param {number} lat - 纬度
   * @param {number} zoom - 缩放级别
   * @param {boolean} animate - 是否使用动画
   * @param {number} duration - 动画持续时间（毫秒）
   */
  setCenterAndZoom(e, t, n, s = true, r = 500) {
    var c, h2, u;
    const o = this.map.getView(), a = ((u = (h2 = (c = o == null ? void 0 : o.getProjection) == null ? void 0 : c.call(o)) == null ? void 0 : h2.getCode) == null ? void 0 : u.call(h2)) || "EPSG:3857", l = ec(e, t, a);
    s ? o.animate({
      center: l,
      zoom: n,
      duration: r
    }) : (o.setCenter(l), o.setZoom(n));
  }
  /**
   * 获取当前地图中心点
   * @returns {Array} [lon, lat]
   */
  getCenter() {
    var s, r, o;
    if (!this.map)
      return [0, 0];
    const e = this.map.getView(), t = e.getCenter(), n = ((o = (r = (s = e == null ? void 0 : e.getProjection) == null ? void 0 : s.call(e)) == null ? void 0 : r.getCode) == null ? void 0 : o.call(r)) || "EPSG:3857";
    return m0(t, n);
  }
  /**
   * 获取当前缩放级别
   * @returns {number}
   */
  getZoom() {
    return this.map ? this.map.getView().getZoom() : 0;
  }
};
var p0 = class {
  constructor(e, t, n, s, r = {}) {
    this.layerManager = e, this.popup = t, this.iconManager = n, this.lineDrawManager = s, this.features = /* @__PURE__ */ new Map(), this.trajectories = /* @__PURE__ */ new Map(), this.options = {
      maxTrajectoryPoints: r.maxTrajectoryPoints || 1 / 0
      // 默认无限制
    };
  }
  /**
   * 注册模型到移动管理器
   * @param {Feature} feature - OpenLayers Feature 对象
   */
  register(e) {
    const t = e.getId();
    this.features.set(t, e), this.trajectories.has(t) || this.trajectories.set(t, []);
  }
  /**
   * 取消注册模型
   * @param {string} uid - 模型唯一标识符
   */
  unregister(e) {
    this.features.delete(e), this.trajectories.delete(e);
  }
  /**
   * 批量更新多个模型
   * @param {Array} updates - 更新数据数组
   * @param {string} updates[].uid - 模型唯一标识符
   * @param {number} updates[].lon - 目标经度
   * @param {number} updates[].lat - 目标纬度
   * @param {number} [updates[].height] - 高度（可选）
   * @param {number} [updates[].rotation] - 朝向角度（弧度，可选）
   * @param {boolean} [updates[].drawTrajectory=false] - 是否绘制轨迹线
   *
   * @example
   * mover.batchUpdate([
   *   { uid: 'model1', lon: 120.5, lat: 30.2, rotation: 1.57, drawTrajectory: true },
   *   { uid: 'model2', lon: 121.0, lat: 31.0, rotation: 0, drawTrajectory: false }
   * ]);
   */
  batchUpdate(e) {
    var r;
    if (!Array.isArray(e) || e.length === 0) {
      console.warn("ModelMover.batchUpdate: updates must be a non-empty array");
      return;
    }
    const t = (r = this.popup) == null ? void 0 : r.getCurrentFeature();
    let n = null, s = null;
    e.forEach((o) => {
      const a = this.updateSingle(o, false);
      a && t && a.feature === t && (n = a.feature, s = a.coord);
    }), n && s && (this.popup.updateContent(n), this.popup.getpopup().setPosition(s));
  }
  /**
   * 更新单个模型
   * @param {Object} update - 更新数据
   * @param {boolean} updatePopup - 是否立即更新弹窗
   * @returns {Object|null} 返回 { feature, coord } 或 null
   */
  updateSingle(e, t = true) {
    const { uid: n, lon: s, lat: r, height: o, rotation: a, drawTrajectory: l = false } = e;
    if (!n || s === void 0 || r === void 0)
      return console.warn("ModelMover.updateSingle: uid, lon, lat are required", e), null;
    const c = this.features.get(n);
    if (!c)
      return console.warn(`ModelMover.updateSingle: feature with uid "${n}" not found`), null;
    const h2 = wt([s, r]);
    return c.getGeometry().setCoordinates(h2), c.set("lon", s), c.set("lat", r), o !== void 0 && c.set("height", o), a !== void 0 && this.iconManager && this.iconManager.setRotation(c, a), l && (this.addTrajectoryPoint(n, s, r), this.updateTrajectoryLine(n)), t && this.popup && this.popup.getCurrentFeature() === c && (this.popup.updateContent(c), this.popup.getpopup().setPosition(h2)), { feature: c, coord: h2 };
  }
  /**
   * 添加轨迹点
   * @param {string} uid - 模型唯一标识符
   * @param {number} lon - 经度
   * @param {number} lat - 纬度
   */
  addTrajectoryPoint(e, t, n) {
    this.trajectories.has(e) || this.trajectories.set(e, []);
    const s = this.trajectories.get(e);
    s.push({ lon: t, lat: n }), this.options.maxTrajectoryPoints !== 1 / 0 && s.length > this.options.maxTrajectoryPoints && s.shift();
  }
  /**
   * 更新轨迹线
   * @param {string} uid - 模型唯一标识符
   */
  updateTrajectoryLine(e) {
    if (!this.lineDrawManager) {
      console.warn("ModelMover: lineDrawManager not available");
      return;
    }
    const t = this.trajectories.get(e);
    !t || t.length < 2 || this.lineDrawManager.drawLine(t, e);
  }
  /**
   * 清除指定模型的轨迹
   * @param {string} uid - 模型唯一标识符
   */
  clearTrajectory(e) {
    this.trajectories.set(e, []), this.lineDrawManager && this.lineDrawManager.clearByIconUID(e);
  }
  /**
   * 清除所有轨迹
   */
  clearAllTrajectories() {
    this.trajectories.clear(), this.lineDrawManager && this.lineDrawManager.clear();
  }
  /**
   * 获取指定模型的轨迹历史
   * @param {string} uid - 模型唯一标识符
   * @returns {Array} 轨迹点数组
   */
  getTrajectory(e) {
    return this.trajectories.get(e) || [];
  }
  /**
   * 销毁管理器，清理所有资源
   */
  destroy() {
    this.features.clear(), this.trajectories.clear();
  }
  /**
   * 单个模型更新（兼容旧接口）
   * @deprecated 建议使用 batchUpdate
   */
  update(e, t, n, s) {
    this.updateSingle({ uid: e, lon: t, lat: n, height: s });
  }
};
var S0 = class {
  constructor(e) {
    this.source = e.getSource();
  }
  /** 创建航迹点 */
  createPoint({ lon: e, lat: t, color: n = "red", id: s, index: r, iconUID: o }) {
    const a = new ae({
      geometry: new he(wt([e, t]))
    });
    return s && a.setId(s), a.set("layerType", "trackPoint"), r !== void 0 && a.set("index", r), o && a.set("iconUID", o), a.setStyle(this.getStyle(n)), this.source.addFeature(a), a;
  }
  /** 点样式 */
  getStyle(e) {
    const t = e === "blue" ? "rgba(0, 120, 255, 0.9)" : "rgba(255, 60, 60, 0.9)";
    return new te({
      image: new we({
        radius: 6,
        fill: new J({ color: t }),
        stroke: new se({
          color: "#fff",
          width: 2
        })
      })
    });
  }
  clear() {
    this.source.clear();
  }
  /** 删除指定图标的航迹点 */
  clearByIconUID(e) {
    if (!e)
      return;
    this.source.getFeatures().filter((s) => s.get("iconUID") === e).forEach((s) => this.source.removeFeature(s));
  }
};
var E0 = class {
  constructor(e) {
    this.map = e, this.overlays = /* @__PURE__ */ new Map();
  }
  init() {
  }
  renderContent(e) {
    const t = e.getGeometry().getCoordinates(), [n, s] = $t(t), r = e.get("speed"), o = e.get("status") || "normal", a = e.get("mounts") || [], l = a.length ? a.map((c) => c.showName || c.name).join(" / ") : "-";
    return `
            <div class="popup-info">
                <p><strong>名称:</strong> ${e.get("showName") || "-"}</p>
                <p><strong>阵营:</strong> ${e.get("nation") || "-"}</p>
                <p><strong>经度:</strong> ${n.toFixed(5)}</p>
                <p><strong>纬度:</strong> ${s.toFixed(5)}</p>
                <p><strong>高度:</strong> ${e.get("height") ?? 0}</p>
                <p><strong>速度:</strong> ${r ?? 0}</p>
                <p><strong>挂载:</strong> ${l}</p>
                <p><strong>状态:</strong> ${o}</p>
            </div>
        `;
  }
  createElement(e) {
    const t = document.createElement("div");
    return t.className = "model-popup", t.innerHTML = this.renderContent(e), t;
  }
  ensureOverlay(e) {
    const t = e.get("UID");
    let n = this.overlays.get(t);
    return n || (n = new xp({
      element: this.createElement(e),
      positioning: "bottom-center",
      offset: [0, -40],
      stopEvent: false
    }), this.overlays.set(t, n), this.map.addOverlay(n)), n;
  }
  syncFeature(e, t = true) {
    if (!e)
      return;
    const n = e.get("UID");
    if (!t) {
      this.removeFeature(n);
      return;
    }
    const s = this.ensureOverlay(e);
    s.getElement().innerHTML = this.renderContent(e), s.setPosition(e.getGeometry().getCoordinates());
  }
  syncAll(e = [], t = true) {
    if (!t) {
      this.hide();
      return;
    }
    const n = /* @__PURE__ */ new Set();
    e.forEach((s) => {
      n.add(s.get("UID")), this.syncFeature(s, true);
    }), Array.from(this.overlays.keys()).forEach((s) => {
      n.has(s) || this.removeFeature(s);
    });
  }
  removeFeature(e) {
    const t = this.overlays.get(e);
    t && (this.map.removeOverlay(t), this.overlays.delete(e));
  }
  hide() {
    Array.from(this.overlays.keys()).forEach((e) => {
      this.removeFeature(e);
    });
  }
  destroy() {
    this.hide(), this.map = null;
  }
  getCurrentFeature() {
    return null;
  }
  getpopup() {
    return null;
  }
  show() {
  }
  updateContent() {
  }
  setPosition() {
  }
  followFeature(e) {
    this.syncFeature(e, true);
  }
};
var w0 = class {
  constructor(e, t) {
    this.pointDraw = e, this.lineDraw = t, this.points = [], this.nation = "red", this.initLon = null, this.initLat = null, this.started = false, this.iconUID = null;
  }
  /** 设置阵营（red / blue）并初始化起始位置 */
  setNation(e, t, n, s = null) {
    this.nation = e === "blue" ? "blue" : "red", !this.started && (this.started = true, this.iconUID = s, this.initLon = t, this.initLat = n, this.points = [{ lon: t, lat: n }], this.lineDraw.setColor(this.nation));
  }
  onMapClick(e) {
    const [t, n] = $t(e.coordinate), s = this.points.length, r = { lon: t, lat: n };
    this.points.push(r), this.pointDraw.createPoint({
      lon: t,
      lat: n,
      color: this.nation,
      index: s,
      iconUID: this.iconUID
    }), this.lineDraw.drawLine(this.points, this.iconUID);
  }
  addPoint(e, t) {
    const n = this.points.length, s = { lon: e, lat: t };
    this.points.push(s), this.pointDraw.createPoint({
      lon: e,
      lat: t,
      color: this.nation,
      index: n,
      iconUID: this.iconUID
    }), this.lineDraw.drawLine(this.points, this.iconUID);
  }
  // 更新点
  updatePoint(e, t, n) {
    this.points[e] && (this.points[e].lon = t, this.points[e].lat = n, this.lineDraw.drawLine(this.points, this.iconUID));
  }
  getPoints() {
    return this.points;
  }
  clear(e = false) {
    e && this.iconUID && this.lineDraw.clearByIconUID(this.iconUID), this.points = [], this.started = false, this.iconUID = null, this.pointDraw.clear();
  }
};
var C0 = class {
  constructor({ getProjectionCode: e, getApp: t }) {
    this.getProjectionCode = e, this.getApp = t;
  }
  init(e = {}) {
    Lh(e);
    const t = Ey(), n = new _0(), s = n.init({
      ...e,
      target: e.target,
      tileUrlTemplate: t
    }), r = new b_(s);
    r.init();
    const o = new E0(s);
    o.init();
    const a = new C_(r.destroyLayer), l = new T_(
      r.iconLayer,
      a,
      () => this.getProjectionCode()
    ), c = new M_(), h2 = new S0(r.pointLayer), u = new R_(r.lineLayer), d = new p0(r, o, l, u), f = new w0(h2, u), g = new v_(
      s,
      r,
      o,
      c,
      this.getApp(),
      f
    );
    return g.init(), {
      core: n,
      map: s,
      layer: r,
      popup: o,
      effectManager: a,
      iconManager: l,
      feature: c,
      pointDraw: h2,
      lineDraw: u,
      mover: d,
      routeEditor: f,
      events: g,
      tileUrlTemplate: t
    };
  }
};
var x0 = class {
  constructor({ getMap: e }) {
    this.getMap = e;
  }
  getIconUrl(e) {
    return Cy(e);
  }
  getProjectionCode() {
    var e, t, n, s, r, o;
    return ((o = (r = (s = (n = (t = (e = this.getMap()) == null ? void 0 : e.getView) == null ? void 0 : t.call(e)) == null ? void 0 : n.getProjection) == null ? void 0 : s.call(n)) == null ? void 0 : r.getCode) == null ? void 0 : o.call(r)) || "EPSG:3857";
  }
  coordinateFromLonLat(e, t) {
    const n = this.getProjectionCode();
    return n === "EPSG:4326" ? [Number(e), Number(t)] : wt([Number(e), Number(t)], n);
  }
  lonLatFromCoordinate(e) {
    const t = this.getProjectionCode();
    return t === "EPSG:4326" ? [Number(e == null ? void 0 : e[0]), Number(e == null ? void 0 : e[1])] : $t(e, t);
  }
  getModelScale(e) {
    return e.status === "hidden" || e.visible === false ? 0 : 0.5;
  }
};
var I0 = class {
  constructor({
    getCore: e,
    getLayer: t,
    getIconManager: n,
    getEffectManager: s,
    coordinateFromLonLat: r,
    lonLatFromCoordinate: o
  }) {
    this.getCore = e, this.getLayer = t, this.getIconManager = n, this.getEffectManager = s, this.coordinateFromLonLat = r, this.lonLatFromCoordinate = o;
  }
  setCenterAndZoom(e, t, n, s = true, r = 500) {
    var o;
    (o = this.getCore()) == null || o.setCenterAndZoom(e, t, n, s, r);
  }
  focusOnIcon(e, t = null, n = true, s = 1500) {
    var c, h2, u, d, f, g;
    const r = (h2 = (c = this.getLayer()) == null ? void 0 : c.getFeatureById) == null ? void 0 : h2.call(c, e);
    if (!r)
      return;
    const o = (d = (u = r.getGeometry()) == null ? void 0 : u.getCoordinates) == null ? void 0 : d.call(u);
    if (!o)
      return;
    const [a, l] = this.lonLatFromCoordinate(o);
    t !== null ? (f = this.getCore()) == null || f.setCenterAndZoom(a, l, t, n, s) : (g = this.getCore()) == null || g.setCenter(a, l, n, s);
  }
  setIconRotationToTarget(e, t, n) {
    var r, o, a, l;
    const s = (o = (r = this.getIconManager()) == null ? void 0 : r.getByUID) == null ? void 0 : o.call(r, e);
    s && ((l = (a = this.getIconManager()) == null ? void 0 : a.setRotationToTarget) == null || l.call(a, s, t, n));
  }
  playEventEffect(e, t = {}) {
    const n = this.getEffectManager();
    if (!n)
      return false;
    const s = this.resolveEffectCoordinate(e);
    return s ? (n.playDestroyEffect(s, {
      type: t.type || "flash",
      duration: t.duration || 1200,
      color: t.color || "rgba(255, 80, 80, 1)",
      spriteUrl: t.spriteUrl || null,
      spriteFrames: t.spriteFrames || 16
    }), true) : false;
  }
  resolveEffectCoordinate(e) {
    var n, s, r, o, a;
    if (Array.isArray(e))
      return e;
    if (e && typeof e == "object" && e.lon !== void 0 && e.lat !== void 0)
      return this.coordinateFromLonLat(e.lon, e.lat);
    const t = (s = (n = this.getLayer()) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, e);
    return ((a = (o = (r = t == null ? void 0 : t.getGeometry) == null ? void 0 : r.call(t)) == null ? void 0 : o.getCoordinates) == null ? void 0 : a.call(o)) || null;
  }
};
var v0 = /* @__PURE__ */ new Set(["edit", "view", "playback"]);
var M0 = class {
  constructor({ onLeaveEditable: e } = {}) {
    this.mode = "edit", this.editable = true, this.onLeaveEditable = e;
  }
  setMode(e = "edit", t = {}) {
    var n;
    return this.mode = v0.has(e) ? e : "edit", this.editable = t.editable ?? this.mode === "edit", this.isEditable() || (n = this.onLeaveEditable) == null || n.call(this), this.mode;
  }
  isEditable() {
    return this.editable !== false && this.mode === "edit";
  }
  setEditable(e) {
    var t;
    return this.editable = !!e, this.isEditable() || (t = this.onLeaveEditable) == null || t.call(this), this.editable;
  }
  getMode() {
    return this.mode;
  }
  reset() {
    this.mode = "edit", this.editable = true;
  }
};
var co = (i, e) => {
  const t = Number(e) || 0;
  return i === "area" ? t >= 1e6 ? `${(t / 1e6).toFixed(2)} km2` : `${t.toFixed(0)} m2` : t >= 1e3 ? `${(t / 1e3).toFixed(2)} km` : `${t.toFixed(0)} m`;
};
var T0 = class {
  constructor({ getMap: e, getMeasureLayer: t, stopInteractions: n, onMeasureEnd: s } = {}) {
    this.getMap = e, this.getMeasureLayer = t, this.stopInteractions = n, this.onMeasureEnd = s, this.drawInteraction = null;
  }
  start(e = "distance") {
    var o, a, l;
    const t = (o = this.getMap) == null ? void 0 : o.call(this), n = (a = this.getMeasureLayer) == null ? void 0 : a.call(this);
    if (!t || !n)
      return false;
    (l = this.stopInteractions) == null || l.call(this);
    const s = e === "area" ? "Polygon" : "LineString", r = new Rh({
      source: n.getSource(),
      type: s
    });
    return r.on("drawend", (c) => {
      var u;
      const h2 = this.appendResultFeature(e, c.feature);
      h2 && ((u = this.onMeasureEnd) == null || u.call(this, {
        type: h2.type,
        value: h2.value,
        label: h2.label,
        measureId: h2.id
      }));
    }), this.drawInteraction = r, t.addInteraction(r), t.getTargetElement().style.cursor = "crosshair", true;
  }
  stop() {
    var t;
    const e = (t = this.getMap) == null ? void 0 : t.call(this);
    e && this.drawInteraction && e.removeInteraction(this.drawInteraction), this.drawInteraction = null;
  }
  clear() {
    var t, n, s, r;
    const e = (t = this.getMeasureLayer) == null ? void 0 : t.call(this);
    (r = (n = e == null ? void 0 : e.getSource) == null ? void 0 : (s = n.call(e)).clear) == null || r.call(s), this.stop();
  }
  end() {
    var t, n;
    this.clear();
    const e = (t = this.getMap) == null ? void 0 : t.call(this);
    (n = e == null ? void 0 : e.getTargetElement) != null && n.call(e) && (e.getTargetElement().style.cursor = "");
  }
  createGeometryStyle(e) {
    const t = e === "area" ? "rgba(255, 190, 92, 0.98)" : "rgba(76, 196, 255, 0.98)", n = e === "area" ? "rgba(255, 190, 92, 0.18)" : "rgba(76, 196, 255, 0.08)";
    return new te({
      stroke: new se({
        color: t,
        width: 3,
        lineDash: e === "area" ? void 0 : [10, 6]
      }),
      fill: new J({ color: n })
    });
  }
  createLabelStyle(e, t) {
    const n = t === "area" ? "rgba(255, 190, 92, 0.98)" : "rgba(76, 196, 255, 0.98)";
    return new te({
      text: new zn({
        text: e,
        padding: [6, 10, 6, 10],
        overflow: true,
        offsetY: t === "distance" ? -18 : 0,
        backgroundFill: new J({ color: "rgba(9, 18, 30, 0.88)" }),
        backgroundStroke: new se({
          color: n,
          width: 1.5
        }),
        fill: new J({ color: "#f8fbff" }),
        stroke: new se({ color: "rgba(0,0,0,0.55)", width: 3 })
      })
    });
  }
  createLabelFeature(e, t, n, s = null) {
    let r = s;
    if (r || (e === "area" && n instanceof at ? r = n.getInteriorPoint().getCoordinates() : e === "distance" && n instanceof tt && (r = n.getLastCoordinate())), !r)
      return null;
    const o = new ae({
      geometry: new he(r),
      measureRole: "label",
      measureType: e,
      measureLabel: t
    });
    return o.setStyle(this.createLabelStyle(t, e)), o;
  }
  createDistanceLabelFeatures(e, t, n) {
    if (!(e instanceof tt))
      return [];
    const s = e.getCoordinates();
    return s.length ? s.map((r, o) => {
      const a = o === 0 ? co("distance", 0) : co(
        "distance",
        po(new tt(s.slice(0, o + 1)), {
          projection: t
        })
      ), l = this.createLabelFeature(
        "distance",
        a,
        e,
        r
      );
      return l ? (l.set("measureId", n), l.set("measurePointIndex", o), l) : null;
    }).filter(Boolean) : [];
  }
  appendResultFeature(e, t) {
    var u, d;
    const n = (u = this.getMap) == null ? void 0 : u.call(this), s = (d = this.getMeasureLayer) == null ? void 0 : d.call(this);
    if (!t || !s || !n)
      return null;
    const r = t.getGeometry(), o = n.getView().getProjection(), a = e === "area" ? bc(r, { projection: o }) : po(r, { projection: o }), l = co(e, a), c = `measure-${Date.now()}-${Math.floor(Math.random() * 1e3)}`;
    t.set("measureRole", "geometry"), t.set("measureType", e), t.set("measureId", c), t.set("measureLabel", l), t.setStyle(this.createGeometryStyle(e));
    const h2 = e === "distance" ? this.createDistanceLabelFeatures(r, o, c) : (() => {
      const f = this.createLabelFeature(e, l, r);
      return f ? (f.set("measureId", c), [f]) : [];
    })();
    return h2.forEach((f) => {
      s.getSource().addFeature(f);
    }), {
      id: c,
      type: e,
      value: a,
      label: l,
      feature: t,
      labelFeatures: h2
    };
  }
};
var b0 = class {
  constructor({ getDisplayConfig: e, getFeatures: t, getPopupManager: n, getIconManager: s, isSelected: r }) {
    this.getDisplayConfig = e, this.getFeatures = t, this.getPopupManager = n, this.getIconManager = s, this.isSelected = r;
  }
  shouldShowPopup() {
    var e;
    return ((e = this.getDisplayConfig()) == null ? void 0 : e.showPopup) !== false;
  }
  shouldDisplayPopupForFeature(e) {
    return !e || e.get("status") === "hidden" ? false : this.shouldShowPopup() || this.isSelected(e);
  }
  getVisiblePopupFeatures() {
    return this.getFeatures().filter((e) => this.shouldDisplayPopupForFeature(e));
  }
  syncPopupForFeature(e) {
    var t, n;
    e && ((n = (t = this.getPopupManager()) == null ? void 0 : t.syncFeature) == null || n.call(t, e, this.shouldDisplayPopupForFeature(e)));
  }
  syncAllModelPopups() {
    var t, n;
    const e = this.getVisiblePopupFeatures();
    (n = (t = this.getPopupManager()) == null ? void 0 : t.syncAll) == null || n.call(t, e, e.length > 0);
  }
  applyFeatureSelectionStyle(e) {
    var t, n, s;
    e && (e.set("showModelName", ((t = this.getDisplayConfig()) == null ? void 0 : t.showModelName) !== false), (s = (n = this.getIconManager()) == null ? void 0 : n.applyCurrentStyle) == null || s.call(n, e));
  }
  refreshModelNameVisibility() {
    this.getFeatures().forEach((e) => this.applyFeatureSelectionStyle(e));
  }
};
var R0 = (i, e, t) => i.get(e) === t ? false : (i.set(e, t), true);
var L0 = (i, e) => ({
  showName: i.showName,
  nation: i.nation,
  NodeType: i.NodeType,
  iconKey: i.iconKey || i.NodeType || "default",
  modelType: i.modelType || i.NodeType || "PLATFORM",
  height: i.height,
  speed: i.speed,
  heading: i.heading,
  status: i.status,
  visible: i.visible,
  lon: i.lon,
  lat: i.lat,
  targetUID: i.targetUID || null,
  line: i.line || e.get("line") || [],
  routeMeta: i.routeMeta || e.get("routeMeta") || {},
  mounts: i.mounts || e.get("mounts") || []
});
var F0 = class {
  setPropertyIfChanged(e, t, n) {
    return R0(e, t, n);
  }
  updateProperties(e, t) {
    const n = L0(t, e);
    return Object.entries(n).reduce(
      (s, [r, o]) => this.setPropertyIfChanged(e, r, o) || s,
      false
    );
  }
};
var D0 = class {
  constructor({
    getLayer: e,
    getEvents: t,
    getRouteEditor: n,
    getPointDraw: s,
    getLineDraw: r,
    getHeadingIndicatorRenderer: o,
    getRelationIndicatorRenderer: a,
    getSituationEffectRenderer: l,
    getPopupManager: c,
    getMover: h2,
    getIconManager: u,
    getSelectionService: d,
    refreshTargetIndicatorsFromFeatures: f,
    refreshSituationEffectsFromFeatures: g,
    stopTargetFlowAnimationIfIdle: y,
    stopEffectAnimationIfIdle: m
  }) {
    this.getLayer = e, this.getEvents = t, this.getRouteEditor = n, this.getPointDraw = s, this.getLineDraw = r, this.getHeadingIndicatorRenderer = o, this.getRelationIndicatorRenderer = a, this.getSituationEffectRenderer = l, this.getPopupManager = c, this.getMover = h2, this.getIconManager = u, this.getSelectionService = d, this.refreshTargetIndicatorsFromFeatures = f, this.refreshSituationEffectsFromFeatures = g, this.stopTargetFlowAnimationIfIdle = y, this.stopEffectAnimationIfIdle = m;
  }
  removeById(e, t = {}) {
    var s, r, o, a, l, c, h2, u, d, f;
    const n = (r = (s = this.getLayer()) == null ? void 0 : s.getFeatureById) == null ? void 0 : r.call(s, e);
    return n ? ((a = (o = this.getSelectionService()) == null ? void 0 : o.removeFeature) == null || a.call(o, n, e), this.stopRouteEditingIfNeeded(n), this.clearModelArtifacts(e), this.refreshDependentIndicators(e), (c = (l = this.getPopupManager()) == null ? void 0 : l.removeFeature) == null || c.call(l, e), (u = (h2 = this.getMover()) == null ? void 0 : h2.unregister) == null || u.call(h2, e), (f = (d = this.getIconManager()) == null ? void 0 : d.deleteIcon) == null || f.call(d, n, t), n) : null;
  }
  stopRouteEditingIfNeeded(e) {
    var n, s, r;
    const t = this.getEvents();
    (t == null ? void 0 : t.currentEditingFeature) === e && ((n = t.disableRouteEdit) == null || n.call(t), (r = (s = this.getRouteEditor()) == null ? void 0 : s.clear) == null || r.call(s, true));
  }
  clearModelArtifacts(e) {
    var t, n, s, r, o, a, l, c, h2, u;
    (n = (t = this.getPointDraw()) == null ? void 0 : t.clearByIconUID) == null || n.call(t, e), (r = (s = this.getLineDraw()) == null ? void 0 : s.clearByIconUID) == null || r.call(s, e), (a = (o = this.getHeadingIndicatorRenderer()) == null ? void 0 : o.removeByUID) == null || a.call(o, e), (c = (l = this.getRelationIndicatorRenderer()) == null ? void 0 : l.removeByModelUID) == null || c.call(l, e), (u = (h2 = this.getSituationEffectRenderer()) == null ? void 0 : h2.removeBySourceUID) == null || u.call(h2, e);
  }
  refreshDependentIndicators(e) {
    const t = String(e);
    this.refreshTargetIndicatorsFromFeatures(t), this.refreshSituationEffectsFromFeatures(t), this.stopTargetFlowAnimationIfIdle(), this.stopEffectAnimationIfIdle();
  }
};
var P0 = class {
  constructor({
    getLayer: e,
    getIconManager: t,
    getMover: n,
    getDisplayConfig: s,
    getIconUrl: r,
    getModelScale: o,
    updateFeatureProperties: a,
    isBatchSelected: l,
    applyFeatureSelectionStyle: c,
    updateHeadingIndicator: h2,
    updateTargetIndicatorsForModel: u,
    syncPopupForFeature: d
  }) {
    this.getLayer = e, this.getIconManager = t, this.getMover = n, this.getDisplayConfig = s, this.getIconUrl = r, this.getModelScale = o, this.updateFeatureProperties = a, this.isBatchSelected = l, this.applyFeatureSelectionStyle = c, this.updateHeadingIndicator = h2, this.updateTargetIndicatorsForModel = u, this.syncPopupForFeature = d;
  }
  async add(e, t = {}) {
    var r, o;
    const s = await this.getIconManager().createIcon(
      {
        ...e,
        rotation: Vs(e.heading),
        scale: this.getModelScale(e)
      },
      this.getIconUrl(e)
    );
    return this.updateFeatureProperties(s, e), s.set("showModelName", ((r = this.getDisplayConfig()) == null ? void 0 : r.showModelName) !== false), (o = this.getMover()) == null || o.register(s), s.set("isBatchSelected", this.isBatchSelected(e.UID)), this.applyFeatureSelectionStyle(s), t.skipIndicators || (this.updateHeadingIndicator(e, this.getDisplayConfig()), this.updateTargetIndicatorsForModel(e)), t.deferPopup || this.syncPopupForFeature(s), s;
  }
  async upsert(e, t = {}) {
    var h2, u, d;
    const n = (u = (h2 = this.getLayer()) == null ? void 0 : h2.getFeatureById) == null ? void 0 : u.call(h2, e.UID);
    if (!n)
      return this.add(e, t);
    const s = n.get("targetUID"), r = n.get("heading"), o = n.get("nation"), a = n.get("status"), l = n.get("isBatchSelected"), c = this.updateFeatureProperties(n, e);
    if ((d = this.getIconManager()) == null || d.updateIcon(n, {
      lon: e.lon,
      lat: e.lat,
      iconUrl: this.getIconUrl(e),
      scale: this.getModelScale(e),
      rotation: Vs(e.heading)
    }), !t.skipIndicators) {
      const f = this.getDisplayConfig();
      (r !== e.heading || o !== e.nation || a !== e.status || c) && this.updateHeadingIndicator(e, f), (s !== (e.targetUID || null) || o !== e.nation || a !== e.status || c) && this.updateTargetIndicatorsForModel(e);
    }
    return n.set("isBatchSelected", this.isBatchSelected(e.UID)), l !== n.get("isBatchSelected") && this.applyFeatureSelectionStyle(n), t.deferPopup || this.syncPopupForFeature(n), n;
  }
};
var A0 = (i) => JSON.parse(JSON.stringify(i));
var k0 = () => ({
  showPopup: true,
  showModelName: true,
  showTargetLine: false,
  targetLineStyle: {
    redColor: "#ffb950",
    blueColor: "#60d2ff",
    width: 3,
    lineType: "flowDash"
  },
  relationLineStyles: {
    target: {
      color: "rgba(255, 185, 80, 0.95)",
      width: 3,
      lineType: "flowDash"
    }
  },
  effectVisible: {},
  effectStyles: {},
  performance: {
    level: "quality"
  },
  headingVisible: {
    red: false,
    blue: false
  }
});
var O0 = (i = {}, e = {}) => ({
  ...i,
  ...A0(e),
  targetLineStyle: {
    ...i.targetLineStyle || {},
    ...e.targetLineStyle || {}
  },
  relationLineStyles: {
    ...i.relationLineStyles || {},
    ...e.relationLineStyles || {}
  },
  effectStyles: {
    ...i.effectStyles || {},
    ...e.effectStyles || {}
  },
  performance: {
    ...i.performance || {},
    ...e.performance || {}
  },
  headingVisible: {
    ...i.headingVisible || {},
    ...e.headingVisible || {}
  },
  effectVisible: {
    ...i.effectVisible || {},
    ...e.effectVisible || {}
  }
});
var N0 = (i = {}) => {
  var n;
  const e = ((n = i == null ? void 0 : i.performance) == null ? void 0 : n.level) || (i == null ? void 0 : i.performanceLevel) || "quality", t = {
    quality: {
      level: "quality",
      effectAnimationInterval: 80,
      relationAnimationInterval: 80,
      effectAnimationStep: 1,
      relationFlowStep: -2
    },
    balanced: {
      level: "balanced",
      effectAnimationInterval: 120,
      relationAnimationInterval: 120,
      effectAnimationStep: 1,
      relationFlowStep: -2
    },
    performance: {
      level: "performance",
      effectAnimationInterval: 200,
      relationAnimationInterval: 200,
      effectAnimationStep: 2,
      relationFlowStep: -3
    }
  };
  return {
    ...t[e] || t.quality,
    ...(i == null ? void 0 : i.performance) || {}
  };
};
var U0 = class {
  constructor({ getDisplayConfig: e, setDisplayConfig: t, restartAnimatedLayers: n }) {
    this.getDisplayConfig = e, this.setDisplayConfig = t, this.restartAnimatedLayers = n;
  }
  getConfig(e = this.getDisplayConfig()) {
    return N0(e);
  }
  applyConfig(e = {}) {
    const t = this.getDisplayConfig() || {};
    return this.setDisplayConfig({
      ...t,
      performance: {
        ...t.performance || {},
        ...e || {}
      }
    }), this.restartAnimatedLayers(), this.getConfig();
  }
};
var G0 = class {
  constructor({ getLayer: e, getRenderer: t, getPerformanceConfig: n, onOffsetChange: s }) {
    this.getLayer = e, this.getRenderer = t, this.getPerformanceConfig = n, this.onOffsetChange = s, this.offset = 0, this.timer = null;
  }
  getOffset() {
    return this.offset;
  }
  isRunning() {
    return !!this.timer;
  }
  start() {
    if (this.timer || !this.getLayer())
      return;
    const e = this.getPerformanceConfig();
    this.timer = window.setInterval(() => {
      var t, n, s, r, o;
      this.offset = (this.offset + Number(e.relationFlowStep || -2)) % 24, (t = this.onOffsetChange) == null || t.call(this, this.offset), (s = (n = this.getRenderer()) == null ? void 0 : n.updateFlowOffset) == null || s.call(n, this.offset), (o = (r = this.getLayer()) == null ? void 0 : r.changed) == null || o.call(r);
    }, Math.max(40, Number(e.relationAnimationInterval) || 80));
  }
  stopIfIdle() {
    var e, t;
    (t = (e = this.getRenderer()) == null ? void 0 : e.hasRelations) != null && t.call(e) || !this.timer || this.stop();
  }
  stop() {
    this.timer && (window.clearInterval(this.timer), this.timer = null);
  }
  reset() {
    var e;
    this.stop(), this.offset = 0, (e = this.onOffsetChange) == null || e.call(this, this.offset);
  }
};
var B0 = (i) => ({
  id: i.id || `${i.type || "relation"}_${i.sourceUID}_${i.targetUID}`,
  type: i.type || "target",
  sourceUID: i.sourceUID ?? i.fromUID ?? i.from,
  targetUID: i.targetUID ?? i.toUID ?? i.to,
  visible: i.visible ?? true,
  style: i.style || {},
  nation: i.nation
});
var z0 = class {
  constructor({
    getSource: e,
    getIconFeatures: t,
    getFeatureKey: n,
    getTargetFeatureKey: s,
    getTargetUID: r,
    getLineCoordinates: o,
    getStyle: a,
    onStartAnimation: l,
    onStopIfIdle: c
  } = {}) {
    this.getSource = e, this.getIconFeatures = t, this.getFeatureKey = n, this.getTargetFeatureKey = s, this.getTargetUID = r, this.getLineCoordinates = o, this.getStyle = a, this.onStartAnimation = l, this.onStopIfIdle = c, this.relationFeatures = /* @__PURE__ */ new Map(), this.targetFeatures = /* @__PURE__ */ new Map();
  }
  hasRelations() {
    return this.relationFeatures.size > 0;
  }
  updateRelation(e, t = {}) {
    var g, y, m, _, p, E;
    const n = (g = this.getSource) == null ? void 0 : g.call(this);
    if (!n)
      return;
    const s = B0(e), r = this.getFeatureKey(s), o = this.relationFeatures.get(r), a = s.type === "target", l = ((y = t.relationLineStyles) == null ? void 0 : y[s.type]) || {}, c = ((m = s.style) == null ? void 0 : m.lineType) || (a ? (_ = t.targetLineStyle) == null ? void 0 : _.lineType : void 0) || l.lineType || "flowDash", u = s.visible !== false && (!a || t.showTargetLine === true) ? this.getLineCoordinates(s) : null;
    if (!u) {
      o && (n.removeFeature(o), this.relationFeatures.delete(r), this.targetFeatures.delete(r), (p = this.onStopIfIdle) == null || p.call(this));
      return;
    }
    const d = new tt(u);
    if (o) {
      o.setGeometry(d), o.set("relation", s), o.set("relationLineType", c), o.setStyle(this.getStyle(s));
      return;
    }
    const f = new ae({
      geometry: d,
      relation: s,
      relationLineType: c,
      relationId: s.id,
      relationType: s.type
    });
    f.setStyle(this.getStyle(s)), this.relationFeatures.set(r, f), a && this.targetFeatures.set(r, f), n.addFeature(f), (E = this.onStartAnimation) == null || E.call(this);
  }
  updateTarget(e, t = {}) {
    const n = this.getTargetUID(e);
    this.updateRelation({
      id: `target_${e.UID}`,
      type: "target",
      sourceUID: e.UID,
      targetUID: n,
      nation: e.nation
    }, t);
  }
  updateTargetsForModel(e, t = {}) {
    this.updateTarget(e, t), this.refreshFromFeatures(String(e.UID), t);
  }
  renderTargets(e = [], t = {}) {
    var r, o;
    const n = (r = this.getSource) == null ? void 0 : r.call(this);
    if (!n)
      return;
    const s = /* @__PURE__ */ new Set();
    e.forEach((a) => {
      s.add(this.getTargetFeatureKey(a)), this.updateTarget(a, t);
    }), Array.from(this.targetFeatures.keys()).forEach((a) => {
      if (!s.has(a)) {
        const l = this.targetFeatures.get(a);
        n.removeFeature(l), this.targetFeatures.delete(a), this.relationFeatures.delete(a);
      }
    }), (o = this.onStopIfIdle) == null || o.call(this);
  }
  renderRelations(e = [], t = {}) {
    var r, o;
    const n = (r = this.getSource) == null ? void 0 : r.call(this);
    if (!n)
      return;
    const s = /* @__PURE__ */ new Set();
    e.forEach((a) => {
      s.add(this.getFeatureKey(a)), this.updateRelation(a, t);
    }), Array.from(this.relationFeatures.entries()).forEach(([a, l]) => {
      (l.get("relation") || {}).type !== "target" && (s.has(a) || (n.removeFeature(l), this.relationFeatures.delete(a)));
    }), (o = this.onStopIfIdle) == null || o.call(this);
  }
  refreshFromFeatures(e = null, t = {}) {
    var s;
    (((s = this.getIconFeatures) == null ? void 0 : s.call(this)) || []).forEach((r) => {
      const o = {
        UID: r.get("UID"),
        nation: r.get("nation"),
        status: r.get("status"),
        lon: r.get("lon"),
        lat: r.get("lat"),
        targetUID: r.get("targetUID")
      };
      e && String(o.UID) !== String(e) && String(o.targetUID) !== String(e) || this.updateTarget(o, t);
    }), this.relationFeatures.forEach((r) => {
      const o = r.get("relation");
      !o || o.type === "target" || e && String(o.sourceUID) !== String(e) && String(o.targetUID) !== String(e) || this.updateRelation(o, t);
    });
  }
  removeByModelUID(e) {
    var s;
    const t = (s = this.getSource) == null ? void 0 : s.call(this);
    if (!t)
      return;
    const n = this.targetFeatures.get(String(e));
    n && (t.removeFeature(n), this.targetFeatures.delete(String(e)), this.relationFeatures.delete(String(e))), Array.from(this.relationFeatures.entries()).forEach(([r, o]) => {
      const a = o.get("relation") || {};
      (String(a.sourceUID) === String(e) || String(a.targetUID) === String(e)) && (t.removeFeature(o), this.relationFeatures.delete(r), this.targetFeatures.delete(r));
    });
  }
  updateFlowOffset(e) {
    this.relationFeatures.forEach((t) => {
      var r;
      if (t.get("relationLineType") !== "flowDash")
        return;
      const n = t.getStyle(), s = (r = n == null ? void 0 : n.getStroke) == null ? void 0 : r.call(n);
      s != null && s.setLineDashOffset ? s.setLineDashOffset(e) : t.setStyle(this.getStyle(t.get("relation")));
    });
  }
  reset() {
    this.relationFeatures = /* @__PURE__ */ new Map(), this.targetFeatures = /* @__PURE__ */ new Map();
  }
};
var tu = (i) => String(
  (i == null ? void 0 : i.id) || `${(i == null ? void 0 : i.type) || "relation"}_${i == null ? void 0 : i.sourceUID}_${i == null ? void 0 : i.targetUID}`
);
var nu = (i) => (i == null ? void 0 : i.targetUID) ?? (i == null ? void 0 : i.target) ?? (i == null ? void 0 : i.targetId) ?? null;
var V0 = (i) => tu({
  id: `target_${i == null ? void 0 : i.UID}`,
  sourceUID: i == null ? void 0 : i.UID,
  targetUID: nu(i),
  type: "target"
});
var X0 = class {
  constructor({ getFeatureById: e }) {
    this.getFeatureById = e;
  }
  getRelationFeatureKey(e) {
    return tu(e);
  }
  getTargetFeatureKey(e) {
    return V0(e);
  }
  getTargetUID(e) {
    return nu(e);
  }
  getRelationLineCoordinates(e) {
    var l, c, h2, u;
    const t = (e == null ? void 0 : e.sourceUID) ?? (e == null ? void 0 : e.fromUID) ?? (e == null ? void 0 : e.from), n = (e == null ? void 0 : e.targetUID) ?? (e == null ? void 0 : e.toUID) ?? (e == null ? void 0 : e.to);
    if (!t || !n)
      return null;
    const s = this.getFeatureById(t), r = this.getFeatureById(n);
    if (!s || !r || s.get("status") === "hidden" || r.get("status") === "hidden")
      return null;
    const o = (c = (l = s.getGeometry()) == null ? void 0 : l.getCoordinates) == null ? void 0 : c.call(l), a = (u = (h2 = r.getGeometry()) == null ? void 0 : h2.getCoordinates) == null ? void 0 : u.call(h2);
    return !o || !a ? null : [o, a];
  }
  getTargetLineCoordinates(e) {
    return this.getRelationLineCoordinates({
      sourceUID: e.UID,
      targetUID: this.getTargetUID(e)
    });
  }
};
var W0 = class {
  constructor({ getDisplayConfig: e, getFeatureById: t, getFlowOffset: n }) {
    this.getDisplayConfig = e, this.getFeatureById = t, this.getFlowOffset = n, this.styleCache = /* @__PURE__ */ new Map();
  }
  getStyle(e = {}) {
    var y, m, _, p, E, S, w, x, b;
    const t = (y = this.getFeatureById) == null ? void 0 : y.call(this, e.sourceUID), n = e.nation || ((m = t == null ? void 0 : t.get) == null ? void 0 : m.call(t, "nation")), s = this.getDisplayConfig() || {}, r = {
      ...((_ = s.relationLineStyles) == null ? void 0 : _[e.type]) || {},
      ...e.type === "target" ? s.targetLineStyle || {} : {},
      ...e.style || {}
    }, a = ((p = r.sideColors) == null ? void 0 : p[n]) || ((E = r.nationColors) == null ? void 0 : E[n]) || (n === "blue" ? r.blueColor : r.redColor) || r.color || "rgba(255, 185, 80, 0.95)", l = Math.max(1, Math.min(10, Number(r.width) || 3)), c = r.lineType || "flowDash", h2 = c === "solid" ? void 0 : [14, 10], u = c === "flowDash" ? this.getFlowOffset() : 0, d = [e.type || "relation", n || "red", a, l, c, JSON.stringify(h2 || [])].join("|"), f = this.styleCache.get(d);
    if (f)
      return (S = f.getStroke()) == null || S.setColor(a), (w = f.getStroke()) == null || w.setWidth(l), (x = f.getStroke()) == null || x.setLineDash(h2), (b = f.getStroke()) == null || b.setLineDashOffset(u), f;
    const g = new te({
      stroke: new se({ color: a, width: l, lineDash: h2, lineDashOffset: u })
    });
    return this.styleCache.set(d, g), g;
  }
};
var Y0 = class {
  constructor({
    getDisplayConfig: e,
    setDisplayConfig: t,
    setMode: n,
    getModelFeatures: s,
    deleteModelFeature: r,
    upsertModel: o,
    renderAnnotations: a,
    renderHeadingIndicators: l,
    renderTargetIndicators: c,
    renderRelationIndicators: h2,
    renderSituationEffects: u,
    refreshModelNameVisibility: d,
    syncAllModelPopups: f
  }) {
    this.getDisplayConfig = e, this.setDisplayConfig = t, this.setMode = n, this.getModelFeatures = s, this.deleteModelFeature = r, this.upsertModel = o, this.renderAnnotations = a, this.renderHeadingIndicators = l, this.renderTargetIndicators = c, this.renderRelationIndicators = h2, this.renderSituationEffects = u, this.refreshModelNameVisibility = d, this.syncAllModelPopups = f;
  }
  async render(e = {}) {
    const t = e.display || this.getDisplayConfig();
    this.setDisplayConfig(t), this.setMode(e.mode || "edit", e.permissions || {}), this.removeStaleModelFeatures(e.models || []);
    for (const n of e.models || [])
      await this.upsertModel(n, { skipIndicators: true, deferPopup: true });
    this.renderAnnotations(e.annotations || []), this.renderHeadingIndicators(e.models || [], e.display || {}), this.renderTargetIndicators(e.models || [], e.display || {}), this.renderRelationIndicators(e.relations || [], e.display || {}), this.renderSituationEffects(e.effects || [], e.display || {}), this.refreshModelNameVisibility(), this.syncAllModelPopups();
  }
  removeStaleModelFeatures(e = []) {
    const t = new Set(e.map((n) => String(n.UID)));
    this.getModelFeatures().forEach((n) => {
      t.has(String(n.get("UID"))) || this.deleteModelFeature(n.get("UID"), { showEffect: false });
    });
  }
};
var ei = (i) => JSON.parse(JSON.stringify(i));
var j0 = class {
  constructor({
    hasMap: e,
    syncScene: t,
    syncSituationChanges: n,
    setDisplayConfig: s,
    refreshDisplayLayers: r,
    setMode: o,
    store: a
  } = {}) {
    this.hasMap = e, this.syncScene = t, this.syncSituationChanges = n, this.setDisplayConfig = s, this.refreshDisplayLayers = r, this.setMode = o, this.store = a || u_;
  }
  getSceneState() {
    return this.store.sceneState;
  }
  getSceneSnapshot() {
    return this.store.exportScenePayload();
  }
  getTemplates() {
    return ei(this.store.modelList.value);
  }
  getHistoryState() {
    return this.store.historyState;
  }
  getStatistics() {
    return ei(this.store.sceneStatistics.value);
  }
  getModelsBySide(e) {
    return this.store.sceneState.models.filter((t) => t.nation === e);
  }
  validateScene() {
    return this.store.validateScene();
  }
  createModelFromTemplate(e, t = {}) {
    return this.store.createModelFromTemplate(e, t);
  }
  async syncCurrentScene() {
    var e;
    return (e = this.hasMap) != null && e.call(this) ? (await this.syncScene(this.store.sceneState), this.store.sceneState) : this.store.sceneState;
  }
  async loadScene(e, t = {}) {
    return this.store.loadSceneData(e, t.label || "Load Scene"), t.sync !== false && await this.syncCurrentScene(), this.getSceneSnapshot();
  }
  async resetScene(e = {}) {
    return this.store.resetScene(), e.sync !== false && await this.syncCurrentScene(), this.getSceneSnapshot();
  }
  async addModel(e, t = {}) {
    const n = this.store.addModelToScene(e, {
      recordHistory: t.recordHistory !== false
    });
    return t.sync !== false && await this.syncSituationChanges({ models: [n] }), n;
  }
  async updateModel(e, t, n = {}) {
    const s = this.store.updateModelInScene(e, t, {
      recordHistory: n.recordHistory !== false
    });
    return s ? (n.sync !== false && await this.syncSituationChanges({ models: [s] }), s) : null;
  }
  async batchUpdateModels(e = [], t = {}) {
    const n = this.store.batchUpdateModelsInScene(e, {
      recordHistory: t.recordHistory !== false,
      label: t.label || "Batch Update Models"
    });
    return n.length && t.sync !== false && await this.syncSituationChanges({ models: n }), n;
  }
  async upsertModels(e = [], t = {}) {
    const n = Array.isArray(e) ? e : [e], s = [];
    return n.filter(Boolean).forEach((r) => {
      const o = this.store.sceneState.models.find((l) => String(l.UID) === String(r.UID)), a = o ? this.store.updateModelInScene(o.UID, r, { recordHistory: false }) : this.store.addModelToScene(r, { recordHistory: false });
      a && s.push(a);
    }), s.length && t.sync !== false && await this.syncSituationChanges({ models: s }), s;
  }
  async removeModel(e, t = {}) {
    const n = this.store.removeModelFromScene(e, {
      recordHistory: t.recordHistory !== false
    });
    return n && t.sync !== false && await this.syncSituationChanges({ removedModels: [n] }), n;
  }
  async removeModels(e = [], t = {}) {
    const n = Array.isArray(e) ? e : [e], s = new Set(n.map((o) => String(o))), r = this.store.sceneState.models.filter((o) => s.has(String(o.UID)));
    return this.store.removeModelsFromScene(n, { recordHistory: t.recordHistory !== false }), r.length && t.sync !== false && await this.syncSituationChanges({ removedModels: r }), r;
  }
  async upsertRelations(e = [], t = {}) {
    const n = this.store.upsertRelationsInScene(e);
    return n.length && t.sync !== false && await this.syncSituationChanges({ relations: n }), n;
  }
  async removeRelations(e = [], t = {}) {
    const n = this.store.removeRelationsFromScene(e);
    return n.length && t.sync !== false && await this.syncSituationChanges({ removedRelations: n }), n;
  }
  async upsertEffects(e = [], t = {}) {
    const n = this.store.upsertEffectsInScene(e);
    return n.length && t.sync !== false && await this.syncSituationChanges({ effects: n }), n;
  }
  async removeEffects(e = [], t = {}) {
    const n = this.store.removeEffectsFromScene(e);
    return n.length && t.sync !== false && await this.syncSituationChanges({ removedEffects: n }), n;
  }
  async setDisplay(e = {}, t = {}) {
    var n;
    return this.store.sceneState.display = O0(this.store.sceneState.display, e), this.setDisplayConfig(ei(this.store.sceneState.display)), (n = this.hasMap) != null && n.call(this) && t.sync !== false && this.refreshDisplayLayers(this.store.sceneState), this.store.sceneState.display;
  }
  async setPerformance(e = {}, t = {}) {
    const n = {
      ...this.store.sceneState.display.performance || {},
      ...ei(e)
    };
    return await this.setDisplay({ performance: n }, t), this.store.sceneState.display.performance;
  }
  setModeState(e) {
    const t = this.store.setSceneMode(e);
    return this.setMode(t, this.store.sceneState.permissions), t;
  }
  setMetadata(e = {}) {
    return this.store.sceneState.metadata = {
      ...this.store.sceneState.metadata,
      ...ei(e),
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    }, this.store.sceneState.metadata;
  }
  setPlaybackConfig(e = {}) {
    return this.store.sceneState.playback = {
      ...this.store.sceneState.playback,
      ...ei(e)
    }, this.store.sceneState.playback;
  }
  setSelectedModelUID(e) {
    this.store.setSelectedModelUID(e);
  }
  setSelectedAnnotationId(e) {
    this.store.setSelectedAnnotationId(e);
  }
  addMountToModel(e, t, n = {}) {
    return this.store.addMountToModel(e, t, n);
  }
  removeMountFromModel(e, t, n = {}) {
    return this.store.removeMountFromModel(e, t, n);
  }
  setModelRoute(e, t, n = {}, s = {}) {
    return this.store.setModelRoute(e, t, n, s);
  }
  undo() {
    return this.store.undoSceneChange();
  }
  redo() {
    return this.store.redoSceneChange();
  }
};
var $0 = class {
  constructor({ getFeatures: e, applyFeatureSelectionStyle: t, syncAllModelPopups: n, onSelectionChange: s }) {
    this.getFeatures = e, this.applyFeatureSelectionStyle = t, this.syncAllModelPopups = n, this.onSelectionChange = s, this.selected = null, this.batchSelectedUIDs = /* @__PURE__ */ new Set();
  }
  getSelectedFeature() {
    return this.selected;
  }
  getBatchSelectedUIDs() {
    return Array.from(this.batchSelectedUIDs);
  }
  isSelected(e) {
    return !!e && this.selected === e;
  }
  isBatchSelected(e) {
    return this.batchSelectedUIDs.has(String(e));
  }
  setBatchSelection(e = []) {
    var s;
    const t = new Set((e || []).map((r) => String(r)));
    this.batchSelectedUIDs = t, (((s = this.getFeatures) == null ? void 0 : s.call(this)) || []).forEach((r) => {
      var o;
      r.set("isBatchSelected", t.has(String(r.get("UID")))), (o = this.applyFeatureSelectionStyle) == null || o.call(this, r);
    });
  }
  selectFeature(e, t = false) {
    var n, s, r, o;
    if (!e) {
      this.clearSelection();
      return;
    }
    if (t && this.selected === e) {
      this.clearSelection();
      return;
    }
    this.selected && (this.selected.set("isSelected", false), (n = this.applyFeatureSelectionStyle) == null || n.call(this, this.selected)), this.selected = e, e.set("isSelected", true), (s = this.applyFeatureSelectionStyle) == null || s.call(this, e), (r = this.syncAllModelPopups) == null || r.call(this), (o = this.onSelectionChange) == null || o.call(this, e);
  }
  clearSelection() {
    var e, t, n;
    this.selected && (this.selected.set("isSelected", false), (e = this.applyFeatureSelectionStyle) == null || e.call(this, this.selected), this.selected = null, (t = this.syncAllModelPopups) == null || t.call(this), (n = this.onSelectionChange) == null || n.call(this, null));
  }
  removeFeature(e, t) {
    var n;
    this.selected === e && (e.set("isSelected", false), this.selected = null, (n = this.onSelectionChange) == null || n.call(this, null)), this.batchSelectedUIDs.delete(String(t));
  }
  reset() {
    this.selected = null, this.batchSelectedUIDs = /* @__PURE__ */ new Set();
  }
};
var K0 = class {
  constructor({
    getSource: e,
    getFeatureKey: t,
    createGeometry: n,
    createStyle: s,
    createExtraFeatures: r,
    getAnimation: o,
    onStartAnimation: a
  } = {}) {
    this.getSource = e, this.getFeatureKey = t, this.createGeometry = n, this.createStyle = s, this.createExtraFeatures = r, this.getAnimation = o, this.onStartAnimation = a, this.features = /* @__PURE__ */ new Map();
  }
  update(e, t = {}) {
    var f, g;
    const n = (f = this.getSource) == null ? void 0 : f.call(this);
    if (!n)
      return false;
    const s = this.getFeatureKey(e), r = this.features.get(s), o = t.effectVisible || {}, l = e.visible !== false && o[e.type] !== false ? this.createGeometry(e) : null;
    if (!l)
      return r && (r.forEach((y) => n.removeFeature(y)), this.features.delete(s)), false;
    const c = this.getAnimation(e), h2 = r || [], u = h2[0] || new ae({ geometry: l });
    u.setGeometry(l), u.set("effect", e), u.setStyle(this.createStyle(e));
    const d = [
      u,
      ...this.createExtraFeatures(e, h2.slice(1))
    ];
    return r ? h2.length > d.length ? h2.slice(d.length).forEach((y) => n.removeFeature(y)) : d.length > h2.length && d.slice(h2.length).forEach((y) => n.addFeature(y)) : d.forEach((y) => n.addFeature(y)), this.features.set(s, d), c && c !== "none" && ((g = this.onStartAnimation) == null || g.call(this)), true;
  }
  render(e = [], t = {}) {
    var r;
    const n = (r = this.getSource) == null ? void 0 : r.call(this);
    if (!n)
      return;
    const s = /* @__PURE__ */ new Set();
    e.forEach((o) => {
      s.add(this.getFeatureKey(o)), this.update(o, t);
    }), Array.from(this.features.entries()).forEach(([o, a]) => {
      s.has(o) || (a.forEach((l) => n.removeFeature(l)), this.features.delete(o));
    });
  }
  refresh(e = null, t = {}) {
    this.features.forEach((n) => {
      var r;
      const s = (r = n[0]) == null ? void 0 : r.get("effect");
      s && (e && String(s.sourceUID) !== String(e) || this.update(s, t));
    });
  }
  removeBySourceUID(e) {
    var n;
    const t = (n = this.getSource) == null ? void 0 : n.call(this);
    t && Array.from(this.features.entries()).forEach(([s, r]) => {
      var a;
      const o = ((a = r[0]) == null ? void 0 : a.get("effect")) || {};
      String(o.sourceUID) === String(e) && (r.forEach((l) => t.removeFeature(l)), this.features.delete(s));
    });
  }
  hasAnimatedEffects() {
    return Array.from(this.features.values()).some((e) => {
      var s;
      const t = (s = e[0]) == null ? void 0 : s.get("effect"), n = this.getAnimation(t);
      return n && n !== "none";
    });
  }
  reset() {
    this.features = /* @__PURE__ */ new Map();
  }
};
var iu = ({ text: i, offsetY: e, style: t }) => i ? new zn({
  text: i,
  offsetY: e,
  fill: new J({ color: t.labelColor || "#f8fbff" }),
  stroke: new se({
    color: t.labelStrokeColor || "rgba(0,0,0,0.65)",
    width: Number(t.labelStrokeWidth) || 3
  })
}) : void 0;
var H0 = (i, e, t) => {
  var u;
  const n = e.quality || i.quality || ((u = i.meta) == null ? void 0 : u.quality) || "good", s = {
    good: "rgba(80, 255, 150, 0.95)",
    weak: "rgba(255, 210, 80, 0.95)",
    broken: "rgba(255, 80, 80, 0.95)",
    ...e.qualityColors || {}
  }[n], r = i.type === "missileFlight", o = i.type === "intercept", a = i.type === "miss", l = r ? e.missileColor || "rgba(255, 185, 72, 0.95)" : o ? e.interceptColor || "rgba(255, 255, 255, 0.96)" : a ? e.missColor || "rgba(200, 200, 200, 0.9)" : s || t.strokeColor, c = Math.max(1, Number(e.width) || (r ? 4.5 : o ? 4 : a ? 2.5 : 3)), h2 = r ? [18, 10] : o ? [10, 8] : n === "broken" ? [4, 10] : e.lineDash || [12, 8];
  return { lineColor: l, width: c, lineDash: h2 };
};
var Z0 = ({ effect: i, style: e, defaults: t, animationOffset: n }) => {
  const { lineColor: s, width: r, lineDash: o } = H0(i, e, t);
  return new te({
    stroke: new se({
      color: e.strokeColor || s,
      width: r,
      lineDash: o,
      lineDashOffset: -n
    }),
    text: iu({ text: i.label, offsetY: -10, style: e })
  });
};
var q0 = ({ effect: i, style: e, defaults: t, animationOffset: n, statusText: s }) => {
  const r = e.animation || "none", o = r === "pulse" ? 1 + n % 20 / 6 : 1, a = i.label || s;
  return new te({
    fill: new J({ color: e.fillColor || e.color || t.color }),
    stroke: new se({
      color: e.strokeColor || e.color || t.strokeColor,
      width: Math.max(1, Number(e.width) || 2) + o,
      lineDash: e.lineDash || (r === "pulse" ? [10, 8] : void 0),
      lineDashOffset: r === "pulse" ? -n : 0
    }),
    text: iu({ text: a, offsetY: -12, style: e })
  });
};
var J0 = ({ effect: i, style: e, defaults: t, animationOffset: n, statusText: s }) => {
  var r;
  return ((r = i.geometry) == null ? void 0 : r.shape) === "line" ? Z0({ effect: i, style: e, defaults: t, animationOffset: n }) : q0({ effect: i, style: e, defaults: t, animationOffset: n, statusText: s });
};
var nc = {
  radar: {
    color: "rgba(64, 200, 255, 0.18)",
    strokeColor: "rgba(64, 200, 255, 0.85)",
    animation: "scan"
  },
  detect: {
    color: "rgba(76, 217, 100, 0.14)",
    strokeColor: "rgba(76, 217, 100, 0.82)",
    animation: "pulse"
  },
  warning: {
    color: "rgba(255, 82, 82, 0.16)",
    strokeColor: "rgba(255, 82, 82, 0.9)",
    animation: "pulse"
  },
  jammer: {
    color: "rgba(178, 93, 255, 0.16)",
    strokeColor: "rgba(210, 130, 255, 0.9)",
    animation: "pulse"
  },
  weapon: {
    color: "rgba(255, 185, 80, 0.12)",
    strokeColor: "rgba(255, 185, 80, 0.85)",
    animation: "none"
  },
  lock: {
    color: "rgba(255, 64, 64, 0.08)",
    strokeColor: "rgba(255, 64, 64, 0.95)",
    animation: "pulse"
  },
  linkQuality: {
    color: "rgba(80, 255, 150, 0.18)",
    strokeColor: "rgba(80, 255, 150, 0.95)",
    animation: "flow"
  },
  missileFlight: {
    color: "rgba(255, 180, 64, 0.18)",
    strokeColor: "rgba(255, 180, 64, 0.95)",
    animation: "flow"
  },
  damaged: {
    color: "rgba(255, 80, 40, 0.12)",
    strokeColor: "rgba(255, 80, 40, 0.95)",
    animation: "pulse"
  },
  lost: {
    color: "rgba(160, 160, 160, 0.12)",
    strokeColor: "rgba(210, 210, 210, 0.85)",
    animation: "pulse"
  },
  discovered: {
    color: "rgba(90, 220, 255, 0.14)",
    strokeColor: "rgba(90, 220, 255, 0.95)",
    animation: "pulse"
  },
  tracking: {
    color: "rgba(80, 255, 150, 0.12)",
    strokeColor: "rgba(80, 255, 150, 0.95)",
    animation: "pulse"
  },
  sensorSearch: {
    color: "rgba(64, 200, 255, 0.12)",
    strokeColor: "rgba(64, 200, 255, 0.9)",
    animation: "scan"
  },
  sensorOff: {
    color: "rgba(128, 128, 128, 0.08)",
    strokeColor: "rgba(180, 180, 180, 0.7)",
    animation: "none"
  },
  sensorSuppressed: {
    color: "rgba(180, 80, 255, 0.16)",
    strokeColor: "rgba(220, 120, 255, 0.95)",
    animation: "pulse"
  },
  intercept: {
    color: "rgba(255, 255, 255, 0.16)",
    strokeColor: "rgba(255, 255, 255, 0.95)",
    animation: "flow"
  },
  miss: {
    color: "rgba(170, 170, 170, 0.12)",
    strokeColor: "rgba(210, 210, 210, 0.85)",
    animation: "flow"
  },
  overlap: {
    color: "rgba(255, 235, 80, 0.2)",
    strokeColor: "rgba(255, 235, 80, 0.95)",
    animation: "pulse"
  }
};
var Q0 = {
  damaged: "Damaged",
  lost: "Lost",
  lock: "Locked",
  discovered: "Discovered",
  tracking: "Tracking",
  sensorSearch: "Sensor Search",
  sensorOff: "Sensor Off",
  sensorSuppressed: "Sensor Suppressed",
  overlap: "Overlap"
};
var su = (i = "radar") => nc[i] || nc.radar;
var dr = (i = {}, e = {}) => ({
  ...i,
  ...e,
  qualityColors: {
    ...i.qualityColors || {},
    ...e.qualityColors || {}
  }
});
var ru = (i = "radar", e = {}) => {
  const t = (e == null ? void 0 : e.effectStyles) || {}, n = su(i), s = dr(n, t.default || {});
  return dr(s, t[i] || {});
};
var eS = (i = {}, e = {}) => dr(ru(i.type, e), i.style || {});
var fr = (i, e, t, n, s = 64) => {
  const r = Number(t) || 0, a = (Number(n) || 360) - r, l = Math.max(8, Math.min(128, s)), c = [i];
  for (let h2 = 0; h2 <= l; h2 += 1) {
    const u = (r + a * h2 / l) * Math.PI / 180;
    c.push([
      i[0] + Math.sin(u) * e,
      i[1] + Math.cos(u) * e
    ]);
  }
  return c.push(i), c;
};
var ou = (i, e) => {
  const t = e / 2;
  return [
    [i[0] - t, i[1] - t],
    [i[0] + t, i[1] - t],
    [i[0] + t, i[1] + t],
    [i[0] - t, i[1] + t],
    [i[0] - t, i[1] - t]
  ];
};
var tS = (i) => Q0[i] || "";
var au = (i = {}, { getFeatureById: e, coordinateFromLonLat: t } = {}) => {
  var s, r, o, a;
  const n = e == null ? void 0 : e(i.sourceUID);
  return ((s = i.geometry) == null ? void 0 : s.lon) !== void 0 && ((r = i.geometry) == null ? void 0 : r.lat) !== void 0 ? (t == null ? void 0 : t(i.geometry.lon, i.geometry.lat)) || null : !n || n.get("status") === "hidden" ? null : ((a = (o = n.getGeometry()) == null ? void 0 : o.getCoordinates) == null ? void 0 : a.call(o)) || null;
};
var nS = (i = {}, {
  getFeatureById: e,
  coordinateFromLonLat: t,
  getResolution: n
} = {}) => {
  var c, h2, u;
  const s = e == null ? void 0 : e(i.sourceUID);
  if ((s == null ? void 0 : s.get("status")) === "hidden")
    return null;
  const r = au(i, { getFeatureById: e, coordinateFromLonLat: t });
  if (!r)
    return null;
  const o = i.geometry || {}, a = Math.max(1, Number(o.radius || o.range || i.radius || 5e4)), l = o.shape || (i.type === "radar" ? "sector" : "circle");
  if (l === "line") {
    const d = e == null ? void 0 : e(i.targetUID || o.targetUID), f = (u = (h2 = (c = d == null ? void 0 : d.getGeometry) == null ? void 0 : c.call(d)) == null ? void 0 : h2.getCoordinates) == null ? void 0 : u.call(h2);
    return f ? new tt([r, f]) : null;
  }
  if (l === "box") {
    const d = Number(n == null ? void 0 : n()) || 1, f = Number(o.size || 56) * d;
    return new at([ou(r, f)]);
  }
  if (l === "sector") {
    const d = Number(s.get("heading") || 0), f = Number(o.direction ?? d), g = Number(o.angle ?? o.width ?? 90), y = o.startAngle ?? f - g / 2, m = o.endAngle ?? f + g / 2;
    return new at([fr(r, a, y, m)]);
  }
  return new at([fr(r, a, 0, 360, 96)]);
};
var iS = (i = {}, { getFeatureById: e } = {}) => {
  var o, a, l, c, h2, u, d;
  const t = e == null ? void 0 : e(i.sourceUID), n = e == null ? void 0 : e(i.targetUID || ((o = i.geometry) == null ? void 0 : o.targetUID)), s = (c = (l = (a = t == null ? void 0 : t.getGeometry) == null ? void 0 : a.call(t)) == null ? void 0 : l.getCoordinates) == null ? void 0 : c.call(l), r = (d = (u = (h2 = n == null ? void 0 : n.getGeometry) == null ? void 0 : h2.call(n)) == null ? void 0 : u.getCoordinates) == null ? void 0 : d.call(u);
  return !s || !r ? null : { start: s, end: r };
};
var lu = (i, e = 0.5, t = [0, 0]) => {
  if (!i)
    return null;
  const [n, s] = i.start, [r, o] = i.end;
  return [
    n + (r - n) * e + t[0],
    s + (o - s) * e + t[1]
  ];
};
var cu = ({
  effect: i = {},
  feature: e = null,
  endpoints: t = null,
  animationOffset: n = 0,
  style: s = {}
} = {}) => {
  var g;
  if (!t)
    return null;
  const r = Math.max(0.15, Math.min(0.9, n % 60 / 60)), o = s.quality || i.quality || ((g = i.meta) == null ? void 0 : g.quality) || "good", a = {
    good: "rgba(80, 255, 150, 1)",
    weak: "rgba(255, 215, 90, 1)",
    broken: "rgba(255, 90, 90, 1)",
    ...s.qualityColors || {}
  }, l = i.type === "missileFlight" ? s.headColor || s.missileHeadColor || "rgba(255, 192, 80, 1)" : o === "weak" ? s.headColor || a.weak : o === "broken" ? s.headColor || a.broken : i.type === "intercept" ? s.headColor || s.interceptHeadColor || "rgba(255, 255, 255, 1)" : s.headColor || a.good, c = new he(lu(t, r)), h2 = i.type === "missileFlight", u = i.type === "intercept", d = h2 || u ? new fs({
    points: h2 ? 3 : 4,
    radius: h2 ? 10 : 8,
    radius2: h2 ? 4 : 0,
    angle: h2 ? Math.PI / 2 : Math.PI / 4,
    fill: new J({ color: l }),
    stroke: new se({ color: s.headStrokeColor || "rgba(255,255,255,0.95)", width: Number(s.headStrokeWidth) || 1.5 })
  }) : new we({
    radius: Number(s.headRadius) || (o === "weak" ? 6 : 7),
    fill: new J({ color: l }),
    stroke: new se({ color: s.headStrokeColor || "rgba(255,255,255,0.9)", width: Number(s.headStrokeWidth) || 1.5 })
  }), f = e || new ae({ geometry: c });
  return f.setGeometry(c), f.set("effect", i), f.setStyle(new te({
    image: d,
    text: h2 && i.label ? new zn({
      text: i.label,
      offsetY: -18,
      fill: new J({ color: s.labelColor || "#fff6d5" }),
      stroke: new se({ color: s.labelStrokeColor || "rgba(0,0,0,0.7)", width: Number(s.labelStrokeWidth) || 3 })
    }) : void 0
  })), f;
};
var No = ({
  effect: i = {},
  mode: e = "intercept",
  feature: t = null,
  endpoints: n = null,
  style: s = {}
} = {}) => {
  if (!n)
    return null;
  const r = e === "miss" ? [
    (n.end[1] - n.start[1]) * 0.05,
    (n.start[0] - n.end[0]) * 0.05
  ] : [0, 0], o = e === "miss" ? lu(n, 1.05, r) : n.end, a = e === "miss" ? s.missPointColor || "rgba(180, 180, 180, 0.95)" : s.hitPointColor || "rgba(255, 210, 90, 0.98)", l = Number(s.eventPointRadius) || (e === "miss" ? 12 : 14), c = t || new ae({ geometry: new he(o) });
  return c.setGeometry(new he(o)), c.set("effect", i), c.setStyle(new te({
    image: new fs({
      points: e === "miss" ? 5 : 6,
      radius: l,
      radius2: e === "miss" ? l / 2 : l / 2.4,
      angle: Math.PI / 6,
      fill: new J({ color: a }),
      stroke: new se({ color: s.eventPointStrokeColor || "rgba(255,255,255,0.95)", width: Number(s.eventPointStrokeWidth) || 2 })
    })
  })), c;
};
var hu = ({
  effect: i = {},
  feature: e = null,
  center: t = null,
  style: n = {},
  defaults: s = {},
  statusText: r = ""
} = {}) => {
  var f, g;
  if (!t)
    return null;
  const o = i.type, a = {
    damaged: "rgba(255, 80, 40, 0.98)",
    lost: "rgba(180, 180, 180, 0.95)",
    lock: "rgba(255, 64, 64, 0.98)",
    discovered: "rgba(90, 220, 255, 0.98)",
    tracking: "rgba(80, 255, 150, 0.98)",
    sensorSearch: "rgba(64, 200, 255, 0.98)",
    sensorOff: "rgba(160, 160, 160, 0.95)",
    sensorSuppressed: "rgba(210, 120, 255, 0.98)",
    overlap: "rgba(255, 235, 80, 0.98)"
  }, l = e || new ae({ geometry: new he(t) });
  l.setGeometry(new he(t)), l.set("effect", i);
  const c = o === "lock", h2 = o === "lost", u = n.badgeColor || a[o] || n.strokeColor || s.strokeColor, d = ((g = (f = u.match(/\d+/g)) == null ? void 0 : f.slice(0, 3)) == null ? void 0 : g.join(",")) || "255,255,255";
  return l.setStyle(new te({
    image: c ? new fs({
      points: 4,
      radius: 14,
      radius2: 6,
      angle: Math.PI / 4,
      fill: new J({ color: n.badgeFillColor || "rgba(255,64,64,0.14)" }),
      stroke: new se({ color: u, width: Number(n.badgeStrokeWidth) || 2 })
    }) : new we({
      radius: Number(n.badgeRadius) || (h2 ? 8 : 10),
      fill: new J({ color: n.badgeFillColor || `rgba(${d},0.24)` }),
      stroke: new se({ color: u, width: Number(n.badgeStrokeWidth) || 2 })
    }),
    text: new zn({
      text: i.label || r,
      offsetY: Number(n.badgeLabelOffsetY) || -18,
      fill: new J({ color: n.labelColor || "#fff" }),
      stroke: new se({ color: n.labelStrokeColor || "rgba(0,0,0,0.7)", width: Number(n.labelStrokeWidth) || 3 })
    })
  })), l;
};
var uu = ({
  effect: i = {},
  feature: e = null,
  center: t = null,
  style: n = {},
  defaults: s = {},
  animationOffset: r = 0
} = {}) => {
  if (!t)
    return null;
  const o = 5 + r % 18, a = Math.max(0.1, 1 - r % 18 / 18), l = e || new ae({ geometry: new he(t) });
  return l.setGeometry(new he(t)), l.set("effect", i), l.setStyle(new te({
    image: new we({
      radius: o,
      fill: new J({ color: n.flashColor || `rgba(90, 220, 255, ${0.22 * a})` }),
      stroke: new se({ color: n.strokeColor || s.strokeColor, width: 2 })
    })
  })), l;
};
var du = ({
  effect: i = {},
  feature: e = null,
  center: t = null,
  heading: n = 0,
  style: s = {},
  defaults: r = {},
  animationOffset: o = 0
} = {}) => {
  if (!t)
    return null;
  const a = i.geometry || {}, l = Math.max(1, Number(a.radius || a.range || i.radius || 5e4)), c = Number(a.direction ?? n) + o * 3, h2 = Number(a.scanWidth || 18), u = new at([
    fr(t, l, c - h2 / 2, c + h2 / 2, 12)
  ]), d = e || new ae({ geometry: u });
  return d.setGeometry(u), d.setStyle(new te({
    fill: new J({ color: s.scanColor || "rgba(255,255,255,0.16)" }),
    stroke: new se({ color: s.scanStrokeColor || s.strokeColor || r.strokeColor, width: 1 })
  })), d;
};
var sS = [
  "damaged",
  "lost",
  "lock",
  "discovered",
  "tracking",
  "sensorSearch",
  "sensorOff",
  "sensorSuppressed",
  "overlap"
];
var rS = ({
  effect: i = {},
  reusableFeatures: e = [],
  animation: t = "none",
  animationOffset: n = 0,
  getLineEndpoints: s,
  getEffectCenter: r,
  getSourceFeature: o,
  getEffectStyle: a,
  getEffectDefaults: l,
  getStatusEffectText: c
} = {}) => {
  var y, m, _, p;
  const h2 = [];
  let u = 0;
  const d = () => e[u++] || null, f = (a == null ? void 0 : a(i)) || {}, g = (l == null ? void 0 : l(i.type)) || {};
  if (((y = i.geometry) == null ? void 0 : y.shape) === "line") {
    const E = s == null ? void 0 : s(i);
    if (["missileFlight", "intercept", "linkQuality"].includes(i.type)) {
      const S = cu({
        effect: i,
        feature: d(),
        endpoints: E,
        animationOffset: n,
        style: f
      });
      S && h2.push(S);
    }
    if (i.type === "intercept") {
      const S = No({
        effect: i,
        mode: "intercept",
        feature: d(),
        endpoints: E,
        style: f
      });
      S && h2.push(S);
    }
    if (i.type === "miss") {
      const S = No({
        effect: i,
        mode: "miss",
        feature: d(),
        endpoints: E,
        style: f
      });
      S && h2.push(S);
    }
  }
  if (t === "scan") {
    const E = o == null ? void 0 : o(i), S = du({
      effect: i,
      feature: d(),
      center: (p = (_ = (m = E == null ? void 0 : E.getGeometry) == null ? void 0 : m.call(E)) == null ? void 0 : _.getCoordinates) == null ? void 0 : p.call(_),
      heading: Number((E == null ? void 0 : E.get("heading")) || 0),
      style: f,
      defaults: g,
      animationOffset: n
    });
    S && h2.push(S);
  }
  if (i.type === "discovered") {
    const E = uu({
      effect: i,
      feature: d(),
      center: r == null ? void 0 : r(i),
      style: f,
      defaults: g,
      animationOffset: n
    });
    E && h2.push(E);
  }
  if (sS.includes(i.type)) {
    const E = hu({
      effect: i,
      feature: d(),
      center: r == null ? void 0 : r(i),
      style: f,
      defaults: g,
      statusText: (c == null ? void 0 : c(i.type)) || ""
    });
    E && h2.push(E);
  }
  return h2;
};
var oS = class {
  constructor({
    getSource: e,
    getFeatureById: t,
    coordinateFromLonLat: n,
    getResolution: s,
    getDisplayConfig: r,
    getPerformanceConfig: o,
    getLayer: a
  }) {
    this.getSource = e, this.getFeatureById = t, this.coordinateFromLonLat = n, this.getResolution = s, this.getDisplayConfig = r, this.getPerformanceConfig = o, this.getLayer = a, this.animationOffset = 0, this.animationTimer = null, this.renderer = new K0({
      getSource: e,
      getFeatureKey: (l) => this.getFeatureKey(l),
      createGeometry: (l) => this.createGeometry(l),
      createStyle: (l) => this.createStyle(l),
      createExtraFeatures: (l, c) => this.createExtraFeatures(l, c),
      getAnimation: (l) => this.getStyle(l).animation || this.getDefaults(l == null ? void 0 : l.type).animation,
      onStartAnimation: () => this.startAnimation()
    });
  }
  getFeatureKey(e) {
    return String((e == null ? void 0 : e.id) || `${(e == null ? void 0 : e.type) || "effect"}_${e == null ? void 0 : e.sourceUID}`);
  }
  getBuiltInDefaults(e = "radar") {
    return su(e);
  }
  mergeStyle(e = {}, t = {}) {
    return dr(e, t);
  }
  getDefaults(e = "radar") {
    return ru(e, this.getDisplayConfig());
  }
  getStyle(e = {}) {
    return eS(e, this.getDisplayConfig());
  }
  createSectorCoordinates(e, t, n, s, r = 64) {
    return fr(e, t, n, s, r);
  }
  createBoxCoordinates(e, t) {
    return ou(e, t);
  }
  getStatusText(e) {
    return tS(e);
  }
  getCenter(e = {}) {
    return au(e, {
      getFeatureById: (t) => this.getFeatureById(t),
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n)
    });
  }
  createGeometry(e = {}) {
    return nS(e, {
      getFeatureById: (t) => this.getFeatureById(t),
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n),
      getResolution: () => this.getResolution()
    });
  }
  createLineStyle(e = {}) {
    return this.createStyle(e);
  }
  getLineEndpoints(e = {}) {
    return iS(e, {
      getFeatureById: (t) => this.getFeatureById(t)
    });
  }
  createLineMovingMarkerFeature(e = {}, t = null) {
    return cu({
      effect: e,
      feature: t,
      endpoints: this.getLineEndpoints(e),
      animationOffset: this.animationOffset,
      style: this.getStyle(e)
    });
  }
  createLineEventFeature(e = {}, t = "intercept", n = null) {
    return No({
      effect: e,
      mode: t,
      feature: n,
      endpoints: this.getLineEndpoints(e),
      style: this.getStyle(e)
    });
  }
  createStatusMarkerFeature(e = {}, t = null) {
    return hu({
      effect: e,
      feature: t,
      center: this.getCenter(e),
      style: this.getStyle(e),
      defaults: this.getDefaults(e.type),
      statusText: this.getStatusText(e.type)
    });
  }
  createStyle(e = {}) {
    return J0({
      effect: e,
      style: this.getStyle(e),
      defaults: this.getDefaults(e.type),
      animationOffset: this.animationOffset,
      statusText: this.getStatusText(e.type)
    });
  }
  createDiscoveredFlashFeature(e = {}, t = null) {
    return uu({
      effect: e,
      feature: t,
      center: this.getCenter(e),
      style: this.getStyle(e),
      defaults: this.getDefaults(e.type),
      animationOffset: this.animationOffset
    });
  }
  createExtraFeatures(e = {}, t = []) {
    const n = this.getStyle(e).animation || this.getDefaults(e.type).animation;
    return rS({
      effect: e,
      reusableFeatures: t,
      animation: n,
      animationOffset: this.animationOffset,
      getLineEndpoints: (s) => this.getLineEndpoints(s),
      getEffectCenter: (s) => this.getCenter(s),
      getSourceFeature: (s) => this.getFeatureById(s.sourceUID),
      getEffectStyle: (s) => this.getStyle(s),
      getEffectDefaults: (s) => this.getDefaults(s),
      getStatusEffectText: (s) => this.getStatusText(s)
    });
  }
  createScanFeature(e = {}, t = null) {
    var s, r, o;
    const n = this.getFeatureById(e.sourceUID);
    return du({
      effect: e,
      feature: t,
      center: (o = (r = (s = n == null ? void 0 : n.getGeometry) == null ? void 0 : s.call(n)) == null ? void 0 : r.getCoordinates) == null ? void 0 : o.call(r),
      heading: Number((n == null ? void 0 : n.get("heading")) || 0),
      style: this.getStyle(e),
      defaults: this.getDefaults(e.type),
      animationOffset: this.animationOffset
    });
  }
  update(e, t = this.getDisplayConfig()) {
    this.renderer.update(e, t) || this.stopAnimationIfIdle();
  }
  render(e = [], t = this.getDisplayConfig()) {
    this.renderer.render(e, t), this.stopAnimationIfIdle();
  }
  refresh(e = null, t = this.getDisplayConfig()) {
    this.renderer.refresh(e, t);
  }
  removeBySourceUID(e) {
    this.renderer.removeBySourceUID(e);
  }
  hasAnimatedEffects() {
    return this.renderer.hasAnimatedEffects();
  }
  isAnimationRunning() {
    return !!this.animationTimer;
  }
  startAnimation() {
    if (this.animationTimer || !this.getLayer())
      return;
    const e = this.getPerformanceConfig();
    this.animationTimer = window.setInterval(() => {
      var t, n;
      this.animationOffset = (this.animationOffset + Number(e.effectAnimationStep || 1)) % 360, this.refresh(), (n = (t = this.getLayer()) == null ? void 0 : t.changed) == null || n.call(t);
    }, Math.max(40, Number(e.effectAnimationInterval) || 80));
  }
  stopAnimationIfIdle() {
    this.animationTimer && (this.hasAnimatedEffects() || this.stopAnimation());
  }
  stopAnimation() {
    this.animationTimer && (window.clearInterval(this.animationTimer), this.animationTimer = null);
  }
  reset() {
    this.renderer.reset(), this.stopAnimation(), this.animationOffset = 0;
  }
  destroy() {
    this.reset();
  }
};
var aS = class {
  constructor({ getMap: e, getFeatureById: t, selectFeature: n }) {
    this.getMap = e, this.getFeatureById = t, this.selectFeature = n;
  }
  focusModel(e) {
    var s, r;
    const t = this.getFeatureById(e == null ? void 0 : e.UID);
    if (!t)
      return;
    this.selectFeature(t);
    const n = (r = (s = t.getGeometry()) == null ? void 0 : s.getCoordinates) == null ? void 0 : r.call(s);
    n && window.setTimeout(() => {
      var o, a, l, c;
      (c = (l = (a = (o = this.getMap()) == null ? void 0 : o.getView) == null ? void 0 : a.call(o)) == null ? void 0 : l.animate) == null || c.call(l, {
        center: n,
        duration: 500
      });
    }, 100);
  }
};
var lS = class {
  constructor({ store: e = Y } = {}) {
    this.store = e, this.selectionHandler = null, this.routeSavedHandler = null, this.routeEditStartedHandler = null, this.templateDropHandler = null, this.annotationDrawEndHandler = null, this.boxSelectionHandler = null, this.measureHandler = null, this.targetFlowOffset = 0, this.mapCoordinateService = new x0({
      getMap: () => this.map
    }), this.mapBootstrapService = new C0({
      getProjectionCode: () => this.getProjectionCode(),
      getApp: () => this
    }), this.contextMenuService = new w_(), this.modelFeatureSyncService = new F0(), this.featureTransformPreviewService = new ku({
      getIconManager: () => this.iconManager,
      getDisplayConfig: () => this.displayConfig,
      updateHeadingIndicator: (t, n) => this.updateHeadingIndicator(t, n),
      refreshTargetIndicatorsFromFeatures: (t) => this.refreshTargetIndicatorsFromFeatures(t),
      refreshSituationEffectsFromFeatures: (t) => this.refreshSituationEffectsFromFeatures(t),
      syncPopupForFeature: (t) => this.syncPopupForFeature(t)
    }), this.mapModeService = new M0({
      onLeaveEditable: () => this.handleLeaveEditableMode()
    }), this.performanceConfigService = new U0({
      getDisplayConfig: () => this.displayConfig,
      setDisplayConfig: (t) => {
        this.displayConfig = t;
      },
      restartAnimatedLayers: () => this.restartAnimatedLayers()
    }), this.mapNavigationService = new I0({
      getCore: () => this.core,
      getLayer: () => this.layer,
      getIconManager: () => this.iconManager,
      getEffectManager: () => this.effectManager,
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n),
      lonLatFromCoordinate: (t) => this.lonLatFromCoordinate(t)
    }), this.modelRemovalService = new D0({
      getLayer: () => this.layer,
      getEvents: () => this.events,
      getRouteEditor: () => this.routeEditor,
      getPointDraw: () => this.pointDraw,
      getLineDraw: () => this.lineDraw,
      getHeadingIndicatorRenderer: () => this.headingIndicatorRenderer,
      getRelationIndicatorRenderer: () => this.relationIndicatorRenderer,
      getSituationEffectRenderer: () => this.situationEffectRenderer,
      getPopupManager: () => this.popup,
      getMover: () => this.mover,
      getIconManager: () => this.iconManager,
      getSelectionService: () => this.selectionService,
      refreshTargetIndicatorsFromFeatures: (t) => this.refreshTargetIndicatorsFromFeatures(t),
      refreshSituationEffectsFromFeatures: (t) => this.refreshSituationEffectsFromFeatures(t),
      stopTargetFlowAnimationIfIdle: () => this.stopTargetFlowAnimationIfIdle(),
      stopEffectAnimationIfIdle: () => this.stopEffectAnimationIfIdle()
    }), this.relationLineGeometryService = new X0({
      getFeatureById: (t) => {
        var n, s;
        return (s = (n = this.layer) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, t);
      }
    }), this.relationLineStyleService = new W0({
      getDisplayConfig: () => this.displayConfig,
      getFeatureById: (t) => {
        var n, s;
        return (s = (n = this.layer) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, t);
      },
      getFlowOffset: () => this.targetFlowOffset
    }), this.relationFlowAnimationService = new G0({
      getLayer: () => {
        var t;
        return (t = this.layer) == null ? void 0 : t.targetLayer;
      },
      getRenderer: () => this.relationIndicatorRenderer,
      getPerformanceConfig: () => this.getPerformanceConfig(),
      onOffsetChange: (t) => {
        this.targetFlowOffset = t;
      }
    }), this.annotationState = new d_({
      renderAnnotations: (t) => this.renderAnnotations(t),
      store: this.store
    }), this.measureService = new T0({
      getMap: () => this.map,
      getMeasureLayer: () => {
        var t;
        return (t = this.layer) == null ? void 0 : t.measureLayer;
      },
      stopInteractions: () => this.stopInteractions(),
      onMeasureEnd: (t) => {
        var n;
        return (n = this.measureHandler) == null ? void 0 : n.call(this, t);
      }
    }), this.annotationDrawingService = new py({
      getMap: () => this.map,
      getAnnotationSource: () => {
        var t, n, s;
        return (s = (n = (t = this.layer) == null ? void 0 : t.annotationLayer) == null ? void 0 : n.getSource) == null ? void 0 : s.call(n);
      },
      stopInteractions: () => this.stopInteractions(),
      onDrawEnd: (t) => {
        var n;
        return (n = this.annotationDrawEndHandler) == null ? void 0 : n.call(this, t);
      }
    }), this.annotationRenderService = new vy({
      getAnnotationLayer: () => {
        var t;
        return (t = this.layer) == null ? void 0 : t.annotationLayer;
      },
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n)
    }), this.treeInteractionService = new aS({
      getMap: () => this.map,
      getFeatureById: (t) => {
        var n, s;
        return (s = (n = this.layer) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, t);
      },
      selectFeature: (t) => this.selectFeature(t)
    }), this.selectionService = new $0({
      getFeatures: () => {
        var t, n, s, r, o;
        return ((o = (s = (n = (t = this.layer) == null ? void 0 : t.iconLayer) == null ? void 0 : n.getSource) == null ? void 0 : (r = s.call(n)).getFeatures) == null ? void 0 : o.call(r)) || [];
      },
      applyFeatureSelectionStyle: (t) => this.applyFeatureSelectionStyle(t),
      syncAllModelPopups: () => this.syncAllModelPopups(),
      onSelectionChange: (t) => {
        var n;
        return (n = this.selectionHandler) == null ? void 0 : n.call(this, t);
      }
    }), this.modelDisplaySyncService = new b0({
      getDisplayConfig: () => this.displayConfig,
      getFeatures: () => {
        var t, n, s, r, o;
        return ((o = (s = (n = (t = this.layer) == null ? void 0 : t.iconLayer) == null ? void 0 : n.getSource) == null ? void 0 : (r = s.call(n)).getFeatures) == null ? void 0 : o.call(r)) || [];
      },
      getPopupManager: () => this.popup,
      getIconManager: () => this.iconManager,
      isSelected: (t) => this.selectionService.isSelected(t)
    }), this.modelUpsertService = new P0({
      getLayer: () => this.layer,
      getIconManager: () => this.iconManager,
      getMover: () => this.mover,
      getDisplayConfig: () => this.displayConfig,
      getIconUrl: (t) => this.getIconUrl(t),
      getModelScale: (t) => this.getModelScale(t),
      updateFeatureProperties: (t, n) => this.updateFeatureProperties(t, n),
      isBatchSelected: (t) => this.selectionService.isBatchSelected(t),
      applyFeatureSelectionStyle: (t) => this.applyFeatureSelectionStyle(t),
      updateHeadingIndicator: (t, n) => this.updateHeadingIndicator(t, n),
      updateTargetIndicatorsForModel: (t) => this.updateTargetIndicatorsForModel(t),
      syncPopupForFeature: (t) => this.syncPopupForFeature(t)
    }), this.sceneRenderService = new Y0({
      getDisplayConfig: () => this.displayConfig,
      setDisplayConfig: (t) => {
        this.displayConfig = t;
      },
      setMode: (t, n) => this.setMode(t, n),
      getModelFeatures: () => {
        var t, n, s, r, o;
        return ((o = (s = (n = (t = this.layer) == null ? void 0 : t.iconLayer) == null ? void 0 : n.getSource) == null ? void 0 : (r = s.call(n)).getFeatures) == null ? void 0 : o.call(r)) || [];
      },
      deleteModelFeature: (t, n) => this.deleteIconById(t, n),
      upsertModel: (t, n) => this.upsertModel(t, n),
      renderAnnotations: (t) => this.renderAnnotations(t),
      renderHeadingIndicators: (t, n) => this.renderHeadingIndicators(t, n),
      renderTargetIndicators: (t, n) => this.renderTargetIndicators(t, n),
      renderRelationIndicators: (t, n) => this.renderRelationIndicators(t, n),
      renderSituationEffects: (t, n) => this.renderSituationEffects(t, n),
      refreshModelNameVisibility: () => this.refreshModelNameVisibility(),
      syncAllModelPopups: () => this.syncAllModelPopups()
    }), this.sceneStateService = new j0({
      hasMap: () => !!this.map,
      syncScene: (t) => this.renderScene(t),
      syncSituationChanges: (t) => this.syncSituationChanges(t),
      setDisplayConfig: (t) => {
        this.displayConfig = t;
      },
      refreshDisplayLayers: (t) => this.refreshDisplayLayers(t),
      setMode: (t, n) => this.setMode(t, n),
      store: this.store
    }), this.batchTransformService = new m_({
      getMap: () => this.map,
      getEvents: () => this.events,
      getRouteEditor: () => this.routeEditor,
      getFeatureById: (t) => {
        var n, s;
        return (s = (n = this.layer) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, t);
      },
      lonLatFromCoordinate: (t) => this.lonLatFromCoordinate(t),
      updateFeatureTransformPreview: (t, n) => this.updateFeatureTransformPreview(t, n),
      stopInteractions: () => this.stopInteractions()
    }), this.boxSelectionService = new E_({
      getMap: () => this.map,
      getFeaturesInExtent: (t) => {
        var n, s, r, o, a;
        return ((a = (r = (s = (n = this.layer) == null ? void 0 : n.iconLayer) == null ? void 0 : s.getSource) == null ? void 0 : (o = r.call(s)).getFeaturesInExtent) == null ? void 0 : a.call(o, t)) || [];
      },
      stopInteractions: () => this.stopInteractions(),
      setBatchSelection: (t) => this.setBatchSelection(t),
      selectFeature: (t, n) => this.selectFeature(t, n),
      onSelection: (t) => {
        var n;
        return (n = this.boxSelectionHandler) == null ? void 0 : n.call(this, t);
      }
    }), this.situationEffectService = new oS({
      getSource: () => {
        var t, n, s;
        return (s = (n = (t = this.layer) == null ? void 0 : t.situationEffectLayer) == null ? void 0 : n.getSource) == null ? void 0 : s.call(n);
      },
      getFeatureById: (t) => {
        var n, s;
        return (s = (n = this.layer) == null ? void 0 : n.getFeatureById) == null ? void 0 : s.call(n, t);
      },
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n),
      getResolution: () => {
        var t, n, s, r;
        return (r = (s = (n = (t = this.map) == null ? void 0 : t.getView) == null ? void 0 : n.call(t)) == null ? void 0 : s.getResolution) == null ? void 0 : r.call(s);
      },
      getDisplayConfig: () => this.displayConfig,
      getPerformanceConfig: () => this.getPerformanceConfig(),
      getLayer: () => {
        var t;
        return (t = this.layer) == null ? void 0 : t.situationEffectLayer;
      }
    }), this.situationEffectRenderer = this.situationEffectService.renderer, this.relationIndicatorRenderer = new z0({
      getSource: () => {
        var t, n, s;
        return (s = (n = (t = this.layer) == null ? void 0 : t.targetLayer) == null ? void 0 : n.getSource) == null ? void 0 : s.call(n);
      },
      getIconFeatures: () => {
        var t, n, s, r, o;
        return ((o = (s = (n = (t = this.layer) == null ? void 0 : t.iconLayer) == null ? void 0 : n.getSource) == null ? void 0 : (r = s.call(n)).getFeatures) == null ? void 0 : o.call(r)) || [];
      },
      getFeatureKey: (t) => this.getRelationFeatureKey(t),
      getTargetFeatureKey: (t) => this.getTargetFeatureKey(t),
      getTargetUID: (t) => this.getTargetUID(t),
      getLineCoordinates: (t) => this.getRelationLineCoordinates(t),
      getStyle: (t) => this.getRelationLineStyle(t),
      onStartAnimation: () => this.startTargetFlowAnimation(),
      onStopIfIdle: () => this.stopTargetFlowAnimationIfIdle()
    }), this.headingIndicatorRenderer = new Gf({
      getSource: () => {
        var t, n, s;
        return (s = (n = (t = this.layer) == null ? void 0 : t.headingLayer) == null ? void 0 : n.getSource) == null ? void 0 : s.call(n);
      },
      getIconFeatures: () => {
        var t, n, s, r, o;
        return ((o = (s = (n = (t = this.layer) == null ? void 0 : t.iconLayer) == null ? void 0 : n.getSource) == null ? void 0 : (r = s.call(n)).getFeatures) == null ? void 0 : o.call(r)) || [];
      },
      coordinateFromLonLat: (t, n) => this.coordinateFromLonLat(t, n),
      getResolution: () => {
        var t, n, s, r;
        return (r = (s = (n = (t = this.map) == null ? void 0 : t.getView) == null ? void 0 : n.call(t)) == null ? void 0 : s.getResolution) == null ? void 0 : r.call(s);
      },
      getPixelLength: () => this.headingPixelLength
    }), this.mode = "edit", this.editable = true, this.headingPixelLength = 42, this.displayConfig = k0(), this.tileUrlTemplate = "";
  }
  init(e = {}) {
    this.map && this.destroy(), Object.assign(this, this.mapBootstrapService.init(e)), this.map.on("moveend", () => {
      this.refreshHeadingIndicatorsFromFeatures(), this.refreshTargetIndicatorsFromFeatures();
    });
  }
  getSceneState() {
    return this.sceneStateService.getSceneState();
  }
  getSceneSnapshot() {
    return this.sceneStateService.getSceneSnapshot();
  }
  getTemplates() {
    return this.sceneStateService.getTemplates();
  }
  getHistoryState() {
    return this.sceneStateService.getHistoryState();
  }
  getStatistics() {
    return this.sceneStateService.getStatistics();
  }
  getModelsBySide(e) {
    return this.sceneStateService.getModelsBySide(e);
  }
  validateScene() {
    return this.sceneStateService.validateScene();
  }
  createModelFromTemplate(e, t = {}) {
    return this.sceneStateService.createModelFromTemplate(e, t);
  }
  async syncScene() {
    return this.sceneStateService.syncCurrentScene();
  }
  async loadScene(e, t = {}) {
    return this.sceneStateService.loadScene(e, t);
  }
  async resetScene(e = {}) {
    return this.sceneStateService.resetScene(e);
  }
  async syncSituationChanges({ models: e = [], removedModels: t = [], relations: n = [], removedRelations: s = [], effects: r = [], removedEffects: o = [] } = {}) {
    if (this.map) {
      for (const a of e)
        await this.upsertModel(a, { skipIndicators: true, deferPopup: true });
      if (t.forEach((a) => {
        this.deleteIconById(a.UID, { showEffect: false });
      }), e.length || t.length) {
        this.refreshDisplayLayers(this.store.sceneState);
        return;
      }
      (n.length || s.length) && this.renderRelationIndicators(this.store.sceneState.relations, this.store.sceneState.display), (r.length || o.length) && this.renderSituationEffects(this.store.sceneState.effects, this.store.sceneState.display);
    }
  }
  refreshDisplayLayers(e = this.store.sceneState) {
    this.renderHeadingIndicators(e.models, e.display), this.renderTargetIndicators(e.models, e.display), this.renderRelationIndicators(e.relations, e.display), this.renderSituationEffects(e.effects, e.display);
  }
  async addModelToState(e, t = {}) {
    return this.sceneStateService.addModel(e, t);
  }
  async updateModelInState(e, t, n = {}) {
    return this.sceneStateService.updateModel(e, t, n);
  }
  async batchUpdateModelsInState(e = [], t = {}) {
    return this.sceneStateService.batchUpdateModels(e, t);
  }
  async upsertModelsToState(e = [], t = {}) {
    return this.sceneStateService.upsertModels(e, t);
  }
  async removeModelFromState(e, t = {}) {
    return this.sceneStateService.removeModel(e, t);
  }
  async removeModelsFromState(e = [], t = {}) {
    return this.sceneStateService.removeModels(e, t);
  }
  async upsertRelationsToState(e = [], t = {}) {
    return this.sceneStateService.upsertRelations(e, t);
  }
  async removeRelationsFromState(e = [], t = {}) {
    return this.sceneStateService.removeRelations(e, t);
  }
  async upsertEffectsToState(e = [], t = {}) {
    return this.sceneStateService.upsertEffects(e, t);
  }
  async removeEffectsFromState(e = [], t = {}) {
    return this.sceneStateService.removeEffects(e, t);
  }
  async setDisplayConfig(e = {}, t = {}) {
    return this.sceneStateService.setDisplay(e, t);
  }
  async setPerformanceConfig(e = {}, t = {}) {
    return this.sceneStateService.setPerformance(e, t);
  }
  async setModeState(e) {
    return this.sceneStateService.setModeState(e);
  }
  setMetadata(e = {}) {
    return this.sceneStateService.setMetadata(e);
  }
  setPlaybackConfig(e = {}) {
    return this.sceneStateService.setPlaybackConfig(e);
  }
  setSelectedModelUID(e) {
    this.sceneStateService.setSelectedModelUID(e);
  }
  setSelectedAnnotationId(e) {
    this.sceneStateService.setSelectedAnnotationId(e);
  }
  addMountToModel(e, t, n = {}) {
    return this.sceneStateService.addMountToModel(e, t, n);
  }
  removeMountFromModel(e, t, n = {}) {
    return this.sceneStateService.removeMountFromModel(e, t, n);
  }
  setModelRoute(e, t, n = {}, s = {}) {
    return this.sceneStateService.setModelRoute(e, t, n, s);
  }
  async addAnnotationToState(e, t = {}) {
    return this.annotationState.add(e, t);
  }
  async updateAnnotationInState(e, t, n = {}) {
    return this.annotationState.update(e, t, n);
  }
  async removeAnnotationFromState(e, t = {}) {
    return this.annotationState.remove(e, t);
  }
  undoSceneChange() {
    return this.sceneStateService.undo();
  }
  redoSceneChange() {
    return this.sceneStateService.redo();
  }
  getPerformanceConfig(e = this.displayConfig) {
    return this.performanceConfigService.getConfig(e);
  }
  applyPerformanceConfig(e = {}) {
    return this.performanceConfigService.applyConfig(e);
  }
  restartAnimatedLayers() {
    const e = this.relationFlowAnimationService.isRunning(), t = this.situationEffectService.isAnimationRunning();
    this.relationFlowAnimationService.stop(), this.situationEffectService.stopAnimation(), e && this.relationIndicatorRenderer.hasRelations() && this.startTargetFlowAnimation(), t && this.startEffectAnimation();
  }
  handleLeaveEditableMode() {
    var e, t, n, s;
    this.stopInteractions(), (t = (e = this.events) == null ? void 0 : e.disableRouteEdit) == null || t.call(e), (s = (n = this.routeEditor) == null ? void 0 : n.clear) == null || s.call(n, true), this.setBatchSelection([]), this.selectFeature(null);
  }
  setMode(e = "edit", t = {}) {
    return this.mode = this.mapModeService.setMode(e, t), this.editable = this.mapModeService.editable, this.mode;
  }
  isEditable() {
    return this.mapModeService.isEditable();
  }
  setEditable(e) {
    return this.editable = this.mapModeService.setEditable(e), this.mode = this.mapModeService.getMode(), this.editable;
  }
  setSelectionChangeHandler(e) {
    this.selectionHandler = e;
  }
  setRouteSavedHandler(e) {
    this.routeSavedHandler = e;
  }
  setTemplateDropHandler(e) {
    this.templateDropHandler = e;
  }
  setContextMenuHandler(e) {
    this.contextMenuService.setHandler(e);
  }
  setContextMenuItems(e = [], t = {}) {
    this.contextMenuService.setItems(e, t);
  }
  setAnnotationDrawEndHandler(e) {
    this.annotationDrawEndHandler = e;
  }
  setBoxSelectionHandler(e) {
    this.boxSelectionHandler = e;
  }
  setMeasureHandler(e) {
    this.measureHandler = e;
  }
  getLocalTileRegions() {
    var e, t;
    return ((t = (e = this.core) == null ? void 0 : e.getLocalTileRegions) == null ? void 0 : t.call(e)) || [];
  }
  setLocalTileRegions(e = []) {
    var t, n;
    return ((n = (t = this.core) == null ? void 0 : t.setLocalTileRegions) == null ? void 0 : n.call(t, e)) || [];
  }
  addLocalTileRegion(e) {
    var t, n;
    return ((n = (t = this.core) == null ? void 0 : t.addLocalTileRegion) == null ? void 0 : n.call(t, e)) || null;
  }
  removeLocalTileRegion(e) {
    var t, n;
    return ((n = (t = this.core) == null ? void 0 : t.removeLocalTileRegion) == null ? void 0 : n.call(t, e)) || false;
  }
  clearLocalTileRegions() {
    var e, t;
    return (t = (e = this.core) == null ? void 0 : e.clearLocalTileRegions) == null || t.call(e), [];
  }
  ensureBoxSelectionVisual() {
    return this.boxSelectionService.ensureVisual();
  }
  updateBoxSelectionVisual(e, t) {
    this.boxSelectionService.updateVisual(e, t);
  }
  hideBoxSelectionVisual() {
    this.boxSelectionService.hideVisual();
  }
  destroyBoxSelectionVisual() {
    this.boxSelectionService.destroyVisual();
  }
  getSelectedFeature() {
    return this.selectionService.getSelectedFeature();
  }
  getBatchSelectedUIDs() {
    return this.selectionService.getBatchSelectedUIDs();
  }
  isBatchTransformActive() {
    return this.batchTransformService.isActive();
  }
  getIconUrl(e) {
    return this.mapCoordinateService.getIconUrl(e);
  }
  getProjectionCode() {
    return this.mapCoordinateService.getProjectionCode();
  }
  coordinateFromLonLat(e, t) {
    return this.mapCoordinateService.coordinateFromLonLat(e, t);
  }
  lonLatFromCoordinate(e) {
    return this.mapCoordinateService.lonLatFromCoordinate(e);
  }
  getModelScale(e) {
    return this.mapCoordinateService.getModelScale(e);
  }
  shouldShowPopup() {
    return this.modelDisplaySyncService.shouldShowPopup();
  }
  shouldDisplayPopupForFeature(e) {
    return this.modelDisplaySyncService.shouldDisplayPopupForFeature(e);
  }
  getVisiblePopupFeatures() {
    return this.modelDisplaySyncService.getVisiblePopupFeatures();
  }
  syncPopupForFeature(e) {
    this.modelDisplaySyncService.syncPopupForFeature(e);
  }
  syncAllModelPopups() {
    this.modelDisplaySyncService.syncAllModelPopups();
  }
  applyFeatureSelectionStyle(e) {
    this.modelDisplaySyncService.applyFeatureSelectionStyle(e);
  }
  refreshModelNameVisibility() {
    this.modelDisplaySyncService.refreshModelNameVisibility();
  }
  setFeaturePropertyIfChanged(e, t, n) {
    return this.modelFeatureSyncService.setPropertyIfChanged(e, t, n);
  }
  updateFeatureProperties(e, t) {
    return this.modelFeatureSyncService.updateProperties(e, t);
  }
  async addIcon(e, t = {}) {
    return !this.map || !this.layer || !this.iconManager ? null : this.modelUpsertService.add(e, t);
  }
  async upsertModel(e, t = {}) {
    return !this.map || !this.layer || !this.iconManager ? null : this.modelUpsertService.upsert(e, t);
  }
  async renderScene(e) {
    !this.map || !this.layer || !this.iconManager || await this.sceneRenderService.render(e);
  }
  renderAnnotations(e = []) {
    this.annotationRenderService.render(e);
  }
  createAnnotationFeature(e) {
    return this.annotationRenderService.createFeature(e);
  }
  getHeadingCoordinates(e) {
    return this.headingIndicatorRenderer.getCoordinates(e);
  }
  getHeadingStyle(e) {
    return this.headingIndicatorRenderer.getStyle(e);
  }
  updateHeadingIndicator(e, t = {}) {
    this.headingIndicatorRenderer.update(e, t);
  }
  renderHeadingIndicators(e = [], t = {}) {
    this.headingIndicatorRenderer.render(e, t);
  }
  refreshHeadingIndicatorsFromFeatures() {
    this.headingIndicatorRenderer.refresh(this.displayConfig);
  }
  getRelationFeatureKey(e) {
    return this.relationLineGeometryService.getRelationFeatureKey(e);
  }
  getTargetFeatureKey(e) {
    return this.relationLineGeometryService.getTargetFeatureKey(e);
  }
  getTargetUID(e) {
    return this.relationLineGeometryService.getTargetUID(e);
  }
  getRelationLineCoordinates(e) {
    return this.relationLineGeometryService.getRelationLineCoordinates(e);
  }
  getTargetLineCoordinates(e) {
    return this.relationLineGeometryService.getTargetLineCoordinates(e);
  }
  getTargetLineStyle(e) {
    return this.getRelationLineStyle({ type: "target", nation: e });
  }
  getRelationLineStyle(e = {}) {
    return this.relationLineStyleService.getStyle(e);
  }
  startTargetFlowAnimation() {
    this.relationFlowAnimationService.start();
  }
  stopTargetFlowAnimationIfIdle() {
    this.relationFlowAnimationService.stopIfIdle();
  }
  updateRelationIndicator(e, t = this.displayConfig) {
    this.relationIndicatorRenderer.updateRelation(e, t);
  }
  updateTargetIndicator(e, t = this.displayConfig) {
    this.relationIndicatorRenderer.updateTarget(e, t);
  }
  updateTargetIndicatorsForModel(e) {
    this.relationIndicatorRenderer.updateTargetsForModel(e, this.displayConfig);
  }
  renderTargetIndicators(e = [], t = {}) {
    this.relationIndicatorRenderer.renderTargets(e, t);
  }
  renderRelationIndicators(e = [], t = this.displayConfig) {
    this.relationIndicatorRenderer.renderRelations(e, t);
  }
  getEffectFeatureKey(e) {
    return this.situationEffectService.getFeatureKey(e);
  }
  getBuiltInEffectDefaults(e = "radar") {
    return this.situationEffectService.getBuiltInDefaults(e);
  }
  mergeEffectStyle(e = {}, t = {}) {
    return this.situationEffectService.mergeStyle(e, t);
  }
  getEffectDefaults(e = "radar") {
    return this.situationEffectService.getDefaults(e);
  }
  getEffectStyle(e = {}) {
    return this.situationEffectService.getStyle(e);
  }
  createSectorCoordinates(e, t, n, s, r = 64) {
    return this.situationEffectService.createSectorCoordinates(e, t, n, s, r);
  }
  createBoxCoordinates(e, t) {
    return this.situationEffectService.createBoxCoordinates(e, t);
  }
  getStatusEffectText(e) {
    return this.situationEffectService.getStatusText(e);
  }
  getEffectCenter(e = {}) {
    return this.situationEffectService.getCenter(e);
  }
  createEffectGeometry(e = {}) {
    return this.situationEffectService.createGeometry(e);
  }
  createLineEffectStyle(e = {}) {
    return this.situationEffectService.createLineStyle(e);
  }
  getLineEffectEndpoints(e = {}) {
    return this.situationEffectService.getLineEndpoints(e);
  }
  createLineMovingMarkerFeature(e = {}, t = null) {
    return this.situationEffectService.createLineMovingMarkerFeature(e, t);
  }
  createLineEventFeature(e = {}, t = "intercept", n = null) {
    return this.situationEffectService.createLineEventFeature(e, t, n);
  }
  createStatusMarkerFeature(e = {}, t = null) {
    return this.situationEffectService.createStatusMarkerFeature(e, t);
  }
  createEffectStyle(e = {}) {
    return this.situationEffectService.createStyle(e);
  }
  createDiscoveredFlashFeature(e = {}, t = null) {
    return this.situationEffectService.createDiscoveredFlashFeature(e, t);
  }
  createEffectExtraFeatures(e = {}, t = []) {
    return this.situationEffectService.createExtraFeatures(e, t);
  }
  createScanFeature(e = {}, t = null) {
    return this.situationEffectService.createScanFeature(e, t);
  }
  updateSituationEffect(e, t = this.displayConfig) {
    this.situationEffectService.update(e, t);
  }
  renderSituationEffects(e = [], t = this.displayConfig) {
    this.situationEffectService.render(e, t);
  }
  refreshSituationEffectsFromFeatures(e = null) {
    this.situationEffectService.refresh(e, this.displayConfig);
  }
  startEffectAnimation() {
    this.situationEffectService.startAnimation();
  }
  stopEffectAnimationIfIdle() {
    this.situationEffectService.stopAnimationIfIdle();
  }
  refreshTargetIndicatorsFromFeatures(e = null) {
    this.relationIndicatorRenderer.refreshFromFeatures(e, this.displayConfig);
  }
  setBatchSelection(e = []) {
    this.selectionService.setBatchSelection(e);
  }
  selectFeature(e, t = false) {
    this.selectionService.selectFeature(e, t);
  }
  treeClick(e) {
    this.treeInteractionService.focusModel(e);
  }
  deleteIconById(e, t = {}) {
    return this.modelRemovalService.removeById(e, t);
  }
  getLayer() {
    return this.layer;
  }
  getFeatureScale(e) {
    return this.featureTransformPreviewService.getFeatureScale(e);
  }
  updateFeatureTransformPreview(e, { coord: t, lon: n, lat: s, heading: r }) {
    this.featureTransformPreviewService.update(e, { coord: t, lon: n, lat: s, heading: r });
  }
  restoreBatchTransformSnapshot(e = []) {
    this.batchTransformService.restoreSnapshot(e);
  }
  disableBatchTransform(e = {}) {
    this.batchTransformService.disable(e);
  }
  createBatchTransformItems(e = []) {
    return this.batchTransformService.createItems(e);
  }
  prepareBatchTransformMode(e = []) {
    return this.batchTransformService.prepare(e);
  }
  startBatchRotateMode(e = [], t = {}) {
    return this.isEditable() ? this.batchTransformService.startRotate(e, t) : false;
  }
  startBatchMoveMode(e = [], t = {}) {
    return this.isEditable() ? this.batchTransformService.startMove(e, t) : false;
  }
  enableBoxSelection() {
    return this.isEditable() ? this.boxSelectionService.enable() : false;
  }
  disableBoxSelection() {
    this.boxSelectionService.disable();
  }
  startRouteEdit(e) {
    var t;
    this.isEditable() && (this.events.startRouteEdit(e), (t = this.routeEditStartedHandler) == null || t.call(this, e));
  }
  handleRouteSaved(e, t) {
    var n;
    e && ((n = this.routeSavedHandler) == null || n.call(this, e.get("UID"), t));
  }
  async handleTemplateDrop(e) {
    return this.templateDropHandler ? this.templateDropHandler(e) : null;
  }
  closeContextMenu() {
    this.contextMenuService.close();
  }
  openContextMenu(e = {}) {
    return this.contextMenuService.open(e);
  }
  stopInteractions() {
    this.map && (this.measureService.stop(), this.annotationDrawingService.stop(), this.disableBoxSelection(), this.disableBatchTransform({ revertPreview: true, emitCancel: false }), this.map.getTargetElement().style.cursor = "");
  }
  clearMeasure() {
    this.measureService.clear();
  }
  endMeasure() {
    this.measureService.end();
  }
  startMeasure(e = "distance") {
    return this.isEditable() ? this.measureService.start(e) : false;
  }
  startAnnotationDrawing(e, t = {}) {
    return this.isEditable() ? this.annotationDrawingService.start(e, t) : false;
  }
  cancelCurrentTool() {
    this.stopInteractions();
  }
  setCenterAndZoom(e, t, n, s = true, r = 500) {
    this.mapNavigationService.setCenterAndZoom(e, t, n, s, r);
  }
  focusOnIcon(e, t = null, n = true, s = 1500) {
    this.mapNavigationService.focusOnIcon(e, t, n, s);
  }
  setIconRotationToTarget(e, t, n) {
    this.mapNavigationService.setIconRotationToTarget(e, t, n);
  }
  playEventEffect(e, t = {}) {
    return this.mapNavigationService.playEventEffect(e, t);
  }
  destroy() {
    var e, t, n, s, r, o, a, l, c, h2, u, d, f, g, y, m;
    this.stopInteractions(), this.clearMeasure(), (t = (e = this.routeEditor) == null ? void 0 : e.clear) == null || t.call(e, true), (s = (n = this.popup) == null ? void 0 : n.destroy) == null || s.call(n), (o = (r = this.events) == null ? void 0 : r.destroy) == null || o.call(r), (h2 = (c = (l = (a = this.map) == null ? void 0 : a.getOverlays) == null ? void 0 : l.call(a)) == null ? void 0 : c.clear) == null || h2.call(c), (g = (f = (d = (u = this.map) == null ? void 0 : u.getLayers) == null ? void 0 : d.call(u)) == null ? void 0 : f.clear) == null || g.call(f), (m = (y = this.core) == null ? void 0 : y.destroy) == null || m.call(y), this.map = null, this.layer = null, this.popup = null, this.effectManager = null, this.iconManager = null, this.feature = null, this.pointDraw = null, this.lineDraw = null, this.mover = null, this.routeEditor = null, this.events = null, this.selectionService.reset(), this.headingIndicatorRenderer.reset(), this.relationIndicatorRenderer.reset(), this.situationEffectService.reset(), this.relationFlowAnimationService.reset(), this.contextMenuService.destroy(), this.measureService.stop(), this.boxSelectionService.destroy(), this.batchTransformService.reset(), this.selectionService.reset();
  }
};
var fu = (i = {}) => {
  const e = new lS({ store: i.store || Oa() }), t = i.bridge || null, n = Du({ bridge: t, mapApp: e, options: i });
  return {
    bridge: t,
    context: n,
    mapApp: e,
    init(s = {}) {
      return e.init({ ...i, ...s });
    },
    destroy() {
      return e.destroy();
    },
    getMapApp() {
      return e;
    },
    getBridge() {
      return t;
    },
    getContext() {
      return n;
    }
  };
};
var cS = () => {
  const i = {
    ready: /* @__PURE__ */ new Set(),
    selectionChange: /* @__PURE__ */ new Set(),
    routeSaved: /* @__PURE__ */ new Set(),
    measure: /* @__PURE__ */ new Set(),
    annotationCreated: /* @__PURE__ */ new Set(),
    templateDrop: /* @__PURE__ */ new Set()
  };
  return {
    emit: (s, r) => {
      var o;
      (o = i[s]) == null || o.forEach((a) => {
        a(r);
      });
    },
    emitAsync: async (s, r) => {
      const o = Array.from(i[s] || []);
      for (const a of o)
        await a(r);
    },
    listeners: i,
    on: (s, r) => !i[s] || typeof r != "function" ? () => {
    } : (i[s].add(r), () => {
      i[s].delete(r);
    })
  };
};
var Lr = fu({ store: Y });
var hS = Lr.getMapApp();
var uS = (i) => Lr.init(i);
var dS = () => Lr.destroy();
var ic = (i) => JSON.parse(JSON.stringify(i));
var Xa = ({
  engine: i = Lr,
  mapApp: e = ((r) => (r = i == null ? void 0 : i.getMapApp) == null ? void 0 : r.call(i))() || hS,
  sceneState: t = ((o) => (o = e == null ? void 0 : e.store) == null ? void 0 : o.sceneState)() || kh,
  initMapClass: n = (a) => {
    var l;
    return ((l = i == null ? void 0 : i.init) == null ? void 0 : l.call(i, a)) ?? uS(a);
  },
  destroyMapClass: s = () => {
    var a;
    return ((a = i == null ? void 0 : i.destroy) == null ? void 0 : a.call(i)) ?? dS();
  }
} = {}) => {
  let a = 0, l = 0, c = null, h2 = null;
  const u = /* @__PURE__ */ new Map(), d = cS();
  let f = null, g = new Promise((I) => {
    f = I;
  });
  const y = (I, L) => d.emit(I, L), m = (I, L) => d.emitAsync(I, L), _ = (I, L) => d.on(I, L), p = () => g, E = (I, L, B = {}) => {
    const re = I == null ? void 0 : I.dataTransfer;
    return re ? (re.setData("application/json", JSON.stringify({
      dragType: "template",
      template: ic(L || {}),
      ...ic(B || {})
    })), re.effectAllowed = "copy", true) : false;
  }, S = (I, L) => Ks(
    [Number(I.lon), Number(I.lat)],
    [Number(L.lon), Number(L.lat)]
  ), w = (I) => {
    const L = Array.isArray(I.line) ? I.line : [];
    if (L.length < 2)
      return {
        route: L,
        totalLength: 0
      };
    let B = 0;
    const re = [];
    for (let ce = 0; ce < L.length - 1; ce += 1) {
      const Ie = S(L[ce], L[ce + 1]);
      re.push(Ie), B += Ie;
    }
    return {
      route: L,
      segments: re,
      totalLength: B
    };
  }, x = () => {
    const I = t.models.map((L) => {
      var ce;
      const { totalLength: B } = w(L), re = Number(L.speed || ((ce = L.routeMeta) == null ? void 0 : ce.speed) || 0);
      return !B || re <= 0 ? 0 : B / re;
    }).filter(Boolean);
    return I.length ? Math.max(...I) : 0;
  }, b = (I) => {
    var L;
    return Array.isArray(I.line) && I.line.length > 1 && Number(I.speed || ((L = I.routeMeta) == null ? void 0 : L.speed) || 0) > 0;
  }, v = () => {
    var I, L;
    u.clear(), (L = (I = e == null ? void 0 : e.lineDraw) == null ? void 0 : I.clear) == null || L.call(I);
  }, R = (I) => {
    const L = Math.max(2, Number(t.playback.maxTrailPoints) || 4e3), B = I.UID, re = {
      lon: Number(I.lon),
      lat: Number(I.lat)
    }, ce = u.get(B) || [], Ie = ce[ce.length - 1];
    for ((!Ie || Ie.lon !== re.lon || Ie.lat !== re.lat) && ce.push(re); ce.length > L; )
      ce.shift();
    u.set(B, ce), ce.length > 1 && e.lineDraw.drawLine(ce, B, I.nation);
  }, P = (I, L) => {
    var Qt;
    const { route: B, segments: re = [], totalLength: ce } = w(I);
    if (B.length < 2 || !ce)
      return { lon: I.lon, lat: I.lat, heading: I.heading || 0 };
    const Ie = Number(I.speed || ((Qt = I.routeMeta) == null ? void 0 : Qt.speed) || 0);
    if (Ie <= 0)
      return {
        lon: Number(B[0].lon),
        lat: Number(B[0].lat),
        heading: I.heading || 0
      };
    let Ae = Math.min(ce, L * Ie);
    for (let xn = 0; xn < re.length; xn += 1) {
      const In = re[xn];
      if (Ae <= In) {
        const Ot = B[xn], en = B[xn + 1], bi = In === 0 ? 0 : Ae / In, Fr = Number(Ot.lon) + (Number(en.lon) - Number(Ot.lon)) * bi, Ri = Number(Ot.lat) + (Number(en.lat) - Number(Ot.lat)) * bi, vn = Math.atan2(
          Number(en.lon) - Number(Ot.lon),
          Number(en.lat) - Number(Ot.lat)
        );
        return { lon: Fr, lat: Ri, heading: vn };
      }
      Ae -= In;
    }
    const rt = B[B.length - 1], lt = B[B.length - 2];
    return {
      lon: Number(rt.lon),
      lat: Number(rt.lat),
      heading: Math.atan2(
        Number(rt.lon) - Number(lt.lon),
        Number(rt.lat) - Number(lt.lat)
      )
    };
  }, U = (I = 0) => {
    v(), t.models.forEach((L) => {
      if (!b(L))
        return;
      const B = P(L, I);
      R({
        ...L,
        lon: B.lon,
        lat: B.lat
      });
    });
  }, N = async (I) => {
    const L = t.models.map((B) => {
      const re = P(B, I);
      return {
        ...B,
        lon: re.lon,
        lat: re.lat,
        heading: re.heading
      };
    });
    for (const B of L)
      await e.upsertModel(B), b(B) && R(B);
    e.renderTargetIndicators(L, t.display);
  }, O = () => {
    t.playback.playing = false, a && (cancelAnimationFrame(a), a = 0);
  }, G = async (I) => {
    if (!t.playback.playing)
      return;
    l || (l = I);
    const L = (I - l) / 1e3;
    l = I;
    const B = Number(t.playback.duration) || 0, re = Number(t.playback.speed) || 1, ce = t.playback.elapsed + L * re, Ie = B ? Math.min(B, ce) : ce, Ae = B ? Math.min(1, Ie / B) : 0;
    if (t.playback.elapsed = Ie, t.playback.progress = Ae, t.playback.currentTimeLabel = `T+${Math.round(Ie)}s`, await N(Ie), B && Ae >= 1) {
      O();
      return;
    }
    a = requestAnimationFrame(G);
  }, $ = async () => (e != null && e.map && (v(), await e.renderScene(t)), t);
  return {
    engine: i,
    mapApp: e,
    sceneState: t,
    addLocalTileRegion: (I) => e != null && e.map ? e.addLocalTileRegion(I) : null,
    cancelMapTool: () => {
      e != null && e.map && e.cancelCurrentTool();
    },
    clearLocalTileRegions: () => e != null && e.map ? e.clearLocalTileRegions() : [],
    deletebtn: async (I, L) => {
      !e || !(L != null && L.UID) || await e.removeModelFromState(L.UID, { recordHistory: true });
    },
    destroyMap: () => {
      O(), v(), c = null, h2 = null, s(), g = new Promise((I) => {
        f = I;
      });
    },
    dragStart: (I, L) => E(I, L),
    emitEditorEvent: y,
    endMeasureMode: () => {
      e != null && e.map && e.endMeasure();
    },
    exportMapScreenshot: (I) => {
      if (!(e != null && e.map))
        return;
      const L = e.map.getViewport(), B = L.querySelectorAll(".ol-layer canvas, canvas.ol-layer");
      if (!B.length) {
        ui.warning("当前没有可导出的地图画面");
        return;
      }
      const re = document.createElement("canvas");
      re.width = L.clientWidth, re.height = L.clientHeight;
      const ce = re.getContext("2d");
      B.forEach((Ae) => {
        var rt, lt;
        if (Ae.width > 0) {
          const Qt = ((lt = (rt = Ae.parentNode) == null ? void 0 : rt.style) == null ? void 0 : lt.opacity) || Ae.style.opacity || 1;
          ce.globalAlpha = Number(Qt) || 1, ce.drawImage(Ae, 0, 0);
        }
      });
      const Ie = document.createElement("a");
      Ie.href = re.toDataURL("image/png"), Ie.download = I || `scenario-${Date.now()}.png`, Ie.click();
    },
    focusModel: (I) => {
      e != null && e.map && e.focusOnIcon(I, 7, true, 800);
    },
    getLocalTileRegions: () => e != null && e.map ? e.getLocalTileRegions() : [],
    initMap: async (I) => (await n(I), e.setSelectionChangeHandler((L) => {
      e.setSelectedModelUID((L == null ? void 0 : L.get("UID")) || null), y("selectionChange", L);
    }), e.setRouteSavedHandler((L, B) => {
      e.setModelRoute(L, B, {}, { recordHistory: true }), y("routeSaved", { uid: L, points: B });
    }), e.setMeasureHandler((L) => {
      y("measure", L), c ? c(L) : ui.success(`测量结果: ${L.label}`);
    }), e.setAnnotationDrawEndHandler(async (L) => {
      const B = await e.addAnnotationToState(L, { recordHistory: true });
      h2 == null || h2(B), y("annotationCreated", B);
    }), e.setTemplateDropHandler(async (L) => (await m("templateDrop", L), L)), await $(), f == null || f(e), y("ready", e), e),
    onEditTrackClick: () => {
      const I = e.getSelectedFeature();
      if (!I) {
        ui.warning("请先选择模型");
        return;
      }
      e.startRouteEdit(I);
    },
    onEditorEvent: _,
    pausePlayback: O,
    removeLocalTileRegion: (I) => e != null && e.map ? e.removeLocalTileRegion(I) : false,
    removeModelByUID: (I) => {
      e != null && e.map && e.deleteIconById(I);
    },
    seekPlayback: async (I) => {
      t.playback.progress = Math.max(0, Math.min(1, Number(I) || 0));
      const L = Number(t.playback.duration) || 120;
      t.playback.elapsed = t.playback.progress * L, t.playback.currentTimeLabel = `T+${Math.round(t.playback.elapsed)}s`, U(t.playback.elapsed), await N(t.playback.elapsed);
    },
    setLocalTileRegions: (I) => e != null && e.map ? e.setLocalTileRegions(I) : [],
    setTemplateDragData: E,
    startAnnotationMode: (I, L = {}, B = null) => {
      e != null && e.map && (h2 = B, e.startAnnotationDrawing(I, L));
    },
    startBatchMoveMode: (I, L = {}) => e != null && e.map ? e.startBatchMoveMode(I, L) : false,
    startBatchRotateMode: (I, L = {}) => e != null && e.map ? e.startBatchRotateMode(I, L) : false,
    startBoxSelectionMode: (I) => {
      e != null && e.map && (typeof I == "function" && e.setBoxSelectionHandler(I), e.enableBoxSelection());
    },
    startMeasureMode: (I, L = null) => {
      e != null && e.map && (c = L, e.startMeasure(I));
    },
    startPlayback: () => {
      if (t.playback.playing)
        return;
      const I = x();
      if (!I) {
        ui.warning("当前没有可播放的轨迹，请先为模型编辑航路并设置速度");
        return;
      }
      t.playback.elapsed === 0 && U(0), t.playback.duration = I, t.playback.playing = true, l = 0, a = requestAnimationFrame(G);
    },
    stopBatchTransformMode: () => {
      e != null && e.map && e.disableBatchTransform();
    },
    stopBoxSelectionMode: () => {
      e != null && e.map && e.disableBoxSelection();
    },
    stopPlayback: async () => {
      O(), t.playback.elapsed = 0, t.playback.progress = 0, t.playback.currentTimeLabel = "T+00:00", v(), await $();
    },
    syncBatchSelectionOnMap: (I) => {
      e != null && e.map && e.setBatchSelection(I);
    },
    syncSceneToMap: $,
    treeModelClick: (I) => {
      e.setSelectedModelUID(I.UID), e.treeClick(I);
    },
    updateModelOnMap: async (I) => {
      e != null && e.map && await e.upsertModel(I);
    },
    waitForMapReady: p
  };
};
var Xe = Xa();
var fS = Xe.mapApp;
Xe.sceneState;
var hE = (...i) => Xe.addLocalTileRegion(...i);
var uE = (...i) => Xe.clearLocalTileRegions(...i);
var dE = (...i) => Xe.endMeasureMode(...i);
var fE = (...i) => Xe.getLocalTileRegions(...i);
var gE = (...i) => Xe.removeLocalTileRegion(...i);
var mE = (...i) => Xe.setLocalTileRegions(...i);
var yE = (...i) => Xe.setTemplateDragData(...i);
var _E = (...i) => Xe.startBatchMoveMode(...i);
var pE = (...i) => Xe.startBatchRotateMode(...i);
var SE = (...i) => Xe.startBoxSelectionMode(...i);
var EE = (...i) => Xe.stopBatchTransformMode(...i);
var wE = (...i) => Xe.stopBoxSelectionMode(...i);
var CE = (...i) => Xe.syncBatchSelectionOnMap(...i);
var gS = ({
  api: i,
  clone: e,
  generateSceneUniqueName: t,
  getSceneState: n,
  mapApp: s,
  resolveTemplate: r,
  canMountTemplateOnModel: o,
  setTemplateDragDataOnMap: a
}) => ({
  createModelFromTemplate: (l, c = {}) => {
    const h2 = r(l);
    return h2 ? s.createModelFromTemplate(h2, c) : null;
  },
  canMountTemplateOnModel: (l, c) => {
    const h2 = r(l);
    if (!h2)
      return false;
    const u = c && typeof c == "object" ? c : n().models.find((d) => String(d.UID) === String(c));
    return o(h2, u);
  },
  setTemplateDragData: (l, c, h2 = {}) => {
    const u = r(c) || c;
    return a(l, u, h2);
  },
  deployModel: async (l, c = {}, h2 = {}) => {
    const u = r(l);
    if (!u)
      throw new Error(`Unknown template: ${String(l)}`);
    const d = c.name || c.showName || u.name, f = s.createModelFromTemplate(u, {
      ...e(c),
      name: d,
      showName: c.showName || t(d)
    });
    return s.addModelToState(f, h2);
  },
  addModel: async (l, c = {}) => s.addModelToState(l, c),
  updateModel: async (l, c, h2 = {}) => s.updateModelInState(l, c, h2),
  batchUpdateModels: async (l = [], c = {}) => s.batchUpdateModelsInState(l, c),
  upsertModels: async (l = [], c = {}) => {
    const h2 = { models: Array.isArray(l) ? l : [l] };
    return (await i.applySituationFrame(h2, c)).models;
  },
  updateModelPosition: async (l, c = {}, h2 = {}) => {
    const u = {};
    return ["lon", "lat", "height", "heading", "speed"].forEach((d) => {
      c[d] !== void 0 && (u[d] = c[d]);
    }), i.updateModel(l, u, h2);
  }
});
var sc = (i, e, t = "application/json") => {
  const n = new Blob([i], { type: t }), s = document.createElement("a");
  s.href = URL.createObjectURL(n), s.download = e, s.click(), URL.revokeObjectURL(s.href);
};
var mS = ({
  exportMapScreenshot: i,
  getSceneState: e,
  mapApp: t,
  onEditorEvent: n,
  pausePlayback: s,
  sceneStatistics: r,
  seekPlayback: o,
  startPlayback: a,
  stopPlayback: l
}) => ({
  setPlaybackConfig: (c) => t.setPlaybackConfig(c || {}),
  startPlayback: () => a(),
  pausePlayback: () => s(),
  stopPlayback: () => l(),
  seekPlayback: (c) => o(c),
  exportSceneJson: (c = 2) => JSON.stringify(t.getSceneSnapshot(), null, c),
  downloadSceneJson: (c) => sc(
    JSON.stringify(t.getSceneSnapshot(), null, 2),
    c || `${e().metadata.name || "scenario"}.json`
  ),
  downloadReport: (c) => {
    const h2 = r.value, u = [
      `Scene: ${e().metadata.name}`,
      `Models: ${h2.modelCount}`,
      `Annotations: ${h2.annotationCount}`,
      `Issues: ${h2.issueCount}`,
      "",
      "Issue List:",
      ...e().validation.length ? e().validation.map((d) => `- [${d.level}] ${d.message}`) : ["- none"]
    ].join(`
`);
    sc(
      u,
      c || `${e().metadata.name || "scenario"}-report.txt`,
      "text/plain;charset=utf-8"
    );
  },
  exportMapScreenshot: (c) => i(c),
  onReady: (c) => n("ready", c),
  onSelectionChange: (c) => n("selectionChange", c),
  onRouteSaved: (c) => n("routeSaved", c),
  onMeasure: (c) => n("measure", c),
  onAnnotationCreated: (c) => n("annotationCreated", c),
  onTemplateDrop: (c) => n("templateDrop", c)
});
var yS = ({
  mapApp: i,
  syncSceneToMap: e,
  waitForMapReady: t
}) => ({
  startRouteEdit: async (n) => {
    var r;
    await t();
    const s = (r = i.getLayer()) == null ? void 0 : r.getFeatureById(n);
    return s ? (i.startRouteEdit(s), true) : false;
  },
  setModelRoute: async (n, s, r = {}, o = {}) => {
    const a = i.setModelRoute(n, s, r, {
      recordHistory: o.recordHistory !== false
    });
    return a ? (o.sync !== false && await e(), a) : null;
  },
  addMount: async (n, s, r = {}) => {
    const o = i.addMountToModel(n, s, {
      recordHistory: r.recordHistory !== false,
      force: r.force === true
    });
    return o != null && o.host ? (r.sync !== false && await e(), o) : null;
  },
  removeMount: async (n, s, r = {}) => {
    const o = i.removeMountFromModel(n, s, {
      recordHistory: r.recordHistory !== false
    });
    return o != null && o.host ? (r.sync !== false && await e(), o) : null;
  }
});
var _S = ({
  addLocalTileRegion: i,
  clearLocalTileRegions: e,
  clone: t,
  configureSceneRuntime: n,
  destroyMap: s,
  getLocalTileRegions: r,
  getResourceOptions: o,
  getSceneState: a,
  mapApp: l,
  removeLocalTileRegion: c,
  setDefaultEffectPresets: h2,
  setLocalTileRegions: u,
  setMountCompatibility: d,
  setMountValidator: f,
  setResourceOptions: g,
  setSceneTemplates: y,
  setSceneValidators: m,
  syncSceneToMap: _
}) => ({
  getResourceOptions: () => o(),
  setResourceOptions: async (p) => (g(p || {}), await _(), o()),
  getSceneState: () => l.getSceneState(),
  getSceneSnapshot: () => l.getSceneSnapshot(),
  getTemplates: () => l.getTemplates(),
  setTemplates: (p) => y(p),
  configureSceneRuntime: (p) => n(p),
  setMountCompatibility: (p) => d(p),
  setMountValidator: (p) => f(p),
  setSceneValidators: (p) => m(p),
  setDefaultEffectPresets: (p) => h2(p),
  getHistoryState: () => l.getHistoryState(),
  getStatistics: () => l.getStatistics(),
  getModelsBySide: (p) => t(l.getModelsBySide(p)),
  getLocalTileRegions: () => t(r()),
  getBatchSelection: () => {
    var p;
    return ((p = l == null ? void 0 : l.getBatchSelectedUIDs) == null ? void 0 : p.call(l)) || [];
  },
  isBatchTransformActive: () => {
    var p;
    return !!((p = l == null ? void 0 : l.isBatchTransformActive) != null && p.call(l));
  },
  setMetadata: async (p) => (l.setMetadata(p || {}), await _(), l.getSceneState().metadata),
  setMode: async (p) => {
    const E = await l.setModeState(p);
    return await _(), E;
  },
  setDisplayConfig: async (p) => {
    var R;
    const E = t(p || {}), S = E.targetLineStyle || {}, w = {
      ...a().display.targetLineStyle || {},
      ...S
    };
    S.color && (w.redColor = S.redColor || S.color, w.blueColor = S.blueColor || S.color);
    const x = {
      ...a().display.relationLineStyles || {}
    };
    Object.entries(E.relationLineStyles || {}).forEach(([P, U]) => {
      x[P] = {
        ...x[P] || {},
        ...U || {}
      };
    });
    const b = {
      ...a().display.effectStyles || {}
    };
    Object.entries(E.effectStyles || {}).forEach(([P, U]) => {
      var N;
      b[P] = {
        ...b[P] || {},
        ...U || {},
        qualityColors: {
          ...((N = b[P]) == null ? void 0 : N.qualityColors) || {},
          ...(U == null ? void 0 : U.qualityColors) || {}
        }
      };
    });
    const v = {
      ...a().display.performance || {},
      ...E.performance || {}
    };
    return await l.setDisplayConfig({
      ...E,
      relationLineStyles: x,
      targetLineStyle: w,
      effectStyles: b,
      performance: v
    }, { sync: false }), (R = l == null ? void 0 : l.applyPerformanceConfig) == null || R.call(l, v), await _(), l.getSceneState().display;
  },
  setPerformanceConfig: async (p) => {
    var S;
    const E = ((S = l == null ? void 0 : l.applyPerformanceConfig) == null ? void 0 : S.call(l, p || {})) || p || {};
    return l.setPerformanceConfig(E);
  },
  getPerformanceConfig: () => {
    var p;
    return ((p = l == null ? void 0 : l.getPerformanceConfig) == null ? void 0 : p.call(l)) || t(a().display.performance || {});
  },
  setContextMenuItems: (p = [], E = {}) => {
    var S;
    return (S = l == null ? void 0 : l.setContextMenuItems) == null || S.call(l, p, E), p;
  },
  setContextMenuHandler: (p) => {
    var E;
    return (E = l == null ? void 0 : l.setContextMenuHandler) == null || E.call(l, p), p;
  },
  loadScene: async (p, E = {}) => l.loadScene(p, E),
  resetScene: async () => l.resetScene(),
  destroy: () => s(),
  setLocalTileRegions: (p) => t(u(p)),
  addLocalTileRegion: (p) => t(i(p)),
  removeLocalTileRegion: (p) => c(p),
  clearLocalTileRegions: () => t(e()),
  validateScene: () => l.validateScene(),
  undo: async () => l.undoSceneChange() ? (await _(), true) : false,
  redo: async () => l.redoSceneChange() ? (await _(), true) : false
});
var rc = (i) => {
  if (!i.length)
    return { lon: 0, lat: 0 };
  const e = i.reduce(
    (t, n) => ({
      lon: t.lon + Number(n.lon),
      lat: t.lat + Number(n.lat)
    }),
    { lon: 0, lat: 0 }
  );
  return {
    lon: e.lon / i.length,
    lat: e.lat / i.length
  };
};
var pS = ({
  focusModel: i,
  mapApp: e,
  normalizeAngle: t,
  syncBatchSelectionOnMap: n
}) => {
  const s = (o) => {
    var c;
    const a = Array.isArray(o) && o.length ? o.map((h2) => String(h2)) : (((c = e == null ? void 0 : e.getBatchSelectedUIDs) == null ? void 0 : c.call(e)) || []).map((h2) => String(h2));
    if (!a.length)
      return [];
    const l = new Set(a);
    return e.getSceneState().models.filter((h2) => l.has(String(h2.UID)));
  }, r = (o, a = {}) => {
    o.length && a.sync !== false && n(o.map((l) => String(l.UID)));
  };
  return {
    removeModel: async (o, a = {}) => e.removeModelFromState(o, a),
    removeModels: async (o = [], a = {}) => e.removeModelsFromState(o, a),
    selectModel: (o) => (e.setSelectedModelUID(o), o),
    setSelectedAnnotation: (o) => (e.setSelectedAnnotationId(o), o || null),
    focusModel: (o) => i(o),
    setBatchSelection: (o) => {
      const a = Array.isArray(o) ? o.map((l) => String(l)) : [];
      return n(a), a;
    },
    clearBatchSelection: () => (n([]), []),
    batchMoveModels: async (o = [], a = {}, l = {}) => {
      const c = s(o);
      if (!c.length)
        return [];
      const h2 = Number(a.deltaLon ?? a.lon ?? 0) || 0, u = Number(a.deltaLat ?? a.lat ?? 0) || 0;
      if (!h2 && !u)
        return [];
      const d = await e.batchUpdateModelsInState(
        c.map((f) => ({
          uid: f.UID,
          patch: {
            lon: Number(f.lon) + h2,
            lat: Number(f.lat) + u
          }
        })),
        {
          recordHistory: l.recordHistory !== false,
          label: l.label || "Batch Move Models"
        }
      );
      return r(d, l), d;
    },
    batchSetModelHeading: async (o = [], a, l = {}) => {
      const c = s(o);
      if (!c.length)
        return [];
      const h2 = t(a), u = await e.batchUpdateModelsInState(
        c.map((d) => ({
          uid: d.UID,
          patch: { heading: h2 }
        })),
        {
          recordHistory: l.recordHistory !== false,
          label: l.label || "Batch Update Heading"
        }
      );
      return r(u, l), u;
    },
    batchLayoutModels: async (o = [], a = {}, l = {}) => {
      const c = s(o);
      if (!c.length)
        return [];
      const h2 = Math.max(0.01, Number(a.spacing) || 0.1), u = a.formation || "row", d = Math.max(2, Number(a.columns) || 2), f = a.center ? {
        lon: Number(a.center.lon),
        lat: Number(a.center.lat)
      } : rc(c), g = [];
      c.forEach((m, _) => {
        let p = 0, E = 0;
        if (u === "row")
          p = (_ - (c.length - 1) / 2) * h2;
        else if (u === "column")
          E = ((c.length - 1) / 2 - _) * h2;
        else if (u === "grid") {
          const S = Math.floor(_ / d), w = _ % d, x = Math.ceil(c.length / d);
          p = (w - (d - 1) / 2) * h2, E = ((x - 1) / 2 - S) * h2;
        } else if (u === "circle") {
          const S = Math.max(h2, c.length * h2 / (2 * Math.PI)), w = Math.PI * 2 * _ / c.length;
          p = Math.cos(w) * S, E = Math.sin(w) * S;
        }
        g.push({
          uid: m.UID,
          patch: {
            lon: f.lon + p,
            lat: f.lat + E
          }
        });
      });
      const y = await e.batchUpdateModelsInState(g, {
        recordHistory: l.recordHistory !== false,
        label: l.label || "Batch Layout Formation"
      });
      return r(y, l), y;
    },
    batchRotateModels: async (o = [], a = 0, l = {}) => {
      const c = s(o);
      if (!c.length)
        return [];
      const h2 = (Number(a) || 0) * Math.PI / 180;
      if (!h2)
        return [];
      const u = l.center ? {
        lon: Number(l.center.lon),
        lat: Number(l.center.lat)
      } : rc(c), d = await e.batchUpdateModelsInState(
        c.map((f) => {
          const g = Number(f.lon) - u.lon, y = Number(f.lat) - u.lat, m = g * Math.cos(h2) - y * Math.sin(h2), _ = g * Math.sin(h2) + y * Math.cos(h2);
          return {
            uid: f.UID,
            patch: {
              lon: u.lon + m,
              lat: u.lat + _,
              heading: t(Number(f.heading || 0) + Number(a || 0))
            }
          };
        }),
        {
          recordHistory: l.recordHistory !== false,
          label: l.label || "Batch Rotate Models"
        }
      );
      return r(d, l), d;
    }
  };
};
var Us = (i) => Array.isArray(i) ? i : [i];
var SS = ({
  applySituationFrameWithMerger: i,
  frameMergerConfig: e,
  mapApp: t,
  waitForMapReady: n
}) => ({
  applySituationFrame: (s = {}, r = {}) => i(s, r),
  upsertRelations: async (s = [], r = {}) => e.enabled === true && r.sync !== false && r.merge !== false ? (await i({ relations: Us(s) }, r)).relations : t.upsertRelationsToState(s, r),
  removeRelations: async (s = [], r = {}) => e.enabled === true && r.sync !== false && r.merge !== false ? (await i({ removedRelations: Us(s) }, r)).removedRelations : t.removeRelationsFromState(s, r),
  upsertEffects: async (s = [], r = {}) => e.enabled === true && r.sync !== false && r.merge !== false ? (await i({ effects: Us(s) }, r)).effects : t.upsertEffectsToState(s, r),
  removeEffects: async (s = [], r = {}) => e.enabled === true && r.sync !== false && r.merge !== false ? (await i({ removedEffects: Us(s) }, r)).removedEffects : t.removeEffectsFromState(s, r),
  playEventEffect: async (s, r = {}) => {
    var o;
    return await n(), ((o = t == null ? void 0 : t.playEventEffect) == null ? void 0 : o.call(t, s, r)) || false;
  }
});
var ES = ({
  cancelMapTool: i,
  endMeasureMode: e,
  mapApp: t,
  startAnnotationMode: n,
  startBatchMoveModeOnMap: s,
  startBatchRotateModeOnMap: r,
  startBoxSelectionModeOnMap: o,
  startMeasureMode: a,
  stopBatchTransformModeOnMap: l,
  stopBoxSelectionModeOnMap: c
}) => ({
  addAnnotation: async (h2, u = {}) => t.addAnnotationToState(h2, u),
  updateAnnotation: async (h2, u, d = {}) => t.updateAnnotationInState(h2, u, d),
  removeAnnotation: async (h2, u = {}) => t.removeAnnotationFromState(h2, u),
  startBoxSelectionMode: (h2) => o(h2),
  stopBoxSelectionMode: () => c(),
  startBatchRotateMode: (h2 = [], u = {}) => r(h2, {
    onCommit: async (d, f) => {
      var y;
      const g = await t.batchUpdateModelsInState(d, {
        recordHistory: u.recordHistory !== false,
        label: u.label || "Batch Rotate Models"
      });
      return await ((y = u.onCommit) == null ? void 0 : y.call(u, g, f)), g;
    },
    onCancel: u.onCancel
  }),
  startBatchMoveMode: (h2 = [], u = {}) => s(h2, {
    onCommit: async (d, f) => {
      var y;
      const g = await t.batchUpdateModelsInState(d, {
        recordHistory: u.recordHistory !== false,
        label: u.label || "Batch Move Models"
      });
      return await ((y = u.onCommit) == null ? void 0 : y.call(u, g, f)), g;
    },
    onCancel: u.onCancel
  }),
  stopBatchTransformMode: () => l(),
  startAnnotationMode: (h2, u = {}, d = null) => n(h2, u, d),
  startMeasureMode: (h2, u = null) => a(h2, u),
  endMeasure: () => e(),
  cancelTool: () => i()
});
var wS = (i = {}) => {
  var r, o, a;
  const e = (i == null ? void 0 : i.engine) || null, t = (i == null ? void 0 : i.context) || ((r = e == null ? void 0 : e.getContext) == null ? void 0 : r.call(e)) || null, n = (i == null ? void 0 : i.mapApp) || (t == null ? void 0 : t.mapApp) || ((o = e == null ? void 0 : e.getMapApp) == null ? void 0 : o.call(e)) || fS, s = (i == null ? void 0 : i.bridge) || (t == null ? void 0 : t.bridge) || ((a = e == null ? void 0 : e.getBridge) == null ? void 0 : a.call(e)) || (e ? Xa({ engine: e, mapApp: n }) : Xe);
  return {
    bridge: s,
    context: t,
    engine: e,
    mapApp: n,
    initMap: (l) => {
      var c;
      return ((c = i == null ? void 0 : i.initMap) == null ? void 0 : c.call(i, l)) ?? s.initMap(l);
    },
    destroyMap: () => {
      var l;
      return ((l = i == null ? void 0 : i.destroyMap) == null ? void 0 : l.call(i)) ?? s.destroyMap();
    },
    ready: () => {
      var l;
      return ((l = i == null ? void 0 : i.ready) == null ? void 0 : l.call(i)) ?? s.waitForMapReady();
    },
    syncSceneToMap: () => {
      var l;
      return ((l = i == null ? void 0 : i.syncSceneToMap) == null ? void 0 : l.call(i)) ?? s.syncSceneToMap();
    }
  };
};
var ri = (i) => JSON.parse(JSON.stringify(i));
var CS = (i) => (i == null ? void 0 : i.UID) ?? (i == null ? void 0 : i.uid) ?? (i == null ? void 0 : i.id);
var xS = (i) => (i == null ? void 0 : i.uid) ?? (i == null ? void 0 : i.UID) ?? (i == null ? void 0 : i.id);
var IS = (i) => (i == null ? void 0 : i.id) || `${(i == null ? void 0 : i.type) || "relation"}_${i == null ? void 0 : i.sourceUID}_${i == null ? void 0 : i.targetUID}`;
var vS = (i) => (i == null ? void 0 : i.id) || `${(i == null ? void 0 : i.type) || "effect"}_${i == null ? void 0 : i.sourceUID}`;
var MS = () => ({
  models: /* @__PURE__ */ new Map(),
  updates: /* @__PURE__ */ new Map(),
  removals: /* @__PURE__ */ new Set(),
  relations: /* @__PURE__ */ new Map(),
  removedRelations: /* @__PURE__ */ new Set(),
  effects: /* @__PURE__ */ new Map(),
  removedEffects: /* @__PURE__ */ new Set(),
  display: null,
  mode: null
});
var oc = (i, e, t, n) => {
  (Array.isArray(t) ? t : []).forEach((s) => {
    const r = n(s);
    r == null || r === "" || (i.set(String(r), ri(s)), e.delete(String(r)));
  });
};
var ho = (i, e, t) => {
  (Array.isArray(t) ? t : []).forEach((n) => {
    n == null || n === "" || (i.delete(String(n)), e.add(String(n)));
  });
};
var TS = (i) => ({
  models: Array.from(i.models.values()),
  updates: Array.from(i.updates.values()),
  removals: Array.from(i.removals.values()),
  relations: Array.from(i.relations.values()),
  removedRelations: Array.from(i.removedRelations.values()),
  effects: Array.from(i.effects.values()),
  removedEffects: Array.from(i.removedEffects.values()),
  ...i.display ? { display: i.display } : {},
  ...i.mode ? { mode: i.mode } : {}
});
var bS = ({ getSceneState: i, mapApp: e, syncSceneToMap: t }) => {
  const n = {
    enabled: false,
    interval: 100,
    maxDelay: 200
  };
  let s = null, r = {}, o = null, a = 0, l = [];
  const c = () => (s || (s = MS(), a = Date.now()), s), h2 = (m) => {
    const _ = c();
    (Array.isArray(m.models) ? m.models : []).forEach((p) => {
      const E = CS(p);
      if (E == null || E === "")
        return;
      const S = String(E);
      _.models.set(S, ri(p)), _.updates.delete(S), _.removals.delete(S);
    }), (Array.isArray(m.updates) ? m.updates : []).forEach((p) => {
      var x;
      const E = xS(p);
      if (E == null || E === "")
        return;
      const S = String(E), w = ri(p.patch || p);
      delete w.uid, delete w.UID, delete w.id, _.models.has(S) ? _.models.set(S, { ..._.models.get(S), ...w }) : _.updates.set(S, {
        uid: S,
        patch: {
          ...((x = _.updates.get(S)) == null ? void 0 : x.patch) || {},
          ...w
        }
      }), _.removals.delete(S);
    }), ho(_.models, _.removals, m.removals), (Array.isArray(m.removals) ? m.removals : []).forEach((p) => _.updates.delete(String(p))), oc(_.relations, _.removedRelations, m.relations, IS), ho(_.relations, _.removedRelations, m.removedRelations), oc(_.effects, _.removedEffects, m.effects, vS), ho(_.effects, _.removedEffects, m.removedEffects), m.display && (_.display = {
      ..._.display || {},
      ...ri(m.display)
    }), m.mode && (_.mode = m.mode);
  }, u = async (m = {}, _ = {}) => {
    const p = Array.isArray(m.models) ? m.models : [], E = Array.isArray(m.updates) ? m.updates : [], S = Array.isArray(m.removals) ? m.removals : [], w = Array.isArray(m.relations) ? m.relations : [], x = Array.isArray(m.removedRelations) ? m.removedRelations : [], b = Array.isArray(m.effects) ? m.effects : [], v = Array.isArray(m.removedEffects) ? m.removedEffects : [], R = [];
    p.length && R.push(...await e.upsertModelsToState(p, { recordHistory: false, sync: false })), E.length && R.push(...await e.batchUpdateModelsInState(E, {
      recordHistory: false,
      label: "Apply Situation Frame",
      sync: false
    }));
    let P = [];
    if (S.length) {
      const $ = new Set(S.map(String));
      P = i().models.filter((z) => $.has(String(z.UID))), await e.removeModelsFromState(S, { recordHistory: false, sync: false });
    }
    const U = w.length ? await e.upsertRelationsToState(w, { sync: false }) : [], N = x.length ? await e.removeRelationsFromState(x, { sync: false }) : [], O = b.length ? await e.upsertEffectsToState(b, { sync: false }) : [], G = v.length ? await e.removeEffectsFromState(v, { sync: false }) : [];
    return m.mode && await e.setModeState(m.mode), m.display && await e.setDisplayConfig(m.display, { sync: false }), _.sync !== false && (_.fullSync === true || m.display || m.mode ? await t() : await e.syncSituationChanges({
      models: R,
      removedModels: P,
      relations: U,
      removedRelations: N,
      effects: O,
      removedEffects: G
    })), { models: R, removedModels: P, relations: U, removedRelations: N, effects: O, removedEffects: G };
  }, d = async () => {
    if (o && (clearTimeout(o), o = null), !s)
      return null;
    const m = TS(s), _ = r, p = l;
    s = null, r = {}, l = [], a = 0;
    try {
      const E = await u(m, _);
      return p.forEach(({ resolve: S }) => S(E)), E;
    } catch (E) {
      throw p.forEach(({ reject: S }) => S(E)), E;
    }
  }, f = (m = {}, _ = {}) => new Promise((p, E) => {
    h2(m), r = {
      ...r,
      sync: _.sync,
      fullSync: r.fullSync === true || _.fullSync === true
    }, l.push({ resolve: p, reject: E });
    const S = Math.max(16, Number(n.interval) || 100), w = Math.max(S, Number(n.maxDelay) || S), x = Date.now() - a;
    if (x >= w) {
      d();
      return;
    }
    o || (o = setTimeout(() => {
      d();
    }, Math.min(S, w - x)));
  });
  return {
    applySituationFrameWithMerger: (m = {}, _ = {}) => _.sync === false || _.merge === false || _.bypassFrameMerger === true || n.enabled !== true ? u(m, _) : f(m, _),
    flushSituationFrameMerger: d,
    frameMergerConfig: n,
    getFrameMergerConfig: () => ri(n),
    setFrameMergerConfig: (m = {}) => (m.enabled !== void 0 && (n.enabled = !!m.enabled), m.interval !== void 0 && (n.interval = Math.max(16, Number(m.interval) || 100)), m.maxDelay !== void 0 && (n.maxDelay = Math.max(n.interval, Number(m.maxDelay) || n.interval)), n.enabled === false && d(), ri(n))
  };
};
var Uo = (i) => JSON.parse(JSON.stringify(i));
var RS = (i) => (e) => e ? typeof e == "object" ? Uo(e) : i.value.find((t) => t.NodeType === e) || i.value.find((t) => t.name === e) || null : null;
var LS = (i) => {
  const e = Number(i) % 360;
  return e >= 0 ? e : e + 360;
};
var ac = (i = {}) => {
  const e = wS(i), t = e.mapApp, n = (t == null ? void 0 : t.store) || {}, s = e.initMap, r = e.ready, o = e.syncSceneToMap, a = () => t.getSceneState(), l = bS({ getSceneState: a, mapApp: t, syncSceneToMap: o }), c = n.sceneStatistics, h2 = e.bridge, u = RS(n.modelList || Ah), d = (w) => {
    var x;
    return ((x = n.configureSceneRuntime) == null ? void 0 : x.call(n, w)) ?? Vh(w);
  }, f = (w) => {
    var x;
    return ((x = n.setSceneTemplates) == null ? void 0 : x.call(n, w)) ?? Nh(w);
  }, g = (w) => {
    var x;
    return ((x = n.setMountCompatibility) == null ? void 0 : x.call(n, w)) ?? Uh(w);
  }, y = (w) => {
    var x;
    return ((x = n.setMountValidator) == null ? void 0 : x.call(n, w)) ?? Gh(w);
  }, m = (w) => {
    var x;
    return ((x = n.setSceneValidators) == null ? void 0 : x.call(n, w)) ?? Bh(w);
  }, _ = (w) => {
    var x;
    return ((x = n.setDefaultEffectPresets) == null ? void 0 : x.call(n, w)) ?? zh(w);
  }, p = (w) => {
    var x;
    return ((x = n.generateSceneUniqueName) == null ? void 0 : x.call(n, w)) ?? Xh(w);
  }, E = (w, x) => {
    var b;
    return ((b = n.canMountTemplateOnModel) == null ? void 0 : b.call(n, w, x)) ?? Oh(w, x);
  }, S = {
    init: (w) => s(w),
    ready: () => r(),
    sync: () => o(),
    setFrameMergerConfig: (w) => l.setFrameMergerConfig(w),
    getFrameMergerConfig: () => l.getFrameMergerConfig(),
    flushFrameMerger: () => l.flushSituationFrameMerger(),
    getContext: () => e.context,
    getEngine: () => e.engine,
    getMapApp: () => t
  };
  return Object.assign(S, _S({
    addLocalTileRegion: h2.addLocalTileRegion,
    clearLocalTileRegions: h2.clearLocalTileRegions,
    clone: Uo,
    configureSceneRuntime: d,
    destroyMap: e.destroyMap,
    getLocalTileRegions: h2.getLocalTileRegions,
    getResourceOptions: Po,
    getSceneState: a,
    mapApp: t,
    removeLocalTileRegion: h2.removeLocalTileRegion,
    setDefaultEffectPresets: _,
    setLocalTileRegions: h2.setLocalTileRegions,
    setMountCompatibility: g,
    setMountValidator: y,
    setResourceOptions: Lh,
    setSceneTemplates: f,
    setSceneValidators: m,
    syncSceneToMap: o
  })), Object.assign(S, gS({
    api: S,
    clone: Uo,
    generateSceneUniqueName: p,
    getSceneState: a,
    mapApp: t,
    resolveTemplate: u,
    canMountTemplateOnModel: E,
    setTemplateDragDataOnMap: h2.setTemplateDragData
  })), Object.assign(S, SS({
    applySituationFrameWithMerger: l.applySituationFrameWithMerger,
    frameMergerConfig: l.frameMergerConfig,
    mapApp: t,
    waitForMapReady: r
  })), Object.assign(S, yS({
    mapApp: t,
    syncSceneToMap: o,
    waitForMapReady: r
  })), Object.assign(S, pS({
    focusModel: h2.focusModel,
    mapApp: t,
    normalizeAngle: LS,
    syncBatchSelectionOnMap: h2.syncBatchSelectionOnMap
  })), Object.assign(S, ES({
    cancelMapTool: h2.cancelMapTool,
    endMeasureMode: h2.endMeasureMode,
    mapApp: t,
    startAnnotationMode: h2.startAnnotationMode,
    startBatchMoveModeOnMap: h2.startBatchMoveMode,
    startBatchRotateModeOnMap: h2.startBatchRotateMode,
    startBoxSelectionModeOnMap: h2.startBoxSelectionMode,
    startMeasureMode: h2.startMeasureMode,
    stopBatchTransformModeOnMap: h2.stopBatchTransformMode,
    stopBoxSelectionModeOnMap: h2.stopBoxSelectionMode
  })), Object.assign(S, mS({
    exportMapScreenshot: h2.exportMapScreenshot,
    getSceneState: a,
    mapApp: t,
    onEditorEvent: h2.onEditorEvent,
    pausePlayback: h2.pausePlayback,
    sceneStatistics: c,
    seekPlayback: h2.seekPlayback,
    startPlayback: h2.startPlayback,
    stopPlayback: h2.stopPlayback
  })), S;
};
var uo = null;
var gu = (i = {}) => i != null && i.engine || i != null && i.context || i != null && i.mapApp ? ac(i) : (uo || (uo = ac()), uo);
var mu = Symbol("scenario-editor");
var FS = (i) => (provide(mu, i), i);
var xE = () => inject(mu, null) || gu();
var DS = (i, e) => {
  const t = i.__vccOpts || i;
  for (const [n, s] of e)
    t[n] = s;
  return t;
};
var PS = {
  __name: "ScenarioEditor",
  props: {
    showWorkbench: {
      type: Boolean,
      default: false
    },
    assetBaseUrl: {
      type: String,
      default: ""
    },
    tileUrlTemplate: {
      type: String,
      default: ""
    },
    mapOptions: {
      type: Object,
      default: () => ({})
    },
    resourceOptions: {
      type: Object,
      default: () => ({})
    }
  },
  setup(i) {
    const e = defineAsyncComponent(() => import("./DemoWorkbench-6a8e00ef-JISI7ZE2.js")), t = i, n = ref(null), s = fu(), r = Xa({
      engine: s,
      mapApp: s.getMapApp()
    }), o = gu({
      engine: s,
      bridge: r,
      context: s.getContext()
    });
    FS(o);
    const a = () => {
      const l = {
        ...t.resourceOptions || {},
        ...t.mapOptions || {},
        target: n.value
      };
      return t.assetBaseUrl && (l.assetBaseUrl = t.assetBaseUrl), t.tileUrlTemplate && (l.tileUrlTemplate = t.tileUrlTemplate), l;
    };
    return onMounted(() => {
      o.init(a());
    }), onUnmounted(() => {
      o.destroy();
    }), (l, c) => (openBlock(), createElementBlock(Fragment, null, [
      createBaseVNode("div", {
        ref_key: "mapRef",
        ref: n,
        class: "scenario-map"
      }, null, 512),
      i.showWorkbench ? (openBlock(), createBlock(unref(e), { key: 0 })) : createCommentVNode("", true)
    ], 64));
  }
};
var IE = DS(PS, [["__scopeId", "data-v-c8eeb6d3"]]);
var Mt = (i, e) => i("update:modelValue", e);
var AS = (i) => i.width ? { width: typeof i.width == "number" ? `${i.width}px` : i.width } : {};
var yu = Symbol("nativeRadioGroup");
var kS = {
  name: "ElButton",
  props: ["type", "plain", "disabled", "size", "circle"],
  emits: ["click"],
  setup(i, { slots: e, emit: t }) {
    return () => {
      var n;
      return h("button", {
        class: ["native-btn", i.type && `native-btn--${i.type}`, i.plain && "is-plain"],
        disabled: i.disabled,
        onClick: (s) => t("click", s)
      }, (n = e.default) == null ? void 0 : n.call(e));
    };
  }
};
var OS = {
  name: "ElInput",
  props: ["modelValue", "placeholder", "clearable", "type", "disabled", "rows"],
  emits: ["update:modelValue", "keyup"],
  setup(i, { slots: e, emit: t }) {
    const n = () => i.type === "textarea";
    return () => h("label", { class: "native-input-wrap" }, [
      e.prefix ? h("span", { class: "native-input-prefix" }, e.prefix()) : null,
      h(n() ? "textarea" : "input", {
        class: "native-input",
        type: n() ? void 0 : i.type || "text",
        value: i.modelValue ?? "",
        placeholder: i.placeholder,
        disabled: i.disabled,
        rows: i.rows,
        onInput: (s) => Mt(t, s.target.value),
        onKeyup: (s) => t("keyup", s)
      })
    ]);
  }
};
var NS = {
  name: "ElInputNumber",
  props: ["modelValue", "min", "max", "step", "precision", "disabled"],
  emits: ["update:modelValue", "change"],
  setup(i, { emit: e }) {
    return () => h("input", {
      class: "native-input native-input-number",
      type: "number",
      value: i.modelValue,
      min: i.min,
      max: i.max,
      step: i.step ?? 1,
      disabled: i.disabled,
      onInput: (t) => {
        const n = t.target.value === "" ? void 0 : Number(t.target.value);
        Mt(e, n), e("change", n);
      }
    });
  }
};
var _u = {
  name: "ElSwitch",
  props: ["modelValue", "disabled"],
  emits: ["update:modelValue", "change"],
  setup(i, { emit: e }) {
    return () => h("input", {
      class: "native-switch",
      type: "checkbox",
      checked: !!i.modelValue,
      disabled: i.disabled,
      onChange: (t) => {
        Mt(e, t.target.checked), e("change", t.target.checked);
      }
    });
  }
};
var US = _u;
var GS = {
  name: "ElSelect",
  props: ["modelValue", "placeholder", "disabled", "clearable", "filterable"],
  emits: ["update:modelValue", "change"],
  setup(i, { slots: e, emit: t }) {
    return () => {
      var n;
      return h("select", {
        class: "native-select",
        value: i.modelValue ?? "",
        disabled: i.disabled,
        onChange: (s) => {
          const r = s.target.value;
          Mt(t, r), t("change", r);
        }
      }, [i.placeholder ? h("option", { value: "" }, i.placeholder) : null, (n = e.default) == null ? void 0 : n.call(e)]);
    };
  }
};
var BS = {
  name: "ElOption",
  props: ["label", "value", "disabled"],
  setup(i) {
    return () => h("option", { value: i.value, disabled: i.disabled }, i.label ?? i.value);
  }
};
var zS = {
  name: "ElColorPicker",
  props: ["modelValue", "disabled"],
  emits: ["update:modelValue", "change"],
  setup(i, { emit: e }) {
    const t = (n) => /^#([0-9a-f]{3}|[0-9a-f]{6})$/i.test(n || "") ? n : "#ffffff";
    return () => h("input", {
      class: "native-color",
      type: "color",
      value: t(i.modelValue),
      disabled: i.disabled,
      onInput: (n) => {
        Mt(e, n.target.value), e("change", n.target.value);
      }
    });
  }
};
var VS = {
  name: "ElSlider",
  props: ["modelValue", "min", "max", "step", "disabled"],
  emits: ["update:modelValue", "input", "change"],
  setup(i, { emit: e }) {
    return () => h("input", {
      class: "native-slider",
      type: "range",
      value: i.modelValue,
      min: i.min ?? 0,
      max: i.max ?? 100,
      step: i.step ?? 1,
      disabled: i.disabled,
      onInput: (t) => {
        const n = Number(t.target.value);
        Mt(e, n), e("input", n);
      },
      onChange: (t) => e("change", Number(t.target.value))
    });
  }
};
var XS = {
  name: "ElSegmented",
  props: ["modelValue", "options"],
  emits: ["update:modelValue"],
  setup(i, { emit: e }) {
    return () => h("div", { class: "native-segmented" }, (i.options || []).map((t) => {
      const n = typeof t == "object" ? t : { label: t, value: t };
      return h("button", {
        class: ["native-segmented-item", i.modelValue === n.value && "active"],
        type: "button",
        onClick: () => Mt(e, n.value)
      }, n.label);
    }));
  }
};
var WS = {
  name: "ElTabs",
  props: ["modelValue"],
  emits: ["update:modelValue"],
  setup(i, { slots: e, emit: t }) {
    const n = ref(i.modelValue);
    return () => {
      var o, a, l;
      const s = (((o = e.default) == null ? void 0 : o.call(e)) || []).filter((c) => {
        var h2, u;
        return ((h2 = c.type) == null ? void 0 : h2.name) === "ElTabPane" || ((u = c.type) == null ? void 0 : u.name) === "NativeTabPane";
      }), r = i.modelValue || n.value || ((l = (a = s[0]) == null ? void 0 : a.props) == null ? void 0 : l.name);
      return h("div", { class: "native-tabs" }, [
        h("div", { class: "native-tabs-nav" }, s.map((c) => {
          var h2, u, d;
          return h("button", {
            class: ["native-tabs-tab", r === ((h2 = c.props) == null ? void 0 : h2.name) && "active"],
            type: "button",
            onClick: () => {
              var f, g;
              n.value = (f = c.props) == null ? void 0 : f.name, Mt(t, (g = c.props) == null ? void 0 : g.name);
            }
          }, ((u = c.props) == null ? void 0 : u.label) || ((d = c.props) == null ? void 0 : d.name));
        })),
        h("div", { class: "native-tabs-body" }, s.filter((c) => {
          var h2;
          return ((h2 = c.props) == null ? void 0 : h2.name) === r;
        }).map((c) => {
          var h2, u;
          return (u = (h2 = c.children) == null ? void 0 : h2.default) == null ? void 0 : u.call(h2);
        }))
      ]);
    };
  }
};
var YS = { name: "ElTabPane", props: ["label", "name"], setup(i, { slots: e }) {
  return () => {
    var t;
    return (t = e.default) == null ? void 0 : t.call(e);
  };
} };
var jS = { name: "ElDialog", props: ["modelValue", "title", "width"], emits: ["update:modelValue"], setup(i, { slots: e, emit: t }) {
  return () => {
    var n;
    return withDirectives(h("div", { class: "native-dialog-mask" }, [h("div", { class: "native-dialog", style: AS(i) }, [h("div", { class: "native-dialog-header" }, [h("strong", i.title), h("button", { type: "button", onClick: () => Mt(t, false) }, "×")]), h("div", { class: "native-dialog-body" }, (n = e.default) == null ? void 0 : n.call(e)), e.footer ? h("div", { class: "native-dialog-footer" }, e.footer()) : null])]), [[vShow, i.modelValue]]);
  };
} };
var $S = { name: "ElForm", setup(i, { slots: e }) {
  return () => {
    var t;
    return h("div", { class: "native-form" }, (t = e.default) == null ? void 0 : t.call(e));
  };
} };
var KS = { name: "ElFormItem", props: ["label"], setup(i, { slots: e }) {
  return () => {
    var t;
    return h("label", { class: "native-form-item" }, [i.label ? h("span", { class: "native-form-label" }, i.label) : null, (t = e.default) == null ? void 0 : t.call(e)]);
  };
} };
var HS = { name: "ElRow", setup(i, { slots: e }) {
  return () => {
    var t;
    return h("div", { class: "native-row" }, (t = e.default) == null ? void 0 : t.call(e));
  };
} };
var ZS = { name: "ElCol", setup(i, { slots: e }) {
  return () => {
    var t;
    return h("div", { class: "native-col" }, (t = e.default) == null ? void 0 : t.call(e));
  };
} };
var qS = {
  name: "ElRadioGroup",
  props: ["modelValue"],
  emits: ["update:modelValue", "change"],
  setup(i, { slots: e, emit: t }) {
    return provide(yu, {
      value: computed(() => i.modelValue),
      update: (n) => {
        Mt(t, n), t("change", n);
      }
    }), () => {
      var n;
      return h("div", { class: "native-radio-group" }, (n = e.default) == null ? void 0 : n.call(e));
    };
  }
};
var JS = {
  name: "ElRadioButton",
  props: ["label", "modelValue"],
  emits: ["update:modelValue", "change"],
  setup(i, { slots: e, emit: t }) {
    const n = inject(yu, null), s = computed(() => n ? n.value.value : i.modelValue), r = (o) => {
      n && n.update(o), Mt(t, o), t("change", o);
    };
    return () => {
      var o;
      return h("label", {
        class: ["native-radio-button", s.value === i.label && "active"]
      }, [
        h("input", {
          type: "radio",
          value: i.label,
          checked: s.value === i.label,
          onChange: () => r(i.label)
        }),
        h("span", ((o = e.default) == null ? void 0 : o.call(e)) || i.label)
      ]);
    };
  }
};
var QS = { name: "ElTag", setup(i, { slots: e }) {
  return () => {
    var t;
    return h("span", { class: "native-tag" }, (t = e.default) == null ? void 0 : t.call(e));
  };
} };
var eE = { name: "ElEmpty", props: ["description"], setup(i) {
  return () => h("div", { class: "native-empty" }, i.description || "暂无数据");
} };
var tE = { name: "ElIcon", setup(i, { slots: e }) {
  return () => {
    var t;
    return h("span", { class: "native-icon" }, (t = e.default) == null ? void 0 : t.call(e));
  };
} };
var nE = { name: "Search", setup() {
  return () => h("span", "⌕");
} };
var iE = {
  ElButton: kS,
  ElInput: OS,
  ElInputNumber: NS,
  ElSwitch: _u,
  ElCheckbox: US,
  ElSelect: GS,
  ElOption: BS,
  ElColorPicker: zS,
  ElSlider: VS,
  ElSegmented: XS,
  ElTabs: WS,
  ElTabPane: YS,
  ElDialog: jS,
  ElForm: $S,
  ElFormItem: KS,
  ElRow: HS,
  ElCol: ZS,
  ElRadioGroup: qS,
  ElRadioButton: JS,
  ElTag: QS,
  ElEmpty: eE,
  ElIcon: tE,
  Search: nE
};
var vE = {
  install(i) {
    Object.entries(iE).forEach(([e, t]) => i.component(e, t));
  }
};

export {
  Du,
  Lh,
  Po,
  aE,
  Ah,
  Dy,
  Bh,
  Vh,
  ui,
  lE,
  fu,
  Xa,
  fS,
  hE,
  uE,
  dE,
  fE,
  gE,
  mE,
  yE,
  _E,
  pE,
  SE,
  EE,
  wE,
  CE,
  gu,
  xE,
  DS,
  IE,
  iE,
  vE
};
//# sourceMappingURL=chunk-J2SAADDG.js.map
